대행: Codex (Claude 한도 초과)
작성: 2026-09-20

# 구조 사이클 4 — 작업

작업 사본은 `../codex_resume/candidate/`다. 운영 폴더·원본 데이터에는 반영하지 않았다.

- app/README.md를 8절로 재작성: 시작, 구조, 파일 형식, 37 API, 108 규칙, 시험, 작은 수정, 한계. 맨 앞과 help.html에 마스킹 지우는 법을 추가했다.
- 공개 함수 113개 타입과 docstring을 보완했다. 기존에 빠진 설명은 22개였다. 16개 모듈의 실행 AST를 주석·타입·typing import 제외 후 대조했다. 복합 반환 Any 48개는 남는다.
- 별도 첫 1시간 문서: `kds0206/문서/260920_툴_인수인계.md`.
- 데이터 소실 예외: 상자 목록은 비어 있지 않지만 모든 상자가 무효인 요청을 400으로 거절한다. 이전에는 명시적 빈 목록과 혼동해 기존 상자와 확정을 지웠다. 별도 사본 재현 후 후보에 반영했다. 유효 저장·혼합 입력·명시적 빈 목록 계약은 유지한다. 3가지 무효 요청에서 기존 상자/판정 바이트 보존을 시험했다.
- scripts/ff.py는 LABELTOOL_URL이 없으면 서버에 접근하기 전에 중단한다. 기존 실서버 기본 주소 위험을 닫았다. 테스트 서버 금지 포트에 5001도 추가했다.
- 원래 기준선은 덮지 않았다. 추가 structure_transition_260920.json은 바뀐 정적 응답 세 개의 이전/새 SHA-256을 각각 고정하고 나머지 API/내보내기/라우트는 원래 기준선으로 검사한다. 무조건 무시가 아니며 7가지 변조 거절을 검사했다. 원래 --check-baseline은 여전히 정적6칸 실패한다.

자기 시험: `evidence/loss_guard_gate_stable.log` 480/0, rc0. 전체 회귀는 구조5가 별도로 수행한다. 앞선 loss_guard_gate.log의 shell 구문 오류는 실행 중 시험목록을 수정한 작업 절차 문제였고, 스크립트를 고정한 안정 실행에서 해소했다. 해당 실패 로그도 보존한다.

새 에이전트가 README만 받고 시작했으며, 첫 API 위치 찾기 7초와 서버+첫 전체시험 8분33초를 확인했다. 연습 수정 뒤 두 번째 전체시험 결과는 2차 검수에 기록한다. 연습 수정은 후보에 가져오지 않는다.
