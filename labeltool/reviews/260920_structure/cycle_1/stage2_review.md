작성: 2026-09-19

# 툴 구조 정리 5사이클 — **사이클 1 2차 검수(공격적)** 보고서

검수자: Opus 5 · 시각은 전부 서버 `date` 실측
시작 **2026-09-19 23:24:17** · 끝 **2026-09-20 00:33** (자정을 넘겼다 — 아래 시각 중 `00:` 은 **09-20**)
대상: 1차 `cycles/260920_structure/cycle_1/stage1_work.md` (498줄) · `T/tests/` · `T/_archive/` 둘
과제: «기준선이 정말 전부를 덮는가» 를 깨뜨리기
⛔ 지킨 것: `app/*`·`export/*`·`scripts/ff.py` **수정 0** · 실서버 5111 **접속 0**(GET 도 안 했다 — `ps`·`ss` 로만 봤다) ·
공용 `T/data`·데이터셋·팀원 폴더 **쓰기 0** · 삭제 0 · GPU 0 · 새 패키지 0 · `pkill` 0 · `작업기록.md` 미갱신

«`app/*` 수정 0» 을 어떻게 아나: `app/server.py`·`instances.py`·`dupes.py`·`maskio.py`·`static/index.html`·
`static/style.css`·`export/*.py`·`scripts/ff.py` 가 **21:58 스냅샷과 md5 가 같다**(실측).
검수 중에 mtime 이 움직인 `app/static/ui.js`(23:36)·`help.html`(23:51)·`app/README.md`·`app/logs/*` 는
**「개수 세기」 사이클5 와 실서버가 만든 것**이다(그 사이클의 `cnt5b` 백업이 같은 시각에 함께 생겼다).
내가 손댄 파일은 **여섯 개뿐**이고 전부 `tests/` 안이다:
`tests/{baseline/snapshot.py, run_all.sh, sim/modesim.js, sim/sim.js, unit/u5_js_contract.py, README.md}`.
그 여섯 개의 원본은 `tests/_backup_260919_st1b/` 에 그대로 있다
(`_backup_260919_st1b_paths.js` 도 거기 있는데 **`tests/lib/paths.js` 는 결국 고치지 않았다** —
구조 3 이 고칠 자리라 §2-3 에 «어떻게 고치나» 만 적어 두었다).
⚠ 백업을 `sim/`·`unit/` 안에 두면 `run_all.sh` 의 글롭이 **그것을 시험으로 돌린다**(§8-2). 그래서 딴 폴더에 뒀다.
내가 띄운 모래상자 서버(5601·5611·5612·5701)는 전부 꺼졌다(`ss -ltn` 에 5401~5799 **없음** · 00:33 실측).

## 0. 한 장 요약 — 판정

| | 1차 주장 | 2차 판정 |
|---|---|---|
| «기준선이 전부를 덮는다» | ①②③④ 243항목 | **깨졌다.** ① 은 **GET 7개**만 지나고, 라우트 **37개 중 19개는 어느 시험도 부르지 않았다**. |
| «동작 무변경을 잡는다» | ①② 가 다르면 실패 | **깨졌다(증명함).** 21:58→23:19 사이 `boxes.py` 가 **실제로 동작을 바꿨는데**(`n_images`·`stale_txt`) 1차 기준선은 «다른 칸 0개» 라고 했다. |
| «두 번 돌려도 같다» | ①②④ 0개 (3회) | **맞다.** 내가 23:28·23:30 에 다시 확인 — ①② 0개. |
| «`--check-baseline` 로 관문을 삼는다» | rc 0 이어야 한다 | **깨졌다.** `snapshot.py --check` 단독·`--only=`·`--no-merged` 는 **거짓 실패**를 낸다(실측 23:28 rc=1). |
| «45개가 죽는다» | 23:00 실측 | **35개**로 줄었다 — 2개는 다른 세션이 스스로 폴백을 넣었고(22:00~22:02), 나머지는 내 측정 기준이 더 좁다. 그리고 «lib 하나만 고치면 산다» 는 **절반만 맞다**(비-lib 스크립트 20개). |
| «되돌릴 길이 있다» | 스냅샷 1,592개 | **맞다. 리허설로 증명.** 스냅샷을 모래상자에 풀어 서버를 띄웠고 옛 기준선 ① 50항목을 **다른 칸 0개**로 재현했다. |

**메운 것**: 기준선에 **⑤ 라우트·쓰기 경로** 를 새로 넣어 라우트 **37/37** 을 덮게 했다.
합격 기준 항목 수 **213 → 352**(+139) · 시험 단정 수 **890 → 941**(+51) · 라우트 덮개 **11/37 → 37/37**.
그리고 «지운 칸» 을 좁혀 사과 `stem` 18칸·`note` 8칸·`by` 60칸을 되살렸고(§3),
`--check-baseline` 의 **거짓 실패**를 없앴다(§4-1). 디스크는 **12G → 3.7M**(§8-1).

**확정한 기준선(구조 2·3 이 견줄 것)**: 「개수 세기」 사이클5 가 코드를 굳히고 실서버를
**2026-09-20 00:04:44** 에 재시작한 뒤(PID 1467097 · 프로세스 = 디스크), 그 판으로 다시 떴다 —
`--rebaseline` **00:24:11** → `--check-baseline` **00:31:47**: **①②③④⑤ 전부 «다른 칸 0개»**
(④ 까지 0칸 = 기준선의 코드 지문이 지금 디스크와 한 글자도 다르지 않다). 옛 판 4개는
`tests/fixtures/_baseline_prev/` 에 보존. 자세한 것은 §7-0.

## 1. 덮이지 않는 경로 — 전수 표 (§공격 1)

### 1-1. 🔴 먼저, 이 구멍이 **이미 사고를 냈다** (실측 증명)

1차 기준선은 **2026-09-19 22:26~23:11** 에 떴다. 그 뒤 23:19 에 「개수 세기」 사이클5 가
`app/boxes.py` 의 `export_boxes_to()` 를 고쳤다. 나는 `_archive/pre_refactor_260920/app/boxes.py`(21:58 판)와
지금 판을 **둘 다 파이썬 모듈로 불러** 같은 입력에 대고 돌려 봤다(`app/` 은 읽기만):

| 입력 | 옛 판(21:58 = 기준선이 뜬 판) | 지금 판(23:19) |
|---|---|---|
| 상자 1개 json + **상자 0개 json** + 이미 있는 0바이트 txt | `n_images=2` · `stale_txt=0` · `stale_txt_names=[]` | `n_images=1` · `stale_txt=1` · `stale_txt_names=["b_빈것.txt"]` |

**달라진 칸 3개.** 그런데 1차 기준선으로 대조한 결과(23:28·23:30 실측)는 **①② 다른 칸 0개**였다.
④ 만 «app/boxes.py 지문이 다르다» 고 경고했고 그것은 «리팩터니까 당연» 으로 넘기게 되어 있다.

왜 놓쳤나 — 두 겹이다.
1. 이 함수를 부르는 라우트 `/api/boxes_export`(`/box` 화면 단추)가 ①·②·시험 묶음 **어디에도 없었다.**
2. `/api/export_start`(②)는 같은 함수를 부르지만, 표본 12장에 **상자를 2개씩 넣어 두었기 때문에**
   «줄이 0개인 json» 갈래에 **한 번도 들어가지 않았다.** 함수는 덮었는데 **갈래는 안 덮었다.**

→ «② 내보내기 바이트 동일» 만으로는 부족하다. 이 변경은 나간 **txt 파일이 바이트까지 같았다**
(이미 있던 0바이트 txt 를 두 판 다 지우지 않는다). 달라진 것은 **응답 숫자**뿐이다.
그래서 ⑤ 는 파일 지문이 아니라 **응답 칸**까지 굳힌다.

### 1-2. 라우트 전수 ↔ 무엇이 덮나

라우트는 `@app.route` **37개**(`server.py` 26 · `boxes.py` 5 · `instances.py` 6. `/login` 은 GET·POST 둘).
1차 기준선 ①은 GET **7개**만 지난다: `/api/fruits`·`/api/export_list`·`/api/list`·`/api/export_plan`·
`/api/item`·`/api/boxes`(GET)·`/api/instance_info`. ② 가 쓰기 4개를 더 지난다
(`POST /api/boxes`·`POST /api/status`·`POST /api/export_start`·`GET /api/export_status`) → **기준선 11개**.

**어느 시험도 직접 부르지 않던 19개** (= 2차가 찾아낸 구멍 · 심각도는 «구조 2·3 이 그 코드를 지나가나» 로 매겼다):

| # | 라우트 | 어느 파일 | 구조 2·3 이 건드리나 | 심각도 |
|---:|---|---|---|---|
| 1 | `POST /api/boxes_export` | boxes.py | **예** — `api/boxes.py` 로 옮긴다 | **높음**(§1-1 이 실제로 깨졌다) |
| 2 | `POST /api/save_instances` | instances.py | **예** — `api/instances.py` · N6 쓰기 규칙이 여기 있다 | **높음** |
| 3 | `POST /api/component` | server.py | **예** — `api/masks.py` (붓·4-연결) | **높음** |
| 4 | `GET /api/duplicate_preview` | server.py | **예** — `api/dupes.py` | **높음** |
| 5 | `POST /api/apply_duplicate_exclusions` | server.py | **예** — `api/dupes.py` | **높음** |
| 6 | `POST /api/undo_duplicate_exclusions` | server.py | **예** — `api/dupes.py` | **높음** |
| 7 | `POST /api/exclude_group` | server.py | **예** — `api/dupes.py` | **높음** |
| 8 | `POST /api/undo_exclude_group` | server.py | **예** — `api/dupes.py` | **높음** |
| 9 | `POST /api/export_excluded` | server.py | **예** — `api/export.py` | 중간 |
| 10 | `GET /api/stats` | server.py | **예** — `api/dashboard.py` | 중간 |
| 11 | `GET /api/boxes_stats` | boxes.py | 예 | 중간 |
| 12 | `GET /api/instance_stats` | instances.py | 예 | 중간 |
| 13 | `GET /api/instance_errors` | instances.py | 예 | 중간 |
| 14 | `GET /api/health` | server.py | 예 | 낮음 |
| 15 | `GET /img` | server.py | **예** — `api/photos.py` | 중간 |
| 16 | `GET /mask` | server.py | **예** — `api/masks.py` | 중간 |
| 17 | `GET /thumb` | server.py | **예** — `api/photos.py`(썸네일 캐시) | 중간 |
| 18 | `GET /` (목록 화면) | server.py | **예** — 구조 3 이 `index.html`·`js/*` 를 쪼갠다 | 중간 |
| 19 | `GET /static/<path:fn>` | server.py | **예** — 구조 3 이 `static/js/` 를 새로 만든다 | 중간 |

특히 **4~8 다섯 개(근접 중복 묶음 전체)** 는 얼린 판정 자료의 `duplicates.json` **네 과일 모두 `{"groups": []}`**(실측)이라
«묶음이 없다» 갈래만 지날 수 있었다. 지시서 §2 가 `api/dupes.py` 를 **이름으로 적어 둔** 모듈인데
기준선이 0% 였다.

### 1-3. 메운 방법 — 기준선 ⑤ (`fixtures/baseline_260920/routes.json`)

`tests/baseline/snapshot.py` 에 `collect_routes()` 를 넣었다. **①② 를 다 뜬 뒤에** 부르므로
**기존 ①② 칸은 한 칸도 흔들리지 않는다**(실측: ①100·②99 값이 그대로).

| 묶음 | 무엇 | 항목 |
|---|---|---:|
| 읽기 GET | health·stats·boxes_stats·instance_stats(4) · instance_errors(4) · duplicate_preview(4+묶음있음 1) · 큐 4과일×3갈래(12) · boxes_seed(4) · 없는 job·없는 사진·없는 과일(3) | 32 |
| 그림·화면 바이트 | `/img`·`/mask`·`/thumb`·`/instances` ×4과일 + `/`·`/box`·`/static/app.js` → (http · 바이트 수 · sha256) | 19 |
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
| 쓰기 — 마스크 | `component`(열매 안쪽·사진 밖) · `save action=fixed` · `save action=ok` · `revert` ×4과일 | 20 |
| 쓰기 — 번호 | `save_instances`(지금 번호본을 **그대로 되보내는 왕복**) · `revert_instances` ×4과일 | 8 |
| 쓰기 — 근접 중복 | 표본 2장으로 **묶음 하나를 모래상자에만** 만들고: exclude_group · undo_exclude_group · apply · undo · export_excluded | 5 |
| 쓰기 — 상자 내보내기 | `boxes_export` ×4과일 + **«상자 0개 json + 0바이트 옛 txt» 갈래** 한 번 | 5 |
| 쓰기 뒤 디스크 | 과일마다 `status.json`·`boxes/*.json`·`boxes_yolo/*.txt`(바이트 수+지문)·`boxes_all.json`·`masks_fixed`·`instances_fixed` (6×4) + `excluded_list.txt` | 25 |
| **합계** | | **118** |

§1-1 의 변경이 지금은 이렇게 잡힌다(실제 기준선 값):
`w_boxes_export__peach` → `empty_txt=1 · empty_txt_names=["210629-t4-17"] · n_images=3 · stale_txt=1`
`w_boxes_export__빈상자갈래` → `empty_txt=2 · n_images=3 · stale_txt=2`
(`210629-t4-17` 은 공용 자료에 **실제로 있는 시험 잔재**다 — 지어낸 것이 아니다.)
\+ 쓰기가 끝난 뒤 `/api/export_start` 를 한 번 더 불러 ② 에 `peach__after_writes` 를 더했다(②  99→119).

**라우트 덮개: 11/37 → 37/37.** 남은 것은 `/login` 의 **429(1분에 10번 틀림)** 한 갈래다 —
일부러 넣지 않았다(127.0.0.1 의 잠금 창을 오염시켜 뒤따르는 실행이 흔들린다). §10 으로 넘긴다.

## 2. JS 쪽 기준선 (§공격 2)

### 2-1. ④ 는 구조 3 뒤에 **당연히 다르다** — 그럼 무엇이 동작을 보나

| 무엇 | 지금 | 구조 3 이 `app.js`(2,664줄) → `static/js/*` 10파일로 쪼개면 |
|---|---|---|
| ④ 정적 파일 sha256 30개 | 경고만 | **전부 달라진다 + `app.js` 칸이 사라진다.** 아무 뜻이 없다. |
| `sim/boxsim.js` 50항목 | `app.js` 에서 **글자를 떼어 내** 돌린다 | **통째로 죽는다** — `throw new Error("app.js 에서 구간을 못 찾음")` |
| `sim/modesim.js` 31항목 | 같은 방식 | **통째로 죽는다** |
| `sim/sim.js` | (2차 전) **단정 0개** | 원래부터 아무것도 못 봤다 |
| `browser/b1_browser.py` 81 · `t2_ui.py` 27 | 진짜 파이어폭스 | 살아남는다(화면 글자·동작만 본다) → **구조 3 의 본 관문은 이 108개다** |

즉 구조 3 을 하면 «JS 동작 기준선» 은 **브라우저 108항목 + 시뮬 90항목** 인데, 시뮬 90개가
**로딩 방식 하나 때문에 통째로 꺼질 수 있다.** 1차는 이것을 적지 않았다.

### 2-2. 시뮬이 `app.js` 를 어떻게 떼어 쓰나 (실측)

- `boxsim.js` → `tests/lib/paths.js` 의 `APP_JS` 한 파일을 읽어 `src.indexOf(문구)` 로 네 구간을 자른다.
  표식 **8개**: `function bpush()` · `function boxInfo(extra)` · `function drawBoxes()` ·
  `const norm = (a, b, c, d)` · `function delSelBox()` · `async function saveBoxes()` · `async function seedBoxes()`.
  자른 것을 `new Function("S","HANDLE","BKO","flash","$","post","errMsg","who", CODE + "return {…}")` 로 감싼다
  → **주입하는 자유변수 8개**와 **내보내는 이름 13개**(`bpush·boxUndo·boxInfo·hitBox·hitBoxesAt·hitHandle·
  bHandleR·bcommit·boxMouseDown·boxMouseMove·boxMouseUp·saveBoxes·PICKTOL`)가 계약이다.
- `modesim.js` → `fn(head)` 가 `src.indexOf("\n}\n")` 로 최상위 함수를 자른다.
  즉 **함수의 닫는 `}` 가 열 0번째 칸에 있어야 한다**(IIFE·모듈로 한 칸 들여쓰면 잘못 자른다).
  대상: `setTool`·`setNumMode`·`numKey` + 최상위 대입문 `$("#box-mode").onchange = () => {` ·
  `window.addEventListener("keydown", (e) => {`.

### 2-3. 메운 것 — `tests/unit/u5_js_contract.py` (**42항목 · 새로 만들었다**)

전역 `S`·`API`·`UI` 셋 규칙을 **검사하는 시험은 없었다**(1차에도, 사이클 시험에도). 그래서 만들었다.
코드는 한 글자도 고치지 않고 **읽기만** 한다. 보는 것:

1. 구간 표식 8개가 **JS 어딘가에 정확히 한 번** 나온다 (7개 — 두 구간이 표식을 공유한다)
2. 네 구간이 **한 파일 안에서 순서대로** 놓여 있다 (4항목)
3. modesim 이 떼어 내는 최상위 함수 3개가 **열 0에서 닫힌다** + 최상위 대입문 2개가 그대로 (5항목)
4. 시뮬이 이름으로 꺼내 쓰는 함수·상수 **25개**가 살아 있다 (지시서 §2 «함수 이름은 유지한다»)
5. 전역 개수 — **지금 경고**: `app.js`+`ui.js` 최상위 이름 **216개** · `S`·`API`·`UI` 중 **`S` 만 있다**(실측).
   구조 3 이 끝나면 이 칸을 «실패» 로 올린다(파일 안 주석에 올리는 법을 적어 두었다).

실행: `42 통과 / 0 실패 / 경고 1`.
구조 3 이 쪼갠 뒤 통과시키는 길 셋을 파일 머리에 적어 두었다 — (가) `paths.js` 에 `appSources()` 를 두고
시뮬 두 줄만 고친다 (나) 상자 관련 표식 8개를 `static/js/boxes.js` 한 파일에 모은다 (다) 표식을 바꿀 셈이면
`MARKERS` 를 같이 고치고 **왜** 를 적는다. **u5 는 그 파일 목록을 `static/js/*.js` 까지 이미 본다.**

### 2-4. 덤 — `sim/sim.js` 는 **단정이 0개**였다

890개 단정 중 `sim/sim` 이 맡은 것은 **0개**였다. 값을 `console.log` 로 **찍기만** 하고
`process.exit` 도 없어서 **절대 실패할 수 없었고**, 기준선 ③ 에도 «-/-» 로 들어가 있었다.
9개 단정을 붙였다(A 역변환 오차 <1e-6 · B 휠 확대 <1e-6 · C 드래그 이동거리 표 7줄).
C 는 **지금 값을 그대로 굳혔다** — 그 값이 이렇다:

| 배율 | 마우스가 간 거리 | 상자가 실제로 간 거리 |
|---:|---:|---:|
| 0.25x·0.5x·1x | 400·200·100 | 400·200·100 (맞다) |
| **2x** | 50.00 | **100** (두 배로 간다) |
| **3x·5x·30x** | 33.33·20.00·3.33 | **0·0·0** (아예 안 움직인다) |

사건마다 `Math.round` 를 하기 때문이다(0.5→1 로 올라가고 0.33→0 으로 내려간다).
**구조 정리는 동작을 바꾸지 않는 작업이므로 이 값이 달라지면 실패로 잡는 것이 맞다.**
«고칠 것» 목록으로 §10 에 올린다(고치는 사이클이 이 표를 갈아 끼우면서 이유를 적는다).
⚠ 남은 한계: `sim.js` 의 `toImg`·`drawPos` 는 `app.js` 에서 **떼어 온 것이 아니라 손으로 옮겨 적은 사본**이다.
구조 3 이 `view.js` 로 쪼갤 때 `boxsim.js` 처럼 떼어 오게 고쳐야 한다(§10).

## 3. 기준선의 정직성 (§공격 3)

### 3-1. 🔴 «변하는 칸» 규칙이 **뜻 있는 칸 26개를 지우고 있었다** (실측)

1차 기준선 ① 의 잎칸(leaf) **3,510개** 중 지워진 것 **166개(4.7%)**. 이름별로 세니:

| 지워진 칸 이름 | 개수 | 왜 지워졌나 | 판정 |
|---|---:|---|---|
| `at` | 60 | 이름 규칙 (시각) | 맞다 |
| `by` | 60 | 이름 규칙 | **과하다 → 고쳤다** |
| `out` · `dir` · `data_root` | 16·2·2 | 이름 규칙 (경로·폴더) | 맞다 |
| **`stem`** | **18** | **값 규칙** | **🔴 틀렸다 → 고쳤다** |
| **`note`** | **8** | **값 규칙** | **🔴 틀렸다 → 고쳤다** |

- **`stem` 18칸**: 사과 사진 이름이 `20150919_174151_image1` 이다. 값 규칙의 `(\d{6}_\d{6})`
  («내보내기 폴더 이름» 을 잡으려던 것)이 그 안의 `150919_174151` 에 걸렸다.
  결과: `api_item__apple__*`·`api_boxes__apple__*`·`api_list__apple` 의 `stem` 칸이 전부 `⟨지움⟩` →
  **«사과 사진을 열면 정말 그 사진이 오나» 를 기준선이 못 봤다.** 목록의 정렬·쪽 나누기도 못 봤다.
- **`note` 8칸**: 사과 메모가 `중복: 20150919_174151_image96` 이라 같은 규칙에 걸렸다.
- **`by` 60칸**: 판정 자료를 얼렸고 검수자 이름도 고정(`기준선`)이라 **돌릴 때마다 같다.**
  지워 두면 «누가 확정했다고 적히나» 가 리팩터로 깨져도 안 잡힌다.

한편 1차가 걱정한 «`path`·`url`·`version`·`date` 칸을 통째로 지웠다»(§8-3)는 **실제로는 0칸**이다 —
① 에 그런 이름의 칸이 아예 없다. 실제 과녁은 `stem`·`note`·`by` 였다.

고친 것(`tests/baseline/snapshot.py`, 코드 무수정):
1. `(\d{6}_\d{6})` → `((?<!\d)\d{6}_\d{6}(?!\d))` — 앞뒤가 숫자가 아닐 때만 «폴더 이름» 으로 본다.
   확인: `20150919_174151_image1` **그대로** · `260919_220945`·`260919_220945_apple` **지움**.
2. `by` 를 `VOLATILE_KEYS` 에서 뺐다.
3. `NEVER_BY_VALUE` 를 새로 두었다 — `stem·stems·rep·name·fruit·note·error·msg·why·
   empty_txt_names·stale_txt_names·members·changed·skipped` 는 **값 규칙을 적용하지 않는다**(뜻이 있는 칸).

### 3-2. 얼린 판정 자료가 실제 분포를 대표하나 — 1차가 남긴 다섯 구멍

실측(`fixtures/status_260920/`): 복숭아 3장(`fixed`·`ok`·`ok`) · 사과 3장(`unreviewed`·`exclude`·`exclude`) ·
블루베리 **1장**(`unreviewed`) · 포도 **0장**(`{}`) · `duplicates.json` **네 과일 모두 빈 묶음**.

| 구멍 | 2차 판정 |
|---|---|
| 판정 `flag` 가 없다 | **못 메웠다** — ⑤ 에 한 줄로 넣을 수 있다(§10 에 그 한 줄을 적었다). `api/regress_all` 이 4과일×3작업으로 `flag` 를 보고 있으므로 «아무것도 안 본다» 는 아니다. |
| 근접 중복 묶음 0개 | **메웠다** — ⑤ 가 표본 2장으로 묶음 하나를 **모래상자에만** 만들고 중복 라우트 6개를 다 지난다. 공용 자료는 안 건드린다. |
| `instances_fixed` 0장 | **메웠다** — ⑤ 의 `save_instances` 왕복이 네 과일에서 `instances_fixed` 를 만들고, `after_instances_fixed__*` 가 그 파일 지문을 굳힌다. |
| `masks_fixed` 1장뿐 | **메웠다** — ⑤ 의 `save action=fixed` 가 네 과일에 하나씩 더 만든다(그 뒤 `revert` 로 지우는 갈래까지 굳는다). |
| 기록 없는 5장(포도 3·블루베리 2) | **일부러 그대로 둔다.** «기록이 없는 사진» 은 **실제 분포**다(4,071장 대부분이 그렇다). 표본에 그 상태가 있는 것이 오히려 맞다. |
| 표본 12장에 포도 CERTH 겹침·조각·팀원 상자가 있나 | **없다**(실측: 포도 3장은 `0`·`1`·`10`, `status.json` 기록 0건, 팀원 상자 폴더 `boxes/` 없음). 포도 번호본은 `SEED_DIRS` 에서 꺼져 있어(교수님 확인 8번 대기) 지금 켤 수 없다. `stems.txt` 를 늘리는 것은 **권고하지 않는다** — 교수님이 포도를 켜는 날 `--restems` 없이 `--rebaseline` 만으로 기대값이 따라가게 `L.grape_seeded()` 가 이미 들어가 있다. |

## 4. 두 번 돌려 같은가 (§공격 4)

### 4-1. 🔴 `--check-baseline` 가 **거짓 실패**를 낸다 — 관문이 못 된다 (실측 · 즉시 고쳤다)

23:28:18·23:28:31 에 `snapshot.py --check` 를 두 번 돌렸다. ①② 는 «다른 칸 0개» 인데 **rc=1** 이었다:

```
── ③ 회귀 묶음 통과/실패 : 다른 칸 2개 (그중 실패로 세는 것 1개)
  merged/tests_merged_260918/통과 | 535 | ⟨사라진 묶음⟩ | 경고
  merged/tests_merged_260918/실패 |   1 | ⟨사라진 묶음⟩ | 실패
[기준선 대조] 합격 기준(①②③) 다른 칸 **1개**
```

원인: ③ 은 `_out/bundles.tsv` — **이번 실행에서 실제로 돌린 묶음** 만 들어 있다.
`snapshot.py --check` 단독, `--only=`, `--no-merged` 로 돌리면 **안 돌린 묶음이 «사라졌다» 로 보인다.**
1차 §7 ④ 가 «그대로 재현하는 명령» 으로 적어 둔 `snapshot.py --check` 는 **절대 통과할 수 없는 명령**이었고,
1차 §4-2 가 권한 관문 `--no-merged --only=syntax,unit,sim,api` 에 `--check-baseline` 을 붙이면 같은 일이 난다.
더 나쁜 것은 **사람이 ③ 을 무시하는 것을 배운다**는 점이다.

고쳤다(`diff_bundles`): `⟨사라진 묶음⟩`·`⟨없던 묶음⟩` 은 **경고**. 시험이 정말 사라지면
«돌린 묶음의 통과 수가 줄어드는 것» 으로 잡힌다(그 규칙은 그대로 실패).

### 4-2. 🔴 `--rebaseline` 이 옛 판을 **되돌릴 수 없게** 덮어썼다 (즉시 고쳤다)

이 폴더는 git 이 아니다(`Is a git repository: false`). `write()` 가 `shutil.rmtree(BASE_DIR)` 로
옛 기준선을 **지우고** 새로 썼다. «전과 무엇이 달라졌나» 를 볼 길이 없었다.
고쳤다: `keep_prev()` 가 옛 판을 `fixtures/_baseline_prev/<YYMMDD_HHMMSS>/` 로 **옮기고**(지우지 않고)
최근 **10판**만 남긴다. 실제로 이번 세션의 옛 판이 `_baseline_prev/260919_234146/` 에 남았고,
§6 의 되돌리기 리허설이 **바로 그 폴더를 기준으로** 대조했다.
(권고: 1차가 제안한 «날짜시각 폴더 + `latest` 링크» 중 링크는 넣지 않았다 — `baseline_260920` 이라는
고정 이름을 README·문서가 이미 가리키고 있어 두 이름이 생기면 헷갈린다. 고정 이름 + 지난 판 10개가 더 낫다.)

### 4-3. `sim/modesim.js` 가 **씨값 없는 난수**로 400번을 눌렀다 (즉시 고쳤다)

«키·체크박스를 400번 마구 눌러도 두 모드가 동시에 켜진 적 없다» 가 `Math.random()` 이었다.
한 번 통과한 것이 다음에 깨져도 **그 순서를 다시 만들 수 없다** — 관문이 흔들린다.
씨값 고정 난수로 바꿨다(`MODESIM_SEED`, 기본 260920). 세 번 돌려 `31/31` 동일.

### 4-4. 최종 실측 — 세 번 돌려 확인

| 실행 | 시각 | ① | ② | ⑤ | ③ | ④ | 합격 기준 |
|---|---|---:|---:|---:|---:|---:|---|
| `--check` 단독 ×2 (⑤ 넣은 직후) | 09-19 23:41:57 · 23:42:15 | 0 | 0 | 0 | 0 | 0 | **0개** |
| `run_all.sh --browser --check-baseline` | 09-19 23:51→23:59 | 0 | 0 | 0 | 0 | 1(help.html) | **0개** |
| `--only=syntax,unit,sim,api --no-merged --check-baseline`(§4-1 이 고친 그 갈래) | 09-20 00:15:15→00:15:53 | 0 | 0 | 0 | 2칸 **전부 경고** | 0 | **0개 · rc=0** |
| `snapshot.py --check` 단독(`--clean` 직후 · bundles.tsv 없음) | 09-20 00:32 | 0 | 0 | 0 | «안 돌렸습니다» | 0 | **0개 · rc=0** |
| 🔴 **최종 — 카운팅5 코드 굳음 + 실서버 00:04:44 재시작 뒤** `--browser --rebaseline` → `--check-baseline` | 09-20 00:16:32→00:24:11 → 00:24:11→**00:31:47** | 0 | 0 | 0 | 0 | **0** | **0개** |

## 5. 정본 시험 45개 — 리허설로 확인 (§공격 5)

### 5-1. 다시 세니 **35개**다 (23:47 실측) · 그리고 «lib 하나» 는 절반만 맞다

`cycles/` 전체에서 «`app|export|scripts/_backup_<태그>_…` 를 **글자로 적어 둔** 스크립트» 를 뽑아
옮긴 태그(51종) ↔ 남긴 태그로 갈랐다(모래상자 사본·`_backup_*` 이름 파일 제외):

| | 개수 | 내용 |
|---|---:|---|
| **지금도 죽는다**(폴백 없음) | **35** | `reg_s2/lib_s2.py` 계열 **15** + `a7_equiv.py` **8** + `s6_drag.py` **5** + `t_ab.py`·`ev_peach.py`·`readrule_diff.py`·`r4_zero.py`·`ab_revert.py`·`c1_fix.py`·`stage2/lib_s2.py` |
| **이미 폴백을 넣었다** | **2** | `260918_paint/cycle_2/stage2b/reg_s2/lib_s2.py` · `260919_count/cycle_5/stage1/reg_cnt5/reg_s2/lib_s2.py` |
| 남긴 태그만 쓴다 = 산다 | 16 | `t3_chars*`·`b3_chars*` (태그를 변수로 이어 붙인다) |

🔴 **폴백을 넣은 2개는 다른 세션이 «맞고 나서» 넣은 것이다.** 그 파일의 주석이 그대로 말한다:
> «0919 «개수 세기» 사이클5 **2차**: 22:00~22:02 에 다른 세션(«구조 정리» 사이클1)이 `_backup_*` 205개를
> `_archive/backups_260920/` 로 옮겨 이 줄이 FileNotFoundError 로 죽었다. 없으면 아카이브에서 읽는다.»
즉 1차의 «원본은 손대지 않았다 · 죽는 것은 목록에만 적었다» 는 **1차 자신에 대해서는 맞지만**,
그 이동이 **동시에 돌던 다른 사이클을 실제로 멈춰 세웠다.** 다음에 205개를 더 옮길 때(남긴 36개)는
**옮기기 전에 아카이브 폴백을 먼저 뿌려야 한다**(§6-3 권고).

그리고 «각 사이클의 `lib_*.py` 한 개만 고치면 그 사이클 전체가 산다» 는 **35개 중 15개**에만 맞다.
`a7_equiv.py`·`s6_drag.py`·`r4_zero.py`·`t_ab.py`·`ev_peach.py`·`readrule_diff.py`·`ab_revert.py`·`c1_fix.py`
**20개는 lib 를 거치지 않고 자기가 직접 백업을 연다.** 각각 한 줄씩 20곳을 고쳐야 한다.

### 5-2. 리허설 — 사본에서 한 줄 고쳐 **77/0 으로 살렸다**

대상: `cycles/260918_paint/cycle_2/stage2/` (여기 `lib_s2.py` 가 죽는 15개 중 하나) → `s1_api.py`.
사본 위치: `tests/_sandbox/st1b_reh/stage2/` (원본은 **무수정** — 09-18 mtime 그대로임을 확인).

| 단계 | 시각 | 결과 |
|---|---|---|
| 사본을 그대로 돌림 | 23:44:29 | **rc=1** · 단정 0개 · `FileNotFoundError: …/app/_backup_260918_c2_server.py` (`lib_s2.py:123 use_old_server`) |
| 사본의 `lib_s2.py` 에 **아카이브 폴백 한 줄**(+`if not os.path.exists`) | 23:44:44 | 옛 서버가 실제로 떠서 넘어갔다 |
| (사본만) 포트 하드코딩 두 번째 자리도 고침 | 23:45:14 | — |
| 다시 돌림 | 23:45:14→23:45:30 | **`[s1_api] 통과 77 / 실패 0 / 경고 0` · rc=0** |

**주장은 맞다.** 단, 리허설에서 **한 가지가 더 필요했다**: `s1_api.py` 는 옛 서버 포트 `5192` 를
**두 군데에 글자로** 적어 두었다(`L.start(port=5192)` 와 `Api(base="http://127.0.0.1:5192")`).
원래 자리에서는 문제가 없지만 «포트 하드코딩은 lib 에만» 이라는 말은 이 정본 시험들에는 **해당되지 않는다.**
되살릴 때 포트 충돌이 나면 두 군데를 찾아야 한다.
(참고: 그 폴더의 09-18 기록은 `72/4` 였다. 오늘 `77/0` 인 것은 그 뒤 사이클들이 지적된 4건을 고쳤기 때문이다.)

## 6. 아카이브 안전 · 되돌리기 리허설 (§공격 6)

### 6-1. 다시 센 숫자 (2026-09-19 23:47)

- 아카이브 `_archive/backups_260920/` **205개** — 1차와 같다.
- 실제 코드가 `_backup_` 을 읽나: 16개 파일을 다시 grep → **0건.**
  걸리는 것은 `server.py` 세 줄의 `_status_backup_<시각>.json`(서버가 `data/` 에 만드는 다른 것)뿐.
- 원래 자리에 **남긴 것 36개** (1차 표기 27 → 23:20 34 → 23:47 **36**).
  「개수 세기」 사이클5 가 돌면서 계속 늘고 있다(`cnt5`·`cnt5b`).
  `app/` 11 · `app/static/` 12 · `app/static/help/` 10 · `export/` 1 · `scripts/` 2 (+`__pycache__` 1).

### 6-2. 스냅샷 무결성 (23:46:45 실측)

`sha256sum -c` 1차가 고친 명령(«79번째 글자부터가 경로» — 사진 이름 빈칸 171개 때문)으로 재검:
검사 줄 **1,592개 · 실패 0개**. 명령이 정말 전부를 검사한다는 것도 줄 수로 확인했다.

### 6-3. 🔴 되돌리기 **리허설을 실제로 했다** (23:46:28→23:46:34)

`_archive/pre_refactor_260920/{app,export}` 를 `tests/_sandbox/st1b_rollback/` 에 풀고,
얼린 판정 자료를 덮고, **빈 포트 5701** 에 서버를 띄웠다. 그리고 **1차가 뜬 옛 기준선**
(`_baseline_prev/260919_234146/`, §4-2 가 보존해 준 것)의 ① 50항목과 대조했다.

```
풀어 놓은 server.py md5 = dfde8c1b67b679db2595e81216a4b029  · 스냅샷과 같은가 True
app/boxes.py      되돌린판 7c08685eb16b / 지금 e6bfe0d546a7 / 다른가 True   ← 23:19 소수정
app/instances.py  되돌린판 3d77b583135a / 지금 3d77b583135a / 다른가 False
[모래상자] 포트 5701 · PID 1394684 …  → 껐습니다 (내 PID 만)
── ① (되돌린 판 ↔ 옛 기준선) : 다른 칸 0개
[되돌리기 리허설] 다른 칸 0개
```

**되돌리기는 정말 된다.** 그리고 되돌린 판이 옛 기준선을 그대로 만족한다.
⚠ 정직하게: 밀폐 묶음(`--only=syntax,unit,sim,api`)을 **스냅샷 판에 대고** 돌리지는 못했다 —
`tests/lib/sandbox.py` 의 `sync()` 가 `T/app` 을 고정으로 읽는다(모래상자 경로는 인자인데 **원본은 아니다**).
권고: `sync(src=...)` 인자를 하나 더 받게 하면 «되돌린 판으로 전체 시험» 이 한 줄이 된다 → §10.

### 6-4. 남긴 36개를 언제 옮기나 — 권고

- **선택지 A**: 「개수 세기」 5 의 3차가 끝난 뒤 한 번에 옮긴다(1차 계획).
  위험: 그 사이클이 3차에서도 `_backup_260919_cnt5c_*` 를 새로 만들면 또 늘어난다. §5-1 처럼
  **돌고 있는 세션을 멈춰 세운다.**
- **선택지 B(권고)**: 옮기기 **전에** 폴백을 먼저 뿌린다. `T/app/_backup_*` 을 읽는 스크립트에
  «원래 자리 → 없으면 `_archive/backups_260920/` » 두 줄을 넣어 두면, 옮기는 순간 아무도 죽지 않는다.
  `tests/lib/sandbox.py` 의 `backup_src()` 가 이미 그 얼개다(`unit/u4` 7항목이 못박고 있다).
  그 함수를 **정본 시험들이 쓸 수 있게** 한 줄 헬퍼로 복사해 두는 것이 가장 싸다.
- 판정 기준(«카운팅 5 가 끝났는지 어떻게 아나»): `INDEX.md` 의 한 줄 명령 대신
  **`_backup_260919_cnt5*` 의 최신 mtime 이 30분 이상 멈췄고 `ss -ltn` 에 그 사이클의 모래상자 포트가 없다**
  를 권한다(파일 개수만 보면 3차 시작 직후 «끝났다» 로 오판한다).

## 7. 프로세스 ≠ 디스크 · 실자료 갈림 (§공격 7)

### 7-0. 🔴 검수 중에 해소됐다 — 그리고 그 판으로 기준선을 확정했다

검수를 하던 **2026-09-20 00:04:44** 에 「개수 세기」 사이클5 가 코드를 굳히고 실서버를 재시작했다
(새 PID **1467097** · `:5111` LISTEN). 디스크의 마지막 코드 수정은 `help.html` **09-19 23:51:36** 이므로
그 시점부터 **«실서버 프로세스 = 디스크»** 다. 그래서 마지막 단계로 그 판에 대고 기준선을 다시 떴다:

```
--browser --rebaseline   00:16:32 → 00:24:11   941 통과 / 1 실패(F7-c) · 기준선 352항목
--browser --check-baseline 00:24:11 → 00:31:47  ① 0 · ② 0 · ⑤ 0 · ③ 0 · ④ **0**  → 합격 기준 0개
```

**④ 까지 0칸**인 것이 핵심이다 — 기준선의 코드 지문 30개가 지금 디스크와 한 글자도 다르지 않다.
이것이 **구조 사이클 2·3 이 견줄 기준선**이다. 굳은 판의 지문:

```
app/server.py dfde8c1b… · app/boxes.py e6bfe0d5… · app/instances.py 3d77b583…
app/static/app.js c774ae54… · ui.js b7abd299… · help.html c9a2f09c… · export/export_dataset.py b8e8b05e…
```

옛 기준선 네 판은 지우지 않고 남겼다(§4-2): `tests/fixtures/_baseline_prev/{260919_234146,
260919_235112, 260920_000738, 260920_002411}`. 첫 판(`260919_234146`)이 **1차가 뜬 판**이다.

### 7-1. 실측 (실서버에 **접속하지 않고** `ps`·`ss`·mtime 으로만)

```
PID 964494 · 2026-09-19 21:45:04 시작 · 23:47 기준 가동 02:02:52 · :5111 LISTEN
(교수님 :5100 · :5105 · 주피터 :5101 — 건드리지 않았다)
```
디스크는 그 뒤 계속 움직였다: `ui.js` 23:19:30 · `boxes.py` 23:19:09 · `help.html` **23:25:56 → 23:5x**.
즉 **실서버 프로세스(21:45 판) ≠ 디스크(지금 판)** 가 여전히 맞다.
기준선은 **디스크 판** 기준이다 — 그것이 옳다(구조 정리는 디스코드를 고치는 작업이다).

### 7-2. 카운팅 5 3차가 서버를 켠 뒤 «프로세스=디스크» 가 되면 기준선을 그대로 써도 되나

**답: 그대로 쓰지 않았다 — §7-0 처럼 다시 떴고, 그래야 맞다.**
이론적으로는 «그 뒤 코드 변경 0» 이면 그대로 써도 된다. 판정 근거는 «서버를 켰다» 가 아니라 **④ 코드 지문**이다.
실측으로 확인했다: 23:51 에 뜬 기준선으로 23:59 에 대조했을 때 ④ 가 **`help.html` 한 칸**만 달랐고
①②③⑤ 는 **0개**였다. 즉 «④ 가 다르면 다시 뜬다» 를 기계적으로 지키면 된다.
권고: 구조 2 는 시작할 때 `--check-baseline` 를 한 번 돌려 **④ 가 0칸인지 확인한 뒤** 손을 대고,
④ 에 뭔가 있으면 먼저 `--rebaseline` 한다. (지금 ④ 는 «참고» 지만 **«작업 시작 전 관문» 으로는 합격 기준**이다.)

### 7-3. 실서버의 첫 사람 확정(22:55:23) — 기준선이 실자료를 안 보는 것이 맞나

독립 확인: `data/peach/status.json` mtime **09-19 22:55:23**, 확정 기록 **딱 1장**
(`210629-t2-of13-03` · `flag` · `by: 익명` · 메모 «…AI 제안 신뢰 금지»). 나머지 세 과일은 09-17~18 mtime 그대로.
23:47 까지 더 늘지 않았다.

**맞다 — 기준선은 실자료를 보지 않는 것이 옳다.** 기준선의 목적은 «코드만의 함수» 이고,
팀원이 라벨을 고칠 때마다 실패하는 관문은 아무도 안 본다. 얼린 자료(`fixtures/status_260920/`)가 정답이다.

**실자료를 봐서 깨지는 손 단정 — 전수**(`tests_merged_260918` 원본을 grep):

| 시험 | 줄 | 단정 | 지금 |
|---|---|---|---|
| `stage2/test_fixes_m2.py` | 111 | `check("F7-c 복숭아 상자 줄 수가 늘었다(973 → 977)", allt == 977)` | **975 → 실패 1건**(1차와 같은 원인) |
| `counts/test_counts.py` | 279 | `check("복숭아 정답 합계가 박성문 표와 같다(977알)", tot == 977)` | **통과** — 이것은 **정답 상자**를 세므로 사람 확정이 빼지 않는다 |
| `stage5/test_spec.py` | 331 | `target_pixels == 2073600 · multiple == 8` | 통과 — 설정값이고 자료와 무관 |

즉 **공용 자료에 매달린 손 단정은 `F7-c` 하나뿐**이고, 그것이 지금 유일한 실패다.
그리고 공용 `T/data` 를 읽는 merged 시험은 **4개**다
(`stage5/test_fix_m5c.py`·`stage2/recompute_manifest.py`·`stage2/attack_data.py`·`stage3/test_fixes_m3.py`) —
지금은 통과하지만 **같은 종류의 시간 폭탄**이다.

### 7-4. 🔴 여기서 구조 2 에 넘길 **가장 위험한 것**을 찾았다 (툴 폴더 **밖**이다)

툴 폴더 밖에서 툴의 모듈을 **이름으로** 불러 쓰는 곳이 두 군데 있다:

```
semantic-segmentation/tools/build_merged_dataset.py:1480   sys.path.insert(0, os.path.join(TOOL_ROOT, "app"))
                                            :1481   import boxes as TOOLBOXES
                                            :1482   TOOLBOXES.export_boxes_to(stage, fruit, box_dir, …)
semantic-segmentation/tools/tests_merged_260918/counts/test_counts.py:158   import instances as INST
```

`TOOL_ROOT` 는 **절대경로**라 격리 사본·심볼릭 링크는 문제가 없다(§8-4 의 1차 질문에 대한 답: **안전하다**).
문제는 다른 것이다 — 지시서 §2 는 `boxes.py` 를 `app/api/boxes.py` 로, `instances.py` 를
`app/api/instances.py` 로 **옮긴다**. 그러면 `import boxes` 가 실패한다. 그런데:

```python
except Exception as e:                  # ← 조용히 삼킨다
    ... 자체 구현 ...
    with open(os.path.join(box_dir, s + ".txt"), "w") as f:
        f.write("\n".join(lines) + ("\n" if lines else ""))   # ← 0줄이면 0바이트 txt 를 만든다
    return "자체 구현(툴 boxes.py 를 못 읽음: %s)" % e
```

**자체 구현 폴백에는 「개수 세기」 사이클5 가 방금 닫은 규칙이 없다.** 즉 구조 2 가 파일을 옮기면
논문용 통합 데이터셋이 **조용히** 0바이트 YOLO 라벨(검출 학습에 «이 사진에는 열매가 없다» 로 읽히는 것)을
다시 내보내기 시작하고, **merged 시험은 그래도 통과한다**(상자가 있는 사진은 정상이므로).
심각도 **높음 · 데이터 품질**. 구조 2 의 «지켜야 할 것» 에 반드시 넣어야 한다(§11 관문 명령에 넣었다).

## 8. 디스크 · 안전 (§공격 8)

### 8-1. `tests/` 6.1GB — 실패하면 모래상자를 남기는 정책

확인: `merged` 묶음이 **통과하면** 사본의 모래상자를 치우고, **실패하면 남긴다.**
지금 `F7-c` 가 계속 실패하므로 **매 실행마다 5.6GB 가 남는다** — «가끔» 이 아니라 **항상**이다.
1차가 «23:20 실측 6.1GB» 라고 적었는데, 내가 기준선을 세 번 더 뜨는 동안 **12G** 가 됐다(09-20 00:31 실측).
`--clean` 플래그가 없어서 사람이 `find … -exec rm -rf` 를 손으로 쳐야 했다.

고쳤다: `run_all.sh --clean` — `tests/_sandbox` 와 `tests/_out` 을 지우고 전후 용량을 찍는다
(`--clean` 하나만 주면 그것만 하고 끝난다). 둘 다 다음 실행에서 다시 만들어진다.
실측(09-20 00:32:54): `치웠습니다: tests/_sandbox · tests/_out  (12G → 3.7M)`.
그 뒤 `snapshot.py --check` 를 단독으로 돌려도 **rc=0** 이다(`bundles.tsv` 가 없어도 §4-1 이 경고로 본다).
**«최근 N개만 남긴다» 는 넣지 않았다** — 모래상자가 묶음마다 하나씩이라 «N개» 라는 축이 없고,
지금 필요한 것은 «실패 원인을 본 다음 사람이 치우는 한 줄» 이다. 기준선의 지난 판은 §4-2 가 10판 남긴다.

### 8-2. 🔴 `run_all.sh` 의 글롭이 «폴더에 떨어진 것을 다 돌린다» (즉시 고쳤다)

`for f in "$HERE"/sim/*.js` 와 `"$HERE"/unit/u*.py`. 내가 백업을 `sim/_backup_260919_st1b_modesim.js` 로
두자 **그것이 시험 묶음으로 잡혔다** — 묶음 수가 조용히 늘어 기준선 ③ 이 «다르다» 가 된다.
고쳤다: 두 글롭에서 `_backup_*`(과 `*_AFTER*`·`*_BEFORE*`)를 이름으로 걸러 낸다.
내 백업들은 `tests/_backup_260919_st1b/` 로 옮겼다(글롭이 닿지 않는 곳).

### 8-3. 남의 프로세스를 죽일 위험 — **없다**(확인)

`tests/` 전체에 `pkill` **0건**. `L.stop()` 은 `p.terminate()` → 25초 뒤 `p.kill()` 로
**자기가 띄운 `Popen` 객체만** 끈다. PID 파일도 쓰지 않는다.
`KEEP_OUT = {5100,5101,5102,5104,5105,5111}` 로 남의 포트는 목록에서 빠지고,
`start()` 는 «이미 쓰이는 포트면 **멈춘다**». 내가 띄운 모래상자(5601·5611·5612·5701)도 다 꺼졌다.
남은 위험 하나: 파이썬이 `SIGKILL` 로 죽으면 `finally` 가 안 돌아 모래상자 서버가 **남는다**.
권고: `run_all.sh` 끝에 «`tests/_sandbox/*/app/logs/server*.log` 의 포트가 아직 LISTEN 이면 알려 준다» 한 줄
(죽이지는 말고 **알리기만**) → §10.

## 9. 즉시 메운 것 — 전/후 항목 수

`tests/` 안에서만 고쳤다. 고친 파일의 원본은 **`tests/_backup_260919_st1b/`** 에 그대로 있다.

| 무엇 | 파일 | 전 | 후 |
|---|---|---:|---:|
| ⑤ 라우트·쓰기 기준선(새로) | `tests/baseline/snapshot.py` → `fixtures/baseline_260920/routes.json` | 0 | **118** |
| ② 내보내기(쓰기 뒤 한 번 더) | 같은 파일 | 99 | **119** |
| ③ 거짓 실패 고침 | `snapshot.py diff_bundles` | 안 돌린 묶음 = **실패** | 안 돌린 묶음 = **경고** |
| 지운 «변하는 칸» 을 좁힘 | `snapshot.py` `VOLATILE_*`·`NEVER_BY_VALUE` | 뜻 있는 칸 **26개**(stem 18·note 8) + `by` 60 이 비어 있었다 | 살아났다 |
| 옛 기준선 보존 | `snapshot.py keep_prev()` | 덮어써서 **소실** | `_baseline_prev/` 에 **10판** |
| JS 계약 시험(새로) | `tests/unit/u5_js_contract.py` | 0 | **42 단정** |
| `sim/sim.js` 단정 | `tests/sim/sim.js` | **0** | **9** |
| modesim 재현성 | `tests/sim/modesim.js` | 씨값 없는 난수 | 씨값 고정(31/31 ×3회 동일) |
| `--clean` · 글롭 안전 | `tests/run_all.sh` | 없음 | 있음 |
| **합격 기준 항목 수(①②③⑤)** | | **213** | **352** (①100 + ②119 + ③15 + ⑤118) |
| **시험 단정 수** | | **890 / 실패 1** | **941 / 실패 1** (실패는 그대로 `F7-c` 하나) |
| 라우트 덮개 | | **11 / 37** | **37 / 37** |

검증(전부 실측 · 증거 로그는 **`cycle_1/stage2_evidence/`** 에 넣었다):

| 실행 | 시각 | 결과 |
|---|---|---|
| `run_all.sh --browser --rebaseline` | 09-20 00:16:32→00:24:11 | 941 통과 / 1 실패(F7-c) · 459초 · 기준선 352항목 |
| `run_all.sh --browser --check-baseline` | 09-20 00:24:11→**00:31:47** | **①②③④⑤ 전부 «다른 칸 0개»** · rc=1 은 F7-c 하나 |
| `--no-merged --only=syntax,unit,sim,api --check-baseline` | 09-20 00:15:15→00:15:53 | **rc=0** (고치기 전에는 여기서 거짓 실패가 났다) |
| `--clean` | 09-20 00:32:54 | `tests/` **12G → 3.7M** |
| 되돌리기 리허설(스냅샷 → 모래상자 → 서버 → ① 대조) | 09-20 00:32:38→00:32:43 | **다른 칸 0개 · rc=0** |
| 정본 시험 리허설(`s1_api`) | 09-19 23:44:29(rc=1 죽음) → 23:45:30 | **77 통과 / 0 실패 · rc=0** |

증거 파일: `stage2_evidence/{final_rebaseline_0024.log, final_check_0031.log,
check_거짓실패_2328.log, check_부분실행_rc0_0015.log, reh_s1_api_BEFORE.log, reh_s1_api_AFTER.log,
prove_boxes.py, prove_boxes.log, rollback_reh.py, rollback_reh.log}`

**덤 — §3 의 고침이 얼마나 되찾았나(실측)**: 되돌리기 리허설을 **옛 기준선**(`_baseline_prev/260919_234146`,
옛 지우기 규칙으로 뜬 것)에 대고 돌리니 **다른 칸 27개**가 나왔는데 **전부 `⟨지움⟩ → 실제 값`** 이었다
(`stem` 6 · `note` 4 · `by` 17). 동작 차이는 0. 즉 ① 한 곳에서만 **27칸**을 되찾았다.

## 10. 3차(판정)에 넘길 것

1. **(높음 · 구조 2 의 전제)** §7-4 — `semantic-segmentation/tools/build_merged_dataset.py:1480` 의
   `import boxes` 가 구조 2 의 파일 이동으로 **조용히 자체 구현으로 떨어진다**. 그 자체 구현에는
   사이클5 의 «0줄 json 은 txt 를 만들지 않는다» 규칙이 **없다.** 판정: (가) `except Exception` 을
   **터지게** 바꾼다 (나) `app/` 에 `boxes.py` 를 얇은 재수출로 남긴다 (다) 옮기지 않는다.
   **권고 (가)+(나)** — 조용한 폴백이 가장 위험하다.
2. **(높음)** `stage2/test_fixes_m2.py:111` `F7-c` — 통합 데이터셋 담당이 «977 을 적지 말고 직전 빌드에서 세어
   견주기» 로 고친다. 고치면 `--rebaseline` 로 ③ 이 «merged 536/0» 으로 돌아간다.
   같은 종류의 시간 폭탄이 **4개 더** 있다(§7-3).
3. **(중간)** 얼린 표본에 판정 `flag` 가 없다. ⑤ 에 한 줄이면 된다 —
   `P("save_flag__%s" % f, "/api/save", {"fruit": f, "stem": s1, "action": "flag", "by": "기준선", "note": "기준선 flag"})`
   (마스크 `save_ok` 바로 뒤). 넣으면 `--rebaseline` 필요.
4. **(중간)** `/login` **429**(1분에 10번 틀림) 갈래가 ⑤ 에 없다 — 잠금 창을 오염시켜 뒤 실행이 흔들리기 때문.
   권고: 별도 `unit/u6_login_rate.py` 로 **자기 모래상자 서버 하나만** 띄워 잠그고 끝낸다.
5. **(중간)** §6-3 — `tests/lib/sandbox.py sync()` 가 `T/app` 고정이라 «되돌린 판으로 전체 시험» 을 못 돌린다.
   `sync(src=...)` 인자 하나. 그러면 구조 2·3 의 «전/후 같은 시험» 이 한 줄이 된다.
6. **(중간)** §2-4 — `sim/sim.js` 의 `toImg`·`drawPos` 는 손으로 옮겨 적은 사본이다. 구조 3 이 `view.js` 를
   만들 때 `boxsim.js` 처럼 떼어 오게 고친다.
7. **(중간 · 결함 목록)** §2-4 표 — 배율 2x 에서 상자가 마우스의 **두 배**로 가고, 3x 이상에서는 **아예 안 움직인다**.
   지금은 기준선으로 굳혔다. 고치는 것은 별개 사이클.
8. **(중간)** 남긴 백업 **36개**(계속 늘고 있다)를 옮기는 절차 — §6-4 **선택지 B(먼저 폴백, 그 뒤 이동)** 권고.
   그리고 죽는 정본 시험 **35개**(그중 **20개는 lib 를 안 거친다** — §5-1) 를 되살릴지 판정.
9. **(낮음)** §8-3 — 강제 종료 뒤 남은 모래상자 서버를 **알리는** 한 줄(죽이지는 않는다).
10. **(낮음)** `u5_js_contract.py` ⑤ 칸(전역 216개 → `S`·`API`·`UI` 셋)을 구조 3 이 끝난 뒤 **경고 → 실패**로 올린다.
11. **(문서)** 1차가 «못 했다» 고 적은 것 중 2차도 못 한 것: `tests/browser/{b2_export,b4_fallback,b5_oldserver}` 미도입 ·
    `app/README.md` 의 «백업은 `_archive/` 로 갔다» 한 줄 · `final/`·`inspect/`·`site/`·`ai_pass/` 의 34개 이동 판정.

## 11. 구조 2·3 이 지켜야 할 «관문 명령» — 한 줄

```bash
T=/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴; bash $T/tests/run_all.sh --browser --check-baseline; echo "rc=$? ← ①②③⑤ «다른 칸 0개» 와 merged 실패가 F7-c 하나뿐이면 합격 (④ 는 경고)"
```

- **시작 전에도** 같은 명령을 한 번. ④ 가 0칸이 아니면 먼저 `--rebaseline`(다른 세션이 코드를 고친 것이다 — §7-2).
- 빠른 관문(약 40초, 코드를 만지는 동안): `bash $T/tests/run_all.sh --no-merged --only=syntax,unit,sim,api`
  (여기에는 `--check-baseline` 을 **붙이지 않는다** — ③ 은 이번 실행에서 돌린 묶음만 본다. §4-1)
- 구조 3 은 추가로 `bash $T/tests/run_all.sh --only=unit` 의 **`u5_js_contract` 42항목**이 통과해야 한다.
  그것이 «시뮬 90개가 아직 `app.js` 를 떼어 쓸 수 있나» 를 본다.
- 구조 2 는 추가로 `semantic-segmentation/tools/build_merged_dataset.py` 의 `import boxes` 가
  **자체 구현으로 떨어지지 않았는지** 확인해야 한다(§7-4 · merged 로그에 «자체 구현» 이 찍히면 실패).
