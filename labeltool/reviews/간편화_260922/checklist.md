작성: 2026-09-22 16:50 · 한 라운드 = 한 항목 · 끝나면 `[x]` + 시각 · 막히면 항목 뒤에 «막힘: 이유» 를 적고 다음 항목으로

- [x] (F, 16:58 대화 세션) T1 «클릭으로 붙이기»(번호 편집 K 의 기본 도구, 이미 구현: `app/static/js/instances.js` clickAdd/floodMaskPx · `index.html` data-ntool="click" · `state.js` ntool 기본 "click"). 모래상자 실측(scratchpad `verify_click.py` 는 합성 MouseEvent 로 눌렀는데 번호가 안 붙음 — main.js 의 실제 mousedown 경로(numMode → UI.numMouseDown?)를 읽어 원인을 찾아 고치고, 블루베리 `Camera 3 Video (94)_121` 에서 «칠해졌지만 번호 없는 자리 클릭 → 새 번호 18» 이 실제로 되는지 확인. 콘솔 오류 0. → 실측: 합성 이벤트 실패는 첫 시험이 사진 맨 위(y=8, 상단 바 아래)를 눌러서였음. 다시 잰 결과 진짜 마우스 경로(main.js mousedown→numMouseDown)로 클릭 → inst=18·instMax 18·numDirty true·콘솔 오류 0. 코드 수정 없음.
- [x] (F, 17:02 대화 세션, syntax/sim 300/0) T2 «지우개로 모든 라벨 지우기»: 일반(마스크) 모드의 지우개(E, 오른쪽 버튼 포함)가 내 수정본뿐 아니라 **열매 번호(S.inst)도 같이 지운다**(지나간 화소의 inst 를 0 으로, pushNumUndo 로 되돌리기 가능, S.numDirty 켬). Ctrl+S 는 마스크 저장 뒤 numDirty 면 번호(`/api/save_instances`)도 이어서 저장. 번호가 없는 사진(S.inst 없음)은 전과 같음. 모래상자 실측: 번호 있는 화소 위로 지우개 드래그 → S.ed 0 · S.inst 0 · 저장 뒤 서버 instances_fixed 반영. 사용법 페이지 문구 갱신. → 구현: mask.js stamp(일반 모드 지우개가 S.inst 도 0, 이전 값 S.eraseInstMap) · instances.js applyEraseInst(마우스 뗄 때 되돌리기 한 단계 + 번호 겹 다시 칠함) · main.js mouseup 훅 · counts.js 수정본 저장 뒤 numDirty 면 번호도 저장. 백업 `_backup_260922_eraseall_*` 4개. 모래상자 실측(복숭아 210629-t1-04, 번호 9 위로 지우개 드래그): ed 0·inst 0·numDirty true·되돌리기 9→다시 0·저장 뒤 masks_fixed·instances_fixed 둘 다 생김·콘솔 오류 0.
- [x] (F, 17:05 대화 세션) T3 포도·사과 AI 판정 재확인 반영(`data/grape,apple/status.json`, 백업 `_status_backup_260922_161532_before_recheck.json` 있음): 포도 flag 72 중 **남길 10장** = 1436·1450·941·942(네모 블록)·1183·2228·606·803·817·962(송이 누락) — 나머지 62장은 status ok, by «AI 재검수 0922(Fable, 그림판 눈확인)», note «원본 GT 가 맞음 — 09-16 규칙 판정은 AI 제안 쪽 문제. 사람 확정 전», 이전 값은 prev 에. 남길 10장은 note 앞에 «0922 눈 확인: 원본 GT 에 진짜 문제 — 사람이 고칠 것». 사과 flag 중 잎·땅 5곳(stem 끝이 174151_image201 · 131346_image906 · 625_image306 · 625_image511 · et1_back_961) → ok, note «후보 자리가 잎·땅이라 사과 아님(0922 눈 확인)». confirmed 칸은 절대 안 건드림. 그림판 근거는 scratchpad 가 아니라 `cycles/260922_툴_간편화/recheck_sheets/` 로 복사해 둘 것(`/tmp/claude-1020/.../scratchpad/recheck/*.png`, 없으면 `sheets_grape.py`·`sheets_apple.py` 를 다시 돌려 만든다 — 스크립트도 같은 폴더에 있음). → 반영 결과: 포도 62장 flag→ok, 남은 flag 10 · 사과 5장 flag→ok(20150919_174151_image201 · 20150921_131346_image906 · 20150921_131625_image306 · 20150921_131625_image511 · dataset1_back_961), 남은 flag 46. by «AI 재검수 0922(Fable, 그림판 눈확인)», prev 에 이전 값.
- [ ] T4 사과 두 출처 구분: `app/static/js/list.js` 목록 필터에 «MinneApple / PApple(front·back)» 구분(stem 이 `20YYMMDD_` 로 시작하면 MinneApple, `datasetN_front/back` 이면 PApple) 을 넣고 카드에 출처 글자 표시. 데이터 분리 자체는 교수님 결정이므로 하지 않는다. `문서/260922_카톡_사과출처.txt` 5줄(미발송): 사과 1,001 = MinneApple 670 + PApple 331 · 둘 다 알마다 번호 마스크 · 통합에 남은 429 = 266 + 163 · 분리 여부는 교수님께.
- [ ] Z1 사용법 페이지(artifact 는 대화 세션만 가능 → md 로 `문서/260922_툴_사용법_추가.md` 에 클릭 번호·통합 지우개·출처 필터 사용법) · `prepare_public.py` → `audit_public.py` → 커밋 → push · `작업기록.md` §1·§2·§3 갱신(날짜·경로·실측값) · `auto/ALL_DONE`.
