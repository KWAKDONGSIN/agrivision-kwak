작성: 2026-09-19

# 상자+개수 세기 5사이클 — 사이클 1 (개수 칸·확정 유도·counts.csv·통합 칸·상자 정의) 3차 판정 (Fable 5.1)

판정 시각(실측): 2026-09-19 16:42 · 근거: 1차 `stage1_work.md` · 2차(공격적) `stage2_review.md` · 소수정 `stage2c_fix.md` · 내가 켠 것(§2)

## 1. 판정 — **합격, 실서버에 켬(16:41:06~09, PID 263865)**
| 항목 | 판정 |
|---|---|
| 1차 다섯 가지(개수 칸·`human_count()` 한 곳·counts.csv 12칸·통합 manifest 6칸·상자 정의 과일별 문구) + 숫자만 치는 칸 없음(yagni) | 수용 |
| 2차 즉시 수정 6 — 🔴 **개수 캐시가 지문을 안 봐 낡은 값을 씀**(첫 편집에서 터질 것) · 화면이 없는 확정을 말함 · 명령줄 0장 죽음 · 다른 스냅샷 · «개수만» 폴더 빈 폴더 표시 · README | 수용 |
| 2차가 밝힌 1차 오류 4(어긋나면 번호 수를 쓰고 있었음 · 복숭아 977 «독립» 은 순환 · 낡은 기대값 2곳은 이미 고쳐져 있었음 · 툴 4/8 갈림은 불가능 — 실서버 항상 4-연결) | 수용, 문서 정정 |
| 결정 1 어긋남이면 `n_human` 비움 + `conflict` | 고침 — 사람이 판단. 어긋난 사진은 «사람 확정만» 에서 빠짐 |
| 결정 2 counts.csv 는 나간 사진만(«사람 확정만» 은 +n_human 조건) | 고침 — manifest «포함» 과 완전히 같은 줄 |
| 결정 3 센 값을 지문과 함께 캐시 | 고침 — 한 장 218ms → 0.32ms |
| 결정 4 초록 우선·가드 `개수 ?·?·?` | 고침 |
| 사이클 4 로 | `export_worker` 세 번째 status 읽기 · `/api/item` 옛 `n_inst` · `mk_reg_c4.py` 스냅샷 재사용 · 1픽셀 상자 w=0.00078 줄 |

## 2. 켜기(내가 직접)
외부 마지막 쓰기 09-18 09:07 → `py_compile` 6·`node --check` 2 → `run.sh restart` 16:41:06~09 → PID 3915126 → **263865**(VS Code 세션의 «수정본 색 시안»·`/box` 화면도 이 재시작으로 서버 쪽 코드가 같이 반영). 켠 뒤 GET: 비로그인 401 · `/api/item` 에 `counts`(사과 image1: 번호 95·팀원 93·93, 상자 없음, human 없음 / 복숭아 t1-01: 팀원 11·11) · `/api/fruits` AI 집계 불변·확정 0 · `/api/export_list` kinds 에 `counts`. 쓰기 0, `exports/` 없음.

## 3. 남긴 것
- 사람 눈: 하단 «개수 …» 줄이 세 모드에서 보이는지 · 상자 저장 → «사람 확정 개수» 초록으로 바뀌는지(Ctrl+F5)
- 사이클 3(공격): 대조표 + 코드↔논문 절차 · 사이클 4(보통): M1 워터셰드 통일 등 코드 + 브라우저 회귀 + 그림 재생성 · 사이클 5 종합
