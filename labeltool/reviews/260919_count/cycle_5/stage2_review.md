작성: 2026-09-19

# 「상자 + 개수 세기」 5사이클 — **사이클 5(종합) 2차 검수** (Opus 5)

검수 시각(`date` 실측): 시작 **22:57:03** · 재현 **22:59:50~23:25:35** · 즉시 고침 **23:19:09~23:37:48** ·
«고친 뒤» 회귀 **23:28:54~23:57:31** · 시나리오 **23:30~23:42** · 마지막 확인(고친 코드로 다시) **23:57~00:02:52**(그 사이 날짜가 **2026-09-20** 으로 넘어갔습니다).
모래상자·포트는 전부 **내 것**(`cycle_5/stage2/` · 5481·5482·5484·5486 · 5433·5434 · 5931~5933 · 574x~576x).
실서버 5111(PID **964494** · 21:45:04 기동)에는 **붙지도 재시작하지도 않았습니다** — 켜는 것은 3차 몫입니다.

**한 줄 요약**: 1차의 소수정 4는 **옳고 재현됩니다**(«고치기 전» 11 실패 → «고친 뒤» **22/0**).
그런데 두 가지가 1차 뒤에 어긋났습니다 — ① **22:55:23 에 사람이 툴에서 복숭아 한 장을 «제외» 로
확정**해서 «복숭아 125 · 합계 1,668 · 사람 확정 0건 · 지표 표 복숭아 세 줄» 이 **전부 낡았고**,
② 소수정 ④ 는 **반만 닫혀** 있었습니다(새 0줄 txt 는 안 만들지만 **옛 0줄 txt 가 그 폴더에 그대로
남고 아무도 그것을 말해 주지 않습니다** — 실서버에 실제로 2개 있습니다). 둘 다 고쳤고(§7),
정본 시험 4파일이 아카이브 이동으로 죽는 것도 되살렸습니다. 고친 것은 **코드·시험 7파일 + 문서 6개**
(§7 표 8줄 · 전부 «전 실패 → 후 통과» 를 붙였습니다). 판정서 재료는 **정정 30곳**이 필요합니다(§4 — 결함 표 3 · 대조표 10 · M 상태 1 ·
22:55 뒤 숫자 6 · 숫자 사전 6 · 교수님·팀원 질문 문장 4).
시나리오는 **O 20 / △ 2 / X 0**.

🔴 **3차가 먼저 볼 것 세 가지**
1. **사람이 지금 툴을 쓰고 있습니다**(22:54~23:00 외부 IP · 쓰기 1건). 켜기 전에 조용한 때를 고르고,
   판정서의 모든 숫자에 «언제 잰 값인가» 를 붙이십시오. 0920 에 **다시 세야** 합니다.
2. **M2 는 ✅ 가 아니라 🟡 부분**입니다(포도는 지금도 4-연결 CC). 상태표 집계가 «완료 10 · 부분 2» 로 바뀝니다.
3. **사람 삭제 목록에 0바이트 txt 두 개를 더해야 합니다**(`data/{peach,grape}/boxes_yolo/…txt`) —
   json 만 지우면 그 파일은 남습니다.

---

## 1. 재현 — 1차 §9 명령을 내 폴더·내 포트로

### 1-1. 🔴 §9 의 첫 명령은 **그대로 돌리면 재현되지 않습니다**

1차 §9 (1)번은 «고치기 전» 판을 이렇게 만듭니다:

```
$PY -c "... M.L.sync(M.SB_OLD); M.L.reset_status(M.SB_OLD)"
cd $C && $PY -u c5_fix.py --before     # 통과 11 / 실패 11
```

그런데 `L.sync()` 는 **지금(고친) 코드를 복사**합니다 — 이 사이클이 고친 7파일을 되돌리는 함수는
`lib_cnt5.sync_before()` 입니다. 글자 그대로 돌린 결과와 `sync_before()` 로 돌린 결과:

| 명령 | 결과 | 실패한 것 |
|---|---|---|
| §9 (1) **그대로**(22:59:50~) | **통과 17 / 실패 5** | ③ 5건만(빌더가 백업 판이라서). ①②④ 는 새 코드라 통과해 버립니다 |
| `M.sync_before()` 로(23:00~) | **통과 11 / 실패 11** | ④ 3 · ③ 5 · ① 2 · ② 1 — **1차가 적은 것과 정확히 같습니다** |

→ **3차·다음 사이클은 §9 (1)번을 `M.sync_before()` 로 바꿔 적으십시오.** 1차가 실제로 쓴 것은
그쪽이고(보고서 §1 표가 «`--before` = `sandbox_before`(22:01:32 에 뜬 고치기 전 정본)» 라고 적습니다),
§9 에 옮길 때 한 줄이 어긋났을 뿐입니다.

### 1-2. «고친 뒤» 22/0 — **한 번 더 걸림돌이 있었습니다**

`c5_fix.py` 를 그대로 돌리면 **통과 18 / 실패 4** 가 나옵니다(③-2·③-4·③-6·③-7).
이유는 코드가 아니라 **자료**입니다 — 시험이 «복숭아 **125**» 를 **글자로 박아** 두었고,
2026-09-19 **22:55:23** 에 사람이 복숭아 한 장을 «제외» 로 확정해 **124** 가 됐습니다(§4-5).
기대값을 «툴이 말하는 남긴 장수» 로 바꾸니 **통과 22 / 실패 0** — 1차 값이 그대로 재현됩니다.

### 1-3. 재현표 (내 폴더 `cycle_5/stage2/` · 내 포트 · 내 모래상자)

| 시험 | 1차 | **2차 재현** | 판단 |
|---|---|---|---|
| `node --check` app.js·ui.js | OK ×2 | **OK ×2** | 🟢 |
| `py_compile` 서버 6 + tools 2 | OK | **OK** | 🟢 |
| `app/boxes.py` 자체 점검 | 통과 | **통과**(내가 고친 뒤에도) | 🟢 |
| `count_metrics.py --selftest` | 손계산 5건 통과 | **통과** | 🟢 |
| c4 `t1_api` | 34 / 0 | **34 / 0** | 🟢 |
| c4 `t2_ui` | 27 / 0(경고 1) | 정본에서 새로 뜨면 **죽습니다**(아카이브) → **정본 고친 뒤 27 / 0** | 🟡 §7-3 |
| c4 `regress_all` | 100 / 0(경고 8) | **100 / 0(경고 8)** | 🟢 |
| `modesim.js` · `boxsim.js` | 31/31 · 50/50 | **31/31 · 50/50** | 🟢 |
| c2-1차 `t1_api`·`t2_queue`·`t3_conc` | 79·43·8 / 0 | **79·43·8 / 0** | 🟢 |
| c2-1차 `t4_paint8` | 22 / 0 | 정본 사본은 **죽음** → 고친 뒤 **22 / 0** | 🟡 §7-3 |
| c2-2차 `s1_api` | 77 / 0 | 죽음 → 고친 뒤 **77 / 0** | 🟡 §7-3 |
| c2-2차 `s2`·`s3`·`s6`·`s7`·`s8` | 20·18·7·2·3 / 0 | **20·18·7·2·3 / 0**(`s8` 은 고친 뒤) | 🟢 |
| c2-2차 `s4_paint8` | 21 / **1** | **21 / 1** — 같은 실패(«확정» 딱지 12.5px) | 🟡 전과 같음 |
| c2-2차 `s5_live_guard` | 12 / **1** | **12 / 1** — 같은 실패(A-2 가드 문장) · 고친 뒤 | 🟡 전과 같음 |
| 중첩 `t1_after` · `t3` · `t4` | 180 / 0 · — · — | **180 / 0 · 90 / 0 · 25 / 0** | 🟢 |
| 중첩 `t6_view` | 139 / **4** | **139 / 4** — 같은 4건(문턱 0.5%) | 🟡 전과 같음 |
| 통합 `test_counts` | 62 / 0 | **62 / 0** | 🟢 |
| 통합 `test_build_merged` | 68항목 전부 통과 | **전부 통과** | 🟢 |
| 통합 `stage4/test_confirm_kinds` | 200 전부 | **200 전부 통과** | 🟢 |
| 통합 `stage5/test_spec`·`test_fix_m5c` | 106 · 67 전부 | **106 · 67 전부 통과** | 🟢 |
| 통합 `--dry-run` 복숭아 «번호» | **125** | **124**(남김 124 · 제외 1) | 🔴 자료가 바뀜 §4-5 |
| `b1_browser` | 81 / 0 | **81 / 0** | 🟢 |
| `b2_export` | 28 / 0 | **27 / 1**(③-c «125장 전부») → 기대값을 자료 기반으로 바꾼 뒤 **28 / 0** | 🔴 같은 이유 |
| `b3_chars` | 4 / 0 · 증감 0자 | **4 / 0 · 증감 0자** | 🟢 |
| `b4_fallback` | 4 / 0 | **4 / 0** | 🟢 |
| `b5_oldserver` | 6 / 0 | `runall_c5.sh` 만 돌리면 **죽습니다**(«Connection refused» — 그 시험이 쓰는 `sandbox_old4` 를 **만드는 줄이 runall 에 없습니다**. §9 손명령에만 있습니다) → 넣고 돌리면 **6 / 0** | 🟡 §8-3 |
| 소수정 4 전/후(`c5_fix.py`) | 전 11 실패 → 후 22/0 | **전 11 / 후 22 / 0**(§1-1·1-2) | 🟢 |
| 🆕 내가 고친 것(`c5b_stale.py`) | — | **전 3/2 → 후 5/0** | 🟢 §7-1 |
| 🆕 시나리오(`scn_newcomer.py`) | — | **전 12/5 → 후 22/0** | 🟢 §6 |
| 지표 표(`count_metrics.py` 네 폴더) | §5-5 표 | **값 하나까지 같음**(단 복숭아는 이제 124장 → §4-5) | 🟢 |

### 1-4. 내가 고친 뒤 **전체 회귀 한 벌** (23:28:54~23:57:31 · 내 포트로 새로 뜬 사본)

`bash runall_c5c.sh` 한 벌: **c4 34/0 · 27/0(경고 1) · 100/0(경고 8)** · **modesim 31/31 · boxsim 50/50** ·
c2 묶음 **79·43·8·22 / 0** · **77·20·18·21(1)·12(1)·7·2·3** · 중첩 **180/0 · 90/0 · 25/0 · t6 139/4** ·
통합 **62 / 전부 통과 / 200 / 106 / 67** · dry-run **rc=0**(복숭아 목록 125 · 남김 **124** · 제외 1 —
표에 «`confirmed_flag` 1 사람이 «문제 있음» 으로 판정» 이라고 **스스로 적습니다**) ·
**b1 81/0 · b5 6/0 · b2 28/0 · b4 4/0 · b3 4/0(글자 증감 0자 — 마스크 162·상자 267·번호 225)**.
→ **1차가 적은 모든 수와 같고, 달라진 것은 «복숭아 125 → 124» 하나입니다**(자료가 바뀐 것 · §4-5).

그리고 **내가 마지막에 고친 코드(`ui.js` 가드 문장 · 빌더 주석)까지 넣어 모래상자를 새로 떠서 한 번 더**
돌렸습니다(23:57~00:02:52 · `run_final_b.sh`): **`c5_fix` 22/0 · `c5b_stale` 5/0 · b1 81/0 · b2 28/0 ·
b4 4/0 · b5 6/0 · b3 4/0(글자 증감 0자) · 시나리오 22/0**. 회귀에서 새로 깨진 것은 **없습니다**.

### 1-5. ⚠ 실서버 5111 에 들어온 요청 (내가 일한 22:57 이후 전수)

로그를 그대로 셌습니다 — **요청 28개**: 사람(112.149.154.206) **GET 25 + POST 1**(23:00:15
`/api/component`) · **127.0.0.1 에서 `GET /login` 2개**(23:20:23 · 23:20:54).
그 두 개는 **읽기뿐이고 로그인도 하지 않았습니다**(쿠키 없는 200). 내 스크립트에는 5111 을 가리키는
줄이 없고(`runall` 은 `s5_live_guard.py` 를 **`part_a`+`part_c` 만** 부릅니다 — `part_b` 가 실서버 담당),
같은 꼴이 **22:03:01 · 22:31:52 · 22:48:32**(1차 작업 중 · 1차도 «붙지 않았다» 고 적었습니다)에도
있습니다. 그래도 **내 것이 아니라고 단정하지는 않겠습니다** — 쓰기는 **0** 입니다.

🔴 **그러나 이 자리는 위험합니다**(3차·구조 정리 쪽에 알림):
`scripts/ff.py:16` 이 `BASE = os.environ.get("LABELTOOL_URL", "http://127.0.0.1:5111")` — **기본값이
실서버**입니다. 그리고 `cycles/260918_paint/cycle_2/stage2b/reg_s2/s5_live_guard.py:142` 는 **명시적으로**
`base="http://127.0.0.1:5111"` 로 붙고(`part_b`), 그 파일을 `python s5_live_guard.py` 로 그냥 돌리면
`__main__` 에서 **`part_b()` 까지 실행**됩니다(GET 만이지만 로그인까지 합니다).
→ 권고: `ff.py` 기본값을 **쓰면 바로 죽는 값**(예: 없으면 `SystemExit`)으로 바꾸고, `part_b` 는
`sys.argv` 로만 켜지게 하십시오. 이번 사이클에서는 **공용 도구라 손대지 않았습니다.**

---

## 2. 5사이클을 가로질러 본 것 — 결정 표 · 어긋난 것 · 낡은 곳

### 2-1. 사이클별 3차 결정 (한 줄씩)

| 사이클 | 3차 판정 | 실서버 | 3차가 **다음에 넘긴 것** |
|---|---|---|---|
| 1 (공격) | 합격. 개수 칸·`human_count()` 한 곳·`counts.csv` 12칸·통합 manifest 6칸·상자 정의 과일별 문구 + 2차 즉시 수정 7 + 총괄 결정 4 | **16:41:06~09** → PID 263865 | 사이클 4 로 4건(`export_worker` 세 번째 읽기 · 옛 `n_inst` · `mk_reg_c4` 스냅샷 · 1px 상자) |
| 2 (보통) | 합격(대조표 1판 28칸). **코드 0** · 문서·대조표 정정 24곳 | 없음 | 사이클 3 공격 7지점 · 사이클 4 코드 목록(M1·M4·M7·M12 …) · 사람 M5·M8·M11 |
| 3 (공격) | 합격(대조표 확정판). 판정 뒤집기 4 · 문맥 이탈 4 · 쪽 번호 7 | 없음 | 사이클 4 에 10항목(M1·M17·M14·M4·M7·M12·M15·M13·회귀) · **보류 M2·M9·M18** |
| 4 (보통) | 합격. M1 팀 워터셰드 초벌 · M17 지표 스크립트 · 그림 3종 · 회귀 b1~b5 · 결함 20 | **21:45:04~07** → PID 964494 | 사이클 5 에 소수정 4 + 시험 위생 2 + 판정서·Codex·카톡 |
| 5 (종합) 1차 | 소수정 4(«원본 정답 번호» · 개수 안내 한 줄 · 통합 빌더 복숭아 · 빈 상자 json 건너뜀) + 위생 2 · **전 11 실패 → 후 22/0** | **안 켬**(3차 몫) | 새로 드러낸 것 6 · 판정서 재료 · Codex C 절 · 카톡 |

검수 방식(홀수 공격·짝수 보통·5 종합)은 다섯 사이클 전부 지켜졌고, 사이클 2·3 이 «코드 0» 인 것도
«한 파일 한 작업자» 규칙과 맞습니다. **다섯 사이클의 결정 자체가 서로 뒤집힌 곳은 없었습니다** —
어긋난 것은 아래 ①~⑧처럼 «결정은 같은데 문서·상태표·기대값이 따라오지 않은» 자리입니다.

### 2-2. 🔴 서로 어긋난 곳 8건 (내가 실측으로 확인한 것)

| # | 어긋남 | 어느 쪽이 옳은가 | 처리 |
|---|---|---|---|
| ① | **M2 상태** — 사이클2 3차·사이클5 §5-4 «✅ M1 으로 흡수» ↔ 사이클3 3차 «**보류**» ↔ `app/README.md` L1152~1155 «통일하지 **않았다** · 4-연결이 더 정확 · 아직 정하지 않았다» ↔ 실측 «포도 2,406장은 지금도 `cc4`» | **사이클3·README 가 옳다.** 워터셰드가 CC 를 대체한 것은 세 과일뿐이고 포도는 여전히 4-연결 CC 다 | §4 정정 표 — **M2 는 ✅ 가 아니라 🟡 부분**(완료 11 → **10** · 부분 1 → **2**) |
| ② | **포도 번호 초벌 켬/끔** — 결정은 «끔»(사이클4 2차·3차)인데 `README` L620·L1136~1143 이 «**네 과일 모두** 번호본이 먼저 · 폴백은 **한 번도** 걸리지 않는다» 라고 적고 있었다 | 결정(끔)이 옳고 **README 두 곳이 거짓 문장**이었다 | §7 — 내가 고쳤다(포도 2,406장은 `cc4` 실측을 넣음) |
| ③ | 같은 것 — `문서/260917_툴_사람확인_체크리스트.md` L58~59 가 사람에게 «**복숭아·포도는 번호가 없습니다**» 라는 풍선말을 찾으라고 한다. 실제 문구는 `ui.js:33` `NUM_WHY`(«③ 열매 번호는 «번호본이 있는 사진» 에서만 됩니다») | 코드가 옳다(`ui.js:31~32` 주석이 «이제 거짓이다» 라고 스스로 적어 두었다) | §7 — 내가 고쳤다 + 사이클5 3항목 추가(15 → **18항목**) |
| ④ | **«AI 초벌» 낱말** — 사이클5 ①이 사과만 «원본 정답 번호» 로 바꿨다. 복숭아·블루베리 번호는 여전히 «**AI 초벌** 번호» 인데 그 출처는 **박성문 님 워터셰드**(사람 도구)다 | 어느 쪽도 «거짓» 은 아니지만 README·help 는 «검출 팀» 이라 부르고 화면만 «AI» 라 부른다 | §8 — 3차 판단(선택지: «팀 초벌 번호» 로 통일 / 그대로) |
| ⑤ | **`counts.csv` 는 «나간 사진»** (사이클1 총괄 결정 2) ↔ **«사람 확정만» 의 뜻이 종류마다 다르다**(마스크=`confirmed` · 상자=`confirmed_boxes` · 번호=`confirmed_instances`). 한 체크박스가 세 가지를 뜻한다 | 규칙은 그대로 두는 것이 맞다(사이클4 3차 결정) | 사이클5 ②가 «개수» 쪽만 미리 말해 준다. **상자·번호 쪽은 아직 한 줄도 없다** → §8 |
| ⑥ | **어긋남이면 `n_human` 을 비운다**(총괄 결정 1) — 코드는 툴·빌더 양쪽이 같은데 **빌더 주석(L261)이 «①을 쓰되 …» 로 반대 말**을 하고 있었다 | 코드가 옳다 | §7 — 내가 주석을 고쳤다 |
| ⑦ | **포도 정답 송이 9,424 ↔ 9,832** 가 §5-5 **한 표 안에** 있다(표 줄 = 툴 2,403장 9,424 · 꼬리 설명 = 원본 2,502장 9,832) | 둘 다 실측이고 «장수 기준» 이 다르다 | §8 — 표 캡션에 한 줄(«툴 2,403장 기준 9,424 / 원본 전체 2,502장 기준 9,832») |
| ⑧ | 대조표 안의 판정 낱말 ↔ 집계 (1-a 칸 «일치» ↔ 집계 «부분» · 3-c «부분 해결» ↔ 집계 «어긋남» · 7 «일치 3편·불일치 2편» ↔ 집계 «부분») + 부록 C L251 «칸 10개 · 4-c 부분» ↔ 본표 «칸 12개 · 일치» | 집계·본표(11/7/6/2/1/1 · 12칸)가 최신 | §8 — 대조표는 **사이클3 3차가 확정판**이라 내가 손대지 않았다. 3차가 여섯 줄만 고치면 된다(목록 §8-2) |

### 2-3. 낡은 숫자·문장 전수 (파일 · 줄 · 옛 → 새)

전수 조사 결과입니다. **«고침»** 은 §7 에서 내가 고친 것, **«3차»** 는 넘긴 것입니다.

| 파일 | 줄 | 옛 | 새 | 처리 |
|---|---|---|---|---|
| `app/README.md` | 145 | 편집 화면 글자 «153 / 162 / 216자» | **162 / 267 / 225자**(`b3_chars` 실측 · 마스크는 사진에 따라 155~162) | 고침 |
| `app/README.md` | 197 | 복숭아 «번호 라벨이 없어 안 뜸» | «그때는 없었고 **0919 사이클4 부터는 번호본이 있다**» | 고침 |
| `app/README.md` | 619 | «번호가 있는 과일(사과·블루베리)» | 사과·블루베리·**복숭아** | 고침 |
| `app/README.md` | 620 · 1136~1143 | «네 과일 모두 · 4,823장 전부 · 폴백 한 번도 안 걸림» | 세 과일 · **포도 2,406장은 `cc4`** | 고침 |
| `app/static/help.html` | 7 | 최종 수정 2026-09-18 | 2026-09-19 | 고침 |
| `app/static/help.html` | 80 | «약 1,030자 → 약 120자» | «약 1,030자 → 150~270자(마스크 155·상자 267·번호 225)» | 고침 |
| `app/static/help.html` | 468 | 번호 편집 제목 «(사과 · 블루베리)» | «(사과 · 블루베리 · **복숭아**)» | 고침 |
| `app/static/help.html` | 데이터 정리 절 | «상자 0개» 문장 **없음** | «건너뜁니다» + «옛 0줄 txt 는 낡은 txt 로 센다» | 고침 |
| `문서/260917_툴_사람확인_체크리스트.md` | 2 · 4 · 7 · 58~59 | 최종 수정 09-18 · 15항목 · «복숭아·포도는 번호가 없습니다» | 09-19 · **18항목** · 실제 `NUM_WHY` 문구 | 고침 |
| `문서/260918_통합데이터셋_갱신법.md` | 116 · 118 · 120~124 | 복숭아 번호 **125** · 합계 **1,668** | **124** · **1,667**(22:55:23 사람 «제외» 확정 1장) | 고침 |
| `tools/build_merged_dataset.py` | 93 · README 생성부 | «0920 부터 복숭아 **125장**» | «번호본 125장 중 제외 확정을 뺀 수 · 실측 **124**» | 고침 |
| `tools/build_merged_dataset.py` | 261 | 주석 «어긋나면 ①을 쓰되 …» | 코드대로 «**비운다**(`conflict`)» | 고침 |
| `문서/260919_카톡_상자카운팅.txt` | 2 · 12 · 25 | «상자 16 · 번호 15 · 팀원 11»(예시 숫자) · «125장» · «아직 아무도 한 장도 확정 안 했습니다» | 측정값만 · «124장» · «22:55 에 한 장 확정(문제 있음) · 마스크·상자·번호는 0장» | 고침 |
| `문서/260918_Codex질문_{공격적,보통}.md` | C-3 8번 · C-6 | «사람 확정 0장» · 경로 `cycle_5/stage1_work.md` | 판정 확정 1장 · `cycle_5/stage1/stage1_work.md` | 고침 |
| `문서/260919_논문근거_대조표.md` | 73 · 87 · 100 · 152~153 · 251 · 312 · 325 | 1-a «일치» · 3-c·7 낱말 · 항목 요약 «툴 쪽 2건» · 부록 C «칸 10개» · «49건/46건» · «일치 15·부분 4» | 집계와 같게 · 3건 · 12칸 · 각주 | **3차**(확정판이라 손대지 않음) |
| `app/instances.py` | 60 | `SEED_SOURCE` 에 `"grape": "certh_gt"` 가 남아 있다 | **낡은 것이 아님** — 되살릴 때 쓰는 값이고 L41~53 주석이 설명한다 | 그대로 |

---

## 3. 1차가 «새로 드러낸 것 6» — 하나씩 판정

| # | 1차가 적은 것 | 내 실측 | 판정 |
|---|---|---|---|
| **1** | 소수정 ④ 가 만든 새 불일치 — 화면 «나갈 상자 1장»(파일이 있나만 본다) ↔ 실제 txt 0장. 맞추려면 상자 json 을 다 열어야 해서 느려진다. 어긋나는 사진은 시험 잔재 2개뿐 | 1차 판단이 맞습니다(`box_set()` 은 파일 이름만 훑습니다). **그러나 1차가 못 본 쪽이 더 큽니다** — §7-1: `/box` 내보내기는 과일마다 **고정 폴더**(`data/<과일>/boxes_yolo/`)라 **옛 판이 만든 0줄 txt 가 그대로 남고**(실서버에 실제로 2개 있습니다) `stale_txt` 도 그것을 세지 않았습니다 | **절반은 «지금 고칠 것»** — 파일이 남는 쪽은 내가 고쳤습니다(전 3/2 → 후 5/0). 화면 precount 쪽은 **사이클 뒤**(권고: 지금 그대로 · 사람이 잔재 2개를 지우면 사라진다) |
| **2** | 재시작 전까지 화면 문장(«건너뛴 사진») ↔ 옛 `boxes.py`(0줄 txt 를 만든다) | 맞습니다. `c5_fix.py --before` 로 재현됨(내 재현에서도 ④ 3건이 «전» 판에서 실패). 재시작하면 사라집니다 | **3차가 켜면 해소** — §9 에 그대로 옮김 |
| **3** | 시험 4개가 다른 세션의 백업 이동으로 죽는다. 1차는 **자기 사본만** 고쳤고 정본은 아직 죽는다 | **재현했습니다.** 정본에서 새로 뜬 사본으로 돌리면 c4 `t2_ui` 가 `FileNotFoundError`(`app/_backup_260918_c4_server.py`)로 죽고, c2-2차 `s1_api`·`s5_live_guard`·`s8_guard_advance`·c2-1차 `t4_paint8` 도 빈 결과였습니다. 🔴 **1차가 «정본은 `cycle_2/stage2b` 쪽» 이라고만 적었는데 `260918_paint/cycle_4/stage1/lib_c4.py` 도 정본이고 그것도 죽습니다** | **즉시 고칠 것** — 정본 4파일에 «없으면 아카이브에서 읽기» 폴백을 넣었습니다(§7-3). 고친 뒤 정본→새 사본에서 `t2_ui` rc=0 |
| **4** | `t6_view` 남은 실패 4건은 문턱(0.5%) 문제이고 코드 결함이 아니다 | 재현 **139 / 4**(1차와 같은 수). 실패 내용도 «바뀐 화소 ≥ 0.5%» 한 종류 | **동의 · 사이클 뒤**(권고: 문턱을 전경 비율 대비로 바꾸거나 전경이 큰 사진을 고정. 지금 흔들리지는 않으니 급하지 않습니다) |
| **5** | `instances_source` 가 한 폴더에 두 이름(블루베리 `detect_seed` · 복숭아 `psm_watershed`) | 코드 확인: `SEED_LABELS = {"blueberry": "detect_seed", "peach": "psm_watershed"}`. **그 값으로 갈라지는 소비 코드는 없습니다**(`grep` — 빌더와 문서뿐). 값 사전도 README 생성부에 있습니다 | **사람 결정 · 지금 그대로가 낫다**(권고). 값을 바꾸면 지난 빌드와 대조가 끊깁니다. 대신 «추정 집합» 이 필요해지면 코드에 `ESTIMATED = {"detect_seed", "psm_watershed"}` 한 줄을 두는 것으로 충분합니다 |
| **6** | 복숭아 `instances/` 가 «추정» 인 것을 **코드가 강제하지 않는다** — 학습이 폴더만 보면 추정이 정답이 된다 | **지금은 위험이 실현될 자리가 없습니다**: `semseg/`·`configs/`·`scripts/` 어디에도 `datasets_merged` 를 읽는 줄이 **0건**(실측 `grep`). 통합본을 읽는 것은 `tools/` 의 빌더·시험·`count_metrics.py` 뿐이고 `count_metrics.py` 는 manifest 칸으로 읽습니다 | **사이클 뒤 + Codex C-1 그대로** — 1차 판단이 맞습니다. 0920 전에 값싸게 할 수 있는 차선은 «빌더가 `<과일>/instances/` 에 «추정» 한 줄을 파일로 같이 쓰기»(3줄)입니다 → §8 선택지 |

---

## 4. 판정서 재료 검증 — 정정 표

### 4-1. 결함 35건 표 · 표본 10건 (1 · 8 · 9 · 12 · 15 · 18 · 22 · 27 · 31 · 35)

**10건 전부 실재합니다**(사이클 보고서에 기술 + 코드에 실물). 근거 몇 줄만:
`instances.py:331 count_fresh()`(#1) · `boxes.py:108~110 conflict`(#8) · `export_dataset.py:240·281`(#9) ·
`instances.py:54~56 SEED_DIRS`(#12) · `server.py:1809~1835 st_snap`(#15) · `boxes.py:23 MIN_SIDE`(#18) ·
`app.js:1691 typeof window` 가드(#22) · `ui.js:1228 needPhoto`(#27) · `instances.py:41~56` 포도 끔(#31) ·
`boxes.py:221~233 continue`(#35).
⚠ 이 줄 번호는 **검증한 때(23:10~23:40) 기준**입니다 — 내가 그 뒤 `boxes.py`·`ui.js` 를 더 고쳐
몇 줄 밀렸습니다(§10 에 지금 줄 수를 적어 두었습니다).

정정 **3건**:

| # | 1차 표현 | 실제 | 조치 |
|---|---|---|---|
| 27 | «내보내기 **가드가 상자 장수를 따로 본다**» | 가드가 고친 것은 «**사진(마스크·번호)이 필요할 때만 막는다**»(`needPhoto`)이고, «상자 장수를 따로 보는» 것은 가드가 아니라 문구 함수 `expOutText`(`ui.js:1166`) | 판정서 문장 교체 |
| 7 | «앞 사이클 시험의 낡은 기대값» 이 **코드 결함 35건**에 들어 있다 | 그것은 시험 파일(`260918_paint/cycle_3/stage1/t2_ui.py`) 기대값 수정이고, `cycle_1/stage3_final.md` 는 «즉시 수정 **6**» 으로 세었습니다(그래서 6 vs 7 이 생깁니다) | 35 는 그대로 두고 캡션에 «시험 기대값 1건 포함(코드 34 + 시험 1)» |
| — | «출처: 각 사이클 판정서» | 판정서에는 «11»·«20» 이라는 수가 **없습니다**(«즉시 수정 6»·«1차 10항» 뿐) | 출처를 «§5-2 표를 세어 만든 값» 으로 |

### 4-2. 대조표 집계 28칸 재계수

집계표(§2 · 일치 11 · 부분 7 · 어긋남 6 · 근거 없음 2(+1) · 오지정 1 · PDF 없음 1 = **28**)는
**칸 수도 28 이고 산술도 맞습니다**(항목 1:6 · 2:6 · 3:3 · 4:3 · 5:5 · 6:4 · 7:1). 인용 «51건» 도
«칸 × citekey × 쪽» 으로 세면 53 − (3-c 의 인용 없는 참조 2) = **51** 로 재현됩니다.
**다만 판정 «낱말» 로 세면 일치가 12 가 되어 3칸이 어긋납니다**:

| 칸 | 집계표 | 그 칸에 적힌 낱말 | 무엇이 맞나 |
|---|---|---|---|
| **1-a** | 부분 | **«일치»(절차 4단계)** 가 아직 남아 있다 | 집계·부록 G·부록 H 가 «부분» 이므로 **칸을 고쳐야 한다** |
| 3-c | 어긋남 | «부분 해결» | 집계 쪽에 «(부분 해결)» 단서가 이미 있음 — 낱말만 맞추면 된다 |
| 7 | 부분 | «일치 3편 · 불일치 2편» | 같은 뜻 — «부분(3편 일치·2편 불일치)» 로 |

같은 문서 안의 낡은 자리 **6곳**(3차가 고칠 것): 1-a 의 «다른 점 **4개**» ↔ ①~⑤ 다섯 개(L132·L341 은 «5개») ·
부록 C L251 «칸 **10개** · 4-c 부분»(본표는 12칸·일치) · 항목 요약 L152 «툴 쪽 **2건** 어긋남»(1-e 가 내려와 **3건**) ·
L153 «`counts.csv` 칸 부족»(해소됨) · 부록 F L325 «일치 15 · 부분 4»(각주 필요) · 부록 F L312 «49건/46건»(각주 필요).
부록 A 확인 명령에 `boatswainjacques2021shallot`·`maierhein2024metrics` 두 편이 빠져 있고, 5-d 에 2차가 넣은
`kapoor2023leakage` 인용까지 세면 **52** 가 될 수 있습니다(«51» 의 셈 규약을 한 줄로 적어야 확정됩니다).

### 4-3. M1~M19 상태표

✅ 로 적힌 11개 중 **10개는 코드·문서에서 확인**됩니다(M1·M3·M4·M7·M10·M12·M13·M14·M15·M17 —
M12 는 그림 3종 mtime **18:50 / 20:27** · 근거 PDF **18:50** 로 실물 확인, M15 는 지정한 **3곳 전부** 확인,
M3 의 `COUNTS_COLS` 는 정확히 **12칸**).

🔴 **M2 만 «✅» 가 과합니다.** 지정된 이유 문장(«정확해서가 아니라 도구를 일치시키려고»)은
`app/README.md` 에 **없고**(대조표의 «README 에 적는다» 라는 **미완 조치칸**에만 있습니다),
그 README 는 오히려 «통일하지 **않았다** · 4-연결이 더 정확 · 아직 정하지 않았다»(L1152~1155)라고 적으며,
**포도 2,406장은 지금도 4-연결 CC(`cc4`)** 를 씁니다(실측).
→ **M2 = 🟡 부분.** 집계는 «✅ 완료 **10** · 🟡 부분 **2**(M2·M6) · 🧑 6 · ⏭ 1» 이 정확합니다.
(표 자체의 ✅ 표시는 12행입니다 — M16 이 ✅+🧑 두 표시를 같이 달고 있고, 집계는 M16 을 사람 쪽으로만 셉니다.
그 규칙이 괄호에 적혀 있어 19 = 11+1+6+1 은 성립합니다.)

### 4-4. 지표 표 — `count_metrics.py` 재실행

`cycle_4/stage2/counts_off/{apple,peach,grape,blueberry}` 로 다시 돌린 결과가 §5-5 표와
**열 하나 값 하나까지 같습니다**(복숭아 0.30/0.73/0.986 · 사과 2.14/3.09/0.985 · 포도 +54.35%/−1.406 ·
꼬리줄 «읽은 줄 4,071 중 뺀 줄 1,114 · 표 4개» · ⚠순환은 사과 «열매 번호» 한 줄).
⚠ 명령 한 가지: `--counts` 는 `nargs="+"` 라 **폴더 네 개를 한 번에** 줘야 합니다
(`--counts A --counts B …` 로 주면 **마지막 하나만** 읽고 «표 1개» 가 나옵니다 — 1차 §9 (6)번 명령은
복숭아 한 폴더만 주는 예라 그대로 돌리면 표가 1개입니다).

🔴 **그런데 그 표의 복숭아 세 줄은 이미 낡았습니다**(아래 4-5).

### 4-5. 🔴 22:55:23 — 사람이 툴을 쓰기 시작했습니다 (1차 보고서 뒤 89초)

| 실측 | 값 |
|---|---|
| `data/peach/status.json` mtime | **2026-09-19 22:55:23** (다른 세 과일은 09-17·09-18 그대로) |
| 그 줄 | `210629-t2-of13-03` → `confirmed {status: "flag", by: "익명", at: "2026-09-19 22:55:23"}` |
| 서버 로그 | `112.149.154.206 … 22:55:23 "POST /api/status"` (그 앞 22:54:56 에 그 사진을 열었습니다) · 23:00:15 에 `POST /api/component` 한 번 더 |
| 툴 dry-run(내 모래상자) | 복숭아 «전체 125장 → **포함 124** (제외 1 — 상태 1)» · 개수 표 **124줄** |
| 실제 통합 빌드(내 모래상자) | manifest 125행 중 `has_instances=1` 이 **124** · `instances/` PNG **124** · 그 한 줄은 `action=excluded_flag` |

그래서 판정서 재료의 숫자 **여섯 곳**이 바뀝니다:

| 자리 | 1차(09-19 22:13~22:53 실측) | 지금(09-19 23:01~09-20 00:02 실측) |
|---|---|---|
| 통합본 복숭아 «열매 번호» | 125 | **124** |
| 통합본 번호 합계 | 1,543 → 1,668 | 1,543 → **1,667** |
| 지표 표 복숭아 세 줄 | n 125 · 정답 977 · 센 939 · −3.89% · MAE **0.30** · RMSE 0.73 · R² 0.986 | n **124** · 정답 **975** · 센 **937** · **−3.90%** · MAE **0.31** · RMSE **0.73** · R² **0.986** |
| 지표 표 꼬리줄 | 읽은 줄 4,071 | **4,070** |
| 숫자 사전 #11 «사람 확정 0건» | 0건 | **판정(flag) 확정 1장** · 마스크·상자·번호 확정은 여전히 0장 |
| 카톡 «아직 아무도 한 장도 확정 안 했습니다» | — | 거짓이 됨 → 고쳤습니다(§7-6) |

**이것은 1차의 잘못이 아닙니다**(22:54 작성 → 22:55:23 사람 조작). 판정서에는 **«언제 잰 값인가» 를
반드시 붙여야** 하고, 0920 빌드 때 **다시 세어야** 합니다 — 사람이 «제외» 를 더 누르면 또 줄어듭니다.

### 4-6. 숫자 사전 18줄

**일치 12줄**(#2·3·4·5·6·7·12·13·15·16·18 + #14 는 내가 직접 재현 — §1).
- #4 «143편» 은 실측으로도 맞습니다(`01_references/pdf/` 에 파일 145개 중 **`*.pdf` 143개** · 나머지 2개는 `*_초록만.txt`. 1차의 «145 오기» 원인까지 설명됩니다).
- #12 PID 964494 · 21:45:04 는 지금도 `ps` 로 그대로입니다.

정정 **2줄 + 출처 표기 4줄**:

| # | 1차 | 실측 | 조치 |
|---|---|---|---|
| **11** | 사람 확정 **0건** | **1건**(판정 확정) | §4-5 |
| **17** | ui.js 1,326 / app.js 2,664 / boxes.py 482 / 빌더 2,583 | 1차 시점에도 ui.js **1,328** · boxes.py **491** · 빌더 **2,588** 였고, **내가 고친 뒤 지금은 1,337 / 2,664 / 491 / 2,591** 입니다. 출처 경로 «`platform/semantic-segmentation/…`» 은 **없는 경로**입니다(정본은 `kds0206/semantic-segmentation/tools/…`) | 숫자·경로 둘 다 정정 |
| 1 | «출처 = 각 사이클 판정서» | 판정서에 그 수가 없음 | §4-1 |
| 8·9·10 | «출처 = `counts_metrics_roundtrip.md`(같은 표)» | 그 파일은 **복숭아 3줄·125장 왕복 확인표**이고 꼬리줄이 «읽은 줄 125 · 뺀 줄 0» 입니다. 10줄 표의 실제 출처는 `cycle_4/stage2/metrics_off.md` | 출처 교체 |
| 16 | «205개 / 4곳» | 205개 ✓. «4곳» 은 **사이클5 가 돌리는 범위**의 수이고, `_archive/backups_260920/INDEX.md`(23:04)는 전 사이클 기준 «죽는 시험 **45개**» 라고 적습니다 | 범위를 밝혀 적기 |

### 4-7. 교수님 질문 6 + 박성문 3

원문 인용·쪽은 **전부 대조표에 그대로 있습니다**(MinneApple **p.4** «…the ones on the ground and trees in
the background were not tagged» · `seo2024peach` **p.5** «except for dropped fruits» · **p.11** amodal 명문 ·
`farjon2023countingreview` **PDF p.16 = 인쇄 p.1698** 식 (1)~(4) · `santos2020grapetracking` **p.2** ·
`maierhein2024metrics` **p.204**(arXiv 판) · `boatswainjacques2021shallot` **p.5** · `demsar2006` p.1).
🔴 **다만 쪽 번호 세 개는 논문에 그대로 옮길 수 없습니다** — MinneApple p.4(프리프린트) ·
`seo2024peach` p.5(심사용 판) · `maierhein2024metrics` p.204(arXiv). M19 가 논문 인용 **전에** 끝나야 합니다.

문장 정정 **4건**:

| 항목 | 1차 문장 | 정정 |
|---|---|---|
| 교수님 1 | «MinneApple … amodal **150개체 중 92**» | 그 값은 **«의심이 큰 상위 150개체» 라는 치우친 표본**입니다(출처는 `문서/260917_교수님확인_7건.md`). 사이클2 2차·사이클3 1차가 «치우친 표본을 실측으로 단정하지 말라» 고 못박았으므로 **«의심 상위 150개체 표본 중 92»** 로 적어야 합니다 |
| 교수님 3 | «143편에 `other` 를 **쓴 사례가 0건**» | 근거는 정규식 4종 전수 검색의 **부재 증명**입니다 → «**143편 전수 검색에서 선례를 찾지 못했습니다**» |
| 교수님 4 | «낙과가 보이는 **42장 중 5장만** 라벨» | 원문은 «**2차 검수의 표본 값**» 이라는 한정을 달았습니다(전수가 아님) — 그 한정을 살려야 합니다 |
| 박성문 ⑨ | «**그 폴더**(`…/instance_maps`)의 `FALLBACK["grape"]="cc"` 주석» | `instance_maps` 는 PNG 125장짜리 산출물 폴더이고, `FALLBACK` 은 **`park_seongmoon/semantic-segmentation/tools/instance_split.py:47`** 에 있습니다. 또 사이클3 2차가 요구한 «CC 는 차선이고 옳은 세기가 아니다(+62.1%)» **주석은 아직 없습니다** → 요청 문구를 그 파일·그 줄로 바꿔 적을 것 |

교수님 2(닿은 송이 31.4% · 조각 43.5% · CC +62.1% · WS +123.5%) · 교수님 5(4,823개 파일 **내용까지** 동일 ·
inode 는 다름 = 복사본) · 박성문 ⑦(블루베리 1,114장)은 **원 실측과 정확히 같습니다**.

---

## 5. Codex C 절 · 카톡 검토

### 5-1. C-3 «검증되지 않은 주장·가정 12개» — 성격이 섞여 있습니다

12개를 하나씩 보면 **두 종류**가 섞여 있습니다.

| 갈래 | 번호 | 왜 |
|---|---|---|
| **측정은 끝났고 «판단·사람 확인» 이 남은 것** (7) | 1 · 5 · 6 · 7 · 10 · 11 · 12 | 숫자가 전부 **실측**입니다 — 5(사과로 튜닝 · 복숭아 최적 0.7 이하 = 사이클3 이 절반 200회·5-fold 세 seed 로 재현) · 6(모든 개수가 `mask_source=gt` · FruitSeg30 −72%) · 7(4,823개 파일 **내용까지** 동일 · inode 만 다름) · 10(상위 150개체 표본 중 92 · MinneApple 원문에 명문 없음) · 11(42장 중 5장 — 표본) · 12(사이클5 1차 자신이 만든 불일치) · 1(«그런 자리가 있는지» 만 미확인) |
| **아직 확인하지 못한 것** (5) | 2 · 3 · 4 · 8 · 9 | 2(사과 번호가 정답이라는 근거가 우리 관찰뿐) · 3(`n_gt` = `detections.csv` 줄 수를 «과일 개수» 로 써도 되나 — 원저자 확인 없음) · 4(⚠순환이 논문 표에 섞일 위험) · 8(사람 확정 경로가 실자료로 안 돌았다) · 9(포도를 끈 채 포도 지표를 싣는 것이 일관적인가) |

→ **«검증 안 된 것» 이라는 제목 그대로 두면 Codex 가 이미 잰 숫자를 다시 재게 됩니다.**
빼기보다 **갈래를 밝히는 것**이 정보를 잃지 않으므로, 두 파일의 C-3 머리에 한 줄을 넣었습니다
(«①⑤⑥⑦⑩⑪⑫ 는 측정이 끝나고 판단이 남은 것 · ②③④⑧⑨ 가 아직 확인 못 한 것 — 공격은 뒤쪽 먼저»).
8번은 **사실이 바뀌었으므로** 문장 자체를 고쳤습니다(§7-7).

### 5-2. C-5 질문 다섯 개 — 답할 수 있는 형태인가

| 질문 | 답할 수 있나 | 비고 |
|---|---|---|
| C-1) `instances/` 를 쓰면서 `instances_source` 를 안 읽는 자리를 파일·줄로 | ✅ 답할 수 있다. **그리고 답은 «지금은 없다»** 입니다(`semseg/`·`configs/`·`scripts/` 에 `datasets_merged` 0건 — 실측). 질문에 «없으면 «없다» 고 답해도 된다» 를 넣어 주면 더 낫습니다 |
| C-2) `KNOWN_LOOP` 가 못 잡는 순환을 하나 만들어 보라 | ✅ 아주 좋은 형태(반례를 만들라 = 검증 가능) |
| C-3) 소수정 ④ 가 숫자를 어긋나게 하는 다른 자리 | ✅ 답할 수 있다. **2차가 이미 하나 찾았습니다**(고정 폴더에 남는 옛 0줄 txt — §7-1). 그 답을 미리 적어 두면 중복을 막습니다 |
| C-4) 소수정 ①의 조건이 거짓말하는 경우(4가지) | ✅ 형태 좋음. `has_numbers()` 가 «앞 3장만 보는 어림값» 이라는 단서까지 붙어 있어 점검 지점이 분명합니다 |
| C-5) 대조표 «일치» 11칸 중 하나를 뒤집어라 | ⚠ 조건은 명확하지만(원문 인용 + 코드 줄) **1-a 는 이미 «부분» 으로 내려갔는데 칸에 «일치» 가 남아 있어**(§4-2) Codex 가 «일치 12칸» 으로 셀 수 있습니다 → 3차가 그 칸을 고친 뒤 질문을 보내야 합니다 |

### 5-3. 카톡 25줄

| 보는 것 | 판정 |
|---|---|
| 줄 수 | **25줄** ✓(내가 고친 뒤에도 25줄) |
| 표·마크다운·이모지 | **없음** ✓(`#`·`*`·`|`·백틱·화살표·이모지 0건 · 가장 긴 줄 61자) |
| 말투 | 담백한 평서문 · 조언체·소제목 없음 ✓ |
| 숫자 | 🔴 세 곳이 어긋나 고쳤습니다(§7-6): «상자 16 · 번호 15 · 팀원 11» 은 **지시서의 예시 숫자**이고 측정값이 아님 · «125장» → **124장** · «아직 아무도 한 장도 확정 안 했습니다» → 22:55 에 한 장(문제 있음) |
| 남겨 둔 숫자 | 0.70 → 0.30 · R² 0.986 · 사과 2.14 / 0.985 / 429장 · 포도 54% · 143편 · 28칸 11/7/6 — **전부 실측**이고 그대로 둡니다(복숭아 지표는 124장 기준으로 다시 재면 0.31 이지만, 카톡의 «0.70 → 0.30» 은 **같은 125장에서 잰 전/후 비교**라 그 문장은 참입니다. 3차가 판정서 표를 124장으로 고치면 카톡도 0.31 로 맞추는 편이 낫습니다) |
| «복숭아 25장부터 시작하겠습니다» | 1차가 쓴 계획 문장입니다(측정값 아님) — 그대로 두었습니다 |

---

## 6. 처음 온 사람 시나리오 — 진짜 파이어폭스 1366×768 (모래상자 `sandbox_scn` · 포트 5486)

`scn_newcomer.py` 로 지시 5 순서를 그대로 밟았습니다. **고치기 전 12/5 → 고친 뒤 22/0.**

| 단계 | 화면이 실제로 한 말 | 판정 |
|---|---|---|
| ① 복숭아 `/box` 열기 | 개수 «**개수 -·-·11**» · 하단 «AI 초벌 상자 11개 — 맞으면 Enter» · 초벌 11개가 이미 깔려 있고 «✨ 초벌» 을 눌러도 11개 그대로 | **O** |
| ① Enter | 개수 «**개수 11·-·11**» · 칸이 **`cnts hum` = 초록**(`style.css` `#d8f3e1` 바탕 / `#1d6b3c` 글자 · 대비 **5.54:1**) · 하단 «✔ 상자 확정 — 원본 그대로 OK (익명)» | **O** — 사이클1 3차가 «사람 눈으로 볼 것» 으로 남긴 «초록으로 바뀌는가» 를 이것으로 답합니다(낱말이 아니라 **색**으로 말합니다) |
| ② 사과 ③ 번호 | 하단 «**원본 정답 번호 95개 — 맞으면 Enter**» · «AI 초벌 번호» 없음 · 풍선말 «이 번호 는 AI 가 만든 것이 아니라 «원본 마스크에 들어 있는 정답 번호»입니다(고칠 일이 거의 없습니다) …» | **O**(풍선말의 `**` 마크다운은 §7-2 에서 고친 뒤) |
| ③ 데이터 정리 · 개수+상자 · 사람 확정만 | «지금 확정된 사진 세그 0 · 상자 1장 … 지금 조건으로 나갈 사진 0장» + 줄바꿈 + «**개수 csv 는 마스크를 확정한 사진만 담깁니다 — 상자만 확정하면 빈 표**» | **O** |
| ④ 서버에 저장 | 이름을 한 번 묻고 → «**0장 — 나갈 것이 없습니다 …**» 로 **막힙니다**. 🔴 «세그 마스크» 는 **기본으로 켜져** 있어 처음 온 사람은 그것을 끄지 않습니다. 사이클4 2차가 잡은 그 자리의 **남은 반쪽**이고, 안내문이 «편집 화면에서 Enter 로 확정하라» 고 **방금 한 일을 또 하라고** 합니다 | **△ → 고친 뒤 O** — 규칙은 그대로 두고 «**«세그 마스크» 의 확정이 0장이어서 막았습니다 — 그 체크를 빼면 상자·개수만 나갑니다**» 를 붙였습니다(§7-4). 빈 날짜 폴더는 생기지 않습니다 |
| ④-b 마스크 체크를 빼고 다시 | job 폴더 `260919_233956_peach` 가 생기고 `counts.csv` 가 나옵니다 — **0줄**(상자만 확정했으므로 · 총괄 결정 2 그대로) | **O / △** — 규칙대로지만 처음 온 사람은 **빈 표**를 받습니다. ③ 의 안내 줄이 그것을 미리 말하므로 O 로 봅니다 |
| ⑤ `count_metrics.py` | rc=0 · «— | 정답(`n_gt`)이 있는 줄이 없습니다 | 0» 과 설명 네 줄 · 조용히 죽지 않습니다 | **O** |

**O 20 / △ 2 / X 0** (△ = ④ 에서 한 번 막히는 것 · 그때 받는 `counts.csv` 가 0줄인 것. 둘 다 «규칙» 이고
화면이 이제 그 이유를 말합니다.)

---

## 7. 즉시 고친 것 (백업 `_backup_260919_cnt5b_<파일명>` · 전 실패 → 후 통과)

| # | 무엇 | 어디 | 증거(전 → 후) |
|---|---|---|---|
| **7-1** | 🔴 **소수정 ④ 가 반만 닫혀 있었다** — 새 0줄 txt 는 안 만들지만 **옛 판이 만든 0줄 txt 는 그 폴더에 그대로 남고** `stale_txt` 도 그것을 세지 않았다(그 사진의 json 은 살아 있으니까). `/box` 내보내기는 과일마다 **고정 폴더**(`data/<과일>/boxes_yolo/`)라 한 번 생긴 파일은 계속 남는다. **실서버 실측: `data/peach/boxes_yolo/210629-t4-17.txt` · `data/grape/boxes_yolo/740.txt` 둘 다 0바이트로 존재.** 화면은 «건너뛰었다» 고 말하는데 폴더에는 «열매 없음» 학습 라벨이 그대로 있었다 | `app/boxes.py` `export_boxes_to()` 낡은 txt 계산 (+10/−1 · 주석 포함) — **건너뛴 사진의 옛 txt 도 «낡은 txt» 로 센다. 지우지는 않는다**(N4 규칙 그대로) | `cycle_5/stage2/c5b_stale.py` — **전 3/2 → 후 5/0** (`stale_txt 0 → 1` · 이름 `BB.txt`) |
| **7-2** | 풍선말에 **마크다운 `**` 가 그대로 새어 나왔다**(`title` 속성이라 별표가 글자로 보인다) | `app/static/ui.js` `taskConfLine()` — «**원본 마스크에…**» → ««원본 마스크에…»» (2자 줄었다) | 시나리오 ②-3 «풍선말에 `**` 가 없다» 전 실패 → 후 통과 |
| **7-3** | 🔴 **정본 시험 4파일이 다른 세션의 백업 이동으로 죽는다** — 1차는 자기 사본만 고쳤고, 1차가 «정본» 으로 지목한 곳(`cycle_2/stage2b`) 말고 **`260918_paint/cycle_4/stage1/lib_c4.py` 도 정본이고 그것도 죽었다** | `cycles/260918_paint/cycle_4/stage1/lib_c4.py` `sync(old=True)` · `cycles/260918_paint/cycle_2/stage2b/{reg_s2/lib_s2.py, reg_s1/t4_paint8.py, reg_s0/s5_fix.py}` — 전부 «없으면 `_archive/backups_260920/` 에서 읽기». **시험이 무엇을 보는지는 한 글자도 안 바꿨다** | 정본에서 **새로 뜬** 사본으로: c4 `t2_ui` **`FileNotFoundError` → rc=0 (27/0)** · 회귀 묶음 4개도 살아남(§1 표) |
| **7-4** | 🔴 **처음 온 사람이 내보내기에서 막히는데 이유를 말하지 않았다** — «세그 마스크» 가 기본으로 켜져 있어 상자만 확정한 사람은 «0장 — 나갈 것이 없습니다 … **Enter 로 확정하거나**» 를 받는다(방금 Enter 를 눌렀는데) | `app/static/ui.js` 내보내기 가드 (풍선말 한 곳까지 합쳐 ui.js 는 +14/−3) — 규칙은 그대로, ««세그 마스크» 의 확정이 0장이어서 막았습니다 — 그 체크를 빼면 상자·개수만 나갑니다» 를 덧붙임 | 시나리오 ④-1′ 전 실패 → 후 통과 · ④-b 에서 체크를 빼면 job 폴더·`counts.csv` 가 나옴 |
| **7-5** | **빌더 주석이 자기 코드와 반대 말**을 했다 — «어긋나면 ①을 쓰되 `count_conflict=1`»(총괄 결정 1 전의 규칙) | `tools/build_merged_dataset.py` L261 주석 | 코드(`count_cells` 의 `conflict` 갈래 = 비운다)와 일치시킴 · `py_compile` OK |
| **7-6** | **낡은 숫자·거짓 문장** — 사람이 22:55:23 에 누른 «제외» 를 반영 | `문서/260918_통합데이터셋_갱신법.md`(§5① 표 · 최종 수정 줄) · `tools/build_merged_dataset.py`(L93 주석 · README 생성부) · `문서/260919_카톡_상자카운팅.txt`(3줄) | 125 → **124** · 1,668 → **1,667** · «아무도 확정 안 함» → «22:55 에 한 장(문제 있음) · 마스크·상자·번호는 0장» · 카톡 **25줄 유지** |
| **7-7** | Codex 두 파일 — C-3 8번이 «사람 확정 0장» 이라 적고 있었고, C-6 이 **없는 경로**(`cycle_5/stage1_work.md`)를 가리켰다 | `문서/260918_Codex질문_공격적.md` · `_보통.md` — **C-3′ 정정 절**(숫자 세 개 + 소수정 ④ 반쪽) 신설 · 8번 문장 정정 · C-3 읽는 법 한 줄 · 경로 정정 | 두 파일 같은 자리에 같은 내용 |
| **7-8** | **문서 세 개의 낡은 줄** — README 620·1136~1143(«네 과일 모두 / 폴백 한 번도 안 걸림») · README 145(글자 수) · 197 · 619 · help 7·80·468 · 데이터 정리 절(상자 0개 문장 없음) · 체크리스트 2·4·7·58~59(«복숭아·포도는 번호가 없습니다») | `app/README.md` · `app/static/help.html` · `문서/260917_툴_사람확인_체크리스트.md` | §2-3 표 · help 태그 균형 확인(div 32/32 · p 44/44) · 체크리스트 **18항목**(사이클5 3항목 추가: 사과 «원본 정답 번호» · 개수 csv 안내 줄 · 상자 0개 건너뜀) |

**시험 쪽 위생 2건**(내 사본만 · 정본 아님): `c5_fix.py` ③ 과 `b2_export.py` ③-c 가 **«125» 를 박아 두어**
사람이 «제외» 를 한 번 누를 때마다 깨졌습니다 → 툴 자신이 말하는 «남긴 장수 / 나갈 장수» 와 맞추는
방식으로 바꿨습니다(전 18/4 · 27/1 → 후 22/0 · 28/0). **3차는 이 방식을 1차 사본에도 옮기십시오.**

만지지 않은 것: 실서버 5111(PID 964494) **재시작 0회 · 로그인(POST) 0회 · 쓰기 0** ·
교수님 5100·5101·5105 **0회** · ⚠ 127.0.0.1 에서 온 `GET /login` 두 개(23:20:23·23:20:54)는
**내 것이 아니라고 단정하지 않습니다**(§1-5 — 읽기뿐이고 로그인도 하지 않았습니다) ·
공용 `T/data`·`T/exports` **쓰기 0**(`T/exports` 는 여전히 없습니다) · `tests/`·`_archive/` **읽기만** ·
데이터셋·검수판·통합본·팀원 폴더·`01_references` **읽기만** · 삭제 0 · GPU 0 · 새 패키지 0 · `pkill` 0 ·
`작업기록.md` 갱신 0.

---

## 8. 3차에 넘길 것 (설계 판단 — 선택지 + 권고)

### 8-1. 판정서를 쓰기 전에 **반드시** 반영할 것 (숫자)
1. **복숭아 124 · 합계 1,667 · 지표 표 복숭아 세 줄(0.31 / 0.73 / 0.986 · n 124 · 정답 975) · 꼬리줄 4,070 ·
   «사람 확정» 0 → 판정 확정 1장.** 그리고 **모든 숫자에 «언제 잰 값인가» 를 붙이십시오** — 사람이
   툴을 쓰기 시작했으므로 0920 빌드 때 다시 세야 합니다.
2. **M2 = 🟡 부분**(완료 10 · 부분 2). 이유: 포도가 여전히 `cc4`, 그리고 «도구를 일치시키려고» 문장이
   README 에 없습니다. 넣을지(문서 1줄) 아니면 상태만 내릴지 3차가 정하십시오 — **권고: 상태를 내리고
   README 의 «보류» 문장을 그대로 둔다**(사실이 그렇습니다).
3. 결함 표 캡션에 «**코드 34 + 시험 기대값 1 = 35**» · #27 문장 교체 · 숫자 사전 #1·#8·#9·#10·#16·#17 출처 교체.
4. 교수님 질문 문장 4건 정정(§4-7) — 특히 «150개체 중 92» 에 «의심 상위 표본» 을 꼭 붙이십시오.
   사이클2 2차·사이클3 1차가 «치우친 표본을 실측으로 단정하지 말라» 고 판정한 값입니다.

### 8-2. 대조표(확정판) — 여섯 줄만 (내가 손대지 않았습니다)
1-a 판정 칸 «일치» → **«부분»** · 1-a 의 «다른 점 4개» → **5개**(또는 ①~⑤ 를 4개로 줄이기) ·
3-c «부분 해결» 옆에 «(집계에서는 어긋남)» · 7 «부분(3편 일치·2편 불일치)» ·
부록 C L251 «칸 10개 · 4-c 부분» → **12칸 · 일치** · 항목 요약 L152 «2건» → **3건** · L153 «칸 부족» 삭제 ·
부록 F L312·L325 에 «이후 3차가 51건 / 11·7 로 바꿨다» 각주 · 부록 A 에 `boatswainjacques2021shallot` ·
`maierhein2024metrics` 확인 명령 추가 · **«51건» 의 셈 규약 한 줄**(칸×citekey×쪽 · 5-d 의 `kapoor` 를 세면 52).

### 8-3. 코드 — 지금 고칠까 / 뒤로 미룰까

| 자리 | 선택지 | 권고 |
|---|---|---|
| 화면 precount(상자 파일 수) ↔ 실제 txt 수 | ① 그대로(시험 잔재 2개를 사람이 지우면 어긋남이 사라진다) ② `box_set()`·`export_caps()` 가 상자 json 을 모두 열어 줄 수를 센다(수천 장이면 `/api/export_plan` 이 느려진다) | **①** — 다만 «낡은 txt» 경고가 이제 이름을 말해 주므로 사람이 찾기 쉽습니다 |
| 복숭아 `instances/` 가 «추정» 인 것 | ① 그대로 + Codex C-1 ② 빌더가 `<과일>/instances/` 에 «추정입니다» 한 줄을 파일로 같이 쓴다(3줄) ③ 폴더 이름을 `instances_est/` 로(규격 변경 · 지난 판과 대조 끊김) | **① 또는 ②** — ③은 반대. 통합본을 읽는 학습 코드가 **아직 0건**이라 0920 전에 ②만 해도 충분합니다 |
| `instances_source` 두 이름(`detect_seed`·`psm_watershed`) | ① 그대로 + 값 사전 ② 하나로 합친다(지난 빌드와 대조 끊김) | **①**(사람 결정 대기 · 필요해지면 코드에 «추정 집합» 한 줄) |
| 복숭아·블루베리 번호를 화면이 «**AI** 초벌» 이라 부르는 것 | ① 그대로 ② «팀 초벌 번호»(박성문 님 워터셰드) 로 ③ 서버 `seed_source` 가 `team:*` 일 때만 «팀 초벌» | **③** — 사과에서 쓴 방식과 같고 화면이 지어내지 않습니다(글자 수 동일: «AI 초벌 »↔«팀 초벌 ») |
| «사람 확정만» 이 종류마다 다른 것 | ① 그대로(규칙) ② 상자·번호 쪽에도 사이클5 ② 같은 한 줄 | **②** — 개수 쪽만 안내가 있어 균형이 안 맞습니다 |
| `t6_view` 남은 실패 4건(문턱 0.5%) | ① 그대로 ② 전경 비율 대비 문턱 ③ 전경이 큰 사진으로 고정 | **③**(사진 고정이 이미 있으니 한 줄) |
| 시험이 «125» 처럼 **살아 있는 자료 수**를 박아 두는 것 | ① 그대로(사람이 누를 때마다 회귀가 깨진다) ② 툴이 말하는 수와 맞춘다(2차가 내 사본에 한 방식) | **②** — 1차 사본(`cycle_5/stage1/{c5_fix,b2_export}.py`)에도 옮기십시오 |
| `runall_c5.sh` 로 `b5_oldserver` 가 죽는 것 | ① §9 손명령을 계속 쓴다 ② runall 에 `sync_old4()` 한 줄(2차가 내 사본에 넣음) | **②** |
| 🔴 **시험 도구 기본값이 실서버**(`scripts/ff.py:16` → 5111) · `s5_live_guard.py` 를 그냥 돌리면 `part_b` 가 실서버에 붙는다 | ① 그대로(규칙으로만 지킨다) ② `ff.py` 기본값을 «없으면 죽는 값» 으로 ③ `part_b` 를 `sys.argv` 로만 켠다 | **②+③** — 공용 도구라 이번에 손대지 않았습니다. 다음 사이클이 실수로 실서버에 쓰기를 보내는 유일한 통로입니다 |

### 8-4. 사람·교수님 (1차 목록에 더할 것)
- **삭제 목록에 2개 추가**: `data/peach/boxes_yolo/210629-t4-17.txt` · `data/grape/boxes_yolo/740.txt`
  (**0바이트 = 검출 학습이 «열매 없음» 으로 읽는 파일**. 1차 목록에는 `boxes/*.json` 두 개만 있었습니다.
  json 을 지우면 txt 는 «낡은 txt» 로 경고에 뜨지만 **파일은 남습니다** — 둘 다 지워야 합니다.)
- 임성후 님 확인(M16) · 포도 «송이» 정의(확인 8번) · modal/amodal · `other`→`trunk` · 낙과 제외 권고 ·
  M8·M19 PDF — 1차 목록 그대로.
- 🆕 **박성문 님께**: `FALLBACK["grape"]="cc"` 는 `instance_maps` 폴더가 아니라
  `park_seongmoon/semantic-segmentation/tools/instance_split.py:47` 입니다. 사이클3 2차가 요구한
  «CC 는 쪼개지 않기 위한 차선이고 옳은 세기가 아니다(+62.1%)» 주석은 **아직 없습니다.**

---

## 9. 3차가 «켜기» 전에 알아야 할 것

1. 🔴 **사람이 지금 툴을 쓰고 있습니다.** 22:54:55~22:55:24 · 23:00:04~23:00:15 에 외부 IP
   (112.149.154.206)에서 복숭아를 열고 `POST /api/status`(22:55:23 · 판정 확정)와
   `POST /api/component`(23:00:15)를 보냈습니다. 재시작은 몇 초 동안 서버를 끊으므로 **그 사람이
   그 순간에 누른 저장이 실패할 수 있습니다**(화면은 그대로 남고 요청만 끊깁니다). 켜기 전에
   `tail -3 app/logs/server.log` 로 마지막 요청 시각을 보고 **조용한 때**를 고르십시오. 공용 자료에 쓰기가 들어온 것은 `data/peach/status.json`
   한 파일뿐이고 내 모래상자는 그것을 그대로 복사해 씁니다.
2. **이번에 고친 파이썬은 `app/boxes.py` 하나**입니다(1차의 소수정 ④ + 2차의 낡은 txt 계산).
   정적 파일(`ui.js`·`app.js`·`help.html`)은 재시작 없이 이미 나가고 있으므로 지금 실서버는
   «새 화면 + 옛 `boxes.py`» 입니다 → **상자 YOLO 를 내보내면 화면은 «건너뛴 사진» 이라 말하는데
   서버는 0줄 txt 를 만듭니다.** 재시작하면 사라집니다(`c5_fix.py --before` 가 그 상태를 보여 줍니다).
3. 켜는 순서(1차·사이클4 3차와 같게): 외부 마지막 쓰기 확인 → `py_compile` 6 + `node --check` 2 →
   `bash app/run.sh restart`(데이터 루트 유지 · `pkill` 금지 · PID 로) → `ps -o lstart` 로 새 PID 기록.
4. **켠 뒤 GET 으로 볼 것**(쓰기 0):
   - 비로그인 `401`
   - `/api/item?fruit=peach&stem=210629-t1-01` → `counts.seed_source == "team:박성문"` · `n_inst == counts.instances`
   - `/api/item?fruit=apple&stem=20150919_174151_image1` → `counts.seed_source == "gt_numbers"`
   - `/api/item?fruit=grape&stem=0` → `seed_source == "cc4"` · `has_instances` 없음(**포도는 꺼진 채**)
   - `/api/item?fruit=peach&stem=210629-t2-of13-03` → `confirmed.status == "flag"`(사람이 누른 그 사진)
   - `/api/fruits` AI 집계 불변 · `/api/export_list` `kinds` 에 `counts`
   - `/api/export_plan` 으로 복숭아를 물으면 «나갈 사진» 이 **124** 계열인지(125 가 아님)
5. **사람 눈으로만 볼 것**(내가 못 보는 것):
   - 크롬에서 ② 상자 Enter 뒤 개수 칸이 **초록**으로 보이는지(파이어폭스 실측 `#d8f3e1`/`#1d6b3c` · 대비 5.54:1)
   - 사과 ③ 하단 줄이 «**원본 정답 번호** 95개» 로 보이는지(글자 잘림 없이 한 줄)
   - 데이터 정리에서 «개수» 를 찍었을 때 안내 줄이 **두 줄로 줄바꿈되어** 보이는지(`<br>` 로 넣었습니다)
   - 내보내기에서 막힐 때 새 문장(«그 체크를 빼면 상자·개수만 나갑니다»)이 한 줄에 들어가는지
   - 체크리스트 새 3항목(D-4 · E-3 · E-4)
6. 🔴 **날짜가 넘어갔습니다 — 지금 2026-09-20 입니다.** 문서들이 «0920 빌드부터» 라고 적은 그 빌드가
   **오늘**입니다. 통합본을 다시 만들기 전에 §8-1 의 숫자를 **그때 다시 세십시오**(사람이 «제외» 를
   더 눌렀으면 124 보다 줄어듭니다).
7. **재시작 뒤 다시 돌려야 하는 것**: `b5_oldserver`(모래상자 `sandbox_old4` 를 먼저 만들어야 합니다 —
   내 `runall_c5c.sh` 에는 그 줄이 들어 있습니다) · 실서버를 켠 뒤 위 GET 여섯 개.

---

## 10. «구조 정리» 사이클 1 에 줄 메모 — **재기준선 시점**

1. **기준선을 다시 뜨십시오.** 1차가 적어 둔 4파일 외에 **2차가 더 고쳤습니다**
   (시각은 전부 2026-09-19 실측):
   - `app/boxes.py` **23:19** (낡은 txt 계산 +9/−2) · 백업 `app/_backup_260919_cnt5b_boxes.py`
   - `app/static/ui.js` **23:36:40** (풍선말 `**` 제거 · 내보내기 가드 문장) · 백업 `_backup_260919_cnt5b_ui.js`
   - `app/README.md` **23:51:36** · `app/static/help.html` **23:51:36** (백업 각각 `_backup_260919_cnt5b_*`)
   - `kds0206/semantic-segmentation/tools/build_merged_dataset.py` **23:37:48** (주석 2곳 · README 생성부)
   - `kds0206/문서/{260918_통합데이터셋_갱신법.md, 260917_툴_사람확인_체크리스트.md, 260919_카톡_상자카운팅.txt,
     260918_Codex질문_공격적.md, 260918_Codex질문_보통.md}`
   - **시험 정본 4파일**: `cycles/260918_paint/cycle_4/stage1/lib_c4.py` ·
     `cycles/260918_paint/cycle_2/stage2b/{reg_s2/lib_s2.py, reg_s1/t4_paint8.py, reg_s0/s5_fix.py}`
   지금 줄 수: `ui.js` **1,337** · `app.js` **2,664** · `boxes.py` **491** · `build_merged_dataset.py` **2,591**.
2. 🔴 **백업을 옮길 때 `grep '_backup_'` 을 시험 코드에도 돌려 주십시오.** 1차가 알린 4곳 말고
   **`260918_paint/cycle_4/stage1/lib_c4.py` 도 정본이고 죽었습니다**(2차가 정본에서 새로 사본을 떠서
   확인 · 지금은 넷 다 폴백을 넣었습니다). 그쪽 `INDEX.md`(23:04)가 «죽는 사이클 시험 45개» 라고
   적은 것과 내 «4곳» 은 **범위가 다릅니다**(나는 사이클5 가 실제로 돌리는 것만 셌습니다).
3. 폴백은 전부 «없으면 `_archive/backups_260920/<원래 상대경로>` 에서 읽기» 한 가지 꼴입니다 —
   아카이브 폴더 이름을 또 바꾸면 그 네 줄이 다시 죽습니다. 옮길 때 알려 주십시오.
4. 🔴 **`scripts/ff.py:16` 의 기본 주소가 실서버 5111 입니다**(`LABELTOOL_URL` 이 없을 때).
   구조를 정리하면서 시험 도구를 옮기신다면 **그 기본값을 «없으면 죽는 값» 으로** 바꿔 주십시오 —
   지금은 시험이 환경변수를 세우기 전에 `ff` 를 쓰면 **실서버에 붙습니다**(§1-5).
5. **`_archive/`·`tests/` 에는 한 글자도 쓰지 않았습니다**(옛 백업을 **읽기**만 했습니다).

---

## 부록 — 3차가 그대로 다시 돌릴 명령 (전부 내 폴더·내 포트)

```bash
PY=/home/kds0206/.conda/envs/kwak/bin/python
T=/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴
S=/data/project/2026summer/kds0206/semantic-segmentation
C=$T/cycles/260919_count/cycle_5/stage2

# (1) 소수정 4 «고치기 전» — 🔴 sync_before() 로 만든다(1차 §9 의 L.sync 는 새 코드를 복사한다)
$PY -c "import sys;sys.path.insert(0,'$C');import lib_cnt5 as M;M.sync_before();M.L.reset_status(M.SB_OLD)"
cd $C && $PY -u c5_fix.py --before        # 통과 11 / 실패 11
# (2) «고친 뒤»
$PY -c "import sys;sys.path.insert(0,'$C');import lib_cnt5 as M;M.L.sync(M.SB);M.L.reset_status(M.SB)"
cd $C && $PY -u c5_fix.py                 # 통과 22 / 실패 0
# (3) 내가 고친 «옛 0줄 txt 를 낡은 txt 로 센다»
cd $C && $PY -u c5b_stale.py              # 5 / 0 (고치기 전 3 / 2)
# (4) 처음 온 사람 시나리오(진짜 파이어폭스 1366×768 · 포트 5486)
cd $C && $PY -u scn_newcomer.py           # 22 / 0
# (5) 전체 회귀 한 벌 (약 25분 · b5 를 위한 sync_old4 가 들어 있다)
bash $C/runall_c5c.sh > $C/runall_c5c_outer.log 2>&1
grep -E '^###|rc=|ALLDONE' $C/runall_c5c_outer.log
# (6) 실서버에 남아 있는 0줄 txt 가 «낡은 txt» 로 잡히는지 (모래상자 자료 = 실서버 사본)
cd $C && $PY -c "
import importlib.util,sys,os
SB='$C/sandbox'; sys.path.insert(0,SB+'/app')
sp=importlib.util.spec_from_file_location('b',SB+'/app/boxes.py'); m=importlib.util.module_from_spec(sp); sp.loader.exec_module(m)
for f in ('peach','grape'):
    print(f, m.export_boxes_to(SB+'/data', f, SB+'/data/'+f+'/boxes_yolo', at='확인'))"
#   → stale_txt 1 · stale_txt_names ['210629-t4-17.txt'] / ['740.txt']
# (7) 지표 표 — 🔴 --counts 는 폴더 네 개를 **한 번에** 준다(따로 주면 마지막 하나만 읽는다)
B=$T/cycles/260919_count/cycle_4/stage2/counts_off
$PY $S/tools/count_metrics.py --counts $B/apple $B/peach $B/grape $B/blueberry
$PY $S/tools/count_metrics.py --selftest
# (8) 통합 dry-run — 복숭아 «번호» 칸이 «남김» 과 같은지(지금 124)
cd $S && $PY -W error::DeprecationWarning tools/build_merged_dataset.py --dry-run | grep 복숭아
```

**내 산출물**(전부 `cycles/260919_count/cycle_5/stage2/`): `lib_cnt5.py`(포트 5481·5482·5484) ·
`c5_fix.py`(기대값을 자료 기반으로) · `c5b_stale.py`(새 시험) · `scn_newcomer.py`(시나리오) ·
`b1~b5`·`mk_*`·`runall_c5c.sh`·`run_all_2nd{,_b}.sh`·`run_final_b.sh` · 로그·json 전부 ·
모래상자 `sandbox`·`sandbox_before`·`sandbox_old4`·`sandbox_scn`·`reg_cnt5{b,c}`·`c4_cnt5{b,c}` ·
`mergebuild_{before,after}`·`t4_boxes_*`·`t5_stale` · 스크린샷 `~/ff_shots/cnt5b2/`.
**모래상자 폴더는 사람 삭제 목록에 올립니다**(각각 수백 MB · 로그·json·md 는 증거이니 남기십시오).
