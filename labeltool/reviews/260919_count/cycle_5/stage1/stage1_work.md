작성: 2026-09-19

# 「상자 + 개수 세기」 5사이클 — **사이클 5(종합) 1차 작업** (Opus 5)

작업 시각(`date` 실측): 시작 **22:01:03** · 모래상자(고치기 전 판) **22:01:32** ·
소수정 4 **22:05~22:11** · «고치기 전» 시험 **22:12:56** · «고친 뒤» 시험 **22:13:59** ·
전체 회귀 **22:17:43~22:41:55** · 아카이브 폴백 뒤 재실행 **22:42:55~22:45:00** ·
보고서·Codex 절·카톡 **22:46~22:53** (각 절에 실측 시각을 다시 적었습니다).

**한 줄 요약**: 사이클 4 3차가 내려 준 **소수정 4가지**를 고쳤습니다(«고치기 전» 시험 **11건 실패**
→ «고친 뒤» **22/0 통과**). 시험 위생 2가지(`b2` 개수 캐시 의존 · `t6_view` 표본 사진 무작위)도
없앴습니다. 회귀는 §3 표에 실측으로 적었고, 도중에 **다른 세션(«구조 정리» 사이클 1)이 22:00~22:02 에
`_backup_*` 205개를 `_archive/backups_260920/` 로 옮긴 것** 때문에 시험 세 개가 죽는 것을 발견해
«없으면 아카이브에서 읽는다» 로 고쳤습니다(내 사본에서만 · §3-3). 실서버 5111(PID **964494** ·
21:45:04 기동)에는 **붙지도 재시작하지도 않았습니다** — 켜는 것은 3차 몫입니다.

🔴 **3차가 켜기 전에 알아야 할 것**: 이번에 고친 **파이썬은 `app/boxes.py` 하나**이고 나머지는
정적 파일·문서·통합 스크립트입니다. 정적 파일은 **재시작 없이 이미 나가고 있으므로**, 지금 실서버는
«새 화면 + 옛 `boxes.py`» 상태입니다. 그 상태에서 «상자 YOLO» 를 내보내면 화면은 새 문장
(«상자가 0개여서 건너뛴 사진 N장»)을 쓰는데 서버는 아직 **0줄 txt 를 만듭니다** — 문장과 실제가
어긋나는 유일한 자리이고, **재시작하면 사라집니다**(`b5_oldserver` 로 확인 · §3-2).

---

## 1. 소수정 4 — «고치기 전 11건 실패 → 고친 뒤 22/0»

시험 한 벌로 넷을 다 봅니다: `cycle_5/stage1/c5_fix.py`
(`--before` = 모래상자 `sandbox_before`(22:01:32 에 뜬 고치기 전 정본) + 포트 5382 + 백업 빌더 /
옵션 없이 = `sandbox` + 포트 5381 + 정본 빌더).

| 판 | 결과 | 로그 |
|---|---|---|
| **고치기 전**(22:11:58~22:12:56) | **통과 11 / 실패 11** | `c5_fix_before.log` · `c5_fix_before.json` |
| **고친 뒤**(22:13:07~22:13:59) | **통과 22 / 실패 0** | `c5_fix_after.log` · `c5_fix_after.json` |

실패한 11건이 정확히 이번 소수정 4가지입니다(④ 3건 · ③ 5건 · ① 2건 · ② 1건).

### 1-1. ① 사과 ③번호 하단 줄 — «AI 초벌 번호» → «원본 정답 번호»

- **왜**: 사과의 번호는 AI 가 만든 것이 아니라 **원본 마스크에 들어 있던 정답 번호**입니다
  (`seed_source=gt_numbers`). 그런데 화면이 그것을 «AI 초벌» 이라고 불러서, 처음 온 사람은
  «AI 가 만든 것이니 내가 고쳐야겠다» 고 읽습니다 — **정답 라벨을 건드리게 되는 자리**입니다.
  (사이클 4 2차 §3-1·§9-3 → 3차 §3)
- **어디**: `app/static/ui.js` `taskConfLine()` 한 곳.
  조건은 **서버가 준 `counts.seed_source` 가 `gt_numbers` 일 때만**입니다(화면이 지어내지 않습니다).
  옛 서버는 그 칸이 없으므로 예전 문장 그대로입니다.
- **글자 예산**: «AI 초벌 »(6자) ↔ «원본 정답 »(6자) — **한 자도 늘지 않았습니다**(§3-4 `b3_chars`).
- **실측**(진짜 파이어폭스 1366×768): 사과 `20150919_174151_image1…` → 전 «AI 초벌 번호 96개 —
  맞으면 Enter» / 후 «**원본 정답 번호 96개** — 맞으면 Enter» · 복숭아는 **전·후 모두**
  «AI 초벌 번호 16개 — 맞으면 Enter»(`seed_source=team:박성문`).
- 풍선말에 «이 번호는 AI 가 만든 것이 아니라 원본 마스크에 들어 있는 정답 번호입니다» 한 문장을
  앞에 붙였습니다(풍선말은 글자 예산 밖입니다).
- 문서도 같이: `app/static/help.html` ③ 카드 · `app/README.md` §16 표 한 줄.

```diff
-  return { cls: "todo go",
-           head: "AI 초벌 " + w + " " + what + " — 맞으면 " + K("Enter"),
+  const gtNum = (t === "num" && m.counts && m.counts.seed_source === "gt_numbers");
+  return { cls: "todo go",
+           head: (gtNum ? "원본 정답 " : "AI 초벌 ") + w + " " + what + " — 맞으면 " + K("Enter"),
```

### 1-2. ② 데이터 정리 탭 — «개수 csv 는 마스크를 확정한 사진만 담깁니다» 한 줄

- **왜**: «사람 확정만 + 개수» 는 규칙대로 **마스크 확정**을 따릅니다(0919 사이클 1 총괄 결정 2 —
  `counts.csv` 는 그 job 이 나간 사진만 담고 manifest «포함» 과 한 줄도 다르지 않다). 그래서 상자만
  확정한 사람은 **빈 표**를 받습니다. 사이클 4 2차의 사용자 시나리오가 **여기서 한 번 막혔습니다**
  (화면이 «나갈 상자 1장» 이라 적고 표는 0줄). **규칙은 그대로 두고 미리 말해 줍니다.**
- **어디**: `app/static/ui.js` `expPlan()` — `#exp-count` 아래에 한 줄.
  «개수» 를 골랐고 조건이 «사람 확정만» 일 때만 뜨고, «AI 제안 포함» 에서는 사라집니다.
- **실측**: 전 — 그 줄 없음 / 후 — «… 지금 조건으로 나갈 사진 0장 / **개수 csv 는 마스크를 확정한
  사진만 담깁니다 — 상자만 확정하면 빈 표**» · «AI 제안 포함» 으로 바꾸면 사라짐(②-4 통과).
- 문서도 같이: `help.html` 내보내기 절 · `README.md` §19-3.

### 1-3. ③ 통합 빌더 `SEED_DIRS` 에 복숭아 — `instances_source=psm_watershed`

- **왜**: 툴은 사이클 4 M1 부터 복숭아 번호 초벌(박성문 님 워터셰드 `instance_maps`)을 쓰는데
  **통합 빌더만 그것을 몰랐습니다**. 그래서 툴에서 «번호 확정» 을 해도 manifest 가
  `count_source=none` 이었습니다(사이클 4 2차 `b2_export.py` ⑤-b′ 가 «알려진 틈» 으로 찍어 둔 자리).
- **어디**: `semantic-segmentation/tools/build_merged_dataset.py`
  `SEED_DIRS`(+`PARK_SEED`·`SEED_LABELS` 3줄) · 번호 출처 라벨 한 줄 · README 생성 코드 4곳.
- **값**: 복숭아는 `psm_watershed`, 블루베리는 예전 값 `detect_seed` **그대로**(값을 바꾸면 지난
  빌드와 대조할 수 없습니다). 둘 다 성격은 같습니다 — **추정이고 사람이 검수한 것이 아닙니다.**
- **0920 빌드부터 내용이 바뀝니다**: 복숭아 **125장에 `instances/` 가 생깁니다**(그 전 판
  `_260918`·`_v2`·`_v3` 에는 아예 없었습니다). 그래서 세 곳에 그 사실을 적었습니다 —
  빌더 README 생성 코드(«주의» 절 + `instances_source` 값 사전) · `문서/260918_통합데이터셋_갱신법.md`
  §5① 표(열매 번호 0 → **125** · 합계 1,543 → **1,668**).
- **실측**(모래상자 `--tool-data`, 내 폴더에 진짜 빌드 한 번):
  dry-run 복숭아 «번호» 칸 **0 → 125** · manifest 에 번호 있는 줄 **125** ·
  `instances_source` 는 **`psm_watershed` 하나뿐** · `instances/` PNG **125장** ·
  `n_instances` 칸 125줄 전부 찼음 · 빌드 rc=0(26.4초).

### 1-4. ④ 상자가 0개인 json 은 내보내기에서 건너뜀 (0줄 txt 를 만들지 않는다)

- **왜**: 0줄 YOLO txt 는 검출 학습이 «이 사진에는 열매가 없다» 는 **정답 라벨**로 읽습니다.
  사이클 4 가 «드러내 두고 고치지 않은» 구멍입니다(2차 §7-2·§9-5).
- **어디**: `app/boxes.py export_boxes_to()` — 줄이 하나도 없으면 `continue`(파일을 쓰지 않음).
  ⚠ 사이클 5 지시서에는 «`export/export_dataset.py`(빈 상자 json)» 이라고 적혀 있었지만, YOLO txt 를
  **실제로 쓰는 곳은 `app/boxes.py`** 한 군데입니다(`export_dataset.py` 는 counts.csv 쪽입니다).
  그래서 `boxes.py` 를 고쳤습니다 — 가정을 밝혀 둡니다.
- 건너뛴 이름은 그대로 돌려줍니다(`empty_txt`·`empty_txt_names`). 뜻만 바뀌었습니다:
  «0줄로 나간 txt» → «**건너뛴 사진**». `n_images` 는 **실제로 나간 txt 장수**입니다.
  화면 문장도 같이 바꿨습니다(`app/static/app.js` 한 줄, 4자 줄었습니다):
  «⚠ 상자가 0개라 빈 txt N개 … 학습에 넣기 전에 지우세요» → «⚠ 상자가 0개여서 건너뛴 사진 N장(이름)».
- **실측**(함수 직접 호출 · 상자 2개 json + 0개 json 한 장씩):
  전 — `AA.txt`·`BB.txt` 두 장(BB 는 0줄) · `n_images=2` /
  후 — **`AA.txt` 한 장** · `BB.txt` **없음** · `n_images=1` · `empty_txt=1`·`empty_txt_names=["BB"]` ·
  살아 있는 상자 수는 2개 그대로.

### 1-5. 시험 위생 2가지

| 무엇 | 전 | 후 |
|---|---|---|
| `b2_export.py` ②-3 이 **공용 개수 캐시가 미리 차 있어야** 통과(사이클 4 2차 §1-2) | 캐시가 비면 `(None, 18)` 로 실패 | 시험이 **스스로 채운다**(`warm_cache()` — 툴의 «세기» 와 같은 `count_one`·`sig_of`, 쓰는 곳은 **모래상자** `app/cache/` 뿐) + «확정 전에도 번호 수를 안다» 한 줄 추가 |
| `t6_view.py` 가 «1쪽 40장 중 번호가 가장 많은 사진» 을 **그때 골라** 실패 4~5건이 흔들림(§1-3·§9-9) | 실행마다 다른 사진(사과 `…image166` ↔ `…image241` 등) | **네 장을 박아 둠**(`FIXED` — 사과 `20150919_174730_image241` · 블루베리 `Camera 1 Video (97)_61` · 복숭아 `210629-t4-17` · 포도 `740`). 그 사진이 없을 때만 예전처럼 고르고 그때는 화면에 «⚠ 고정 표본이 없어…» 를 찍는다 |

---

## 2. 🔴 **코드 고친 파일 목록** (구조 정리 사이클이 기준선을 다시 뜰 때 쓸 것)

지금 **다른 세션이 «구조 정리 사이클 1(기준선)» 을 `260916_라벨링툴/tests/`·`_archive/` 에 만들고
있습니다.** 아래 파일이 바뀌었으니 **그 기준선은 다시 떠야 합니다.**
(저는 `tests/`·`_archive/` 에 **한 글자도 쓰지 않았습니다.**)

### 2-1. 실제로 돌아가는 코드 (백업 `_backup_260919_cnt5_<파일명>` 을 같은 폴더에 남겼습니다)

| # | 파일 | 무엇을 | 줄(+/−) | 재시작 필요? |
|---|---|---|---:|---|
| 1 | `260916_라벨링툴/app/static/ui.js` | 소수정 ①(하단 줄)·②(개수 안내 한 줄) | +23 / −4 | 아니오(정적) |
| 2 | `260916_라벨링툴/app/static/app.js` | 내보내기 결과 문장(«건너뛴 사진») | +3 / −1 | 아니오(정적) |
| 3 | `260916_라벨링툴/app/boxes.py` | 소수정 ④ 상자 0개 json 건너뜀 | +16 / −8 | 🔴 **예** |
| 4 | `semantic-segmentation/tools/build_merged_dataset.py` | 소수정 ③ `SEED_DIRS`+복숭아 · 라벨 · README 문구 | +27 / −7 | — (0920 빌드) |

### 2-2. 문서 (백업 같은 규칙)

| # | 파일 | 무엇을 | 줄(+/−) |
|---|---|---|---:|
| 5 | `260916_라벨링툴/app/static/help.html` | ③ 카드(사과 «원본 정답 번호») · 내보내기 조건 안내 | +6 / −2 |
| 6 | `260916_라벨링툴/app/README.md` | §16 표·§16-3(0줄 txt)·§19-3(개수 안내) | +11 / −1 |
| 7 | `kds0206/문서/260918_통합데이터셋_갱신법.md` | §5① 표 «0920 빌드부터 복숭아 번호 125» + 최종 수정 줄 | +11 / −3 |
| 8 | `kds0206/문서/260918_Codex질문_공격적.md` | «C. 상자+개수 세기 5사이클 + 논문 대조» 절 추가(질문 5문 · C-6 은 3차가 붙임) | +134 |
| 9 | `kds0206/문서/260918_Codex질문_보통.md` | 같은 C 절(질문 5문은 «팀원이 쓰는 관점») | +133 |
| 10 | `kds0206/문서/260919_카톡_상자카운팅.txt` | **새로 만듦**(25줄) | 신규 |

### 2-3. 시험 파일 (기대값·위생 — «무엇을 보는지» 를 바꾼 곳은 ①로 표시)

| # | 파일 | 무엇을 |
|---|---|---|
| 11 | `cycles/260918_paint/cycle_4/stage1/t2_ui.py` | ① 사과 ③ 하단 줄 기대값을 «원본 정답 번호» 로(백업 `_backup_260919_cnt5_t2_ui.py`). **정본**을 고쳤습니다 — 안 고치면 다음 사이클마다 낡은 기대값으로 실패합니다(사이클 1 2차 §2-6-다의 교훈) |
| 12 | `cycles/260918_paint/stage1/t6_view.py` | 표본 사진 4장 고정(백업 `_backup_260919_cnt5_t6_view.py`). **정본**입니다 |
| 13 | `cycles/260919_count/cycle_5/stage1/reg_cnt5/reg_s1/runall_stage1/t6_view.py` | 같은 고정(내 사본 — 이번 회귀가 실제로 돌린 것) |
| 14 | `cycles/260919_count/cycle_5/stage1/c4_cnt5/{lib_c4.py,t2_ui.py}` · `reg_cnt5/reg_s2/lib_s2.py` | 아카이브로 옮겨진 `_backup_*` 를 «없으면 `_archive/backups_260920/` 에서 읽기»(§3-3) — 내 사본만 |

⚠ `mk_reg_cnt5.py` 는 회귀 묶음을 `cycles/260918_paint/cycle_2/stage2b/` 에서 복사합니다. 그쪽 사본의
`t6_view.py`·`lib_s2.py` 는 **아직 옛 판**이라, 다음 사이클이 같은 방식으로 복사하면 «무작위 표본» 과
«아카이브 때문에 죽는 시험» 이 되살아납니다. 정본(`260918_paint/stage1`·`cycle_4/stage1`)은 고쳤으니
다음 사이클은 **거기서 복사하거나 제 사본을 쓰십시오.**

### 2-4. 새로 만든 것 (전부 `cycles/260919_count/cycle_5/stage1/`)

`lib_cnt5.py`(모래상자·포트 5381/5382/5383) · `c5_fix.py`(소수정 4 전/후 시험 22항목) ·
`runall_c5.sh`(회귀 한 벌) · `mk_c4_cnt5.py`·`mk_reg_cnt5.py`(시험 사본을 정본에서 새로) ·
`b1_browser.py`·`b2_export.py`(위생 2곳)·`b3_chars.py`·`b4_fallback.py`·`b5_oldserver.py`(사본) ·
`mk_cache_b.py`·`mk_counts_b.py`(사본) · 로그·json·모래상자 폴더들.

---

## 3. 회귀 — **사이클 4 와 전부 같은 숫자**(달라진 세 줄은 이유가 있고 §3-2·§3-3 에 적었습니다)

전부 **내 폴더**(`cycle_5/stage1/`) · **내 포트**(5381·5382·5383 · 5911~5913 · 5931·5932 · 5941~5969) ·
**내 모래상자**. 실서버 5111(PID 964494)에는 붙지도 재시작하지도 않았습니다.
한 벌로 돌린 것: `bash cycle_5/stage1/runall_c5.sh`(**22:17:43 ~ 22:41:55 · 24분 12초**).

### 3-1. 재현표

| 시험 | 사이클 4 | **사이클 5** | 판단 |
|---|---|---|---|
| `node --check` app.js·ui.js | OK ×2 | **OK ×2** | 🟢 |
| `py_compile` 서버 6 + tools 2 | OK | **OK** | 🟢 |
| `app/boxes.py` 자체 점검 | 통과 | **통과** | 🟢 |
| `count_metrics.py --selftest` | 손계산 5건 통과 | **통과** | 🟢 |
| c4 `t1_api` | 34 / 0 | **34 / 0** | 🟢 |
| c4 `t2_ui` | 27 / 0(경고 1) | **27 / 0(경고 1)** | 🟢 기대값 한 줄을 이번 소수정에 맞게 고친 뒤(§3-2) |
| c4 `regress_all` | 100 / 0(경고 8) | **100 / 0(경고 8)** | 🟢 사이클 4 사본 로그와 같은 숫자 |
| `sim.js` · `modesim.js` · `boxsim.js` | — · 31/31 · 50/50 | **— · 31/31 · 50/50** | 🟢 |
| c2-1차 `t1_api`·`t2_queue`·`t3_conc`·`t4_paint8` | 79·43·8·22 / 0 | **79·43·8·22 / 0** | 🟢 (`t4_paint8` 은 §3-3 뒤) |
| c2-2차 `s1_api` | 77 / 0 | **77 / 0** | 🟢 (§3-3 뒤) |
| c2-2차 `s2`·`s3`·`s6`·`s7`·`s8` | 20·18·7·2·3 / 0 | **20·18·7·2·3 / 0** | 🟢 (`s8` 은 §3-3 뒤) |
| c2-2차 `s4_paint8` | 21 / **1** | 21 / **1** — 같은 실패(«확정» 딱지 12.5px) | 🟡 전과 같음 |
| c2-2차 `s5_live_guard`(part_a+part_c) | 12 / **1** | 12 / **1** — 같은 실패(A-2 가드 문장) | 🟡 전과 같음 |
| 중첩 `t1_after`(배치·글자) | 180 / 0 | **180 / 0** | 🟢 |
| 중첩 `t6_view` | 138 / **5**(사진이 실행마다 달라 흔들림) | **139 / 4 — 이제 흔들리지 않는다** | 🟡 §3-4 |
| 통합 `test_counts` | 62 / 0 | **62 / 0** | 🟢 |
| 통합 `test_build_merged` | 68항목 전부 통과 | **68항목 전부 통과** | 🟢 |
| 통합 `stage4/test_confirm_kinds` | 200 전부 통과 | **200 전부 통과** | 🟢 |
| 통합 `stage5/test_spec` · `test_fix_m5c` | 106 · 67 전부 통과 | **106 · 67 전부 통과** | 🟢 |
| 통합 `--dry-run`(네 과일 · 진짜 툴 자료) | rc=0 · 429·2,403·125·1,114 | **rc=0 · 같은 장수** · 복숭아 **번호 0 → 125** | 🟢 의도한 변화(소수정 ③) |
| `b1_browser` | 81 / 0 | **81 / 0** | 🟢 |
| `b2_export` | 25 / 0 | **28 / 0**(내가 3항목 더 넣음: ②-0·②-0′ 캐시 위생 · ⑤-b″) | 🟢 |
| `b3_chars` | 4 / 0 | **4 / 0** · 편집 화면 글자 증감 **①0 ②0 ③0자** · 개수 칸 글자 0 | 🟢 글자 예산 지킴 |
| `b4_fallback` | 4 / 0 | **4 / 0** | 🟢 |
| `b5_oldserver` | 6 / 0 | **6 / 0** | 🟢 (§3-2 — 모래상자를 `sandbox_old4` 로 바꾼 뒤) |
| **소수정 4 전/후**(`c5_fix.py`) | — | **전 11 실패 → 후 22 / 0** | 🟢 |

### 3-2. 이번에 **기대값·모래상자를 손댄 세 줄** (숨기지 않고 적습니다)

1. **`t2_ui` 나-1** — «③ 한 줄이 «AI 초벌 번호 …»» 를 보던 검사입니다. 소수정 ① 이 사과에서 그 낱말을
   바꿨으니 **기대값이 낡은 것**이 됐습니다(고치기 전 실측: `FAIL … | 원본 정답 번호 1개 — 맞으면 Enter`).
   → «**원본 정답 번호** … 이고 «초벌» 이 들어 있지 않다» 로 바꿨습니다. **정본과 내 사본 둘 다** 고쳤습니다
   (정본을 안 고치면 다음 사이클마다 같은 실패가 재생산됩니다 — 사이클 1 2차 §2-6-다).
2. **`b5_oldserver`** — 이 시험의 세 검사는 «**옛 서버는 `seed_source` 를 모른다**» 를 봅니다. 사이클 5 가
   고친 파이썬은 `boxes.py` 하나라 «새 화면 + 옛 `boxes.py`»(`sandbox_oldpy`)로는 그 뜻이 성립하지 않아
   **3/3 으로 실패**했습니다(서버가 M1 뒤 판이라 `seed_source` 를 압니다 — 코드 결함이 아닙니다).
   → 사이클 4 와 같은 뜻이 되도록 `sandbox_old4`(새 정적 파일 + `_backup_260919_cnt4_*` 파이썬 4개)를
   만들어 다시 돌렸습니다: **6 / 0**.
   ⚠ 그래서 «새 화면 + 옛 `boxes.py`» 상태(= **지금 실서버**)는 `c5_fix.py --before` 가 대신 증명합니다:
   그 판에서는 0줄 txt 가 **여전히 만들어집니다**. 3차가 재시작하면 사라집니다.
3. **`b2_export`** — ②-3 이 개수 캐시에 기대던 것을 없애고(§1-5) 통합본이 복숭아 번호본을 아는지 보는
   줄(⑤-b′)의 기대를 **«알려진 틈» → «닫혔다»** 로 바꿨습니다(+⑤-b″ 출처 확인). 25 → **28항목**.

### 3-3. 🔴 다른 세션이 옮긴 `_backup_*` 때문에 시험 4개가 **죽었습니다**(내 코드와 무관)

22:00~22:02 에 **«구조 정리» 사이클 1** 이 `app/`·`export/`·`scripts/` 의 `_backup_*` **205개**를
`_archive/backups_260920/` 로 옮겼습니다(그쪽 `INDEX.md` 에 적혀 있습니다 — `cnt4`·`cnt4b` 두 세대는
남겨 두었지만 **`260918_c2`·`260918_c4`·`260918_paint2` 세대는 옮겨졌습니다**).
그 세대를 «옛 판» 으로 쓰는 시험 4개가 `FileNotFoundError` 로 죽었습니다:

| 시험 | 없어진 파일 | 고친 방법(내 사본만) | 다시 돌린 결과 |
|---|---|---|---|
| c4 `t2_ui` part_old | `app/_backup_260918_c4_server.py` 외 3개 | `c4_cnt5/lib_c4.py` — 없으면 아카이브에서 읽기 | **27 / 0** |
| c2-2차 `s1_api`·`s5_live_guard`·`s8_guard_advance` | `app/_backup_260918_c2_server.py` | `reg_cnt5/reg_s2/lib_s2.py use_old_server()` 같은 폴백 | **77/0 · 12/1 · 3/0** |
| c2-1차 `t4_paint8` | `app/static/_backup_260918_c2_{index.html,style.css}` | `reg_cnt5/reg_s1/t4_paint8.py` 에 `_bk()` 폴백 | **22 / 0** |
| (예방) `reg_s0/s5_fix.py` | `app/static/_backup_260918_paint2_*` | 같은 폴백 | 이번 회귀에서 `ALLDONE2` |

**시험이 «무엇을 보는지» 는 한 글자도 바꾸지 않았습니다** — 읽는 자리만 바꿨습니다.
🔴 **구조 정리 쪽에 알릴 것**: 백업을 옮기기 전 `grep '_backup_'` 을 **실제 코드**에만 돌렸고
**시험 코드(`cycles/**/*.py`)에는 돌리지 않았습니다.** 지금 살아 있는 참조가 위 4곳입니다.

### 3-4. `t6_view` — 흔들림을 없앴습니다(그리고 남은 4건의 정체를 밝혔습니다)

표본을 고정한 뒤 **139 / 4** 이고, 실패 4건은 전부 같은 종류입니다:
«원본만 ↔ 겹쳐 사이에 바뀐 화소 ≥ 0.5%» — 실측 **사과 0.33% · 블루베리 0.07%**.
즉 **그 사진의 색칠 면적이 문턱보다 작아서** 나는 실패이고, 보기 전환 자체는 정상입니다
(같은 사진의 «겹쳐 복귀 — 그림이 처음과 같다» 는 **다른 화소 0%** 로 통과). 사이클 4 에서는
사진이 매번 바뀌어 사과 1.44%·복숭아 밝기 등으로 **실패 내용이 달라졌습니다**(138/5).
→ 다음 사이클에 넘기는 것: **문턱을 사진마다 정하거나(전경 비율 대비) 전경이 큰 사진을 고르기.**

---

## 4. 사이클 5 가 **새로 드러낸 것** (2차가 볼 것)

| # | 무엇 | 왜 지금 고치지 않았나 |
|---|---|---|
| 1 | 🟡 **소수정 ④ 가 만든 새 불일치**: 상자 0개 json 을 건너뛰면서, 화면의 «지금 조건으로 나갈 상자 **1장**» (`box_set()` = **파일이 있나**만 봅니다)과 실제로 나가는 txt **0장**이 어긋납니다. 사이클 4 결함 27 과 같은 종류입니다 | 맞추려면 `box_set()`·`export_caps()` 가 **모든 상자 json 을 열어** 줄 수를 세야 합니다(지금은 파일 이름만 훑습니다). 사람이 수천 장을 저장하면 `/api/export_plan` 이 그만큼 느려집니다. 게다가 지금 어긋나는 사진은 **시험 잔재 2개뿐**이고 그 둘은 §5-7 삭제 목록에 있습니다 → **사람이 지우면 저절로 사라집니다.** 2차·3차가 «지금 고칠까» 를 정해 주십시오 |
| 2 | 🟡 **재시작 전까지 화면 문장과 서버가 어긋납니다**: 새 `app.js` 는 «⚠ 상자가 0개여서 **건너뛴** 사진 N장» 이라 말하는데 옛 `boxes.py` 는 아직 **0줄 txt 를 만듭니다** | 정적 파일이 서버보다 먼저 나가는 구조(§8-10) 때문입니다. **3차가 재시작하면 사라집니다.** `c5_fix.py --before` 가 그 상태를 그대로 보여 줍니다 |
| 3 | 🔴 **시험 4개가 다른 세션의 백업 이동으로 죽습니다**(§3-3). 제 사본은 고쳤지만 **정본 시험(`260918_paint/cycle_2/stage2b/` 쪽)은 아직 죽습니다** | 그 폴더는 남의 사이클 산출물이라 손대지 않았습니다. 구조 정리 쪽이 «시험 코드에도 `grep '_backup_'` 을 돌려» 남은 참조를 고치거나, 다음 사이클이 제 사본을 쓰면 됩니다 |
| 4 | 🟡 `t6_view` 남은 실패 4건은 **문턱 문제**(0.5%)이고 코드 결함이 아닙니다(§3-4) | 문턱을 사진마다 정하는 것은 이 사이클의 목록에 없습니다 |
| 5 | 🟡 **`instances_source` 값이 같은 폴더에 두 이름**입니다 — 블루베리 `detect_seed`, 복숭아 `psm_watershed`(둘 다 박성문 님 `instance_maps`) | 지시가 «`psm_watershed`(블루베리와 같은 «추정» 표기)» 였고, 블루베리 값을 바꾸면 **지난 빌드와 대조가 끊깁니다**. 값 사전에 «성격은 같다» 를 적었습니다. 하나로 합칠지는 사람이 정할 일입니다 |
| 6 | 🟡 **복숭아 `instances/` 가 «추정» 인 것을 코드가 강제하지는 않습니다** — 학습 쪽이 `instances_source` 칸을 읽지 않고 `instances/` 폴더만 보면 추정이 정답처럼 쓰입니다 | 통합본은 아직 학습에 들어가지 않았습니다(0920 이 첫 실전). Codex 질문 C-4 로 넘겼습니다 |

---

## 5. 팀용 판정서 재료 (3차가 `문서/260919_상자카운팅_5회검수_최종판정.md` 로 낼 것)

형식은 `문서/260919_툴_방향전환_5회검수_최종판정.md` 와 같게 맞췄습니다.
아래 숫자는 전부 각 사이클 판정서·이번 회귀 로그에서 가져온 **실측값**입니다.

### 5-1. 5사이클 표

| 사이클 | 무엇을 | 고친 결함 | 실서버 재시작 | 판정서 |
|---|---|---|---|---|
| 1 (공격적) | 개수 칸·확정 유도·`counts.csv`·통합 manifest 개수 칸·상자 정의 문서화 | **11**(2차 즉시 7 + 3차 전 총괄 결정 4) + 문서 정정 4 | **2026-09-19 16:41:06~09** → PID 3915126 → **263865** | `cycles/260919_count/cycle_1/stage3_final.md` |
| 2 (보통) | 논문 근거 대조표 1판(28칸) + 어긋난 문서 수정 | **코드 0** · 문서·대조표 정정 **24곳**(인용 3 · 대조표 11 · 문서 3 · 판정 뒤집기 1 · 1차 문서 4 · 자기 정정 2) | 없음(실서버 접속 0) | `cycle_2/stage3_final.md` |
| 3 (공격적) | 대조표 공격 검수(인용 51건 글자 대조 · 쪽 번호 전수 재실측 · 순환 튜닝 · 포도 실측) | **코드 0** · 판정 뒤집기 **4** · 문맥 이탈 **4** · 쪽 번호 **7** · 대조표 33항목 | 없음 | `cycle_3/stage3_final.md` |
| 4 (보통) | M1 팀 워터셰드 초벌 · M17 `count_metrics.py` · 그림 3종+근거 PDF · 브라우저 회귀 · 문서 | **20**(1차 13 + 2차 6 + 포도 끄기 1) | **2026-09-19 21:45:04~07** → PID 263865 → **964494** | `cycle_4/stage3_final.md` |
| 5 (종합) | 소수정 4 + 시험 위생 2 + 최종 판정서·Codex 질문·카톡 | **4** + 위생 2 | **3차가 켠다**(파이썬은 `boxes.py` 하나) | `cycle_5/stage3_final.md`(3차) |

검수 방식: 홀수 사이클은 공격적 리뷰, 짝수는 보통 리뷰, 5는 종합(곽동신 지시 2026-09-18).
코드는 모래상자에서 만들고 **3차가 켠다**(§8-10 규칙). 사이클 2·3 은 **대조표 전용**이라 코드를
한 글자도 건드리지 않았습니다(«한 파일 한 작업자» — 사이클 1 이 같은 파일을 쥐고 있었습니다).

### 5-2. 고친 결함 전체 표 (사이클 1~5 · 코드 결함 **35건**)

| # | 사이클 | 무엇이 잘못됐나 | 어떻게 고쳤나 |
|---|---|---|---|
| 1 | 1 2차 | 🔴 개수 캐시가 지문을 안 봐, 번호를 95→10개로 고쳐 저장해도 화면·`counts.csv` 가 «95개»(거짓)를 말한다 — 지표 표 전체가 무의미해진다 | `instances.py count_fresh()` 신설 · `server.py`·`export_dataset.py` 가 그것을 쓴다 |
| 2 | 1 2차 | 상자를 저장(=확정)했는데 화면이 «사람 확정 개수: 아직 없습니다», 전부 지우면 거꾸로 «2개» — 두 값을 반대로 말한다 | `app.js cntReload()` — 그 한 장만 `/api/item` 을 다시 묻는다(규칙은 서버에만) |
| 3 | 1 2차 | 명령줄 내보내기가 «나갈 사진 0장» 이면 `FileNotFoundError` 로 죽는다(README 가 적어 둔 명령이 바로 그것) | `export_dataset.py` — 0장이어도 폴더·«0장» manifest·counts.csv |
| 4 | 1 2차 | `counts.csv` 와 manifest 가 판정을 따로 읽어 **다른 스냅샷**이 된다(내보내는 중 누가 확정하면 갈린다) | `counts_rows(..., st=)` — 같은 스냅샷 한 번만 읽는다 |
| 5 | 1 2차 | «개수만» 내보낸 폴더를 목록이 «빈 폴더» 라고 적어 사람이 실패로 읽는다 | `api_export_list` 의 빈 폴더 판정에 `n_counts` 를 넣었다 |
| 6 | 1 2차 | README §19 설명이 사실과 달랐다(칸 5개 ↔ 실제 6개 등) | `app/README.md` §19-1·19-3·19-4 정정 |
| 7 | 1 2차 | 앞 사이클 시험의 낡은 기대값 때문에 회귀가 «새로 깨진 것» 으로 보인다 | `260918_paint/cycle_3/stage1/t2_ui.py` 기대값 (4,3,2)→(4,4,2) |
| 8 | 1 3차전 | 🔴 문구는 «툴은 고르지 않습니다» 인데 코드는 어긋날 때 **번호 쪽 수를 골라** 지표로 보냈다 | `boxes.py human_count()` 가 어긋나면 비우고 `conflict` · 통합 빌더도 같은 규칙 |
| 9 | 1 3차전 | 데이터셋에서 **빠진 사진**도 `counts.csv` 에 남아 MAE·R² 의 분모가 달라진다 | `exclude_reason()`·`included_stems()` 를 꺼내 «나간 사진» 만 담는다(4줄 → 1줄) |
| 10 | 1 3차전 | 번호 확정 사진을 열 때마다 다시 세어 한 장 218 ms 를 계속 낸다 | `count_now()` — 지문과 함께 캐시에 적는다(218 ms → **0.32 ms**) |
| 11 | 1 3차전 | 어긋남 노랑이 «확정» 초록을 덮고, 옛 서버 가드가 «값 없는 사진» 과 같은 글자다 | 확정이 있으면 초록 우선 · 옛 서버는 `개수 ?·?·?` |
| 12 | 4 1차 | 🔴 번호 없는 과일에서 툴이 이진 마스크를 **4-연결 CC** 로 세어 초벌이 어긋난다(복숭아 MAE 0.70 ↔ 팀 워터셰드 0.30) | `instances.py SEED_DIRS` 에 복숭아(+포도) · `seed_source` 를 API·화면에 실었다 |
| 13 | 4 1차 | 팀원 상자 파일이 그 사진에 없으면 초벌이 **404** 로 끝나 아무것도 안 나온다 | 마스크·번호로 물러서고 `fallback_from` 을 실었다(네 칸 404 → 200) |
| 14 | 4 1차 | M1 뒤 «복숭아·포도는 번호가 없습니다» 가 거짓이 됐는데 화면·문서에 그대로 남았다 | `ui.js`·`app.js`·`help.html` 4곳·`README` 2곳 |
| 15 | 4 1차 | 한 번의 내보내기가 판정을 **세 번** 읽어, manifest 는 «제외» 인 사진이 상자 YOLO 로 나갔다 | `export_worker` 가 맨 앞에서 한 번 읽어 셋에 같은 스냅샷을 넘긴다 |
| 16 | 4 1차 | `/api/item` 안에 «번호 수» 가 둘이라(옛 `n_inst`) 고쳐 저장하면 값이 갈린다 | `n_inst = cnt["instances"]` 로 맞췄다 |
| 17 | 4 1차 | `mk_reg_c4.py` 가 «있으면 그대로» 라 09-18 사본을 계속 돌려 **낡은 기대값 실패가 재생산**된다 | 있으면 멈추고 정본 md5 를 찍는 `mk_reg_cnt4.py`·`mk_c4_cnt4.py` |
| 18 | 4 1차 | 1픽셀·0폭 상자가 YOLO txt 에 `w=0.000781` 로 그대로 나간다(저장 때는 버리는데) | `export_boxes_to()` 에도 같은 `MIN_SIDE` · `dropped_small` 로 알린다 |
| 19 | 4 1차 | 그림 3종 **파일**이 08-11·08-12 판이라 논문·발표 그림이 옛 단정문을 담고 있었다 | `reports/figures/` 3종 재생성(18:50) |
| 20 | 4 1차 | 근거 PDF 스크립트만 08-12 그대로라 «낙과 미라벨(단정)»·«미라벨 유지» 를 찍는다 | `make_evidence_workflow_pdf.py` L126·L282~283 + PDF 재생성 |
| 21 | 4 1차 | 화면 «내 수정본» 색은 시안인데 사용법 그림만 초록이다 | `make_help_figs_260917.py` 색 통일 + `ex1~ex5.jpg` 재생성 |
| 22 | 4 1차 | 🔴 1차가 `app.js` 최상위에서 `window.` 를 가드 없이 만져 `boxsim.js` **50항목이 통째로 죽었다** | `typeof window !== "undefined"` 가드 — 50/50 복구 |
| 23 | 4 1차 | M1 으로 앞 사이클 시험 **5곳**의 기대값이 거짓이 됐다 | 정본·사본 양쪽 + 결정 번호 주석(옛 증거는 지우지 않고 칸을 옆에 추가) |
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
| 25 | 4 2차 | 🔴 `fig_samples_annotated` 캡션 **마지막 두 줄(교수님이 넣으라던 낙과 문장)이 그림 밖으로 잘렸다** | 그림 높이 +0.9in · 재생성(아래 여백 72px) |
| 26 | 4 2차 | 통합 개수 시험이 **공용** 캐시에 시험용 항목 5개를 쓴다 | `test_counts.py` 가 `INST.CACHE_DIR` 도 모래상자로 돌린다 |
| 27 | 4 2차 | 🔴 화면이 «나갈 상자 1장» 이라 적고 저장을 누르면 «0장» 이라며 거절한다 | 내보내기 가드가 상자 장수를 따로 본다 |
| 28 | 4 2차 | «⚠순환» 이 **사람이 다 맞힌 줄**에도 붙어 거짓 설명이 달린다 | 알려진 순환 짝만 ⚠순환, 나머지 오차 0 은 **⚠오차0** |
| 29 | 4 2차 | help 그림 `ex4_box.jpg` 위 여백 0px 로 제목 윗선이 깎였다 | `save()` 에 바깥 여백 + 5장 재생성 |
| 30 | 4 2차 | 화면·문서의 거짓 문장 3종(복숭아 번호 출처를 «포도 CERTH» 라 함 등) | `app.js`·`help.html` 8곳·`README` +18 |
| 31 | 4 2차 | 🔴 1차가 **지난 사람 결정**(포도 번호 켜지 않기 · 교수님 확인 8번 미답)을 뒤집은 채 넘겼다 | 포도를 **껐다**(되살릴 한 줄은 주석) · 시험 기대값 되돌림 |
| **32** | **5 1차** | 사과 ③ 번호 하단 줄이 **원본 정답 번호를 «AI 초벌»** 이라고 불러, 사람이 «AI 가 만든 것이니 고쳐야겠다» 고 읽는다 | `ui.js` — `seed_source=gt_numbers` 일 때 «원본 정답 번호»(글자 예산 그대로) |
| **33** | **5 1차** | «사람 확정만 + 개수» 가 **빈 표**를 주는데 화면이 미리 말하지 않는다(사이클4 시나리오가 여기서 막혔다) | `ui.js expPlan()` 에 안내 한 줄(규칙은 그대로) |
| **34** | **5 1차** | 통합 빌더가 **복숭아 번호본을 몰라** 툴에서 번호를 확정해도 manifest 가 `count_source=none` 이다 | `build_merged_dataset.py SEED_DIRS` + 복숭아 · `instances_source=psm_watershed`(추정) |
| **35** | **5 1차** | 상자가 **0개인 옛 json** 이 0줄 YOLO txt 로 나가 검출 학습이 «열매 없음» 정답으로 읽는다 | `boxes.py export_boxes_to()` 가 건너뛴다(이름은 알려 준다) |

### 5-3. 논문 근거 대조표 최종 집계 (`문서/260919_논문근거_대조표.md` 에서 **다시 셈** — 2026-09-19 22:2x)

| 판정 | 칸 수 | 어느 칸 |
|---|---:|---|
| **일치** | **11** | 1-b · 1-c · 2-a · 2-b · 2-c · 2-d · 4-a · 4-b · 4-c · 5-b · 5-c |
| **부분** | **7** | 1-a · 3-b · 5-a · 5-d · 6-b · 6-c · 7 |
| **어긋남** | **6** | 1-d(복숭아 CC) · 1-e(포도 CC) · 1-f(4↔8 연결) · 2-f(modal/amodal) · 3-a(라벨 범위 규칙 미정) · 3-c(그림 캡션 — **부분 해결**) |
| **근거 없음** | **2**(+1) | 2-e(`other` 클래스) · 6-a(2,073,600) · (+2-f 의 사과 쪽은 «어긋남» 과 겹쳐 셈) |
| **오지정 정정** | **1** | 5-e(`demsar2006` 은 누수 근거가 아니다 — Friedman 검정 근거로만) |
| **PDF 없음/미대조** | **1** | 6-d(SNIP·Amarnath·Ullah 3편) |
| **합계** | **28** | 11+7+6+2+1+1 |

- 인용 **51건** 전부 `pdftotext` 로 **글자까지 일치**(1차 51 · 2차 84건 대조) · **지어낸 인용 0건**.
- 🔴 표의 `p.N` 은 **PDF 쪽**입니다. **6편 22건은 프리프린트라 논문에 옮길 수 없고**,
  `seo2024peach` 는 **심사용 판**(4·5·7쪽 «FOR PEER REVIEW»)입니다 → **M19**(출판판 쪽 재확인)가
  논문 인용 **전에** 끝나야 합니다.
- 대조표 머리의 경고를 판정서에도 그대로 옮길 것: 이 근거들은 **사후 정당화**입니다.
  «논문을 보고 설계했다» 가 아니라 «**이렇게 했는데, 알아보니 같은 방법이 문헌에 있더라**» 가 정확합니다.

### 5-4. M1~M19 상태표

| M | 무엇 | 상태 |
|---|---|---|
| **M1** | 툴 초벌을 팀 워터셰드 출력으로(포도는 CC/정답 송이) | ✅ **사이클 4 완료** · 🆕 **사이클 5 가 통합 빌더까지 맞춤**(복숭아) · 포도는 **끔**(교수님 8번) |
| M2 | 4↔8 연결 통일 | ✅ **M1 으로 흡수**(워터셰드가 CC 를 대체) · 이유 문장은 «정확해서가 아니라 도구를 일치시키려고» |
| M3 | `counts.csv`·manifest 에 정답 개수 칸 + 지표 계산 | ✅ **완료**(사이클 1 칸 12개 · 사이클 4 `count_metrics.py`) |
| M4 | 상자 정의를 과일별 문장으로(«지금은 modal» 한 줄 금지) | ✅ **완료**(README §10 · help 카드) |
| M5 | `other`→`trunk` 또는 «선례 없음» 명시 | 🧑 **교수님 결정**(143편에 다른 사례 **0**) |
| M6 | «포도 송이 상자는 알에 맞지 않는다» 한 줄 | 🟡 **부분** — README §16 에 **실측 문장은 있고**(붙은 송이 합쳐짐·가려진 송이 쪼개짐) `santos2020grapetracking` p.2 **인용 표기는 없음** |
| M7 | help «낙과·뒷줄 나무는 규칙 미정 — 손대지 말 것» | ✅ **완료**(README §10 + help 빨간 카드) |
| M8 | SNIP·Amarnath·Ullah **PDF 재확보** | 🧑 **사람(로컬 세션)** |
| M9 | 마스크 크기 변경 NEAREST ↔ one-hot+argmax | 🧑 **0920 재빌드 결정**(교수님) |
| M10 | `demsar2006` 을 누수 근거로 쓰지 않기 | ✅ **문서 반영**(대조표 5-e) · 논문 집필에서 지킬 것 |
| M11 | 교수님확인 2·9·10 에 **원논문 근거**를 붙여 재상정 | 🧑 **사람** — 근거는 준비됨(MinneApple p.4 · `seo2024peach` p.5) |
| M12 | 그림 3종 재생성 + 근거 PDF 정정 | ✅ **완료**(사이클 4 1차 18:50 · 2차가 캡션 잘림까지 고침) |
| M13 | 통합 README 분할 각주(포도는 사진 한 장 = 사실상 무작위) | ✅ **완료** |
| M14 | 포도 `n_gt` = CERTH 송이 · «자동으로는 못 센다» 를 결과로 싣기 | ✅ **코드 전수 확인 완료**(2,502장 전부 일치 · 합계 9,832) |
| M15 | 매개변수는 **사과 정답으로 골랐고** 숫자는 **정답 마스크 입력 = 상한** | ✅ **완료 3곳**(README §20-2 · 재학습 계획 §11 · 빌더 README) |
| M16 | `n_team_park` ↔ `n_team_im` 은 **바이트까지 같은 파일** | ✅ **README 반영** · 🧑 **임성후 님 확인이 먼저**(칸을 줄이는 것은 그 뒤) |
| M17 | MAE·RMSE·R² 계산 스크립트 | ✅ **완료**(`tools/count_metrics.py` · `--selftest` 포함) |
| M18 | **예측 마스크로 센 값이 없다**(모든 개수가 `mask_source=gt`) | ⏭ **재학습 단계**(0920 이후) |
| M19 | `seo2024peach` 출판판 재확보 · 프리프린트 5편 쪽 확인 | 🧑 **사람 — 논문 인용 전 필수** |

✅ 완료 **11** · 🟡 부분 **1**(M6) · 🧑 사람·교수님 **6**(M5·M8·M9·M11·M16 후속·M19) · ⏭ 뒤 단계 **1**(M18).

### 5-5. 카운팅 지표 표 (`tools/count_metrics.py` · **포도 끈 판** = 지금 실서버와 같은 설정)

식은 `farjon2023countingreview` **PDF p.16 = 인쇄 p.1698** 의 식 (1)~(4) 그대로입니다
(MAE·RMSE·R² + 총량편향%는 박성문 님 `eval/summary.md` §7.2 의 Σŷ/Σy−1). 손계산 3장과 일치하고
`--selftest` 5건이 통과합니다.

| 과일 | 출처(무엇으로 센 값인가) | 사진 n | 정답 합계 | 센 합계 | 총량편향% | MAE | RMSE | R² |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 복숭아 | **열매 번호**(= 툴 초벌 = 박성문 워터셰드) | 125 | 977 | 939 | −3.89 | **0.30** | 0.73 | **0.986** |
| 복숭아 | 박성문 자동 상자 | 125 | 977 | 939 | −3.89 | 0.30 | 0.73 | 0.986 |
| 복숭아 | 임성후 자동 상자 | 125 | 977 | 939 | −3.89 | 0.30 | 0.73 | 0.986 |
| 복숭아 | 사람이 저장한 상자 | 1 | 4 | 0 | −100.00 | 4.00 | 4.00 | — |
| 사과 | **열매 번호**(원본 마스크의 정답 번호) **⚠순환** | 429 | 17,567 | 17,567 | +0.00 | 0.00 | 0.00 | 1.000 |
| 사과 | 박성문 자동 상자 | 429 | 17,567 | 17,700 | +0.76 | **2.14** | 3.09 | **0.985** |
| 사과 | 임성후 자동 상자 | 429 | 17,567 | 17,700 | +0.76 | 2.14 | 3.09 | 0.985 |
| 포도 | 박성문 자동 상자 | 2,403 | 9,424 | 14,546 | **+54.35** | 2.43 | 3.58 | **−1.406** |
| 포도 | 임성후 자동 상자 | 2,403 | 9,424 | 14,546 | +54.35 | 2.43 | 3.58 | −1.406 |
| 포도 | 사람이 저장한 상자 | 1 | 9 | 0 | −100.00 | 9.00 | 9.00 | — |

꼬리줄(실측): «읽은 줄 4,071개 중 **정답이 없어 뺀 줄 1,114개** · 표 4개».
**블루베리는 표에 없습니다** — 정답 상자(`gt_boxes`)가 아예 없습니다(1,114줄 전부 제외).
논문에도 그렇게 적을 것. 포도를 켜면 «포도 / 열매 번호 **⚠순환** 2,403·9,424·9,424·+0.00·0.00·0.00·1.000»
한 줄이 더 붙습니다(그것이 켠 판과 끈 판의 **유일한 차이**).

**«⚠순환» 과 «⚠오차0» 의 뜻**(스크립트가 표 아래에 직접 적습니다)
- **⚠순환** = 센 값과 정답이 **같은 파일**에서 나온 줄(사과 원본 번호 ↔ 그 마스크로 만든 `gt_boxes` ·
  포도 CERTH 송이 ↔ 같은 송이로 만든 `gt_boxes` · 통합본 `n_boxes` ↔ `boxes_source=psm_gt`).
  MAE 0 · R² 1.000 은 정확도가 아니라 «**두 경로가 어긋나지 않는다**» 는 확인입니다.
  논문에 «우리 방법이 오차 0» 으로 쓰면 **사후 정당화**입니다.
- **⚠오차0** = 오차가 0 이지만 순환이라 **단정하지 않는** 줄. ① 순환일 수도 ② 표본이 적어
  **사람이 정말 다 맞힌** 것일 수도 있습니다 → `gt_source`·`human_source`(또는 `boxes_source`·
  `instances_source`)로 확인한 뒤 쓰십시오. (사이클 4 2차가 반례를 만들어 잡은 자리입니다 —
  사람이 두 장 다 맞힌 표에 «같은 파일에서 나왔습니다» 라는 **거짓 설명**이 달렸습니다.)

🔴 **논문 표에 쓸 때 주의 2가지**(사이클 4 2차 §4-5)
1. **복숭아·블루베리에서 «열매 번호»·«박성문»·«임성후» 세 줄은 같은 값입니다** — 125장 전부에서
   `n_instances == n_team_park`(M1 이 툴 초벌을 박성문 워터셰드로 바꿨으니 당연). 표에서는 세 줄이
   같은 숫자로 나란히 보여 «세 방법이 일치한다» 로 오해하기 쉽습니다 — **한 번의 측정이 세 줄로
   보이는 것**입니다. (사과 330장 · 포도 1,857장에서는 번호와 팀원 상자 값이 다릅니다.)
2. **사과 표는 1,001장이 아니라 429장**이고 정답 합계도 40,468 이 아니라 **17,567** 입니다
   (툴이 근접중복 대표만 들고 있습니다). 캡션에 «사과 429장(대표 1장 기준)» 과
   «**정답 마스크 입력 기준 = 상한**»(M15)을 함께 적어야 합니다.

### 5-6. 교수님·팀원에게 물을 것 (이번 5사이클이 **새로 만든 분량**)

**교수님(안홍렬)**
1. **modal / amodal 을 어느 쪽으로 통일할까** — 지금은 **사과 한 과일 안에서 섞여** 있습니다
   (MinneApple 사진은 가려진 데까지 원으로 통째 = amodal 150개체 중 92 · dataset1~3 은 보이는 것만).
   복숭아는 원논문이 **amodal 이라고 명시**(`seo2024peach` p.11)하고, MinneApple 원문에는
   **폴리곤 범위를 적은 문장이 아예 없습니다**(대조표 2-f).
2. **포도 «송이» 의 정의**(교수님 확인 8번, 아직 답 없음) — 이것이 오기 전까지 포도 번호 초벌은
   **끈 채**로 둡니다(`app/instances.py` 주석 한 줄만 되살리면 켜집니다). 실측: 닿은 송이 31.4% ·
   조각난 송이 43.5% · 자동 계수 CC +62.1% / 워터셰드 +123.5% → **자동으로는 못 셉니다.**
3. **클래스 `2 other` 를 `trunk` 로 바꿀까** — PDF **143편에 `other` 를 쓴 사례가 0건**입니다.
   남기면 README 에 «논문 선례 없음 — 학습에 쓰지 않음» 을 적겠습니다.
4. 🆕 **낙과(땅에 떨어진 과실)를 빼자는 권고** — 질문이 아니라 **근거 있는 권고**로 올립니다:
   MinneApple(p.4)과 `seo2024peach`(p.5) **두 원저자가 모두 낙과를 뺐습니다**
   (*"except for dropped fruits"*). 우리는 규칙이 없어 **낙과가 보이는 42장 중 5장만** 라벨돼
   있습니다. 빼기로 하면 남은 17장은 사람이 다시 손봐야 합니다.
5. **임성후 님 산출물 확인**(M16) — `im_seonghu/bbox_outputs` 가 박성문 님 것과 **4,823개 파일
   바이트까지 동일**합니다. 두 칸을 «서로 대조» 로 쓰면 **없는 독립성을 주장**하는 것이 됩니다.
   본인 산출물인지 확인한 뒤 칸을 하나로 줄일지 정해야 합니다.
6. 🆕 **포도 번호 초벌을 켤까**(위 2번과 한 묶음) · 🆕 **복숭아 `instances/` 를 «추정» 으로 통합본에
   넣는 것**을 그대로 두어도 되는지(0920 빌드부터 125장에 생깁니다 · 값 `psm_watershed`).

**박성문** ⑦ 블루베리 정답 상자(지금은 워터셰드 추정 1,114장) ⑧ 상자 YOLO 폴더 구조·클래스 번호
⑨ 🆕 `bbox_outputs/peach/all/instance_maps` 를 통합 데이터셋의 `instances/` 로 쓰는 것에 동의하는지
(그 폴더의 `FALLBACK["grape"]="cc"` 주석은 **박성문 님 파일이라 우리가 고치지 않았습니다**).

### 5-7. 사람이 지울 것 (툴·데이터를 건드리는 삭제라 **제가 하지 않았습니다**)

| # | 무엇 | 왜 · 무엇이 함께 사라지나 |
|---|---|---|
| 1 | `260916_라벨링툴/app/cache/instance_counts/peach.json` | 시험이 남긴 **가짜 항목 5개**(`c01_both_same`·`c02_both_diff`·`c04_inst_only`·`c08_conf_nofile`·`c09_inst_nocache`)뿐이고 **실제 사진은 한 장도 없습니다**(실측). 지우면 복숭아를 열 때 한 장씩 다시 셉니다(한 장 66 ms) |
| 2 | `260916_라벨링툴/data/peach/boxes/210629-t4-17.json` | 상자 **0개**인 시험 잔재. 지우면 지표 표의 «복숭아 / 사람이 저장한 상자 n=1 · MAE 4.00» 줄과 «건너뛴 사진 1장» 경고가 함께 사라집니다 |
| 3 | `260916_라벨링툴/data/grape/boxes/740.json` | 같은 것(상자 0개). 지우면 «포도 / 사람이 저장한 상자 n=1 · MAE 9.00» 줄이 사라집니다 |
| 4 | 모래상자 폴더 — `cycles/260919_count/cycle_5/stage1/{sandbox, sandbox_before, sandbox_oldpy, sandbox_old4, sb_chars_before, sb_chars_after, c4_cnt5/sandbox*, reg_cnt5/sandbox, mergebuild_before, mergebuild_after, mergebuild2, t4_boxes_before, t4_boxes_after}` · 사이클 4 의 `cycle_4/stage2/{sandbox*, onoff_*, c4_cnt4b, reg_cnt4b, mergebuild2, counts_*}` | 시험용 사본입니다(각각 수백 MB). **로그·json·md 는 남기십시오** — 그것이 증거입니다 |
| 5 | `platform/work/kwak_dongsin/260919_c5_fix_placeholder.txt` | 🔴 **제 실수로 만든 빈 파일**입니다(22:15 무렵). 지우려 했지만 삭제 권한이 없어 그대로 있습니다 — **지워도 아무 영향이 없습니다.** 정직하게 적어 둡니다 |

### 5-8. 숫자 사전 (실측·출처)

| # | 숫자 | 뜻 | 출처 |
|---|---|---|---|
| 1 | **35건** | 5사이클이 고친 **코드 결함**(1: 11 · 2: 0 · 3: 0 · 4: 20 · 5: 4) | 각 사이클 판정서 · §5-2 |
| 2 | **28칸** | 논문 근거 대조표(일치 11·부분 7·어긋남 6·근거 없음 2·오지정 1·PDF 없음 1) | `문서/260919_논문근거_대조표.md` §2 |
| 3 | **51건 / 0건** | 글자까지 대조한 인용 / 지어낸 인용 | 대조표 부록 A·H |
| 4 | **143편** | `01_references` PDF 전수(1차 «145» 는 오기) | 사이클 3 |
| 5 | **125 / 0 → 125** | 0920 빌드에서 복숭아 열매 번호 장수(그 전 판에는 없었음) | `--dry-run` 22:3x |
| 6 | **1,543 → 1,668** | 통합본 «열매 번호» 합계(사과 429 + 블루베리 1,114 + **복숭아 125**) | 갱신법 §5① |
| 7 | **0.30 / 0.73 / 0.986** | 복숭아 번호 초벌 MAE·RMSE·R²(옛 CC 는 MAE **0.70**) | `count_metrics` 표 |
| 8 | **2.14 / 3.09 / 0.985** | 사과 팀원 자동 상자 MAE·RMSE·R²(429장·정답 17,567) | 같은 표 |
| 9 | **+54.35% · R² −1.406** | 포도 팀원 자동 상자 — **자동 계수 실패**(정답 송이 9,424) | 같은 표 |
| 10 | **1,114줄** | 정답이 없어 지표에서 뺀 줄 = 블루베리 전부 | 같은 표 꼬리줄 |
| 11 | **0건** | 사람 확정(마스크·상자·번호 세 칸 전부) — 재학습의 1순위는 GPU 가 아니라 «Enter» | `status.json` |
| 12 | **PID 964494 · 21:45:04** | 지금 실서버(사이클 4 3차가 켠 것) — 사이클 5 는 **붙지 않았습니다** | `ps -o lstart` 22:53 |
| 13 | **16:41:06~09 · 21:45:04~07** | 이번 5사이클의 실서버 재시작 두 번(사이클 1 · 사이클 4) | 각 3차 판정서 |
| 14 | **전 11 실패 → 후 22/0** | 소수정 4 전/후 시험 | `c5_fix_before.log`·`c5_fix_after.log` |
| 15 | **+0자 · +0자 · +0자** | 편집 화면 글자 증감(마스크 162·상자 267·번호 225자) | `b3_chars` |
| 16 | **205개 / 4곳** | 다른 세션이 아카이브로 옮긴 백업 / 그 때문에 죽은 시험 | `_archive/backups_260920/INDEX.md` · §3-3 |
| 17 | **1,326 / 2,664 / 482 / 2,583줄** | `ui.js` / `app.js` / `boxes.py` / `build_merged_dataset.py`(고친 뒤) | `wc -l` 22:1x |
| 18 | **9,832송이 / 2,502장** | 포도 CERTH 정답 송이(툴이 보는 2,403장과 다릅니다 — 원본 전체 기준) | 사이클 3·4 |

---

## 6. Codex 질문 두 파일에 넣은 «C» 절 (요약)

`문서/260918_Codex질문_공격적.md` · `문서/260918_Codex질문_보통.md` 의 **B 절 뒤에** 같은 구조로
`## C. 「상자 + 개수 세기」 5사이클 + 논문 근거 대조 (2026-09-19)` 를 붙였습니다.

- **C-1** 대조표 §요약 — 집계 28칸 · 항목별 한 줄 · 🔴 «p.N 은 PDF 쪽 · 6편 22건은 프리프린트» ·
  🔴 «이 근거는 **사후 정당화**» 경고 원문.
- **C-2** 판정서 재료 요약 — 5사이클 표 · 고친 결함 35건의 갈래 · M1~M19 상태 · 지표 표와
  «⚠순환/⚠오차0» 설명 · 논문 표 주의 2가지.
- **C-3** **검증 안 된 가정 12개**(아래 §6-1).
- **C-4** 질문 — 공격적 파일은 «깨뜨릴 곳» 5문, 보통 파일은 «팀원이 쓰는 관점» 5문.
- B-3 과 같이 **판정서 전문은 3차가 붙입니다**(지금은 자리만 만들어 두었습니다).

### 6-1. 검증 안 된 가정 12개 (그대로 물어볼 목록)

1. 복숭아 `instances/`(0920부터 125장)는 «추정» 인데, 학습·평가 코드가 `instances_source` 를 읽지
   않고 폴더만 보면 **정답처럼 쓰인다** — 그런 자리가 실제로 있는지 아직 확인하지 않았다.
2. 사과 «원본 정답 번호» 가 정말 정답이라는 근거는 원본 데이터셋 문서가 아니라 «마스크에 번호가
   들어 있다» 는 **우리 관찰**뿐이다.
3. `n_gt`(정답 개수)는 박성문 님 `gt_boxes/csv/detections.csv` **줄 수 = 상자 수**다. 그것을
   «과일 개수» 로 쓰는 것이 맞는지(포도는 **송이** 수) 원저자 확인이 없다.
4. ⚠순환 줄(사과·포도 번호)은 정확도가 아니라 일관성 확인인데, **논문 표에 섞일 위험**이 남아 있다.
5. 워터셰드 매개변수(`PEAK_FRAC=1.1`·`RADIUS_PCT=90`)는 **사과 정답으로 골라** 복숭아·블루베리에
   그대로 옮겼다(복숭아 최적은 **0.7 이하**로 계속 좋아진다 = 끝값).
6. 모든 개수는 **정답 마스크 입력**에서 나온 값 = **상한**이다. 예측 마스크로 센 값은 아직 없다(M18).
   참고로 기각된 FruitSeg30 에서 마스크 IoU 중앙 0.977 인데도 CC 개수가 **−72%** 달라졌다.
7. `n_team_park` ↔ `n_team_im` 은 **4,823개 파일이 바이트까지 같다** — 독립 확인이 아니다(M16).
8. 사람 확정이 **0장**이라 «사람 확정만» 경로는 실자료로 한 번도 안 돌았다(빈 표만 봤다).
9. 포도 «송이» 정의가 미결이라 포도 번호 초벌은 **끈 채**인데, 그 상태에서 포도 개수 지표
   (정답 송이 9,424/9,832)를 논문에 싣는 것이 일관적인가.
10. 상자 정의가 **사과 안에서 modal·amodal 로 섞여** 있다(MinneApple amodal 92/150 · dataset1~3 modal).
11. 낙과 규칙이 없다(보이는 42장 중 **5장만** 라벨).
12. 사이클 5 가 만든 새 불일치 — 화면 precount(파일 존재) 1장 ↔ 실제 txt 0장(§4-1).

---

## 7. 카톡 문구

`kds0206/문서/260919_카톡_상자카운팅.txt` — 순수 텍스트 **25줄**(표·마크다운·이모지 없음), 실측 숫자만.
3차가 그대로 보내거나 한 줄씩 지우면 됩니다.

---

## 8. 2차가 그대로 다시 돌릴 명령

```bash
PY=/home/kds0206/.conda/envs/kwak/bin/python
T=/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴
S=/data/project/2026summer/kds0206/semantic-segmentation
C=$T/cycles/260919_count/cycle_5/stage1

# (1) 소수정 4 «전» 판 — 11건이 실패해야 정상
$PY -c "import sys;sys.path.insert(0,'$C');import lib_cnt5 as M;M.L.sync(M.SB_OLD);M.L.reset_status(M.SB_OLD)"
cd $C && $PY -u c5_fix.py --before     # 통과 11 / 실패 11
# (2) «후» 판
cd $C && $PY -u c5_fix.py              # 통과 22 / 실패 0
# (3) 회귀 한 벌(약 24분 · 포트 5381~5383·5911~5913·5931·5932·5941~5969)
bash $C/runall_c5.sh > $C/runall_c5_outer.log 2>&1
grep -E '^###|rc=|ALLDONE' $C/runall_c5_outer.log
# (4) 아카이브 이동 때문에 죽었던 4개 · b5 는 sandbox_old4 로
cd $C/reg_cnt5/reg_s2 && $PY -u s1_api.py && $PY -u s8_guard_advance.py
cd $C/reg_cnt5/reg_s1 && $PY -u t4_paint8.py
$PY -c "import sys;sys.path.insert(0,'$C');import lib_cnt5 as M;M.sync_old4();M.L.reset_status(M.SB_OLD4)"
cd $C && $PY -u b5_oldserver.py        # 6 / 0
# (5) 통합 dry-run 으로 «0920 에 복숭아 번호 125» 확인(파일을 만들지 않습니다)
cd $S && $PY -W error::DeprecationWarning tools/build_merged_dataset.py --dry-run | grep -A3 '과일'
# (6) 지표 표 · 손계산 · ⚠오차0 반례
$PY $S/tools/count_metrics.py --selftest
$PY $S/tools/count_metrics.py --counts $T/cycles/260919_count/cycle_4/stage2/handcalc/peach
$PY $S/tools/count_metrics.py --counts $T/cycles/260919_count/cycle_4/stage2/handcalc/loopcheck_peach --fruit peach
```

**제 산출물**(전부 `cycle_5/stage1/`): `c5_fix.py`·`c5_fix_{before,after}.{log,json}` ·
`runall_c5.sh`·`runall_c5_outer.log`·`sync.log` · `b1_browser`·`b2_export`·`b3_chars`·`b4_fallback`·
`b5_oldserver` 의 `.py/.log/.json` · `c4_cnt5/`(t1_api·t2_ui·regress_all) · `reg_cnt5/`(reg_s0~s2 +
runall_stage1 + `rerun_archivefix.log`) · `tm_*.log`(통합 6) · `reg_{sim,modesim,boxsim}.log` ·
`mk_c4_cnt5.py`·`mk_reg_cnt5.py` · 모래상자 6개 · `mergebuild_{before,after}/`(시험용 통합본) · `t4_boxes_{before,after}/`(④ 시험 입력) ·
`_parts/`(이 보고서를 이어 붙인 조각 — 내용은 이 파일과 같습니다) · 스크린샷 `~/ff_shots/cnt5/`.

---

## 9. 제가 만지지 않은 것

- **실서버 5111(PID 964494 · 21:45:04 기동)** — 재시작·로그인·붙기 **0회**(`ps` 로 두 번 확인) ·
  교수님 5100·5101·5105 **0회** · `pkill -f` **0회** · 남의 프로세스 kill **0회**.
- 공용 `T/data` · `T/exports` — **한 글자도 쓰지 않았습니다**(쓰기는 모래상자 안에서만 ·
  개수 캐시도 모래상자 `app/cache/` 만 · `T/exports` 는 만들지 않았습니다).
- **`260916_라벨링툴/tests/` · `_archive/`** — 다른 세션이 만들고 있는 폴더입니다. **읽기만** 했고
  (아카이브에서 옛 백업을 **읽어** 쓴 것이 전부) **한 글자도 쓰지 않았습니다.**
- 원본 `datasets_resized_2mp` · 검수판 `_260916`·`_260917` · 통합본 `_260918`·`_v2`·`_v3` ·
  팀원 폴더(박성문·최인훈·임성후) · `01_references` — **읽기만**.
- 다른 사이클 산출물 — `cycle_1~4/` 의 보고서·로그·json 은 **읽기만** 했습니다. 예외는 §2-3 의
  시험 **정본 두 개**(`260918_paint/cycle_4/stage1/t2_ui.py` · `260918_paint/stage1/t6_view.py`)이고,
  둘 다 백업을 남기고 «기대값·표본» 만 고쳤습니다(까닭은 §3-2·§1-5).
- **삭제 0** · GPU 0 · 새 패키지 0 · 실서버 재시작 0 ·
  `작업기록.md` 갱신 0 — 이번 지시가 **금지**했습니다(디렉터리 `CLAUDE.md` 의 «요청마다 갱신» 보다
  이 지시가 우선 · 3차·총괄이 마지막에 한 번에 씁니다).
- ⚠ 예외 하나를 정직하게: 22:15 무렵 **실수로 빈 파일**
  `platform/work/kwak_dongsin/260919_c5_fix_placeholder.txt` 를 만들었고 지우려 했으나 삭제 권한이
  없어 그대로 있습니다(§5-7 목록 5).
