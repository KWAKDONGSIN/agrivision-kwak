작성: 2026-09-25

# E02 전체 회귀 결과 (코드 C00~C20 전부 끝난 뒤)

- 돌린 것: `tests/_out/e02/run_e02.sh` (22:07:01 시작) — `run_all.sh --browser` → `run_paint_e2e.sh 5441` → `run_e01.sh 5441`, 모두 `auto/tests.lock` 으로 한 번에 하나씩.
- 원본 로그: `tests/_out/e02/` (run_all_browser.log · bundles.tsv · paint_e2e.log · e01.log · times.txt · rerun_*.log · before_*.log)

## 결과

| 묶음 | 결과 | 끝난 시각 |
|---|---|---|
| run_all 문법·unit·sim·api·merged (브라우저 전) | 전부 통과 (merged 536/0 포함) | |
| run_all --browser 브라우저 묶음 25개 | 21개 통과, 4개 실패(아래) | 22:29:30, rc=1 |
| 그림판 e2e (파이어폭스·크로미움) | 138/138 ×2, 디스크 대조 둘 다 통과 | 22:31:37, rc=0 |
| E01 (✨ Shift+클릭·키보드만) | 27/27 ×2 | 22:32:14, rc=0 |

bundles.tsv 합계는 통과 2,283 / 실패 7 이다(b15 는 중간에 멈춰 개수가 안 잡혔다).

## 실패 4개 — 이번 사이클 탓이 아님

사이클 전 코드(`snapshot_before/app`)를 모래상자에 넣고 같은 시험을 돌려 비교했다(`tests/_sandbox/e02_before_src/run_before.py`).

| 시험 | 실패 내용 | 판정 | 근거 |
|---|---|---|---|
| b1_browser (77/4) | 복숭아 `210629-t1-01`·사과 `20150919_174151_image1` 의 seed_source 가 기대(team:박성문·gt_numbers)가 아니라 `human_fixed` | 자료가 바뀜. 시험 기대값이 낡음 | 실데이터 `data/peach/instances_fixed/210629-t1-01.png`(09-23 13:56), `data/apple/instances_fixed/20150919_174151_image1.png`(09-24 20:06) 이 생겨서 서버가 «사람이 고친 번호» 를 먼저 쓰는 것이 맞다 |
| b15_pressed | 옛 툴 목록에서 카드를 못 찾아 시간 초과 | 가끔 나는 실패 | 혼자 다시 돌리니 55/0 통과 |
| b24_shadow_mobile (49/1) | ⑩ 휴대폰 편집 화면 높이 732.3 대 730.3(2px) | 이번 사이클 전부터 있음 | 사이클 전 코드로도 같은 값으로 실패 |
| t2_ui (25/2) | 다-3 «할 일» 자동 확대 없음 · 마-1 편집 화면 글자 310·323자(300자 기준) | 이번 사이클 전부터 있음 | 사이클 전 코드로도 같은 값으로 실패 |

이번 사이클이 옛 툴(`/old`)에서 바꾼 것은 `app/static/js/main.js` 의 `#exp=` 링크 처리(C03) 하나뿐이다.
그림판 쪽 회귀는 없다.

## Z01 에 넘길 것

- Z01 «전체 회귀 통과» 는 위 4개 때문에 `--browser` 가 rc=1 이다. 앱 버그가 아니라 옛 툴 시험의 기대값·자료 문제라서 코드 항목으로 올리지 않았다.
- b1 은 사람이 아직 안 고친 사진으로 STEM 을 바꾸거나 `human_fixed` 도 받게 고치면 된다. b24 ⑩·t2_ui 다-3·마-1 은 옛 툴 화면 기준이 언제부터 어긋났는지 따로 봐야 한다.
