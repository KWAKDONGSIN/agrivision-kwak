작성: 2026-09-25

# C00 기준선 — 업그레이드 전 그림판 시험·속도 (w1)

잰 시각 2026-09-25 18:32~18:42 KST(`date` 실측). 모든 시험은 모래상자(포트 5441, 자동 포트)에서만 돌았다. 실서버 5111 은 건드리지 않았다.
서버 부하는 load average 약 21(80코어), ✨ 도우미는 GPU7(측정 전 사용률 0%, 970MiB).
원자료는 `auto/baseline_260925_raw/` 에 복사해 두었다.

## 1. 시험 통과

| 시험 | 결과 |
|---|---|
| `tests/run_all.sh --only unit,api` (1차) | 517 통과 / 5 실패 — 전부 `api/t1_api` |
| 같은 것 (시험 고친 뒤 2차) | **522 통과 / 0 실패** (37초) |
| `tests/browser/run_paint_e2e.sh 5441` 파이어폭스 | **64/64 통과** + 디스크 대조 통과 |
| 같은 것 크로미움 | **64/64 통과** + 디스크 대조 통과 (둘 합쳐 76초) |

1차 실패 5건의 원인은 앱이 아니라 시험의 가정이었다. 실자료 `data/apple/status.json` 에 2026-09-24 20:06:45 사람이 그림판으로 저장한
사과 첫 장(`20150919_174151_image1`, 마스크·상자·번호 확정 + `boxes/` 파일)이 생겼다. 모래상자는 실자료를 복사하므로
«확정 0 에서 시작» 을 가정한 단정(가-5·다-5·라-0·라-2·라-4)이 1씩 어긋났다.
`tests/api/t1_api.py` 가 시작 전 확정·상자 파일을 세어(`take_pre`) 기대값에 더하도록 고쳤다. 백업 `tests/api/_backup_260925_C00_t1_api.py`.

## 2. 속도 (새 스크립트 `tests/browser/run_perf_paint.sh 5441` → `perf_paint.mjs`)

재는 법. 창 크기 1366×768, 캐시 없는 새 창. 시각은 Playwright 쪽 벽시계(ms).
- 첫 화면 로드 = 주소 입력 → 사진·라벨이 다 그려지고 로딩 표시가 꺼질 때까지(새 창 3번).
- 사진 넘김 = 바꾼 것 없이 D 키 → 다음 사진이 다 그려질 때까지(과일마다 8번).
- ✨ 첫 클릭 = 그 사진에서 처음 누른 ✨ 클릭 → 칠하기 끝날 때까지(사진 3장). 두 번째 = 같은 사진 다른 자리.

| 항목 (중앙값 / 90% / 최대, ms) | 크로미움 | 파이어폭스 |
|---|---|---|
| 첫 화면 로드 | 804 / 1348 / 1348 | 903 / 1872 / 1872 |
| └ DOMContentLoaded | 158 / 207 / 207 | 142 / 581 / 581 |
| 사진 넘김 — 복숭아 | 469 / 556 / 556 | 596 / 663 / 663 |
| 사진 넘김 — 사과 | 532 / 585 / 585 | 956 / 1026 / 1026 |
| ✨ 첫 클릭 | 255 / 280 / 280 | 285 / 442 / 442 |
| ✨ 같은 사진 두 번째 클릭 | 150 / 164 / 164 | 164 / 165 / 165 |
| (도우미가 알려 준 계산 시간) | 80 / 181 | 171 / 255 |

읽는 법(쉽게).
- 첫 화면은 1초 안팎이다. 가장 느린 값(1.3~1.9초)은 늘 첫 번째 창이다(서버·도우미가 처음 읽는 비용).
- 사진 넘김은 0.5초 정도, 파이어폭스의 사과(큰 사진)만 약 1초다. 업그레이드 뒤 이 값이 커지면 회귀로 본다.
- ✨ 는 0.3초 안이다. 도우미 계산은 0.1~0.2초이고 나머지는 전송·칠하기다.
- 한 번씩만 잰 값이라 ±20% 흔들림은 잡음으로 본다(서버 load 약 21).

## 3. 다음 레인에게
- 회귀 확인은 같은 명령 두 개(`run_paint_e2e.sh`, `run_perf_paint.sh`)를 5441 에서 다시 돌려 이 표와 비교하면 된다. 전체 시험과 겹치지 않게 `flock auto/tests.lock` 으로.
- 측정 중 알게 된 작은 것. ✨ 로 칠한 뒤 Ctrl+Z 로 전부 되돌려도 «● 저장 안 됨» 이 남아 D 를 누르면 확인창이 뜬다. 버그인지 의도인지는 정하지 않았다(코드 항목으로 올리지 않음).
