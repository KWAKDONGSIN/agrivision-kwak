심각한 문제 0개, 수정이 필요한 기능 문제 2개입니다.

- `app/static/paint/paint.js:894` (`simAccept`의 `++next`) · 새 번호의 상한 검사가 없습니다 · 기존 최대 번호가 65534이면 다음 후보가 예약값 65535(`HOLE`)가 되고, 이후에는 `Uint16Array`에서 0 또는 기존 번호로 넘쳐 열매가 사라지거나 합쳐집니다 · `next + 1 >= HOLE`이면 추가를 중단하고 안내해야 합니다.

- `app/static/paint/paint.js:564` (`wlTick`, 입력 이벤트 등록) · 작업 시간 측정에서 드래그 중 움직임을 기록하지 않습니다 · 붓·올가미로 60초 넘게 연속 작업하면 다음 입력에서 최대 60초만 합산되어 실제 작업 시간이 누락됩니다 · `pointermove`도 활동으로 기록하고, 긴 입력 공백은 별도로 유휴 처리해야 합니다.
