심각한 문제 2개(저장 불일치 1개는 «추정»), 그 외 버그 3개입니다.

- **높음 · `app/static/paint/paint.js:1385` (`sort.onchange`)** · 정렬 변경이 저장 확인 없이 사진을 바꿉니다. · «안 한 것만»을 켜고 현재 사진을 저장한 뒤 다시 수정하고 정렬을 바꾸면, 현재 사진이 필터에서 빠져 미저장 수정이 사라집니다. · `leaveOk()`를 거치고 취소하면 정렬 선택도 복구하세요.

- **높음 · `app/static/paint/paint.js:868` 부근 (`simAccept`) · «추정»** · `S.busy` 검사 없이 저장 도중 라벨을 변경할 수 있습니다. · 후보를 띄운 상태에서 저장하고 응답 대기 중 «모두 받기»를 누르면, 순차 전송되는 마스크·번호·상자가 서로 다른 편집 상태를 담거나 미저장 변경의 표시가 지워질 수 있습니다. · 저장 중 후보 수락을 막고, 저장 데이터는 동일한 스냅샷에서 만드세요.

- **중간 · `app/static/paint/paint.js:881` 부근 (`const v = ++next`)** · 새 번호의 상한을 검사하지 않습니다. · 번호가 `HOLE`에 도달하면 열매가 번호 없는 영역으로 취급되고, 65536부터는 `Uint16Array`에서 0 또는 기존 번호로 순환하여 삭제·병합됩니다. · 할당 전에 `next + 1 < HOLE`을 확인하고 부족하면 중단하세요.

- **중간 · `ai_helper/sam_server.py:156` (`big[...] = em[...]`)** · 본보기와 타일의 교집합을 계산하지 않고 음수 슬라이스를 사용합니다. · 같은 번호의 떨어진 조각이나 큰 열매로 본보기 경계 상자가 `T`보다 커지면, 대입 배열 크기가 달라져 유효한 본보기도 검색에 실패합니다. · 양쪽 좌표의 교집합을 계산해 대응하는 부분만 복사하세요.

- **중간 · `app/api/instances.py:682–685`** · 숫자 범위 제한으로 `NaN`을 제거하지 못합니다. · `edits: "NaN"`은 `int(v)`에서 예외가 나 500이 되고, `active_s: "NaN"`은 비표준 JSON 값으로 로그에 기록됩니다. · 변환 직후 `math.isfinite(v)`로 검사하고 비유한 값은 거부하거나 0으로 처리하세요.
