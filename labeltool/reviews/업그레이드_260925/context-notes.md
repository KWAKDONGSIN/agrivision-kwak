작성: 2026-09-25

# 결정 기록 — 260925 업그레이드

- 2026-09-25 10:50 (대화 세션, Opus 5.5) 사이클 설계. 09-21 `run_auto.sh` 를 바탕으로 레인 8개로 나눔.
  앱 코드는 w1 한 레인만 고친다 — 같은 `paint.js` 를 두 세션이 동시에 고치면 서로 덮어쓰기 때문.
- 09-21 사이클에서 한도에 걸린 판 27번이 «resets 5:20am» 만 찍고 헛돌았다 → 이번엔 리셋 시각을 읽어 그때까지 잔다.
- 시작 시점 코드 사본: `snapshot_before/`(app 전체 + ai_helper 코드). Codex 검토 기준선.
- 09-24 밤 작업 결과: 초벌 v4(1536)는 mAP50 0.8882 vs v2 0.8847 로 «낫지 않다» 판정 → draft_v2 유지. `datasets_merged_260924` 후보 빌드 검사 통과(정본 전환은 사람 몫).
- GPU: 7번(✨ 도우미)이 이미 씀 → 사용자 «3장 정도» 에 맞춰 새로 쓰는 건 5·6번만.
- 2026-09-25 10:59(추정) 사용자 «연구실 한도 생각해서 최적으로» → 레인 4개로 축소(w1 Opus 코드·r1 Sonnet 읽기/정리·f1 Fable 검수·cx Codex). 조사처럼 읽고 요약하는 일은 Sonnet 이 싸다.

2026-09-25 18:27 [r1] N01 녹취 대조 완료. 새로 읽기보다 09-22 전수목록(G01~G78)을 재료로 툴 관련 지시만 골라 09-08·09-15 원문으로 확인했다(한도 절약).
툴에 빠진 것은 카운팅 100장 표본 진행률·닮은 사진 표시·재학습 첫 단계 등 5개를 코드 항목 C01~C05 로 올림. 결과 문서 문서/260925_녹취지시_툴대조.md.

2026-09-25 18:31 [r1] D01 끝. 후보 0924 와 정본 v3 를 manifest·파일 해시로 대조했다(새 사진 0, 빠진 107장 = 포도 103 중복·복숭아 2·블루베리 2, 내용 바뀐 5장).
정본 전환은 사람 몫이라 권고만: 0924 를 후보로 삼되 포도 분할 묶음 2403→2300 이 sweep 시작 전 시점에 맞아야 하고, 복숭아 instances 122장은 자동 초벌임. 문서/260925_데이터셋0924_비교.md.
- 2026-09-25 18:32 w1 C00 이 2번 즉시 실패(rc=1): `~/.npm-global/bin/claude` 2.1.278 이 claude-opus-5-5 를 모름(2.1.280 이상 필요). run_lane.sh 가 `~/.local/bin/claude`(2.1.282)를 쓰도록 고치고 w1 만 재시작. r1·f1 은 옛 버전으로도 Sonnet/Fable 이 돌아 그대로 둠.

2026-09-25 18:33 [r1] R01 끝. 논문 폴더엔 라벨링 도구 자료가 거의 없어(내부 논문 1편) 인터넷 3검색+1열람으로 보완했다. 후보 6개를 jev 로 점수화: 비슷한 열매 한꺼번에 찾기 3.75 > 불확실 순서 2.78 > 의심 열매 2.65 > ✨ 모양 경고 2.35 > 시간 기록 1.93 > 이웃 사진 전파 1.19(제외).
상위 5개를 코드 C06~C10 으로 추가(총 코드 항목 10개). 결과 문서 문서/260925_라벨링툴_리서치.md. SAM3 도입 가능성과 초벌 신뢰도 저장 여부는 확인 못 함(문서에 명시).

- 2026-09-25 18:34 (f1, Fable) F01 그림판 감사 끝. 실서버 대신 모래상자 5443(`auto/f1_sandbox.sh`)에 Playwright(`auto/f1_audit.mjs`)로 파이어폭스·크로미움 둘 다 재서 `문서/260925_그림판_감사.md` 로 정리했다.
  왜 모래상자인가 — ✨·붓질을 실제로 눌러야 하는데 실서버에 칠하면 저장 위험이 있어서. 저장은 안 했고 끝나고 껐다(rm -rf 없이 rsync --delete 로 새로 만듦).
  결론 — 속도의 병목은 사진 PNG 3~4MB + /instances 서버 0.2~0.3초(넘김 0.4~1.1초) → 미리 받기(C06)·캐시(C07)가 제일 큼. 접근성은 팔레트 키보드 불가(17개)·aria-live 없음·대비 4곳 미달. 코드 항목 C06~C13 여덟 개를 추가했다(상한 8 지킴, 전체 코드 항목 14/30).
  jev 두 번째 의견은 쓰지 않았다 — 측정값이 그대로 근거라서.

2026-09-25 18:34 [r1] M01 끝. 논문 폴더에 SAM·라벨링 도구 자료는 없었고 툴과 이어지는 3편만 요약했다(FewShot 라벨 선택=불확실 순서 근거, MinneApple=작고 빽빽한 열매, Lin2025=인스턴스 라벨이 객체 지표의 전제). 새 코드 항목은 없음(불확실 순서는 이미 있음).
체크리스트 코드 구역에 C06~C10 번호가 두 번 쓰였다(23~27줄 vs 31~35줄). 번호 정리는 w1/f1 몫으로 남김. 문서/260925_참고논문폴더_툴관련요약.md.
- 2026-09-25 18:35 (f1) 체크리스트 코드 번호 충돌 정리 — r1 이 같은 시각에 넣은 리서치 항목 다섯 개가 내 C06~C10 과 번호가 겹쳐 r1 것을 C14~C18 로 바꿨다(내용은 안 건드림). 지금 코드 항목 19/30. 모래상자 5443 은 setsid 손자 프로세스가 남아 있어 cwd 로 확인하고 껐고, `f1_sandbox.sh stop` 도 그렇게 고쳤다.

2026-09-25 18:36 [r1] M02 끝. 감사 문서를 코드와 대조해 속성 0건·/instances 무캐시는 사실로 확인했다.
C06·C07 미리 받기/캐시는 저장 뒤 옛 번호가 보일 위험이 있어 안전조건을 코드 항목 C19 로 추가(총 19개). 순서는 C06→C07→C11→C09 최소판→C13, C12 는 보류 권고. 문서/260925_그림판_감사_두번째의견.md.

2026-09-25 18:38 [r1] S01 끝. 사용법 문서를 index.html·paint.js 와 대조해 틀린 곳 5(✨ 후보 순서·돋보기 도구 동작·보호옵션 범위·오른쪽 클릭·mAP 근거 없음) + 빠진 단축키 표를 auto/S01_사용법점검.md 에 정리했다.
문서 자체는 고치지 않았다(점검 항목이라). 브라우저로 눌러 보지는 않았고 소스 대조만 했다.

2026-09-25 18:42 [w1] C00 기준선 끝. unit·api 1차 5건 실패는 앱 버그가 아니라 09-24 20:06 실자료에 사람 확정 1장(사과 image1)이 생겨 t1_api 의 «확정 0 에서 시작» 가정이 깨진 것이라, 시험이 시작 전 확정 수를 세어 더하도록 고쳤다(tests/api/t1_api.py, 백업 _backup_260925_C00_t1_api.py). 재실행 522/0.
그림판 e2e 파이어폭스·크로미움 64/64. 속도는 새 스크립트 tests/browser/run_perf_paint.sh(→perf_paint.mjs)로 재서 auto/baseline_260925.md 에 표로 남겼다(첫 화면 ~0.8~0.9초, 넘김 ~0.5초, ✨ ~0.3초). 뒤 레인이 같은 명령으로 회귀 비교하게 하려고 스크립트로 만들었다.

2026-09-25 18:50 [w1] C01 끝. 표본은 서버를 안 고치고 그림판(paint.js sampleOf)에서 «과일|이름» 해시로 정했다 — 목록 순서·사람과 상관없이 같은 100장이고 파이썬 재시작이 필요 없어서.
FNV 만 쓰니 포도 1214~1222 처럼 이웃 영상 장면이 몰려 뽑혀 murmur3 마무리를 더했다(이웃 쌍 4개 = 무작위 수준). 진행률의 «확정» 은 ✓ 만 세고 ✕(뺌)는 개수 정답이 아니라 따로 적는다.
사진이 새로 들어오면 표본이 바뀔 수 있어 지금 목록을 auto/C01_표본목록.json 으로 고정해 두었다(C01_make_sample_list.mjs 가 paint.js 의 함수를 잘라 써서 규칙이 둘로 갈라지지 않음). 시험 e2e 67/67×2 · run_all 822/0.

2026-09-25 19:04 [w1] C02 끝. 서버는 안 고쳤다 — /api/item 이 이미 묶음·대표·구성원 확정(dup_members·dup_rep·dup_member_confirmed)을 주고, «대표만 남기기» 는 옛 «묶음째 확정»(/api/status confirm+stems) 한 번으로 한다.
옛 /api/exclude_group 은 안 썼다. 그 API 는 AI 판정 칸(status)만 바꾸고 «아직 안 봄» 인 장만 건드리는데, 실자료 묶음 구성원 대부분(사과 647·포도 98·블루베리 81)이 이미 AI 3회 검수로 exclude 라 단추가 거의 아무 일도 안 하고, 그림판 목록(사람 확정만 보는 ✓·✕)에도 안 보여서다. 사람 확정은 status 를 안 건드리므로 대표가 바뀌지 않고 내보내기 결과도 그대로다.
묶음 되돌리기 단추는 두지 않았다(서버에 «확정 지우기» 주소가 없음). 한 장씩은 그 사진을 열고 파일 → 빼기 취소. 곁가지로 markOption 의 slice(2) 가 빈칸 «　»(한 글자) 줄에서 번호 첫 자리를 자르던 버그를 relabel() 로 고쳤다. e2e 75/75×2 · run_all 1358/0.

2026-09-25 19:12 [w1] C03 끝. 그림판 상태줄에 «사람 확정 N장 · 마지막 내보내기 MM-DD HH:MM (n장)» 과 «내보내기 ↗» 링크를 붙였다(paint.js loadExports/renderExport, index.html #expinfo). 서버는 안 고쳤다 — /api/export_list 가 이미 과일·끝난 시각·장수를 준다.
«확정» 은 목록의 ✓ 수(✕ 뺌 제외)다. 빈 폴더(empty 표) 내보내기는 «마지막» 으로 치지 않는다 — 아무것도 안 나간 것을 재학습 준비로 보이면 안 돼서. 내보내기는 옛 툴 창에서 하므로 그림판 창으로 돌아오면(focus) 다시 읽는다.
링크는 /old#exp=과일 이고, 옛 툴 main.js 가 이 주소를 보면 그 과일로 곧장 «데이터 정리» 탭을 연다(옛 툴이 기억하는 과일 localStorage "fruit" 도 그 과일로 바뀜). 확정 시각이 목록 API 에 없어 «내보낸 뒤 새 확정 있음» 표시는 짐작이 되므로 넣지 않았다. e2e 78/78×2 · run_all --no-merged 822/0.

2026-09-25 19:21 [w1] C04 끝. 그림판 보기 메뉴 «512 크롭 네모» 와 단축키 C 로 사진 가운데 512×512(사진 화소 기준) 자리에 빨간 선을 그린다(paint.js drawHud·ACTS toggle-crop, 기본은 꺼짐). 녹취 08-10 에서 교수님이 «AI 는 512 로 자르고, 평가는 센터 크롭» 이라 해서 가운데 자리로 정했다(학습의 무작위 자리는 크기만 같다).
가정: 모델 입력 전 리사이즈가 없다고 보고 툴이 여는 사진 화소 그대로 512 를 쓴다(블루베리는 이미 1440² 로 줄인 데이터라 그 크기 기준). 서버는 안 고쳐 재시작 없음. e2e 80/80×2 · run_all --no-merged 822/0.

2026-09-25 19:28 [w1] C05 끝. 사과·포도를 고르면 메뉴 아래에 노란 한 줄(#rule)로 «규칙 미정(교수님 답 대기)» 과 무엇이 미정인지 적는다(사과 = 가려진 부분 칠하기·낙과·뒷줄, 포도 = 송이/알·지주대). 블루베리·복숭아는 줄을 숨긴다.
근거는 문서/260917_교수님확인_7건.md 1·3·9·10번이고 작업기록에도 답이 나온 흔적이 없어 «미정» 으로만 적었다. 글은 paint.js RULES 한 곳에만 있어 답이 나오면 그 글만 고치면 된다. 도움말 «기본 규칙» 에도 한 줄 안내를 붙였다.
시험에 과일을 바꾼 직후 목록이 다 안 읽힌 채로 재는 틈이 있어 S.item.fruit 가 포도가 될 때까지 기다리게 했다. 서버는 안 고쳐 재시작 없음. e2e 82/82×2 · run_all --no-merged 822/0.

2026-09-25 19:44 [w1] C06 끝. 사진을 열면 목록 앞·뒤 사진의 상세·사진(디코드)·번호 L 까지 미리 받아 풀어 둔다(paint.js loadBundle·prefetchAround·PF). 넘길 때는 /api/item 을 새로 받아 «상태 지문»(크기·has_fixed·status·at·확정·번호확정·counts)이 같을 때만 쓰고, 다르면 새로 받는다 — 남이 그새 저장한 사진을 낡은 번호로 덮어쓰지 않게(두번째의견 §2-1, C19 의 화면 쪽 조건). 원본 다시 불러오기는 안 씀, 내 저장·빼기·대표만 남기기 때 그 항목 삭제, 10분 넘으면 버림. 여는 중 D 를 또 누르면 마지막 것만 기억해 이어서 연다.
속도(모래상자, 1.5초 본 뒤 D 중앙값): 크로미움 복숭아 125ms·사과 160ms, 파이어폭스 복숭아 178ms·사과 448ms(전 534~973ms). 곧바로 연달아 누르면 미리 받기 전이라 그대로다. 파이어폭스에 남은 0.3초는 큰 사진을 실제로 그리는 비용(첫 동기 레이아웃에서 약 0.1초씩)이라, «그린 뒤 확인»·«캔버스 재사용» 을 해 봤지만 재어 보니 이득이 없어 되돌렸다(단순한 쪽 유지).
서버(C07 캐시·지문, C19 서버 쪽)는 안 고쳐 재시작 없음. perf_paint.mjs 에 «1.5초 본 뒤 넘김» 을 더했다. e2e 85/85×2(C06 3개: 미리 받은 라벨 = 새로 받은 라벨 · 남이 바꾸면 새로 받음 · 빨리 D 3번 = 3장 뒤) · run_all --no-merged 822/0.
(덧붙임) 단계별 시간 재기에 쓴 임시 모래상자 tests/_sandbox/c06probe/ (1.6GB, sb_* 복사본 4개)를 남겼다. rm -rf 금지 규칙이라 지우지 않았으니 사람이 확인 뒤 지워도 된다.

2026-09-25 19:51 [w1] C07 끝. `/instances` 에 파일 지문 ETag(Cache-Control: private, no-cache)와 304, 그리고 서버 메모리 캐시(48장, 넣은 순서로 버림)를 붙였다(app/api/instances.py inst_etag·serve_instances). 지문은 고친 번호·팀 초벌·원본 마스크·고친 이진 마스크 네 파일의 경로+inode+mtime_ns+크기 전부라, 저장·되돌림·마스크 저장 중 무엇이든 바뀌면 새로 만든다(C19 서버 쪽 조건). 저장 경로의 load_inst 는 캐시를 안 쓴다.
속도(모래상자 사과): 첫 238ms → 같은 브라우저 304 6ms · 다른 브라우저 메모리 4ms. 앞으로 처음 여는 사진은 그대로 0.2초대이고, C06 미리 받기가 그 몫을 숨긴다. 새 시험 tests/api/t5_inst_cache.py(8개, 포트 5441 고정, run_all 에 등록 — 저장 뒤 옛 지문이면 200+새 번호, 되돌리면 처음 지문). run_all --no-merged 830/0 · e2e 85/85×2. 실서버 restart.flag → 19:51:01 재시작 확인.
C19 는 화면 쪽(C06)과 서버 쪽(C07) 조건이 이제 다 들어갔으니, 다음 라운드에서 확인만 하고 닫아도 된다.

2026-09-25 20:00 [w1] C08 끝. 팔레트 색 칸(.sw 전부 — 지우기·＋·?·번호)·지금 색 #curbox·작업자 #who 에 role=button + tabindex=0 을 붙이고, keydown 에서 Enter(늘)·스페이스(Tab 으로 왔을 때만)를 click 으로 바꿨다(paint.js renderPalette·keydown, index.html, paint.css). 진짜 button 으로 바꾸면 기본 모양·#statusbar>div 규칙이 흔들려서 div 를 그대로 두는 쪽을 골랐다.
스페이스는 «끌어서 옮기기» 단축키라, 마우스로 칸을 누른 뒤(초점이 칸에 남음) 스페이스는 예전처럼 옮기기여야 한다. 처음엔 :focus-visible 로 가렸는데 크로미움은 키보드를 쓴 적이 있으면 마우스 클릭 뒤에도 focus-visible 이라 실패해서, mousedown/Tab 으로 마지막 조작을 직접 기억(viaMouse)하게 바꿨다. 크로미움에선 그때 칸에 점선 테두리가 보일 수 있다(모양만).
#curbox 는 원래 누르는 기능이 없어서 «누르면 팔레트의 지금 색 칸으로 초점·스크롤 + 상태줄에 지금 색» 을 붙였다. «?» 칸은 누르면 설명을 상태줄에 쓴다. 선택된 칸에 aria-pressed. e2e 94/94×2(C08 8개) · run_all --no-merged 830/0. 정적 파일만이라 재시작 없음.

2026-09-25 20:05 [w1] C09 끝. #msg·#loading 에 role=status+aria-live=polite, say() 가 실패(err)일 때만 assertive 로 바꿔 바로 읽히게 했다. 도구 9개 중 data-tool 8개와 «원본 보기» 에 aria-pressed(setTool·applyView 에서 맞춤), 메뉴 단추 전부(닮은 사진 포함)에 aria-haspopup/expanded — 메뉴를 여닫는 곳이 여럿이라 .menu 의 class 를 MutationObserver 로 지켜보고 맞추게 했다(한 곳에서만 관리). 과일·사진·찾기 칸 aria-label, 탭 제목 = «사진이름 - 라벨 그림판»(사진 없으면 원래대로), 여는 중 #stage aria-busy.
#loading 은 display:none 으로 숨겨졌다 나타나는 방식이라 스크린리더에 따라 «불러오는 중» 을 안 읽을 수 있다. 연 뒤 #msg 의 «열었습니다» 는 읽히므로 그대로 두었다. e2e 100/100×2(C09 6개) · run_all --no-merged 830/0. 정적 파일만이라 재시작 없음.

2026-09-25 20:10 [w1] C10 끝. 열린 메뉴에서 ↑↓ 로 항목(쓸 수 있는 단추·링크·색 진하기 칸)을 돌고(끝에서 처음으로 넘어감), 닫힌 메뉴 단추에서 ↓ 는 메뉴를 열고 첫 항목으로 간다(paint.js keydown). Esc 는 초점이 메뉴 안이거나 body 였을 때만 그 메뉴 단추로 되돌린다 — 이름 찾기 칸에서 Esc 를 눌렀는데 초점이 메뉴로 튀면 안 되기 때문. 메뉴 항목을 고르면(키보드든 마우스든) 동작 전에 초점을 그 메뉴 단추로 옮긴다(click 처리) — 확인창이 닫힌 뒤에도 초점이 단추로 돌아온다.
←→ 는 여전히 이전·다음 사진이라 메뉴 사이 옮기기는 안 넣었다(단축키 충돌). e2e 104/104×2(C10 4개) · run_all --no-merged 830/0. 정적 파일만이라 재시작 없음.

2026-09-25 20:14 [w1] C11 끝. 상태줄 바탕(#c0c0c0) 위 실패 글자 #b00000→#8b0000(4.06→5.50), 완료 글자 #006000→#004d00(4.32→5.60). 제목줄 «● 저장 안 됨» 은 파란 띠 오른쪽(#1084d0)에서 2.9 였는데, 노란 글자는 그대로 두고 글자가 있을 때만 검은 칩 배경(:not(:empty))을 깔아 15.17 로 올렸다 — 노란 = 경고라는 익숙한 느낌을 살리려고. 도구 이름 11→12px(스포이트도 단추 안에 들어감을 시험으로 확인).
감사 §3 의 팔레트 흰 숫자 3.52 는 그림자가 있고 이번 항목 범위 밖이라 손대지 않았다. 제목줄 흰 글자도 오른쪽 끝(#1084d0)에서는 약 4.1 이지만 굵은 14px·사진 이름은 왼쪽(진한 남색)에 있어 두었다. e2e 106/106×2(C11 2개) · run_all --no-merged 830/0. 정적 파일만이라 재시작 없음.

2026-09-25 20:26 [w1] C12 끝. say() 가 늘 #msg.title 에 전체 글을 넣는다. 1024×640 이하(paint.css @media 와 paint.js SMALL 같은 기준)에서 상태줄 칸은 두 줄 고정 높이(-webkit-line-clamp 2) · 알림 칸 최소 45% · 나머지 칸 5em 까지 줄어듦. flex-wrap 으로 두 줄 배치도 해 봤지만 그림 영역이 800×600 에서 341→288 로 줄어 버렸다.
도구 설명은 <details class="tip"> 로 감싸 작은 화면에서만 «▸ 설명» 으로 접힌다(큰 화면은 summary 숨김·늘 펼침). 펼친 상태는 도구를 바꿔도 유지. toggle 이벤트가 키 입력보다 늦게 와서, setTool 이 다시 그리기 직전에 지금 details.open 을 직접 읽게 했다. 800×600 에서 «진행»·«사람 확정» 칸은 말줄임이 되지만 두 칸 다 원래 title 이 있다. e2e 112/112×2(C12 5개) · run_all --no-merged 830/0. 정적 파일만이라 재시작 없음.

2026-09-25 20:33 [w1] C13 끝. 감사의 117ms 는 다시 재 보니 안 나왔다(첫 붓질의 JIT 로 보임). 대신 손 뗄 때 changed() 의 computeCenters 가 사진 전체를 훑어 크로미움 12~30ms·파이어폭스 13~22ms 를 매번 썼다(tests/browser/perf_stroke.mjs, 결과 tests/_out/perf_stroke/*.json).
그래서 번호마다 화소 수·x 합·y 합(CN·CX·CY)을 들고 있다가 pushUndo·swapRect 가 이미 도는 «바뀐 네모» 안에서 바뀐 화소만 더하고 빼게 했다(tally). 사진을 열 때·초벌을 불러올 때는 예전처럼 전체를 다시 센다. changed() 는 CR 2~9ms·FF 6~15ms, 손 뗀 뒤 다음 그림까지 CR 10~48ms. requestIdleCallback 미루기는 번호·팔레트가 늦게 바뀌어 헷갈릴 수 있어 쓰지 않았다.
✨ 계산 중(samClick·samNeg·samBox)에는 #stage 에 sambusy class → 커서 progress(도구 커서가 inline style 이라 !important). e2e 115/115×2(C13 3개: 부분 갱신 = 전체 다시 센 값 · changed 8ms 이하 · 커서) · run_all --no-merged 830/0. 정적 파일만이라 재시작 없음.

2026-09-25 21:04 [w1] C14 끝. ✨ 도우미에 POST /similar(ai_helper/sam_server.py _similar), 라벨링 툴에 중계 /api/sam_similar(app/api/ai.py), 그림판에 «🔍 비슷한 열매 찾기(G)» 를 붙였다. 지금 색(번호)의 열매가 본보기. 도우미는 본보기 색과 가까운 자리에 반 지름 간격으로 씨앗 점을 뿌리고, 사진을 지름 약 10배 칸으로 나눠 칸마다 SAM 을 한 번 돌려 씨앗마다 모양을 고른 뒤 크기 0.35~2.8배·둥근 정도·볼록·색 분포·SAM 특징 닮음(코사인 0.55)으로 거르고 겹침을 정리한다. 화면은 노란 테두리 후보 → 누르면 뺌(빨간 테두리)/다시 넣음 → Enter 모두 받기(후보마다 새 번호, 빈 곳·회색에만, 노란 동그라미, Ctrl+Z 한 번) · Esc 버림.
처음엔 색·모양만 봤더니 잎을 많이 잡았다(맞은 비율 10~20%). SAM 특징 닮음을 재 보니 맞는 후보는 54개 중 52개가 0.5 이상, 틀린 후보는 324개 중 68개만 0.5 이상이라 기준으로 넣었다. 밝기(L)는 0.35배만 본다(그늘진 복숭아와 해 받은 복숭아). 시제품 측정(사람 번호가 있는 사진 7장, tests/_out/c14/eval_default.txt): 한 번 찾기 찾은 비율 0.28·맞은 비율 0.52, 못 찾은 열매로 한 번 더 찾으면 0.40. 본보기에 따라 차이가 커서 사과 한 장은 그늘진 본보기 12% · 밝은 전형적 본보기 48/95(후보 62개). 2MP 사진 1~7초.
찾는 동안 도우미 LOCK 을 쥐므로 다른 사람의 ✨ 클릭은 몇 초 기다린다. 기준값은 환경변수(SIM_*)로 바꿀 수 있다. 도우미는 run_helper.sh restart 로 20:53 에 다시 켰다(GPU7). 시험: tests/api/t6_sam_similar.py(도우미 직접, 10개 — run_all 에는 도우미가 필요해 안 넣음) · e2e 124/124×2(C14 9개) · run_all --no-merged 830/0(u6 승인 주소에 /api/sam_similar 추가). restart.flag → 21:04:01 재시작 확인.

2026-09-25 21:11 [w1] C15 끝. 그림판 목록 옆에 «이름 순 / 헷갈리는 순» 고르기 칸(#sort, 고른 것은 localStorage paint.sort 에 기억)을 붙였다. 헷갈리는 순은 사람 미확정(✓·✕ 아님) 사진을 앞에, 점수 큰 순. 점수 = (|초벌 확신 열매 수 − 라벨 열매 수| ÷ 둘 중 큰 수 + 초벌 헷갈림 unc) 의 평균, 있는 값만(paint.js uncScore). 확정한 사진은 뒤에 이름 순.
«초벌 열매 수» 는 draft PNG 를 사진마다 다시 세는 대신, 09-24 에 이미 만든 app/static/paint/unc/<과일>.json 의 sure(점수 0.5 이상 열매 수)를 썼다 — 새 서버 코드·재시작 없이 정적 파일 하나만 받으면 된다. draft PNG(conf 0.25)의 수와는 조금 다를 수 있다. 라벨 열매 수는 /api/list 의 n_inst(번호 개수 캐시).
포도는 번호 개수 캐시가 비어 있어(n_inst 0/2406) 헷갈림 unc 만으로 줄 선다. 0 대 1·2 처럼 수가 아주 작은 사진은 차이 비율이 1.0 이라 위로 오기 쉽다(모델이 아무것도 못 본 사진이라 볼 가치는 있다고 보고 그대로 둠). 저장해도 그 자리에서 순서를 바꾸지 않는다(«다음 사진» 이 이어지게). e2e 128/128×2(C15 4개) · run_all --no-merged 830/0. 정적 파일만이라 재시작 없음.

2026-09-25 21:20 [w1] C16 끝. 저장이 끝나면 그림판이 번호마다 넓이·둥근 정도·맞닿음·떨어진 조각을 재서(paint.js suspects, 사진 한 장 40~50ms) 튀는 열매에 빨간 점선 동그라미를 그리고 알림 줄에 «한 번 볼 열매 N개: 16번 둥근 정도 0.20 …» 을 쓴다. 저장은 막지 않는다(표시만). 근거 칸의 instances.py 대신 그림판에서 계산했다 — L 이 이미 브라우저에 있고, 서버 코드·재시작이 필요 없기 때문.
기준값은 짐작 대신 원본·초벌 번호 마스크 3,322개(사과 gt·블루베리·복숭아 초벌 각 40장, tests/sim/c16_suspect_calib.py → tests/_out/c16/calib.tsv)로 쟀다. 넓이 0.12배 미만·6배 초과(사진 안 가운데값 대비, 열매 5개 이상일 때만) · 둥근 정도 0.25 미만(원=1) · 테두리 60% 넘게 다른 번호와 맞닿음(«겹침» 을 한 장짜리 번호 마스크에서는 이렇게 봄) · 떨어진 조각 2개 이상 → 열매의 2.1~3.1% 가 걸린다. 블루베리는 사진 절반에 한 개쯤 걸려 조금 잦을 수 있다. 포도는 번호 마스크가 없어 측정에서 빠졌다.
동그라미는 사진을 바꾸면 사라지고, 그 번호를 지우면 안 그린다. 고친 뒤 다시 저장하면 다시 잰다. e2e 132/132×2(C16 4개: 저장 목록=다시 잰 목록·알림 · 400ms 이하 · 만든 장면에서 넓이/둥근/조각/둘러싸임 잡고 보통 원은 안 잡음 · 빨간 점선 그림) · run_all --no-merged 830/0. 정적 파일만이라 재시작 없음.

2026-09-25 21:35 [w1] C17 끝. ✨ 클릭·네모·«같은 자리 다시 누르기» 로 칠한 모양이 길쭉하면(긴 축÷짧은 축 3.5배 초과, 아니면 둥근 정도 0.2 미만) 또는 너무 크면(사진의 10% 초과, 아니면 다른 열매 가운데값의 6배 초과·열매 5개 이상일 때) 알림 줄에 빨간 글씨로 «⚠ 잎·가지·배경일 수 있습니다 — 길쭉함(24.0배)» 를 붙인다(paint.js SAMW·samShapeWarn, applyCand 에서 후보마다 한 번 계산해 cd.warn 에 둠). 칠하기는 막지 않는다 — 확신 낮은 항목이라 표시만.
기준값은 원본·초벌 번호 마스크 3,321개(tests/sim/c17_sam_shape_calib.py → tests/_out/c17/calib.tsv)에서 길쭉함 3.5 초과 0.36%·둥근 정도 0.2 미만 0.3%·사진 10% 초과 0% 로 골랐다. 실제 도우미 /sam 결과로 재 보니(tests/sim/c17_sam_real_check.py → tests/_out/c17/real_check.json) 열매 한가운데 클릭 85번 중 3번(3.5%, 복숭아 2·사과 1)만 경고, 열매 밖(배경) 클릭은 53번 중 28번(53%, 복숭아 15/18·사과 9/17·블루베리 4/18) 경고. 블루베리 잎은 잘 못 잡는다. 포도는 번호 마스크가 없어 빠짐.
e2e 134/134×2(C17 2개: 원·타원 조용/막대·대각선·큰 덩어리 경고 · 가짜 ✨ 답으로 칠+빨간 경고+Ctrl+Z) · run_all --no-merged 830/0. 정적 파일만이라 재시작 없음. 백업 app/static/paint/_backup_260925_C17_paint.js.

2026-09-25 21:46 [w1] C18 끝. 그림판이 사진마다 작업 시간·수정 횟수를 세어 저장·빼기 때 POST /api/worklog 로 보내고, 서버(app/api/instances.py)는 app/logs/worklog.jsonl 에 한 줄 덧붙이기만 한다(data/·status.json 은 안 건드림). 칸: at·fruit·stem·action(save|exclude)·by·active_s(입력 사이 간격의 합, 한 간격은 60초까지 — 자리 비운 시간 제외)·wall_s(연 뒤 흐른 시간)·edits(칠이 실제로 바뀐 동작 수)·undos·redos·sam(✨ 도우미 부른 수, samBusy 한 곳에서 셈)·n_open(열 때 번호 수)·n_save(저장한 번호 수).
저장하면 0 부터 다시 센다(두 번째 저장은 더 한 일만) — 논문 비교는 사진별 합으로 보면 된다. ✨ «같은 자리 다시 누르기» 가 안에서 부르는 undo 는 undo(true) 로 바꿔 사람 되돌리기로 안 센다. 기록 실패는 알리지 않는다(저장은 이미 끝남). 모래상자는 app/ 을 복사해 돌아 시험 줄이 실서버 로그에 섞이지 않는다.
시험: e2e 137/137×2(C18 3개 + 디스크 대조 2개: 모래상자 worklog.jsonl 에 저장·빼기 줄) · run_all --no-merged 830/0(u6 승인 주소에 /api/worklog). restart.flag → 21:46:00 재시작 확인. 백업 _backup_260925_C18_{instances.py,paint.js,paint_e2e.mjs,paint_e2e_check.py}.

2026-09-25 21:53 [w1] C19 끝. 서버 쪽은 C07 에서 이미 다 들어간 것을 코드로 확인했다 — inst_etag 가 고친 번호·팀 초벌·원본 번호(=원본 이진)·고친 이진 네 파일의 경로+inode+mtime_ns+크기를 보고, 캐시 키는 (과일, 이름, layer), 저장 경로(api_save_instances 532줄)의 load_inst 는 캐시를 안 거친다. 화면 쪽도 C06 대로 저장·빼기·대표만 남기기 때 PF 항목 삭제, 원본 다시 불러오기(fromOriginal)는 PF 를 안 쓰고, 미리 받는 것은 늘 layer=auto 하나라 키가 섞이지 않는다.
빈틈 하나를 막았다. 넘길 때 비교하던 «상태 지문»(itemSig)은 at 이 초 단위라, 남이 같은 초 안에 /api/status 다음 /api/save_instances 를 하면(그림판 저장 순서) 미리 받은 옛 번호를 그대로 쓸 수 있었다. 그래서 /api/item 에 file_sig(= /instances ETag 와 같은 값, stat 만) 칸을 더하고(app/api/photos.py) itemSig 에 넣었다(paint.js). 서버가 옛날이어도 양쪽 다 undefined 라 무해.
시험: t5_inst_cache C19 2개(저장 뒤 file_sig = 새 ETag · 되돌리면 처음 지문) · e2e 138/138×2(C19 1개: 미리 받은 item 의 file_sig 만 바꾸면 새로 받음) · run_all --no-merged 832/0. restart.flag → 21:53:06 재시작 확인. 백업 _backup_260925_C19_{photos.py,paint.js,paint_e2e.mjs}.
- 2026-09-25 21:56 [cx] X01 결과 `auto/X01_codex.md`

2026-09-25 22:00 [w1] E01 끝. 새 시험 tests/browser/e01_sam_neg_keyboard.mjs + 돌리는 스크립트 tests/browser/run_e01.sh(모래상자 5441, 파이어폭스·크로미움, 결과 tests/_out/e01/). ✨ 답은 page.route 로 정해진 모양을 돌려준다 — 도우미(GPU7)를 안 쓰고 결과가 늘 같게 하려고.
A(마우스) Shift+클릭: 붙은 두 알을 ✨ 로 한 번호로 칠한 뒤 옆 알을 Shift+클릭하면 가장 작은 후보만 떨어짐 · Ctrl+Z/Y · 다른 번호 자리/✨ 전/통째로 사라질 때/후보 없음 안내 10개 모두 통과. B(마우스 안 씀) D→Tab 38번에 색 칸→Enter→G→Enter→Ctrl+Z/Y→Ctrl+Enter 저장(디스크 fixed·새 번호 둘)→Shift+Tab «파일»·↓·Enter «빼기»(exclude) · worklog 에 save·exclude 줄.
못 넘은 1개(두 브라우저 같음)는 앱 버그라 코드 C20 으로 등록 — 색 칸에 초점이 있으면 G 뒤 Enter 가 «모두 받기» 대신 색 칸을 다시 눌러 도구가 붓으로 바뀐다(keydown 의 role=button 분기가 S.sim Enter 보다 앞). Shift+Tab 으로 «모두 받기» 단추에 가면 받을 수 있다. 헤드리스 파이어폭스는 문서 끝에서 Tab 이 처음으로 안 돌아와 시험은 Shift+Tab 으로 찾는다. 앱 코드는 안 고쳤다(재시작 없음).

2026-09-25 22:06 [w1] C20 끝. 그림판 keydown 에서 role=button 분기 바로 앞에 한 줄을 넣었다 — ✨ 후보(S.sim)가 떠 있고 초점이 팔레트 색 칸(.sw)이나 지금 색(#curbox)이면 Enter = simAccept. 작업자(#who) Enter 는 이름 바꾸기 그대로, 스페이스는 색 칸 누르기 그대로 둬서 바뀐 건 «후보 있을 때 색 칸 Enter» 하나뿐이다.
단추로 초점을 옮기는 방식은 고르지 않았다 — 도구 막대 초점이 튀면 Tab 순서가 흔들리고, 이미 단추 제목이 «모두 받기 (Enter)» 라 Enter 한 번으로 받는 쪽이 맞다.
시험: run_e01 FF·CR 27/27(우회 길 없이 바로 받음) · e2e 138/138×2 · run_all --no-merged 832/0. 정적 파일만이라 재시작 없음. 백업 app/static/paint/_backup_260925_C20_paint.js.

2026-09-25 22:40 [w1] E02 끝. 지난 라운드가 22:07 에 띄운 tests/_out/e02/run_e02.sh 를 새로 돌리지 않고 끝까지 기다렸다. 그림판 e2e 138/138×2 · E01 27/27×2 · run_all 은 브라우저 앞 묶음이 전부 통과했고 --browser 는 25개 중 4개가 실패했다.
실패 4개는 사이클 전 코드(snapshot_before)로 모래상자를 만들어 다시 돌려서 가렸다. b24 ⑩·t2_ui 다-3·마-1 은 전 코드에서도 같은 값으로 실패하고, b1 은 실데이터에 사람 번호본이 생겨(09-23·09-24) 기대값이 낡았고, b15 는 다시 돌리니 통과했다. 앱 버그가 아니라서 코드 항목은 올리지 않았다. Z01 은 이 4개 때문에 --browser rc=1 인 것을 알고 판단해야 한다. 기록 auto/E02_회귀_260925.md.
- 2026-09-26 11:47 F02 {after:코드,코덱스} ↔ X02 {after:검수} 서로 기다려 22:40 부터 13시간 멈춤. X01 은 이미 끝났으므로 F02 조건을 {after:코드} 로 바꿈.
- 2026-09-26 12:00 사용자 «카톡초안은 안써도될것같아» → S02 에서 카톡 초안 뺌.

2026-09-26 12:01 [f1] F02 끝. X01(auto/X01_codex.md) 5건을 코드와 대조했다. 진짜 4건을 코드 C21~C24 로 올렸다 — ① 정렬·«안 한 것만»·이름 찾기 뒤 renderList 가 dirty 를 안 보고 openPhoto(C15 의 정렬은 새 것, 나머지 둘은 사이클 전부터 같은 구멍) ② 저장 중 undo/redo·simAccept·newNumber·loadDraft·splitBy 가 S.busy 를 안 봐 이진·번호·상자가 다른 시점의 L 을 담고 setDirty(false) 가 그새 편집을 가림(Codex 는 «추정» 이라 했지만 코드로 확인) ③ 도우미 _similar 의 음수 슬라이스 — numpy 로 재현(본보기 60×800 두 조각 → broadcast 오류 → 400) ④ /api/worklog "NaN" → int() 500. 3번 «번호 65535 도달» 은 한 사진에서 새 번호를 6만 개 만들어야 해 뺐다.
직접 검토(snapshot_before 와 diff: ai.py·instances.py·photos.py·main.js·paint 3파일·sam_server.py)에서는 더 올릴 것을 못 찾았다 — /instances 메모리 캐시는 Lock 과 48장 상한, PF 는 앞뒤 1장씩만 두고 나머지는 지움, main.js 는 #exp 해시 처리 4줄뿐. 전체 코드 항목 25개(상한 30).
- 2026-09-26 12:03 사용자: Codex 한도 없음, 09-26 18:00 KST 에 다시 참 → auto/limit_codex = 18:00 (cx 는 그때까지 대기).

2026-09-26 12:06 [w1] C21 끝. renderList 필터에 «지금 사진이 안 저장(dirty)이면 조건과 상관없이 목록에 남김» 한 줄을 넣었다. 그러면 want 가 늘 지금 사진이라 openPhoto 가 안 불려 칠이 안 사라진다. «안 한 것만»·정렬·이름 찾기 세 길을 한 곳에서 막는다. 1384줄 주석도 맞게 고쳤다.
leaveOk 를 거치는 방식은 고르지 않았다 — 정렬만 바꿔도 저장 창이 떠 귀찮고, 취소하면 체크·정렬·찾기 칸을 저마다 되돌려야 한다. 저장한 뒤 다음에 목록을 다시 그리면 그 사진은 조건대로 빠진다.
시험: paint_e2e 에 C21 두 개 추가(디스크 안 쓰고 목록 항목의 확정 표시만 잠깐 바꿈) → FF·CR 140/140, run_all --no-merged 832/0. 옛 코드로 새 시험이 실패하는지 보는 모래상자 실행은 권한에서 막혀 못 돌렸다(코드로 보면 shown[0] 이 열려 실패함). 백업 app/static/paint/_backup_260925_C21_paint.js · tests/browser/_backup_260925_C21_paint_e2e.mjs.
주의: 처음 고칠 때 괄호를 하나 잘못 넣어 실서버 paint.js 가 12:01~12:02:56 사이 약 1분 문법 오류였다(그 사이 연 그림판은 새로고침 필요). node --check 로 확인 뒤 바로 고쳤다. 정적 파일이라 재시작 없음.

2026-09-26 12:12 [w1] C22 끝. save() 가 첫 await 전에 이진·번호·상자를 한 번에 만든다(세 파일이 같은 순간의 칠). 칠이 바뀔 때마다 S.ver 를 올리고(changed), 저장 끝에 ver 가 달라졌으면 setDirty(true) 로 남겨 «저장 안 됨» 을 가리지 않는다 — ✨ 답이 저장 중에 오는 길처럼 진입점을 다 막을 수 없는 경우도 이것으로 덮인다.
진입점 막기는 waitBusy() 하나로: undo(사람이 누른 것만 — ✨ 후보 바꾸기의 자동 undo 는 두어야 후보가 겹치지 않음)·redo·simAccept·newNumber·loadDraft·splitBy·전부 지우기. leaveOk 와 «저장하고 다음» 은 저장 사이 또 바뀌었으면 넘기지 않는다(다시 물음).
시험: paint_e2e 에 C22 한 개(가짜 postJSON 으로 답을 손으로 풀어 줌, 디스크 안 씀) → FF·CR 141/141·디스크 대조 통과, run_all --no-merged 832/0. 정적 파일만이라 재시작 없음(모래상자에서 고쳐 node --check 뒤 복사해 문법 오류 공백 없음). 백업 app/static/paint/_backup_260925_C22_paint.js · tests/browser/_backup_260925_C22_paint_e2e.mjs.

2026-09-26 12:17 [w1] C23 끝. 도우미 sam_server.py 에 _ex_tile() 을 새로 두고, 본보기 둘레 T×T 칸에 본보기를 옮길 때 «본보기 네모와 칸이 겹치는 좌표만» 복사한다(음수 슬라이스 없음). 본보기 네모가 칸보다 크면(같은 번호가 떨어진 두 조각) 가장 큰 조각의 가운데에 칸을 둔다 — 두 조각 사이 빈 곳에 칸을 두면 특징이 0 이 되어 후보가 하나도 안 나오기 때문. 네모가 칸 안에 들어가는 평소 경우는 옛 계산과 똑같다(fx·fy 같음, 후보 수 32=32 확인).
paint.js exemplarOf 는 그대로 둠(번호 전체를 보내는 게 맞고, 서버가 잘라 쓰면 된다).
시험: 옛 도우미(5112, 재시작 전)에 같은 요청 → 400 «could not broadcast (639,60) into (161,60)» 재현, 새 코드(모래상자 5441·GPU5) → 200. tests/sim/c23_ex_tile_check.py(GPU 없이 4경우) 통과 · tests/api/t6_sam_similar.py 에 C23 한 개 추가 → 실서버 도우미 11/11 · run_all --no-merged 832/0. 도우미는 run_helper.sh restart 로 12:16:08 다시 켬(GPU7). 앱 파이썬은 안 고쳐서 restart.flag 는 안 씀. 백업 ai_helper/_backup_260925_C23_sam_server.py · tests/api/_backup_260925_C23_t6_sam_similar.py.

2026-09-26 12:21 [w1] C24 끝. app/api/instances.py api_worklog 에서 float 변환 뒤 math.isfinite 로 검사해 NaN·±Infinity 는 0 으로 바꾼 다음 0~1e6 으로 자른다. NaN 은 min/max 를 그대로 빠져나가 int() 에서 500, _s 칸은 worklog.jsonl 에 비표준 NaN 으로 적히던 문제. Infinity 는 지시대로 1e6 이 아니라 0 으로 둠(유한하지 않은 값은 측정값이 아니므로).
시험: tests/unit/u11_worklog_finite.py 새로 둠(모래상자 복사본·test_client, 'NaN'/'Infinity' 등 5종 + 평소 값 1경우) → 옛 코드 500 재현, 새 코드 6/6. run_all --no-merged 838/0. restart.flag 로 실서버 12:21:01 재시작 확인. 백업 app/api/_backup_260925_C24_instances.py.

2026-09-26 12:30 [r1] S02 끝. 문서/260923_그림판툴_사용법.md 를 S01 점검표(A1~A5·C)와 지금 index.html·paint.js 로 다시 써서 갱신했다(파일명 날짜는 작성일이라 그대로, 최종 수정 2026-09-26). ✨ 후보 순서·돋보기·오른쪽 클릭 지우기·올가미 표기를 바로잡고, 단축키 표·상단 목록 도구(표본 100장·헷갈리는 순·닮은 사진)·상태줄·G·의심 열매/✨ 경고·저장 중 편집 보호를 넣었다.
초벌 정확도 숫자(마스크 mAP50 0.84, 표본 60장 비율)는 근거 파일을 못 찾은 채라(S01 A5) 지우고 «뺐다» 고 적었다. 카톡 초안은 사용자 지시대로 안 씀. 백업 문서/_backup_260926_S02_260923_그림판툴_사용법.md. 실제 브라우저로 눌러 보지는 않고 소스 대조만 했다.
- 2026-09-26 18:00 [cx] X02 결과 `auto/X02_codex.md`

2026-09-26 18:34 [w1] C25 끝. paint.js 571~573줄 — wlTick 을 pointerup 에도 걸고, 버튼을 누른 채의 pointermove 는 100ms 에 한 번만 wlTick 하게 했다. 한 간격 60초 상한은 그대로라 자리 비움은 여전히 안 센다. 화면 기능은 안 바뀜(정적 파일이라 재시작 없음). 백업 app/static/paint/_backup_260925_C25_paint.js.
시험: tests/browser/paint_e2e.mjs 에 C25 단정 1개(가짜 시계로 90초 붓질+떼기 = 100초, 안 누른 움직임 0, 100ms 안 움직임 묶음). 앞 라운드가 쓴 묶음 검사는 기준 tick 이 50초 전이라 잘못 실패했음 → 한 번 tick 한 뒤 50ms 움직임으로 고침. e2e 5441 파폭·크로미움 286 PASS/0 FAIL, run_all --no-merged 838/0. 옛 코드면 drag=60000 으로 실패.

2026-09-26 19:04 [f1] Z01 — 회귀·공개 사본까지. 18:02 라운드의 회귀는 w1 이 C25 를 고치는 중(18:12~18:34)에 돌아 paint.js 가 바뀌는 사이의 코드를 쟀고, 그때 b14·b19·b21 이 «목록 카드 못 찾음» 시간초과로 깨지고 e2e 의 C25 단정 2개가 실패했다. 그래서 C25 가 끝난 최종 코드로 `tests/_out/z01_2/run_z01.sh`(같은 순서, 포트 5443, flock) 를 다시 돌렸다 — run_all --browser 2,344/7(1,300초), 그림판 e2e 파폭·크로미움 286/0 + 디스크 대조, E01 27/27×2. 실패 7 은 E02 가 사이클 전 코드로도 같은 값임을 확인한 그 7개(b1 4 = 실데이터에 사람 번호본이 생겨 기대값이 낡음 · b24 ⑩ · t2_ui 다-3·마-1)라 통과로 판단했다. b14·b19·b21 은 이번엔 전부 통과 → 지난번 깨짐은 동시 실행 부하.
공개 사본은 18:10 수집본 위에 `prepare_public.py` 를 다시 돌려(C25 뒤 paint.js·e2e·기록 반영) 545 파일. README 덩어리는 이미 붙어 있어 «C01~C24 · 버그 4개» 를 «C01~C25 · 버그 5개(긴 드래그 작업 시간)» 로 손으로 고치고 스크립트 본문도 같게 맞췄다. README·labeltool/README 는 더한 줄만 있고 지운 줄 0(지난 사이클의 덩어리 유실 사고 재점검). 이 기록을 쓴 뒤 한 번 더 수집 → audit_public.py → 커밋 → push 순서(기록이 낡은 채 올라가지 않게). push 해시는 auto/STATUS.md 와 작업기록.md 「1. 현재 상태」에 남긴다.
