작성: 2026-09-25

# 260925 «전부 업그레이드» 자동 사이클 — 계획

## 무엇을, 왜
사용자(곽동신) 지시 2026-09-25 10:40 경.
«내가 만든 웹사이트(라벨링 툴 그림판, 실서버 5111)를 더 좋게 쓸 수 있게, 녹음(txt)·논문·인터넷 리서치·팀원들 작업환경까지
모든 수단으로 확인해서 업그레이드. 서버 GPU 는 3장 정도만. 전부 자동 진행, 컴퓨터가 꺼져도 계속.
한도가 차면 기다렸다가 이어서.»

## 엔진(레인)과 몫 — 사용자가 정한 한도 안에서
| 레인 | 엔진 | 맡는 태그(앞에 있는 것 먼저) |
|---|---|---|
| w1 | 연구실 Claude Opus 5.5 | 코드 → 시험 (앱 코드를 고치는 유일한 레인) |
| r1 | 연구실 Claude Sonnet 5 | 팀원 → 녹취 → 데이터 → 조사 → 문서 (읽고 정리하는 일이라 Sonnet 으로 한도 절약) |
| f1 | 개인 Claude Fable 5.1 | 설계 → 검수 → 마무리 |
| cx | Codex gpt-6-astra (low) | 코덱스 (2026-09-26 초기화 뒤) |

(2026-09-25 10:59(추정) 수정: 사용자 «연구실 Claude 한도 생각해서 최적으로» → 8레인 → 4레인. 연구실 계정은 동시에 2개만(Opus 1 + Sonnet 1).
 Gemini·개인 Opus 레인은 뺐다. 제미나이 태그 항목은 r1 이 대신한다.)
- 연구실 계정은 같이 쓰는 것이라 Fable 은 안 돌린다.
- 한도에 걸리면 메시지의 «resets 5:20am» 시각을 읽어 그때까지 잔다(못 읽으면 30분). 같은 계정의 다른 레인도 같이 잔다.
- 마감 2026-09-27 12:00 KST. `auto/STOP` 파일을 만들면 모든 레인이 멈춘다.

## 규칙(모든 레인 공통)
- 남의 프로세스 kill 금지(5100·5105 교수님, 5101 Jupyter). 원본 데이터·팀원 폴더는 읽기만.
- GPU 는 이미 쓰는 7번(✨ 도우미) 포함 3장까지 → 새로 쓸 수 있는 것은 5·6번뿐. 쓰기 전 nvidia-smi.
- 앱 코드는 w1 만 고친다. 다른 레인은 새 `코드` 항목을 체크리스트에 **추가**만 한다(근거 파일 경로 필수, 전체 코드 항목 상한 30).
- 파이썬(서버) 수정은 모래상자 시험 통과 뒤 `touch app/logs/restart.flag` 로 감시자에게 재시작을 맡긴다(1분 안).
- 데이터셋 정본 전환·카톡 발송 등 사람이 정할 일은 하지 않고 문서로만 남긴다.
- GitHub push 는 마지막 Z01 에서 한 번만(기존 공개 저장소 절차, force 금지).

## 진행 보기
- 사람용 한 줄 기록: `auto/STATUS.md`
- 레인별 자세한 기록: `auto/<레인>.log`, 라운드 출력 `auto/rounds/`
- 멈추기: `touch auto/STOP` / 다시 켜기: `rm auto/STOP; bash auto/start_all.sh`
