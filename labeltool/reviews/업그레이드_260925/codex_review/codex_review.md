결론. 저장 중 상태 변경, 사진 전환 경쟁, SAM 후보 교체의 되돌리기 오류는 확정적인 데이터 손상 위험이며, 실제 API 구현이 빠져 있어 서버의 동시 저장·경로 방어는 판정할 수 없습니다.

1. **[치명적] `app/static/paint/paint.js:839–852, 375–377, 474–485` · 저장 도중 마스크·번호·상자가 서로 다른 편집 상태로 만들어집니다.**  
   **상황.** 마스크 전송 후 기다리는 동안 Ctrl+Z, 전부 지우기, 이미 요청한 SAM 응답이 `S.L`을 바꿀 수 있습니다. `S.busy`는 이 경로들을 막지 않습니다. 번호와 상자는 이후의 `S.L`로 만들어지며, 마지막에는 저장 후 변경까지 `dirty=false`로 덮습니다.  
   **수정.** 저장 시작 시 사진 식별자·라벨 배열·편집 버전을 함께 고정하고 모든 산출물을 그 복사본에서 생성합니다. 완료 시 편집 버전이 같을 때만 저장 표시를 해제합니다.

2. **[치명적] `app/static/paint/paint.js:132–142, 171–199, 939–941` · 과일 식별자와 실제 열린 사진이 어긋날 수 있습니다.**  
   **상황.** 사진을 불러오는 동안 과일을 바꾸면 `S.fruit`는 바뀌지만 새 `openPhoto()`는 `busy` 때문에 무시됩니다. 기존 요청은 세대 검증 없이 이전 과일의 사진을 설치합니다. 이후 저장은 새 과일과 이전 사진의 `stem`을 사용합니다. 같은 이름이 있으면 잘못된 사진을 덮어쓸 위험이 있습니다. 목록 페이지 요청도 매번 변경 가능한 `S.fruit`를 참조합니다.  
   **수정.** 목록·사진 요청마다 과일과 요청 세대를 고정하고, 최신 응답만 사진 식별자와 배열을 함께 교체하게 합니다.

3. **[높음] `app/static/paint/paint.js:847–851` · 저장이 네 개의 독립 요청이라 부분 저장됩니다.**  
   **상황.** 마스크와 확정 상태를 저장한 뒤 번호 또는 상자 요청이 실패하면 일부만 새 데이터가 됩니다. 특히 번호·상자 저장 전에 마스크가 확정됩니다. **동시 사용자 간 섞임·덮어쓰기는 추정**이며, 서버가 별도 보호를 하지 않으면 두 사람의 요청이 교차할 수 있습니다. 붙인 코드에는 서버 구현이 없어 확인할 수 없습니다.  
   **수정.** 한 저장 API에서 세 산출물과 상태를 하나의 버전으로 반영하고, 읽었을 때의 버전이 달라졌으면 충돌로 거절합니다. 파일별 잠금만으로는 전체 저장의 일관성을 보장하지 못합니다.

4. **[높음] `app/static/paint/paint.js:144–162, 947–948` · 검색·필터 변경이 미저장 작업을 버립니다.**  
   **상황.** 편집한 사진이 검색 결과에서 빠지거나, 이미 저장한 사진을 다시 편집하다가 ‘안 한 것만’을 켜면 `leaveOk()` 없이 다른 사진을 열거나 화면을 비웁니다. 빈 목록에서는 즉시 `dirty=false`까지 됩니다.  
   **수정.** 현재 사진이 바뀌거나 사라지는 모든 목록 변경도 동일한 이탈 확인을 거치게 합니다.

5. **[높음] `app/static/paint/paint.js:488–495, 158–160, 199` · SAM 후보 교체가 엉뚱한 작업을 되돌립니다.**  
   **상황.** SAM으로 칠한 뒤 붓질하고 같은 자리를 클릭하면 SAM 작업 대신 마지막 붓질을 `undo()`합니다. SAM 작업을 직접 되돌린 뒤 클릭해도 이전 작업을 추가로 취소합니다. `samLast`는 사진 전환 때 초기화되지 않고 `stem`만 비교하므로, 같은 사진을 다시 열거나 다른 과일의 동명 사진을 열면 이전 후보도 적용할 수 있습니다.  
   **수정.** 후보를 해당 사진 세대와 정확한 편집 기록에 연결합니다. 다른 편집·되돌리기·사진 전환 시 후보 교체 상태를 무효화하고, 후보 교체 중에도 중복 실행을 막습니다.

6. **[높음] `app/static/paint/paint.js:361–364, 484–486` · 되돌리기 기록 개수로 SAM 적용 성공을 판단합니다.**  
   **상황.** 기록이 40개이면 새 기록을 넣고 오래된 것을 빼므로 길이가 늘지 않습니다. 메모리 제한으로 기록을 버릴 때도 같습니다. 실제로 칠했는데 `applied=false`가 되어 다음 후보 선택 시 기존 후보를 제거하지 않습니다.  
   **수정.** `pushUndo()`가 실제 변경 여부나 생성한 기록 식별자를 반환하게 합니다.

7. **[높음] `app/static/paint/paint.js:765–788, 888–897, 962–965` · 진행 중인 붓질이 저장·사진 전환·되돌리기와 충돌합니다.**  
   **상황.** 붓질은 즉시 배열을 바꾸지만 `dirty`와 undo 기록은 마우스를 놓아야 생깁니다. 누른 채 D를 누르면 미저장 확인 없이 이동할 수 있고, 남은 `drag`가 새 사진에 이어집니다. Ctrl+Z 후 마우스를 놓으면 서로 다른 편집 상태가 하나의 기록으로 섞입니다.  
   **수정.** 저장·이동·undo 전에 진행 중인 스트로크를 확정하거나 취소합니다. 드래그에도 사진 세대를 기록하고 전환 시 초기화합니다.

8. **[높음·추정] `app/static/paint/paint.js:849, 854` · 열매를 전부 지워 저장해도 기존 번호 파일이 남을 수 있습니다.**  
   **상황.** 번호가 0개이면 `/api/save_instances`를 호출하지 않습니다. 기존 번호 파일과 번호 확정을 서버의 마스크 저장이 별도로 제거하지 않는다면, 마스크·상자만 비워지고 번호 데이터는 남습니다. 화면에서 `confirmed_instances=null`로 바꾸는 것은 서버 정리가 아닙니다.  
   **수정.** 빈 번호 배열도 명시적으로 저장하고 관련 확정 상태를 함께 갱신합니다. 실제 삭제 여부는 누락된 API 구현 확인이 필요합니다.

9. **[높음] `app/static/paint/paint.js:56–59, 184–198` · 서버 오류를 ‘라벨 없음’으로 처리합니다.**  
   **상황.** 마스크 요청의 500·403도 `null`이 되어 빈 마스크로 열립니다. 번호 요청만 실패하면 연결 요소로 번호를 새로 만들어, 떨어진 조각이었던 같은 열매를 다른 번호로 나눌 수 있습니다. 그대로 저장하면 기존 라벨을 덮습니다.  
   **수정.** API 계약상 ‘파일 없음’인 응답만 대체 처리하고, 인증·서버 오류에서는 열기를 중단합니다.

10. **[중간] `app/static/paint/paint.js:189–195, 579–583, 611–614` · 라벨 PNG의 가로·세로를 검증하지 않습니다.**  
    **상황.** 번호 PNG가 사진과 다른 크기여도 그대로 설치됩니다. 전체 픽셀 수가 같고 가로·세로만 달라도 라벨이 다른 위치로 해석됩니다. 초벌·번호 나누기도 배열 길이만 검사합니다.  
    **수정.** 모든 PNG의 `w`, `h`가 사진 크기와 각각 같은지 확인한 뒤 사용합니다.

11. **[중간] `app/static/paint/paint.js:828–837` · 1∼3화소짜리 번호는 마스크·번호에 남지만 상자가 없습니다.**  
    **상황.** 작은 붓으로 찍거나 지우개로 작은 조각만 남긴 뒤 저장하면 ‘번호마다 상자 하나’ 규칙이 깨집니다.  
    **수정.** 모든 유효 번호의 상자를 만들거나, 작은 객체를 삭제하기로 했다면 동일한 저장 배열에서 마스크·번호도 함께 제거합니다.

12. **[중간] `app/static/paint/paint.js:509, 561, 593, 664, 751` · 새 번호가 uint16의 유효 범위를 넘습니다.**  
    **상황.** 최대 번호 65534에서 새 번호를 만들면 65535는 회색 예약값이 됩니다. 65536은 배열에 기록할 때 0으로 바뀌고, 이후에는 기존 번호와 충돌합니다. 번호가 큰 파일을 불러오면 적은 작업으로도 발생합니다.  
    **수정.** 번호 할당을 한 곳에서 처리하고 1∼65534의 빈 번호만 반환하게 합니다.

보안 관련 값·설정 세부는 공개본에서 생략했습니다.
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
보안 관련 값·설정 세부는 공개본에서 생략했습니다.

14. **[보안·조건부] `ai_helper/sam_server.py:29–35, 91–108` · 임의 이미지 경로를 받고 좌표 범위를 검증하지 않습니다.**  
    **상황.** 헬퍼에 접근 가능한 호출자는 서버가 읽을 수 있는 임의 이미지 경로를 지정할 수 있습니다. 잘못된 점은 78행에서 음수 인덱스로 해석되거나 범위를 벗어나 실패합니다. 단, 헬퍼는 로컬 바인딩이며, 외부 사용자가 `/api/sam`을 통해 이를 악용할 수 있는지는 **추정**입니다. 프록시 구현이 없습니다.  
    **수정.** 허용 데이터셋 내부의 실제 경로인지 확인하고, 이미지 크기에 맞춰 점·상자 범위와 형태를 검증합니다.

15. **[보안·추정] `app/static/paint/paint.js:122, 150` · 과일명·파일명을 이스케이프 없이 HTML에 삽입합니다.**  
    **상황.** 공격자가 목록의 이름을 제어할 수 있다면 따옴표나 태그로 속성·마크업을 깨뜨릴 수 있습니다. 실제 XSS 가능 여부는 서버의 이름 검증과 데이터 유입 경로에 달렸습니다.  
    **수정.** `new Option(표시문구, 값)` 또는 `textContent`와 `value`로 옵션을 생성합니다.

라벨링 속도 개선은 다음 세 가지가 우선입니다.

- **`app/static/paint/paint.js:398–400, 395–396` · 붓 이동마다 여러 번 화면에 복사합니다.** 긴 이동에서 각 stamp가 `putImageData()`를 호출합니다. 픽셀 변경은 모아서 처리하고 합쳐진 영역을 프레임당 한 번만 갱신합니다.
- **`app/static/paint/paint.js:351, 765, 297–309, 377` · 작은 붓질에도 전체 사진 복사와 전체 통계를 수행합니다.** 고해상도에서 반복 비용이 큽니다. 처음 수정하는 타일의 원본만 기록하고, 바뀐 번호의 화소 수·좌표 합만 갱신합니다.
- **`ai_helper/sam_server.py:60, 108–113` · 매 SAM 요청에서 이미지 임베딩을 다시 설정합니다.** 같은 영역의 연속 클릭은 동일 crop의 임베딩을 재사용할 여지가 있습니다. 파일 버전과 crop 좌표를 키로 제한된 캐시를 두되, 실제 재계산 여부는 사용 중인 `SAM2Predictor` 구현 확인이 필요합니다.

`app/boxes.py`, `instances.py`, `maskio.py`, `dupes.py`는 재내보내기뿐입니다. 실제 `app/api/*.py`, `core/paths.py`, `core/status_store.py`, `core/auth.py`가 없어 경로 조작 방어, 서버 잠금, 충돌 검출, 저장 원자성은 검증하지 못했습니다. 도구나 실행 테스트 없이 붙인 코드만 검토했습니다.
