작성: 2026-09-18

# 사이클 4 (카운팅 흐름을 같은 큐에 + 마스킹 편의 + 전 조작 회귀) — 3차 최종 판정 (Fable 5.1)

판정 시각(실측): 2026-09-18 23:09
근거: 1차 `stage1_work.md` · 2차(보통) `stage2_review.md` · 소수정 `stage2c_fix.md` · 내가 직접 본 것(§3)

## 1. 판정 — **합격, 실서버에 켬(23:06:13, PID 2593295)**

| 항목 | 판정 | 근거 |
|---|---|---|
| 1차: `confirmed_boxes`·`confirmed_instances`(마스크 확정과 독립), `kind`, 종류별 집계·큐, 내보내기 종류별 확정, 상자/번호 Enter 확정, 옛 서버 가드, 마스킹 편의 3, `regress_all` 99/0, 기대값 갱신 44칸 | 수용 | 2차가 두 번 재현(고치기 전/후), 새로 깨진 것 0. 사진 밖 클릭 400 결함은 1차가 스스로 잡음 |
| 2차 즉시 수정 3 — **목록 카드가 사람 확정을 안 보여 줌**(현재 작업의 확정으로 테두리·딱지) · 상자 0개 문구 · «나갈 상자 N장» | 수용 | 실사용에서 «내가 뭘 제외했지» 를 못 보던 것. 글자 예산 마스크 +0 · 상자 −7 · 번호 −8 |
| 1차 갈림길 1(상자 Enter = 저장→확정 두 걸음) · 3(`ai_boxes_status` saved/seed) | 동의 | 초벌은 파일이 아니라 화면에만 있어 저장 없이는 내보낼 것이 없음 |
| 갈림길 2(N8 은 `/api/status` 만) | 결론 유지, 주석 근거 교체 | «AI 3회 검수» 는 HTTP 가 아니라 파일 직접 쓰기였음(실측) |
| 갈림길 4(되돌리기가 확정을 안 지움) | **뒤집음 — 되돌리기 성공 시 그 종류의 확정을 지움** | manifest `fixed` ↔ 파일 원본 불일치(소수정이 manifest 한 줄로 실증). 사람은 되돌린 뒤 Enter 한 번. 옛 시험 3자리(«되돌려도 확정 남음»)는 이 결정으로 갱신 |
| 넘긴 것 «상자 파일 없어도 나갈 상자로 셈» | 고침 | `box_set` 교집합 → 화면 숫자 = 실제 txt 수 |
| 시험 위생(runall t3_chars · mk_rep 옛 로그 · `t2_queue` 잔재) | 고침 | 같은 모래상자 3판 43/0 |
| 목록에 지금 작업 표시 · 확정 뒤 카드 즉시 갱신 · 빈 날짜 폴더 | 사이클 5 | 총괄에서 최소 구현 |
| 붓 크기 «창을 완전히 닫았다 열기» | 사람 확인 | 시험 도구가 새 프로필을 만들어 못 잼(체크리스트에) |

## 2. 켜기 (내가 직접, 실측)
- 23:06 외부 IP 마지막 쓰기 09:07(변동 없음) → `py_compile` 6 · `node --check` 2 통과 → `bash app/run.sh restart` **23:06:10~13** → PID 1908658 → **2593295**, 원본 폴더 유지
- 켠 뒤 GET(쓰기 0건): 비로그인 401 · `/api/health` 200 · `/api/fruits` AI 집계 불변(사과 572/51 · 블루베리 81/4 · 포도 3/72 · 복숭아 0/30), `n_confirmed/n_confirmed_boxes/n_confirmed_instances` 전부 0 · `/api/list?mode=boxes&sort=queue` 항목에 `ai_boxes_status·confirmed_boxes·confirmed_instances·n_inst` · `/api/export_list` 종류별 집계 칸(`n_confirmed_boxes_out` 0) · `/api/export_plan` 200. `exports/` 여전히 없음
- 진짜 파이어폭스 1366×768(사과 `…image201`): **«할 일» 자동 확대가 실서버에서 동작**(후보 1 점선 상자로 확대) · 기본 도구 «자동채움» 선택됨 · 하단 «⚠ 라벨 빠진 열매 후보 1곳 미확정 · Enter=맞다». 스크린샷 `~/ff_shots/c4/stage3/apple_box_mode_live.png`
- 실측 메모: 내 스모크가 상자 모드로 못 들어감(단추 선택자가 옆 칸의 다른 «상자» 단추를 눌러 «저장된 상자가 없습니다» 안내만 뜸) — 상자 모드 하단 줄은 2차가 **같은 정적 파일**로 모래상자에서 실측(«AI 초벌 상자 96개 — 맞으면 Enter» → 확정 → txt 2=확정 2). 사람 확인 항목: 실서버에서 상자 모드 하단 줄 눈으로 한 번

## 3. 내가 직접 본 것
- 백업 `_backup_260918_c4_*`(1차 9파일) · `_backup_260918_c4b_{app,ui}.js` · `_backup_260918_c4c_*`(13파일) 존재
- 소수정이 보고한 2차 표 오기(`s2_rest d` 5/0 → 로그는 5/1, 2차 자신이 «결함 아님» 판정한 동작) — 결함 아님, 표 오기로 기록

## 4. 사이클 5 에 내리는 결정
1. 넘어온 3건 최소 구현(현재 작업 한 단어 ≤4자 · 카드 즉시 갱신 · 빈 날짜 폴더는 «만들지 않기» 우선).
2. 처음 온 사람 10장 시나리오는 **안내창 읽고 닫기부터** 시작, 문서 없이 화면 문구만으로. 막힌 곳은 문구로 고치되 글자 예산 ≤4자.
3. 문서 정합 37곳 + 체크리스트 재편(15항목 이내, 크롬 10분) + README 스키마 표 + 재학습 단계 계획서(다음 주 착수 기준).
4. 2차는 **종합 리뷰**(5회 전체를 통틀어 남은 위험·모순·낡은 숫자), 3차가 팀용 최종 판정서와 켜기.

## 5. 사람이 할 것
Ctrl+F5 · 상자 모드 하단 줄과 «붓 크기가 창을 닫았다 열어도 남는지» 눈으로 · 모래상자 삭제 목록에 `cycle_4/stage1/sandbox`·`stage2/sandbox`·`stage2c/sandbox` 추가
