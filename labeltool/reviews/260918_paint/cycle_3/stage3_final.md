작성: 2026-09-18

# 사이클 3 (데이터 정리 탭 — 백엔드+프런트, 서버 저장) — 3차 최종 판정 (Fable 5.1)

판정 시각(실측 `date`): 2026-09-18 18:46
근거: 1차 `stage1_work.md` · 2차(공격적) `stage2_review.md` · 3차 전 소수정 `stage2c_fix.md` · 내가 직접 본 것(§3)

## 1. 판정 — **합격, 실서버에 켬(18:44:54)**

| 항목 | 판정 | 근거 |
|---|---|---|
| 1차: `/api/export_start·export_status·export_list·export_plan`, 날짜 폴더 `exist_ok=False`, 같은 과일 409, job.json 복원, manifest 4칸, flag 기본 제외, `boxes.py` 공용화, «데이터 정리» 탭+가드, help·README | 수용 | 2차가 시험 코드 무수정으로 105항목·회귀 22묶음 숫자까지 재현 |
| 2차 즉시 수정 F1(사람 확정이 «AI 제안 포함» 에서도 최종) | **수용 — 이번 사이클의 핵심 수정** | 사람 exclude 3장이 나가고 사람 ok 2장이 빠지던 것. 방향 문서 §3-1 «사람이 확정하면 그것이 위에 선다» 그대로. 확정 0장인 실자료에서는 전후 바이트 동일(복숭아 251·블루베리 2,393 파일) |
| F2(편집 화면 밖 Ctrl+S/Z/Y 차단) | 수용 | 데이터 정리 탭에서 Ctrl+S 가 마지막 사진에 판정을 저장하던 것 — 원래 있던 결함이 새 탭으로 드러남 |
| F3·F4(실패/도는 중 표시 · 상자만 고를 때 문구) | 수용 | 글자 예산 유지 |
| 넘긴 것 2(스레드 실패→영영 409) | 고침 | 503 + state error + 다음 요청 정상 |
| 4(반쪽 폴더) | 고침 | 시작 때 job.json running 먼저, 죽으면 stale 표시 |
| 5(dict 아님 500) · 6(`confirmed_only` null→True, 이상값 400) · 7(409 문구 한글은 화면이) | 고침 | 전/후 시험 7/23→32/0 |
| 8(1차 `t1_api` 두 줄 순서 + 기대값 4개 갱신) | 고침 | D-6 은 «사람 flag 는 AI 제안 포함에서도 빠진다» 로 — 사이클 2 3차 §4-2 결정과 일치 |
| 3·9(문서 셋, `t3_helpfig` 재실행 가드) | 고침 | 가드 실측 rc=1 |
| **1(상자 YOLO 가 사람 확정을 안 가림)** | **사이클 4 본 과제** | 상자에는 아직 «확정» 이 없다. 지금은 확인창 문구로 알림 |
| 2차 `a4_input` 57/5 · `a3_conc` 12/4 로 «나빠진» 것 | 결함 아님 | 다섯+넷 전부 결정 6·2 가 옛 기대값을 뒤집은 자리. 사이클 4 회귀 때 기대값 갱신 목록에 넣음 |

## 2. 켜기 (내가 직접, 실측)
- 18:44 로그: 외부 IP 마지막 쓰기 09:07(변동 없음) → `py_compile` 6파일·`node --check` 2파일 통과 → `bash app/run.sh restart` **18:44:54~57** → PID 1458190 → **1908658**, 원본 폴더 유지
- 켠 뒤 GET(쓰기 0건): 비로그인 `/api/export_list` **401** · 로그인 뒤 **200**(`items [] · running [] · n_all 0`, 네 과일 `n_confirmed 0`, has_mask 전부 참, has_instances 사과·블루베리만) · `/api/fruits` AI 집계 불변(사과 572/51 · 블루베리 81/4 · 포도 3/72 · 복숭아 0/30) · `/api/export_status?job=../x` **404** · `/api/export_plan` 200
- 진짜 파이어폭스 1366×768: «데이터 정리» 탭 → 과일 라디오 4 · 종류 3(복숭아 «열매 번호 (없음)» 비활성) · «사람 확정만» 기본 · **«지금 확정된 사진 0장 — 0장이면 아무것도 나가지 않습니다 … · 지금 조건으로 나갈 사진 0장»** · 스크롤 없음(682/682) · «아직 내보낸 것이 없습니다». 저장은 누르지 않음(`exports/` 여전히 없음, `export_start` 요청 0건). 스크린샷 `~/ff_shots/c3/stage3/exp_tab_live_1366.png`
- 실측 메모: 첫 방문 안내창(`#tour`)이 탭 단추를 덮어 WebDriver 클릭이 막힘 — 사람은 안내창을 닫고 누르므로 문제 아님. 사이클 5 «처음 온 사람» 시나리오에서 안내창→탭 순서를 그대로 밟을 것

## 3. 내가 직접 본 것
- 백업 목록 `_backup_260918_c3_*`(1차 9파일) · `_backup_260918_c3b_*`(2차 3파일) · `_backup_260918_c3c_*`(소수정) 존재
- 실서버 화면의 확정 장수 줄이 `/api/export_list` 의 `n_confirmed_out` 과 같은 값(0) — 화면 숫자와 서버 숫자가 같은 코드(export_plan dry-run)에서 나옴

## 4. 사이클 4 에 내리는 결정
1. **상자·번호에도 «확정»**: status 항목의 `confirmed` 를 작업 종류별로 나누지 않고, **`confirmed_boxes`·`confirmed_instances` 두 칸을 추가**(마스크 확정 `confirmed` 와 독립). 큐에서 상자 모드/번호 모드일 때 Enter = 그 작업의 확정. 내보내기 «사람 확정만» 은 종류별로 해당 확정 칸을 본다(상자 YOLO 는 `confirmed_boxes`).
2. 마스킹 편의 세 가지(붓 크기 기억·자동 확대·스마트 채우기 우선)는 **localStorage + 한 줄씩**, 글자 예산 0 증가.
3. 전 조작 회귀 = 네 과일 × 세 작업 × (열기·판정·저장·되돌리기·확정·내보내기 dry-run). 낡은 기대값 갱신 목록: 사이클 2 `t2_queue` 8칸 · 사이클 3 2차 `s1_api` 5칸 · `a4_input` 5칸 · `a3_conc` 4칸 · N7(stems 타입 500) · N8(«AI…» 이름) · s5 C-2/C-4.
4. 규칙 §8-10 유지: 모래상자에서 만들고, **2차는 보통 리뷰**(짝수), 3차가 켬.

## 5. 사람이 할 것
Ctrl+F5 · 이름 적기 · 첫 내보내기는 «AI 제안 포함 · 복숭아 · 세그 마스크» 로 한 번 눌러 `exports/` 폴더가 생기는지 보기(11초 안팎) · 모래상자 삭제 목록에 `cycle_3/stage1/sandbox`·`stage2/sandbox`·`stage2c/sandbox` 추가
