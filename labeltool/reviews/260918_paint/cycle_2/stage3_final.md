작성: 2026-09-18

# 사이클 2 (사람 확정 모델 + 검수 큐) — 3차 최종 판정 (Fable 5.1)

판정 시각(실측 `date`): 2026-09-18 16:00
근거: 1차 `stage1_work.md` · 2차(보통 리뷰) `stage2_review.md` · 3차 전 소수정 `stage2b_fix.md` · 내가 직접 본 것(아래 §3)

## 1. 판정 — **합격, 실서버에 켬(15:58)**

| 항목 | 판정 | 근거 |
|---|---|---|
| 1차 작업(확정 칸·큐·진행률·묶음째 확정·export --confirmed-only) | 수용 | 2차가 시나리오·큐 순서·필터 개수·동시성 전부 재현(71/5 → 소수정 뒤 72/4, 남은 4 는 설계 몫) |
| 2차의 즉시 수정 F1~F6 | 수용 | 백업 `_backup_260918_c2b_*`, 전/후 시험 있음 |
| 2차가 3차에 넘긴 N1(1~4 가 AI 판정을 되살릴 수 없게 지움) | **고침** | `api_save` ok/flag/exclude 갈래에 `keep_prev=True` — 마스크 저장 길과 같은 장치, «수정본 되돌리기» 가 복구. 전 실패 → 후 통과 |
| N3(묶음째 확정이 남의 확정을 덮음) | **고침(서버)** | `confirm_status()` 가 사람이 확정한 구성원을 건너뛰고 `skipped` 로 알림. 화면 필터(F2)+서버 = 두 겹 |
| N5(묶음 대표를 무조건 «원본 OK» 로 확정) | **정책 확정 + 고침** | 대표는 **자기 AI 제안 그대로** 확정(flag 면 flag → 큐에 «고칠 것» 으로 남음), 구성원은 exclude. 확인창 «대표: <제안> 으로 확정 · 나머지 N장: 제외로 확정» |
| 가드(«서버가 아직 확정을 모릅니다» 인데 다음 장으로 넘어감) | **고침** | `enterConfirm()` 두 갈래 `if (!j) return;` — 옛 서버에서 Enter 열 번 = 열 장 넘어가고 확정 0 이던 오해 제거 |
| N7(stems 타입 500) · N8(«AI…» 이름) | 보류 → 사이클 4 | 데이터 소실 아님. 사이클 4 «전 조작 회귀» 에서 입력 검증 묶음으로 |
| s5 C-2·C-4(마스크 저장 뒤 판정 흐름) | 사이클 4 | 2차·소수정 모두 «설계 몫» 으로 분류, 동의 |

소수정 뒤 합계: `t_fix4.py` 전 16/13 → 후 **29/0**. 회귀에서 새로 실패한 것은 1차 `t2_queue` 8칸뿐이고 전부 N5 결정으로 **기대값이 낡은 것**(값이 여덟 번 다 flag) — 시험이 낡은 것이지 코드 결함이 아님. 사이클 4 회귀 때 기대값을 갱신한다.

## 2. 켜기 (내가 직접, 실측)

- 15:57 `app/logs/server.log` 외부 IP 마지막 쓰기 = 09:07 `POST /api/revert` 1건, 마지막 읽기 10:57 → 지금 쓰는 사람 없음
- PID 313878(내 계정, 04:49 기동) 확인 → `py_compile` 서버 5파일 통과 → `bash app/run.sh restart` **15:58:22~25** → 새 PID **1458190**, 원본 폴더 `datasets_reviewed_260916` 그대로(run.sh 가 `logs/last_data_root` 로 유지)
- 켠 뒤 확인 GET(쓰기 0건):
  - 비로그인 `/api/fruits` → **401** (게이트 정상)
  - `/api/fruits`: peach 125 · grape 2406 · apple 1001 · blueberry 1195 · 네 과일 모두 `n_confirmed 0 · by_person {}` · AI 제안 집계 사과 exclude 572/flag 51 · 블루베리 81/4 (변동 없음)
  - `/api/list?sort=queue`: items 에 `ai_status · confirmed · confirmed_by · src` 있음, 사과 큐 앞 20장 전부 AI flag(큐 순서 = 문제 → 묶음 대표 → 나머지)
  - `/api/item`: `dup_member_confirmed` 옴
  - 진짜 파이어폭스(GET 만): 로그인 → 사과 `…image101` → 하단 «AI 제안: 제외(중복) — 맞으면 Enter» 표시. 스크린샷 `~/ff_shots/c2/stage3/apple_image101_live.png`
- 소수정 보고서 §5 의 `GET /api/counts` 는 **존재하지 않는 경로(404)** — 확정 집계는 `/api/fruits` 안에 있다. 문서 오기, 기능 문제 아님.

## 3. 내가 직접 본 것
- `diff _backup_260918_c2c_server.py server.py` / `ui.js`: 뜻이 바뀌는 자리 5곳(keep_prev 2 · skipped · repv · return 2) — 보고서와 일치, 그 밖의 변경 없음
- 소수정이 정직 보고한 부수 효과(회귀가 `cycles/260917_신기능/` 시험 json 3개를 다시 씀)는 문서·데이터 무관 → 넘김

## 4. 사이클 3 에 내리는 결정 (2차 §12-3 세 가지)
1. **기본값 = «사람 확정만»**. 확정이 0장이면 «서버에 저장» 을 누르기 전에 화면에 **«지금 확정된 사진이 N장입니다 — 0장이면 아무것도 나가지 않습니다. AI 제안까지 포함하려면 아래를 고르세요»** 를 항상 보여 준다(경고가 아니라 상시 표시).
2. **사람이 확정한 «문제 있음(flag)» 은 기본 제외**(= 아직 고칠 것이므로 «확실한 데이터셋» 이 아니다). manifest 에는 `action=dropped · reason=flag` 로 남긴다. 포함 옵션은 두지 않는다(원하면 고쳐서 «수정함» 으로 확정하면 된다).
3. `exports/` 는 **툴 폴더 안** `260916_라벨링툴/exports/<YYMMDD_HHMMSS>_<과일>/`. 팀 데이터셋 폴더(`kds0206/datasets_*`)는 건드리지 않는다.
4. 규칙 §8-10(만들기/켜기 분리): 사이클 3 은 **모래상자에서 만들고 시험**, 실서버 재시작은 3차(나)가. 2차는 **공격적 리뷰**(홀수 사이클).

## 5. 사람이 할 것(변동 없음)
Ctrl+F5 한 번(정적 파일 새 판) · 이름 칸에 이름 적기 · 크롬/엣지 확인 · 모래상자 폴더 삭제(사이클 끝나면 목록으로 정리)
