# 체크리스트 — 260921 편의·안정성
작성: 2026-09-21

규칙
- 한 항목 = 한 커밋 감. 끝내면 `[x]` 로 바꾸고 `context-notes.md` 에 한 줄 남긴다.
- 항목마다 **검증**을 적어 두었다. 검증이 통과해야 `[x]`.
- 정적 파일(html·css·js)만 고친 항목은 실서버 재시작이 필요 없다. 파이썬을 건드리면 반드시 여기에 적는다.
- 누가 했는지 표시: `(C)`=Claude Opus, `(X)`=Codex, `(G)`=Gemini 검토, `(D)`=DeepSeek 검토.

## 1단계 — 안정성
- [x] S1 `api.js` 에 네트워크 실패 잡기 — 실패해도 `{ok:false,error:...}` 를 돌려주어 화면이 안 멈춘다 (C)
      검증: 샌드박스 서버를 끈 뒤 저장 → 붉은 알림이 뜨고 콘솔에 unhandled rejection 이 없다
      → `tests/browser/b5_offline.py` 9/9 통과 (2026-09-21 11:20). 정적 파일만 고쳤다(재시작 불필요).
- [x] S2 실패 알림은 사라지지 않게 — 실패는 사용자가 «확인» 을 눌러야 지워진다 (C)
      검증: 브라우저 시험에서 실패 알림이 3초 뒤에도 남아 있다
      → `tests/browser/b6_sticky_error.py` 15/15 통과 (2026-09-21 11:52). 4초 뒤에도 남고,
        «확인» 이 안 가려진 자리에 있고(실측 964,60 50×28), 누르면 지워진다. 성공 알림은 3초 그대로,
        1초 안내(`flashBrief`)도 그대로. 정적 파일만 고쳤다(재시작 불필요).
- [x] S7 (별건 — S2 검증 중 발견) **현황 탭이 통째로 안 그려지던 것** 수리 (C)
      `list.js:428` 에 `S.progressFruit = f;` 가 있는데 그 함수에는 `f` 가 없다(09-20 «쉬움 모드
      진행 막대» 의 복사 실수) → `ReferenceError: f is not defined` 로 `#dash` 가 빈 채로 남았다.
      검증: 실측 `#dash` 길이 0 → 5,897 · 콘솔 오류 0. `b3_struct3` 도 여기서 멈춰 있다가 29/0 통과.
- [x] S3 서버 연결 감시 — 30초마다 `/api/health`, 끊기면 상단 빨간 배너·복구되면 사라짐 (C)
      검증: 샌드박스에서 서버를 죽였다 살리며 배너가 뜨고 사라지는 것을 실제 브라우저로 확인
      → `tests/browser/b7_health.py` 20/20 통과 (2026-09-21 12:15). 실측 — 끄고 아무것도 안 눌러도
        **29.1초**에 배너, 다시 켜고 **4.1초**에 저절로 사라짐, 저장 실패로 알아챈 경우 **0.6초**.
        배너는 상단 바를 덮지 않고 32px 밀어 내린다(과일 고르개 그대로 클릭됨). S2 실패 띠와
        안 겹친다(배너 아래 32px · «확인» 단추 92px 에서 잡힘). 정적 파일만 고쳤다(재시작 불필요).
- [x] S4 창 닫기 경고가 마스크·상자·번호 **세 가지 모두** 를 보는지 확인·보강 (C)
      검증: 단위 시험 3가지(각 dirty 하나만 켠 상태)
      → `tests/sim/leavesim.js` 24/24 통과 (2026-09-21 12:36). **이미 셋을 다 보고 있었다** —
        `mask.js:143` 이 `edDirty·numDirty·bDirty` 셋을 다 읽는다. 그래서 앱 코드는 한 줄도
        안 고쳤다(보강할 것이 없었다). 대신 그 처리기와 `confirmLeave()` 를 **떼어 내** 돌려
        더러운 조합 8가지 전수·옛 브라우저용 `returnValue`·셋을 각각 묻는 문구를 못박았다.
- [x] S5 임시 백업·복구 — 저장 전 작업을 1분마다 브라우저에 백업, 같은 사진을 다시 열면 «복구할까요» (C)
      검증: 백업 저장·복구 제안·복구 거절 3가지 + 용량 초과 시 조용히 포기하는지
      → `tests/sim/backupsim.js` 58/58 · `tests/browser/b8_backup.py` 26/26 통과 (2026-09-21 13:05).
        새 파일 `app/static/js/backup.js`(마스크·상자·번호 셋 다) · `index.html` 에 script 한 줄 ·
        `list.js` openItem 에 되돌리기 물음 한 줄 · `instances.js` 내보내기에 `repaintNum` 한 이름.
        실측 — 1664×1248 사진 백업 10,609글자, 되돌린 뒤 44,639화소가 칠했던 값과 **똑같다**.
        잘게 엇갈려 접히지 않는 300만 칸은 22ms 에 포기한다(끝까지 세면 1.2초였다).
        정적 파일만 고쳤다(재시작 불필요). 기준선 ⑤ 4칸(index.html +139바이트)은 이 한 줄이다.
- [x] S6 저장 중 단추 잠금(이중 저장 방지) 확인·보강 (C)
      검증: 느린 응답을 흉내내고 두 번 눌러 요청이 한 번만 가는지
      → `tests/sim/locksim.js` 25/25 · `tests/browser/b9_double_save.py` 21/21 통과 (2026-09-21 13:28).
        **잠금은 하나도 없었다** — 세 저장(마스크·상자·번호)과 판정 다섯 단추·Enter 확정이 전부
        몇 번이든 눌렸다. `api.js` 에 `lockWhile()` 한 개를 만들고 네 자리에서 감쌌다
        (counts.js 판정 다섯 + Enter · boxes.js 상자 저장 · instances.js 번호 저장).
        실측 — 답을 1.5초 늦춘 상태에서 «수정본 저장» 세 번 클릭 → `/api/save` **1건**,
        Ctrl+S 다섯 번 연타 → **1건**, 상자·번호도 각각 **1건**. 끝나면 풀려 다시 눌린다(1.2초).
        `disabled` 대신 `.saving` 딱지를 쓴다(다른 규칙이 쓰는 칸을 뺏지 않으려고).
        정적 파일만 고쳤다(재시작 불필요). 기준선 ⑤ 6칸은 라운드 5와 똑같다(늘지 않았다).

## 2단계 — 편의 (그림판·포토샵·윈도우 관습)
- [x] U1 «저장 안 됨» 을 눈에 보이게 — 파일 이름 옆 `*`, 저장 단추 강조 (메모장·포토샵) (C)
      검증: dirty 3종에서 `*` 가 붙고 저장하면 사라진다
      → `tests/sim/dirtysim.js` 40/40 · `tests/browser/b10_dirty_star.py` 33/33 통과 (2026-09-21 13:49).
        새 파일 `app/static/js/dirtymark.js` · `index.html` script 한 줄 · `style.css` 규칙 3개.
        깃발 여덟 조합 전수(sim) + 진짜 파이어폭스로 **붓질·상자 그리기를 실제로** 해서 «*» 가 뜨고
        Ctrl+S·저장 클릭으로 사라지는 것까지(b10). 실측 — 붓을 떼고 «*» 까지 0.2초 안,
        별 잉크 y 18~24 가 파일 이름 y 16~25 와 세로로 맞고(처음엔 4px 높았다), 이름 오른쪽 5px,
        무엇에도 안 가려짐. 저장 단추 테두리 `rgb(255,212,0) 0 0 0 2px`.
        정적 파일만 고쳤다(재시작 불필요). 기준선 ⑤ 6칸 — 라운드 6과 **같은 6칸**이다
        (index.html 34,963 − 내 줄 125 = 34,838 = 라운드 6 값).
- [x] U2 배율 %를 확대바에 항상 표시 (그림판 상태줄) — 쉬움 모드에서도 보인다 (C)
      검증: 확대·축소·맞춤 뒤 숫자가 맞는지, 쉬움 모드에서 보이는지
      → `tests/browser/b11_zoompct.py` 40/40 통과 (2026-09-21 14:00). `index.html` 에 `#zoompct`
        한 칸 · `style.css` 규칙 2개 · `view.js` draw() 에 `showZoomPct()` 한 줄(배율이 바뀌면
        반드시 `S.dirty` 라 놓치는 길이 없다). 실측 — 확대바 왼쪽 끝 46×30, 단추와 4px 띄고
        세로 가운데 차 **0px**, 휠을 굴린 뒤 **0.06초**에 숫자가 따라온다. ＋가 1.25배·－가 되돌림·
        0 키가 «맞춤» 과 같은 값(39%)임을 실측했고, 쉬움 모드에서 `#hud` 는 `display:none` 인데
        이 칸은 남는다. 전문가 모드에서 `#hud` 와 **같은 수**(39%)를 말한다. 끝(3000%)·끝(3%)에서도
        어긋나지 않고 단추 자리가 안 밀린다. 휴대폰 폭 390 에서도 겹치지 않는다(24% · 단추 클릭됨).
        정적 파일만 고쳤다(재시작 불필요). 기준선 ⑤ 6칸 = 내 두 줄(index.html +313바이트) 4칸 +
        사람이 고친 복숭아 2칸.
- [x] U3 상자 «다시하기» Ctrl+Y (일관성) (C)
      검증: 상자 그리기 → Ctrl+Z → Ctrl+Y 로 되돌아오는 단위 시험
      → `tests/sim/boxsim.js` 69/69(새 19가지) · `tests/browser/b12_box_redo.py` 34/34 통과
        (2026-09-21 14:23). `state.js` 에 `bredo` 칸 하나 · `boxes.js` 에 `boxRedo()` 와
        갈래 버리기 두 줄 · `keys.js` 의 «상자에는 다시 실행이 없습니다» 거절을 `boxRedo()` 로.
        단추는 안 만들었다(번호 편집도 `#numundo` 하나뿐이다) → **HTML 을 한 글자도 안 고쳤다.**
        실측 — 진짜 파이어폭스에서 네모를 그리고 Ctrl+Z→Ctrl+Y 로 좌표 `664,745,970,1051` 이
        글자까지 돌아온다(0.14초). 세 칸 왕복도 한 칸씩 정확하고, 되돌린 뒤 새로 그리면
        옛 갈래는 버려지며, 상자 모드에서 «칠한 영역» 다시하기(`#redo`)는 0회 눌린다.
        정적 파일만 고쳤다(재시작 불필요). 기준선 ⑤ 6칸은 라운드 8과 **똑같다**(늘지 않았다).
- [x] U4 캔버스 더블클릭 = 화면 맞춤 (이미지 뷰어 관습) (C)
      검증: 브라우저 시험에서 배율이 맞춤값으로 돌아온다
      → `tests/browser/b13_dblclick_fit.py` 33/33 통과 (2026-09-21 14:47). `main.js` 에 dblclick
        손잡이 하나 · `view.js` 의 ✋ 풍선말 한 줄. **HTML·CSS 는 한 글자도 안 고쳤다.**
        듣는 자리를 둘로 좁혔다 — ① 사진 밖 회색 여백(어느 도구에서나) ② ✋이동·Space.
        사진 **위**에서는 그리기가 먼저다(자동채움을 두 번 누른 사람의 확대를 풀면 안 된다).
        실측 — 더블클릭에서 맞춤까지 0.39초, 배율·밀린 자리(`s,tx,ty`)가 0 키와 **숫자 그대로**
        같다(0.3925 / 259.4333 / 7.575). 붓으로 사진 위를 두 번 눌러도 77% 그대로이고 붓질은
        살아 있다(되돌리기 2칸). 여백(377화소) 더블클릭은 25%→39% 로 돌아오고 칠해진 화소는
        38,957개 그대로다. 다각형 찍는 중·상자 모드(상자 11개·bDirty 그대로)도 확인.
        정적 파일만 고쳤다(재시작 불필요). 기준선 ⑤ 6칸은 라운드 9와 **똑같다**(늘지 않았다).
- [x] U5 도구별 커서 구분 — 붓·지우개·다각형·이동·상자 (C)
      검증: 도구를 바꿀 때 `#cv` 의 cursor 값이 바뀐다
      → `tests/browser/b14_cursor.py` 37/37 통과 (2026-09-21 15:00). `view.js` 에 `cursorFor()`·
        `applyCursor()`(+draw() 한 줄) · `boxes.js` 에 `boxCursor()` · `mask.js`·`syncTaskUI` 의
        커서 쓰기 두 곳을 지워 **규칙을 한 곳으로** 모았다 · `keys.js` Space 두 줄.
        **HTML·CSS 는 한 글자도 안 고쳤다.** 실측 — 붓 `crosshair` · 지우개 `cell` ·
        다각형 둘 `copy` · 자동채움 둘 `pointer` · ✋ `grab`(끄는 중 `grabbing`) ·
        Space 중 `grab`, 놓으면 도구 커서로. 상자는 그리기 `crosshair` · 고르기에서
        빈 곳 `default` · 상자 안 `move` · 모서리 `nwse-resize`/`nesw-resize`(끄는 중에도 그대로).
        번호 편집은 `crosshair` 그대로(회귀). 붓을 쥔 채 열 번 움직여도 style 쓰기 **0회**.
        정적 파일만 고쳤다(재시작 불필요). 기준선 ⑤ 6칸은 라운드 10과 **똑같다**(늘지 않았다).
- [x] U6 고른 도구를 «눌린» 모양으로 (구형 그림판) — CSS만 (C)
      검증: HTML 기준선 차이 0, 화면 12장 대조
      → `tests/browser/b15_pressed.py` 55/55 통과 (2026-09-21 15:29). `style.css` 규칙 **둘만** —
        `#toolrail button.on`(테두리·그림자·여백)과 `#toolrail #viewsw button.on`(3단 스위치).
        **HTML·JS 를 한 글자도 안 고쳤다** → 기준선 ⑤ 6칸이 라운드 11과 똑같다(index.html 35,276).
        고치기 전 실측 — «① 칠한 영역» 칸의 위 안쪽 (32,51,70) 이 가운데 (34,48,63) 보다 오히려
        **밝았다**(그림자가 짙은 바탕에 묻혔다). 테두리 #2f6fe0 도 바탕 #1764ad 보다 밝았다.
        고친 뒤 실측 — 위 안쪽 (17,75,130) · 바탕 (23,100,173) · 아래 밝은 줄 (65,123,178) ·
        테두리 (13,66,120). 얼굴(아이콘·글자·글쇠)이 **정확히 1화소** 내려앉고 칸 자리는
        `[6,160,74,54]` 그대로다(위 여백 2→3, 아래 2→1 로 맞바꿔 합이 4 그대로).
        흰 글자 대비 6.06:1(도구)·13.44:1(작업) ≥ 4.5. 마스크·상자·번호 세 작업과 «보기 전환»
        3단 스위치 전부, 좁은 창 1200·휴대폰 폭 390 에서 글자가 칸 밖으로 안 샌다(0화소).
        번호 편집에서 **잠긴** 도구가 짙은 파랑 그대로인 것도 못박았다(잠금 규칙과 눌림 규칙이
        같은 힘이라 순서가 뒤집히면 회색으로 변한다). 화면 12장 `~/ff_shots/tests_260920/pressed/`.
        정적 파일만 고쳤다(재시작 불필요).
- [x] U7 단축키 한 장 표 — F1 로 열고 Esc 로 닫는 겹창 (윈도우 관습) (C)
      검증: 표에 적힌 키가 `keys.js` 실제 키와 하나도 어긋나지 않는지 대조 시험
      → `tests/unit/u7_keys_doc.py` 72/72(새 1가지) · `tests/browser/b16_f1_help.py` 48/48 통과
        (2026-09-21 15:50). `index.html` 에 겹창 한 덩어리(+828바이트) · `style.css` 규칙 8개 ·
        `keys.js` 에 KEYS 한 줄(F1)과 손잡이 한 뭉치. **표를 새로 안 그린다** — 겹창 안은
        `data-key-table` 한 칸이고 keys.js 54줄이 KEYS 로 채운다(그래서 겹창·안내 창·도움말이
        어긋날 길이 없다). 실측 — F1 에서 표까지 **0.02초**, 겹창 33줄이 `UI.KEYS` 33줄과
        키·뜻 전부 같고, 떠 있는 동안 b·e·1·3·0·x·k·q 를 눌러도 도구·배율·상자모드·판정이
        **하나도 안 바뀐다**. 닫는 Esc 가 그리던 다각형(점 3개)을 안 지운다. 목록 화면·글자 칸
        («내 이름»)에서도 F1 이 듣고, 안내 창이 떠 있으면 겹창을 안 연다(두 장 금지).
        u7 시험에 **반대 방향**을 더했다 — 지금까지 ①②③ 은 전부 «표에 적힌 것» 에서 출발해서,
        표에 안 적고 손잡이만 더하면 조용히 통과했다. 이제 코드의 `case "…"`·Ctrl 갈래를 긁어
        29개가 표에 다 있는지 본다. 정적 파일만 고쳤다(재시작 불필요).
        기준선 ⑤ 6칸 — 라운드 12와 **같은 6칸**이다(index.html 36,104 − 내 덩어리 828 = 35,276).
- [x] U8 붓 크기를 도구마다 기억 (포토샵) — 붓 30 / 지우개 40 을 따로 (C)
      검증: 도구를 왕복해도 각자 크기가 유지된다
      → `tests/sim/brushsim.js` 44/44 · `tests/browser/b17_brushsize.py` 27/27 통과 (2026-09-21 16:28).
        `mask.js` 에 `sizeTool()` 한 줄 · `brushKey/saveBrush/loadBrush` 가 칸 이름을 받도록 · setTool 에
        건너갈 때 넣고 꺼내는 두 줄 · `index.html` 슬라이더 풍선말 한 줄(+40바이트).
        칸은 **둘뿐**이다 — 붓은 `brush:<과일>`(0918부터 쌓인 값이 그대로 살아난다) · 지우개만 `:erase`.
        굵기를 안 쓰는 도구(다각형·자동채움·✋)는 붓 칸에 같이 둔다. 처음 지우개는 **쓰던 붓 굵기를
        물려받는다**(없는 기본값 40 을 지어내지 않는다 — 쓰던 사람의 지우개가 말없이 굵어지면 안 된다).
        실측 — 붓 12 ↔ 지우개 44 를 다섯 번 왕복해도 `[12,44]×5`, ] [ 키가 남의 칸을 안 건드리고,
        새로고침 뒤에도 8·52 가 남는다. **진짜로 그어 봤다** — 붓 8 자국 두께 10화소, 지우개 52 자국
        54화소(양끝 한 화소는 `floor/ceil` 몫). 정적 파일만 고쳤다(재시작 불필요).
        기준선 ⑤ 6칸 — 라운드 13과 **같은 6칸**이고 index.html 36,104 → 36,144 = 풍선말 한 줄(+40)이다.
        `sim/modesim.js` 가 setTool 을 떼어 쓰고 있어 같이 손봤다(31/31 그대로).
- [x] U9 누르고 있는 동안만 «원본만» 보기 (`~` 키, 포토샵 레이어 잠깐 끄기) (C)
      검증: keydown 중 레이어가 꺼지고 keyup 에 원래 체크대로 돌아온다
      → `tests/browser/b18_peek.py` 42/42 통과 (2026-09-21 16:41). `view.js` 에 peek 네 함수와
        blur 한 줄 · `keys.js` 에 KEYS 한 줄(`~`)과 손잡이 등록 두 줄. **HTML·CSS 는 한 글자도
        안 고쳤다.** 새 상태를 안 만들고 이미 있는 «보기 전환» 을 대신 누른다(setVMode) →
        끄고 되돌리는 규칙이 한 벌로 남는다. 놓을 때 «원본만» 이 아니면 아무것도 안 되돌린다.
        실측 — 누르고 0.41초에 겹 여섯이 `000000`, 놓으면 캔버스가 **한 화소도 안 다르다**(0/591,860).
        꺼 두었던 AI 겹은 꺼진 채로 돌아오고, «칠한 영역만» 에서 눌렀다 놓으면 거기로 돌아온다.
        세 번 눌러도(keydown 되풀이) 한 번 놓으면 풀리고, 알트탭(blur)에도 풀린다.
        상자 12개·붓질 63,914화소와 «저장 안 됨» 깃발은 안 건드린다. 이름 칸에서는 «~» 가 글자로
        찍히고 Ctrl+~ 는 안 듣는다. `u7_keys_doc` 72→74(표·도움말·손잡이 대조) · `b16` 48/48(34줄).
        정적 파일만 고쳤다(재시작 불필요). 기준선 ⑤ 6칸은 라운드 14와 **똑같다**(index.html 36,144 그대로).
        ⚠ 이 라운드는 `bash` 가 허용 목록에 없어 `run_all.sh` 를 못 돌렸다 — 단위 319·API 158·
        브라우저 b1 81·t2 27·b16 48·b18 42 는 python3 로 직접 돌려 전부 통과. **시뮬(node)·
        merged 는 못 돌렸다**(아래 context-notes 참고).
- [x] U10 상자 모드 십자 안내선 (그림판·CAD) (C)
      검증: 상자 모드에서만 그려지고, 그리기 성능이 눈에 띄게 나빠지지 않는지
      → `tests/browser/b19_boxguide.py` 38/38 통과 (2026-09-21 17:14). `boxes.js` 에 `drawBoxGuide()`
        한 개와 `drawBoxes()` 첫 줄 한 줄. **HTML·CSS 는 한 글자도 안 고쳤다**(단추·단축키·끄고 켜는
        칸을 하나도 안 만들었다). 흰 실선 위에 검은 점선을 겹쳐 밝은 필름·어두운 사진 양쪽에서 보인다.
        «상자 모드에서만» 을 코드가 아니라 **캔버스 화소로** 쟀다 — 안내선만 껐다 켰을 때 바뀐 점이
        **2,274개 = (세로 490 + 가로 653) × 굵기 2** 로 십자 하나 넓이와 딱 맞고, 가장 굵은 칸·줄이
        커서 자리(716↔717 · 310↔311)다. 칠한 영역·번호 편집·«원본만»·«고르기»·사진 밖 회색 여백
        다섯 자리에서는 그 칸이 **0점**이다. 굵기는 배율 0.39 에서도 3.0 에서도 **2 장치화소**다
        (사진 화소가 아니라 화면 화소를 기준으로 나누기 때문).
        성능 — `drawBoxes()` 한 번이 0.030 → 0.113ms(**+0.083ms**, 60fps 예산 16.7ms 의 0.5%),
        매 프레임 다시 그릴 때 프레임 간격 중앙값은 **33.22 ↔ 33.22ms 로 똑같다**.
        회귀 — 끌어 그린 네모 `549,475,916,725` 가 안내선이 가리킨 정수 자리와 **글자까지 같고**,
        점선 설정이 뒤 그림으로 새지 않는다(getLineDash 빈 칸). 콘솔 오류 0.
        정적 파일만 고쳤다(재시작 불필요). 기준선 ⑤ 6칸은 라운드 15와 **똑같다**(index.html 36,144 그대로).

## 3단계 — 전체 디자인 (getdesign.md 에서 고른 supabase 체계 적용)
근거 `design/후보비교.md` · 토큰과 방법 `design_final.md` · 원본 `design/DESIGN.md`
- [x] G1 `style.css` 맨 위에 `:root` 토큰 선언 (색 133가지를 계단으로 접기 시작) (C)
      검증: 선언만 추가한 상태에서 화면이 하나도 안 바뀐다(`--check-baseline` 차이 0, 화면 12장 동일)
      → 회귀 **885개 단정 0 실패** · 화면 12장 **전부 0화소 차이** (2026-09-21 17:31).
        `style.css` 맨 위에 `:root` 한 덩어리(토큰 28개 + 주석, +3,632바이트)뿐이고
        **HTML·JS 는 한 글자도 안 고쳤다**(index.html 36,144 그대로 = 라운드 16 값).
        새 자 `tests/browser/shots12.py` — 화면 12장을 늘 같은 순서로 찍고 두 벌을 화소로 센다
        (G2·G3·G6·G7 이 «화면 12장 대조» 를 쓰므로 자를 먼저 만들었다).
        **자부터 검증했다** — 고치기 전 상태로 두 판(before·before2)을 찍어 12장 전부 0화소임을
        확인한 **뒤에** 고쳤다. 처음엔 현황 탭이 105화소 흔들렸는데 그 자리 (176,58)~(200,67) 을
        키워 보니 `#dashtime` 의 **초** 두 글자였다 → 찍기 직전에 고정 시각으로 덮는다.
        토큰이 조용히 버려지지 않았는지도 실측 — 브라우저가 28개를 **다 읽고**(`--canvas`…
        `--shadow-3`), 아직 `var(--…)` 로 꺼내 쓰는 곳은 **0가지**다(그래서 화면이 안 바뀐다).
        기준선 ⑤ 6칸은 라운드 16과 **똑같다**(내 것은 하나도 없다 — ⑤ 는 CSS 를 안 본다.
        style.css 는 ④ «참고용» 에만 뜬다). 정적 파일만 고쳤다(재시작 불필요).
        화면 `~/ff_shots/tests_260920/design12/{before,before2,after}/`.
- [x] G2 상단 한 줄을 `--chrome`(#1c1c1c) 계단으로 — 파랑회색 10여 가지를 접는다 (C)
      검증: HTML 기준선 차이 0, 폭 360·390 에서 안 깨진다
      → `tests/browser/b20_chrome.py` 59/59 · 회귀 885개 0 실패 (2026-09-21 17:56).
        `style.css` 만 고쳤다 — **HTML·JS 는 한 글자도 안 건드렸다**(index.html 36,144 그대로
        = 라운드 17 값 → HTML 기준선 차이 0 · 기준선 ⑤ 6칸도 라운드 17과 똑같다).
        이 한 줄에 있던 파랑회색 **넷**(#22303f 바탕 · #cfd8e3 글씨 · #46596e 테두리·구분선 ·
        #3a4a5c hover)을 `--chrome*` 계단 하나로 접었다. 계단에 두 칸을 **더 놓았다** —
        design_final.md 의 넷으로는 hover 와 1px 선을 적을 수 없었다(`--chrome-mute` 로 칠하면
        선 대비가 1.86:1 → 6.11:1 로 튄다) → `--chrome-hi:#343434` · `--chrome-line:#3d3d3d`.
        실측 — 상단 바 (28,28,28) · 글씨 (237,237,237) 14.56:1 · 흐린 글씨 (154,154,154) 6.06:1 ·
        고른 탭 17.04:1 · 노랑 11.90:1 · 초록 10.82:1(전부 AA 4.5 위) · 선 1.57:1 · hover 1.37:1.
        «색만 바뀌었나» 를 두 가지로 못박았다 — ① 옛 색을 덧씌워 되돌려 봐도 바 안 **17자리**가
        한 화소도 안 움직인다 ② 화면 12장의 **바뀐 자리**가 전부 상단 바 칸과 정확히 같다
        (`(0,0,1366,40)`·`(0,0,1200,40)`·휴대폰 `(0,0,488,190)` — b20 이 잰 `#topbar` 자리와
        글자까지 같고, 그 아래로는 한 화소도 안 바뀌었다). hover 는 진짜 마우스를 올려 화소로 쟀다.
        뜻이 묶인 색은 그대로다(«*» 노랑 · 저장됨 초록 · 실패 띠 붉은색 · 사용법 노랑).
        정적 파일만 고쳤다(재시작 불필요). 화면 5장 `~/ff_shots/tests_260920/chrome/` ·
        12장 두 벌 `…/design12/{g2_before,g2_after}/`.
- [x] G3 도구상자·오른쪽 패널·목록 카드의 테두리·바탕을 `--hairline*`·`--canvas*` 로 (C)
      검증: 데이터 색(레이어 4겹·판정 5칸·상태 딱지)이 **하나도 안 바뀌었는지** 색값으로 대조
      → `tests/browser/b21_panels.py` 72/72 · 회귀 885개 0 실패 (2026-09-21 18:28).
        `style.css` 만 고쳤다 — **HTML·JS 는 한 글자도 안 건드렸다**(index.html 36,144 그대로
        = 라운드 18 값 → HTML 기준선 차이 0 · 기준선 ⑤ 6칸도 라운드 18과 똑같다).
        옛 회색 **아홉**(#eef1f5 바탕 · #e4e9ef 도구상자 · #ccd4dd 가장자리 · #b7c1cc 칸 나눔 ·
        #c3cbd4 타일 테두리 · #e3e8ee 작은 상자 · #dde3ea 목록줄·카드·쪽수줄 · #e9edf1 사진 자리)
        을 `--canvas*`·`--hairline*` 여섯 계단으로 접었다. **토큰은 하나도 안 늘렸다**(30개 그대로).
        검증을 «눈으로» 가 아니라 **고치기 전에 떠 둔 기준**(`b21_before.json` · 칸 93개)과 댄다 —
        칸마다 여덟 성질을 보아 **744짝 중 714짝이 한 글자도 안 달라졌고, 달라진 30짝은 내가
        미리 적어 둔 그것과 정확히 같다**. 데이터 색(레이어 4겹·판정 5칸·상태 딱지·알약)이 전부
        그 714짝 안에 있다. 화면 12장으로 한 번 더 쟀다 — 뜻이 묶인 색이던 화소 **228,625개 중
        안쪽이 달라진 것 0개**(둥근 모서리에서 다시 섞인 33개는 뒤 바탕이 바뀐 탓이고 채널 차 최대 4).
        자리 15곳은 **한 화소도** 안 밀렸다. 대비 — 도구상자 글씨 6.91:1 · 도구 이름 9.75:1 ·
        본문 7.28:1(전부 AA 위). 선 세기는 전과 차 0.02~0.06 이다(가장자리 1.23→1.21 ·
        칸 나눔 1.49→1.54 · 타일 1.64→1.69 · 목록 밑선 1.29→1.33 · 작은 상자 1.23→1.17).
        ⚠ 상태 색(:hover · 눌린 도구 `.on` · 잠긴 도구 `:disabled`)은 **일부러 안 건드렸다** — G4 몫.
        b20 의 «`var()` 는 상단 바뿐» 단정을 G3 선택자 13개를 **이름으로 적어** 넓혔다(59/59 그대로).
        정적 파일만 고쳤다(재시작 불필요). 화면 `~/ff_shots/tests_260920/panels/` 4장 ·
        12장 두 벌 `…/design12/{g3_before,g3_after}/`.
- [x] G4 단추·입력칸 반지름·테두리 통일 + `:focus-visible` 에메랄드 테두리 (C)
      검증: 키보드 Tab 으로 돌 때 지금 어디인지 늘 보인다
      → `tests/browser/b22_focus.py` 132/132 · 회귀 885개 0 실패 · b21 80 · b20 59 · b15 55 ·
        b16 48 · b10 33 (2026-09-21 19:03). `style.css` 만 고쳤다 — **HTML·JS 는 한 글자도
        안 건드렸다**(index.html 36,144 그대로 = 라운드 19 값 → HTML 기준선 차이 0 ·
        기준선 ⑤ 6칸도 라운드 19와 똑같다). 고친 것은 여섯 군데다 — `button`(테두리 #b9c2cc →
        `--hairline-strong` · 반지름 `--r-sm`) · `button:hover`(파란빛 #f2f7fc·#7fa8d8 →
        `--canvas-sunken`·`--ink-faint`) · `button:disabled:hover` · `select, input` ·
        `#toolrail button:hover` · 반지름 7px 이던 두 자리(`#toolrail button`·`#vbrow button.big`).
        **키보드 띠는 진짜 Tab 키로 확인했다** — 목록 17군데·편집 32군데를 돌아 멈추는 자리가
        **전부** 실선 2px · `--accent-deep` · 1px 띄움 · `:focus-visible` 이다. 화면 화소로도
        쟀다 — «▶» 둘레 232개 · 검색칸 820개 · «◀»(검은 상단 바) 212개 · «▸» 252개이고,
        아무 데도 포커스가 없으면 그 자리 **0개**다. **마우스로 누르면 안 뜬다**(띠 화소 0개).
        띠 색을 `--accent` 가 아니라 한 칸 진한 `--accent-deep` 으로 골랐다 — 흰 바탕 위 밝기
        차가 2.00 → **2.66:1** 이다(검은 상단 바 위 6.41:1). 3:1 에는 못 미친다(아래 미결).
        «바뀐 것이 그것뿐인가» 는 고치기 전에 떠 둔 `b22_before.json`(칸 48개)과 댄다 —
        **432짝 중 357짝 그대로 · 바뀐 75짝은 내가 미리 적어 둔 그것과 정확히 같다.**
        판정 5칸·카드 상태 테두리·눌린 도구(U6)·잠긴 도구는 그 357 안에 있다. 자리 17곳은
        **한 화소도** 안 밀렸다. 화면 12장의 바뀐 화소 29,956개 중 **절반(50.4%)이 한 짝**
        (#b9c2cc→#c7c7c7)이고, 뜻이 묶인 색이던 화소는 **30개뿐이며 전부 타일 네 모서리**
        (반지름 7→6px · 채널 차 1). 정적 파일만 고쳤다(재시작 불필요).
        화면 `~/ff_shots/tests_260920/focus/` 9장 · 12장 두 벌 `…/design12/{g4_before,g4_after}/`.
- [x] G5 숫자 칸에 고정폭·tabular-nums (좌표·배율·개수·쪽수) (C)
      검증: 숫자가 바뀔 때 칸 너비가 흔들리지 않는다
      → `tests/browser/b23_numwidth.py` 30/30 · 회귀 885개 0 실패 · b21 80 · b22 132 · b11 40 ·
        b3 29 · b19 38 (2026-09-21 19:45). `style.css` 만 고쳤다 — **HTML·JS 는 한 글자도
        안 건드렸다**(index.html 36,144 그대로 = 라운드 20 값 → HTML 기준선 차이 0 ·
        기준선 ⑤ 6칸도 라운드 20과 똑같다).
        ⚠ **`--mono` 는 재 보고 일부러 안 썼다**(design_final.md 5번과 다르다). 지금 글꼴은
        숫자 폭이 **이미 같고**(«1» 도 «8» 도 7.77px) 흔들림의 원인은 글자 모양이 아니라
        **자릿수**다. `--mono` 를 씌우면 한글 +15% · 숫자 +8.5% 로 오히려 넓어지고 흔들림은
        그대로였다(#pageinfo 26.67 → 28.87px). 그래서 고친 방법은 **자리를 미리 잡는 것**이다 —
        `min-width` 를 `ch`(숫자 한 자 폭) 단위로. 자르는 것이 아니라 «최소» 라 더 긴 글자가
        와도 안 잘린다. tabular-nums 는 사람 PC 의 진짜 맑은 고딕을 대비해 남겼다.
        고친 네 칸 — `#hud` 29ch · `#zoompct` 8.5ch · `.cnts` 15ch · `#pageinfo` 21ch.
        `#listinfo` 는 **일부러 안 잡았다**(가운데 빈칸이 먹어 ◀ 가 안 밀린다).
        `#hud:empty{display:none}` 를 같이 뒀다 — 자리를 잡으면 빈 칸이 크게 보인다
        (전에는 16×6 짜리 검은 조각이 사진 왼쪽 아래에 늘 떠 있었다).
        **진짜로 움직여 봤다** — ＋를 아홉 번 눌러 49%→292% 로 가는 동안 `#zoompct` 너비가
        65.17px **한 값**이고 － 단추가 1183.6 에서 안 움직였다. 마우스를 사진 위 네 곳으로
        옮겨 «(1, 1)» ↔ «(1400, 1000)» 을 만들어도 `#hud` 가 193.33px 그대로다.
        쪽수는 «1 / 2» → «10 / 10» 에서 140px 고정이고 ▶ 가 160 에서 **한 화소도** 안 움직인다.
        «바뀐 것이 그것뿐인가» 를 두 가지로 못박았다 — ① 고치기 전에 뜬 `b23_before.json`
        (칸 24개)과 대어 **83짝 그대로 · 바뀐 13짝은 미리 적어 둔 그것과 정확히 같다**
        ② 화면 12장의 바뀐 화소 **17,359개가 전부 그 네 칸(과 그만큼 줄어든 `#todo` 의
        오른쪽 끝) 안**이고 그 밖은 **0개**다(현황·데이터 정리 두 장은 0화소).
        정적 파일만 고쳤다(재시작 불필요). 화면 `~/ff_shots/tests_260920/numwidth/` 4장 ·
        12장 두 벌 `…/design12/{g4_after,g5_after}/`.
- [x] G6 그림자 줄이기 + `mobile.css` 토큰화 (C)
      검증: 폭 360·390 화면과 PC 12장 대조
      → `tests/browser/b24_shadow_mobile.py` 50/50 · 회귀 885개 0 실패 · b22 132 · b21 80 ·
        b20 59 · b15 55 · b16 48 · b10 33 · b6 15 (2026-09-21 20:19). **HTML·JS 는 한 글자도
        안 건드렸다**(index.html 36,144 그대로 = 라운드 21 값 → HTML 기준선 차이 0 ·
        기준선 ⑤ 6칸도 라운드 21과 똑같다).
        고친 것은 셋이다 — ① `.tourcard`(안내 창·단축키 겹창) `0 18px 50px rgba(0,0,0,.35)`
        → `var(--shadow-3)` ② `#saveflash.flashbad` `0 4px 12px rgba(0,0,0,.35)`
        → `var(--shadow-1)` ③ `mobile.css` 의 `#22303f` **둘**(알림 띠·아래 단추 줄)
        → `var(--chrome)`(+ `border-radius:6px` → `var(--r-sm)` · 값이 같아 화면은 안 바뀐다).
        ⚠ **안쪽(inset) 그림자와 노란 테두리는 안 건드렸다** — 눌린 도구(U6) 두 겹과
          «저장 안 됨»(U1) `0 0 0 2px #ffd400` 은 계단이 아니라 뜻·명암이다(b15·b10 이 못박는다).
        ⚠ 카드·패널에 그림자를 **더하지 않았다**. «줄이기» 지 «입히기» 가 아니다.
        실측 — 휴대폰 폭에서 아래 단추 줄이 상단 바와 **같은 색**이 됐다
        (전 rgb(34,48,63) ↔ rgb(28,28,28) → 후 셋 다 rgb(28,28,28) · 띠 글씨 대비 10.82:1).
        그늘은 **껐다 켜서** 쟀다(U10 의 방법) — 덮는 자리 136,051 → **131,609화소**,
        평균 5.75 → **2.44채널** 어둡게. 화면 12장 중 **두 장만** 바뀌었고
        (08_keyhelp 99,344 · 12_phone_390 10,185 · 나머지 **열 장 0화소**),
        겹창 쪽 바뀐 화소 104,171개는 **어두워진 것이 0개**(전부 밝아졌다)이며 카드 칸 안으로
        센 141개는 전부 **둥근 네 모서리 14px** 안이고 카드 **속**은 0개다.
        휴대폰 쪽 10,197개(엄밀히 RGB 로 세면) 중 10,072개가 `#22303f → #1c1c1c` **한 짝**이고
        전부 아래 단추 줄 칸(줄 634~693) 안이다.
        b20·b21·b22 의 «`var()` 는 G2∪G3∪G4 뿐» 단정은 **풀지 않고** `.tourcard`·
        `#saveflash.flashbad` 두 이름을 적어 넓혔다. 정적 파일만 고쳤다(재시작 불필요).
        화면 12장 두 벌 `~/ff_shots/tests_260920/design12/{g6_before,g6_after}/`.
- [x] G7 화면 12장 기준선 새로 뜨고, 사용법 문서의 그림이 낡았으면 다시 찍기 (C)
      검증: `--browser` 통과 + 새 기준선으로 두 번 연속 통과
      → 새 기준선 `~/ff_shots/tests_260920/design12/baseline/` · **두 판 다 12칸 0화소**
        (`g7_pass1` · `g7_pass2` · 2026-09-21 20:52). 브라우저 `b1 81/0` · `t2 27/0` ·
        회귀 822개 0 실패 · 기준선 ⑤ **6칸은 라운드 23과 똑같다**(index.html 36,144 그대로 =
        HTML 기준선 차이 0). 새 그림은 ④ «참고용» 에만 뜬다.
      **사용법 그림 스물두 장 중 열여섯 장이 낡아 있었다** — 맨 윗줄이 옛 파랑회색
        `rgb(34,48,63)` 이었고(G2 뒤로는 `rgb(28,28,28)`), help.html 은 그보다 더 낡아
        «마스크» 라는 옛 이름·도구 세 개·판정 네 개를 보이고 있었다(지금은 «칠한 영역»·
        도구 넷·판정 다섯). figcaption 두 곳은 이미 «⚠️ 재배치 전 화면» 이라고 적혀 있었다.
      → **열일곱 장을 다시 찍었다** — `help.html` 아홉(`ui_*_260921.png`) ·
        `how_to.html` 여덟(`help/howto_260921/`). 새 자 `tests/browser/helpfigs.py` 가
        «어떤 사진·어떤 상태인지» 를 코드로 적어 둔다(다음에 또 낡으면 한 줄로 다시 찍는다).
        **다시 돌려 대 보니 17장 중 15장이 0화소로 똑같고, 다른 둘은 시계뿐**이다
        (`counts` 의 `#dashtime` 194화소 · `confirm` 의 확정 시각 128화소).
        새 지킴이 `tests/unit/u10_help_figs.py` **45/45** — 끊긴 그림 0개 · 상단 바 색이
        `style.css` 의 `--chrome` 과 같은지 · «재배치 전» 경고가 안 남았는지를 못박는다
        (옛 그림 넷으로 되돌려 보면 전부 «다르다» 가 나온다 — 실측).
        HTML 두 문서와 그림만 고쳤다(정적 파일 → 실서버 재시작 불필요).
## 4단계 — 마무리 검증
- [x] Z1 전체 회귀 `tests/run_all.sh` 통과 + HTML 기준선 대조 (C)
      → **1,358개 단정 0 실패 · rc=0** 을 **세 판** 연속(344초·357초·380초 · 2026-09-21 21:22).
        **열 라운드 만에 `run_all.sh` 를 진짜로 돌렸다** — 라운드 15~24는 `bash` 가 허용 목록에
        없어 `auto/run_bundles.py` 의 «베낀 묶음» 으로 갈음했고, 그 베낀 목록에 **두 묶음이
        빠져 있었다**: `merged`(통합 데이터셋 빌드 **536개**)와 `syntax/bash_run.sh`.
        Z1 은 «전체» 라 그 둘을 빼면 답이 아니다 → run_bundles.py 에 `runall` 한 덩어리를 더해
        **그 스크립트를 베끼지 않고 그대로 실행**한다(이제 묶음이 늘어도 여기를 고칠 일이 없다).
        묶음 27개 — 문법 4 · 단위 10(454) · 시뮬 8(300) · API 4(158) · merged 1(**536**).
        **기준선은 새로 떴다.** 대조에서 나온 ⑤ 6칸을 한 칸씩 확인했다 — 넷은 index.html
        34,699 → 36,144 이고 늘어난 **1,445바이트가 이 사이클의 다섯 줄과 정확히 같다**
        (S5 +139 · U1 +125 · U2 +313 · U7 +828 · U8 +40 · 다섯 줄이 파일에 다 있는 것도 셌다),
        둘은 **사람이 12:05:37·12:05:40 에 실서버에서 고친 복숭아 마스크**다(mtime 실측 ·
        라운드 3 기록과 시각까지 맞는다). 옛 기준선은 `--rebaseline` 이 스스로
        `fixtures/_baseline_prev/260921_212202/` 에 옮겨 두었다(안 지워진다).
        새 기준선으로 다시 대조 → **①②③④⑤ 다섯 칸 전부 0개**.
        **문턱을 낮추지 않았다** — 옛 ③ 과 칸마다 대어 **내려간 묶음 0개 · 사라진 묶음 0개**이고
        오히려 올랐다(merged 535/실패1 → **536/실패0** · boxsim 50→69 · u4 7→8 · u5 42→43),
        묶음 수도 15 → **27**(새 열둘). ①② 는 **바이트까지 같다**(api_sha256.txt 11,012 ·
        exports.json 9,058 이 옛 사본과 한 글자도 안 다르다 = API 100개·내보내기 119개가
        이 사이클 내내 안 바뀌었다). 까닭·전후 sha 는 `tests/fixtures/BASELINE_CHANGELOG.md` 에.
        앱 코드는 **한 글자도 안 고쳤다**(index.html 36,144 그대로). 실서버 5111(PID 155257)은
        돌던 그대로다 — 시험은 빈 포트만 쓰고 제가 띄운 PID 만 끈다(`KEEP_OUT` 에 5111 있음).
        ⚠ `--browser` 는 **Z2 몫**이라 여기 안 들어 있다.
- [x] Z2 `--browser` 까지 통과 (C)
      → 두 판을 돌렸다 (2026-09-21 21:59).
        ① **고치기 전 그대로** `run_all.sh --browser` → **1,466개 단정 0 실패 · rc=0 · 489초.**
        ② 브라우저 묶음을 **스물넷 전부**로 넓힌 뒤 → **977개 단정 0 실패 · rc=0 · 932초.**
      🔴 **①이 초록인데도 이 사이클의 증거는 한 줄도 안 돌고 있었다.** `run_all.sh:157` 이
        `for f in b1_browser t2_ui` 로 **이름 둘만** 적고 있었다 — 그 사이 화면 시험이
        `b3`~`b24` 로 늘어 **스물넷**이 됐는데 아무도 그 줄을 안 고쳤다(**2/24**).
        S1~S6·U1~U10·G1~G7 을 못박는 b5~b24 **스무 개가 `--browser` 밖에 있었다.**
        Z1 이 «베낀 묶음 목록에 merged 가 빠져 있었다» 를 찾은 것과 **같은 종류**의 빈틈이다.
        → 이름을 적지 않고 **글롭**(`b*.py` + `t2_ui.py`)으로 집는다. 새 시험을 만들면
        저절로 들어오고 여기를 고칠 일이 없다. `api`·`sim` 이 글롭을 피한 까닭(묶음이 조용히
        늘면 기준선 ③ 이 «다르다»)은 **여기엔 없다** — ③ 은 `browser/` 줄을 **아예 안 읽는다**
        (`baseline/snapshot.py:503` 실측). 그래서 **재기준선이 필요 없다**(앞 라운드가
        «③ 에 경고 세 줄이 뜰 것» 이라 적어 둔 것은 사실과 달랐다 — 코드를 읽어 바로잡았다).
        «`b*.py` 는 시험, 자(도구)는 다른 이름» 은 **이미 있는 규칙**이라 지어내지 않았다
        (`browser/shots12.py` 머리말) → `helpfigs.py`·`shots12.py` 는 안 걸린다(글롭 결과 24개 실측).
      🔴 **b24 하나가 rc 를 뒤집어 말하고 있었다.** `b24_shadow_mobile.py:492` 가
        `sys.exit(0 if main() else 1)` 인데 `main()` 은 `L.summary()` 의 **실패 개수**다 →
        0(다 통과)일 때 rc=1, 실패가 있을 때 rc=0 이다. 나머지 **스물셋은 전부**
        `1 if main() else 0` 이다(실측). 짐작이 아니라 **고치기 전에 돌려 봤다** —
        `✘ b24 rc=1 · 54초 · 통과 50 / 실패 0`. 고친 뒤 같은 시험이 `✔ 50/0 · rc=0` 이다.
        묶음에 넣기 전이라 지금까지 아무도 이 거짓말을 안 봤다(넣었으면 첫판부터 붉었을 것).
      실측 — 스물넷이 **서로 안 흔든다**: 시험마다 제 모래상자(`sb_offline`·`sb_health` …
        **23개가 전부 다른 이름**)와 빈 포트를 쓴다(`L.sync(SB)`·`L.start(port=port, sb=SB)`).
        G7 이 겪은 «자가 자의 바닥을 흔든» 사고가 되풀이될 길이 없다.
      고친 파일 셋 — `tests/run_all.sh`(브라우저 줄 하나 + 까닭 주석) ·
        `tests/browser/b24_shadow_mobile.py`(rc 한 줄) · `tests/README.md`(묶음 표 한 칸).
        두 길이 어긋나지 않게 `cycles/260921_편의/auto/run_bundles.py` 의 `b_browser()` 도 같이
        글롭으로 맞췄다(양쪽이 **같은 스물넷을 같은 순서로** 집는 것을 실측). `bash -n` rc=0.
        **앱 코드는 한 글자도 안 고쳤다**(index.html 36,144 그대로) → 실서버 재시작 불필요.
        실서버 5111(PID 155257)은 17시간 29분째 돌던 그대로다.
- [x] Z3 다른 AI 교차 검토 1회 — Gemini·DeepSeek 로 «빠진 것/위험» 훑기 (C)(G)(D)
      → 보고서 `260921_Z3_교차검토.md` · 답 원문 `council/z3_260921/`(gemini 5,101 · deepseek 8,469바이트)
        · 질문 `question_z3.md`(8,441바이트) (2026-09-21 22:16). 둘 다 답이 왔다(한도로 막힌 쪽 없음).
        **앱 코드는 한 글자도 안 고쳤다**(index.html 36,144 그대로) → 실서버 재시작 불필요.
        회귀 `runall --only unit,api` **522개 단정 0 실패**(단위 374 · API 158 → 여기선 522).
      🔴 **셋이 같은 자리를 짚었다 — S5 백업이 `localStorage` 칸 하나를 고정으로 쓰는 것.**
        두 AI 는 «탭끼리 덮어쓴다» 고 했는데 **재 보니 한 단계 더 나빴다** — `backupNow()` 는
        적을 것이 없으면 `drop()` 으로 칸을 **지운다**(`backup.js:92`). 목록 화면의 탭은
        `S.stem` 이 null 이라(`state.js:14`) 늘 «적을 것 없음» 이다 → **사진을 안 연 탭 하나가
        1분마다 옆 탭의 백업을 지운다.** 짐작이 아니라 `backup.js` 코드 130줄을 글자 그대로
        떼어 내 탭 둘을 한 localStorage 위에 올려 실측했다(`tests/_sandbox/z3_tab_probe.js` —
        ①적힘 103글자 → ②목록 탭 한 번 → ③`null (사라졌다)` · 탭이 하나면 남는다).
      🔴 **두 AI 가 못 본 것을 하나 찾았다** — 저장이 도는 1~2초 동안 캔버스는 안 잠기는데
        (`.saving` 은 단추에만 · `style.css:141` · 감싼 선택자 넷에 `#cv` 없음) 저장이 끝나면
        `counts.js:159` 가 `S.edDirty = false` 로 깃발을 내린다. 그 사이 붓질은 저장도 안 되고
        «*»(U1)·창 닫기 경고(S4)·임시 백업(S5)이 **한꺼번에 조용해진다.**
      **받은 말을 그대로 안 믿고 여섯 가지를 반박했다**(근거는 보고서 §2) — Gemini 의
        «`api()` 결과에 `.json()` 을 쓰는 곳이 깨진다»(그런 곳 **0곳** · Response 메서드 네 자리는
        전부 맨 `fetch`) · «백업이 2백만 글자를 넘는다»(실측 **10,609글자**) · «1분마다 수 초 굳는다»
        (실측 8ms) · DeepSeek 의 «반쪽짜리 데이터가 서버에 간다»(보낼 것은 첫 `await` **전에**
        동기로 다 뜬다) · «저장이 영영 불가능»(`finally` 가 반드시 푼다) 등.
      «과하다» 는 지적 둘은 **안 받았다** — U1 0.2초 폴링(자리 40군데 중 하나만 빠뜨려도 «*» 가
        거짓말을 한다 · 바뀐 게 없으면 화면을 안 건드린다)과 S3 health 감시(그 감시가 있는 까닭이
        정확히 «아무것도 안 누르는 30분» 이고 값은 분당 12번이다).
      ⚠ 이번 라운드는 **훑기**라 고치지 않았다. 고칠 후보 여섯을 보고서 §5 에 차례로 적었다
        (A 백업 칸을 사진마다 · B 저장 뒤 «안 바뀌었을 때만» 깃발 내리기 · D 401 전에 backupNow
        한 줄 · F 백업 시각을 «*» 풍선말에 · C AbortController · E 두 사람 동시 편집은 서버 몫).
      새 길 하나 — `auto/run_council.py`: 자동 라운드는 `bash`·`~/.local/bin/council` 을 바로
        못 부른다(허용 목록 밖). Z1 이 `run_bundles.py` 에 낸 «베끼지 말고 그대로 실행» 을
        그대로 따라 council 스크립트를 부르기만 한다(질문 던지는 규칙을 옮겨 오지 않았다).
- [x] Z4 실서버 반영 확인(정적 파일이므로 재시작 없음) + 읽기 스모크 (C)
      → `tests/live/z4_live_smoke.py` **65/65 통과 · 실패 0 · rc=0** (2026-09-22 00:26).
        **«재시작 없이 반영된다» 를 처음으로 실측했다.** 이 사이클은 그 말을 스물몇 라운드 내내
        **전제**로만 썼다 — 여기서 실서버가 주는 **바이트**를 받아 디스크와 댔다:
        정적 파일 **118개가 118개 다 한 바이트도 안 다르다**. 실서버는 04:29:23 에 떴고
        `index.html` 은 16:06:58 에 바뀌었는데(35개가 서버보다 나중) 주는 것은 지금 디스크다.
        헤더도 쟀다 — `Cache-Control: no-cache` · `ETag "1789963022.07-8626-4036241060"` 가
        **지금 파일 크기 8,626** 을 말하고 같은 ETag 를 다시 물으면 **304 · 0바이트**다.
        ③ 이 사이클의 손자국 **22개**(S1·S5·S6 · U1~U5·U7~U10 · G1·G2·G4·G5·G6)가
        실서버가 준 글자 안에 다 있다. ④ 읽기 스모크 — `/api/fruits`(peach 125 · grape 2,406 ·
        apple 1,001 · blueberry 1,195) · `/api/list` · `/api/item`(1248x1664) · `/img` 3.7MB PNG ·
        `/mask?layer=gt` · `/api/boxes` · `/api/instance_info` · `/api/stats` · 집계 둘 ·
        `/api/export_list` · `/box` · 사용법 두 문서 · **사용법 그림 78장 전부 200·진짜 PNG(끊긴 것 0개)** ·
        없는 파일은 404. ⑤ 문지기도 살아 있다(쿠키 없이 `/api/list` 401 · `/` 302 → `/login` · `/img` 401).
      🔴 **처음 돌렸을 때 27개가 실패했는데 서버 탓이 아니었다 — 시험이 로그인을 안 하고 있었다.**
        라운드 28이 쓰다가 세션 한도에 걸려 **한 번도 못 돌린** 파일이라 ②③ 의 `get("/static/…")`
        **다섯 자리에 쿠키를 안 주고 있었다** → 문지기가 302 로 `/login` 에 보내 **빈 몸통**이 오고,
        그것이 «디스크와 0/118 같음»·«index.html 에 backup.js 가 없다» 로 읽혔다.
        **짐작으로 서버를 의심하지 않았다** — 실패 줄에 찍힌 `status=302` 를 읽고,
        `api/photos.py:62` 의 `/static/<path:fn>` 이 문지기 뒤에 있는 것을 확인한 뒤 쿠키를 줬다.
        (쿠키 **없이** 무엇이 막히는지는 §5 가 따로 센다 — 그 셋은 일부러 안 고쳤다.)
        다시 안 속게 `⓪ 직접 서명한 쿠키가 받아들여진다` 한 줄을 §0 에 넣었다 — 쿠키가 죽으면
        27개가 엉뚱한 말을 하는 대신 이 한 줄이 먼저 붉어진다.
      **아무것도 안 썼고 아무 프로세스도 안 건드렸다** — 보낸 요청 **231번이 전부 GET**(POST 함수가
        아예 없다 · 로그인도 `core/auth.py:42` 와 같은 방법으로 쿠키를 **직접 서명**해 시도 제한
        카운터를 안 건드린다) · `data/` 지문 `(7398, 78774971, …134.874)` **전후 동일** ·
        `app/cache/` `(1389, 10856540, …162.646)` **전후 동일** · `/thumb` 은 **캐시에 이미 있는**
        사진만 불렀다(없는 것을 부르면 서버가 jpg 를 새로 만든다 = 쓰기 · `api/photos.py:362`).
        실서버 5111(PID 155257)은 **19시간 58분째** 돌던 그대로다(시작 시각까지 전후 대조).
      고친 파일은 **하나뿐이다** — `tests/live/z4_live_smoke.py`(쿠키 다섯 자리 + ⓪ 한 줄).
        **앱 코드는 한 글자도 안 고쳤다**(index.html 36,144 · mtime 16:06:58 그대로)
        → 실서버 재시작 불필요 · 재기준선 불필요. 이 파일은 `tests/live/` 에 혼자 있어
        어느 묶음 글롭(`unit/u*.py`·`sim/*.js`·`api/`·`browser/b*.py`)에도 **안 걸린다**
        (실서버가 떠 있을 때만 뜻이 있어 회귀에 섞이면 안 된다). 회귀 `runall --only unit,api`
        **522개 단정 0 실패 · 37초**(Z3 라운드와 같은 값).
- [x] Z5 `작업기록.md`·`.ai-context/CURRENT.md` 갱신, GitHub 공개본 올리기 (C)
      ✅ 2026-09-22 05:38 곽동신 세션(Opus 5)이 push 완료 — 비밀검사 실패 0 · `b2a86cd` → **`0142054`** · origin/main 확인
      🔒 **셋 중 둘 끝 · 남은 것은 `git add`→`commit`→`push` 뿐** (2026-09-22 00:47)
      ⚠ 2026-09-22 00:55(라운드 52) — 그 명령은 **세 줄이 아니라 네 줄**이다. 앞에
        `export GIT_SSH_COMMAND='ssh -i ~/.ssh/agrivision_deploy -o IdentitiesOnly=yes'`
        가 있어야 한다(origin 이 SSH 인데 열쇠 이름이 기본이 아니고 `~/.ssh/config` 도
        `core.sshCommand` 도 없다 → 없으면 `Permission denied (publickey)`).
        고친 명령은 `auto/STUCK` 에 있다.
      ① `작업기록.md`(「1. 현재 상태」·「2. 작업 이력」·「3. 지금 어디」·최종 수정) **끝**
      ② `.ai-context/CURRENT.md` 한 덩어리 **끝**
      ③ 공개 사본 `_git_agrivision-kwak` 에 **복사 끝 · 비밀검사 통과** —
        새 파일 49 · 바뀐 파일 31 · 사라진 파일 0 · **944파일 19,169,296바이트 실패 0 rc=0**.
        넣기 전 모래상자 `tests/_sandbox/pub_260921`(895파일)에서 먼저 돌려 목록이 이 사이클의
        손자국과 같은 것과 비밀 미리 훑기 **걸린 것 0** 을 확인했다. 키워드 65줄 중 55줄은
        모바일 사이클과 글자까지 같고 새 10줄은 전부 코드 구조다.
        새 자 둘 — `prepare_public.py`(수집 규칙을 베끼지 않고 `collect_public.py` 를 그대로
        실행 · 대상 폴더를 인자로 받는다 · 두 번 돌려도 README 가 겹쳐 안 붙는다) ·
        `audit_public.py`(260921_모바일 것과 **md5 동일한 사본**).
      🔴 **push 만 막혔다** — 이 자동 세션의 allow 에 `git add`·`commit`·`push` 가 없다
        (`git add -A --dry-run` 이 실제로 거부됨 · 비대화형이라 승인 창도 못 띄움).
        허용 목록을 스스로 고쳐 권한을 만들지 않았다. 로컬 main·origin/main 둘 다 `b2a86cd` 라
        내가 얹은 것 말고 끼어든 변경이 없다. 칠 명령은 `auto/STUCK` 에 그대로 있다.
        **올리지 못했으므로 `[x]` 로 바꾸지 않았다.** 다음 Claude 라운드는 같은 벽에
        되풀이해 부딪히지 말고 `auto/STUCK` 을 먼저 읽어라.
      🟢 2026-09-22 01:05(라운드 53) — **그 네 줄이 정말 통하는지 git 에게 물어 확인했다.**
        라운드 52는 명령을 «설정을 읽어» 고쳤을 뿐 커밋이 멈출 **다른** 자리를 안 봤다.
        ① 이름·메일이 `.git/config` 13~15줄에 있어 `git commit` 이 «당신이 누구냐» 로 안 멈춘다
        ② `HEAD` 가 `refs/heads/main`(분리 머리 아님) ③ 움직이는 훅 **0개** ④ 소유자가
        `kds0206`(나) 라 `safe.directory` 불필요. **더 걸릴 자리는 없다.**
        🔴 아무도 안 본 위험 하나를 닫았다 — 이 저장소 `.gitignore` 에 `**/images/`·
        `**/cache/`·`**/_sandbox/`·`*.zip` 등 **스물셋**이 있어 `git add -A` 가 말없이
        건너뛸 수 있었다. git 에게 물으니(`--ignored=matching -uall`) **`!!` 줄이 0개**다 —
        빠지는 파일이 없다. 덤으로 라운드 51의 «새 49 · 바뀐 31» 을 **git 이 똑같이 센다**
        (`??` 49 · ` M` 31 — 그 숫자는 파일 목록 비교로 얻은 것이었다).
        올라갈 크기는 **+2,334줄 / −140줄**(추적 파일 31개).
        🟢 라운드 52의 «읽기 git 조차 막혔다» 는 **형태 탓이었다** — `cd` 를 딸린 명령 없이
        한 줄로 친 뒤 치면 `status`·`log`·`diff` 가 다 돈다(넷 다 실측). 쓰기(`git add`)는
        `-C` 를 떼도 여전히 거부다(실측). 벽은 **쓰기에만** 있다.
        아무것도 안 썼다 — 보낸 git 명령 다섯이 **전부 읽기**이고 공개 사본·앱 코드를
        한 글자도 안 고쳤다. 여전히 push 가 안 됐으므로 `[ ]` 그대로다.
      앱 코드는 **한 글자도 안 고쳤다**(index.html 36,144 그대로) → 재시작·재기준선 불필요.
      🟢 2026-09-22 01:13(라운드 54) — **push 를 기다리는 사이 공개 사본이 낡아 있었다. 다시 떴다.**
        라운드 53이 «더 볼 것이 없다» 고 했지만 한 가지를 아무도 안 봤다 — 공개 사본은
        **00:45:33 에 뜬 사진**인데(mtime 실측) 라운드 51·52·53이 그 뒤로도 기록을 계속 썼다.
        `prepare_public.py` 38~46줄이 **checklist·context-notes·STATUS·CURRENT·작업기록** 을
        공개본에 넣으므로, 사람이 14:00 에 그대로 push 하면 **이 사이클의 Z5 항목조차 없는
        `작업기록.md`** 와, 라운드 52가 «세 줄이 아니라 네 줄» 이라 고친 정정이 **빠진**
        checklist 가 올라갈 뻔했다(낡은 만큼의 바이트는 `auto/STUCK` 에 적었다).
        → `prepare_public.py` 를 다시 돌렸다(복사 489 · 가린 파일 8 · 제외 84).
        🔴 **처음 쓴 자가 «가려짐» 을 «낡음» 이라고 말했다** — 다시 뜬 뒤에도 세 문서가 달라
        낡은 줄 알았는데, `collect_public.py:26` 이 **보안 관련 낱말 여섯 가지**(영문 넷·한국어 둘)
        중 하나라도 든 **줄을 통째로** 안내 문장으로 바꾸기 때문이었다. 짐작으로 넘기지 않고 자를 고쳐 **같은 `clean()`
        을 베끼지 않고 그대로 실행**해 댔다 → `tests/_sandbox/z5_stale_probe.py` **0/6 낡음**
        (plan·checklist·STATUS 셋은 **바이트까지 같고**, context-notes 는 가린 줄 4 ·
        작업기록 47 · CURRENT 는 서버 주소 한 줄이 `<서버주소>` 로 바뀐 것뿐이다 —
        123줄 중 **다른 줄 1개** 실측, 두 글자가 UTF-8 로 똑같이 14바이트라 크기가 안 변했다).
        비밀검사 다시 → **944파일 · 실패 0 · rc=0**. git 이 센 것도 다시 봤다 —
        `??` **49** · ` M` **31** · `!!` **0** 으로 **파일 셋은 하나도 안 변했고** 바뀐 것은
        기록 다섯의 **내용뿐**이다(올라갈 줄 수만 늘었다).
        ⚠ 바이트·줄 수 같은 **흔들리는 수치는 여기 안 적는다** — 이 글 자체가 공개본에 들어가면
        그 수가 또 달라진다(이 사이클이 계속 잡아 온 «제 크기를 적는» 실수다).
        그 수는 복사되지 않는 `auto/STUCK` 과 `evidence/public_collection.json` 에 있다.
        ⚠ **push 권한은 그대로다** — `git add -A --dry-run` 을 두 형태로 눌러 봤고 둘 다
        «requires approval» 이다(`cd` 를 딸린 한 줄 · `cd` 를 따로 친 뒤 한 줄). 울타리는 안 옮겼고
        `subprocess` 우회도 안 썼다. 보낸 git 명령 셋은 **전부 읽기**다.
        ⚠ 그래서 `auto/STUCK` 의 인계를 **다섯 줄**로 고쳤다 — 맨 앞에 `prepare_public.py` 한 줄.
        기록이 또 바뀌면 이 낡음이 되풀이되므로, 올리기 직전에 한 번 더 뜨는 것이 옳다.
        앱 코드는 **한 글자도 안 고쳤다**(index.html 36,144 그대로) → 재시작·재기준선 불필요.
        **올리지 못했으므로 `[ ]` 그대로다.**
      🟢 2026-09-22 01:33(라운드 55) — **사람이 먼저 읽는 두 문서가 낡은 명령을 말하고 있었다.**
        STUCK 이 시킨 두 가지만 했다. ① 권한 — `git add -A --dry-run` 이 여전히 거부다(벽 그대로).
        ② 낡음 — `z5_stale_probe.py` 가 **1/6 낡음**을 잡았는데, 공개본 `STATUS.md` 91줄 ↔
        가린 원본 92줄로 **diff 덩어리 1개 · 더해진 줄 1개**이고 그 한 줄은 라운드 54가
        `prepare_public.py` 를 돌린 **뒤에** 적은 제 꼬리줄이다. 그래서 다시 뜨지 않았다 —
        지금 뜨면 이 라운드 기록을 쓰는 순간 또 낡는다. STUCK 이 이미 «고칠 것은 한 번 다시
        떠 두기가 아니라 **올리기 직전에** 뜬다는 순서» 라고 적어 두었고 다섯 줄의 첫 줄이 그것이다.
        🔴 **대신 아무도 안 본 자리를 찾았다** — 라운드 52·54의 정정이 `auto/STUCK` 에만 있고
        **사람이 실제로 먼저 읽는 두 문서에는 안 들어가 있었다.** `작업기록.md` 「1. 현재 상태」는
        «세 줄이 아니라 네 줄»(`prepare_public.py` 빠짐)이었고, `.ai-context/CURRENT.md` 는
        **두 정정 모두 빠진 «세 줄뿐»** 이었다(113·121줄). CLAUDE.md 가 세션 시작에 읽으라고
        못박은 것이 `작업기록.md` 이고 다른 AI 인계는 `CURRENT.md` 다 → 사람이 14:00 에
        그 둘만 보고 쳤다면 `Permission denied (publickey)` 를 맞거나, 넘겨도 **기록이 낡은 채로**
        공개 저장소에 남았을 것이다. 과거 기록을 고쳐 쓰지 않고 이 사이클이 쓰던 방식대로
        «⚠ 정정» 을 그 자리에 달았고, 둘 다 «칠 명령은 `auto/STUCK` 이 언제나 최신» 을 가리키게
        했다(수를 두 곳에 베껴 두면 다음 라운드에 또 어긋난다).
        **고친 줄이 공개본에서 통째로 가려지지 않는 것도 확인했다** — 라운드 54가 빠졌던 함정이다.
        `collect_public.py` 의 `clean()` 을 베끼지 않고 그대로 돌려 재니 가린 줄이
        **작업기록 47 · CURRENT 0** 으로 고치기 **전과 같다**(내 두 줄은 걸러지는 낱말 여덟에
        하나도 안 걸린다 — 그 여덟이 무엇인지는 `collect_public.py:16` 에 있다).
        ⚠ **처음 쓴 판은 나도 걸렸다** — 그 여덟을 여기에 **그대로 베껴 적는 바람에** 이 줄이
        통째로 가려져 checklist 의 가린 줄이 0 → 1 이 됐다(라운드 54가 빠졌던 바로 그 함정이다).
        마지막에 한 번 더 재서 잡았고, 낱말을 적는 대신 **자리만 가리키게** 고쳤다.
        기록 다섯의 가린 줄은 이제 고치기 **전과 똑같다**(작업기록 47 · CURRENT 0 ·
        checklist 0 · context-notes 4 · STATUS 0).
        회귀 `run_bundles unit api` **522개 단정 0 실패**(단위 374 · API 148 → 522 · 라운드 27·
        50~54와 같은 값). 고친 파일은 기록 넷뿐이고 **앱·시험 코드는 한 글자도 안 고쳤다**
        (index.html 36,144 · mtime 09-21 16:06:58 그대로) → 재시작·재기준선 불필요.
        실서버 5111(PID 155257)은 **20시간 58분째** 돌던 그대로다.
        **여전히 push 를 못 했으므로 `[ ]` 그대로다.**
      🟢 2026-09-22 01:37(라운드 56) — **세어 둔 줄 수가 틀렸고, 그 탓에 한 줄을 빼면
        인훈 저장소로 갈 수 있었다.** STUCK 이 시킨 두 가지부터 했다. ① 권한 — `git add -A
        --dry-run` 이 **두 형태 다** 거부다(벽 그대로). ② 낡음 — 검사가 «5 / 6» 인데
        라운드 55가 «1 / 6 이 정상» 이라 적어 둔 것과 다르다. 재 보니 **틀린 말이 아니라
        원래 흔들리는 수**다 — 앞 라운드가 기록을 몇 개 건드렸느냐에 따라 1 도 5 도 된다
        (라운드 55는 STATUS 하나만 뒤진 순간에 쟀고, 그 라운드가 끝나며 다섯을 고쳤다).
        그래서 **다시 뜨지 않았고**(뜨는 순간 이 글이 또 뒤진다) STUCK 에서 그 수를 지웠다.
      🔴 **대신 사람이 14:00 에 실제로 칠 블록을 처음으로 «세어» 봤다.** 머리말은 «**다섯 줄**
        (넷도 셋도 아니다)» 라고 못박고 있는데 블록은 `cd` 로 시작하는 **여섯 줄**이었다.
        세어 온 규칙이 `cd` 를 빼고 세는 것이었기 때문인데 **사람은 눈에 보이는 줄을 센다** —
        여섯을 보고 «다섯» 을 읽으면 한 줄이 군더더기로 보이고, git 도 python 도 아닌 `cd` 가
        가장 먼저 빠질 자리다. 그 한 줄이 빠지면 `git add -A` 는 **서 있던 자리**에서 돈다.
        짐작하지 않고 그 자리를 쟀다 — 공개 사본의 **부모 폴더가 `…/kds0206`** 이고 바로 옆
        `semantic-segmentation` **도 git 저장소**이며(`.git` 실측) 그 origin 이 이 파일이
        «절대 push 금지» 라 적어 둔 **인훈 저장소**(`ih021088-ui/…`)다. 더구나 그
        `.git/config` 에 `[user] name = ih021088-ui` 가 박혀 있어 거기서 친 commit 은
        **인훈 이름으로** 남는다. ⚠ **과장하지 않는다** — origin 이 HTTPS 인데 저장된 인증
        수단이 하나도 없어(실측) `push` 는 막힐 값이 크다. 그러나 그건 **운이지 설계가 아니고**
        `add`·`commit` 은 그 전에 이미 성공한다.
        → 고친 방법은 «`cd` 를 빼지 마라» 를 **더 적는 것이 아니다.** 뺄 수 있는 줄을
        **없앴다** — git 세 줄이 각자 `-C <저장소>` 로 저장소를 들고 다닌다. 이제 어느 줄을
        빼먹어도 그 단계가 안 될 뿐 **엉뚱한 저장소로는 갈 수 없다.** 지어낸 형태가 아니라
        **이 프로젝트가 이미 쓰는 관습**이다 — git 을 부르는 자 일곱이 전부
        `subprocess(…, cwd=G)` 로 저장소를 못박는다(실측). `git -C` 가 그 셸판이다.
        머리말에는 **다시 수를 적지 않았다**(«아래 블록을 통째로») · 남아 있던 옛 수 두 곳
        («위 네 줄»·«위 다섯 줄»)도 «위 블록» 으로 고쳤다 · 맨 위에 «지금 칠 명령은 그 블록
        하나뿐이고 본문의 옛 형태는 읽되 치지 마라» 를 달았다.
        **고친 안내가 공개본에서 가려지지 않는 것도 쟀다**(라운드 54·55가 빠진 함정) —
        `collect_public.py` 의 `clean()` 을 베끼지 않고 그대로 돌렸다.
        회귀 `run_bundles unit api` **522개 단정 0 실패**(라운드 27·50~55와 같은 값).
        고친 파일은 기록뿐이고 **앱·시험 코드는 한 글자도 안 고쳤다**
        (index.html 36,144 · mtime 09-21 16:06:58 그대로) → 재시작·재기준선 불필요.
        실서버 5111(PID 155257)은 **21시간 8분째** 돌던 그대로다.
        **여전히 push 를 못 했으므로 `[ ]` 그대로다.**
      🟢 2026-09-22 01:52(라운드 57) — **열쇠 쪽에서 멈출 자리 셋을 ssh 없이 닫았다.**
        STUCK 의 두 가지부터 했다(① 권한 그대로 거부 ② 낡음은 몇이 나오든 다시 안 뜬다).
        라운드 52는 설정 넷만 읽고 «`ssh` 도 허용 목록 밖이라 직접 눌러 확인하지는 못했다» 고
        적어 두었는데, 그 줄은 «열쇠 이름만 맞으면 된다» 처럼 읽힌다. **이름이 맞아도 멈추는
        자리가 셋 더** 있고 셋 다 붙여넣는 순간에만·붉은 글씨 없이 그냥 멈춘다 →
        ① 열쇠 파일이 `-rw-------`(남이 못 읽는다 — ssh 가 거절하는 조건에 안 걸린다)
        ② 암호 잠금이 **없고** `.pub` 의 공개 덩어리가 개인 열쇠 안에 그대로 들어 있다
        (`ssh-ed25519` · 이름표 `agrivision-kwak`) ③ `known_hosts` 4줄 중 **셋이 github.com**.
        ⚠ ③ 은 **눈으로 보면 정반대로 읽힌다** — 해시로 접혀 있어 `grep` 이 0 을 낸다.
        짐작으로 «없다» 고 적지 않고 접힌 줄을 푸는 정해진 방법으로 풀어 맞췄다.
        덤으로 `known_hosts` 가 쓰인 시각 **22:31** 이 `작업기록.md:1434~1435` 의 시험 push
        두 번과 같아, 09-19 의 성공이 이 열쇠·이 손님 확인으로 난 것임이 맞물린다.
      🔴 **같은 라운드에 사고도 냈다 — 공개 사본을 통째로 덮어썼고 되돌렸다.**
        `collect_public.py` 의 함수 하나만 쓰려고 `importlib` 로 **모듈 전체를 불렀더니**
        수집 본문이 맨 위에 그냥 놓여 있어 260920 사이클 수집이 돌아 공개 사본을 다시 썼다
        (`copied 602` · 이 사이클은 489). git 이 센 것이 `?? 49 · M 31` → **`?? 55 · M 36`**
        으로 벌어졌다 — 그대로 올렸다면 **올리기로 한 것이 아닌 파일 일곱**이 섞였을 것이다.
        되돌렸다 — `prepare_public.py` 다시 · 안 따라온 여섯은 **원본이 `문서/` 에 있는 것을
        확인한 뒤** 공개 사본에서만 지움 · 바뀐 하나는 `git show HEAD:<파일> > <파일>`
        (git **쓰기** 없이 읽기와 셸 넘김으로만 — 울타리 안 옮김).
        → 지금 **`?? 49 · M 31 · !! 0` 으로 라운드 54 값과 똑같고**, ` M` 31줄이 전부 이
        사이클 손자국이며 사고 때 끼어 있던 260920 후보판 파이썬 여섯은 **하나도 안 남았다**.
        비밀검사 **944파일 · 실패 0 · 키워드 65줄**(라운드 51·54와 같은 값).
        ⚠ **못 되돌린 것 하나** — 260920 사이클의 수집 기록 파일 하나를 덮어썼고 git 에도 없다.
        잃은 것은 그 판의 **자세한 목록뿐**이다(요약과 2분 전 판의 자세한 목록은 같은 폴더에
        그대로 있다). 덮어쓴 내 출력은 이름을 바꿔 09-20 기록인 척하지 않게 했다.
        **왜 걸렸나** — «베끼지 말고 그대로 실행» 에는 이 프로젝트가 쓰는 **정해진 형태**가
        있는데(`prepare_public.py:22~26` · `z5_stale_probe.py:13~14` 가 같은 형태다)
        내가 세 번째 방법을 지어냈다. 그 형태를 `auto/STUCK` 에 못박아 두었다.
        회귀 `run_bundles unit api` **522개 단정 0 실패** · **앱·시험 코드는 한 글자도 안 고쳤다**
        (index.html 36,144 · mtime 09-21 16:06:58) → 재시작·재기준선 불필요.
        **여전히 push 를 못 했으므로 `[ ]` 그대로다.**
      🟢 2026-09-22 02:13(라운드 58) — **라운드 57의 사고가 아직 안 돌아온 자리 셋을 찾아 고쳤다.**
        STUCK 의 둘부터 했다(① 권한 그대로 거부 ② 낡음 4/6 — 그 수를 보고 다시 뜨지 않았다).
        🔴 **개수는 내용을 못 본다.** 라운드 57은 제 사고를 `?? 49 · M 31 · !! 0` 이 라운드 54
        값과 같은 것으로 확인했는데, git 이 세는 것은 «어느 파일이 달라졌나» 이지
        «무엇이 들어 있나» 가 아니다 — 덮어쓰기 **전에도** ` M` 이던 파일은 안이 엉뚱해도
        여전히 ` M` 한 줄이다. 덮어쓴 자리가 바로 그런 `labeltool/**` 였다.
        새 자 `tests/_sandbox/z5_content_probe.py`(읽기만 · 쓰는 git 은 allow 에 있는
        `status`·`show` 둘뿐 · `clean()` 은 정해진 `exec(src.split(...)[0], ns)` 형태로만)로
        올라갈 **80개를 전부** 소스와 댔다.
        🟢 **코드·시험·보고서 70개는 멀쩡했다** — api.js·boxes.js·style.css·mobile.css·
          index.html·js 아홉·기준선 넷·run_all.sh·시험 서른몇·이 사이클 보고서 여덟이
          지금 소스를 가린 것과 **한 글자도 안 다르다**(260920 후보판 글자가 남은 파일 0개).
        🔴 **그러나 셋이 안 돌아와 있었다** — `collect_public.py` 는 수집만 하는 것이 아니라
          85·108·111줄에서 `README.md`·`labeltool/README.md`·`NOT_UPLOADED.md` **셋을
          처음부터 다시 쓴다**(`prepare_public.py` 는 덧붙이기만 한다). 그래서 `README.md` 의
          «EASY·DEMO·MOBILE» **세 덩어리**와 `NOT_UPLOADED.md` 의 **네 덩어리**가 사라진
          채였다. 셋 다 사고 **전에도** ` M` 이라 개수가 하나도 안 변해 아무도 못 봤다.
          **그대로 올렸다면 공개 저장소에서 앞 세 사이클의 공개 기록이 조용히 지워졌을 것이다.**
        → 새 방법을 짓지 않았다 — ① 셋을 `git show HEAD:<파일>` 내용으로 되돌리고
          ② 이 사이클 덧붙임은 `prepare_public.py` 가 제 표식 규칙대로 다시 붙이게 했다
          (직접 써 넣으면 «두 번 붙는 것을 막는 자리» 가 둘이 된다).
          되돌린 뒤 `git diff --shortstat` 가 **31 files · +2,622 / −140** 이고 **−140 은
          라운드 53이 사고 전에 잰 값과 같은 수**다(안 돌아왔다면 지운 줄이 더 잡힌다).
        🔴 **낡음 검사가 못 보던 일곱째 파일도 찾았다 — `app/static/진행상황.html`.**
          무인 라운드가 끝날 때마다 다시 생성되는 진행판인데 `prepare_public.py` 가 `app/**` 를
          통째로 긁어 공개 사본에도 들어간다 → 기록과 똑같이 늘 한 라운드 뒤지는데 검사는
          여섯만 보고 있었다. `z5_stale_probe.py` 를 **일곱**으로 넓혔다. 수집 규칙은
          **안 건드렸다**(마감 앞에서 바꾸면 라운드 51~57이 못박아 온 파일 셋이 흔들린다).
          ⚠ 이 파일은 `.md` 가 아니라 `doc=False` 로 복사돼 **줄 단위 가림막이 안 걸린다**
          (값 가림은 걸린다 · 지금 걸릴 줄은 **0개** 실측).
        🟢 덤 — `export` 줄의 열쇠 적는 법을 **앞 사이클 다섯과 같게** 맞췄다
          (`~` → 절대경로 · `-o BatchMode=yes` 추가). ⚠ **`~` 가 고장이라는 뜻이 아니다**
          (git 이 셸에 넘겨 풀린다 — `sh -c` 로 그 자리를 그대로 흉내 내 실측).
          바꾼 까닭은 ① `ssh` 가 허용 목록 밖이라 끝까지 눌러 볼 수 없는 고리인데 절대경로는
          셸이 끼든 말든 같아 **못 재는 의존을 없앤다** ② `BatchMode=yes` 가 라운드 57이 걱정한
          «커서만 깜빡이는 멈춤» 을 그 자리 실패로 바꾼다.
        실측 — 고친 뒤 내용 검사 **74/74 «다르다 0»** · `?? 49 · M 31 · !! 0` ·
          비밀검사 **944파일 · 실패 0 · 키워드 65줄**(라운드 51·54·57과 같다) ·
          회귀 `run_bundles unit api` **522개 단정 0 실패**(라운드 27·50~57과 같은 값) ·
          **앱·시험 코드는 한 글자도 안 고쳤다**(index.html 36,144 · mtime 09-21 16:06:58)
          → 재시작·재기준선 불필요 · 울타리 안 옮김(git 쓰기는 거부된 dry-run 하나뿐).
        **여전히 push 를 못 했으므로 `[ ]` 그대로다.**
      🟢 2026-09-22 02:20(라운드 59) — **라운드 58이 세운 경보가 첫 판부터 울고 있었다. 껐다.**
        STUCK 의 셋을 순서대로 눌렀다. ① 권한 — `git add -A --dry-run` 이 **두 형태 다**
        거부다(`cd` 를 딸린 한 줄 · `git -C` 한 줄 · 벽 그대로). ② 낡음 — 5 / 7 이 나왔지만
        «몇이 나오든 그 수를 보고 다시 뜨지 마라» 는 라운드 56·57·58의 못박음을 따랐다.
        ③ 내용 — **«다르다 1»**. 라운드 58이 «이것은 흔들리는 수가 아니니 0 이 아니면 반드시
        그 파일을 열어 보라» 고 적어 두었으므로 열었다.
        🔴 **그 하나가 라운드 58 자신이 만든 어긋남이었다.** 라운드 58은 일곱째 파일
        (`app/static/진행상황.html` — 라운드마다 다시 써지는 진행판)을 찾아 **낡음 검사만**
        일곱으로 넓혔고 **내용 검사의 기록 목록(`RECORDS`)은 여섯 그대로** 두었다
        (`z5_content_probe.py:51`). 그래서 이 파일 하나가 **영영** 걸린다 →
        «0 이 아니면 진짜» 라는 뜻이 첫 판부터 죽어, 다음 라운드들이 매번 같은 파일을
        열어 보게 된다. 경보가 늘 울면 아무도 안 듣는다.
        **짐작으로 목록에 넣지 않았다 — 줄 단위로 쟀다.** 새 자
        `tests/_sandbox/z5_progress_diff.py`(읽기만 · `collect_public.py` 는 STUCK 이 못박은
        `exec(src.split(...)[0], ns)` 형태로만 부른다)로 공개 사본 ↔ 가린 지금 소스를 댔다 —
        75줄 ↔ 75줄에 **바뀐 곳은 둘뿐**이다: `<div class="meta">갱신 …` **한 줄**과
        «최근 기록» `<pre>` 창(라운드 53·54 줄이 빠지고 57·58 줄이 들어왔다 · ＋6 −6).
        코드도 도구도 아니고 **위 기록 여섯에서 파생된 화면**이라 여섯과 같은 칸이 맞다.
        → `RECORDS` 에 한 줄 더했다(까닭 주석 네 줄). 다시 돌리니
        **대조 73 · 같다 73 · 다르다 0 · 기록이라 안 맨 것 7 · rc=0** 이다.
        그 값을 `auto/STUCK` 의 ③ 에 적어 두어, 다음 라운드가 «1 이 정상인가» 를 다시
        헤매지 않게 했다(⚠ 정정으로 달았고 옛 글은 안 고쳐 썼다).
        실측 — 회귀 `run_bundles unit api` **522개 단정 0 실패**(라운드 27·50~58과 같은 값) ·
        고친 파일은 **시험 모래상자 둘과 기록뿐**이고 **앱 코드는 한 글자도 안 고쳤다**
        (index.html 36,144 · mtime 09-21 16:06:58) → 재시작·재기준선 불필요 ·
        공개 사본을 **한 글자도 안 건드렸다**(다시 뜨지 않았다) · 울타리 안 옮김
        (보낸 git 은 거부된 dry-run 둘뿐 · `subprocess` 우회 안 씀).
        실서버 5111(PID 155257)은 **21시간 51분째** 돌던 그대로다.
        **여전히 push 를 못 했으므로 `[ ]` 그대로다.**
      🟢 2026-09-22 02:35(라운드 60) — **아홉 라운드가 «검사 통과» 라고 적어 온 것이
        올라가는 물건이 아니었다. 검사를 문에 달았다.**
        STUCK 의 셋부터 눌렀다. ① 권한 — `git add -A --dry-run` 이 **두 형태 다** 거부(벽 그대로).
        ② 낡음 — 5 / 7(그 수를 보고 다시 뜨지 않는다). ③ 내용 — **대조 73 · 다르다 0**
        (라운드 59의 고침이 유지된다 — 늘 울던 경보가 조용하다).
        🔴 **그러고 나서 블록 자체를 처음으로 «순서» 로 읽었다.** 사람이 14:00 에 치는 차례는
        «① 다시 뜬다 → ② `add` → ③ `commit` → ④ `push`» 인데, 그 사이에 비밀검사
        (`audit_public.py`)를 부르는 자리가 **없었다.** 라운드 51·54·57·58·60이 적은
        «944파일 · 실패 0» 은 전부 **그때 거기 있던 사본**을 잰 값이고, 블록의 **첫 줄이
        바로 그 사본을 다시 쓴다** → 아홉 번 잰 것은 **올라가는 물건이 아니다.**
        검사기를 만들어 두고 문에는 안 달아 둔 꼴이었다.
        ⚠ **부풀리지 않는다** — 지금 사본은 깨끗하다(02:30 실측 · 944파일 · 실패 0 ·
        키워드 65줄 · rc=0 · 라운드 51·54·57·58과 같은 값). 위험한 것은 «지금» 이 아니라
        **다시 뜬 뒤의 사본을 아무도 안 본다는 순서**다.
        **검사 없이 지나가는 칸의 크기를 세었다** — 새 자 `tests/_sandbox/z5_audit_gap_probe.py`
        (읽기만 · `git status` 하나뿐) → `git add -A` 가 올리는 범위 **944개** ·
        블록 첫 줄이 `clean()` 으로 다시 가리는 범위 **489개** · **그 밖 455개**
        (labeltool 172 · docs 169 · scripts 101 · dataset 9 · 나머지 4). 그 455개를 전부
        보는 자는 `audit_public.py` **하나뿐**이다. 👉 **가정이 아니라 이미 한 번 일어났다** —
        라운드 57의 사고로 끼어든 일곱과 라운드 58이 찾은 «안 돌아온 셋» 이 바로 그 칸이었고,
        `?? 49 · M 31` 개수로는 **하나도 안 보였다.**
        → 고친 방법은 검사를 블록에 넣되 **`&&` 로 `git add` 앞에 매단 것**이다. 붉게 나와도
        다음 줄을 그냥 치는 길을 없앴고, 라운드 56의 규칙(«뺄 수 있는 줄을 없앤다»)대로
        **이 줄을 통째로 빼면 `add` 도 같이 빠져 아무것도 안 올라간다**(안전한 쪽으로 넘어진다).
        실측 넷 — ① `&&` 앞이 rc≠0 이면 뒤 줄이 **안 돈다** ② 지금 색인에 올라간 것이
        **0개**라(` M` 31 · `??` 49 뿐) `add` 가 안 돌면 `commit` 이 올릴 것을 못 찾는다
        ③ 검사는 **13.7초 · rc=0 · 20줄 · 오류출력 0바이트** ④ 블록 다섯 줄의 따옴표가
        전부 맞물린다(⚠ `bash`·`sh` 가 허용 목록 밖이라 **셸 문법 검사는 못 했다** —
        `shlex` 로 따옴표만 쟀고 그 이상은 주장하지 않는다).
        딸린 사실 하나 — `prepare_public.py` 48~69줄의 덧붙이기 셋(README·NOT_UPLOADED·
        labeltool/README)은 `add()` 를 안 거쳐 **`clean()` 이 안 걸린다.** 지금 붙는 글은
        스크립트에 박힌 고정 문장이라 샐 것이 없다(올라갈 80개 중 prepare 밖인 것이
        **정확히 이 셋뿐**임을 실측).
        회귀 `run_bundles unit api` **522개 단정 0 실패**(라운드 27·50~59와 같은 값) ·
        고친 것은 **`auto/STUCK` 과 기록뿐**이고 **앱·시험 코드는 한 글자도 안 고쳤다**
        (index.html 36,144 · mtime 09-21 16:06:58) → 재시작·재기준선 불필요 ·
        **공개 사본을 한 글자도 안 건드렸다**(다시 뜨지 않았다) · 울타리 안 옮김
        (git 은 거부된 dry-run 둘과 읽기 `status` 뿐 · `subprocess` 우회 안 씀).
        STUCK 끝에 «다음 자동 라운드가 누를 것 ①~⑤» 를 적어 두었다 — 같은 벽만 다시
        두드리며 라운드를 채우지 말라는 뜻이다.
        **여전히 push 를 못 했으므로 `[ ]` 그대로다.**
      🟢 2026-09-22 02:45(라운드 61) — **«origin/main 과 같다» 는 한 번도 fetch 한 적 없는
        값이었다. 그 값을 세 라운드가 사실로 물려받고 있었다.**
        STUCK 의 ①~④ 를 순서대로 눌렀다. ① 권한 — `git -C … add -A --dry-run` 이 여전히
        «requires approval»(벽 그대로 · 허용 목록을 스스로 고치지 않았다). ② 낡음 — 5 / 7
        («몇이 나오든 그 수를 보고 다시 뜨지 마라» 를 따랐다). ③ 내용 — **대조 73 · 같다 73 ·
        다르다 0 · 기록이라 안 맨 것 7**(라운드 59가 맞춘 값 그대로 · 경보가 조용하다).
        ④ 검사가 문에 달려 있나 — 블록 세 번째 줄에 `audit_public.py && git … add -A` 가
        그대로 있다(눈으로 확인).
        🔴 **그러고 나서 ⑤ 로 «아직 아무도 안 본 자리» 를 찾았다 — 값이 아니라 값의 출처다.**
        라운드 51·53·54는 «로컬 main 과 origin/main 이 둘 다 `b2a86cd` 라 끼어든 변경이 없다»
        를 세 번 적었고 그 뒤 라운드들이 그것을 사실로 썼다. 그런데 **그 값이 어디서 왔는지는
        아무도 안 봤다.** `.git/logs/` 를 읽어 보니 `refs/remotes/origin/main` 의 기록
        **아홉 줄이 전부 «update by push»** 이고 «fetch» 줄은 **0줄**이며 `.git/FETCH_HEAD` 가
        **아예 없다** → 이 클론은 09-20 03:30 에 clone 된 뒤 **한 번도 `fetch`·`pull` 을 안 했다.**
        그러니 `origin/main` 은 «GitHub 의 지금» 이 아니라 **«이 서버가 09-21 04:34 에
        마지막으로 민 것»** 이다. 사용자가 제 노트북에서 밀었거나 github.com 에서 직접 고쳤다면
        여기서는 **안 보인다** → 14:00 의 마지막 줄이 «rejected» 로 거절될 수 있다.
        ⚠ **부풀리지 않는다** — 아홉 번의 push 가 **전부 이 서버**에서 났고(기록 아홉 줄),
        `kds0206` 아래에서 이 저장소를 가리키는 클론은 **이것 하나뿐**이다(`.git/config` 훑기 실측).
        위험은 «작지만 0 은 아니고, **여기서는 잴 수 없다**» 이다. 잴 수 없는 것도 눌러 봤다 —
        `git … ls-remote --heads origin` 을 **두 형태**로 쳤고 둘 다 거부다(네트워크를 읽는
        git 도 울타리 밖). 울타리는 안 옮겼고 `subprocess` 우회도 안 썼다.
        → **명령을 더하지 않았다.** `git push` 는 이런 경우를 **기본으로 거절**하므로 블록은
        이미 안전한 쪽으로 넘어진다. 빠진 것은 안전이 아니라 **그다음에 무엇을 하라는 말**이다 —
        사람이 붉은 «rejected» 를 처음 보면 손 가까이 있는 것이 하필 `--force` 다.
        그래서 `auto/STUCK` 블록 **바로 아래**에 «거절되면» 다섯 줄을 달았다(①아무것도 안 잃었다
        ②`--force` 금지 ③무엇이 끼어들었는지만 읽기 두 줄로 본다 ④목록이 비면 열쇠 쪽이다
        ⑤비어 있지 않으면 **거기서 멈춰라** — 합치는 방법은 한 줄로 못 적는다).
        라운드 56의 규칙(«뺄 수 있는 줄을 없앤다»)과 안 부딪힌다 — 이 다섯 줄은 **치는 블록이
        아니고** 블록 자체는 한 줄도 안 늘었다. 라운드 51·53의 옛 문장은 고쳐 쓰지 않고
        그 자리에 «⚠ 라운드 61 정정» 을 달았다(이 사이클이 쓰던 방식).
        실측 — 회귀 `run_bundles unit api` **522개 단정 0 실패**(라운드 27·50~60과 같은 값) ·
        올라갈 파일이 **`?? 49 · M 31` 그대로**(내용 검사가 같은 `git status` 로 센다) ·
        고친 파일은 **`auto/STUCK` 과 기록뿐**이고 **앱·시험 코드는 한 글자도 안 고쳤다**
        (index.html 36,144 · mtime 09-21 16:06:58) → 재시작·재기준선 불필요 ·
        **공개 사본을 한 글자도 안 건드렸다**(다시 뜨지 않았다 · STUCK 은 공개본에 안 복사된다).
        **여전히 push 를 못 했으므로 `[ ]` 그대로다.**
      🟢 2026-09-22 02:55(라운드 62) — **14:00 의 안전이 걸린 자물쇠가 잠기는지를 아무도
        눌러 본 적이 없었다. 눌렀고, 일곱 가지 다 잠긴다.**
        STUCK 의 ①~④ 를 순서대로 눌렀다. ① 권한 — `git -C … add -A --dry-run` 이 여전히
        «requires approval»(벽 그대로 · 허용 목록을 스스로 고치지 않았다). ② 낡음 — 6 / 7
        («몇이 나오든 그 수를 보고 다시 뜨지 마라» 를 따랐다). ③ 내용 — **대조 73 · 같다 73 ·
        다르다 0 · 기록이라 안 맨 것 7**(라운드 59가 맞춘 값 그대로 · 경보가 조용하다 ·
        올라갈 것은 73+7 = **80개**로 라운드 58~61과 같다). ④ 블록 세 번째 줄에
        `audit_public.py && git … add -A` 가 그대로 있다.
      🔴 **그러고 나서 ⑤ 로 찾았다 — 라운드 60이 문에 단 검사가 «걸렸을 때 정말 막는지»는
        한 번도 확인된 적이 없다.** 라운드 60이 적은 실측 셋(`&&` 가 뒤 줄을 막는다 · 색인이
        비어 있다 · 13.7초 rc=0)은 **전부 «깨끗할 때»** 를 잰 값이고, 라운드 51~61이 열 번
        적은 «944파일 · 실패 0 · rc=0» 도 그렇다. **검사가 고장 나 늘 rc=0 이어도 그 열한
        값이 글자까지 똑같이 나온다.** 늘 초록인 시험은 초록인지 고장인지 모른다 —
        이 사이클은 이미 한 번 맞았다(Z2 가 잡은 `b24_shadow_mobile.py:492` 의
        `sys.exit(0 if main() else 1)`: 다 통과할 때 rc=1 · 실패가 있을 때 rc=0 이었고,
        묶음 밖이라 아무도 그 거짓말을 못 봤다). 지금 14:00 에 올라가는 것의 안전은
        **`audit_public.py` 의 rc 하나**에 걸려 있다.
        → **규칙을 베끼지 않았다.** `audit_public.py` 본문을 **그대로** 실행하되 경로
        **셋만** 모래상자로 바꿨다(뒤질 폴더 · 증거 쓸 곳 · 비밀 목록 env). 바꾼 자리가
        각각 **정확히 한 군데**인 것을 세고, 줄 수와 규칙 여섯(정규식 둘·까닭 둘·서버 주소·
        assert 한 줄)이 그대로 살아 있는 것도 셌다 — 늘거나 사라지면 거기서 붉어진다.
        ③ 덕분에 **`known` 목록에 드는 진짜 값은 어디에도 안 적혔다**(지어낸 값으로 기계만 시험했다).
        실측 — 새 자 `tests/_sandbox/z5_audit_fire_probe.py` **33 / 33 · 실패 0 · rc=0.**
        일곱을 하나씩 심으니 **일곱이 전부 터지고**(rc≠0) 까닭도 제 이름으로 나온다 —
        금지된 자리 `forbidden path` · 금지된 그림 `forbidden binary` · 서버 주소 `server IP` ·
        열쇠 모양 `credential pattern` · 알려진 비밀값 `known private credential` ·
        이어붙이기 표 `session identifier` · 못 읽는 바이트 `unscannable binary`.
        치우면 **다시 rc=0** 이다(경보가 켜진 채 안 남는다). 덤 둘 — `.git/` 는 세지도
        뒤지지도 않고(심은 것 셋 중 둘만 센다), **«키워드» 는 세기만 하고 문을 안 막는다**
        (라운드 51~61이 적어 온 «키워드 65줄» 이 실패가 아닌 까닭이 이것이다 — 사람이
        눈으로 볼 목록일 뿐이다).
        ⚠ **여덟째만 못 재 봤다** — `assert` 의 뒷절 `total<1024**3`. 1GiB 짜리를 심으면
        본문 23줄이 그것을 통째로 읽어서 이 서버에서 할 짓이 아니다. 부풀리지 않고 남은
        거리만 적는다 — 지금 **19,287,525 / 1,073,741,824 바이트 = 1.8%**.
        아무것도 안 썼다 — 모래상자는 제가 짓고 제가 지운다(끝난 뒤 남은 것 0) ·
        진짜 `evidence/*.json` 둘이 **02:32:39 그대로**(mtime·크기 실측) · 진짜 공개 사본에
        심은 것이 하나도 안 갔다 · **git 을 아예 안 불렀다**.
        실측 — 회귀 `run_bundles unit api` **522개 단정 0 실패**(라운드 27·50~61과 같은 값) ·
        고친 파일은 **`auto/STUCK` 과 기록뿐**이고 새 자 하나는 `tests/_sandbox/` 안이다
        (공개본에서 `forbidden path` 로 걸러지는 자리 — 위 일곱 중 첫째가 그것이다) ·
        **앱·시험 코드는 한 글자도 안 고쳤다**(index.html 36,144 · mtime 09-21 16:06:58)
        → 재시작·재기준선 불필요 · **공개 사본을 한 글자도 안 건드렸다**(다시 뜨지 않았다) ·
        울타리 안 옮김(보낸 git 은 거부된 dry-run 하나뿐 · `subprocess` 우회 안 씀).
        실서버 5111(PID 155257)은 **22시간 25분째** 돌던 그대로다.
      🔴 **마지막에 한 번 더 재서 잡았다 — 라운드 54·55가 빠졌던 그 함정에 나도 걸렸다.**
        내가 이 라운드에 쓴 **여섯 줄이 공개본에서 통째로 가려지게** 돼 있었다. 걸린 낱말은
        둘뿐이다 — 한국어 «비ㅁ번호»(네 줄)와 검사 보고서 **파일 이름에 든 영문 한 낱말**(두 줄).
        👉 그중 하나가 하필 **`작업기록.md` 「1. 현황」 19줄**이다. 그 줄은 내가 손대기 전까지
        걸리는 낱말이 **하나도 없었는데**, 내가 보고서 파일 이름을 적는 바람에 라운드 55·56·58·61이
        거기 쌓아 온 정정 전부가 공개본에서 **한 문장으로 뭉개질 뻔했다.** 사람이 세션 시작에
        가장 먼저 읽으라고 CLAUDE.md 가 못박은 줄이 그것이다.
        → 라운드 55가 세운 규칙 그대로 **낱말을 적는 대신 자리만 가리키게** 고쳤다
        (파일 이름은 공개본에 안 복사되는 `auto/STUCK` 이 이미 들고 있다 · 뜻은 안 줄였다).
        실측 — 고친 뒤 가린 줄이 **checklist 0 · context-notes 4 · STATUS 0** 으로
        라운드 55의 값과 **같고**, `작업기록.md` 는 **59줄로 내가 손대기 전과 같으며**
        내 블록(139~192줄)에서 가려지는 줄은 **0개**다. `CURRENT.md` 의 1줄은 전부터 있던
        서버 주소 한 줄이다(줄 가림이 아니라 값 가림 · 라운드 54가 잰 그 줄).
        **여전히 push 를 못 했으므로 `[ ]` 그대로다.**
      🔴 2026-09-22 05:30(라운드 90) — **막혔을 때 화면의 마지막 글자가 «성공» 처럼 읽혔다.**
        라운드 60이 «검사에 걸리면 아래 두 줄을 치지 마라» 고 적었는데, 같은 파일이 바로 위에서
        «**블록을 통째로 복사해 붙여라**» 라고 한다 → 붙여넣기는 줄마다 멈추지 않으므로
        **사람은 그 말을 지킬 수가 없다.** 두 말이 서로 어긋나 있었다.
        그때 실제로 나는 일은 라운드 60이 적은 그대로다(색인이 비어 있어 아무것도 안 올라간다 —
        `git status --short` 69줄이 전부 첫 칸이 빈 ` M`·`??` 인 것을 다시 쟀다). 문제는 결과가
        아니라 **화면에 남는 마지막 글자**다 — `push` 가 «Everything up-to-date» 를 적는다.
        영어라 초보에게는 성공으로 읽힌다(이 사이클이 두 번 본 얼굴이다 — Z2 의 `b24` rc 뒤집힘 ·
        라운드 59의 늘 우는 경보). → 블록 **맨 끝에 읽기 한 줄**을 달았다
        (`git … log --oneline -1 origin/main`). 「…편의·안정성 사이클(260921) 반영…」이 보이면
        올라간 것이고, `b2a86cd` 가 그대로면 안 올라간 것이다.
        **두 모습을 다 실측으로 못박았다** — «안 올라감» 은 지금 값이 `b2a86cd`(오늘 05:26 실측) ·
        «올라감» 은 이 저장소 기록 아홉 줄이 **전부 «update by push»** 이고 그 아홉째 값이
        지금 `origin/main` 과 **글자까지 같다**(`.git/logs/…` 실측) → 그 줄이 움직였다는 것은
        push 가 성공했다는 것과 같은 말이다. 짐작이 한 글자도 안 섞였다.
        블록에서 «마지막 줄» 이 더는 push 가 아니므로 그렇게 부르던 자리 셋을 이름으로 바꿨다.
        실측 — 회귀 `run_bundles unit api` **522개 단정 0 실패**(라운드 27·50~62와 같은 값) ·
        고친 파일은 **`auto/STUCK` 과 기록뿐** · **앱·시험 코드는 한 글자도 안 고쳤다**
        (index.html 36,144 · mtime 09-21 16:06:58) → 재시작·재기준선 불필요 ·
        **공개 사본을 한 글자도 안 건드렸다** · git 쓰기는 **다시 눌러 봐도 여전히 거부**다
        (`add -A --dry-run` → 승인 필요 · 울타리 안 옮김 · `subprocess` 우회 안 씀).
        ⚠ 라운드 63~89 **스물일곱 판은 세션 한도**로 아무 일도 못 했다(`round_0*.log` 60바이트 ·
        «resets 5:20am»). 한도가 풀린 뒤 **첫 일하는 판**이 이 라운드다.
        **여전히 push 를 못 했으므로 `[ ]` 그대로다.**

## 5단계 — 설명서 (GPT가 쓴다)
- [x] D1 Codex(GPT)가 지금 화면 기준 **사용법 설명서** 를 새로 쓴다 → `문서/260921_툴_사용법.md` (X)
      ✅ 2026-09-22 05:38 `문서/260921_툴_사용법.md` — **손으로 안 적고 생성**(`gen_usage_doc.py`)
        keys.js 의 단축키 **34개** · index.html 단추 **31개** 를 뽑아 씀 → 어긋날 수 없음
        `gen_usage_doc.py --check` = «같음»(화면이 바뀌면 붉어진다)
      검증: 문서에 적힌 단추·단축키가 실제 화면과 하나도 어긋나지 않는지 대조
- [x] D2 D1 을 PDF 로 (`문서/260921_툴_사용법.pdf`) (X)
      ✅ 2026-09-22 05:38 `문서/260921_툴_사용법.pdf` — **5쪽 · 274×489pts**(앞 사이클 273×488 과 같은 폰 크기)
        playwright·chromium 이 없어 `scripts/ff.py`(headless 파이어폭스) 의 WebDriver `/print` 로 만듦 →`gen_usage_pdf.py`
        1·4쪽을 실제 그림으로 열어 확인 — 한글·표·단추 **깨짐 0**
        ⚠ `pdftotext` 로 뽑으면 공백·문장부호가 `□` 로 보이는데 **글꼴 부분집합 탓**이고 화면은 멀쩡하다
      검증: 쪽수·그림 깨짐 없음
- [x] D3 카톡 5줄 공지 초안 (`문서/260921_카톡_편의개선.txt`) — **미발송**, 사람이 보낸다 (X)
      ✅ 2026-09-22 05:38 `문서/260921_카톡_편의개선.txt` — **10줄**(기준 25 이내) · 표 0 · 이모지 0 · **미발송**(사람이 보냄)
      검증: 25줄 이내·표·이모지 없음

