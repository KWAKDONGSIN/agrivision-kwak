# 계획 — 라벨링 툴 «사용자 편의 + 안정성» 올리기
작성: 2026-09-21

## 사용자 요청 원문 요약 (2026-09-21 10:45)
- 그림판·윈도우 프로그램·포토샵처럼 **사용자가 편하게 쓸 수 있는 UI 관습을 참고**해서 더 편하게 만들어라.
- **안정성**도 함께 올려라.
- **설명서는 GPT(Codex)가** 만들어라.
- 내가 **컴퓨터를 꺼도 계속 돌아가게** 해라. Opus 주간 한도를 다 써도 된다.
- 마감은 **2026-09-22(화) 14:00 KST**.
- Codex는 남은 절반 중 20%만 쓰고 30%는 남긴다. DeepSeek 남은 토큰과 Gemini 무료 한도는 다 써도 된다.
- 내가 받은 스킬을 최대한 활용해 가장 효율적으로 해라.

## 무엇을 하지 않는가
- 화면을 새로 만들지 않는다. 지금 구조(위 줄·왼쪽 도구상자·가운데 캔버스·아래 판정 줄) 위에 얹는다.
- 요소 id·기존 단축키·서버 API 규칙을 바꾸지 않는다 (회귀 1,199개 + HTML 기준선이 걸려 있다).
- 새 라이브러리·CDN·빌드 도구를 넣지 않는다. 순수 JS/CSS.
- 쉬움 모드를 더 복잡하게 만들지 않는다. 새 기능은 기본적으로 `adv`(전문가)나 «있어도 방해 안 되는» 것만.
- 서버 파이썬 파일은 되도록 건드리지 않는다 → 정적 파일만 고치면 **실서버 재시작이 필요 없다**(Cache-Control no-cache).

## 근거로 삼은 것
- 내가 직접 읽고 확인한 코드 빈틈 (아래 «확인된 빈틈»)
- Gemini 자문 `council/gemini.md`, Codex 자문 `council/codex.md`, DeepSeek 자문 `deepseek.md`
- 이미 있는 것과 겹치는 제안은 버린다. 예 — 붓 원형 커서·휠 확대·스페이스 이동·풍선말·단축키 배지는 이미 있다.

## 확인된 빈틈 (2026-09-21 내가 코드에서 직접 확인)
| 빈틈 | 확인한 자리 |
|---|---|
| `fetch` 실패(서버 재시작·와이파이 끊김)를 잡는 `.catch` 가 없다 → 클릭이 조용히 씹힌다 | `app/static/js/api.js` |
| 실패 알림이 3초 뒤 사라진다 → 저장 실패를 못 보고 넘어간다 | `api.js` `flash()` |
| 서버가 살아 있는지 알 방법이 화면에 없다 | — (`/api/health` 는 이미 있다) |
| 상자 작업에 «다시하기» 가 없다 (마스크·번호에는 있다) | `js/keys.js:74` «상자에는 다시 실행이 없습니다» |
| 배율 %가 `#hud` 에만 있고 **쉬움 모드에서는 숨는다** | `style.css:404` `body.easy #hud{display:none}` |
| 붓 크기가 붓·지우개 공용 한 칸이다 (포토샵은 도구마다 기억) | `index.html` `#brush` |
| 브라우저가 죽으면 저장 전 작업이 사라진다 (임시 백업 없음) | — |

## 단계
1. **안정성 6가지**(S1~S6) — 데이터를 잃거나 조용히 실패하는 길을 막는다.
2. **편의 10가지**(U1~U10) — 그림판·포토샵·윈도우 관습 중 아직 없는 것.
3. **설명서** — Codex(GPT)가 지금 화면 기준으로 새로 쓰고 PDF까지. 카톡 5줄은 사람이 보내기 전까지 미발송.
4. 각 항목마다 `tests/run_all.sh` 통과 + HTML 기준선 대조. UI 항목은 `--browser` 까지.

## 사람이 꼭 확인할 것
- 실제 휴대폰으로 한 바퀴 (지금까지 360/390 폭 흉내내기뿐)
- 카톡 공지 발송 여부
