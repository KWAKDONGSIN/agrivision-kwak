작성: 2026-09-17

# 0917 새 기능 5회 검수 — 사이클 2 · 3차 최종 판정 (Fable 5.1)

주제: **사람이 쓸 때 막히는 곳**(상자) + **번호 편집 네 동작·되돌리기**(열매 번호)
읽은 것: 다섯 갈래 1차(`stage1_work{,_boxes,_server,_revert,_client}.md`) · 2차 `stage2_review.md` · `cycle_1/stage3_final.md` · 계획서 · 참고저장소 적용기록 · `~/.claude/skills/ponytail-review/SKILL.md`
코드는 **읽기만** 했고 한 줄도 고치지 않았습니다(`app/boxes.py`·`instances.py`·`server.py` 일부·`static/app.js`·`index.html`·`export/export_dataset.py`·`README.md`).
근거 표기: ✅ 논문 근거(이번엔 없음) · 📄 코드·파일·실행 실측(경로 명시) · ❌ AI 판단

내가 직접 다시 돌린 것 📄 (전부 종료코드 0)
```
node --check app/static/app.js                                   → OK
python3 app/boxes.py                                             → 자체 점검 통과(실제 clean())
python cycles/260917_신기능/cycle_1/stage1/test_clean.py          → 「5개(2픽셀 미만 2개 포함) → n_boxes 3 · 응답 boxes 길이 3」 통과 (2차가 넣은 검사)
python -m py_compile app/{instances,boxes,server}.py export/export_dataset.py → OK
블루베리 초벌 1,195장 전수(내 스크립트): 장당 번호 min 1 · 중간 27 · 평균 37.8 · 최대 204 · 300 초과 0장 · **번호 총합 45,124**
INSTCORE 를 떼어 내 새 번호 규칙 대조(아래 ③ R2): 55 지운 뒤 화면 경로(doAdd) 새 번호 56 · 순수 함수 경로 2
초벌 상자 수 대조(아래 ⑦): 사과 image1 번호 95 ↔ GT 연결성분 74 ↔ AI 제안 연결성분 30
서버: PID 2911132 · 14:48:50 기동 · /health 302(로그인 게이트) · 여섯 파일 mtime 전부 기동 전 → 2차 N4 그대로
```

---

## ① 한 줄 결론

다섯 갈래의 구현은 2차가 전부 재현했고 나도 핵심 검사를 다시 돌려 **고칠 버그는 없습니다**. 갈린 것은 «말» 뿐이며 ③에서 정리합니다 — 2차의 반박 2건은 **둘 다 절반만 맞습니다**(45,124 는 실재하는 «총합» 이고, 빈 번호 재사용은 «저장 뒤 다시 열 때만»). 사이클 1 판정서의 모순은 **«D1 결정 전 손대지 마라» 쪽으로 확정**합니다. 남은 위험은 브라우저 눈 확인(④)과, 사이클 3 주제에서 이미 드러난 **초벌 상자가 번호 마스크를 안 쓴다는 것**(⑦)입니다.

## ② 사이클 1 판정서의 모순 — 확정

`cycle_1/stage3_final.md` 는 `boxes.py` 의 `CLASSES.index(b.get("cls","fruit")) if b.get("cls") in CLASSES else 0` 한 식을 ⑤(ponytail)·⑥-4 에서 «깎아라», ④ D1·맨 끝 줄에서 «D1 결정 전엔 손대지 마라» 로 적었습니다.

**확정: «손대지 마라» 가 맞습니다. ⑤ 의 그 항목과 ⑥-4 의 «L157» 은 취소합니다.** 나 갈래가 금지 쪽을 따른 것이 옳습니다.
- 📄 `app/boxes.py:L41` `clean()` 이 `cls∈CLASSES` 를 보장하고, `api_boxes_export`(L157-161) 가 읽는 JSON 은 `api_boxes_save`(L95) 가 `clean()` 을 거쳐 쓴 것뿐 → 두 꼴은 **동작이 같고** 깎아서 얻는 것은 반 줄입니다.
- ❌ D1 이 (다)«내보낼 때 대응표» 로 결정되면 그 자리가 `MAP[fruit][cls]` 가 되어 어차피 다시 씁니다. 두 번 고칠 이유가 없습니다.
- 사이클 4(내보내기) 주제라 그때 D1 결정과 함께 한 번에 처리합니다. 지금 줄 번호는 **L160**(나 갈래가 `clean()` 을 올리면서 3줄 밀림).

## ③ 갈린 항목 판정 (1차 갈래 ↔ 2차, 그리고 2차가 넘긴 10건)

### ③-1 2차의 반박 2건

| | 갈래 주장 | 2차 반박 | **판정** | 근거 |
|---|---|---|---|---|
| R1 블루베리 규모 | 가 ③「블루베리(장당 300개 넘음)」, ⑤「번호 45,124개 규모에서 화면이 느려지지 않는지」 | 「최대 204, 300 넘는 사진 0장. 『번호 45,124개』 같은 규모는 없다」 | **2차가 맞되, 마지막 문장은 틀림.** 장당 300 은 거짓(📄 최대 204). 그러나 **45,124 는 1,195장 번호의 총합으로 실재**합니다(📄 내 전수 재측정 `total ids 45124`). 가 갈래가 «총합» 을 «한 장» 처럼 쓴 것이 오류이고, 2차는 그 숫자가 어디서 왔는지 확인하지 않고 «없다» 고 적었습니다 | 「장당 300개」의 **출처는 박성문 지시서 §2 L44**(`work/park_seongmoon/260917_라벨링툴_열매번호편집_작업지시서.md`: «블루베리는 장당 최대 300개를 넘어 uint8 로 부족할 수 있음»)입니다. 가 갈래는 그것을 옮겨 적은 것. 16비트를 쓰는 근거는 2차 말대로 «`/instances` 가 bitdepth 16 으로 준다」(📄 `instances.py:L37-41 png_u16_bytes`)와, 덧붙여 «나누기·새로 붙이기가 번호를 최대+1 로 늘려 세션이 쌓이면 255 를 넘을 수 있다» 입니다 |
| R2 새 번호 규칙 | 가 가정 5「항상 그 사진의 최대+1, 빈 번호를 다시 쓰지 않는다」 | 「최대 번호를 지우면 새 번호가 그 번호를 재사용한다」(`bbsim.js` §D) | **둘 다 부정확. 정확한 문장: «같은 편집 세션 안에서는 재사용하지 않고, 저장한 뒤 다시 열면 재사용할 수 있다».** 2차의 §D 는 순수 함수에 `instStats().maxId+1` 을 직접 넣어 본 것이고(📄 `bbsim.js:L113-119`), 실제 화면은 `S.instMax` 를 씁니다: 📄 `app.js:L1480` 불러올 때 `S.instMax = st.maxId`, `L1507 repaintNum` 은 **커질 때만** 갱신, `L1614 runNumOp` 도 커질 때만, `L1636 doSplit`·`L1668 doAdd` 가 `S.instMax(+1)` 을 넘김 → 55 를 지워도 `S.instMax` 는 55 로 남아 다음 새 번호는 **56**. 📄 내가 INSTCORE 로 재현: 화면 경로 56 · 순수 경로 2. 저장 뒤 다시 열면 `loadInstances` 가 파일의 실제 최대로 초기화하므로 그때는 빈 번호가 다시 쓰입니다 | 겹침은 어느 경로에서도 없습니다(2차 확인). **결정: 코드는 그대로**(2차 ⑥-2 의 (가)). README §11 에 위 «정확한 문장» 한 줄. 가 갈래 보고서 L165 와 2차 ⑤-R2·⑥-2, 계획서 L145 의 문장은 아래 ③-3 대로 정정 |

### ③-2 2차가 넘긴 10건

| # | 내용 | **판정** | 근거·지시 |
|---|---|---|---|
| 1 | 사람이 크롬에서 눈으로 볼 것 | **사람 결정(확인)** → ④ 체크리스트 | 1차 다섯 갈래·2차·나 모두 브라우저가 없었습니다. 함수 단위 검사 149개가 통과했어도 캔버스·마우스는 아무도 못 봤습니다 |
| 2 | 지운 번호 재사용 — (가) 그대로 + README 한 줄 / (나) 최대값 기억 | **해결 — (가).** 단 문장은 ③-1 R2 의 «정확한 문장» 으로 | ❌ (나)는 이미 세션 안에서는 `S.instMax` 로 되어 있고, 저장을 넘어 기억하려면 파일 형식이 늘어납니다. 지시서 §2 «1부터 연속일 필요 없음» 이라 겹침만 없으면 됩니다 |
| 3 | `Ctrl+Y` 가 상자 모드에서 마스크 «다시 실행» 으로 감 | **미해결(사이클 3, 1줄)** | 📄 `app.js:L774` `if (S.numMode) numRedo(); else $("#redo").click();`. 상자에는 다시하기가 없는데 상자 모드에서 마스크 되돌리기 스택을 건드리는 것은 사람이 놀랄 자리입니다. **고칠 것**: `L774` 를 `if (S.numMode) numRedo(); else if (S.boxMode) flash("상자에는 다시 실행이 없습니다", true); else $("#redo").click();`. `modesim.js` 에 «상자 모드 Ctrl+Y → `#redo` 클릭 0회» 한 줄 |
| 4 | 번호 편집 모드에서 판정 키 `1`·`3`·`4` 가 먹음 | **해결(그대로)** | 📄 `app.js:L717-723` `doAction` 이 `ok/flag/exclude` 면 `confirmLeave()` 를 먼저 부르고, `L485` 가 `S.numDirty` 를 물어봅니다 → 저장 안 한 번호 편집이 말없이 사라지지 않습니다. 잠그면 «번호만 보고 판정» 흐름이 막힙니다(2차 판단 동의) |
| 5 | 다 갈래의 의도한 변경 — `layer=gt` 가 «고친 번호 있음 + 원본 이진» 에서 404 | **해결(새 쪽이 맞음).** API 가 화면이 못 하는 일을 하게 둘지는 **사이클 5** | 📄 `instances.py:L77-90 source_of` 가 «0/255 는 번호가 아니다» 를 한 군데서 판정. 지금 데이터로 도달 불가(네 과일 `instances_fixed/` 0개) |
| 6 | 나 갈래가 안 깎은 `boxes.py` 클래스 식 | **사람 결정(D1) 대기 — ② 대로 손대지 않음** | ② 참조 |
| 7 | 다 갈래 가정 — `instances.py` 가 `DUP.DATA_DIR` 을 믿음 · `export_dataset.py` 가 flask 를 간접 필요 | **해결(지금은 문제 없음) → 정리는 사이클 5** | 📄 `server.py:L32-34` `ROOT=dirname(APP_DIR)`, `DATA_DIR=join(ROOT,"data")` == `dupes.py:L11-13` 같은 식. `instances.py:L60-61` 에 `ponytail:` 주석으로 전제가 적혀 있음. flask 없는 파이썬에서 내보내기를 돌릴 계획이 없으므로 사이클 5 에서 `maskio.py` 이동 여부만 결정 |
| 8 | 마 갈래의 덤 1줄(`#boxcls` 종류가 정말 바뀔 때만 기록) | **해결(남김)** | 📄 `app.js:L1167-1170`. D4 와 같은 뜻이고 `boxsim.js` 가 덮음 |
| 9 | D2 `dpr` 불일치 | **미해결(보류 → 사이클 5 목록)** | 최대 0.3 CSS px 라 눈으로 못 봅니다. ④에 넣지 않았습니다. 고치는 법은 `cycle_1/stage3_final.md` ④ D2 그대로 |
| 10 | `/api/list` 에 `sort=insterr` 서버 지원 | **미해결(사이클 4, 선택)** | 📄 화면 방식이 동작함(500씩 3번=1,001장, 중복 0). 사이클 4 주제(목록 정렬·딱지)에서 요청 1번으로 줄일지 결정. 기능 문제 아님 |

### ③-3 틀린 문장이 남아 있는 문서 — 사이클 3 의 1차가 고칠 것 (⛔ 지금 고치지 않음)

| 파일 | 자리 | 지금 문장 | 바꿀 문장 |
|---|---|---|---|
| `cycle_2/stage1_work.md` | L119 | «블루베리(장당 300개 넘음)에서도 안전하다» | «블루베리 초벌은 장당 최대 204개(1,195장 총합 45,124)라 255 를 넘지 않지만, `/instances` 가 16비트로 주고 편집이 번호를 늘리므로 uint16 이 맞다» |
| 〃 | L165 가정 5 | «빈 번호를 다시 쓰지 않는다» | «같은 편집 세션 안에서는 `S.instMax+1`, 저장 뒤 다시 열면 파일의 최대+1(빈 번호 재사용 가능, 겹침 없음)» |
| 〃 | L220 | «번호 45,124개 규모에서 화면이 느려지지 않는지» | «장당 최대 204개(총합 45,124)라 화면 부담은 작다» |
| `cycle_2/stage2_review.md` | ⑤-R1 끝 | «『번호 45,124개』 같은 규모는 없습니다» | «45,124 는 1,195장 총합이다(장당이 아니다)» |
| 〃 | ⑤-R2·⑥-2 | «그다음 새로 붙이기가 비어 버린 55 번을 다시 씁니다» | «순수 함수 층에서만 그렇고, 화면은 `S.instMax` 로 세션 안 재사용을 막는다. 저장 뒤 다시 열면 재사용» |
| 계획서 `260917_0917신기능_5회검수_계획.md` | L145 | 2차 요약의 «반박 2건» 문장 | 위 두 정정을 반영한 한 줄 |
| `app/README.md` | §11 «네 가지 편집» 아래 | (없음) | «새 번호는 그 사진의 최대+1 입니다. 저장한 뒤 다시 열면 지워서 빈 번호가 다시 쓰일 수 있지만 겹치지는 않습니다» 한 줄 추가 |
| `app/README.md` | §6 L253 | «사과는 원본이 알마다 번호가 든 인스턴스 라벨이라 알 단위로 상자가 나옵니다» | **지금 코드에서 거짓**(⑦ 참조). 사이클 3 에서 코드를 고친 뒤 문장을 맞출 것 |
| 박성문 지시서 §2 L44 | — | «블루베리는 장당 최대 300개를 넘어» | **우리 파일이 아님** → 박성문에게 «실측 최대 204, uint16 근거는 16비트 서빙» 이라고 알리기(사람) |

## ④ 사람이 크롬에서 눌러 볼 체크리스트 (곽동신·최인훈)

준비: 라벨링 툴 주소(카톡)로 로그인. 목록 화면 «정렬» 에 「번호 편집 대상(번호오류 먼저)」 항목이 있으면 새 화면입니다(없으면 Ctrl+F5). 아래는 **한 줄에 하나**, «누른다 → 보여야 한다». 끝나면 마지막 줄로 자취를 되돌립니다.

**A. 열매 번호 — 사과 `20150919_174151_image361`(번호오류 있는 사진)**
- [ ] 과일 «사과» → 정렬 «번호 편집 대상» → 첫 쪽 카드에 `번호오류 N` 딱지가 붙고, 딱지 없는 카드는 33번째 이후에만 나온다
- [ ] 그 사진을 연다 → 레이어 칸에 «열매 번호 (번호마다 색, J)» 줄과 옆 «열매 번호 편집 — 원본 번호 마스크» 패널이 보인다
- [ ] 알마다 다른 색이 칠해지고 알 한가운데에 흰 글자·검은 테두리 번호가 찍혀 있다. 휠로 3배 확대해도 글자 크기가 화면에서 비슷하다
- [ ] 빨간 점선 상자가 알 위에 있고, 상자 위 글자와 오른쪽 `번호오류` 목록에 «한 알에 두 번호 #50|49» 같은 설명이 보인다
- [ ] `J` → 번호 색이 사라진다. 다시 `J` → 돌아온다. «번호 글자 보이기» 체크 해제 → 글자만 사라진다
- [ ] `K` → 안내 «번호 편집 모드 — 브러시·다각형·수정본 저장이 잠깁니다», 도구 단추 6개와 «수정본 저장»·«원본 GT 복사»·«전부 지우기»·«상자 모드» 체크박스가 회색으로 잠기고 패널 테두리가 보라색이 된다
- [ ] `B` 를 누른다 → «잠겨 있습니다 (K 로 끄기)» 안내만 뜨고 브러시로 바뀌지 않는다
- [ ] 알 하나 클릭 → 그 알에 고른 표시가 나고 패널 글이 «고른 번호 N» 이 된다. `D` → 그 알 색이 사라지고 «번호 72개 … 지움 1 … 저장 안 됨»
- [ ] `Ctrl+Z` → 그 알이 돌아오고 «지움 0», «저장 안 됨» 이 사라진다. `Ctrl+Y` → 다시 지워진다
- [ ] 알 A 클릭, 알 B 클릭, `M` → B 가 A 색으로 바뀌고 «합침 1»
- [ ] 알 하나 위를 가로질러 드래그(선이 그려짐) → `X` → 선을 경계로 한쪽이 새 색이 되고 «번호 N 를 나눠 새 번호 M» 안내. 알 안에서 끝나는 짧은 선으로 `X` → «나뉘지 않았다» 안내만 뜨고 그림이 안 바뀐다
- [ ] «새로 붙이기 모양 = 다각형», 빈 곳에 세 번 클릭 후 오른쪽 클릭 → 새 색이 채워지고 «새 번호 M 를 붙였습니다». 기존 알 위에 겹쳐 그리면 «겹쳐 뺀 화소 N» 이 0 보다 크다
- [ ] 이 상태에서 `.`(다음 사진) → «저장하지 않은 열매 번호 편집이 있습니다» 확인창. 취소
- [ ] `1`(원본 OK) → 같은 확인창이 먼저 뜬다. 취소
- [ ] `Ctrl+S` → «번호 N개 저장 완료 (최대 M)». 패널 머리글이 «사람이 고친 번호» 로 바뀐다. `.` 로 넘어갔다 `,` 로 돌아와도 고친 색이 그대로다
- [ ] «번호 되돌리기(고친 것 삭제)» → 확인창 → 처음 색·번호로 돌아오고 머리글이 «원본 번호 마스크»
- [ ] `20150921_132038_image146` 을 열어 번호 8 위에 두 알을 가르는 선을 긋고 `X` → 갈라진 쪽이 사람 눈에 «오른쪽/아래» 로 자연스러운지 본다(외적 부호 규칙, 가 갈래 미확인)

**B. 블루베리 초벌 — `Camera 1 Video (1)_1`**
- [ ] 연다 → 패널 머리글 «검출팀 초벌(watershed)», «번호 55개 (최대 55)». 알 55개에 색·글자가 찍힌다(작은 알이 많아 글자가 겹쳐 보이면 «번호 글자 보이기» 를 끄고 색만 본다)
- [ ] `K` → 알 하나 클릭 → `D` → `Ctrl+S` → «번호 54개 저장 완료». «번호 되돌리기» → 55개로 복귀
- [ ] 화면이 느려지지 않는다(확대·이동이 사과와 비슷하게 매끄럽다)

**C. 상자 — 복숭아 `210629-t4-17`(시험용 stem, 상자 0개)**
- [ ] `X` → «상자 모드 — 드래그로 네모» 안내, 상자 패널이 열린다. 도구 단추 이름이 «상자 그리기 (드래그)» 다(«(X)» 가 아님)
- [ ] 큰 네모 하나, 그 «안» 에 작은 네모 하나를 그린다 → 각각 노란 테두리
- [ ] `V` → 작은 네모 안을 클릭 → 작은 것이 흰 테두리로 고른 표시. **같은 자리를 다시 클릭** → 큰 네모가 고른 표시로 바뀐다. 한 번 더 → 다시 작은 것(순환)
- [ ] 고르기만 여러 번 한 뒤 아래 «상자 N개» 글에 «저장 안 됨» 이 **없다**
- [ ] 고른 상자를 끌어 옮긴다 → «저장 안 됨» 이 붙는다. `Ctrl+Z` → 제자리
- [ ] 휠로 아주 작게 축소(사진이 손톱만큼) → 상자 «안» 을 눌러 끈다 → 옮겨진다(크기조절로 잡히지 않는다). 흰 손잡이 네모가 상자보다 작다
- [ ] 1배로 돌아와 고른 상자의 모서리 흰 네모를 끈다 → 크기가 바뀐다. 반대편 모서리를 지나 끌어도 상자가 1×1 로 붕괴하지 않는다
- [ ] 아주 작은 네모(2픽셀 미만)를 하나 «찍듯이» 그린다 → 상자가 안 생기고 «저장 안 됨» 도 안 붙는다
- [ ] `Ctrl+S` → «상자 N개 저장». 이어서 아주 작게 축소한 상태에서 1~2픽셀짜리 상자를 억지로 하나 만들어 `Ctrl+S` → **경고색** flash «버린 것 1개», 아래 글 «너무 작아 버린 것 1 — 화면에서도 지웠음(Ctrl+Z 로 되돌려 볼 수 있음)», 화면에서 그 상자가 사라져 있다
- [ ] `Ctrl+Z` → 사라졌던 작은 상자가 다시 보인다(무엇이 버려졌는지 확인용). 다시 `Ctrl+S` → 또 버려진다
- [ ] 종류 «송이 (1)» 로 바꾼다 → 고른 상자가 하늘색이 된다. 같은 종류를 다시 고른다 → 아무 일 없음
- [ ] `Ctrl+Y` 를 누른다 → (지금은 마스크 «다시 실행» 이 눌리는 것이 현재 동작. 이상 없어 보이면 넘어가고, 사이클 3 에서 1줄 고침 — ③-2 #3)

**D. 두 모드 충돌 — 사과 아무 장**
- [ ] `K`(번호 편집) 켜진 상태에서 «상자 모드» 체크박스 클릭 → 체크가 안 되고 «번호 편집 모드를 먼저 끄세요 (K)»
- [ ] `K` 켜진 채 `X` → 상자 모드가 **안 켜지고** «나눌 열매 위에 선을 드래그한 뒤 X» 안내(= 나누기)
- [ ] `K` 로 끈 뒤 `X` → 상자 모드가 켜진다. 이 상태에서 `K` → 상자 모드가 **꺼지고** 번호 편집이 켜진다
- [ ] 상자 모드에서 `D` → «차이 보기» 체크가 토글된다(예전 뜻 그대로)

**E. 번호 없는 과일 — 복숭아 `210629-t1-01`, 포도 `0`**
- [ ] 연다 → «열매 번호» 줄·«열매 번호 편집» 패널이 **보이지 않는다**. `K`·`J` 를 눌러도 아무 일 없다
- [ ] 브러시로 한 번 칠하고 «수정본 저장» → 전과 같이 «수정본 저장 완료». «수정본 되돌리기» 로 원복

**F. 끝나면**
- [ ] A·B 는 «번호 되돌리기», C 는 «상자 전부 지우기» → «상자 저장», E 는 «수정본 되돌리기». 남는 것은 `status.json` 항목뿐이며 지우는 명령은 `260917_지울목록_시험잔재.md`

## ⑤ ponytail — 이번 사이클 코드에서 지울 것

형식 `파일:함수 — 자를 것 — 대체물`. 정확성 문제는 ③에 있고 여기엔 없습니다. 자체 점검(`demo()`·`test_clean.py`·`sim.js` 류)은 최소치라 건드리지 않습니다.

- `app/static/app.js:saveBoxes(L1113-1121) — yagni: «예전 서버는 boxes 를 안 준다» 방어(`Array.isArray(j.boxes)` 분기 + `else { S.bDirty = false; }`) — 서버가 재시작돼 `boxes` 를 주는 것을 2차가 확인했으므로 `if (cut) bpush(); S.boxes = j.boxes; S.bsel = -1; S.bDirty = false;` 로 평평하게. −3줄.` (사이클 5, 서버 재시작 확인 뒤)
- `app/instances.py:serve_instances(L121) + L26 MAX_ID — delete: `arr[arr > MAX_ID] = MAX_ID` — PNG(L·I;16·RGB 첫 채널)에서 읽은 값은 65535 를 넘을 수 없다. 없음. −2줄.`
- `app/instances.py:seed_path(L66-68) — shrink: 3줄 함수, 호출자 `source_of` 하나 — `source_of` 안에 `d = SEED_DIRS.get(fruit); sp = d and os.path.join(d, stem + ".png")` 로 인라인. −2줄.`
- `app/static/app.js:inflateZlib+decodeGrayPng(L1197-1271) — native(후보): 손으로 쓴 PNG 해독기 75줄(IHDR·IDAT·zlib·필터 0~4) — 서버 `/instances` 가 저장 때 받는 것과 **같은 인코딩**(RGBA, 번호 = R+G·256, 8비트)으로 주면 `createImageBitmap(blob, {colorSpaceConversion: "none"})` + `getImageData` 5줄로 읽힌다. 단 `/instances` 의 «16비트 그대로» 계약(사이클 1 시험·다 갈래 parity·2차 ev_* 스크립트)이 바뀌므로 **지금은 두고 사이클 5 «과잉 코드» 때 결정**. −70줄 가능.`

`net: -7 lines possible.` (해독기까지 바꾸면 −77.) 그 밖의 새 코드(`bcommit`·`bHandleR`·`hitBoxesAt`·`PICKTOL`·되돌리기 «바뀐 화소만» 기록·`api_revert_instances` 의 두 번 쓰기)는 각각 한 가지 일을 하고 대체물이 더 짧지 않아 Lean 입니다. `api_revert_instances` 두 번 쓰기를 한 번으로 줄이는 것은 `server.py` `ctx` 변경이 필요해 사이클 5(2차 ⑥-7 과 함께).

## ⑥ 분류표

| 항목 | 분류 | 어디서 |
|---|---|---|
| D3 저장 응답으로 화면 덮어쓰기(서버+화면) · D4 실제 변화 때만 «저장 안 됨» · 손잡이 상한 · 겹친 상자 순환 · 라벨 정정 | **해결** | 사이클 2 나·마, 2차 재현 |
| 번호 편집 네 동작·되돌리기 40단계·16비트 해독·저장 두 곳(번호+이진)·`instances_fixed`/`masks_fixed`/status 왕복 | **해결** | 사이클 2 가·다·라, 2차 N1(블루베리 43/43) — 계획서 «사이클 3 번호 편집 항목(저장 두 곳·status)» 은 사실상 이미 확인됨 |
| 되돌리기가 편집 기록을 안 지우던 것 · 편집 안 한 사진을 덮어쓰던 것 | **해결** | 라, 2차 재현 |
| `test_clean.py` 검사 · `boxes.py` ponytail 3건 · `instances.py`↔`export` 중복 통합 | **해결** | 나·다·2차 |
| 사이클 1 판정서 모순(`boxes.py` 클래스 식) | **해결(손대지 않음으로 확정)** | ② |
| 2차 반박 R1·R2 의 문장 정정 8곳 | **미해결(사이클 3, 문서만)** | ③-3 |
| `Ctrl+Y` 상자 모드 1줄 | **미해결(사이클 3)** | ③-2 #3 |
| **초벌 상자가 번호 마스크를 안 씀**(사과 95→30, 블루베리 55→39) | **미해결(사이클 3 주제 그 자체)** | ⑦ |
| README §6 «사과는 알 단위 상자» 문장 | **미해결(사이클 3, 코드 고친 뒤)** | ③-3 |
| `sort=insterr` 서버 지원 | **미해결(사이클 4, 선택)** | ③-2 #10 |
| D2 `dpr` · `layer=gt` API 방어 · `maskio` 이동 · `saveBoxes` 방어 분기 삭제 · 해독기 native 교체 · `api_revert_instances` 한 번 쓰기 | **미해결(사이클 5 보류 목록)** | ③-2 #5·#7·#9, ⑤ |
| 브라우저 눈 확인 ④ | **사람(확인)** | 곽동신·최인훈 |
| D1 클래스 번호(검출 팀 `grape.yaml` 송이 0 ↔ 우리 송이 1) | **사람 결정** | 계획서 «사람이 정해야 하는 것» 1. 결정 전 `boxes.py:L17 CLASSES`·`L160` 불변 |
| 사과·블루베리 검수판(`datasets_reviewed_260916`) 여부 | **사람 결정** | 계획서 2 |
| 박성문 지시서 §2 «장당 300개» 문장 | **사람(박성문에게 알림)** | ③-1 R1 |
| 시험 자취 삭제(`data/{apple,blueberry}/status.json`, 빈 폴더, 복숭아 상자 0개 파일) | **사람(실행)** | `260917_지울목록_시험잔재.md` — 명령은 이미 적혀 있음. 📄 지금 상태: `blueberry/status.json` 항목 1(15:09:23) · `apple/status.json` 항목 1(14:59:02) · 네 폴더 비어 있음 · `peach/boxes/210629-t4-17.json` 상자 0 |

## ⑦ 사이클 3 · 1차가 할 일 (주제: AI 초벌이 네 과일 모두에서 맞는가 — 사과 인스턴스·포도 송이 개수 대조)

내가 먼저 재 놓은 것 📄 (`boxes_seed` 와 같은 규칙 — `load_mask_bool` → `ndimage.label`, 4화소 미만 버림):

| 과일 · 사진 | 번호 마스크의 열매 수 | GT>0 연결성분 | AI 제안>0 연결성분 | 지금 «마스크에서 초벌 생성» 이 주는 것 |
|---|---:|---:|---:|---|
| 사과 `image1` | **95**(gt) | 74 | 30 | **30개** — `app.js:L1128` 이 AI 제안이 있으면 `source=ai`, 네 과일 4,823장 전부 제안이 있어 항상 AI 제안. `server.py:L741 labeled()` 는 번호를 버리고 이진 연결성분으로 다시 세므로 `gt` 를 골라도 74 |
| 사과 `image361` | 73 | 62(≥4px 57) | 34 | 34개 |
| 블루베리 `Camera 1 Video (1)_1` | **55**(검출팀 watershed) | 47 | 39(≥4px 38) | 38개 — 검출 팀 초벌이 있는데 안 쓴다 |
| 포도 `0` / `1` / `10` | (번호 없음) | 15 / 8 / 9 | 15 / 5 / 19(≥4px 16) | 15 / 4 / 16 — 송이가 붙어 있으면 합쳐지고 잎에 가려지면 쪼개짐 |
| 복숭아 `210629-t1-01` | (번호 없음) | 11 | 11 | 11 |

즉 README §6 «사과는 알 단위로 상자가 나온다» 는 **지금 코드에서 성립하지 않습니다.** 사이클 3 1차가 할 일(우선순위):

1. **`app/boxes.py:api_boxes_seed`** — `from instances import source_of, read_u16, ids_of` 를 써서 `source_of(fruit, stem)` 이 번호 마스크를 주면(사과 `gt`·블루베리 `seed`·사람이 고친 `fixed`) **번호마다** 상자 하나(`lab = read_u16(p)`, 지금 루프 그대로), 없을 때만(복숭아·포도) 지금처럼 `labeled(...)` 연결성분. 응답 `source` 에 `"instances:gt|seed|fixed"` 를 실어 화면 안내가 «번호 마스크에서 95개 초벌» 이 되게. **`app/static/app.js:seedBoxes(L1126-1137)`** 의 `S.ai ? "ai" : "gt"` 는 «`S.inst` 가 있으면 번호 우선, 없으면 지금 규칙» 으로. 확인: `image1` → 95, `Camera 1 Video (1)_1` → 55, 복숭아·포도는 지금과 같은 수. `boxes.py` 가 바뀌므로 **서버 재시작 필요**(남의 작업 없을 때, `bash app/run.sh restart`).
2. **개수 대조표를 실측으로**: 네 과일 × 각 10장에서 «초벌 상자 수 ↔ 정답 수». 정답은 사과=번호 수, 블루베리=검출 팀 watershed 수, 포도=검출 팀 YOLO 라벨(`work/park_seongmoon/yolo_grape/` 아래 `labels/*.txt` 줄 수 — 있는지 먼저 확인, 읽기만), 복숭아=GT 연결성분(다른 정답 없음). 붙은 송이가 합쳐지는 사진·가려져 쪼개지는 사진을 각각 1장 이상 골라 «왜 다른가» 를 적을 것.
3. 문서 정정 ③-3 여덟 곳(코드 안 고침) + README §6 문장을 1번 결과에 맞게. `Ctrl+Y` 1줄(③-2 #3) + `modesim.js` 검사 1줄.
4. 번호 저장 → `export_dataset.py --instances` 왕복: 사과·블루베리 각 1장을 저장(`/api/save_instances`)한 뒤 내보내 `instances/<stem>.png` 가 `instances_fixed/` 와 **바이트 동일**한지(다 갈래는 원본만 3장 확인, 고친 것은 아무도 안 함). 끝나면 `/api/revert_instances`.
5. 보고서에 «시험한 것/안 한 것» 표, 남긴 자취를 `260917_지울목록_시험잔재.md` 에 «덧붙임 3» 으로. GPU 없음, 원본 데이터셋·`work/park_seongmoon/` 읽기만.

사람 결정 대기: **D1 클래스 번호**(사이클 4 주제, 결정 전 `boxes.py:L17`·`L160` 불변) · **사과·블루베리 검수판 여부** · 박성문 지시서 «장당 300개» 정정 알림.
