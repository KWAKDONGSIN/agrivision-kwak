작성: 2026-09-17

# 0917 새 기능 5회 검수 — 사이클 2 · 2차 검수 (Opus 5)

검수자: AI 2차(Opus 5) · 지시문: `cycles/prompts/stage2_c2.md`
읽은 것: 다섯 갈래 보고서(가 `stage1_work.md` · 나 `stage1_work_boxes.md` · 다 `stage1_work_server.md` ·
라 `stage1_work_revert.md` · 마 `stage1_work_client.md`), `cycle_1/stage3_final.md`, 계획서,
`app/{boxes.py,instances.py,server.py,dupes.py}`, `app/static/{app.js,index.html,style.css}`, `export/export_dataset.py`
새 증거 폴더: `cycles/260917_신기능/cycle_2/stage2/`
근거 표기: 📄 내가 직접 돌려 본 실측(명령·값 명시) · ❌ 근거 없는 내 판단

**브라우저가 없습니다.** 화면 조작은 전부 `app.js` 에서 코드를 **글자 그대로 떼어 내** node 로 돌린
시뮬레이션이고, 그 사실을 항목마다 적었습니다. 사람이 크롬에서 눈으로 볼 일은 ⑥에 모았습니다.

---

## ① 합격 기준 (실행 «전» 에 정하고 그다음 돌렸습니다)

| # | 기준 | 판정 |
|---|---|---|
| **재현** | | |
| R1 | 다섯 갈래가 남긴 시험을 내가 다시 돌려 **같은 결과**가 나온다 | ✅ 9개 전부 |
| R2 | 번호 편집 네 동작이 번호를 잃지 않는다 · 16비트 해독이 사과 번호를 살린다 | ✅ |
| R3 | 번호 저장이 `instances_fixed/`(번호)와 `masks_fixed/`(이진) 두 곳에 일관되게 쓰이고, 되돌리면 **두 파일과 편집 기록이 모두** 사라진다 | ✅ |
| R4 | 상자 저장 응답의 목록으로 화면이 덮어써져 버려진 상자가 화면에만 남지 않는다 | ✅ |
| R5 | 번호가 없는 과일(복숭아·포도)에서 화면·저장이 **전과 똑같다** | ✅ |
| **새 확인(아무도 못 한 것)** | | |
| N1 | **블루베리 초벌**을 불러와 지우기·합치기·나누기·새로 붙이기 각 한 번 → 저장 → 되읽기 → 되돌리기가 전부 된다 | ✅ 43개 검사 |
| N2 | 번호 편집 모드와 상자 모드가 **동시에 켜지지 않는다**. `K`·`J`·`X`·`D`·`M`·`N`·`Ctrl+S`·`Ctrl+Z` 가 서로 먹지 않는다 | ✅ 31개 검사 |
| N3 | 갈래끼리 **서로 되돌려 놓은 것이 없다**(가·마의 `app.js`, 나·다·라의 서버 세 파일) | ✅ |
| N4 | 살아 있는 서버가 세 갈래의 서버 변경을 **실제로 물고 있다** | ✅ |
| **규칙** | | |
| G1 | 고치기 전 `_backup_260917_c2f_<파일명>` 사본을 남긴다 | ✅ (고친 파일 2개) |
| G2 | 원본 데이터셋·`work/park_seongmoon/` 은 읽기만. 파일 삭제 없음. GPU 없음. 새 패키지·CDN 없음 | ✅ |
| G3 | 시험 산출물은 **API 로** 되돌리고, 남는 자취는 사람이 지울 수 있게 적는다 | ✅ ⑦ |

## ② 한 줄 결론

**다섯 갈래가 «했다» 고 한 것은 내가 다시 돌려 전부 사실로 확인됐고**(9개 시험 재현, 실패 0),
아무도 못 했던 **블루베리 초벌 편집·저장 왕복(43/43)** 과 **두 모드·키 충돌(31/31)** 도 통과했고,
갈래끼리 서로 깨뜨린 것은 **없습니다**. **확실한 버그는 0건**이어서 코드는 한 줄도 고치지 않았고,
아무도 담당이 없던 숙제 **`test_clean.py` 검사 추가**만 내가 넣었습니다. 대신 **틀린 «주장» 2건**을
실측으로 반박합니다(⑤) — 블루베리 규모, 새 번호 붙이는 규칙.

## ③ 재현 결과 (갈래 / 주장 / 내 확인 방법 / 판정)

돌린 명령과 결과는 이 표 아래 ③-1 에 그대로 있습니다.

| 갈래 | 주장 | 내가 확인한 방법 | 판정 |
|---|---|---|---|
| 가 | 16비트 PNG 를 캔버스로 읽으면 번호가 전부 0 이 되므로 `decodeGrayPng` 를 넣었다 | 📄 `/instances` 응답 헤더 실측: **bitdepth=16 colortype=0**(사과·블루베리 둘 다). 배포되는 `app.js` 의 INSTCORE 를 떼어 내 해독 → 사과 73개·블루베리 55개가 살아남 | **사실** |
| 가 | 편집 네 동작이 번호 수를 `-1 -1 +1 +1` 로 움직이고 되돌리면 완전 복원 | 📄 `node sim.js` → **21/21**. 내가 새로 쓴 `bbsim.js`(블루베리) → **14/14**, 되돌린 뒤 **화소 단위로** 초벌과 같음 | **사실** |
| 가 | 번호 편집 모드가 브러시·다각형·수정본 저장·상자 모드를 잠근다 | 📄 `modesim.js` 31/31 — `B` 키까지 잠기고(도구 안 바뀜), `#btn-save`·`#box-mode` `disabled=true` | **사실** |
| 가 | `X` 는 모드가 켜졌을 때만 나누기, 꺼지면 예전처럼 상자 모드 토글 | 📄 `modesim.js`: 꺼짐 → `boxMode=true`, 켜짐 → `setNTool(split)` + 상자 모드 **안 건드림** | **사실** |
| 가 | 복숭아는 화면·저장이 전과 똑같다 | 📄 `ev_peach.py` → **15/15**(`instance_info has:false`, `/instances` 404, `edToPngDataUrl`·`doAction`·`applyPolygon`·`stamp` 가 백업본과 한 글자도 안 다름, 0/255 저장 화소 일치, `instances_fixed` 안 생김) | **사실** |
| 가 | 목록 정렬을 화면에서 한다(`page_size=500` 으로 사과 3번) | 📄 `?sort=insterr` → 서버가 거부하지 않고 이름순 `total 1001`. `page_size` 는 **20 밑으로 줄지 않고 500 위로 늘지 않음**(3→20, 600→500). 500씩 3번 = **500+500+1 = 1001장, 중복 0** | **사실** |
| 가 | «서버 쪽에 필요한 것 1» — 되돌려도 `instances_edited` 가 남는다 | 📄 지금은 **남지 않음** — 라 갈래가 같은 사이클에 고쳤다. 가의 보고서는 그 시점 기준으로 맞고 지금은 **낡은 서술** | **이미 해결** |
| 나 | 저장 응답에 `boxes` 가 실리고 기존 필드는 그대로 | 📄 `probe.sh after` + `verify_c2b.py` → **24개 전부 통과**. 저장 응답 `boxes` == 되읽기 `boxes` 완전 일치 | **사실** |
| 나 | 저장·되읽기·`over`·YOLO 값이 이전과 같다 | 📄 `yolo_before.txt` vs `yolo_after.txt` **바이트 동일**, 3,005개 → `n_boxes 3000 over 5` | **사실** |
| 나 | `clean()` 최상위 · `import time` 없음 · `demo()` 가 실제 `clean()` 을 시험 | 📄 `python3 app/boxes.py` → «실제 clean() 을 시험했습니다» 통과. `grep -n "^def clean"` 최상위, `import time` 0건 | **사실** |
| 나 | 3차 제안 중 `saved` 를 `os.path.exists` 로 바꾸면 **동작이 바뀐다**(깨진 파일) | 📄 코드 확인: `read_boxes()` 는 없을 때와 깨졌을 때 모두 `None` → 지금 꼴이 맞다 | **나가 맞다** |
| 다 | `instances.py`↔`export_dataset.py` 중복을 한 군데로, 산출물은 바이트 동일 | 📄 `parity_instances.py` 재실행 → 48+12곳 중 **다른 곳 1**(의도한 것). `export_dataset.py` 196→157줄, 그 파일은 다 말고 아무도 안 건드림 | **사실** |
| 다 | 블루베리 `--instances` dry-run 1195장 · 복숭아 0장 | 📄 실제로 돌림: 블루베리 **1195장**, 복숭아 **0장**(`instances/` 안 생김) | **사실** |
| 다 | `layer=gt` 한 군데만 달라진다(고친것 있음+원본 이진 → 404) | 📄 parity 출력 `f1_s0_bin gt: (200,'gt') → (404,None)` 그대로 재현. 지금 데이터로 도달 불가도 확인(네 과일 `instances_fixed/` 전부 0개에서 시작) | **사실** |
| 라 | 되돌리면 `instances_edited`·`instance_counts` 가 **같이** 사라진다 | 📄 `real_apple_once.py` → **14/14**(실제 `data/`, 스스로 원복). 내 블루베리 시험에서도 `status.json` 항목에 두 필드 없음 | **사실** |
| 라 | 되돌릴 것이 없으면 status 를 **안 쓴다**(`changed:false`) | 📄 살아 있는 서버 응답에 `changed` 필드 실려 옴 = 이 변경이 서버에 올라가 있다는 증거 | **사실** |
| 마 | 저장 응답 목록으로 화면을 덮어쓰고, 버린 것 있을 때만 되돌리기 1칸 | 📄 `node boxsim.js` → **54/54**(예전 서버 `boxes` 5종 방어·저장 중 사진 바꿈·저장 실패 포함) | **사실** |
| 마 | D4 — 고르기만 100회로는 «저장 안 됨» 이 안 뜬다 | 📄 `boxsim.js` 해당 항목 통과. 코드도 mousedown 에서 `before` 만 잡고 `boxMouseUp` 의 `bcommit` 이 견줌 | **사실** |
| 마 | 손잡이 반경 상한 · 겹친 상자 순환 | 📄 `boxsim.js` 통과(배율 6가지×크기 6가지=36, 3겹 순환). `bHandleR` 를 **그리기에도** 같이 써서 «보이는 것 = 잡히는 것» | **사실** |
| 마 | 번호 편집 코드를 한 줄도 건드리지 않았다 | 📄 `diff _backup_260917_c2e_app.js app.js` — 바뀐 7구간이 **전부 L927–1170 상자 블록**, INSTCORE·`numKey`·`setNumMode` 무변경. `sim.js` 21/21 이 현재 파일에서 계속 통과 | **사실** |
| 마 | 정적 파일이라 재시작 없이 반영된다 | 📄 로그인 뒤 `curl /static/*` md5 = 디스크 md5 (app.js `9f75078d…` · index.html `ec72d1f9…` · style.css `87e4af5b…`) | **사실** |

### ③-1 내가 돌린 명령과 결과 (전부 종료코드 0)

```
node --check app/static/app.js                                    → OK
python -m py_compile app/{instances,boxes,server}.py export/export_dataset.py → OK
python3 app/boxes.py                                              → 자체 점검 통과(실제 clean() 시험)
import server 로 라우트 확인  → /api/boxes · /api/save_instances · /api/revert_instances ·
                                /instances · /api/instance_info · /api/instance_errors · /api/boxes_export 전부 등록
node cycles/260917_신기능/cycle_2/stage1/sim.js                     → 21개 중 21개 통과       (가)
python ev_peach.py                                                → 실패 0건 (15개)        (가)
bash stage1_boxes/probe.sh after ; python3 stage1_boxes/verify_c2b.py → 검사 24개 전부 통과   (나)
python stage1_server/parity_instances.py                          → 다른 곳 1 (의도한 것)    (다)
python stage1_revert/scripts/real_apple_once.py                   → 14개 확인, 실패 0       (라)
node stage1_client/boxsim.js                                      → 54개 중 54개 통과       (마)
node cycles/cycle_1/stage2/verify_resize_real.js                  → === 전부 통과 ===      (사이클1 2차 회귀)
python3 cycles/260917_신기능/cycle_1/stage1/test_clean.py           → 통과 (내가 넣은 검사 포함)
node   cycles/260917_신기능/cycle_2/stage2/bbsim.js                 → 14개 중 14개 통과   ← 새로 씀
python cycles/260917_신기능/cycle_2/stage2/ev_blueberry.py          → 실패 0건 (43개 확인) ← 새로 씀
node   cycles/260917_신기능/cycle_2/stage2/modesim.js               → 31개 중 31개 통과   ← 새로 씀
```

### ③-2 N4 — 살아 있는 서버가 세 갈래 변경을 실제로 물고 있다 (마 갈래가 넘긴 의문 해소)

```
📄 ps -o lstart -p 2911132   → 2026-09-17 14:48:50 기동, 포트 5111 하나만 쥠
📄 app/boxes.py mtime 14:38:34 · app/instances.py 14:39:01 · app/server.py 14:06:56
   → 서버 기동(14:48:50)이 세 파일보다 «뒤» 이므로 나·다·라 변경이 모두 올라가 있다
📄 살아 있는 응답으로 교차 확인: POST /api/boxes 응답에 boxes 실림(나) ·
   POST /api/revert_instances 응답에 changed 필드(라) · /instances 헤더·바이트가 parity 와 일치(다)
📄 app.js 14:50:05 · index.html 14:48:16 는 정적 파일 — 요청마다 디스크에서 읽히고 md5 가 서빙본과 같다
→ 마 갈래 ⑤-7 의 «PID 2898501 → 2911132» 는 **이 14:48:50 재시작** 때문이다.
   2차는 재시작이 필요하지 않아 **한 번도 재시작하지 않았다**(서버는 계속 PID 2911132).
```

### ③-3 N3 — 갈래끼리 되돌려 놓은 것이 없다

| 파일 | 누가 언제 | 내가 본 방법 | 판정 |
|---|---|---|---|
| `app/static/app.js` | 가 1079→1946 → 마 →2008 | 📄 `diff c2e_app.js app.js` = 7구간 전부 상자 블록(+79/−20). `sim.js` 21/21 이 **현재 파일**에서 통과 = 가의 INSTCORE 살아 있음. `boxsim.js` 54/54 = 마의 것도 살아 있음 | **둘 다 남아 있다** |
| `app/static/index.html` | 가(번호 패널) → 마(라벨 2줄) | 📄 `diff` = L90·L146 **두 줄만** 교체. 번호 패널·정렬 항목 그대로 | 이상 없음 |
| `app/static/style.css` | 가 | 📄 `diff c2e_style.css style.css` **차이 없음**(마는 손댈 것이 없었다) | 이상 없음 |
| `app/boxes.py` | 나만 | 📄 `diff c2b_boxes.py boxes.py` = `boxes` 실기 + ponytail 3건. 212→212줄 | 이상 없음 |
| `app/instances.py` | 다(209→225) → 라(→245) | 📄 `diff c2c→c2d` = **다의 변경만**, `diff c2d→현재` = **라의 변경만**. 두 벌이 겹쳐 있고 서로 지운 것 없음 | **둘 다 남아 있다** |
| `export/export_dataset.py` | 다만 | 📄 196→157줄, 다 갈래 백업과만 차이 | 이상 없음 |

## ④ 새 확인 두 가지 (아무도 못 했던 것) · 새로 찾은 문제 · 내가 고친 것

### ④-1 N1 블루베리 초벌 번호 편집·저장 왕복 — **처음 시험, 43개 확인 실패 0**

증거: `stage2/bbsim.js`·`bbsim.out`(화면 쪽 14/14) · `stage2/ev_blueberry.py`·`ev_blueberry.out`(서버 쪽 43개)
대상 `blueberry / Camera 1 Video (1)_1` (1920×1080, 검출 팀 watershed 초벌 **55개**)

```
초벌을 화면 해독기(app.js INSTCORE)로 읽음 → 번호 55개 최대 55 앞면화소 50,007
 1) 지우기 번호 54      번호 54개              105화소 지움
 2) 합치기 1 → 2        번호 53개              2,446화소 옮김
 3) 나누기 번호 2       번호 54개 최대 56      조각 2→3, 새 번호 56
 4) 새로 붙이기         번호 55개 최대 57      1,600화소, 새 번호 57
  → 번호 수 -1 -1 +1 +1 로 정확히 움직이고, 4단계 모두 되돌리면 초벌과 «화소 단위로» 같음

POST /api/save_instances → {"ok":true,"n_instances":55,"max_id":57,"fg_pixels":51334}
  서버가 센 값 3개 모두 화면이 센 값과 일치
  status: instances_edited:true · instance_counts{erase:1,merge:1,split:1,add:1}
  instances_fixed/…png  mode=I;16 uint16 번호 55개 최대 57 (지운 54 없음·합쳐 사라진 1 없음·56·57 있음)
  masks_fixed/…png      8비트 0/255, 모든 화소가 (번호>0)*255 와 정확히 일치        ← R3
GET /instances layer=auto  → X-Instance-Source: fixed, 저장본과 화소 단위로 같음
GET /instances layer=fixed → fixed 동일
GET /instances layer=gt    → X-Instance-Source: **seed**, 번호 55개 = 초벌이 가려지지 않았다
POST /api/revert_instances → removed[instances_fixed,masks_fixed] · changed:true
  두 파일 삭제 · status.json 항목에 편집 기록 없음 · instance_info 가 다시 source=seed n=55
```

덧붙여 **초벌 1195장 전수 검사**(사람이 어느 장을 열어도 저장이 되는지):
📄 초벌 크기 ≠ 원본 마스크 크기 **0장**, 원본 마스크 없는 것 **0장**, 원본 이미지 없는 것 **0장**.
→ 크기가 하나라도 달랐다면 `/api/save_instances` 가 400 «번호 마스크 크기가 원본과 다릅니다» 로 거절했을 자리인데, 1195장 전부 안전합니다.

### ④-2 N2 두 모드·키 충돌 — **처음 시험, 31개 확인 실패 0**

증거: `stage2/modesim.js`·`modesim.out`. `app.js` 에서 `setTool`·`setNumMode`·`numKey`·`$("#box-mode").onchange`·
`keydown` 처리 **127줄을 글자 그대로** 떼어 내 가짜 DOM 위에서 돌렸습니다(❌ 사본 재구현 아님).

- **동시에 안 켜짐**: `X`→상자 모드만, `K`→번호 편집이 켜지며 상자 모드가 꺼지고 체크박스가 `disabled`.
  잠금을 무시하고 체크박스를 눌러도 거절 + «번호 편집 모드를 먼저 끄세요 (K)».
  📄 **키·체크박스를 400번 마구 눌러도 두 모드가 동시에 켜진 적 0회.**
- **키가 서로 안 먹음**: 모드 켜짐 `X`=나누기 / `D`=지우기 / `M`=합치기 / `N`=새로 붙이기 / `J`=번호 레이어 / `K`=끄기,
  `B`(브러시)는 «잠겨 있습니다» 안내만 하고 도구를 바꾸지 않음.
  모드 꺼짐 `X`=상자 모드, `D`=차이 보기(예전 뜻), **`M`·`N` 은 아무 일도 안 함**(원래 없는 키라 충돌 없음).
- **Ctrl 조합**: `Ctrl+S` → 번호 모드=번호 저장 / 상자 모드=상자 저장 / 둘 다 꺼짐=수정본 저장.
  `Ctrl+Z` → 번호 되돌리기 / 상자 되돌리기 / 마스크 되돌리기. `Ctrl+Y` → 번호 다시하기 / (상자는 없음) / 마스크 다시하기.
- **번호가 없는 사진**: `S.inst` 가 없으면 `K`·`J` 가 아무 일도 안 함 → 복숭아·포도 화면 무변경(R5 와 같은 뜻).
- **목록 화면**: 편집 화면이 숨어 있으면 키가 전혀 안 먹음.

### ④-3 새로 찾은 문제 · 고친 것

**확실한 버그는 0건입니다.** 코드(`app/*`, `static/*`, `export/*`)는 **한 줄도 고치지 않았습니다.**
애매한 것은 ⑥으로 넘겼습니다(지시대로).

고친 파일은 **하나뿐** — 아무도 담당이 없어 비어 있던 숙제입니다:

| 파일 | 백업 | 무엇을 |
|---|---|---|
| `cycles/260917_신기능/cycle_1/stage1/test_clean.py` | `_backup_260917_c2f_test_clean.py` | 사이클1 3차 판정 ③ 이 요구한 검사 추가 |
| `260917_지울목록_시험잔재.md` | `_backup_260917_c2f_260917_지울목록_시험잔재.md` | 내 시험 자취를 사람이 지울 수 있게 «덧붙임 2» 추가 |

추가한 검사(나 갈래 ⑤·마 갈래 ⑤-8 이 «아직 아무도 안 넣었다» 고 적은 그 한 줄):

```
5개(2픽셀 미만 2개 포함) -> {'n_boxes': 3, 'dropped': 2, 'over': 0} | 응답 boxes 길이 3
  + 응답 boxes == 되읽기 boxes · 뒤집힌 좌표 정렬 [650,850,700,900] · id 가 1,2,3 으로 다시 붙음
python3 test_clean.py → 통과 (종료코드 0)
```

## ⑤ 반박 — 갈래가 «사실» 처럼 적었지만 실측과 다른 것 2건

두 건 모두 **코드 동작은 옳고**, 틀린 것은 **보고서의 서술**입니다. 3차 판정서에 그대로 옮기면 안 됩니다.

**R1. 「블루베리는 장당 300개 넘음」 (가 갈래 ②-1·⑤ «번호 45,124개 규모») — 틀렸습니다.**
📄 초벌 **1195장 전수** 실측: 장당 번호 수 최소 **1** · 중간 **27** · 평균 **37.8** · 최대 **204**.
**300개를 넘는 사진은 0장**이고 200개를 넘는 것도 1장뿐입니다.
**[사이클3 정정]** 「번호 45,124개」 는 **1,195장 전체의 총합**으로 실재하는 숫자입니다 — 틀린 것은 그것을
«한 장의 규모» 처럼 쓴 것이고, 「그런 규모는 없다」 는 내 문장도 지나쳤습니다.
→ 그래서 «블루베리에서 화면이 느려질까» 하는 가 갈래의 걱정(⑤-3)은 실제로는 **작은 문제**입니다.
❗ 다만 **16비트 해독·uint16 저장이 필요하다는 결론 자체는 그대로 유효합니다** — 이유가 «번호가 255를 넘어서»가
아니라 📄 «`/instances` 가 **bitdepth 16 colortype 0** PNG 로 주기 때문»(캔버스로 읽으면 상위 바이트만 남아
사과 1~95 가 전부 0 이 된다)이기 때문입니다. 근거를 바꿔 적어야 합니다.

**R2. 「새 번호는 항상 최대+1 이고 빈 번호를 다시 쓰지 않는다」 (가 갈래 가정 5) — 조건부로 틀렸습니다.**
📄 `bbsim.js` §D 로 실측: **순수 함수 층에** `instStats().maxId+1` 을 직접 넣으면, 최대 번호(55)를 지워
최대가 54 로 내려간 뒤 «새로 붙이기» 가 **비어 버린 55 번을 다시 씁니다**.
**[사이클3 정정]** 그러나 실제 화면은 `S.instMax`(커질 때만 갱신, 📄 `app.js` `repaintNum`·`runNumOp`)를 쓰므로
**같은 편집 세션 안에서는 재사용하지 않습니다**(55 를 지워도 다음 새 번호는 56). 빈 번호가 다시 쓰이는 것은
**저장한 뒤 사진을 다시 열 때**(`loadInstances` 가 파일의 실제 최대로 다시 시작할 때)뿐입니다.
❌ 내 판단: **버그는 아닙니다** — 그 번호는 비어 있었으므로 겹치지 않고(화소 400개로 확인), 지시서 §2
«번호는 1부터 연속일 필요 없음» 도 어기지 않습니다. 하지만 «지웠다가 다시 붙이면 지운 열매의 번호가 새 열매에
붙는다» 는 사람이 오해할 수 있는 동작이라 **문서에 적어야** 하고, 되돌리기 이력과 함께 보면 헷갈릴 수 있습니다.
→ ⑥-2 로 넘깁니다.

**(반박은 아니고 정정)** 가 갈래 ⑤ «서버 쪽에 필요한 것 1 — 되돌려도 `instances_edited` 가 남는다» 는
같은 사이클의 라 갈래가 이미 고쳤습니다. 가의 보고서만 읽으면 미해결로 보이니 3차 판정서에서 **해결로** 적어야 합니다.

## ⑥ 3차 최종이 결정할 항목

| # | 내용 | 내 의견(❌) · 근거(📄) |
|---|---|---|
| 1 | **사람이 크롬에서 눈으로 볼 것** — 2차도 브라우저를 못 썼습니다. ㉠ 번호 색·번호 글자·빨간 점선 상자가 제자리인지 ㉡ 겹친 상자를 같은 자리로 두 번 눌러 아래 것이 잡히는지 ㉢ 많이 축소(배율 0.03)해서 상자 «안» 을 눌러 옮겨지는지 ㉣ 저장 때 버려진 상자가 화면에서 사라지고 Ctrl+Z 로 돌아오는지 | ❌ 남은 위험은 **거의 전부 여기**입니다. 함수 단위로는 세 갈래가 149개 검사를 통과했지만 마우스 사건·캔버스 그리기는 아무도 못 봤습니다. 사이클 3 전에 사람이 10분만 봐 주면 좋겠습니다 |
| 2 | **⑤-R2 «저장한 뒤 다시 열면 지운 번호를 다시 쓰는» 동작**을 (가) 그대로 두고 README 에 한 줄 적기 (나) 그 사진에서 한 번 쓴 번호는 다시 쓰지 않게 «최대값 기억» 넣기 | ❌ **(가)를 추천**. (나)는 «어디까지 썼나» 를 저장·되읽기 사이에도 들고 다녀야 해서 파일 형식이 늘어납니다. 📄 지금도 번호 겹침은 없습니다 |
| 3 | **`Ctrl+Y` 가 상자 모드에서 «마스크 다시하기» 로 간다** (상자에는 다시하기가 없음) | ❌ 사이클 2 전부터 그랬고 마 갈래가 만든 것이 아닙니다(📄 `app.js:L774`). 상자 «다시하기» 를 새로 만들지 않는다면 **그대로 두기**. 고친다면 `if (S.boxMode) return;` 한 줄 |
| 4 | **번호 편집 모드에서 판정 키 `1`·`3`·`4`(원본OK·문제있음·제외)가 그대로 먹는다** | ❌ **그대로 두기.** 📄 `doAction` 이 `confirmLeave()` 를 먼저 부르고 그 안에서 `S.numDirty` 를 물어보므로(`app.js:L485`) 저장 안 한 번호 편집이 말없이 사라지지는 않습니다. 잠그면 «번호만 보고 판정» 하는 흐름이 막힙니다 |
| 5 | **다 갈래의 의도한 동작 변경** — «고친 번호가 있는데 원본이 0/255 이진» 인 사진에 `layer=gt` → 404 | ❌ **새 쪽이 맞습니다.** 📄 오늘 데이터로는 도달 불가(복숭아·포도에 번호를 저장한 사람이 없어야 함). 단 `/api/save_instances` 를 직접 쏘면 복숭아에도 번호를 만들 수 있으니, **화면이 못 하는 일을 API 가 하게 둘지**는 사이클 5 «방어» 주제로 |
| 6 | 나 갈래가 **안 깎은** `boxes.py:L157` (`CLASSES.index(...) if ... else 0`) | ❌ **D1(클래스 번호) 결정까지 그대로.** 📄 그 자리가 (다)안의 «대응표» 가 될 자리입니다. 나 갈래 판단에 동의 |
| 7 | 다 갈래 가정 — `instances.py` 가 `ctx["DATA_DIR"]` 대신 `dupes.DATA_DIR` 을 믿는다 · `export_dataset.py` 가 flask 를 간접으로 필요로 한다 | ❌ 오늘은 둘 다 문제 없습니다(📄 `server.py:L34` == `dupes.py:L13`, `py_compile` 통과). **사이클 5 정리**로: 번호 읽기·쓰기 함수를 flask 를 안 쓰는 `app/maskio.py` 로 옮기면 두 가정이 함께 사라집니다 |
| 8 | 마 갈래의 **덤 1줄**(`#boxcls` 종류를 «정말» 바꿀 때만 기록) | ❌ **남기기.** D4 와 같은 뜻이고 📄 `boxsim.js` 가 덮고 있습니다 |
| 9 | 사이클 1 3차가 남긴 **D2(`dpr` 불일치)** | ❌ 여전히 **브라우저 실측이 필요**해 2차도 못 했습니다. 사이클 5 «보류 목록» 으로 |
| 10 | 가 갈래가 서버에 요청한 «`/api/list` 에 `sort=insterr` 를 서버가 지원» | ❌ 급하지 않습니다. 📄 화면 방식이 실제로 동작함을 확인했습니다(500씩 3번 = 1001장, 중복 0). 서버가 하면 요청 1번으로 줄어드는 **성능 개선**일 뿐 |

## ⑦ 시험 뒤 되돌린 것 · 남은 자취 (⛔ 삭제는 사람이 — CLAUDE.md 3항)

**되돌린 것 (파일 삭제 명령 대신 전부 API 로)**
- 블루베리 번호 시험 → `POST /api/revert_instances` → `instances_fixed/`·`masks_fixed/` 파일 **0개**, 편집 기록도 사라짐.
- 사과 번호 시험(라 갈래 스크립트 재실행) → 같은 API 로 원복, 파일 0개.
- 복숭아 0/255 시험(가 갈래 스크립트) → `/api/revert` + `/api/status` 로 원래 «ok / AI 3회 검수… » 복원.
- 복숭아 상자 시험(나 갈래 스크립트) → **빈 목록 저장**으로 되돌림(파일은 남김).

**남은 자취** — `260917_지울목록_시험잔재.md` 에 지울 명령까지 적어 두었습니다(내가 «덧붙임 2» 를 추가).

| 자취 | 상태 |
|---|---|
| `data/blueberry/status.json` | ⚠️ **원래 없던 파일** · 항목 1개(`Camera 1 Video (1)_1`, `unreviewed`, «번호 편집 되돌림») ← **2차가 새로 만든 것** |
| `data/blueberry/instances_fixed/` | ⚠️ **빈 폴더**(저장 때 생기고 되돌리기로 비워짐) ← **2차가 새로 만든 것** |
| `data/apple/status.json` | 원래 없던 파일 · 항목 1개(`…image361`) — 1차 갈래 것, 시각만 갱신 |
| `data/apple/{instances_fixed,masks_fixed}/` | 빈 폴더 |
| `data/peach/boxes/210629-t4-17.json` | 상자 **0개**. `by`·`at` 만 `검수C2B 되돌림 / 15:00:19` 로 갱신 |
| `data/peach/boxes_yolo/210629-t4-17.txt` · `boxes_all.json` | 0바이트 · `n_boxes 0` |
| `stage2/` 증거 | `bbsim.js/.out`·`ev_blueberry.py/.out`·`modesim.js/.out`·`expected_bb.json`·`save_body_bb.json`(26KB)·`inst_bb.png`·`bb_hdr_*.txt`. `bb_ck.txt` 는 로그인 쿠키가 들어 있어 **내용을 덮어썼습니다** |

**건드리지 않은 것**: 원본 데이터셋(`datasets_resized_2mp`·`datasets_reviewed_260916`)·`work/park_seongmoon/`
은 읽기만 했습니다. GPU 안 썼습니다. 새 패키지·외부 CDN 없습니다. 서버는 **한 번도 재시작하지 않았고**
계속 켜져 있습니다(PID **2911132**, 포트 5111).

## ⑧ 시험한 것 / 안 한 것 (사이클1 3차 ⑥-5 의 의무 표)

| 시험한 것 | 안 한 것 · 왜 |
|---|---|
| 다섯 갈래 시험 9개 재실행 · 내 새 시험 3개(블루베리 43개·모드/키 31개·블루베리 초벌 1195장 크기 전수) · 살아 있는 서버로 네 과일 `instance_info`·`/instances`·`instance_errors`·`list` 쪽 나눔 · 서버 기동 시각 대조 · 여섯 파일 갈래 간 diff · 최종 문법·라우트 점검 · `test_clean.py` 추가 검사 | **실제 브라우저 없음** — 마우스 사건·캔버스 그리기·flash 색·커서는 전부 함수 단위 시뮬레이션까지(⑥-1). **포도로 번호 편집** 안 함(원본에 번호가 없어 복숭아와 같은 갈래, `instance_info has:false`·`/instances` 404 만 확인). **블루베리 `--instances` 실제 내보내기**(1195장) 안 함 — dry-run 장수만. **사과 1,001장 전체 내보내기** 안 함(다 갈래가 3장으로 바이트 동일 확인). **두 사람이 같은 사진을 동시에 저장하는 경합** 안 함. D2(`dpr`) 안 함 |
