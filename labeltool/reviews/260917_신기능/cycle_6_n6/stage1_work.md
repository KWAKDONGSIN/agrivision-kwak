작성: 2026-09-17

# 0917 새 기능 — 별건 «N6 보충» · 1차 작업 (Opus 5)

대상: 5회 검수 최종 판정서(`cycle_5/stage3_final.md`)가 **유일하게 남은 미수정 결함**으로 지목한 **N6**
— «번호 저장» 이 «전경인데 번호 없는» 자리를 이진본에서 지우는 것. 판정서 **③-2 절의 지시를 그대로** 따랐습니다.
읽은 것: `cycle_5/stage3_final.md`(지시 본체) · `cycle_5/stage1_work.md` · `cycle_5/stage2_review.md` ·
`app/{instances,server,maskio}.py` · `export/export_dataset.py`(`check_cell`·`export_one`) · `app/README.md` §11 ·
`app/static/app.js`(`saveInstances` 자리만) · 회귀 시험 7개 원본(`n1_cut`·`n1_three_paths`·`n5_brush_vs_number`·
`export_coherence`·`export_inst`·`edge_seed`·`nonum_before_after`·`live_c5`)
근거 표기: 📄 내가 직접 실행·확인한 실측(경로·수치) · ❌ AI·우리 판단 · ✅ 논문 근거(이번엔 없음)

⛔ 지킨 것: 고친 파일은 **`app/instances.py` 와 `app/README.md` 둘뿐**입니다(판정서가 허락한 자리).
`계획서`·`kds0206/문서/`·`work/park_seongmoon/`·원본 데이터셋·`app.js`·`server.py`·`export_dataset.py` **손대지 않음**(고칠 문장은 ⑧).
**포도 안 켬** — 📄 재시작한 살아 있는 서버에서 `grape 1439`·`1564`·`peach 210629-t1-01` 전부 `has=False`(`live_c5.json`).
**프로젝트 파일 삭제 0** · GPU 0 · 새 패키지·외부 CDN 0 · 남의 프로세스 kill 0.
⛔ **브라우저 눈 확인은 0입니다** — 다섯 사이클 열다섯 단계에 이어 저도 0(⑥ 마지막 줄).

---

## ① 합격 기준 (먼저 쓰고 실행했습니다)

판정서 ③-2 «고치는 지시» + «검증 1~4» + 제 지시서의 두 금지(순환 논증·자기 통과)에서 그대로 옮겼습니다.

| # | 기준 | 결과 |
|---|---|---|
| **N6-1** | 백업을 **판정서가 지정한 이름**(`_backup_260917_c5c_instances.py`)으로 남긴다 | ✅ 📄 ③ |
| **N6-2** | 고치는 자리는 `app/instances.py` 의 `api_save_instances` **하나**, 식은 판정서 원문(`(arr>0) | (이진본 & 잘린번호본==0)`), 조건(`keep.shape != arr.shape` → 400)·응답 `hole_px` 포함 | ✅ 📄 ③ |
| **N6-3** | **읽기 규칙은 한 줄도 바뀌지 않는다**(`load_inst`·`mask_path_of`·`/instances`·`instance_info`·`boxes_seed`·내보내기) | ✅ 📄 ③ diff 가 쓰기 자리 밖을 건드리지 않음 |
| **N6-4** | 판정서 **검증 1**: `n1_cut.py` S4 뒤 «번호 저장(편집 없음)» → `masks_fixed` 전경 불변 · `hole_px=500` · manifest `hole=500` | ✅ 📄 ④-1 실패 0 |
| **N6-5** | 판정서 **검증 2**: `(144)_121` 저장 → 전경 **71,749** · `hole_px` **151** · manifest `hole=151`; 번호 204 지우고 저장 → 손실 **76** · hole 151 | ✅ 📄 ④-2 실패 0 |
| **N6-6** | 판정서 **검증 3**(회귀): `n5`·`n1_cut` S1~S5·`export_coherence`·`export_inst`·`nonum_before_after`(복숭아·포도 동일)·`edge_seed`(블루베리 `(1)_1` hole 0 → 왕복 그대로)·`live` 0.5초 미만 | ✅ 📄 ⑤ 전부 실패 0 |
| **N6-7** | 판정서 **검증 4**: **갱신한 단정을 고치기 전 코드에 대고 돌려 실패를 본다**(순환 논증 금지) | ✅ 📄 ④-4 **실패 15개** |
| **N6-8** | 🔴 «브러시로 지운 알이 되살아나지 않는다» 와 «번호 없는 전경이 지워지지 않는다» 가 **한 저장에서 동시에** 성립 | ✅ 📄 ⑥ 두 경로에서 |
| **N6-9** | «같다» 의 증거는 **서로 다른 코드 경로**에서 나오고 무엇이 독립인지 한 줄로 밝힌다 | ✅ ④-3 |
| **N6-10** | 기존 회귀 4종(`export_coherence`·`n5`·`export_inst`·`edge_seed`)을 **그대로**(바이트 동일) 돌려 실패 0 | ✅ 📄 ⑤ (md5 대조표) |
| **N6-11** | 포도 안 켬 · 공용 `data/` 손실 0 · 파일 삭제 0 · GPU 0 | ✅ 📄 ⑦ |

## ② 한 줄 결론

**N6 은 닫혔습니다** — «번호 저장» 은 이제 **번호가 있던 화소만** 이진본에서 뺍니다.
📄 판정서가 실측으로 제시한 네 시나리오가 그대로 재현됐습니다: 블루베리 `(144)_121` 아무것도 고치지 않고 저장 → **손실 151 → 0**,
`hole` 기록 **151 유지**(전에는 0으로 사라짐), manifest **`hole=151`**(전에는 빈칸); 번호 204 를 지우면 손실은 **정확히 그 76화소**;
브러시로 500화소 넓힌 사과는 **손실 500 → 0**. 그리고 2차가 걱정한 지점 — «브러시로 지운 알» 과 «번호 없는 전경» —
은 **한 번의 저장에서 동시에** 지켜집니다(📄 사과 `image1` 알 1 되살아남 0/1,034 + 넓힌 500화소 손실 0 · 블루베리 `(144)_121` 알 1 되살아남 0/1,161 + hole 151 손실 0).
자기 통과가 아님을 보이려고 **같은 단정을 고치기 전 코드에 대고 돌려 15개 실패**를 확인했고, 기존 회귀 4종+4종은 **전부 실패 0** 입니다.
남은 것은 **2차 독립 재현**(⑨)과 **브라우저 눈 확인 0**(여전히 아무도 못 봤습니다).

## ③ 바꾼 식과 줄 수

- 파일 **`app/instances.py`** 하나. 함수 **`api_save_instances`**(`:196-240`). 백업 **`_backup_260917_c5c_instances.py`**(판정서 ③-2 지정 이름, 282줄 그대로).
  📄 **282 → 297줄**. `diff` 로 정확히: 추가 **16줄**(실행 **11** · 주석 **5**) − 삭제 **1줄**(옛 `arr > 0` 저장 줄) = **순 +15**.
  자리별로는 쓰기 자리 14줄(그중 1줄이 옛 저장 줄을 대체) + 응답 칸 2줄. 판정서가 말한 «약 +6줄» 보다 조금 큰데, 늘어난 몫은 주석 5줄과 크기 불일치 400 처리 4줄입니다.
- 고친 자리(`_decode` 뒤, **`inst:` 자물쇠 안**, `write_u16_atomic` **앞** — `keep` 은 낡은 번호본을 덮기 **전에** 계산해야 합니다):

```python
        with lock_for("inst:%s:%s" % (fruit, stem)):
            # N6(0917 사이클6 보충): 쓰기 규칙을 읽기 규칙과 **같은 문장**으로 맞춘다 —
            # «번호가 있던 화소만» 이진본에서 뺄 수 있다. 저장 직전의 «이진본으로 자른» 번호본을
            # 읽어서, 번호가 **없던** 전경(블루베리 초벌의 구멍·브러시로 넓힌 자리)은 이진본에 그대로 둔다.
            # 브러시로 지운 알은 이진본에 *없으므로* keep 에 들어오지 않는다(되살아나지 않는다).
            try:
                cur, _, _ = load_inst(fruit, stem)          # 저장 직전의 «이진본으로 자른» 번호본
            except ValueError as e:
                return err_json(str(e), 400)
            keep = (load_mask_bool(mask_path_of(fruit, stem)) & (cur == 0)) if cur is not None \
                else np.zeros(arr.shape, bool)              # 번호 없던 전경 — 지키는 자리
            if keep.shape != arr.shape:
                return err_json("번호 마스크와 이진 마스크의 크기가 다릅니다(번호 %s · 이진 %s)."
                                % (arr.shape, keep.shape), 400)
            write_u16_atomic(p, arr)
            save_mask_atomic(fixed_path(fruit, stem), (arr > 0) | keep)   # 번호가 있던 화소만 뺀다
```
  그리고 응답에 `"hole_px": int(keep.sum())` 한 칸(판정서 원문). 새 import 0 — `np`·`load_mask_bool`·`mask_path_of`·`load_inst` 는 이미 이 파일에 있습니다.
- **읽기 규칙은 그대로**입니다 📄 `diff` 가 위 자리와 응답 칸 밖을 한 줄도 건드리지 않았습니다(`load_inst`·`mask_path_of`·`source_of`·`/instances`·`/api/instance_info`·`api_revert_instances` 본문 동일).
- 부수 효과(판정서 ③-2 마지막 줄 재확인): `cur is None` 인 **복숭아·포도는 `keep` 이 전부 거짓**이라 지금과 완전히 동일 — 📄 ⑤-5 에서 출력 대조로 확인.
- **`app/README.md`** §11 자르기 절에 **한 줄(3행)** 보완: «**«번호 저장» 은 번호가 있던 화소만 이진본에서 뺍니다**(0917 N6 보충) …
  브러시로 지운 알은 이진본에 *없으므로* 번호 저장으로 **되살아나지 않습니다.**» 📄 328 → 331줄. 백업 `_backup_260917_n6_README.md`.
  (판정서 ⑪ 이 README 에 요구한 «보충 뒤 문장» 을 그대로 넣은 것이고, 그 앞의 «⚠️ 지금은 지웁니다» 경고는 **필요 없어졌으므로 넣지 않았습니다**.)
- ⛔ **`app.js` 는 건드리지 않았습니다**(판정서가 «선택» 으로 둔 1줄). 그래서 `hole_px` 는 **API 응답에만 있고 화면에는 아직 안 보입니다** — 화면 문구는 다음 사람 몫(⑧).

## ④ 검증 4단계 결과 (판정서 ③-2 «검증»)

### ④-1 검증 1 — `n1_cut.py` S4 뒤에 «번호 저장(편집 없음)» 📄 `cycle_6_n6/stage1/n6_save.py|json` · **26단정 실패 0**

원본 `cycle_5/stage1/n1_cut.py` 를 한 줄도 고치지 않고(⑤ 에서 따로 돌림) **그 상태 뒤에 저장 단계를 넣은 사본**을 만들었습니다.
사과 `20150919_174151_image1` · 배경 500화소를 전경으로 넓힌 `masks_fixed`(원본 96,443 → 96,943).

| 단계 | 단정 | 📄 고친 코드 |
|---|---|---|
| S4 | 넓힌 상태에서 manifest `hole=500` · `instance_info hole_px=500` · 번호 95개 | 통과(사이클 5 와 같음) |
| **S4b** | 🔴 **아무것도 고치지 않고 «번호 저장»** → `masks_fixed` 전경 **96,943 그대로(손실 0)** | 통과 |
| 〃 | 저장 응답 `hole_px=500` · 저장 **뒤에도** `instance_info hole_px=500`(단서가 남는다) · 번호 95개 | 통과 |
| 〃 | 내보내기 manifest **`hole=500`** · 내보낸 이진본 전경 96,943 · 번호 없는 전경 500 · 유령 번호 0 | 통과 |
| **S4c** | 두 번째 저장(`instances_fixed` 가 이미 있는 상태)도 전경 불변·`hole_px` 500 (**안정**) | 통과 |
| **S4d** | 번호 하나(알 1 = 1,034화소)를 지우고 저장 → 빠진 화소가 **정확히 1,034**(넓힌 500 은 그대로) · 번호 95→94 | 통과 |
| **S7** | 🔴 **동시 성립** — ⑥ 표 참조 | 통과 |

### ④-2 검증 2 — 갈래4 를 판정서 단정으로 📄 `cycle_6_n6/stage1/n6_three_paths.py|json` · **42단정 실패 0**

2차가 실패 3개로 남긴 `cycle_5/stage2/n1_three_paths.py` 의 갈래4 를 판정서 기대값으로 채우고, 갈래1~3(회귀)은 그대로 뒀습니다.
📄 첫 단정으로 **격리**를 박았습니다: `/api/health` 의 `out_dir` = 사본 폴더. 아니면 **아무것도 쓰지 않고 중단**하도록 `sys.exit` 를 넣었습니다.

| 갈래 | 단정 | 📄 결과 |
|---|---|---|
| 1 (회귀) | 사과 `image451` 알 11(3,113) 브러시 삭제 → `/instances` 0화소 · 번호 62→61 · `cut_px=3113` | 통과 |
| 〃 | 번호 저장 뒤에도 알 11 **되살아남 0/3,113** · 이진본 전경 = 원본−알11−알26 = **68,534** | 통과 |
| 2 (회귀) | 번호 고친 사진의 마스크 되돌리기 **400** · 번호 되돌리기는 파일 둘·기록 함께 · 안 고친 사진은 **200** | 통과 |
| 3 (회귀) | **진짜 명령줄** 내보내기 — 유령 번호 0 · manifest `cut=2457 lost_ids=1` · 출처 칸 | 통과 |
| **4-0** | 독립3 로 센 값이 판정서 ③-1 과 같다 — 전경 **71,749** · hole **151** · 번호 204 = **76화소** | 통과 |
| **4-b** | 🔴 아무것도 고치지 않고 저장 → **사라진 전경 0화소**(2차에서는 151) · 전경 **71,749** | 통과 |
| **4-c** | 응답 `hole_px=151` · 저장 뒤 `instance_info hole_px` **151 → 151**(전에는 151→0) | 통과 |
| **4-d** | 명령줄 내보내기 manifest **`hole=151`**(전에는 빈칸) · 내보낸 이진본 전경 71,749 · 유령 0 | 통과 |
| **4-e** | 번호 204 지우고 저장 → 손실 **정확히 76** · 전경 **71,673** · hole **151 유지** · hole 화소 151개 중 지워진 것 **0** · 번호 204→203 | 통과 |
| **4-h** | 🔴 **동시 성립**(블루베리) — ⑥ 표 참조 | 통과 |

### ④-3 🔴 무엇이 독립인가 (한 줄씩)

- (독1) **서버**: ④-1 은 Flask `test_client`(같은 프로세스), ④-2 는 **따로 띄운 진짜 서버 프로세스**(사본 코드·사본 `data/`·포트 5198, `urllib` HTTP) — WSGI·라우팅·직렬화가 실제 경로입니다. 서버는 시험 스크립트가 띄우고 **자기 PID 만** SIGTERM 으로 껐습니다(`pkill` 안 씀 — 2차가 겪은 사고를 피했습니다).
- (독2) **내보내기**: ④-1 은 `export_one()` 직접 호출, ④-2 는 **진짜 명령줄** `export_dataset.py --fruit … --instances --out …`(`subprocess`).
- (독3) **기대값**: 우리 코드가 아니라 **팀 표준 원본 마스크**(사과는 마스크값 = 번호)와 **검출팀 초벌 파일**을 PIL 로 직접 세서 얻었습니다 — 71,749·151·76·1,161·1,034·3,113 전부. 해시는 근거로 쓰지 않았습니다.
- (독4) **해독기**: ④-1 은 내보낸 16비트 PNG 를 **PIL 없이 손으로 쓴 zlib+PNG 파서**로 읽고(독립1), 이진본은 PIL/maskio 로 읽어(독립2) 두 쪽을 맞댔습니다 — 인코더와 해독기가 다릅니다.
- (독5) **사진**: ④-1 사과 `image1`, ④-2 사과 `image451` + 블루베리 `(144)_121` — 같은 사진으로 두 번 세지 않았습니다.
- ⚠️ 정직하게: ④-1 과 ④-2 의 **시험 골격은 사이클 5 의 두 스크립트에서 물려받았습니다**(제가 새로 쓴 것은 저장 단계와 단정). 그래서 이 보고서만으로 «통과» 를 주장하지 않고 ⑨ 의 2차 재현을 남깁니다.

### ④-4 검증 4 — 같은 단정을 **고치기 전 코드**에 대고 📄 `n6_save_before.json` · **실패 15개 / 26단정**

`app/` 를 고치기 **전에** 사본 트리를 떠 두고(`scratchpad/n6_before/`, 📄 `instances.py` md5 `8a3d02fe…` = 백업과 동일),
**같은 시험 파일**을 `python n6_save.py <그 트리>` 로 돌렸습니다(`sys.path` 만 바꿉니다).

```
갱신된 단정 + 고치기 전 코드 → 실패 15개
   S4b 전경 96,943 → 96,443 (손실 500) · 응답 hole_px 없음(None) · 저장 뒤 hole_px 500→0
   S4b manifest hole=500 → 빈칸 · 내보낸 이진본 전경 96,443
   S4c 두 번째 저장도 손실 · S4d 손실 1,534(=1,034+500, 넓힌 자리까지 같이 사라짐)
   S7-나 번호 없는 전경 500화소 사라짐 (S7-가 «되살아남 0» 은 **고치기 전에도 통과** — N5-A 는 이미 닫혀 있었다)
갱신된 단정 + 지금 코드      → 실패 0개
```
❌ 판정: **단정을 낮춘 것이 아닙니다.** 이 시험은 수정을 실제로 재고 있고 고치기 전 코드를 정확히 잡아냅니다.
그리고 **S7-가가 양쪽에서 통과**하는 것이 중요합니다 — 제 수정이 «되살아남» 을 새로 깨뜨리지 않았다는 뜻입니다(⑥).

## ⑤ 회귀 결과 — 기존 시험을 **바이트 그대로**

📄 남의 폴더에 결과 json 을 덮어쓰지 않으려고 **내 폴더로 복사해서** 돌렸습니다. 코드는 한 바이트도 다르지 않습니다(md5 대조):

| 시험 | md5(원본 = 사본) | 어디서 왔나 | 📄 결과 |
|---|---|---|---|
| `export_coherence.py` | `d5a9b89ef68729af0277b404385884d4` | `cycle_4/stage2` | **실패 0** (`cut=1034 lost_ids=1` · 유령 0 · 어긋남 0화소) |
| `n5_brush_vs_number.py` | `5d4b7773cb42dd118caf89d00d6801bb` | `cycle_4/stage3` | **실패 0** (A 되살아남 0 · B 400 거절 · C 유령 0) |
| `export_inst.py` | `bc978635a6830f2fc41f8bbefab4dd5f` | `cycle_4/stage1` | **실패 0** — `apple blueberry`(번호 합 366, `(144)_121` → 실측 151 ↔ manifest `hole=151`) · `grape --n 6`(번호 합 26) |
| `edge_seed.py` | `c18357d65382d8010ba5188bd9fed16f` | `cycle_3/stage2` | **실패 0** — 살아 있는 서버 · 사과 `image361`(73개) · 블루베리 `(1)_1`(55개, hole 0) **왕복 그대로**(저장→3화소 규칙→0개→되돌리기→편집 전 복귀·파일 둘 다 사라짐·기록 없음) |
| `n1_cut.py` (S1~S5) | `9541caff4b890c988b43698deb83c39d` | `cycle_5/stage1` | **실패 0** (자르기·`hole=500` 기록·크기 불일치 400/`err:`) |
| `nonum_before_after.py` | `0e4d04dec6ff729dfee7d4386b6b8dc2` | `cycle_5/stage2` | **복숭아·포도 전/후 동일** — 고치기 전 트리와 지금 트리의 출력 `diff` 가 **임시 폴더 이름 4줄뿐**(값은 전부 동일: `has=False`·404 3종·`boxes_seed`·브러시 저장 `fg_pixels`·`masks_fixed` 생성/삭제·되돌리기 200·내보낸 전경 화소·manifest) |
| `live_c5.py` | `c2ad6fea6adb66ff8c44e97b261de63f` | `cycle_5/stage1` | **실패 0** — 재시작한 살아 있는 서버. 포도·복숭아 `has=False`(⛔ 안 켬) · 사과 95/95·62↔61 · `(144)_121` `hole_px=151` · 초벌 5회 **0.187~0.204초**(기준 0.5초 미만) · `/instances` 16비트 IHDR |

추가로 📄 `python -m py_compile app/instances.py` 통과 · 제 시험 3개도 `py_compile` 통과.
⚠️ **edge_seed 는 공용 `data/` 에 쓰고 되돌리는 시험**입니다(2차가 그래서 건너뛴 것). 판정서 검증 3 이 이름을 박아 두었고
제 지시서도 «4종을 그대로» 라고 했으므로 **돌렸습니다.** 남은 자취는 ⑦ 에 정확히 적었습니다.

## ⑥ 🔴 «두 성질 동시 성립» 증거 (2차가 걱정한 지점)

2차 ⑥-2 의 걱정은 «(나) 안은 브러시로 지운 알과 번호 없는 전경을 **구별**해야 하는데 그 구별은 사람 결정이다» 였습니다.
판정서 ③-2 의 ❌ 판단은 «구별은 이미 정의돼 있다 — 지운 알은 이진본에 **없고**, 번호 없는 전경은 이진본에 **있다**» 였습니다.
📄 그 판단이 맞는지, **한 사진·한 저장에 두 상황을 함께 넣어** 두 경로에서 쟀습니다.

| | ④-1 S7 (test_client · 사과 `image1`) | ④-2 4-h (진짜 HTTP 서버 · 블루베리 `(144)_121`) |
|---|---|---|
| 만든 상태 | 브러시로 알 1(1,034화소) **지우고** + 배경 500화소 **넓힌** 이진본 (전경 95,909) | 브러시로 알 1(1,161화소) **지우고** + 초벌의 구멍 151화소가 원래 있는 이진본 (전경 70,588) |
| 저장 전 진단 | `cut_px=1034` · `hole_px=500` (두 방향이 동시에 있다) | `cut_px=1161` · `hole_px=151` (동시) |
| 저장(편집 없음) 뒤 **(가)** | 지운 알 되살아난 화소 **0 / 1,034** | 지운 알 되살아난 화소 **0 / 1,161** |
| 〃 **(나)** | 번호 없는 전경 사라진 화소 **0 / 500** | hole 화소 사라진 것 **0 / 151** |
| 이진본 전경 | **95,909 그대로** | **70,588 그대로** |
| 응답·기록 | `hole_px=500` · 저장 뒤 `instance_info` `hole_px=500`·`cut_px=0` | `hole_px=151` · manifest **`hole=151`** |
| 내보내기 | 유령 번호 0 · 번호 없는 전경 500 · 번호 집합에서 알 1 만 빠짐(94개) | 유령 0 · 번호 없는 전경 151 · 전경 70,588 (명령줄 내보내기) |

❌ 왜 이것이 «동시에» 성립하는가(한 줄): `keep` 은 **이진본 전경 ∩ (잘린 번호본 == 0)** 이라서,
브러시로 지운 자리는 «이진본 전경» 조건에서 탈락하고, 번호 없는 전경은 «번호본 0» 조건으로 정확히 걸립니다.
두 집합이 애초에 겹치지 않으므로 사람 결정이 필요 없습니다 — 📄 위 두 줄이 그 실측입니다.
그리고 ④-4 에서 **S7-가는 고치기 전에도 통과**했으므로, 제 수정이 (나)를 얻으면서 (가)를 깨뜨린 것이 아닙니다.

**⛔ 브라우저 눈 확인 0**: 위 전부 서버 쪽 바이트입니다. `hole_px` 를 화면에 띄우는 줄은 넣지 않았으므로 눈으로 볼 것도 아직 없습니다.

### ⑥-2 내가 먼저 재 본 반례 후보 — «구멍 위에 `N`» (판정서 ⑩-1 이 Codex 에게 물으라고 한 것)
📄 `n6_hole_on_n.py|json`(4단정 실패 0): 구멍 500화소에 `N` 으로 새 번호(96)를 붙여 저장하면
**전경 손실 0**·번호 95→96 으로 맞지만, **응답 `hole_px` 는 500 이고 저장 뒤 `instance_info hole_px` 는 0** 입니다.
❌ 판정: **화소는 어느 쪽도 잃지 않습니다**(`keep` 은 union 이므로). 다만 두 숫자의 **뜻이 다릅니다** —
응답은 «저장 직전에 번호가 없던 전경», 조회는 «지금 남은 구멍». 판정서가 준 식(`int(keep.sum())`)을 **그대로** 썼고,
화면에 띄울 때는 «방금 이진본에 그대로 둔 화소» 로 읽어야 합니다(문구는 ⑧ 에 적었습니다). 다른 반례는 못 찾았습니다:
`D`·`X`(나누기 선 1~3px)·`M`·`N`·두 번째 저장·번호 0개 저장(edge_seed)·크기 불일치(400) 전부 위 시험들에 들어 있습니다.

### ⑥-3 고치지 **않은** 것 (판정서·2차가 이미 «별건» 으로 둔 것)
- 자물쇠 이름 `inst:` ↔ `mask:`(2차 ⑥-3c · 판정서 ⑩-3): 제 수정은 «저장 직전에 이진본을 다시 읽» 지만 **경쟁 자체를 없애지 못합니다** —
  브러시 저장이 `keep` 계산과 `save_mask_atomic` 사이에 끼어들면 여전히 lost update 입니다(창이 좁아졌을 뿐). ❌ 한 사람 전제에서 무해 · Codex 질문 3 그대로 유지.
- `layer=gt` 도 잘린다(판정서 ⑩-4) · PNG 해독기 · `api_revert_instances` 두 번 쓰기 — 전부 «유지 확정» 이라 손대지 않았습니다.

## ⑦ 남긴 자취 (⛔ 삭제는 사람 — CLAUDE.md 3항)

**공용 `data/`** 📄 지문 전/후 대조(`cycle_5/stage2/fingerprint.py` 그대로 실행):
- `masks_fixed`·`instances_fixed` — 사과 **0/0** · 블루베리 **0/0** · 포도 0 · 복숭아 39(예전 것) → **전/후 완전히 같습니다. 실손실 0.**
- `status.json` **항목 수 2·1·183·125 전/후 같음**. 값이 바뀐 것은 **`edge_seed` 가 건드린 두 항목의 `at` 시각 한 칸**뿐입니다:
  `apple/20150919_174151_image361` `17:53:34 → 18:49:01` · `blueberry/Camera 1 Video (1)_1` `17:53:37 → 18:49:04`
  (`by`=«AI 사이클3 2차 경계시험» · `note`=«번호 편집 되돌림» · `status`=`unreviewed` 는 그대로 — 1차 자취와 **같은 두 항목이고 새 항목은 0개**입니다).
  sha256 은 그래서 `c7d6f07b…→f3317215…`·`d98d2b47…→9cf87d9d…` 로 바뀌었습니다. 포도·복숭아 `status.json` 은 **바이트 동일**.
- 시험이 만든 `masks_fixed`·`instances_fixed` 는 `edge_seed` 가 스스로 되돌렸습니다(📄 «번호본·이진본 파일 둘 다 사라졌다» 통과).
**살아 있는 서버**: 판정서 ③-2 반영을 위해 **재시작했습니다** — PID **3141139 → 3211490**(18:48:23 기동 > `instances.py` 18:41:50 · README 18:45:54).
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
**`/tmp`**: `export_inst.py` 는 스스로 안 지웁니다 → `c4_expinst_*` 가 9 → **11개**(제 것 `fcbvq4mj`·`2paz2tor`). 나머지 제 시험은 전부 자기 임시 폴더를 지웠습니다(`c6_n6save_*`·`c6_n6http_*`·`c6_holeN_*`·`c4b_coh_*`·`c4c_n5_*`·`c5_n1cut_*`·`c5b_nonum_*` 남은 것 0).
→ 지울목록 덧붙임 제안: `rm -rf /tmp/c4_expinst_*` (11개). 그 밖에 제가 만든 `/tmp` 잔재는 없습니다.
**새 폴더** `cycles/260917_신기능/cycle_6_n6/` — 이 보고서 + `stage1/` 에 새 시험 3개(`n6_save.py`·`n6_three_paths.py`·`n6_hole_on_n.py`)·
결과 json 10개·기존 시험 사본 8개(md5 대조용)·`__pycache__`.
**세션 임시**(프로젝트 밖, `scratchpad/`): `n6_before/`(고치기 전 코드 트리 — ⑨ 재현용)·`n6_nonum_{before,after}.json`.
**삭제**: 프로젝트 파일 **0건**. (제 시험 스크립트가 `os.remove` 를 부르는 곳은 **자기 임시 폴더 안**뿐입니다 — `n6_save.py` S7 의 상태 초기화 1회.)

## ⑧ 제 담당이 아닌 문서에서 고칠 문장 (⛔ 저는 손대지 않았습니다 — 곽동신 몫)

판정서 ⑪ 표를 **N6 이 닫힌 뒤 기준**으로 갱신한 것입니다. ⚠️ 모두 **2차 독립 재현(⑨) 뒤에** 반영하는 것이 안전합니다(판정서 ③-3 «1차 고침 → 2차 재현»).

| 파일 | 지금 | 바꿀 것 |
|---|---|---|
| `kds0206/문서/260917_툴_사람확인_체크리스트.md` H 2번째 | (판정서 ⑪) «블루베리에서는 N6 보충 수정 전까지 «번호 저장» 을 누르지 마세요 … 사과에서는 브러시로 **넓힌** 뒤 번호 저장을 하지 마세요» | 🔴 **임시 규칙 세 줄 전부 해제 대상** → «번호 저장은 **번호가 있던 화소만** 이진본에서 뺍니다(N6 보충, 1차 실패 0 · 2차 재현 뒤 해제). 블루베리 번호 저장·브러시로 넓힌 뒤 저장 **둘 다 됩니다**. 넓힌 자리는 번호가 없는 채로 남고 내보낼 때 `hole` 로 찍힙니다 — `N`/`M` 으로 붙여 주세요» |
| 〃 H 마지막 «(포도 번호를 켠 뒤에만)» | 전제 넷 중 **④ N6 보충 재현 = 미충족** | «④ **1차 완료(실패 0)** — 2차 재현 뒤 충족» (켜기 자체는 여전히 교수님 8번 뒤) |
| 〃 G 1번째 · H 5번째 | «실측 0.12초» · «0.1초» | «**0.2초**» (📄 이번 재측정 0.187~0.215초, 판정서 ⑦ 과 같음) |
| 〃 새 줄(A 묶음 맨 위) | — | 판정서 ⑪ 의 «🔴 알마다 색이 칠해지는가» 그대로 (제 수정과 무관, 여전히 1순위) |
| `cycles/260917_0917신기능_5회검수_계획.md` 「사이클 5 진행 상태」 | «최종 판정서 작성 중» | 판정서 ⑪ 문장 + **«별건 «N6 보충» 1차 완료(2026-09-17, `cycle_6_n6/stage1_work.md`) — 검증 4단계·회귀 8종 실패 0, 고치기 전 코드에서 15개 실패 확인. 2차 독립 재현 대기»** |
| 〃 「산출물 위치」 | — | `cycles/260917_신기능/cycle_6_n6/`(1차 보고서·시험 3개·결과 json) 를 덧붙임 |
| `kds0206/문서/260917_교수님확인_7건.md` 8번 뒤 | «(툴 쪽 준비는 끝났고, 켜는 것은 N6 보충 재현 뒤)» | 그대로 두고 «(N6 보충 **1차 완료** — 2차 재현 뒤)» 로 한 단어만 |
| `kds0206/문서/260917_카톡_상자와번호기능.txt` 다음 카톡 | N6 임시 규칙 두 줄 | «번호 저장이 구멍을 지우던 문제(N6) **고쳤습니다** — 번호 저장은 번호가 있던 화소만 뺍니다. 블루베리 번호 저장 보류는 2차 확인 뒤 풀 예정» + 사진 이름 오기 정정(`20150921_131234_image451`)은 판정서 ⑪ 그대로 |
| 곽동신 → 박성문 | «번호 저장이 그 구멍을 지우던 문제(N6)는 보충 수정 예정 — 그 전까지 블루베리 번호 저장 보류» | «**고쳤습니다**: 저장해도 구멍(`(144)_121`=151화소)은 마스크에 그대로 남고 manifest `hole` 도 그대로 찍힙니다. 번호를 지우면 그 번호 화소만(204 = 76화소) 빠집니다. → **블루베리 초벌 재생성은 손실 방지 목적으로는 불필요**하고, 25알(21장)은 사람이 `N` 으로 붙이는 쪽이 맞습니다(판정서 ⑤ 권고와 같음)» |
| `260917_지울목록_시험잔재.md` 덧붙임 6 | 판정서 ⑪ 이 적은 `/tmp` 16개 | 📄 지금 `c4_expinst_*` 는 **11개**(제 회귀가 2개 늘림) → `rm -rf /tmp/c4_expinst_*` · `data/{apple,blueberry}/status.json` 두 항목은 **여전히 두 개**(제 `edge_seed` 가 `at` 만 갱신) |
| `app/README.md` §11 | — | ✅ **제가 이미 고쳤습니다**(③ 마지막 줄). 판정서 ⑪ 이 요구한 «⚠️ 지금은 지웁니다» 경고는 **넣지 않았습니다**(이미 사실이 아니므로) |
| (새로) `app/static/app.js` `saveInstances` | `번호 N개 저장 완료` · `앞면 화소 …` | ❌ 선택 1줄: «전경인데 번호 없음 `j.hole_px`화소 — 이진본에 그대로 둠». ⚠️ 그 숫자는 «**저장 직전**» 기준입니다(⑥-2) — «방금 그대로 둔 화소» 로 읽히게 쓰세요 |

## ⑨ 2차 독립 재현 절차 (제 시험을 한 줄도 쓰지 말 것)

1. **고친 것 확인**: `diff app/_backup_260917_c5c_instances.py app/instances.py` → 쓰기 자리 14줄·응답 2줄 추가에 옛 저장 줄 1줄 삭제(순 +15) 뿐인지. 읽기 함수 본문이 그대로인지.
2. **자기 잣대로 다시 재기**(권고 — 제 스크립트를 쓰지 마세요): 아직 아무도 안 쓴 사진으로 **새 시험**을 쓰십시오.
   후보 📄: 블루베리 `Camera 3 Video (52)_421`(hole **5,887** — 가장 큰 어긋남) · `Camera 4 Video (79)_1`(hole 1,151) · 사과 `20150919_174151_image161`(번호 123, hole 0).
   기대값은 **원본 마스크·초벌 파일을 직접 세서**(`cycle_5/stage2/count_hole_direct.py` 방식) 얻고, 저장 뒤 `masks_fixed` 전경이 **저장 전과 같은지**만 보면 됩니다.
3. **제 시험을 그대로 재현**하려면(보조): `cd cycles/260917_신기능/cycle_6_n6/stage1`
   `python n6_save.py` (26단정 실패 0 기대) · `python n6_three_paths.py` (42단정 실패 0 기대 — 포트 5198 을 쓰며 서버를 스스로 띄우고 끕니다. 포트가 막혀 있으면 `N6_PORT=5197`).
4. **순환 논증 차단 재확인**: 고치기 전 트리를 백업에서 다시 만들고 같은 시험을 대 보십시오 — **실패 15개**가 나와야 합니다.
   ```
   mkdir -p /tmp/n6chk && cp -r app export /tmp/n6chk/ && cp app/_backup_260917_c5c_instances.py /tmp/n6chk/app/instances.py
   python n6_save.py /tmp/n6chk        # 실패 15개가 나와야 «시험이 수정을 재고 있다»
   ```
5. **회귀 8종**(⑤ 표) 를 md5 대조한 사본으로 다시 돌려 실패 0. `edge_seed` 는 공용 `data/` 에 쓰므로 **돌릴지 말지 먼저 정하고**, 돌렸으면 두 항목의 `at` 만 바뀌었는지 지문으로 확인하십시오.
6. **판정할 것 3개**: (가) 응답 `hole_px` 의 뜻이 «저장 직전» 인 것을 그대로 둘지, 조회와 같은 «남은 구멍» 으로 바꿀지(⑥-2) ·
   (나) `app.js` 1줄을 이번에 넣을지 · (다) 체크리스트·카톡의 임시 규칙 해제를 2차 이름으로 승인할지(⑧).
7. **⛔ 여전히 금지**: 포도 켜기(교수님 8번 전) · 공용 `data/` 삭제 · 남의 프로세스 kill. 브라우저 눈 확인은 **아직 0** 이라는 문장을 지우지 마십시오.
