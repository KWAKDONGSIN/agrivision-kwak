작성: 2026-09-17

# 0917 새 기능 5회 검수 — 사이클 3 · 2차 검수 (Opus 5)

주제: **AI 초벌이 네 과일 모두에서 맞는가**
읽은 것: `cycle_3/stage1_work.md` · `cycle_2/stage3_final.md` · `cycles/prompts/stage2_c3.md` ·
박성문 지시서 · `app/{boxes,instances,server,dupes,maskio}.py` · `app/static/app.js` · `export/export_dataset.py` ·
`app/README.md` · `cycle_3/stage1/{count_table,api_seed,roundtrip}.py` · 검출 팀 `semantic-segmentation/tools/{grape_gt,make_yolo_grape_labels,prepare_certh_grape}.py`
근거 표기: 📄 내가 직접 실행한 실측(경로·수치 명시) · ❌ AI 판단 · ✅ 논문 근거(이번엔 없음)

내 시험 스크립트·결과는 전부 `cycle_3/stage2/` 에 있습니다(1차 파일은 **읽기만** 했고 한 줄도 고치지 않았습니다).

---

## ① 합격 기준 (먼저 적고 그다음 실행했습니다)

| # | 기준 | 무엇이 나오면 통과인가 | 결과 |
|---|---|---|---|
| **A1** | 1차의 개수 주장이 **1차와 다른 사진**에서도 선다 | 사과·블루베리 각 **5장 이상**(1차 표본 제외)에서 «초벌 상자 수 = 내가 따로 센 4화소 이상 번호 수» | ✅ 📄 각 6장, 실패 0 (③-1) |
| **A2** | 상자가 개수만이 아니라 **위치**도 맞다 | 상자 좌표가 «그 번호 화소의 실제 최소·최대» 와 **완전히 같다**(집합 비교) | ✅ 📄 20장 전부 일치 |
| **A3** | 한 번호가 **잎에 가려 여러 조각**일 때 상자가 조각 전체를 감싼다 | 조각난 번호가 있는 사진에서 그 번호의 상자가 조각 전체의 경계와 같다 | ✅ 📄 사과 `image6` 조각난 번호 4개 전부 (④-1) |
| **A4** | 번호가 **고쳐진** 사진에서 초벌이 `instances_fixed` 를 쓴다 | 응답 `source="instances:fixed"` + 개수·좌표 일치 | ✅ 📄 사과·블루베리 (③-4) |
| **A5** | 경계에서 안 깨진다 | 번호 1개 → 상자 1개 · 번호 0개 → 500 아님 · 4화소 미만 번호는 빠짐 | ✅ 📄 실패 0 (③-5) |
| **A6** | 번호 없는 과일은 **예전 경로 그대로** | 복숭아·포도에서 `source=ai`·`gt`·모르는 값 셋 다 내가 센 연결성분과 일치 | ✅ 📄 (③-6) |
| **A7** | 3~4초 동안 **다른 요청이 막히지 않는다** | 느린 초벌이 도는 중 가벼운 요청이 평소 시간 안에 온다 | ✅ 📄 0.11초 유지 (④-3) |
| **A8** | 기존 검사가 하나도 안 깨진다 | `modesim 31/31` · `boxsim 50/50` · `sim 21/21` · `test_clean` · `boxes.py` 자체 점검 통과 | ✅ 📄 (③-7) |
| **A9** | 포도 ㉠ 을 **근거를 들어** 판정한다 | 「포도를 번호로 셀 자료가 있는가」에 실측으로 답한다 | ✅ 📄 **있습니다** (⑥) |
| **A10** | 시험 자취를 되돌리고 남은 것을 적는다 | `instances_fixed`·`masks_fixed` 파일 0, 남은 것은 보고서에 | ✅ 📄 (⑧) |

## ② 한 줄 결론

1차의 다섯 주장은 **전부 재현됐고**(1차가 안 본 사진 20장·전수 2,196장 포함) **확실한 버그는 0건이라 코드를 한 줄도 고치지 않았습니다.** 그러나 1차 결론 중 **하나는 뒤집힙니다 — 포도는 «개수로 못 쓰는 과일» 이 아닙니다**: 검출 팀 `grape_gt.py` 가 CERTH 송이 인스턴스를 **2,502장 9,832송이** 라벨맵으로 이미 만들어 주고(📄 직접 실행), 1차가 «정답» 으로 쓴 YOLO txt 가 바로 **그 라벨맵에서 뽑은 상자**입니다. 또 ㉡ 속도는 3~4초가 최악이 아니라 **7.6초**(블루베리 204번호)이고, `find_objects` 로 바꾸면 **0.07초·결과 동일**(51~107배)임을 서버 밖에서 실측했습니다.

## ③ 재현 결과 표 — 1차의 주장 / 내 확인 방법 / 판정

### ③-1 주장 1: 초벌이 번호 마스크를 먼저 쓴다 (사과 411=411, 블루베리 363=363)

| 1차 주장 | 내 확인 방법 | 판정 |
|---|---|---|
| 사과 10장 411개 = 411개, 블루베리 10장 363개 = 363개 | 1차 표본을 **전부 제외**하고 고른 새 사진으로 `cycle_3/stage2/seed_check2.py` — 번호는 내가 `bincount` 로 따로 세고, 초벌은 돌고 있는 서버에 실제 호출 | ✅ **재현** |

📄 새 표본(1차와 겹치지 않음), **개수·좌표 모두 일치, 실패 0**:

| 과일 | 사진 | 내가 센 번호(4화소↑) | 초벌 상자 | 응답 source | 좌표 일치 |
|---|---|---:|---:|---|---|
| 사과 | `20150919_174730_image6` | 36 | **36** | `instances:gt` | ✅ |
| 사과 | `20150921_131346_image816` | 24 | **24** | `instances:gt` | ✅ |
| 사과 | `20150921_131625_image686` | 37 | **37** | `instances:gt` | ✅ |
| 사과 | `20150921_132038_image1076` | 13 | **13** | `instances:gt` | ✅ |
| 사과 | `dataset1_front_1141` | 21 | **21** | `instances:gt` | ✅ |
| 사과 | `dataset2_front_750` | 3 | **3** | `instances:gt` | ✅ |
| **사과 합** | **6장** | **134** | **134** | | |
| 블루베리 | `Camera 1 Video (48)_61` | 23 | **23** | `instances:seed` | ✅ |
| 블루베리 | `Camera 1 Video (96)_181` | 14 | **14** | `instances:seed` | ✅ |
| 블루베리 | `Camera 2 Video (30)_1` | 75 | **75** | `instances:seed` | ✅ |
| 블루베리 | `Camera 3 Video (121)_4081` | 10 | **10** | `instances:seed` | ✅ |
| 블루베리 | `Camera 3 Video (59)_481` | 9 | **9** | `instances:seed` | ✅ |
| 블루베리 | `Camera 3 Video (88)_1` | 29 | **29** | `instances:seed` | ✅ |
| **블루베리 합** | **6장** | **160** | **160** | | |
| 복숭아 | `210629-t2-07` · `-of12-02` · `-of17-02` · `t3-17` | 22 · 4 · 9 · 8 (AI 제안 연결성분) | **22 · 4 · 9 · 8** | `ai` | ✅ |
| 포도 | `1442` · `1891` · `2350` · `547` | 7 · 13 · 2 · 6 (AI 제안 연결성분) | **7 · 13 · 2 · 6** | `ai` | ✅ |

### ③-2 전수 확인 — 1차는 각 10장 표본, 나는 **2,196장 전부** 다시 셌습니다

📄 `cycle_3/stage2/scan_ids.py` (사과 1,001 + 블루베리 1,195, 원본은 읽기만):

| | 사과 1,001장 | 블루베리 1,195장 |
|---|---|---|
| 번호 마스크가 있는 사진 | **1,001 / 1,001** | **1,195 / 1,195** |
| 번호 총합 | **40,468** (지시서 §1 «40,468» 과 일치) | **45,124** (사이클2 실측과 일치) |
| 장당 최대 번호 | **123** (`20150919_174151_image161`) | **204** (`Camera 1 Video (144)_121`) |
| 장당 중앙값 | 37 | 27 |
| 번호 마스크 크기 ≠ 사진 크기 | **0장** | **0장** |
| AI 제안이 없는 사진 | **0장** | **0장** |
| 번호 0개인 사진 | **0장** | **0장** |
| 3,000개(상자 상한) 초과 | **0장** | **0장** |
| **번호 수 ≠ 상자 수인 사진** | **1장** 🔴 `20150921_131234_image451` (번호 62 ↔ 상자 61) | **0장** |

📄 그 1장을 **서버에 직접 물어** 확인했습니다: 패널의 «번호 62개» ↔ 초벌 상자 **61개**(4화소 미만 번호 1개가 규칙대로 빠짐). 즉 «번호 = 상자» 는 **1,001장 중 1,000장에서** 맞습니다 → ⑤-R4.

### ③-3 주장 2: 고친 번호의 내보내기 왕복이 sha256 동일

| 1차 주장 | 내 확인 방법 | 판정 |
|---|---|---|
| 저장→`--instances`→비교가 sha256까지 동일(사과·블루베리) | 1차 `roundtrip.py` 를 **임시 폴더만 내 작업칸으로 바꿔** 그대로 재실행(`C3_TMP=<scratchpad>`) | ✅ **재현** (실패 0) |

📄 두 과일 모두 «화소 단위 동일» · sha256 `f13c63ea16710d57…` / `f7879cb9b1048845…` · `I;16` · `manifest.csv` = `instances_fixed` · 되돌린 뒤 파일 0·기록 0. **다만 sha256 동일은 증거로서 약합니다 → ⑤-R3.**

### ③-4 주장 (1차가 «안 해 봤다» 고 적은 것): 고친 번호(`fixed`)로 초벌

📄 `cycle_3/stage2/edge_seed.py` — 번호 편집 기록이 **이미 있는** 사진만 써서(공용 `status.json` 에 새 항목을 만들지 않으려고) 저장 → 초벌 → 되돌리기:

| 확인 | 사과 `20150919_174151_image361` | 블루베리 `Camera 1 Video (1)_1` |
|---|---|---|
| 편집 전 초벌 | 73개 `instances:gt` | 55개 `instances:seed` |
| 번호 1개만 남겨 저장 → 초벌 | **`instances:fixed` · 1개** ✅ | **`instances:fixed` · 1개** ✅ |
| 그 상자 = 그 번호의 실제 경계 | ✅ `[426,240,484,296]` | ✅ `[1362,870,1386,908]` |
| 되돌린 뒤 초벌 | 73개 `instances:gt` ✅ | 55개 `instances:seed` ✅ |
| 파일·status 잔재 | 없음 ✅ | 없음 ✅ |

### ③-5 경계 (1차가 안 본 것)

📄 같은 스크립트, 실패 0:

| 경계 | 결과 |
|---|---|
| 번호가 **1개**뿐 | 상자 1개, 좌표 정확 ✅ (전수에도 실재: 사과 6장·블루베리 6장) |
| 번호 **0개**(전부 지운 마스크) | 저장됨 · 초벌은 **200 응답에 `n=0`**(500 아님), 화면은 «마스크에 덩어리가 없어…» 안내만 ✅ |
| **3화소** 번호 | 초벌에서 **빠짐**(4화소 규칙) — 그런데 패널의 «번호 수» 는 **2** 로 보임 🔴 사람이 «상자가 하나 모자라다» 고 느낄 자리 (④-2) |
| 번호가 있는데 **AI 제안이 없는** 사진 | 📄 **그런 사진은 0장**(사과·블루베리 전수) — 경계가 자료에 없음 |

### ③-6 주장 1의 나머지: 복숭아·포도는 고치기 전과 같은 수

📄 `cycle_3/stage2/legacy_path.py` — 화면이 보내지 않는 값까지 넣어 봤습니다:

| 사진 | `source=ai` | `source=gt` | `source=inst`(모르는 값) | 내가 센 것 |
|---|---|---|---|---|
| 복숭아 `210629-t1-01` | 11 (`ai`) | 12 (`gt`) | 12 (`gt`) | AI 11 · GT 12 ✅ |
| 포도 `1439` | 17 (`ai`) | 9 (`gt`) | 9 (`gt`) | AI 17 · GT 9 ✅ |

✅ 예전 경로가 그대로입니다. ❌ 다만 **번호가 있는 과일에서는 `source` 파라미터가 완전히 무시됩니다** — 📄 사과·블루베리 12장에 `source=ai` 로 불러도 전부 `instances:gt`/`instances:seed` 로 같은 수가 나왔습니다. 판정서 지시(번호 우선)대로라 **버그는 아니지만**, 이제 그 파라미터는 번호 없는 과일에서만 뜻이 있습니다 → ⑦-4.

### ③-7 주장 3·4: 문장 7곳 · ponytail · `Ctrl+Y` · `boxsim.js` 교체

| 1차 주장 | 내 확인 | 판정 |
|---|---|---|
| 문장 7곳 정정 | 📄 `[사이클3 정정]` 표시 5곳(`cycle_2/stage1_work.md` 3 · `stage2_review.md` 2) + README 2곳(표시는 없지만 diff 로 확인: §10 초벌 블록 4줄 교체, §11 새 번호 규칙 1줄 추가) = **7곳** | ✅ 내용도 내 실측과 맞음 |
| `Ctrl+Y` 1줄 + `modesim.js` 검사 1줄 | 📄 `app.js:774` 에 `else if (S.boxMode) flash(...)` 확인, `modesim.js:146` 이 «`click:redo` 0회 **그리고** 안내 뜸» 을 **둘 다** 검사 | ✅ (검사가 «관찰» 에서 «단정» 으로 제대로 바뀜) |
| ponytail 정확히 **−7줄** | 📄 파일 합계는 −7이 맞지만 **구성이 다릅니다** | ⚠️ 반박 ⑤-R2 |
| 낡은 `boxsim.js` 교체(50/50) | 📄 다시 돌려 50/50. 지운 방어 분기가 정말 불필요한지 따로 확인 | ✅ **교체가 타당**, ⑤-동의 참조 |

📄 내가 다시 돌린 검사 (전부 종료코드 0):
```
node --check app/static/app.js                                   → OK
python3 app/boxes.py                                             → 자체 점검 통과
python3 -m py_compile app/{boxes,instances,server}.py export/export_dataset.py → OK
node cycles/260917_신기능/cycle_2/stage2/modesim.js               → 31/31
node cycles/260917_신기능/cycle_2/stage1_client/boxsim.js         → 50/50
(cd cycles/260917_신기능/cycle_2/stage1 && node sim.js)           → 21/21
python cycles/260917_신기능/cycle_1/stage1/test_clean.py          → 통과
python cycles/260917_신기능/cycle_3/stage1/roundtrip.py           → 실패 0
python cycles/260917_신기능/cycle_3/stage2/seed_check2.py         → 20장, 실패 0
python cycles/260917_신기능/cycle_3/stage2/edge_seed.py           → 실패 0
python cycles/260917_신기능/cycle_3/stage2/legacy_path.py         → 실패 0
서버: PID 2970927 (15:33 기동, 원본 폴더 datasets_reviewed_260916) — 기동 이후 **500 은 0건**
```

## ④ 새로 찾은 문제와 고친 내용

⛔ **고친 코드는 없습니다.** 아래 어느 것도 «확실한 버그» 가 아니라고 판단했으므로(지시: 확실한 버그만) 코드를 건드리지 않았고, 따라서 `_backup_260917_c3b_*` 사본도 **만들지 않았습니다.** 고쳐야 할 것은 3차가 결정하도록 ⑦ 에 근거와 함께 올립니다.

### ④-1 ✅ 조각난 번호도 상자 하나로 잘 감쌉니다 (1차가 못 한 확인)

📄 사과 `20150919_174730_image6` 에 **조각이 2개인 번호가 4개** 있습니다(잎에 가림). 그 4개 전부 «두 조각 전체를 감싸는 상자 하나» 로 나왔습니다. 1차 표본 3장(`image1`·`image146`·`Camera 1 Video (1)_1`)에는 조각난 번호가 **하나도 없어** 이 성질이 시험되지 않았습니다 — 이번에 메웠습니다.

### ④-2 🔴 «번호 수» 와 «상자 수» 가 다를 수 있고 화면이 그 이유를 말하지 않습니다

📄 전수에서 실재: 사과 `20150921_131234_image451` 번호 62 ↔ 상자 61. 초벌 안내는 «원본 번호 마스크에서 **61개** 초벌» 이라고만 하고, 번호 패널은 «번호 **62**개» 입니다. ❌ 판단: 기능 오류가 아니라 **4화소 규칙**(`boxes.py:141`)의 결과이고, 고칠 것은 코드가 아니라 **문장**입니다 → ⑦-5.

### ④-3 🔴 속도 — 최악은 3~4초가 아니라 **7.6초**이고, 화면이 «멈추지는» 않습니다

📄 `cycle_3/stage2/bench_seed.py` (서버 밖, 서버 코드 안 고침):

| 사진 | 번호 | PNG 읽기 | **지금 루프** | `find_objects`+`bincount` | 배수 | 결과 같음 |
|---|---:|---:|---:|---:|---:|---|
| 사과 `image1` | 95 | 0.02초 | **3.60초** | 0.068초 | 53배 | ✅ |
| 블루베리 `(99)_1` | 102 | 0.02초 | **3.89초** | 0.076초 | 51배 | ✅ |
| 사과 `image161`(사과 최대) | 123 | 0.01초 | **4.65초** | 0.073초 | 64배 | ✅ |
| 블루베리 `(144)_121`(**전수 최대**) | 204 | 0.02초 | **7.60초** | 0.071초 | **107배** | ✅ |

- 📄 시간은 **PNG 읽기가 아니라 루프**입니다(읽기는 0.02초). 원인은 1차 진단대로 번호마다 `np.where(lab == v)` 로 2MP 배열을 다시 훑는 것이고, 번호 수에 **비례**하므로 204번호에서 7.6초가 됩니다.
- 📄 **막히지는 않습니다** (`server.py:1110 threaded=True`): 느린 초벌이 도는 동안 `/api/instance_info` 응답은 0.112~0.120초(평소 0.108초). 같은 초벌을 **동시에 2번**(단추 두 번 누른 상황) 눌러도 벽시계 3.72초로 둘 다 95개를 정상 반환하고, 서로 다른 무거운 초벌 2개도 3.92초에 함께 끝났습니다.
- ❌ 다만 **화면 단추에는 잠금이 없습니다**: 📄 `app.js:seedBoxes` 에 진행 중 가드가 없고 `#boxseed` 를 `disabled` 로 만들지 않습니다. 두 번 누르면 계산이 두 번 돌고 `bpush()` 가 두 번 불려 **되돌리기 스택에 2칸**이 쌓입니다(같은 결과로 덮어쓰므로 그림은 같습니다). **브라우저가 없어 «7.6초 동안 커서·캔버스가 어떻게 보이나» 는 확인하지 못했습니다** → ⑦-3.

### ④-4 🔴 지울 목록에 **빠진** 자취 1건

📄 `data/grape/boxes/740.json`(상자 0개, `by=자동시험 정리(검수C1B)`, 14:04) · `data/grape/boxes_all.json` · `data/grape/boxes_yolo/740.txt` 가 있는데 `260917_지울목록_시험자료.md`·`_시험잔재.md` **어디에도 적혀 있지 않습니다**(두 파일에 `grape/boxes`·`740` 문자열 0건). 사이클 1~2 의 상자 시험 잔재로 보입니다. ⛔ 삭제는 사람이 — ⑦-7 에 명령안을 적었습니다.

## ⑤ 반박

**R1 🔴 «포도 초벌은 개수로 못 쓴다 / 포도에는 번호 마스크가 없다» — 뒷부분이 틀렸습니다.**
1차 ③-3 은 «진짜로 포도를 세려면 포도도 **번호 마스크가 있어야** 하고, 검출 팀 **YOLO 라벨**을 초벌로 끌어오는 길이 있다» 고 적었습니다. 📄 실제로는 **포도의 송이 인스턴스 정답이 이미 있고**(CERTH COCO RLE), 검출 팀 `grape_gt.py` 가 그것을 **라벨맵으로 만들어 주는 함수까지** 갖고 있습니다. 내가 직접 실행한 결과 `grape_gt.totals()` → **(2,502장, 9,832송이)**. 그리고 1차가 «정답» 으로 쓴 YOLO txt 는 📄 `make_yolo_grape_labels.py:35-44` 가 **그 `label_map()` 에 `find_objects` 를 걸어 뽑은 상자**입니다. 즉 1차는 **정답의 «상자» 만 보고 그 원본인 «인스턴스 마스크» 를 놓쳤습니다.** «연결성분으로는 원리적으로 못 센다» 는 앞부분은 맞고(동의), «못 쓰는 과일» 이라는 결론은 **과장**입니다 → ⑥.

**R2 ⚠️ «ponytail 정확히 −7줄, 판정서가 요구한 −7줄과 같다» — 숫자는 맞지만 구성이 섞였습니다.**
📄 diff 실측: `boxes.py` +13(전부 새 기능·주석) · `instances.py` **−6** · `app.js` **−1**. 파일 합계는 −7 이 맞습니다. 그런데 `app.js` 의 −1 은 **ponytail −4**(`saveBoxes` 방어 분기: 8줄 삭제·4줄 추가)와 **새 기능 +3**(`SEEDKO` 표)이 상쇄된 값입니다. 따라서 **ponytail 삭제만 세면 −10줄**(instances −6 + app.js −4)이고, 판정서가 예측한 배분(app.js −3 · `MAX_ID` −2 · `seed_path` −2 = −7)과는 다릅니다. «요구한 −7과 정확히 같다» 는 **우연의 일치**이므로 그 문장은 «ponytail 로 −10줄을 지우고 새 기능으로 +16줄이 늘어 파일 합계 −7» 로 고쳐 적는 것이 정확합니다.

**R3 ⚠️ «내보낸 번호본 = `instances_fixed` (sha256 바이트 동일)» — 참이지만 증거로서는 약합니다.**
📄 `export_dataset.py:129-130` 은 `read_u16(inst_src)` 로 읽어 **같은 `write_u16_atomic`(= 같은 `png_u16_bytes`)** 으로 다시 씁니다. 같은 배열을 같은 인코더에 넣었으니 바이트가 같은 것은 «인코더가 결정적이다» 는 뜻이고, 번호가 보존됐다는 **의미 있는 증거는 화소 단위 비교**(`exp == ed`)입니다 — 그것도 1차가 같이 했으므로 결론 자체는 유효합니다. 보고 문장을 «화소 단위로 같고, 같은 인코더라 바이트까지 같다» 로 낮춰 적기를 권합니다.

**R4 ⚠️ «사과는 번호 = 상자 10/10» — 표본에서는 맞지만 전수에서는 1,001장 중 1장이 예외입니다.**
📄 `20150921_131234_image451` 번호 62 ↔ 상자 61(서버에 직접 확인). 4화소 미만 번호가 규칙대로 빠진 것이라 **버그는 아니지만**, README·체크리스트의 «번호마다 상자 하나» 는 «(4화소 미만 번호는 빠집니다)» 한 마디가 있어야 참입니다. 블루베리는 📄 1,195장 전부 예외 없음(1차 주장대로).

**R5 ⚠️ «번호 많은 사진 초벌이 3~4초» — 전수 최악은 7.6초입니다.**
📄 블루베리 `Camera 1 Video (144)_121`(204번호). 1차 표본 최대가 102번호였기 때문에 과소평가됐습니다. 사람 체크리스트에 «3~4초» 로 적으면 실제로 7~8초를 겪는 사람이 «멈췄다» 고 판단할 수 있습니다.

**동의(각 한 줄).** ㉠ 초벌이 `source_of` 한 군데에 맡긴 것 · 루프를 안 건드린 것 · 응답에 `source` 를 실은 것: 재현했고 동의합니다. ㉡ 복숭아·포도 무변경: 동의(③-6). ㉢ `Ctrl+Y` 수정과 검사: 동의. ㉣ 되돌리기·`status.json` 청소: 동의(③-4). ㉤ **`boxsim.js` 교체: 동의합니다** — 📄 `saveBoxes` 는 `j.ok` 가 아니면 이미 되돌아가고, 서버 `api_boxes_save` 는 D3 계약대로 언제나 `boxes` 를 실으며(📄 `boxes.py:112`), 그 계약은 **서버 쪽 검사**(`test_clean.py` 의 «응답 boxes 길이 3»)가 지키고 있습니다. 즉 시험이 약해진 것이 아니라 **지켜야 할 곳으로 옮겨진 것**입니다. 지운 방어 분기는 되살릴 필요가 없습니다.

## ⑥ 포도 판정 (㉠)

**판정: «포도 초벌을 개수로 쓸 수 없다» 는 지금 코드에 대해서는 맞지만, «포도는 셀 수 없다» 는 틀렸습니다. 다만 YOLO 라벨을 번호 마스크로 쓰는 것은 ❌ 아닙니다 — 그 라벨의 원본인 CERTH 송이 인스턴스를 써야 합니다.**

**(1) YOLO txt 를 번호 마스크로 쓰면 안 되는 이유** ❌
- YOLO txt 는 **상자**(cx,cy,w,h)일 뿐 마스크가 아닙니다. 번호 마스크로 만들려면 상자를 채워야 하고, 📄 포도는 송이가 서로 겹쳐 상자끼리 포개지므로(1차 표에도 «붙은 송이» 사례 `1656`) 겹친 화소가 어느 번호인지 정할 수 없습니다. 번호 편집 화면은 «화소마다 번호 하나» 를 전제로 합니다.
- 상자를 채우면 잎·배경까지 송이 번호가 되어 **세그 팀 이진본**(`save_instances` 가 같이 쓰는 `masks_fixed`)이 망가집니다.

**(2) 맞는 길 — CERTH 송이 인스턴스를 초벌 번호로** 📄 내가 실측한 근거:

| 포도 사진 | CERTH 송이 | YOLO 줄 | 우리 이진 마스크 연결성분(4px) | 화소 일치(우리∩CERTH / 우리) |
|---|---:|---:|---:|---:|
| `1439` (1차의 «1송이를 17개») | **1** | 1 | 9 | 0.983 |
| `1656` | **5** | 5 | 5 | 0.996 |
| `0` | **8** | 8 | 15 | 0.993 |
| `1` | **5** | 5 | 8 | 0.995 |
| `10` | **6** | 6 | 9 | 0.994 |
| `318` | **6** | 6 | 6 | 0.997 |
| `2345` | **5** | 5 | 9 | 0.995 |
| `1219` | **2** | 2 | 1 | 0.998 |

- 📄 **CERTH 송이 수 = YOLO 줄 수**(8장 전부) → 1차의 «정답 51송이» 는 그대로 유효하고, 그 정답을 **마스크 형태로** 쓸 수 있다는 것이 새로 확인된 사실입니다.
- 📄 해상도: 원본 3840×2160 → 팀 표준 1920×1080 으로 **정확히 1/2**, `label_map` 이 정수배 축소를 하므로 어긋나지 않습니다. 📄 표본 120장 443송이에서 **우리 해상도에서 4화소 미만이 되어 사라지는 송이 0개**.
- 📄 장당 최대 송이 **18개**(`1564`), 평균 3.93 → uint16 으로 충분하고, 3,000개 상한에도 한참 못 미칩니다.

**(3) 그러려면 무엇이 필요한가 (구체적으로)**
1. **생성 스크립트 1개** — 입력: `CERTH_annotations.zip`(읽기 전용) + `grape_gt.label_map(stem, H, W)`. 출력: 2,502장 **uint16 L 모드 PNG**(`instances.py:write_u16_atomic` 재사용). 쓰는 곳은 **우리 폴더**(`work/kwak_dongsin/…`) — 📄 `work/park_seongmoon/` 은 읽기 전용이고 `ANN_DIR` 은 존재하지 않아 zip 에서 바로 읽어야 합니다(`grape_gt` 가 이미 그렇게 합니다).
2. **`app/instances.py:SEED_DIRS` 에 `"grape": <그 폴더>` 한 줄.** 그러면 `source_of` → `seed` 경로가 그대로 잡혀 초벌·편집·내보내기(`export --instances`)·`instance_info` 가 **블루베리와 똑같이** 동작합니다. 코드 수정은 이 한 줄뿐입니다(📄 사이클 3 이 규칙을 `source_of` 한 군데로 모아 둔 덕).
3. **넘어야 할 것(사람 결정거리)** ❌ 📄 CERTH 라벨맵과 우리 이진 마스크는 화소가 **98.3~99.8% 만** 겹칩니다(완전 일치가 아님). 번호 마스크와 이진 GT 가 어긋나면 편집 화면에 «전경인데 번호가 없는 화소»·«번호가 있는데 전경이 아닌 화소» 가 생깁니다. 블루베리 초벌(watershed)은 우리 마스크에서 만들어 이 문제가 없었습니다. **어긋남을 그대로 둘지, 우리 마스크로 잘라 맞출지(`lab[~mask] = 0`)는 사람이 정해야 합니다.**
4. 같은 자료 상태인 **복숭아**(지시서 §1: COCO 인스턴스 977 + 세그 팀 보강 83)도 같은 방식이 가능합니다. 확인은 안 해 봤습니다(이번 주제 밖).

**❌ 내 권고**: «할지» 는 사람 결정이지만 **기술적으로는 한 줄 + 스크립트 하나**이고, 네 과일 전부가 «번호 = 상자» 가 되므로 카운팅 벤치마크(지시서 §7-3)에 곧바로 쓰입니다. 사이클 4(내보내기) 와 묶는 것이 맞다고 봅니다.

## ⑦ 3차 최종이 결정할 항목

1. **속도 — `find_objects` 로 바꿀지.** 📄 근거: 0.07초·결과 동일(51~107배), 최악 7.6초. 내가 고치지 않은 이유: 판정서의 «지금 루프 그대로» 지시를 2차가 뒤집는 것은 권한 밖이고, 느린 것은 버그가 아닙니다. **바꾸기로 하면** `boxes.py:137-147` 을 `bincount`+`find_objects` 로(4화소 규칙·상자 순서 유지), 사이클 4 또는 5.
2. **포도 CERTH 인스턴스 도입 여부 — 사람 결정** ❌ (⑥). 함께 정할 것: 라벨맵과 우리 마스크의 어긋남을 자를지.
3. **초벌 단추 두 번 누름 잠금(1줄) 여부.** 📄 서버는 막히지 않지만 계산이 두 번 돌고 되돌리기 스택에 2칸 쌓입니다. 고치려면 `seedBoxes` 에 진행 중 플래그 1줄. 브라우저 확인이 없으니 사람 체크리스트에 «7~8초 걸리는 사진에서 단추를 두 번 눌러 보기» 를 넣는 편이 먼저입니다.
4. **`source` 파라미터의 뜻.** 번호가 있는 과일에서는 완전히 무시됩니다(📄 12장 확인). 그대로 둘지, `ponytail:` 주석 한 줄로 «번호 없는 과일에서만 쓰인다» 를 적을지.
5. **문장 고칠 곳 (⛔ 제 담당 아님 — 그대로 쓰실 수 있게 적습니다)**
   - `app/README.md` §10 «열매 번호가 있는 과일(사과·블루베리)은 번호마다 상자 하나입니다» → 뒤에 «**(4화소 미만인 아주 작은 번호는 상자에서 빠집니다 — 사과 1,001장 중 1장이 그렇습니다: `20150921_131234_image451` 번호 62 ↔ 상자 61.)**» 추가.
   - 계획서 `260917_0917신기능_5회검수_계획.md` 의 «사이클 3 · 1차 완료» 블록 중 ㉠ 줄 «**포도 초벌은 개수로 쓸 수 없습니다** … 버그가 아닙니다» → «**지금 코드의 포도 초벌은 개수로 쓸 수 없습니다**(연결성분의 원리적 한계). 다만 2차 검수에서 **포도 송이 인스턴스 정답(CERTH, 2,502장 9,832송이)이 이미 있고 검출 팀 `grape_gt.py` 가 라벨맵으로 만들어 준다는 것**이 확인됐습니다 — 끌어올지는 사람 결정(사이클 4).» 로.
   - 같은 블록 «㉡ 초벌이 **3~4초**» → «㉡ 초벌이 번호 수에 비례해 오래 걸립니다 — **최악 7.6초**(블루베리 204번호). `find_objects` 로 바꾸면 0.07초(결과 동일).»
   - 같은 블록 «ponytail **−7줄**» → «ponytail **−10줄** · 새 기능 +16줄 · 파일 합계 −7줄».
   - `/data/project/2026summer/kds0206/문서/260917_툴_사람확인_체크리스트.md` (⛔ 제 담당 아님): 1차가 ⑦(5)에서 올린 초벌 4줄에 더해 **«블루베리 `Camera 1 Video (144)_121` 에서 초벌 → 상자 204개, 7~8초 걸린다(그 사이 화면이 멈추는지·단추를 두 번 누르면 어떻게 되는지 본다)»** 와 **«사과 `20150921_131234_image451` 에서 번호 패널은 62개인데 초벌 상자는 61개다(아주 작은 번호는 빠짐)»** 두 줄 추가.
6. **1차의 ⑦(1)~(5)** 는 내가 검증한 범위에서 전부 타당합니다 — R1·R5 만 위 문장으로 바꿔 반영하시면 됩니다.
7. **지울 목록 보강(④-4).** `260917_지울목록_시험잔재.md` 에 아래를 덧붙여야 합니다(⛔ 실행은 사람):
   ```bash
   D=/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/data
   # 4) 포도 상자 시험 잔재 (사이클1~2, 상자 0개 — 지금까지 어느 목록에도 없었음)
   rm -f "$D/grape/boxes/740.json" "$D/grape/boxes_yolo/740.txt" "$D/grape/boxes_all.json"
   rmdir "$D/grape/boxes" "$D/grape/boxes_yolo" 2>/dev/null
   ```

## ⑧ 내가 남긴 자취 · 시험하지 않은 것

**공용 `data/` 에 남은 것** 📄 (파일 삭제는 하지 않았습니다 — CLAUDE.md 3항)
- `data/apple/status.json` — 항목 **2개 그대로**(새 항목 없음). 내 경계 시험이 `20150919_174151_image361` 의 `by`·`at` 를 «AI 사이클3 2차 경계시험 / 15:52:10» 으로 **갱신**했고, 1차 `roundtrip.py` 재실행이 `20150919_174151_image1` 의 `at` 을 15:56:11 로 갱신했습니다.
- `data/blueberry/status.json` — 항목 **1개 그대로**(`Camera 1 Video (1)_1`), `by`·`at` 만 갱신(15:56:12).
- 번호본·이진본 파일은 **전부 되돌리기 API 로 삭제**됨 📄 `data/{apple,blueberry}/instances_fixed`·`data/apple/masks_fixed` 는 **빈 폴더**, `.tmp` 잔재 0건. **새 삭제 명령은 필요 없고 기존 1)~3) 명령이 그대로 덮습니다.**
- 📄 `/tmp/c3_roundtrip_y0h0ldih` (1차가 남긴 것)이 **아직 있습니다** — 기존 목록의 `rm -rf /tmp/c3_roundtrip_*` 로 지워집니다. 내 재실행분은 `/tmp` 가 아니라 세션 작업칸(`…/scratchpad/c3b_roundtrip`, 164KB)에 만들어 공용 폴더·`/tmp` 에는 아무것도 늘리지 않았습니다.
- 내가 만든 파일: `cycles/260917_신기능/cycle_3/stage2/` 안의 스크립트 6개와 결과 3개(`scan_ids.json`·`seed_check2.json`·`grape_certh.json`) + 로그 1개. 3차가 그대로 다시 돌릴 수 있습니다.
- 원본 데이터셋 · `work/park_seongmoon/` · 검출 팀 CERTH zip 은 **읽기만** 했습니다. GPU 안 씀 · 새 패키지 없음 · 외부 CDN 없음 · 서버 재시작 안 함(1차가 띄운 PID 2970927 그대로 씀).

**시험하지 않은 것 (정직하게)**
| 안 한 것 | 왜 |
|---|---|
| **브라우저에서 눈으로 본 것 — 하나도 없음** | 브라우저가 없습니다. 상자가 «알 위에» 놓이는지는 좌표가 번호 경계와 **완전히 같다**는 것까지만 확인했고(A2·A3), 화면 렌더링·7.6초 동안의 체감·단추 두 번 누름의 화면 반응은 **사람이 봐야** 합니다 |
| 사과·블루베리 초벌의 **API 전수 호출** | 번호 통계는 전수(2,196장) 실측이지만 `/api/boxes_seed` 는 32장만 호출했습니다(1차 40장 + 내 새 표본 20장 중 겹치지 않는 것). 규칙이 `source_of` 한 곳이라 표본으로 충분하다고 봤습니다 ❌ |
| 포도 **CERTH 인스턴스를 실제로 PNG 로 만들어 툴에 붙여 보기** | ⑥ 의 3단계는 «사람 결정» 이라 자료 검증(개수·해상도·겹침)까지만 했습니다. 만들지 않았습니다 |
| 복숭아 COCO 인스턴스 | 이번 주제 밖 |
| `find_objects` 로 **서버를 실제로 바꿔** 본 것 | 서버 밖에서 결과 동일·시간만 재고 코드는 건드리지 않았습니다(⑦-1) |
