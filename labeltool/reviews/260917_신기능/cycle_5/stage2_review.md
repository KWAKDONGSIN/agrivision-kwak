작성: 2026-09-17

# 0917 새 기능 5회 검수 — 사이클 5 · 2차 검수 (Opus 5)

주제: **1차의 N1 수정(P0)을 가장 세게 검증** + 낡은 단정 ③ 판정 + 번호 없는 과일 회귀 + `server.py`·`app.js` 자백건.
읽은 것: `cycle_5/stage1_work.md` · `cycle_4/stage3_final.md`(지시 본체) · 사이클 1~4 전 보고서(정정 표 포함) · `cycles/prompts/stage2_c5.md` ·
`app/{instances,boxes,server,dupes,maskio}.py` 와 그 `_backup_260917_c5_*` 백업 · `app/static/app.js`(+백업) · `export/export_dataset.py`(+백업) ·
`scripts/make_grape_instances.py`(+백업) · `app/README.md`(+백업) · `cycle_4/stage3/n5_brush_vs_number.py`(+백업) · `cycle_4/stage1/export_inst.py` ·
`cycle_4/stage2/{export_coherence,bb_quant}.py|json` · `cycle_5/stage1/{n1_cut,n4_stale,live_c5}.py|json`
근거 표기: 📄 내가 직접 실행·확인한 실측(경로·수치 명시) · ❌ AI 판단 · ✅ 논문 근거(이번엔 없음)

⛔ 지킨 것: **포도 번호를 켜지 않았습니다**(📄 살아 있는 서버 `instance_info` — `grape 0`·`grape 2345`·`peach 210629-t1-02` 전부 `has=False`,
`/instances` 와 `layer=gt` 는 404). `app/instances.py:25 SEED_DIRS` 안 건드림. **파일 삭제 0 · GPU 0 · 새 패키지·외부 CDN 0.**
`계획서`·`kds0206/문서/`·`work/park_seongmoon/`·원본 데이터셋은 **읽기만** 했습니다.
📄 **공용 `data/` 변화 0** — 제 검수 전후 지문이 완전히 같습니다(`cycle_5/stage2/fingerprint_{before,after}.json`, 4과일 파일 수·`status.json` sha256·항목 수 전부 동일).
고친 것은 **1건**이고 고치기 전 `_backup_260917_c5b_export_inst.py` 를 남겼습니다.
⛔ **브라우저 눈 확인은 저도 0입니다** — 다섯 사이클 아홉 단계 전부 0이라는 1차 ⑦-(가)는 📄 사실입니다(제 단계까지 열 단계 0).

---

## ① 합격 기준 (먼저 쓰고 실행했습니다)

| # | 기준 | 결과 |
|---|---|---|
| **G1** | N1 세 갈래가 **1차의 시험을 한 줄도 쓰지 않는 경로**에서도 막혀 있다 | ✅ 📄 ③ (17단정 전부 통과) |
| **G2** | 1차가 갱신한 수락 시험이 «통과하도록 낮춘 것» 이 아니다 — **같은 단정을 고치기 전 코드에 대고 돌리면 실패해야** 한다 | ✅ 📄 ③-3 **실패 6개** (고친 코드에서는 0) |
| **G3** | 낡은 단정 ③ 을 판정하고, «설계상 달라야 하는 151화소» 를 **내가 직접** 센다 | ✅ 📄 151 확인 → **(가)로 고침** ⑥-1 |
| **G4** | 번호 없는 과일(복숭아·포도)의 저장·되돌리기·판정·내보내기가 사이클 5 **전/후 동일** | ✅ 📄 ④-1 (출력 diff = 새 칸 1개뿐) |
| **G5** | «유지 확정» 3건(PNG 해독기·두 번 쓰기·`maskio`)을 1차가 건드리지 않았다 | ✅ 📄 ④-4 (본문 바이트 동일) |
| **G6** | 자백건 — `server.py`·`app.js` 의 +8줄이 무엇이고 필요했는지, 다른 것을 깨지 않았는지 | ✅ 📄 ⑦-4 (타당) |
| **G7** | 1차의 문서·숫자 대조표를 표본으로 재확인 | ✅ 📄 ⑤ (1건 오류 발견 — sim.js) |
| **G8** | 공용 `data/` 변화 0 · 포도 안 켜짐 · 남의 프로세스 kill 0 · 삭제 0 | ✅ 📄 위 ⛔ |
| **G9** | 살아 있는 서버가 **최종 코드**를 돌고 있다 | ✅ 📄 PID 3141139, 17:53:18 기동 > 코드 17:44~17:46 |
| **G10** | 표준 시험 세트가 그대로 통과한다 | ✅ 📄 ④-5 |

## ② 한 줄 결론

**N1 세 갈래는 정말로 막혔습니다** — 1차의 시험을 전혀 쓰지 않는 경로(따로 띄운 진짜 서버 + 진짜 명령줄 + 다른 사진)에서 17단정 전부 통과했고,
1차가 갱신한 단정은 고치기 전 코드에서 6개나 실패하므로 «시험을 낮춘 것» 이 아닙니다. 1차의 «151화소는 설계상 달라야 한다» 도 📄 **맞습니다**(내가 직접 셈) —
낡은 단정 ③ 은 1차 권고대로 **(가)** 로 제가 고쳤고 이제 회귀 4종이 **전부 실패 0** 입니다.
그러나 같은 뿌리의 **네 번째 갈래가 열려 있습니다(N6 🔴)**: «번호 저장» 이 «이진본은 전경인데 번호가 없는» 자리를 이진본에서 **조용히 지우고 그 기록까지 없앱니다**
(📄 블루베리 `(144)_121` — 아무것도 고치지 않고 저장만 눌러 전경 **151화소 손실**, `hole_px` 151→**0**, manifest 칸 **빈칸**).
사이클 4 가 확정한 «채우지 않고 **기록만** 한다» 는 읽기에서만 지켜지고 쓰기에서는 **거꾸로**입니다.

## ③ N1 세 갈래 검증 결과 — 1차와 다른 경로

🔴 **무엇이 독립인가 (한 줄씩)**
- (독1) 서버: 1차는 전부 Flask `test_client` (같은 프로세스). 저는 **따로 띄운 진짜 서버 프로세스**(사본 코드·사본 `data/`·포트 5199)에 **HTTP(urllib)** 로 불렀습니다 — WSGI·라우팅·쿠키·직렬화가 실제 경로입니다. 📄 `/api/health` 의 `out_dir` 가 사본 폴더임을 시험 첫 단정으로 박았습니다(공용 `data/` 격리 증명).
- (독2) 내보내기: 1차는 `EX.export_one()` **직접 호출**. 저는 **진짜 명령줄** `export_dataset.py --fruit apple --instances --out …` 를 `subprocess` 로 돌렸습니다(argparse·main·경로 조립 전부 지나감).
- (독3) 기대값: 우리 코드가 아니라 **팀 표준 원본 마스크**를 PIL 로 직접 세서 얻었습니다(사과 마스크값 = 번호). 해시는 근거로 쓰지 않았습니다.
- (독4) 사진: 1차·사이클 4 의 세 시험은 **전부 사과 `image1`**. 저는 **`20150921_131234_image451`**(번호 62개)과 블루베리 `(144)_121` 을 썼습니다.
- 시험 파일: `cycles/260917_신기능/cycle_5/stage2/n1_three_paths.py` (`n1_three_paths.json`)

| 갈래 | 단정 | 📄 결과 |
|---|---|---|
| **1. 브러시로 지운 알이 번호 저장에서 되살아나는가** | 브러시로 알 11(3,113화소) 지움 → 저장된 전경 = 원본−3,113 (71,536) | 통과 |
| 〃 | `/instances` 가 알 11 을 **0화소**로 준다 · 번호 수 62 → **61** | 통과 (전에는 3,113화소) |
| 〃 | `instance_info` `cut_px`=**3,113**(내가 원본에서 센 수와 일치) · `hole_px`=0 | 통과 |
| 〃 | 번호 모드에서 알 26 만 지워 «번호 저장» → `masks_fixed` 에서 알 11 이 **되살아난 화소 0/3,113** | 통과 🔴 |
| **2. 마스크 되돌리기** | 번호를 고친 사진 → **400** «이 사진은 열매 번호를 고친 사진입니다…» · 번호본·이진본 파일 둘 다 그대로 · `instances_edited` 기록 그대로 | 통과 🔴 |
| 〃 | «번호 되돌리기» 는 `['instances_fixed','masks_fixed']` 를 함께 치우고 기록도 지운다 | 통과 |
| 〃 | ⚠️ **회귀**: 번호를 고치지 **않은** 사진의 마스크 되돌리기는 **예전처럼 200·removed=True** (거절이 정상 흐름을 막지 않음) | 통과 |
| **3. 낡은 번호 파일 → 유령 번호** | 번호 편집 뒤 브러시로 알 29 를 또 지워 번호본을 낡게 만든 다음 **명령줄** 내보내기 → 유령 번호 **0개** · «이진본에 없는 번호» 화소 **0** | 통과 🔴 |
| 〃 | manifest `instances_check` = **`cut=2457 lost_ids=1`** (내가 원본에서 센 2,457과 일치) · 출처 칸 `masks_fixed`/`instances_fixed` | 통과 |

### ③-3 «시험을 손본 것 아닌가» — 결정적 확인

1차는 수락 시험 `n5_brush_vs_number.py` 를 **고쳤습니다**(📄 142→149줄, 백업 `_backup_260917_c5_n5_brush_vs_number.py` 와 `diff`):
A 의 단정을 «아직 준다(1,034화소)» → «**0화소로 준다**» 로, B 의 세 단정을 «되돌리기가 파일을 지운다» → «**400 으로 거절되고 파일·기록이 그대로**» 로 뒤집고 주석 5줄을 넣었습니다.
**단정을 지운 것은 없고 방향이 뒤집힌 것**이며, 사이클 4 판정서 ③-4(c)·검증 2 가 지시한 대로입니다.
그래도 «통과하도록 낮춘 것» 일 수 있으므로 📄 **같은(갱신된) 시험을 고치기 전 코드에 대고 돌렸습니다**
(`cycle_5/stage2/n5_on_before_code.py` — `sys.path` 만 백업 사본 트리로 돌림):

```
갱신된 단정 + 고치기 전 코드  → 실패 6개  (A 두 개 · B 세 개 · C 한 개. 알 1 이 1,034화소 그대로 되살아남 · revert 200 · 유령 번호 1개)
갱신된 단정 + 지금 코드        → 실패 0개
```
❌ 판정: **단정은 낮춰지지 않았습니다.** 이 시험은 수정을 실제로 재고 있고, 고치기 전 코드를 정확히 잡아냅니다(순환 논증 아님).

## ④ 회귀 확인

**④-1 번호 없는 과일(복숭아·포도) — 사이클 5 전/후 «출력 대조»** 📄 `cycle_5/stage2/nonum_before_after.py`
백업 사본으로 «고치기 전 트리», 지금 파일의 바이트 동일 사본으로 «고친 뒤 트리» 를 만들어 **같은 시나리오**를 각각 돌리고 출력을 `diff` 했습니다
(복숭아·포도 각 2장: `instance_info` → `/instances` 세 레이어 → `boxes_seed` → 브러시 저장 → 다시 조회 → 되돌리기 → `--instances` 내보내기).
📄 **차이는 두 가지뿐이고 둘 다 의도된 것**입니다: manifest 헤더에 `instances_check` 칸 1개 추가 · 화면 요약 줄 1개 추가.
그 밖의 모든 값이 동일했습니다 — `has=False`·`n=0`, `/instances`·`layer=gt`·`layer=fixed` **404**, `boxes_seed` 개수·출처, 브러시 저장의 `fg_pixels`·`status`,
`masks_fixed` 생성/삭제, 되돌리기 `200 removed=True`·note «수정본 되돌림», 내보낸 마스크 전경 화소 수, `instances/` 폴더가 비는 것까지.
→ ❌ **복숭아·포도 동작은 사이클 1 이전과 같습니다.**

**④-2 살아 있는 서버(읽기 전용, 1차가 안 본 사진들)** 📄 `cycle_5/stage2/live_c5b.py`
```
grape 0 · grape 2345 · peach 210629-t1-02      → has=False n=0 · /instances 404 · layer=gt 404   (⛔ 포도 안 켜짐 재확인)
apple 20150919_174151_image161  n=123 cut=0 hole=0 src=gt   | 초벌 상자 123개 0.199초
apple dataset1_back_751         n=30  cut=0 hole=0 src=gt   | 초벌 상자  30개 0.201초
blueberry Camera 3 Video (13)_661  n=5   hole=0            | 상자 5개 0.205초
blueberry Camera 3 Video (52)_421  n=9   hole=**5887**      ← 2차 bb_quant.json 의 only_mask 와 같은 수(다른 경로)
```
**④-3 회귀 4종** 📄 `export_inst.py apple blueberry` **실패 0**(단정 고친 뒤 — ⑥-1) · `export_inst.py grape --n 6` **실패 0** ·
`grape_seed_api.py` 실패 0(포도를 프로세스 안에서만 켬) · `api_seed.py`(살아 있는 서버 40장) **기대와 다른 것 0장** ·
`export_coherence.py` 실패 0(`cut=1034 lost_ids=1`) · `n5` 실패 0 · `n1_cut.py` 실패 0(S1~S5) · `n4_stale.py` 3/3.
⚠️ `edge_seed.py` 는 **일부러 돌리지 않았습니다** — 그 시험이 공용 `data/{apple,blueberry}/status.json` 두 항목을 또 갱신하기 때문입니다(1차 자취 참조). 대신 «마스크 되돌리기 회귀» 를 격리 서버에서 직접 쟀습니다(③ 갈래2 마지막 줄).

**④-4 «유지 확정» 3건** 📄 손으로 쓴 PNG 해독기(`inflateZlib`+`decodeGrayPng` 87줄) — 백업과 **본문 바이트 동일**(줄 번호만 +1 밀림) ·
`api_revert_instances` 두 번 쓰기 — **함수 전체 동일** · `app/maskio.py` — md5 `4d80e501…`, mtime **2026-09-16 10:53**(이번 사이클에 손 안 댐). ✅ 1차 주장 맞음.

**④-5 표준 시험 세트** 📄 `python3 app/boxes.py` «자체 점검 통과» · `test_clean.py` 통과 · `node --check app/static/app.js` 통과 ·
`modesim.js` **31/31** · `boxsim.js` **50/50** · `py_compile` 로 제가 고친 파일 문법 확인.
⚠️ `sim.js` 는 **단정이 0개**입니다(⑤ 참조).

## ⑤ 재현 결과 표 (숫자 재확인)

| 숫자 | 어디에 적혀 있나 | 📄 내가 다시 센 값 | 판정 |
|---|---|---|---|
| 블루베리 `(144)_121` 어긋남 **151화소** | 1차 ③-3 «설계상 달라야 한다» | 마스크 전경 71,749 · 초벌 전경 71,598 · **only_mask 151 · only_seed 0** · 번호 204개(자른 뒤에도 204) — PIL 로 원본 두 파일을 직접 (`count_hole_direct.py`) | **맞음** |
| 블루베리 어긋난 장 **239** · 화소 **61,620** · 최대 **5,887** · 반대 방향 **0** | 판정서 ④ · 1차 ⑤ · README §11 | `bb_quant.json` 1,195행 재집계 → 239 · 61,620 · 5,887 · 0 · 추정 누락 25알 · 최악 7.3835% | **맞음** |
| 사과 `image451` 번호 **62**개 | README §10 · 1차 ⑤ | 원본 마스크에서 62 · 살아 있는 서버 62 / 상자 61 | 맞음 |
| 사과 `image1` 알 1 = **1,034화소** | 2차·3차·1차 | 1,034 (고치기 전 코드가 되살린 화소도 정확히 1,034) | 맞음 |
| 응답 시간 **0.5초 미만** | 판정서 검증 4 | 살아 있는 서버 0.193~0.205초(1차) · 제 호출도 0.199~0.205초(다른 사진 4장) | 맞음 |
| README **306 → 328줄** · `make_grape_instances.py` **162 → 159줄** | 1차 ④·⑧-2 | 328 · 159 | 맞음 |
| `server.py` +7 · `app.js` +1 = **+8줄** | 1차 자백 ③-1 | `diff` 로 정확히 +7 / 순 +1 | 맞음 |
| «`sim.js` **31/31** 통과» | 1차 ③-3 · ⑧-3 · ⑩ | 🔴 `sim.js` 에는 **단정이 0개**·«합계» 줄도 없습니다(`console.log` 4개로 오차 숫자만 찍는 스크립트, 10줄 출력). 31/31 은 `modesim.js` 의 숫자입니다 | ❌ **틀림** |
| 판정서 «(79)_1 only_mask **1,151**» | `cycle_4/stage3_final.md` L26 | 그 사진은 **`Camera 4 Video (79)_1`** 입니다. `Camera 1 Video (79)_1` 은 **0**(살아 있는 서버 `hole=0` 로도 확인) — 사진 이름 표기가 불완전 | ⚠️ 표기 정정 |
| README §11 새 절이 지금 코드와 같은가 | 1차 P1 | 📄 여섯 항목 전부 코드와 일치(자르기·채우지 않음·`instances_check` 세 열·되돌리기 거절 문구·`layer=gt` 404 — 404 는 살아 있는 서버로 확인). **다만 «번호 저장이 그 hole 을 지운다» 는 사실이 빠져 있습니다**(⑥-2) | 한 줄 보완 필요 |

## ⑥ 새로 찾은 문제 · 고친 것

### ⑥-1 고친 것 1건 — 낡은 단정 ③ 을 **(가)** 로 (판정 근거 포함)
- 파일: `cycles/260917_신기능/cycle_4/stage1/export_inst.py` (140 → 156줄) · 백업 **`_backup_260917_c5b_export_inst.py`**
- 낡은 단정: «내보낸 `instances/>0` 이 내보낸 `masks/` 와 **화소 단위로 완전히 같다**».
- ❌ **판정: 낮추는 것이 아니라 «채우기» 전제를 뺀 자리에 기록 대조를 넣는 것이 맞습니다 → (가) 채택.** 근거:
  1. 📄 **151화소는 제가 직접 셌고 설계상 달라야 하는 값입니다** — `only_mask=151`, 반대 방향 `only_seed=0`, 번호 204개는 자른 뒤에도 204개(즉 «없는 번호» 는 0이고 차이는 전부 «번호 없는 전경» 쪽).
  2. 사이클 4 판정서 ④ 가 «채우지 않는다, 기록만 한다» 를 **확정**했으므로 이 단정은 폐기된 전제(«채운다»)를 재고 있습니다. 영구히 붉은 시험은 다음 사람에게 «진짜 실패» 를 숨깁니다.
  3. 1차가 «내가 만든 시험을 내가 통과시키는 것» 을 피해 남긴 판단은 옳았고, 그래서 **독립된 2차가** 고치는 것이 맞습니다.
- 고친 내용: 한 단정을 **두 개로 쪼갰습니다.** ③-1 «이진본이 배경인데 번호가 있는 화소 = 0»(유령 번호 금지 — 예전보다 **엄격**),
  ③-2 «이진본에만 있는 화소 = manifest `instances_check` 의 `hole=` 값과 **정확히 같다**»(기록과 대조 — 예전에 없던 검사).
- 📄 결과: `apple blueberry` **실패 0** (`(144)_121` → 실측 151 ↔ manifest `hole=151`) · `grape --n 6` **실패 0** (전부 0 ↔ 빈칸).
  → 판정서 «검증 3» 이 이제 **완전히** 충족됩니다(1차의 «4종 중 3종» → **4종**).

### ⑥-2 🔴 새 문제 **N6 — 쓰기 방향은 규칙이 거꾸로다** (고치지 않았습니다, 설계 결정이므로)
규칙은 «**이진본이 번호본을 자른다**» 인데, `api_save_instances`(`instances.py:215`)는 `save_mask_atomic(fixed_path, arr > 0)` 로
**번호본이 이진본을 덮어씁니다.** 그래서 «이진본은 전경인데 번호가 없는» 자리가 저장 한 번으로 사라집니다.
📄 실측(블루베리 `(144)_121`, **아무것도 고치지 않고 «번호 저장» 만** 누른 경우 — 갈래4):
```
저장 전: instance_info hole_px=151 (내가 센 151과 일치) · 마스크 전경 71,749
저장 후: masks_fixed 전경 71,598  → **전경 151화소가 지워졌다**(원본 GT 에 있는 전경)
         instance_info hole_px 151 → **0**        ← 문제를 알려 주던 기록 자체가 사라짐
         내보내기 manifest instances_check → **빈칸** · 내보낸 이진본 전경 71,598
```
- 왜 중요한가: 그 151화소는 사이클 4 가 «21장 25알이 카운팅 정답에서 빠지는 자리» 로 확정하고 **박성문 결정 4·5** 로 넘긴 바로 그 자리입니다. 지금은 검수자가 그 사진에서 «번호 저장» 을 한 번 누르면 **세그 팀 마스크에서도 그 전경이 없어지고, 누락을 찾을 단서(`hole`)까지 지워집니다.** 사이클 4 의 «채우지 않고 기록만» 확정과 정면으로 어긋납니다.
- ❌ 이것은 사이클 5 가 만든 회귀가 **아닙니다**(고치기 전에도 `arr>0` 으로 덮어썼습니다). 그러나 1차가 «세 갈래를 닫았다» 고 한 그 뿌리(«두 파일이 어긋난다»)의 **네 번째 갈래**이고, 1차 `n1_cut.py` S4 는 «브러시로 넓혀 `hole=500` 이 기록된다» 까지 확인하고 **그 다음에 «번호 저장» 을 누르지 않아** 놓쳤습니다(📄 저는 눌러 봤습니다).
- 제가 고치지 않은 이유: 고치는 방법이 **동작 선택**입니다(아래 ⑧-1). 조용히 의미를 바꾸면 «브러시로 지운 알» 과 «번호 없는 전경» 을 구별해야 하는데 그 구별은 사람 결정입니다.

### ⑥-3 작은 것들 (전부 기록만)
| # | 것 | 📄 확인 | 위험 |
|---|---|---|---|
| a | `sim.js` 는 단정이 없는 «눈으로 읽는» 스크립트인데 보고서들이 «31/31 통과» 로 셈 | 출력 10줄·단정 0 (`cycle_5/stage2/sim_output.txt`) | 통과 개수가 실제보다 31개 많게 집계돼 왔습니다. 그 출력은 사람이 읽어야 하고, 배율 3x 이상에서 «상자가 간 거리 0px» 라는 `dpr` 문제(영구 보류 확정)를 보여 줍니다 |
| b | `/instances?layer=gt` 도 이제 **잘려서** 나간다 | 코드 `instances.py:149` `allow_fixed=(layer!="gt")` 뒤에 자르기가 걸림 | «원본 번호를 그대로 보여 달라» 는 뜻이 «masks_fixed 로 자른 원본» 이 됩니다. 📄 화면은 `layer` 를 **쓰지 않습니다**(`app.js:1468` 이 `/instances?fruit&stem` 만 부름) → 지금은 무해. README 는 이 경우를 안 적었습니다 |
| c | 자물쇠 이름이 다름(`inst:` ↔ `mask:`) | 1차 ⑦-다와 같은 판단 | 번호 저장과 브러시 저장이 **동시에** 들어오면 `masks_fixed` 를 누가 마지막에 쓰는지 정해지지 않습니다. 사람 하나 전제에서는 무해 — 새 시험거리 |
| d | `/tmp` 잔재 | 📄 `c4_expinst_*` **9개**(제 재현이 4개 늘림) + `c4b_yolo_*` 2 · `c4_oneline_*` · `c4_yolo_*` · `c4_grape_try*` · `c3_roundtrip_*` | `export_inst.py` 가 스스로 안 지웁니다(다른 시험들은 지움). 삭제는 사람 — ⑧-4 |

## ⑦ 반박 (1차 보고서에서 고칠 곳)

1. **«검증 4종 중 3종 통과»** → 실제로는 «낡은 단정 1건» 이 원인이었고 1차의 분석(설계상 151화소)은 📄 **정확했습니다.** 제가 (가)로 고쳐 **4종 전부 실패 0** 입니다. 1차가 스스로 고치지 않은 선택도 타당합니다(자기 시험을 자기가 통과시키지 않기).
2. **«sim.js 31/31»** ❌ 근거 없음(⑤·⑥-3a). 보고서 세 곳(③-3·⑧-3·⑩)에서 «`sim.js` 는 단정이 없는 눈 확인용 출력» 으로 고쳐야 합니다.
3. **«세 갈래가 닫혔다»** → 📄 맞습니다. 다만 결론 문장을 «N1(두 파일 어긋남)이 닫혔다» 로 넓혀 쓰면 **틀립니다** — 쓰기 방향(N6)이 열려 있습니다(⑥-2).
4. **자백건(`server.py` +7 · `app.js` +1)** → ❌ **타당합니다.** `/api/revert` 는 `server.py` 에만 있어 P0(N1-c)를 다른 파일로는 할 수 없고, `app.js` 는 판정서 ⑦ P1 이 지목한 문구 1줄 교체와 N4 경고 표시(2줄로 1줄 교체 → 순 +1)입니다. 📄 다른 것을 깨지 않았음을 확인했습니다: `node --check` 통과 · `modesim` 31/31 · `boxsim` 50/50 · 복숭아·포도 전/후 동일 · 정상 마스크 되돌리기 200 유지 · `app.js` 가 읽는 `j.stale_txt` 를 서버가 실제로 줌(`boxes.py:204`, `n4_stale` 3/3). 백업도 남겼습니다.
5. **«살아 있는 서버가 최종 코드를 돌고 있다»** → 📄 맞습니다(PID **3141139**, 기동 17:53:18 · `server.py` 17:45:47 · `instances.py` 17:44:56 · `boxes.py` 17:45:16 · `app.js` 17:46:41). 기동 뒤 고친 `export_dataset.py`(17:56)는 📄 서버가 import 하지 않습니다(`grep` 확인). 지금도 `/api/health` 응답합니다.
6. 1차 ⑨ 의 «체크리스트 H 금지를 풀어도 된다» → 📄 **세 갈래 기준으로는 동의**합니다. 단 **N6 때문에 한 줄을 더 붙여야** 합니다: «블루베리에서 «번호 없는 전경» 이 있는 사진(239장)은 «번호 저장» 을 누르면 그 전경이 마스크에서 지워집니다 — 최종 판정 전까지 그 239장은 번호 저장을 미뤄 주세요.»

## ⑧ 최종 판정(3차)이 결정할 항목

1. 🔴 **N6 — «번호 저장» 이 «번호 없는 전경» 을 지우는 것.** 세 갈래:
   **(가)** 저장 응답·화면에 «번호 없는 전경 N화소가 이진본에서 지워집니다» 를 알리고 `hole` 기록을 남긴다(사이클 4 의 «기록만» 정신에 맞음, 코드 약 +4줄) ·
   **(나)** `masks_fixed` 를 덮어쓸 때 «번호가 없던 전경» 을 보존한다(브러시 편집과 구별해야 함 — 의미가 무거움) ·
   **(다)** 지금처럼 조용히 지운다(문서에만 적는다).
   ❌ 제 권고: **(가)** + 박성문에게 «블루베리 초벌 재생성은 지금이 가장 싼 시점(`instances_fixed` 0장)» 을 함께 전달.
2. **포도 켜기** — 전제 «N1 수정 재현» 은 세 갈래 기준으로 📄 충족됐습니다. 다만 N6 는 포도에도 그대로 적용됩니다(브러시로 **넓힌** 자리가 곧 `hole`). ❌ 1번을 정한 다음 켜는 것이 안전합니다.
3. **`export_inst.py` 단정 ③ 을 제가 고친 것**이 옳은지(⑥-1). 되돌리려면 `_backup_260917_c5b_export_inst.py` 로 복원하면 됩니다.
4. **자취 정리(사람 몫, 저는 손대지 않음)**: `/tmp/c4_expinst_*` **9개**(+ `c4b_yolo_*` 2 · `c4_oneline_*` · `c4_yolo_*` · `c4_grape_try*` · `c3_roundtrip_*`) · 공용 `data/{apple,blueberry}/status.json` 의 두 항목(1차 자취, 제 검수는 **변화 0**).
5. **다섯 사이클 열 단계 모두 브라우저 눈 확인 0** — 가장 위험한 미확인은 여전히 «16비트 PNG 해독기가 진짜 브라우저에서 사과 번호 1~95 를 살려 내는가» 입니다(제가 검증한 것은 전부 서버 쪽 바이트).

### 제가 남긴 것
- 새 폴더 `cycles/260917_신기능/cycle_5/stage2/` — 시험 4개·지문 2개·증거 4개(`n1_three_paths.py|json` · `nonum_before_after.py`+`nonum_{before,after}.json` · `n5_on_before_code.py` · `live_c5b.py` · `count_hole_direct.py` · `fingerprint.py|_before|_after.json` · `sim_output.txt`)
- 고친 파일 1개 + 백업 1개(`cycle_4/stage1/export_inst.py`, `_backup_260917_c5b_export_inst.py`)
- 다시 돌려 **덮어쓴** json(전부 재현 결과, 결론은 그대로이거나 «실패 0» 으로 좋아짐): `export_inst_apple_blueberry.json` · `export_inst_grape.json` · `grape_seed_api.json` · `export_coherence.json` · `n5_brush_vs_number.json` · `n1_cut.json` · `cycle_3/stage1/seed_after.json`
- 제가 띄운 격리 서버(포트 5199, 사본 코드·사본 `data/`, PID 3164742)는 **제가 끝내고 껐습니다.** 팀 서버(3141139)는 건드리지 않았습니다.
  ⚠️ 정직하게 적습니다: 그 서버를 끌 때 `pkill -f` 패턴이 제 쉘까지 잡아 명령이 한 번 끊겼습니다(그 뒤 PID 로 정확히 껐습니다). 그 패턴은 사본 경로만 담고 있어 **팀 서버에는 걸리지 않습니다** — 확인했습니다.
