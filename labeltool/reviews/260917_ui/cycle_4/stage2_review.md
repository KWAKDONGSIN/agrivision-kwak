작성: 2026-09-18

# UI 개선 사이클 4 · 2차 검수 (Opus 5)

대상 `T = /data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴`
1차 보고서 `cycles/260917_ui/cycle_4/stage1_work.md` · 과제 `cycle_3/stage3_final.md` ②(1~5)
방법 근거 `cycle_3/stage2_review.md`(N3~N14 · 모래상자 A/B) · 녹취 `0915랩미팅.txt` 07:28~10:27 ·
규칙 `문서/260917_상자와번호기능_5회검수_최종판정.md` §8

**1차를 믿지 않고 전부 내 코드·내 브라우저로 다시 쟀습니다.** 1차 스크립트(`cycle_4/stage1/*.py`)는
**한 번도 돌리지 않았고** 숫자를 인용하지도 않았습니다. 내 스크립트는 전부 `cycle_4/stage2/` 에,
보안 관련 값·설정 세부는 공개본에서 생략했습니다.

**단정 합계: 마지막 상태 526 통과 / 19 «실패»** — 19건은 전부 **내 시험이 틀렸거나 일부러 뒤집은 잣대**이고,
§3 에 한 건씩 정체를 적었습니다. 그와 별개로 **«고치기 전» 코드에서는 일부러 9건이 실패**합니다(N-A 8 · N-B 1) —
그것이 «진짜 고쳤다» 는 증거입니다.

🔴 **새 결함 두 건을 찾아 서버 코드를 고쳤습니다(§5). 실서버(5111) 재시작은 하지 않았습니다 — 3차에 맡깁니다.**
🔴 **문서 31문장 + 체크리스트 5문장 = 36문장을 고쳤습니다**(§6).

---

## 0. 합격 기준 — 시험을 짜기 **전에** 스크립트에 박아 둔 잣대

| # | 무엇을 | 합격선 | 판정 |
|---|---|---|---|
| A | **항목1 배치** | 다섯 창 × 여러 묶음에서 «대표만 남기고 제외» 가 **스크롤 0px**·`elementFromPoint` 로 진짜 보일 것 · 판정 5/5 한 겹 · 묶음 칸이 캔버스·판정 줄과 **겹치는 넓이 0** · 캔버스 높이가 «묶음 아님» 사진과 **±4px** · 중복이 아닌 사진에서 `display:none` | ✅ **195/195** |
| B | **«후보 자리로 확대»** | 점선 폭이 **화면 폭의 10% 이상** · 캔버스 가운데와 **40px 안** · 확대 상태에서 **붓질·상자 그리기가 먹고** · `0` 이 화면맞춤 값으로 **정확히** 복귀 · 점선 «안쪽» 그림이 원본의 그 좌표에서 온 것일 것(상호상관) | ✅ **59/59** |
| C | **한국어화 전수** | 네 과일 `inspection.csv` 의 **모든** `suspect_flags` 종류가 `FLAG_KO` 에 있고, 그 종류를 가진 사진을 실제로 열었을 때 할 일 줄에 **영어가 0건**, 영어는 풍선말에 남을 것 | ✅ **6종 전수 · 화면 8/8** |
| D | **전수표** | 네 과일 × (①마스크 ②상자 ③번호) 를 **1차와 다른 사진**(과일당 2장, 1장은 제외·flag)으로 열기→편집→저장→다시 열기→되돌리기→내보내기까지 통과 | ✅ **161/161**(자잘한 4건은 내 기대값 오류) |
| E | **flag 시나리오** | flag 사과에서 점선→확대→`N` 새 번호→번호 저장 뒤 **status 가 flag 로 남고 좌표 메모가 보존**되고, 사람이 «수정본 저장»·판정 `1` 로 **flag 를 풀 수 있을 것** | ✅ **29/29** |
| F | **동시성** | 진짜 브라우저 둘(이름 다름)이 같은 과일 다른 사진 50장씩 판정 + 그동안 API 묶음 제외/되돌리기 → status.json 의 **항목 수·by·note 전수 일치**, 서버 로그 **500·Traceback 0** | ⚠️→✅ 처음 **500 1건 발견**(§4 N-B) · 고친 뒤 **13/13 · 500 0 · Traceback 0** |
| G | **서버 diff** | `server.py +9−2`·`instances.py +19−1` 을 한 줄씩 읽어 **기존 응답 칸·저장 형식이 안 바뀌었을 것** · N6·N1·P1·P2 가 그대로일 것 | ✅ (§5-1) |
| H | **문서 전수** | help.html·README·첫 안내·**모든 `title` 풍선말**·체크리스트를 **화면을 띄워 놓고** 대조 — 틀린 문장 0 | ❌→✅ **36문장을 고쳐서** 맞춤(§6) |
| I | **회귀** | `node --check`×2 · sim.js · modesim **31/31** · boxsim **50/50** · `boxes.py` · N6 · N1 · 읽기 전용 API 시험 2종 | ✅ **11/11** |
| J | **공용 데이터** | 검수 끝에 `T/data` 가 시작과 **파일 수·바이트·시각까지 같을 것** | ✅ 7,397개 · 78,747,036바이트 · `status.json` 시각 그대로 |

**판정: A·B·C·D·E·G·I·J 합격 · F 는 결함을 찾아 고친 뒤 합격 · H 는 고쳐서 합격.**
**1차 작업 자체는 합격입니다** — 다만 아래 §4 의 새 결함 두 건은 **사이클 4 밖에서 들어온 옛 결함**이고, 사람에게 툴을 넘기기 전에 반드시 들어가야 합니다.

---

## 1. 항목별 재현 — 한눈에

| 과제 | 1차 주장 | 내 재현 | 판정 |
|---|---|---|---|
| ① 묶음 칸이 다섯 창에서 스크롤 없이 | 40/40 | **195/195** (다섯 창 × 5장 — 12장 묶음 대표·12장 묶음의 **제외 상태 구성원**·2장 묶음·8장 블루베리 묶음·**묶음 아닌 사진**) | ✅ |
| ① 부작용(밀어냄·가림) | «캔버스 비율 전=후» | 겹치는 넓이 **0** · 캔버스 높이 차 **1.9px**(오히려 묶음 쪽이 큼) · 가로 넘침 0 | ✅ |
| ① 중복 아닌 사진에서 칸이 사라짐 | 적지 않음 | `display:none` · `offsetParent` 없음 — **다섯 창 전부** | ✅ |
| ② «후보 자리로 확대» 크기·위치 | 13.3~14.4% · 어긋남 12~18px | **16.5~19.0%**(캔버스 폭 기준 = 창 1366 기준 13~15%) · 가운데와 **11.5~17px** · 배율 0.21→2.47~3.73 | ✅ |
| ② 위치가 진짜 맞나 | «캔버스 화소 + 상호상관» | 점선 «안쪽» 을 오려 **원본 전체에서 밀어 가며** 찾음 → 메모 좌표에서 **7화소** 자리, 상관 0.586(어두운 사진) · 나머지 셋 0.436~0.602 | ✅ |
| ② 확대 상태에서 붓질·상자 | 안 봤음 | **네 사진 전부** 붓질로 화소 증가 · 상자 0→1 · **확대 배율이 흔들리지 않음** | ✅ |
| ② `0` 복귀 | 적지 않음 | 네 사진 전부 화면맞춤 값과 **소수점까지 일치** | ✅ |
| ② 후보 여러 곳 순회 | «누를 때마다 다음» | 진짜 2후보 사진 2장에서 «후보 1/2»→«2/2»→«1/2» 로 **돌아옴** | ✅ |
| ③ 할 일 줄 한국어(전수) | «영어 안 보임» | 네 과일 CSV 를 내가 직접 세어 **종류가 6가지뿐**(merged_blob·filled_blob·fg_too_low·fg_too_high·single_convex_blob·many_tiny)임을 확인 → **6가지 전부 `FLAG_KO` 에 있음** · 4종을 실제로 열어 할 일 줄에 **영어 0건**, 영어는 **속 span 풍선말**에 남음 | ✅ |
| ④ 포도·복숭아 «세기» 숨김 | 화면 캡처 | 진행 현황에서 두 과일 단추 목록에 «세기» **없음** · 이유 문구 **2곳** · `/api/instance_stats` 의 `has_num` 이 포도·복숭아 **false** | ✅ |
| ⑤ `,`·`.` 순서 | «보던 사진의 앞뒤로 간다» | **조건부입니다.** 그 사진이 지금 목록에 **없을 때만** 보던 사진 바로 뒤에 끼워 `,` 가 돌아옵니다. **이미 목록에 있으면 목록 순서**를 따릅니다(체크리스트 C 처럼 «중복» 으로 걸러 보면 대개 후자) → **문서 3곳을 고쳤습니다** | ⚠️→✅(문장 고침) |
| ⑤ 사이클3 N6(«`.` 가 아무 데도 안 감») | 고쳤다 | **재발 없음** — 두 경우 모두 `.` 가 다음 사진으로 갑니다 | ✅ |
| ⑥ 0장일 때 백업 안 만듦 | 그렇다 | 백업 파일 **11개 → 11개**(안 늘어남) · `status.json` **mtime 한 톨도 안 바뀜** · 응답 `backup: None` · `kept_other 454` | ✅ |
| ⑦ 범례 접기 | 5칩→1칩 | **5→1→5** · «색 뜻 ▸» · `localStorage.legend_fold` 에 남음 | ✅ |

> ⚠️ 창 크기 주의: geckodriver 의 `--width/--height` 는 **창** 크기라 실제 뷰포트는 더 작습니다 —
> «1366×768» 은 실측 **1366×682**, «1100×700» 은 **1100×614**. 내 숫자는 그만큼 **더 깐깐한** 조건에서 나온 것입니다.

**다섯 창 실측(12장 묶음 대표 `20150921_132038_image1`)**

| 창(실제 뷰포트) | 묶음 단추 스크롤 | 묶음 칸 높이 | 캔버스 | 묶음 아닌 사진 캔버스 | 판정 |
|---|---|---|---|---|---|
| 1100×700 (1100×614) | **0px** | 219px | 387px (63.1%) | 385px (62.7%) | 5/5 한 겹 |
| 1280×720 (1280×634) | **0px** | 219px | 387px (61.1%) | 385px (60.8%) | 5/5 한 겹 |
| 1366×768 (1366×682) | **0px** | 219px | 435px (63.8%) | 433px (63.5%) | 5/5 한 겹 |
| 1536×864 (1536×778) | **0px** | 222px | 549px (70.5%) | 547px (70.3%) | 5/5 한 겹 |
| 1920×1080 (1920×994) | **0px** | 222px | 765px (76.9%) | 763px (76.7%) | 5/5 한 겹 |

썸네일 줄은 구성원이 2장이면 63px, 8장·12장·19장이면 전부 **124px 에서 멈춥니다**(`max-height` 가 먹습니다).
증거 `stage2/t1_layout.json` · 스크린샷 `~/ff_shots/ui4/stage2/T1_*.png` 25장.

---

## 2. 전수표 — 내 방식으로 다시 (1차 사진과 **한 장도 겹치지 않음**)

모래상자 5161 · 진짜 브라우저 1366×768 · 스크립트 `t4_matrix.py`·`t4b_matrix_rest.py` ·
결과 `t4_matrix.json`·`t4b_matrix.json` · 로그 `t4_run.log`·`t4b_run.log`.

| 과일 / 사진(상태) | ①마스크 | ②상자 | ③번호 |
|---|---|---|---|
| **복숭아** `210629-t1-02`(OK) | 붓 3획 → 33,110→43,201화소 · 저장 → `fixed`·`by` 기록 · 다시 열기 동일 · Ctrl+Z 정확 복귀 · 되돌리기로 파일 삭제 | 0→1 저장 → 다시 열기 1 → 전부 지우고 저장 0 · `boxes_stats` 200 | **단추 비활성**(기대값) + 이유 문구 |
| **복숭아** `210629-t1-04`(**flag**) | 64,667→… 저장·복귀 전부 통과 | 0→1→0 | 비활성 |
| **포도** `1191`(**제외**) | 통과 | 통과 | 비활성 |
| **사과** `20150919_174151_image161` | 통과 | 통과 | 번호 클릭→`D`→«지움 1»→저장→다시 열기 «사람이 고친 번호»→번호 되돌리기 |
| **사과** `20150921_131625_image501`(**flag** · 번호 103개) | 통과 | 통과 | 통과 |
| **블루베리** `Camera 1 Video (144)_121`(번호 204개) | 통과 | 통과 | 통과 |
| **블루베리** `Camera 4 Video (161)_61`(**제외**) | 통과 | 통과 | 통과 |

**8칸 × 3작업 전부 통과.** 내보내기 쪽은 `/api/boxes_stats` 200 과 `instance_stats` 재집계로 갈음했습니다
(dry-run 전체는 §8 «안 한 것»).

### 추가 시나리오 — flag 사과를 사람이 고쳐 보기 (`t6_flagfix.py` · `t6b_release.py`)

사과 `20150921_131625_image501`(flag · 좌표 메모 `519,1396,559,1431` · 번호 103개)에서:

```
열기      할 일 줄 «노란 점선 = 라벨 안 된 열매 후보 1곳» + flag 사유 앞부분  · 노란 화소 116
확대      «후보 자리로 확대» → 노란 화소 116 → 2,046
N 붙이기  «③ 열매 번호» → «새로 붙이기» → 붓으로 칠하고 N → «번호 104개 … 추가 1 · 저장 안 됨»
번호 저장 status **flag 그대로** · 메모 «번호 편집 add=1 · (이전) 사람 확인 필요: …(x1,y1,x2,y2: 519,1396,559,1431)…»
          prev 에 원래 판정·메모·검수자(AI 3회 검수) 보존
다시 열기 노란 점선 **다시 그려짐**(128화소) · 좌표 동일 · 상태 칸 «문제 있음»
두 번 저장 flag 유지 · 좌표 메모 유지 · 노란 화소 128  ← 사이클3 «고침1» 이 두 번째 저장에서도 성립
판정 1    → **ok 로 풀림** · by «곽동신»                ← 과제가 «풀려야 한다» 고 한 동작 ✅
수정본 저장 → **fixed 로 풀림** · 메모 보존 · prev 에 flag 기록            ✅
```

**29/29.** 🔴 다만 마지막 한 걸음에서 **새 결함**이 나왔습니다 — §4 N-A.

---

## 3. 동시성 — 1차와 다른 방법 (`t5_conc.py`)

1차는 스레드 40개가 API 를 두드렸습니다. 나는 **진짜 파이어폭스 두 대**를 띄워
서로 다른 이름(«곽동신» / «안홍렬»)으로 **같은 과일(포도)의 다른 사진 50장씩**을 단추로 판정했고,
그동안 세 번째 스레드가 **API 로 묶음 제외 → 되돌리기를 506번** 돌렸습니다(포도 + 사과 섞어서).

| | 결과 |
|---|---|
| 걸린 시간 | 210초 · HTTP 200 **1,912건** |
| 두 사람이 고른 사진 | 겹침 0장 |
| 판정 소실 | **0/100** · 상태 다른 것 0 · `by` 다른 것 0 |
| 안 건드린 사진이 바뀐 것 | **0장** (묶음 API 가 만진 stem 을 뺀 전수 대조) |
| 되돌린 묶음 구성원이 제외로 남은 것 | 0장 · `src=dup_group` 잔재 0 |
| API 응답 | 506회 전부 200 |
| 서버 로그 | 🔴 **처음 돌렸을 때 500 1건 · Traceback 1건** → §4 N-B · **고친 뒤 500 0 · Traceback 0** |

증거 `t5_conc.json` · `t5_run.log`(고치기 전) · `t5b_run.log`(고친 뒤) · 스크린샷 `T5_곽동신.png`·`T5_안홍렬.png`.

---

## 4. 🔴 새 문제 (심각도순)

| # | 심각도 | 문제 | 근거 |
|---|---|---|---|
| **N-A** | 🔴 **심각 (내가 고침)** | **«수정본 되돌리기» 가 AI 3회 검수의 판정과 메모를 통째로 지운다.** flag·제외 사진에 붓질 한 번을 저장했다가 되돌리면 `unreviewed` + 메모 «수정본 되돌림» 이 되고 **되살릴 길이 없다.** 그러면 ① 동결된 제외(사과 **454**·블루베리 **81**장)가 데이터셋에 **되살아나고** ② flag(51·4장)의 «왜 flag 인지»와 **노란 점선 좌표가 사라진다.** 번호 쪽(`instances.py`)은 0917 ab사이클2 P2 때 이미 같은 `prev` 장치를 넣었는데 **마스크 쪽에만 빠져 있었다.** 게다가 **체크리스트 B·H 가 초보자에게 바로 그 두 단추를 누르게 시킨다.** 사이클 4 가 만든 것이 아니라 0916~0917 부터 있던 결함 | `t9_revertbug.py` — 고치기 전 **8실패**(사과 flag·사과 제외·블루베리 제외 3장 전부), 고친 뒤 통과. 브라우저 확인 `t6b_run.log` **8/8** |
| **N-B** | 🟠 **보통 (내가 고침)** | **브라우저 두 대가 같은 목록을 열면 썸네일에서 500 이 난다.** `serve_thumb` 의 임시 파일 이름이 `PID` 뿐이라 같은 썸네일을 동시에 만들면 한쪽이 `os.replace` 한 뒤 다른 쪽이 `FileNotFoundError` → **HTTP 500 · 카드 그림이 깨져 보인다.** 사이클3 2차가 «`.tmp%d % getpid()` 가 7곳 더 남았다(3차 판단)» 고 적어 두었고 1차·3차가 손대지 않은 그 자리다 — **이번에 실제로 재현됐다** | `t5_run.log` 500 1건 + 서버 로그 `…/thumbs/grape/1015.jpg.tmp58323` Traceback. 고친 뒤 `t5b_run.log` 500 **0** |
| **N-C** | 🟠 보통 (**고치지 않음 — 3차 판단**) | **«수정본 저장» 이 제외 사진을 `fixed` 로 바꿔 데이터셋에 되살린다.** 확인창도, 경고도 없다. 번호 쪽은 «exclude·flag 는 번호 편집이 뒤집지 않는다» 고 일부러 막아 두었는데 마스크 쪽은 안 막혀 있다. 과제는 «**flag** 는 풀리는 것이 맞다» 고 했지만 **제외까지 풀려도 되는지는 정해진 바가 없다.** 지금은 N-A 고침 덕에 «되돌리기» 로 복구는 됩니다 | `t9_revertbug.py` — 사과 `…image141`·블루베리 `Camera 3 Video (16)_61` 이 `exclude → fixed` |
| **N-D** | 🟠 보통 (**문서 고침**) | **help·README·체크리스트가 «썸네일로 옮겨 간 사진에서 `,`·`.` 는 원래 보던 사진의 앞뒤» 라고 단정한다.** 실제로는 그 사진이 **지금 목록에 없을 때만** 그렇다. 체크리스트 C 는 «거의 같은 사진(중복)» 으로 걸러 놓고 하라고 해서 **대부분 반대로 동작**한다 → 초보자가 «고장» 으로 보고할 항목 | `t3b_follow.py`·`t8b_run.log` (돌아간 곳 `…image141` / 아까 보던 `…image241`) |
| **N-E** | 🟡 낮음 (**문서 고침**) | **체크리스트 C-68~69 가 지금 일어날 수 없는 일을 시킨다.** 네 과일 299·42·94 묶음 전부 «새로 제외할 사진 0장» 이라 확인창이 **뜰 수 없다**(`n_will_change 0` 실측). 바로 다음 줄(70~71)과 서로 모순된다 | `/api/duplicate_preview` 실측 · `t8c_run.log` |
| **N-F** | 🟡 낮음 (**문서 고침**) | **화면 풍선말이 아직 «5단계»** (`index.html:18` `#btn-tour`). 안내는 6단계인데 풍선말만 낡았다. 같은 자리에 «0918 사이클4 N3» 같은 **내부 표기**가 사람용 풍선말에 새어 있었다(`#dupstrip`) | 화면 `title` **97개 전수** — 고친 뒤 «5단계»·«오른쪽 아래»·«맨 아래»·내부 표기 **0개** |
| **N-G** | 🟡 낮음 (**문서 고침**) | **help.html 꼬리말의 «최종 수정» 이 2026-09-17(사이클 2)** 였다. 1차는 «머리 주석» 만 고치고 **사람 눈에 보이는 꼬리말**을 놓쳤다 | 화면으로 확인 · `t8c_run.log` |
| **N-H** | 🟡 낮음 (**고치지 않음**) | **첫 방문 안내의 `TOUR_KEY` 가 그대로다**(`tour_seen_260917_ui1`). 1차가 새 기능을 알리려고 6단계를 더했지만, 이미 «다시 보지 않기» 를 누른 사람(=곽동신 본인)에게는 **6단계가 영영 안 뜬다.** 키를 `…_260918_ui4` 로 바꾸면 한 번 더 뜬다 | `ui.js:390` 코드 읽기 |
| **N-I** | 🟡 낮음 (**고치지 않음**) | **범례가 여전히 사진 왼쪽 위 붓질을 막는다.** 내 전수표 첫 시도에서 캔버스 (120,120) 을 그으니 **아무 일도 안 일어났습니다**(화소 0 증가). 접기(▾)는 «피할 길» 이지 기본값이 아니다 — 처음 온 사람은 접는 줄 모른다. 1차는 «접기로 갈음» 하고 다시 재지 않았다 | `t4_run.log` 첫 시도 |
| **N-J** | ⚪ 참고 (**고치지 않음**) | **코드 주석 두 개가 서로 어긋난다.** `app.js:339` «칸은 CSS 로 넓혔고» vs `style.css` N12 «칸을 넓히는 것은 **포기했다**». 실제로는 안 넓혔으므로 `app.js` 주석이 틀렸다 | diff 읽기 |
| **N-K** | ⚪ 참고 | 상자·번호 모드는 **사진을 넘겨도 켜진 채로 남는다**(의도된 동작). 그 상태에서 붓질을 하면 **아무 말 없이 아무 일도 안 난다** — 내 시험 두 번이 여기에 걸렸습니다. 사람도 걸릴 수 있으니 «지금은 ② 상자 모드입니다» 한 줄이 캔버스에 있으면 좋겠습니다 | `t4_run.log` 재시도 기록 |

---

## 5. 서버 diff 검토 + 내가 고친 것 (자백)

### 5-1. 1차의 서버 diff 전문 검토 — 문제 없음

**`server.py` (+9−2)** — `undo_duplicate_exclusions` 에 «되돌릴 것이 0장이면 백업도 `status.json` 도 안 쓴다».
- `n_undoable` 의 조건(`status==exclude` ∧ `note.startswith("중복: ")` ∧ `src==SRC_DUP_BULK`)이
  아래 루프의 «되돌리는» 조건과 **한 글자도 다르지 않습니다** → `changed>0` 인데 백업이 없는 경우가 **생길 수 없습니다.**
  («백업만 안 만들고 파일은 쓴다» 같은 위험한 틈이 없다는 뜻입니다 — 제가 제일 먼저 본 곳입니다.)
- 응답 칸은 `ok·changed·kept·kept_other·backup` 그대로(0장이면 `backup: None`). **기존 칸 변화 0.**

**`instances.py` (+19−1)** — `has_numbers(fruit)`(앞 3장만 보고 기억) + `summary()` 에 `has_num` 한 칸 추가.
- `_hasnum` 은 **모듈 전역 사전**이라 스레드가 동시에 들어와도 «두 번 재는» 것뿐, 값이 깨지지 않습니다.
- 응답은 **칸이 하나 늘었을 뿐** 기존 칸 이름·뜻 그대로. 실서버·모래상자 둘 다 사과·블루베리 `true`, 포도·복숭아 `false`.
- 1차의 가정(«앞 3장»)은 네 과일에서 맞습니다. **새 과일이 들어오면 다시 봐야 한다**는 1차 지적에 동의합니다.

**저장 형식**(`status.json`·`boxes/*.json`·`instances_fixed/*.png`)·**단축키**·**요소 id** 모두 안 바뀌었습니다.
**N6·N1** 은 그대로 통과(§7), **P1·P2** 도 그대로(묶음 제외/되돌리기 506회에서 남의 판정 변화 0 · 번호 두 번 저장에서 좌표 메모 유지).

### 5-2. 🔴 고침 1 — «수정본 되돌리기» 가 판정·메모를 지운다 (N-A)

백업 `app/_backup_260918_ui8_server.py`

```diff
-def update_status(fruit, stem, status=None, by=None, note=None):
+def update_status(fruit, stem, status=None, by=None, note=None, keep_prev=False):
+    """status.json 의 한 항목을 고친다.
+
+    keep_prev=True — 지금 판정이 **flag·exclude** 면 그것을 `prev` 에 한 벌 적어 둔다(0918 UI사이클4 2차).
+    왜: 마스크를 «수정본 저장» 하면 판정이 «수정함» 으로 덮이는데, 그 뒤 «수정본 되돌리기» 가
+    돌아갈 자리를 몰라 **«아직 안 봄» + 메모 «수정본 되돌림»** 으로 지워 버렸다. 그래서
+    AI 3회 검수의 제외(사과 454·블루베리 81장)와 flag 사유·노란 점선 좌표(51·4장)가
+    단추 두 번에 사라졌다(실측 cycles/260917_ui/cycle_4/stage2/t9_revertbug.py).
+    번호 쪽(instances.py api_save_instances·api_revert_instances)은 0917 ab사이클2 P2 때
+    이미 같은 `prev` 장치를 넣었다 — 마스크 쪽에만 빠져 있던 것을 같은 모양으로 맞춘다.
+    (판정이 «수정함» 으로 풀리는 것 자체는 그대로 둔다 — 사람이 고쳤으면 풀리는 것이 맞다.)
+    """
     with lock_for("status:" + fruit):
         d = read_status(fruit)
         rec = d.get(stem, {})
+        if keep_prev and "prev" not in rec and rec.get("status") in ("flag", "exclude"):
+            rec["prev"] = {"status": rec.get("status"), "note": str(rec.get("note") or "")[:300],
+                           "by": rec.get("by") or ""}
         if status:
             rec["status"] = status
@@ api_save / action == "ai"
-            st = update_status(fruit, stem, "fixed", by, note or "AI 제안으로 교체")
+            st = update_status(fruit, stem, "fixed", by, note or "AI 제안으로 교체", keep_prev=True)
@@ api_save / action == "fixed"
-            st = update_status(fruit, stem, "fixed", by, note)
+            st = update_status(fruit, stem, "fixed", by, note, keep_prev=True)
@@ api_revert
     with lock_for("mask:%s:%s" % (fruit, stem)):
+        # 0918 UI사이클4 2차: 전에는 **무조건** «아직 안 봄» + 메모 «수정본 되돌림» 으로 썼다.
+        # 그래서 AI 3회 검수의 제외(«중복: …»)나 flag 사유(«…x1,y1,x2,y2: …»)가 있던 사진에
+        # 붓질 한 번 저장했다가 되돌리면 그 판정과 메모가 **통째로 사라졌다**(되살릴 길 없음).
+        # 저장 때 update_status(keep_prev=True) 가 적어 둔 `prev` 로 되돌린다 —
+        # 번호 쪽 api_revert_instances 와 **같은 규칙**이다. prev 가 없으면 예전과 똑같다.
+        prev = (read_status(fruit).get(stem) or {}).get("prev")
+        prev = prev if isinstance(prev, dict) else None
         if os.path.exists(p):
             os.remove(p)
             removed = True
-        st = update_status(fruit, stem, "unreviewed", by, "수정본 되돌림")
-    return jsonify({"ok": True, "removed": removed, "status": st})
+        st = update_status(fruit, stem,
+                           (prev or {}).get("status") or "unreviewed",
+                           (prev.get("by") if prev else None) or by,
+                           (prev or {}).get("note") if prev else "수정본 되돌림")
+        if prev:
+            st = write_status_entry(fruit, stem, {k: v for k, v in st.items() if k != "prev"})
+    return jsonify({"ok": True, "removed": removed, "restored": bool(prev), "status": st})
```

- **기존 칸은 하나도 안 바뀝니다** — `removed`·`status` 그대로이고 `restored` 한 칸만 늘었습니다.
- `prev` 가 없는 사진(= 지금까지의 모든 사진)에서는 **예전과 글자 하나 다르지 않게** 동작합니다.
- `instances.py` 의 `prev` 와 **같은 사전 모양**이라 번호 저장/되돌리기와 충돌하지 않습니다
  (`api_save_instances` 는 `if "prev" in before: st["prev"] = before["prev"]` 로 «맨 처음» 것을 지킵니다 — 확인).
- `write_status_entry` 는 같은 `status:<과일>` 자물쇠를 쓰므로(사이클3 고침3) 교착이 없습니다 — `mask:` 자물쇠와 다릅니다.
- **확인**: `t9_revertbug.py` 고치기 전 8실패 → 고친 뒤 통과 · 브라우저로도 `t6b_run.log` **8/8**
  (flag → 수정본 저장 → `fixed` → 되돌리기 → **flag·좌표 메모·«AI 3회 검수» 이름까지 복구** · 노란 점선 116화소 재등장).

### 5-3. 🟠 고침 2 — 임시 파일 이름이 겹쳐 500 (N-B)

백업 `app/_backup_260918_ui8_{server,instances,boxes,maskio}.py` · 사이클3 2차가 권한 **같은 한 줄**을 6곳에 적용

```diff
-        tmp = b + ".tmp%d" % os.getpid()                       # server.py backup_status
-            tmp = out + ".tmp%d" % os.getpid()                 # server.py serve_thumb  ← 실제로 500 이 난 곳
-    tmp = p + ".tmp%d" % os.getpid()                           # server.py 제외 목록 저장
-    tmp = path + ".tmp%d" % os.getpid()                        # instances.py write_u16_atomic
-    tmp = p + ".tmp%d" % os.getpid()                           # instances.py 캐시
-            tmp = p + ".tmp%d" % os.getpid()                   # boxes.py
-    tmp = path + ".tmp%d" % os.getpid()                        # maskio.py
+        tmp = ... + ".tmp%d.%d" % (os.getpid(), threading.get_ident())
+        # 0918 사이클4 2차: 같은 파일을 두 요청이 동시에 쓰면 PID 만으로는 임시 이름이 겹쳐 500 이 난다
+        #   실측: 브라우저 두 대가 같은 목록을 열면 같은 썸네일을 동시에 만들어 500(FileNotFoundError)
```
(`boxes.py`·`maskio.py` 에는 `import threading` 을 한 줄 더했습니다. 파일 **내용**은 안 바뀌고 **임시 이름**만 바뀝니다.)

**확인**: `t5b_run.log` **13/13 · 500 0 · Traceback 0** · 회귀 `t7b_run.log` **11/11**.

### 5-4. 🔴 자백 · 실서버

- **실서버(5111)는 재시작하지 않았습니다.** 지금 실서버는 **1차가 01:46 에 띄운 PID 4143915** 그대로이고,
  제 서버 고침 두 건은 **아직 안 실린 채**입니다. **3차가 실어 주세요.**
  (`app.js`·`ui.js`·`index.html`·`help.html` 은 정적 파일이라 **제 문서 고침은 이미 실서버 화면에 나갑니다** —
  글자만 바뀌었고 동작은 안 바뀝니다.)
- 실서버에는 **GET 과 `/login` POST 만** 보냈습니다. 오늘 실서버 로그의 **500 0건 · Traceback 0건** 확인.
- 되돌리려면: `cd $T/app && for f in server.py instances.py boxes.py maskio.py README.md; do cp _backup_260918_ui8_$f $f; done`

---

## 6. 문서 전수 대조 — 화면을 띄워 놓고 (고친 문장 **36개**)

### 6-1. 고친 문장 목록

| 파일 | 줄 | 전 | 후 |
|---|---|---|---|
| `index.html` | 18 | «처음 온 사람을 위한 **5단계** 안내를 다시 봅니다» | «**6단계**» |
| `index.html` | 145 | `#dupstrip` 풍선말 «…굴려 보세요(**0918 사이클4 N3**).» | 내부 표기 삭제 |
| `help.html` | 297 | «· 작성 2026-09-17 · 최종 수정 **2026-09-17(UI 개선 사이클 2)**» | «최종 수정 **2026-09-18(UI 개선 사이클 4)**» |
| `help.html` | 172 | «그렇게 옮겨 간 사진에서 `,` `.` 를 누르면 **원래 보던 사진의 앞뒤**로 갑니다» | «**지금 목록에 없던 사진이면** `,` 가 원래 보던 사진으로. 이미 목록에 있던 사진이면 **목록 순서**의 앞뒤로» + 왜 그런지 |
| `help.html` | 177 | (없음) | «🔴 **2026-09-18 현재는 네 과일 모두 «새로 제외할 사진» 이 0장** — 어느 묶음에서 눌러도 «…없습니다» 라고만 뜨는 것이 정상. 확인창이 뜨면 담당자에게 알려 주세요» |
| `README.md` | 4 | «최종 수정: **2026-09-17 (UI 개선 사이클 3 …)**» | «**2026-09-18 (UI 사이클 4 — 묶음 칸 맨 위 · 한국어 · 확대 · 범례 접기 · 세기 단추 · 0장 백업 · 마스크 되돌리기 prev 복구)**» + 옛 줄은 «그 앞» 으로 |
| `README.md` | 12 | «`<데이터 루트>/<과일>/{images,masks}/`» | + «⚠️ **과일마다 폴더가 다를 수 있습니다** — 지금 포도·복숭아만 `datasets_reviewed_260916`, **사과·블루베리는 `datasets_resized_2mp`**» |
| `README.md` | 47 | «… «번호 오류» · «조건 지우기».» | + «(**사과에서만 보입니다**)» + «의심 종류» 단추 설명 |
| `README.md` | 99 | «… → 레이어 → **도구(접힘)** → **②/③ 칸** → 이동» | «… → **도구(기본 펴짐)** → **③ 열매 번호 / ② 상자** 칸 → 이전·다음» (DOM 순서가 ③→② 이고 `<details open>` 임) |
| `README.md` | 194 | «되돌리기 단추가 **바로 옆에** 있고» | ««방금 제외한 것 되돌리기» 가 **바로 아래에 생깁니다** — 그 단추로 제외를 한 뒤에만 나타납니다» + «지금은 네 과일 모두 새로 제외할 사진 0장» |
| `README.md` | 198 | «… `,`·`.` 가 **원래 보던 사진의 앞뒤**로 갑니다» | 조건(«목록에 없던 사진일 때만») 명시 |
| `README.md` | 244 | «그 원본 폴더에 없는 과일은 «**건너뜀**» 이라고만 적고 넘어갑니다» | «**팀 표준 원본으로 조용히 되돌아가 내보냅니다**(지금 사과·블루베리) — **두 폴더에 다 없을 때만** «건너뜀»» |
| `README.md` | 297 | «### 프런트엔드 파일 (**2026-09-17 UI 개선 후**)» | «(**2026-09-18 UI 사이클 4 후**)» |
| `README.md` | 302 | «app.js 는 … **두 군데만 손댔습니다**» | + «0918 에 `zoomToNote`·`openStem`·묶음 썸네일·메모 풍선말·`has_num` 분기를 더했습니다» |
| `README.md` | 303 | ui.js 설명 | + «**FLAG_KO 6가지 · 범례 접기 · 후보 자리로 확대 · 묶음 칸 강조 · 안내 6단계**» |
| `README.md` | 351 | «AI 제안은 복숭아 125 · **포도 2,502** · 사과 1,001 · 블루베리 1,195 = **4,823장** 전부» | «**지금 데이터셋 4,727장 전부**(포도 **2,406**)» + proposals 폴더 설명 |
| `README.md` | 373 | «열매마다 번호(1번, 2번, … **최대 95번**)» | «한 장에 보통 40개 · 가장 많은 장이 **123개** · 합계 **40,468개**» |
| `README.md` | 442 | «한 장에 **0.2~0.6초**(실측 0.19·0.18초)» | «한 장에 **0.2초쯤**» (앞뒤 숫자가 서로 어긋났음) |
| 체크리스트 | 18~34 | «먼저 볼 것 **3개**» 의 ③ = «묶음 칸이 오른쪽 맨 위인가» | «먼저 볼 것 **4개**» — ③ 을 **«저장한 것이 남아 있나»**(제일 위험한 고장)로 새로 넣고 묶음 칸은 ④ 로 |
| 체크리스트 | 37 | «안내 창이 떠 있는 동안 `1` 을 눌러도 뒤의 사진에 판정이 안 나간다» | 목록 화면에는 «뒤의 사진» 이 없음 → «**사진을 하나 연 뒤** `?` 로 띄우고 눌러 보세요» |
| 체크리스트 | 50 | «오른쪽 «도구» 를 **펴고**» | ««도구» 칸(**처음부터 펴져 있습니다**)의» |
| 체크리스트 | 65 | «작은 사진을 누른 뒤 `,` 를 누르면 **아까 보던 사진으로** 돌아온다» | 두 경우를 갈라 적고 «**`.` 가 아무 데도 안 가면** 알려 주세요» 로 바꿈 |
| 체크리스트 | 68~71 | «…확인창이 먼저 뜬다. 여기서는 «취소»» + 다음 줄에 «확인창 없이…» | 지금은 확인창이 **뜰 수 없음** → «**확인창 없이 «…없습니다» 만 뜨는 것이 정상**, 확인창이 뜨면 취소하고 알려 주세요» |
| 체크리스트 | 87·97 | «E. 열매 번호 — 사과·블루베리 (10분)» | **E1(사과) / E2(블루베리·번호 없는 과일)** 로 쪼갬 — 9항목은 10분을 넘김 |
| 체크리스트 | 99 | «눌러도 «…번호가 있는 사진에서만 됩니다» **안내만 뜬다**» | 단추가 `disabled` 라 **아무 일도 안 남** → «**눌러도·`K` 를 눌러도 아무 일이 안 일어나는 것이 정상**» |
| 체크리스트 | 122 | ««중복 그룹 일괄 제외 적용» 은 확인창까지만 보고 «취소»» | + «확인창에 «아직 안 본 **0장**» 이 뜨는 것이 지금은 정상입니다» |
| 체크리스트 | 134 | «지우는 명령은 …에 있습니다» | «**담당자가 실행합니다 — 여기서는 누르지 마세요.**» |
| 체크리스트 | 13·머리 | «묶음 A~G», «먼저 볼 것 3개» | «A · B · C · D · **E1 · E2** · F · G», «4개» · **«검색 칸에 이름을 붙여넣고 카드를 누르면 됩니다»** 한 줄 추가 |
| 체크리스트 | 131 | «E 에서 저장했으면 …» | «E1·E2 …» + «**먼저 볼 것 3** 에서 저장했으면 복숭아 `210629-t1-01` 의 «수정본 되돌리기»» |

파일별 diff 줄 수: `index.html` 4 · `help.html` 10 · `README.md` 40 · 체크리스트 50 줄.
백업: `app/_backup_260918_ui8_README.md` · `app/static/_backup_260918_ui8_{help.html,index.html}` ·
`문서/_backup_260918_ui8_260917_툴_사람확인_체크리스트.md`.

### 6-2. 화면으로 확인했고 **맞았던** 것 (고치지 않음)

- **화면의 `title` 풍선말 97개 전수** — 고친 뒤 «5단계»·«오른쪽 아래»·«맨 아래»·내부 표기(«사이클4 N3» 등) **0개**.
- 체크리스트가 이름으로 부른 **사진 7장 전부 실제로 열립니다**(`…image116`·`…image201`·`…image966`·
  `dataset2_front_630`·`210629-t1-01`·`210629-t4-17`·`Camera 3 Video (121)_721`).
- 숫자·화면 글자 **전부 일치**: 묶음 머리글 «**#93 · 19장**»(썸네일 19개) · 대표에 «남김» ·
  사과 «번호 **106개** (최대 106)» / 머리글 «— 원본 번호 마스크» · 블루베리 «번호 **163개**» / «— 검출팀 초벌» ·
  «고른 번호 N» 안내 · 후보 **1곳/2곳** · «후보 1/2»→«2/2» · 메모 풍선말에 좌표 전문 ·
  개수 **40,468 / 45,124** · «속이 찬 동그라미 **117**» · 되돌리기 알림 «되돌릴 것이 없습니다» ·
  일괄 제외 확인창이 뜨고 «취소» 로 닫힘 · **사용법 그림 10장 전부 200 응답**.
- help.html 의 판정 5가지 표·색 읽는 법·단축키 한 장·상자 절·열매 번호 절은 화면과 같습니다.

### 6-3. 체크리스트가 «초보자가 10분 묶음으로 따라 할 수 있는가»

| 물음 | 답 |
|---|---|
| 첫 세 항목이 **가장 위험한 것**인가 | ❌→✅ **아니었습니다.** ①(사과 알 색)·②(판정 단추)는 맞지만 ③ 이 «묶음 칸이 예쁘게 놓였나» 였습니다. **«저장한 것이 남아 있나»** 가 B·E 에 묻혀 있었는데 그것이 제일 위험합니다(사람 작업이 사라짐) → **③ 으로 올리고 묶음 칸을 ④ 로 내렸습니다.** |
| 각 항목에 «정상 / 이러면 알려 주세요» 가 둘 다 | ⚠️ **62항목 중 «알려 주세요» 가 붙은 것은 원래 1개뿐**이었습니다. 전부에 두 줄을 넣으면 문서가 배가 되므로, **판단이 애매한 항목**에만 붙였습니다(먼저 볼 것 ③·④ · C-65 · C-68 · E2-99 · G-122). 나머지는 머리말의 총론(«다르면 줄 번호만 알려 주세요»)에 맡깁니다 — **3차가 «전부에 붙여라» 로 판단하면 그때 늘리는 것이 낫습니다.** |
| 10분 묶음인가 | ⚠️→✅ **E 가 9항목·두 과일이라 10분을 넘겼습니다** → **E1(사과)·E2(블루베리·번호 없는 과일)** 로 쪼갰습니다. 나머지 묶음은 7~9항목으로 10분에 들어갑니다. |
| 전문용어 | ✅ 16비트·인스턴스·stem·mtime·localStorage 가 **한 번도 안 나옵니다**(grep 확인). |
| 되돌리기 어려운 조작 | ✅ «중복 그룹 일괄 제외 적용» 은 «확인창까지만 보고 취소» 로 적혀 있고, 실제로도 지금은 **바꿀 것이 0장**이라 실수로 눌러도 `changed=0` 입니다. 지우는 명령은 «담당자가 실행» 으로 못 박았습니다. |
| 🔴 남은 위험 | **B·H 가 시키는 «수정본 저장 → 수정본 되돌리기»** 가 N-A 그대로였다면 **동결된 제외·flag 를 지웠을 것**입니다. 제 고침이 **실서버에 실려야** 이 체크리스트를 사람에게 줄 수 있습니다. |

---

## 7. 회귀 (`t7_reg.py` · `t7b_run.log`) — **11/11**

```
node --check app/static/app.js                                   ✅
node --check app/static/ui.js                                    ✅
cycles/260917_신기능/cycle_1/stage1/sim.js                        ✅
cycles/260917_신기능/cycle_2/stage2/modesim.js                    ✅ 31개 중 31개 통과, 0개 실패
cycles/260917_신기능/cycle_2/stage1_client/boxsim.js               ✅ 50개 중 50개 통과, 0개 실패
python app/boxes.py 자체 점검                                     ✅
cycle_6_n6/stage1/n6_save.py  (N6 번호 저장이 번호 없는 전경을 안 지움) ✅ 실패 0
cycle_6_n6/stage1/n1_cut.py   (N1 이진본이 번호본을 자름)           ✅ 실패 0
cycle_5/stage1/live_c5.py     (읽기 전용 API 16항목)               ✅ 실패 0  ※ 모래상자 5161
cycle_3/stage1/api_seed.py    (상자 초벌 표본 40장)                ✅ 기대와 다른 것 0  ※ 모래상자 5161
py_compile server·instances·boxes·dupes·maskio                    ✅
```
`modesim.js`·`boxsim.js` 는 **지금 배포되는 `app.js` 의 함수를 글자 그대로 떼어 내** 돌립니다 —
같은 숫자가 나온 것은 «단축키·모드·상자 로직을 아무도 안 건드렸다» 는 증거입니다.
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
결과 파일도 `seed_after_sbx2.json` 으로 돌려 원본을 덮지 않았습니다.

---

## 8. 초보자 눈 — 다섯 시나리오와 녹취 충족표

### 8-1. 다섯 시나리오 (1366×768, 처음 온 사람처럼)

| 시나리오 | 막히나 | 무엇이 |
|---|---|---|
| (a) 한 장 검수 | **안 막힘** | 할 일 줄이 한국어로 «무엇을 보라» 를 말하고 판정 5단추가 늘 보입니다. |
| (b) 중복 묶음 정리 | **안 막힘**(사이클3 에서 고쳐짐) | 묶음 칸이 **오른쪽 맨 위**라 바로 보이고, 할 일 줄의 «묶음 #… ›» 을 누르면 강조됩니다. ⚠️ 다만 **지금은 어느 묶음에서도 «새로 제외할 사진이 없습니다» 만 뜹니다** — 문서에 적어 두지 않으면 «고장» 으로 보입니다(고쳤습니다). |
| (c) 상자 그리기 | **반쯤 막힘(남음)** | «상자 저장»·«YOLO 로 내보내기» 가 1366×682 뷰포트에서 **화면 밖**이라 WebDriver 가 «scroll into view 못 함» 을 낼 정도입니다. 옆 패널을 굴리면 나오지만 **처음 온 사람은 저장 단추를 못 찾습니다.** 묶음 칸(219px)이 맨 위로 올라오면서 **중복 사진에서는 더 밀립니다.** → 사이클 5 |
| (d) 사과 번호 고치기 | **안 막힘** | «고른 번호 N» → `D` → «지움 1 · 저장 안 됨» 이 또렷합니다. ⚠️ 용어가 아직 세 가지(«③ 열매 번호» / «열매 번호 편집» / «열매 개수(카운팅)»). |
| (e) flag 사진의 노란 점선 고치기 | **안 막힘 (이번에 좋아짐)** | «후보 자리로 확대» 한 번에 점(30×35화소)이 화면 폭의 13~15%가 되고, 번호 `N` 으로 붙인 뒤에도 **flag 와 좌표가 남습니다.** ⚠️ 번호 레이어의 흰 숫자가 점선을 덮는 것은 그대로(`J` 로 끌 수 있고 help 에 적혀 있음). |

### 8-2. 녹취 요구 대비 최종 충족표

| 녹취(0915 랩미팅 07:28~10:27) | 충족 | 근거 화면·수치 |
|---|---|---|
| «**굉장히 편하게** 이렇게 할 수 있는» (09:10) | **충족** | 한 장 10~30초 · 판정 5단추가 다섯 창 전부에서 스크롤 0 · 할 일 줄이 무엇을 볼지 한국어로 지정 · 처음 안내 6단계. `T1_*.png` 25장 |
| «**일일이 우리가 할 수는 없었고**» (09:10) | **충족** | 묶음 «대표만 남기고 제외» 한 번으로 묶음 정리(사과 **299**묶음·블루베리 42) · 진행 현황의 «중복 그룹 일괄 제외 적용» · 목록 «무엇부터 볼까» 5단추 + 의심 종류 단추 · «마스크에서 초벌 생성»(상자) · 열매 개수 자동 집계(사과 1,001장 189초). `T8_dash.png` |
| **AI 1차 → 사람 수정** | **충족** | 네 과일 **4,727장 전부** AI 제안이 있고, 파랑(AI)·빨강(원본)·초록(내 수정본)을 겹쳐 보며 `F`/`G` 로 덩어리째 가져옵니다. 판정 `2`(AI 제안으로 교체)가 한 번에 끝냅니다. `T4_*.png` |
| **카운팅과 세그멘테이션 둘 다** | **충족** | 맨 위 «지금 할 일» 3칸: ①마스크(네 과일) ②상자(네 과일) ③열매 번호(사과·블루베리). 번호가 없는 포도·복숭아는 **단추가 회색이고 이유가 적힙니다**(0918). 개수 실측 사과 **40,468** · 블루베리 **45,124**. `T3_dash.png` |
| **연속 스냅샷(거의 같은 사진) 걸러내기** (08:00·09:38) | **충족** | `duplicates.json` 으로 묶음을 만들고(사과 299·블루베리 42·포도 94), 목록 «거의 같은 사진(중복)» 단추 + 편집 화면 묶음 칸 + 대표 자동 선정. 지금 **사과 454장·블루베리 81장이 제외**로 반영돼 있습니다 |
| «**중간이 비어야 하는데 동그랗게**» (09:25) | **충족** | 자동 점검이 `filled_blob` 로 잡아 목록 단추(«속이 찬 동그라미 117»)와 할 일 줄에 **한국어로** 띄웁니다(0918) |
| «**복숭아 안 된 것들 · 블루베리 없는 것들 추가**» (09:38) | **부분** | 툴은 다 됩니다(붓·다각형·스마트 채우기·`N` 새 번호). 다만 **복숭아 amodal vs 윤곽 규칙이 교수님 결정 전**이라 사람이 무엇을 칠할지 아직 못 정합니다 — 툴 문제가 아닙니다 |
보안 관련 값·설정 세부는 공개본에서 생략했습니다.

---

## 9. 시험한 조작 · 안 한 조작

| 조작 | 어디서 | 했나 | 결과 |
|---|---|---|---|
| 다섯 창 × 5장 배치 재기(`elementFromPoint`) | 모래상자 5161 | ✔ | §1 (195/195) |
| «후보 자리로 확대» 진짜 클릭 + 화소 측정 + 원본 상호상관 | 모래상자 | ✔ 4장 | §1 |
| **확대 상태에서 붓질·상자 그리기·`0` 복귀** | 모래상자 | ✔ 4장 | 전부 통과 |
| 후보 2곳 사진에서 순회 | 모래상자 | ✔ 2장 | «1/2»→«2/2»→«1/2» |
| 네 과일 CSV 의 suspect_flags **전수 집계** | 파일 | ✔ | 6종 · 사전에 6종 전부 |
| 종류별 사진을 실제로 열어 한국어 확인 | 모래상자 | ✔ 4종 + merged_blob | 영어 0건 |
| 범례 접기·펴기 + localStorage | 모래상자 | ✔ | 5→1→5 |
| 썸네일 이동 뒤 `,`·`.` — **목록에 있을 때/없을 때 갈라서** | 모래상자 | ✔ | §4 N-D |
| 전수표(네 과일 × 3작업 · 1차와 다른 사진 7장) | 모래상자 | ✔ | §2 |
| flag 사진 «붓질→저장→되돌리기», «번호 N→저장→되돌리기» | 모래상자 | ✔ | §2·§4 N-A |
| 진짜 브라우저 두 대 × 50판정 + API 506회 | 모래상자 | ✔ | §3 |
| 0장 되돌리기(백업 파일·mtime) | 모래상자 API | ✔ | 늘지 않음 |
| 진행 현황 세기 단추·확인창·알림 글자 | 모래상자 | ✔ | §6-2 |
| 화면 `title` 풍선말 97개 전수 | 모래상자 | ✔ | §4 N-F |
| 사용법 그림 10장 HTTP | 모래상자 | ✔ | 전부 200 |
| 회귀 11종 | 현재 코드·모래상자 | ✔ | §7 |
| 서버 diff 전문 읽기(`server.py`·`instances.py`·`app.js`·`ui.js`·`index.html`·`style.css`) | 파일 | ✔ | §5-1 |
| **실서버 쓰기 단추** | — | ✘ 일부러 안 함 | GET 과 `/login` POST 만 |
| **실서버 재시작** | — | ✘ | 서버 코드를 고쳤으므로 **3차에 맡깁니다** |
| 크롬·엣지·모바일 | — | ✘ | 서버에 파이어폭스뿐 → 사람 체크리스트로 넘김 |
| 내보내기 `export_dataset.py` dry-run 전체 | — | ✘ | `/api/boxes_stats`·`instance_stats` 재집계로 갈음 |
| 포도·복숭아 «세기» 를 진짜 8분 돌리기 | — | ✘ | 단추가 없다는 것과 `has_num=false` 만 확인 |
| 범례가 붓질을 막는 «비율» 재측정 | — | ✘ 정성만 | (120,120) 붓질이 안 먹는 것만 확인(§4 N-I) |
| 공용 `T/data`·원본·새 데이터셋·`T/inspect`·`T/cycles/260917_ab` 쓰기 | — | ✘ | 읽기만 |
| `작업기록.md` 갱신 | — | ✘ | 지시대로 안 했습니다 |

---

## 10. 자취 (지울목록에 적을 것)

| 무엇 | 어디 | 크기 | 처리 |
|---|---|---|---|
| 내 시험 스크립트 15개 | `cycles/260917_ui/cycle_4/stage2/{lib,t1_layout,t2_zoom,t2b_zoom_follow,t3_misc,t3b_follow,t4_matrix,t4b_matrix_rest,t5_conc,t6_flagfix,t6b_release,t7_reg,t8_docs,t8c_docs_rest,t9_revertbug}.py` | 약 110KB | 남깁니다(재현용) |
| 내 결과·로그 | 같은 폴더의 `t1_layout.json`·`t2_zoom.json`·`t4*.json`·`t5_conc.json`·`t8_tips.json`·`t*_run.log` | 약 240KB | 남깁니다 |
| 스크린샷 **63장** | `~/ff_shots/ui4/stage2/` | **27MB** | 남깁니다(증거) |
| 모래상자 서버 | `127.0.0.1:5161` | — | **껐습니다** — 내가 띄운 PID 만. 남의 **5111·5100·5101·5105 는 안 건드렸습니다** |
| 모래상자 `app/` | `cycles/260917_ui/cycle_1/stage2/sandbox/app/` | — | 지금 `T/app` 사본(내 고침 포함) |
| 모래상자 `data/` | 같은 폴더 | — | 네 과일 `status.json` 을 **`T/data` 것으로 되돌려 놓았습니다.** 시험이 만든 `masks_fixed`·`instances_fixed`·`boxes/*.json` 몇 장은 앞 사이클 것과 섞여 남아 있습니다(지우려면 폴더째) |
| 백업 **8개** | `app/_backup_260918_ui8_{server,instances,boxes,maskio}.py` · `app/_backup_260918_ui8_README.md` · `app/static/_backup_260918_ui8_{help.html,index.html}` · `문서/_backup_260918_ui8_260917_툴_사람확인_체크리스트.md` | 약 200KB | 남깁니다 |
| 임시 사본 | 스크래치패드의 `reg_live_c5.py`·`reg_api_seed.py`·`sbx_labeltool.env`·`docfix*.py`·`tmpfix.py` | 작음 | 자동으로 사라집니다 |

**공용 `T/data` 지문 — 시작(01:55)과 끝이 같습니다**: 파일 **7,397개 · 78,747,036바이트** ·
`apple`·`blueberry` `status.json` **09-18 01:32:56** · `grape` 09-17 00:55:26 · `peach` 09-17 14:59:05.
`T/inspect`·`T/cycles/260917_ab`·원본 데이터셋에는 **한 바이트도 쓰지 않았습니다.**
실서버 로그의 **오늘(09-18) 500 0건 · Traceback 0건**(로그에 남은 Traceback 12건은 전부 09-16 23:35~09-17 00:22 의 옛 것) ·
실서버 PID **4143915**(01:45:45 기동) 그대로 · 끝에 GET 으로 확인한 판정 수도 사과 454/51 · 블루베리 81/4 로 그대로.

> ⚠️ 작업 도중 **다른 세션(«freeze» 작업자)이 `app/README.md` 를 01:54:46 에 고쳤습니다**
> (데이터 숫자를 452·82 → **454·81** 로 맞추고 «`--tool` 을 다시 돌리지 마십시오» 경고를 넣었습니다 — 옳은 수정입니다).
> 저는 그 판 위에 제 문장 고침을 얹었고, `_backup_260918_ui8_README.md` 가 **그 판**입니다.
> 1차가 §9 에 적은 «밖에서 바뀐 01:32:56» 도 그 작업자의 정상 작업임이 확인됐습니다(총괄 지시).

---

## 11. 3차(Fable) · 사이클 5 가 판정할 것

1. 🔴 **내가 고친 서버 코드 두 건을 실서버에 실을 것인가.** (N-A 되돌리기 복구 · N-B 임시 이름 겹침)
   **N-A 는 사람에게 툴을 넘기기 «전» 에 반드시 들어가야 한다고 봅니다** — 체크리스트 B·H 가
   그 두 단추를 누르게 시키고, 지금 상태로는 동결된 제외 454·81장과 flag 51·4장이 지워집니다.
   실으려면: 저장 중인 사람이 없는지 로그로 보고 `bash app/run.sh restart` → 네 과일·게이트·개수 확인.
2. 🟠 **N-C — «수정본 저장» 이 «제외» 를 풀어도 되는가.** 과제는 flag 만 풀린다고 했습니다.
   («제외 사진을 고쳐 저장하면 확인창을 띄운다» 가 제 제안이지만, 이건 **규칙 결정**이라 제가 하지 않았습니다.)
3. 🟠 **(c) 상자 저장 단추가 화면 밖** — 묶음 칸이 맨 위로 오면서 중복 사진에서는 더 밀립니다.
   ②상자 칸을 위로 올리거나 «상자 저장» 을 판정 줄 옆에 두는 안. → 사이클 5
4. 🟡 **N-H — `TOUR_KEY` 를 `…_260918_ui4` 로 올릴 것인가.** 안 올리면 곽동신·최인훈에게 6단계가 안 뜹니다.
5. 🟡 **체크리스트의 «이러면 알려 주세요» 를 62항목 전부에 붙일 것인가**(지금은 위험한 6항목만).
6. 🟡 남겨 둔 것: **N-I**(범례가 왼쪽 위 붓질을 막음) · **N-J**(app.js↔style.css 주석 모순) ·
   **N-K**(상자·번호 모드가 사진을 넘겨도 남는데 안내가 없음) · 사이클3 의 **N11**(`kept` 뜻) ·
   **N13**(`ff.open_photo`) · ponytail 세 가지(`/api/list` 가 호출마다 `flag_counts` 를 다시 셈 등).
7. 🟡 **README §2 의 «데이터 루트» 설명** — 지금 사과·블루베리가 `datasets_resized_2mp` 에서 읽히고 있습니다.
   이것이 **의도한 바가 맞는지**(검수판 폴더에 사과·블루베리가 없어서 생긴 일) 총괄이 확인해 주세요.

---

## 12. 그대로 재현하는 명령

```bash
T=/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴
PY=/home/kds0206/.conda/envs/kwak/bin/python
S2=$T/cycles/260917_ui/cycle_4/stage2
SB=$T/cycles/260917_ui/cycle_1/stage2/sandbox
DR=/data/project/2026summer/kds0206/datasets_reviewed_260916

# 1) 모래상자 — app 을 지금 T/app 으로, 네 과일 status/duplicates 를 T/data 로
rsync -a --exclude logs --exclude cache --exclude __pycache__ $T/app/ $SB/app/
for f in apple blueberry peach grape; do cp $T/data/$f/status.json $SB/data/$f/status.json;
                                         cp $T/data/$f/duplicates.json $SB/data/$f/duplicates.json; done
ss -ltnp | grep 5161                     # 비었는지 먼저
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
  nohup $PY -u server.py >> logs/server5161.log 2>&1 &

cd $S2
$PY t1_layout.py        # 배치 195/195      (다섯 창 × 5장)
$PY t2_zoom.py          # 확대 51/53        (2건은 시험 오류 — t2b 가 설명)
$PY t2b_zoom_follow.py  # 8/8
$PY t3_misc.py          # 한국어·세기·범례·0장백업 35/42 (7건은 시험 오류 — t3b)
$PY t3b_follow.py       # 14/14
$PY t4_matrix.py        # 전수표 복숭아
$PY t4b_matrix_rest.py  # 전수표 포도·사과·블루베리 106/110
$PY t9_revertbug.py     # 🔴 N-A — 고치기 전이면 8실패, 고친 뒤면 통과
$PY t5_conc.py          # 동시성 13/13 (고치기 전이면 500 1건)
$PY t6_flagfix.py       # flag 시나리오 21/21
$PY t6b_release.py      # flag 풀기·되살리기 8/8
$PY t7_reg.py           # 회귀 11/11
$PY t8_docs.py          # 문서 대조(앞)   ; $PY t8c_docs_rest.py   # 문서 대조(뒤) 20/21

# 2) 끄기 — 반드시 PID 로 (남의 5111 을 죽이지 않게)
ss -ltnp | grep 5161                     # PID 확인 뒤 kill <PID>

# 3) 내 수정을 되돌리려면
cd $T/app        && for f in server.py instances.py boxes.py maskio.py README.md; do cp _backup_260918_ui8_$f $f; done
cd $T/app/static && for f in help.html index.html; do cp _backup_260918_ui8_$f $f; done
cd /data/project/2026summer/kds0206/문서 && cp _backup_260918_ui8_260917_툴_사람확인_체크리스트.md \
                                               260917_툴_사람확인_체크리스트.md
cd $T && bash app/run.sh restart
```
