작성: 2026-09-18

# UI 개선 사이클 4 — 3차 최종 판정 (Fable 5.1)

읽은 것: `stage1_work.md` · `stage2_review.md` 요지 · 2차가 고친 서버 코드 diff(`app/_backup_260918_ui8_server.py` ↔ 현재)를 내가 읽음.

## ① 결론 — 합격, 서버 수정 두 건을 실서버에 실었습니다(03:05 재시작)
- 1차 주장 재현: 묶음 칸 195/195(다섯 창 × 5종 사진, 실제 뷰포트는 1차보다 좁은 1366×682 조건) · «후보 자리로 확대» 59/59(점선 위치를 원본과의 상관으로 독립 검증, 7화소) · 한국어화 6종 전부 · 전수표 161/161(1차와 한 장도 안 겹치는 사진, 제외·flag 포함) · 동시성(진짜 브라우저 두 대 + API 506회, 소실 0) · 회귀 11/11.
- 2차가 찾아 고친 것 **인정**: 🔴 **N-A «수정본 되돌리기» 가 AI 3회 검수의 제외·flag 와 메모를 통째로 지움**(마스크 쪽에만 `prev` 복구 장치가 없었음 — 번호 쪽은 P2 때 넣음) → `update_status(keep_prev=True)` + `api_revert` 가 `prev` 로 복구. 🟠 N-B 브라우저 두 대가 같은 목록을 열면 썸네일 500(임시 파일 이름이 PID 뿐인 남은 6곳) → 스레드 id. 문서 36문장(`,`·`.` 순서의 조건, «5단계»→6단계, help 꼬리말 날짜, 체크리스트 «먼저 볼 것» 4개·E1/E2 분리).
- 실서버: `py_compile` 통과 → 로그에 최근 저장 없음 확인 → `bash app/run.sh restart` **2026-09-18 03:05**. 확인: 네 과일 개수 그대로(사과 제외 454·flag 51 / 블루베리 81·4), 쿠키 없는 API 401.
- 2차의 질문 ⑥ «사과·블루베리가 `datasets_resized_2mp` 에서 읽히는 것이 의도인가» → **의도입니다.** 툴은 1,001·1,195장 전부를 보여 주고 제외는 «제외» 상태로 표시합니다(검수판 폴더에는 남긴 사진만 있으므로 사람이 제외 판정을 되짚어 볼 수 없게 됨). README §2 에 한 줄로 명시할 것(사이클 5).

## ② 사이클 5(총괄)가 할 일
1. **N-C 제외 사진에 «수정본 저장»/«AI 제안으로 교체» 를 하면 `fixed` 가 되어 데이터셋에 되살아남** → 판정: **제외는 마스크 저장이 뒤집지 않는다**(번호 저장 P2 와 같은 규칙 — exclude 는 «이 사진을 쓸지» 에 대한 판정). 저장은 되고 상태는 exclude 유지 + 화면에 «제외 상태는 그대로입니다. 되살리려면 1(원본 OK)이나 상태 변경을 누르세요» 안내. flag 는 지금처럼 `fixed` 로 풀림(사람이 고쳤으므로).
2. 상자 모드의 «상자 저장» 단추가 1366 창에서 화면 밖 → ②상자 작업일 때 판정 줄 자리에 «상자 저장 · 초벌 생성 · 되돌리기» 를 보이게(또는 상자 패널을 옆 패널 맨 위로). ③번호 작업의 «번호 저장» 도 같은 점검.
3. `TOUR_KEY` 를 올려 이미 «다시 보지 않기» 를 누른 사람에게도 새 6단계 안내가 한 번 뜨게 · 범례가 왼쪽 위 붓질을 막는 것(N-I) → 범례를 캔버스 밖(할 일 줄 아래)으로 옮기거나 접힌 상태를 기본으로 · 상자 모드가 사진을 넘겨도 남는다는 안내(N-K) · app.js↔style.css 주석 모순(N-J) · README §2 에 «사과·블루베리는 팀 표준 원본에서 읽고 제외는 상태로 표시» 한 줄.
4. **총괄**: 사이클 1~4 의 열린 문제 전수 목록(해결/미해결/사람 결정), 녹취 요구 충족표 확정, 네 과일 × 세 작업 최종 확인, 크롬에서 사람이 볼 체크리스트 확정, **UI 최종 판정서 초안 `/data/project/2026summer/kds0206/문서/260918_툴UI개선_5회검수_최종판정.md`**.

> 후속(2026-09-18): 위 «사과 제외 454» 는 이 판정 시각(03:05)의 값입니다. 데이터 재검수가 03:07:53 에 **572**(남긴 429)로 확정했습니다 — `cycles/260917_ab/cycle_5/stage3_final.md`.
