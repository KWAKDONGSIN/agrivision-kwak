작성: 2026-09-18

# UI 개선 사이클 3 — 3차 최종 판정 (Fable 5.1) · 겸 데이터 재검수 사이클 2(툴 반영) 3단계

읽은 것: `stage1_work.md` · `stage2_review.md` 요지 · 2차가 고친 서버 코드 diff(`app/_backup_260917_ui6_{server,instances}.py` ↔ 현재)를 내가 한 줄씩 읽음.

## ① 결론 — 합격, 서버 수정 두 건을 실서버에 실었습니다
- 툴 결함 **P1~P4 는 진짜 고쳐졌습니다.** 2차가 «고치기 전» 모래상자와 «고친» 모래상자를 나란히 띄워 A/B: P1 전 6통과/10실패 → 후 16/16(진짜 브라우저로 «일괄 제외→되돌리기→일괄 제외» 를 사과·블루베리 둘 다 눌러 매 단계 status.json 차이 0), P2 전 13/10실패 → 후 23/23, P3·P4 는 화면 문구와 노란 점선 위치를 캔버스 화소 + 원본 사진 상호상관으로 검증(어긋남 14~16화소).
- 2차가 새로 찾아 고친 3건 **전부 인정**: (가) 🔴 동시에 저장하면 판정이 통째로 사라지던 것 — `write_status` 임시 파일 이름이 PID 뿐 + `write_status_entry` 만 자물쇠 없음(0916 부터 있던 결함, 46 동시 요청에서 판정 40건 소실 재현) → 스레드 id 를 붙이고 같은 `status:<과일>` 자물쇠 사용. 내가 diff 를 읽어 확인: `threading` 은 이미 import, 부르는 쪽은 `inst:` 자물쇠만 쥐므로 교착 없음. (나) 번호를 두 번 저장하면 flag 사유와 노란 점선이 사라지던 것 → `prev` 의 맨 처음 메모 유지. (다) 0장인데 «되돌렸습니다» 라고 하던 문구.
- **실서버 조치(2026-09-18 01:00)**: `py_compile` 통과 → 데이터셋 v2 를 툴에 반영(`final/build_reviewed_dataset_260917_v2.py --tool`, 백업 `data/<과일>/_status_backup_260918_v2.json`) → `bash app/run.sh restart`(새 PID 4020748). 확인: `/api/fruits` 사과 제외 452·flag 51 / 블루베리 82·4(복숭아·포도 불변) · 쿠키 없는 API 401 · 내보내기 dry-run 이 사과 549·블루베리 1,113(= 새 폴더)로 일치.
- 2차의 경고 «v2 반영이 `src` 를 넣으면 P1 보호가 깨진다» → 내 반영 코드는 `src` 를 넣지 않습니다(by=«AI 3회 검수…» 만). 일괄 제외 되돌리기는 `src="dup_bulk"` 만 되돌리므로 v2 제외는 보호됩니다.

## ② 사이클 4 가 할 일 (확정 · 우선순위순) — 주제: 전 조작 회귀 + 문서 일치 + 남은 사용성
1. **N3 묶음 칸이 화면 밖**(1366px 에서 옆 패널을 1,331px 내려야 보임) → 묶음 칸을 옆 패널 **맨 위**(사진 이름 칸 바로 아래, 중복 묶음일 때만 보임)로. «이 사진에서 할 일» 줄의 묶음 안내를 누르면 그 칸이 강조. 합격 기준: 다섯 창에서 묶음 단추가 스크롤 없이 보임.
2. N5 할 일 줄의 영어 의심 이름을 `FLAG_KO` 로 한국어화 · 노란 점선 후보가 있으면 **«후보 자리로 확대»** 단추(또는 점선 상자 클릭 → 그 자리로 확대) · 메모 칸을 긴 글이 보이게(두 줄 또는 풍선말에 전문) · 포도·복숭아의 «세기» 단추 숨김(번호 없는 과일) · 묶음 썸네일로 이동한 뒤 `,`·`.` 순서 정리 · 0장일 때 백업 파일을 만들지 않기 · 범례가 사진 위쪽을 가리는 정도 줄이기(접기 단추).
3. **전 조작 회귀(모래상자 + 진짜 브라우저)**: 네 과일 × (마스크·상자·번호) 전수표 — 열기·그리기·저장·다시 열기·되돌리기·내보내기. 데이터셋 v2 가 반영된 상태(사과 제외 452 등)에서. 기존 시험 전부(sim·modesim 31·boxsim 50·boxes.py·`cycles/260917_신기능/` 의 API 시험들·N1·N6 시험).
4. **문서 일치**: help.html·README(12·13절 포함)·첫 방문 안내·풍선말이 지금 화면과 글자까지 맞는지 전수(틀린 문장은 고침). help.html 에 새 기능 예시 화면 캡처(묶음 정리·노란 점선·개수 화면). `문서/260917_툴_사람확인_체크리스트.md` 를 지금 화면 기준으로 새로 씀(크롬에서 사람이 볼 것: 옛 A~H + 새 기능).
5. 동시성: 2차가 고친 자물쇠를 다른 방식으로 재시험(두 브라우저가 같은 과일을 동시에 판정 100회) + 묶음 제외와 개별 판정이 겹칠 때.

## ③ 사이클 5
총괄 판정 + 녹취 대비 최종 점검 + 사람 체크리스트 확정.
