작성: 2026-09-18

# UI 개선 사이클 3 · 2차 검수 (Opus 5)
## 겸 — 데이터 재검수 사이클 2 · **2단계**(툴 결함 P1~P4 수정 확인)

대상 `T = /data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴`
1차 보고서 `cycles/260917_ui/cycle_3/stage1_work.md` · 과제 `cycle_2/stage3_final.md` ③(1~7)
\+ `cycles/260917_ab/cycle_2/stage1_report.md` 의 **P1~P4**

**1차를 믿지 않고 전부 내 코드·내 브라우저로 다시 쟀습니다.** 1차 스크립트(`cycle_3/stage1/*.py`)는
한 번도 돌리지 않았고, 인용도 하지 않았습니다. 내 스크립트는 전부 `cycle_3/stage2/` 에 있습니다.

**단정 합계(고친 코드): 내 시험 267개 중 260 통과 · 7 «실패»** — 7건의 정체는
**N3(묶음 칸이 화면 밖) 다섯 창 × 1 · N6(`,`·`.` 순서) 1 · 내 잣대가 너무 깐깐했던 것 1**(§2-4)입니다.
그리고 **«고치기 전» 코드에서는 일부러 25건이 실패**합니다(P1 10 · P2 10 · P3·P4 3 · 동시쓰기 2) —
그것이 «정말 고쳐졌다» 는 증거입니다.
\+ 회귀 8종 전부 통과 · 브라우저 스크린샷 39장.

🔴 **내가 서버 코드를 고쳤습니다(3곳).** 실서버 재시작은 **하지 않았습니다** — 3차에 맡깁니다(§8).

---

## 0. 합격 기준 (실행 **전**에 정한 것)

| # | 무엇을 | 합격선 |
|---|---|---|
| A | **P1 (가)** | 고치기 «전» 트리에서는 **실패**하고 고친 코드에서는 통과할 것. 실패가 재현되지 않으면 «고쳤다» 를 인정하지 않음 |
| B | **P1 (나)** | 진짜 브라우저로 사과·블루베리 둘 다 「일괄 제외」→「되돌리기」→「일괄 제외」를 실제 클릭. **매 단계 status.json 이 3차 판정 원본과 한 글자도 다르지 않을 것**(exclude 314/71 · flag 68/6 · 메모 · `by`) |
| C | **묶음 단추** | 사람이 판정한 구성원(ok·fixed·flag)·대표가 flag·이미 3차 제외·두 사람이 다른 이름 — 네 상황에서 **사람 판정을 한 장도 바꾸지 않을 것** |
| D | **P2 (다)** | flag·exclude·판정 없음 세 사진에서 저장→되돌리기→저장 2번→되돌리기를 하고, **판정·메모·`by`·내보내기 포함 여부가 처음으로 돌아올 것** |
| E | **P3·P4 (라)** | 노란 점선이 **메모 좌표와 같은 자리**에 그려질 것(화면 코드를 믿지 말고 화소로 잴 것) · 좌표 없는 flag·깨진 좌표에서 오류 0 · 대표에게 «4 제외» 를 안 시킬 것 |
| F | **새 API 3종** | 이상한 입력·동시 요청·캐시 손상에서 **500 이 0건**, 합계가 딴 데서 온 숫자(원본 마스크·`inspection.csv`)와 일치할 것 |
| G | **배치** | 다섯 창에서 판정 5/5·한 겹·가로 넘침 0 · 묶음 썸네일이 캔버스를 가리거나 밀어내지 않을 것 |
| H | **회귀** | `node --check`·31/31·50/50·`boxes.py`·실서버 읽기 시험·**N6·N1** 이 전과 같을 것 |
| I | **공용 데이터** | 검수 끝에 `T/data` 가 시작과 같을 것(파일 수·바이트·`status.json` 시각) |

**판정: A·B·C·D·E·F·H·I 합격 · G 부분 불합격**(묶음 칸이 다섯 창 전부에서 화면 밖 — §9 N3).

---

## 1. 항목별 한눈에

| # | 항목 | 판정 | 근거(내 스크립트) |
|---|---|---|---|
| P1 (가) | 고치기 전/후 A/B | ✅ | 전 **10 실패**/16 · 후 **16/16** — `p1_ab.py` |
| P1 (나) | 진짜 브라우저 클릭(사과·블루베리) | ✅ | **30/30**, 매 단계 diff 0 — `b_p1.py` |
| P1 (나) | 묶음 단추 네 상황 + 이상한 입력 | ✅ | **28/28** — `p1_group.py` |
| P2 (다) | 번호 저장·되돌리기 | ✅ (내가 한 곳 더 고침) | 전 **10 실패**/23 · 후 **23/23** — `p2_ab.py` · `p2_twice.py` |
| P3 | 대표 문구 | ✅ | 전 «…나머지는 4 제외» · 후 «이 묶음의 대표(남길 장)입니다» — `b_p34.py` |
| P4 | 노란 점선이 메모 좌표 자리 | ✅ | 전 **노란 화소 0** · 후 사진 대조 상관 0.71·0.79, 어긋남 14·16화소 — `b_p4_match.py` |
| P4 | 좌표 없음·깨진 좌표 | ✅ | 8가지 깨진 메모에서 **오류 0** — `b_p34.py` |
| ② | `/api/list?flag=` 견고성 | ✅ | 없는 이름·빈 값·다른 과일·`suspect=1`·이상한 값 — `api2.py` |
| ③ | `/api/instance_stats` | ✅ | 캐시 손상·동시 6요청·계산 중 요청·무효화·무작위 30장 대조 — `api2.py` **43/43** |
| ① | 묶음 제외/되돌리기 API | ✅ | 401·404·400·거대 입력·섞인 stem — `p1_group.py` |
| 화면 1~7 | 다섯 창 배치 | ⚠️ | 판정 5/5·한 겹·넘침 0·캔버스 47~65% ✅ / **묶음 칸이 다섯 창 전부 화면 밖** ❌ — `b_layout.py` |
| 화면 7 | 범례 칩이 붓질을 막는 넓이 | ⚠️ | 캔버스의 **3.6%**(1100px) · 위쪽 1/4 의 **14.5%** — `b_layout.py` |
| 화면 | 썸네일 이동 뒤 `,`·`.` | ⚠️ | `.` 은 안 움직이고 `,` 는 **목록 앞 사진**으로 감 — `b_layout.py` |
| 사용성 | 다섯 시나리오 | ⚠️ | §5 (막히는 곳 3 · 글자 과다 · 영어 용어 노출) |
| 회귀 | 8종 | ✅ | §6 전부 통과(N6·N1 포함) |
| 코드 | 리뷰 | ⚠️ | §7 — **동시 저장에서 판정이 사라지는 결함**을 찾아 고침 |
| 공용 데이터 | 안 건드림 | ✅ | 파일 **7,395개 · 78,635,531바이트** · `status.json` 시각 22:22·22:24 그대로 |

---

## 2. P1~P4 — 고치기 «전» 과 «후»

«전» 트리: `cycles/260917_ui/cycle_3/stage2/sandbox_before/`
(`app/_backup_260917_ui5_{server,instances,dupes,boxes}.py` + `static/_backup_260917_ui5_*` 를 그대로 놓고
`data/` 는 공용 3차 판정 상태를 복사, 포트 **5142**).
«후» 트리: 기존 모래상자 `cycle_1/stage2/sandbox/` 에 지금 `T/app` 을 복사, 포트 **5141**.
보안 관련 값·설정 세부는 공개본에서 생략했습니다.

### 2-1. P1 — 「일괄 제외 → 되돌리기 → 일괄 제외」 (`p1_ab.py`, 내가 새로 쓴 시험)

| 단계 | **전(5142)** | **후(5141)** |
|---|---|---|
| ① 일괄 제외 | 바뀐 것 0 ✅ | 바뀐 것 0 ✅ |
| ② 되돌리기 | 사과 **exclude 314 → 0** · 메모 전부 지워짐(차이 628건) ❌ / 블루베리 **71 → 0**(142건) ❌ | **차이 0건** ✅ · 응답 `changed 0 · kept_other 314` |
| ③ 다시 일괄 제외 | 사과 exclude 314 이지만 **flag 68 → 59** · 원본과 **350건 차이**(9장이 뒤바뀌고 314장의 `by` 가 «AI 3회 검수» → «곽동신») ❌ | **차이 0건** ✅ |
| 합계 | **6 통과 / 10 실패** | **16 통과 / 0 실패** |

→ ab사이클2 P1 을 **내 코드로 그대로 재현**했고(사과 flag 68→59, 9그룹), 고친 코드에서는 사라집니다.
(원인도 확인: `dupes.pick_representative` 가 flag 를 건너뛰던 줄. 지금은 `exclude` 만 건너뜁니다.)

### 2-2. P1 (나) — 진짜 브라우저로 실제 클릭 (`b_p1.py` · 스크린샷 `~/ff_shots/ui3/stage2/dash_*.png`)

사과·블루베리 둘 다 진행 현황 탭에서 **단추를 눌렀습니다**(확인창 글자를 읽고 «확인»).

```
사과   확인창① «중복 그룹 190개에서 대표를 뺀 314장 중 «아직 안 본» 0장을 «제외» 로 표시합니다.
                (314장은 이미 제외돼 있습니다)»                      → 클릭 뒤 status.json 차이 0
       알림창②  «이 단추로 넣은 제외가 없습니다 — 되돌릴 것이 없습니다.
                (지금 «제외» 314장은 AI 판정·사람이 넣은 것이라 이 단추가 건드리지 않습니다)»
                                                                    → 클릭 뒤 status.json 차이 0
       클릭③   → 원본과 **한 글자도 다르지 않음** (메모·by 384장 전수 동일)
블루베리 같은 절차 → 33그룹·71장 · 차이 0 · exclude 71 · flag 6 그대로
```

되돌리기가 **정말 돌긴 하는지**도 따로 확인했습니다: 구성원 2장을 «아직 안 봄» 으로 만들고 일괄 제외를
누르면 확인창이 «2장» 이라고 말하고 그 2장만 제외되며, 되돌리기 확인창은
«이 단추로 넣은 제외 **2장만** … (AI 판정·사람이 넣은 제외 **312장**은 그대로 둡니다)» 라고 말하고
실제로 그 2장만 돌아옵니다(exclude 312·flag 68 유지).

**편집 화면의 «이 묶음: 대표만 남기고 제외» / «방금 제외한 것 되돌리기» — 네 상황 실제 클릭**

| 상황 | 결과 |
|---|---|
| 사람이 판정한 구성원(ok·fixed·flag)이 낀 묶음 | 확인창 없이 «이 묶음에는 새로 제외할 사진이 없습니다» · **status.json 차이 0** ✅ |
| 대표가 flag 인 묶음(`…image196`) | 대표는 flag·좌표 메모 그대로, 나머지 1장만 제외(`src=dup_group`) ✅ · 되돌리기로 표식까지 지워짐 ✅ |
| 구성원이 이미 3차 제외인 묶음 | 아무것도 안 바뀌고 되돌리기 단추는 숨어 있음 ✅ |
| **두 사람이 다른 이름으로** | 곽동신이 3장 제외 → 안홍렬이 되돌리기: **한 장도 안 돌아감(안전)** ✅ 다만 화면이 «되돌렸습니다» 라고 **거짓말**을 했습니다 → 내가 고쳤습니다(§8 고침2) |

### 2-3. P2 — 번호 저장·되돌리기 (`p2_ab.py` · `p2_twice.py`)

세 사진(**flag**+좌표 메모 / **exclude**(3차 중복) / **판정 없음**)에서
저장 → 되돌리기 → 저장 → 저장 → 되돌리기 를 실제로 돌리고 매 단계 status 항목과
**내보내기 포함 여부**(`export_dataset.py` 와 같은 규칙을 모래상자의 `dupes.py` 로 직접 계산)를 적었습니다.

| | **전(5142)** | **후(5141)** |
|---|---|---|
| flag 사진에 저장 | `flag → fixed` · 메모가 «번호 편집» 으로 **덮어써짐** ❌ | `flag` 유지 · 메모 «번호 편집 erase=1 · (이전) 사람 확인 필요: … 900,939,944,975 …» ✅ |
| 되돌리기 | `unreviewed` + «번호 편집 되돌림»(원래 판정 안 돌아옴) ❌ | `flag` + 원래 메모 + 원래 `by`(AI 3회 검수) 복원 ✅ |
| exclude 사진 | `exclude → fixed` ❌ | `exclude` 유지 ✅ |
| 내보내기 포함 여부 | 두 사진 다 안 바뀜(중복 규칙이 따로 막아 줌) | 안 바뀜 ✅ |
| 합계 | **13 통과 / 10 실패** | **23 통과 / 0 실패** |

🔴 **다만 1차의 P2 수정에 빈틈이 있었습니다 — 번호를 «두 번» 저장하면 flag 사유가 화면에서 사라집니다.**

```
저장 0번: 메모 «사람 확인 필요: … (x1,y1,x2,y2: 900,939,944,975) …»   노란 화소 117 · 안내 있음
저장 1번: 메모 «번호 편집 erase=1 · (이전) 사람 확인 필요: … »          노란 화소 108 · 안내 있음
저장 2번: 메모 «번호 편집 erase=1»                                  노란 화소 **0** · 안내 **없음**   ← 문제
```

`prev` 에는 남지만 **화면은 `note` 만 봅니다** — P4 의 노란 점선이 `note` 의 좌표로 그려지므로
«왜 flag 였는지»와 «어디를 고쳐야 하는지»가 같이 사라집니다. 사람이 두 번 저장하는 것은 흔한 일입니다.
→ **고쳤습니다**(§8 고침1). 고친 뒤 저장 2번에서도 노란 화소 117 · 안내 있음(`p2_twice.py` 3/3).

### 2-4. P3·P4 — 화면 문구와 노란 점선 (`b_p34.py` · `b_p4_match.py`)

**P3** — 나머지가 전부 제외된 묶음의 대표(`…image71`)에서 «이 사진에서 할 일» 줄:

| 전(5142) | 후(5141) |
|---|---|
| «거의 같은 사진 묶음 #25 (4장) — 대표 한 장만 남기고 나머지는 **4 제외**.» | «**이 묶음의 대표(남길 장)입니다 — 나머지 3장은 이미 제외됐습니다. 마스크만 보시면 됩니다.**» |

**P4** — 좌표→화면 변환을 «그쪽 코드» 로 계산하면 순환 논증이 되므로 **두 가지 방법**으로 쟀습니다.

1. **캔버스 화소 읽기**: 레이어를 전부 끄고 `#ffd400`(정확히 255,212,0) 화소만 골라 상자 자리를 잼.
   1366×768·사과 `…image196`: 세로 점선 두 줄이 x=661·692, 가로 줄이 y=422·457.
   내가 `fitView()` 식을 다시 적어 계산한 자리와 **가로 2.5화소** 차이(세로는 글자 딱지 때문에 16화소 느슨).
2. 🔴 **사진끼리 맞춰 보기**(더 확실한 잣대): 점선 «안쪽» 그림을 오려 내어 **원본 사진 전체에서**
   FFT 정규화 상호상관으로 «어디서 온 그림인지» 찾음.

```
20150921_131234_image306  메모 (900,939,944,975) → 찾은 자리 한가운데 (922,943) · 상관 0.712 · 어긋남 14화소
20150919_174151_image196  메모 (774,1418,826,1487) → 한가운데 (800,1437) · 상관 0.792 · 어긋남 16화소
   (사진은 1080×1920 — 어긋남은 1.5% 미만이고, 내가 오려 낸 자리가 글자 딱지를 피해 조금 작았기 때문입니다)
```
확대 그림 두 장을 남겼습니다(`p4_zoom_orig_*.png` / `p4_zoom_screen_*.png`) — **눈으로 봐도 같은 사과**이고,
노란 점선이 «라벨 안 된 그 사과» 를 둘러싸고 있습니다. **고치기 전 판에서는 노란 화소가 0개**입니다.

**깨진 메모·좌표 없는 flag** — 8가지를 넣어 봤고 **자바스크립트 오류 0건**:

| 메모 | 그려지나 | 안내 |
|---|---|---|
| 좌표 없음(`dataset2_back_840` 실제 2장) | 안 그림 | 안 뜸 ✅ |
| 숫자가 둘뿐 / 숫자가 아님 / 값이 없음 / 좌표 400개 | 안 그림 | 안 뜸 ✅ |
| `-50,-90,99999,99999`(화면 밖) | 안 그림(음수는 정규식이 안 잡음) | 안 뜸 ✅ |
| 좌우가 뒤집힘(`944,975,900,939`) | **그림**(음수 폭 상자) | 뜸 — 데이터가 그러면 그대로 그립니다(⚠️ 낮음) |
| 띄어쓰기 범벅 | 정상 | 정상 ✅ |

---

## 3. 새 API 3종의 견고성 (`api2.py` — **43/43**, 서버 로그 500 **0건**)

**`/api/list?flag=`**
- 다섯 종류 전부 `total` 이 `flag_counts` 와 일치(merged_blob 833 · filled_blob 117 · fg_too_high 10 · fg_too_low 10 · single_convex_blob 6)
- 없는 이름 → 0장(오류 아님) · **빈 값·공백만 → 예전과 똑같이 1,001장** · 다른 과일의 종류 → 0장
- `suspect=1` 과 함께 → 교집합(≤) · `%00`·`../../etc`·500자·한글 → 전부 200
- 포도·복숭아도 `flag_counts` 가 사전으로 옴

**`/api/instance_stats`**
- 캐시 파일을 **깨뜨려도**(JSON 아님 / 배열) 500 이 아니라 «아직 안 셈» 으로 답하고, 목록도 뜹니다(«N개» 딱지만 빠짐)
- **동시 6요청** 전부 200 · 답이 모두 같음 · **동시에 «지금 세기» 두 번 → 한 번만 시작**(`started` True/False)
- 계산 «중» 에 다른 요청을 넣으면 `running {done, total, sec}` 가 보이고, 끝나면 `stale 0`,
  **다시 센 값이 처음 캐시와 완전히 동일**
- 캐시 무효화를 내 방식으로 재현: 번호 파일을 새로 만들면 **그 1장만** 낡음 → 다시 세니 62→61,
  **다른 1,000장의 캐시 줄은 한 글자도 안 바뀜**, 과일 합계도 딱 1 줄어듦. 파일을 지우면 복귀.
- 없는 과일 이름 → 400 · 번호 없는 과일(복숭아) 합계 0
- 🔴 **합계를 딴 데서 온 숫자와 대조**:
  - 무작위 **30장**을 내가 **원본 마스크에서 직접 세어**(numpy `unique`) 캐시와 비교 → **30/30 일치**
  - 같은 30장을 `inspection.csv` 의 `gt_instances`·`/api/instance_info` 와도 비교 → **전부 일치**
  - 전수: 캐시 1,001장 = `inspection.csv` 1,001장, **다른 것 0장 · 합계 둘 다 40,468**

**`/api/exclude_group` · `/api/undo_exclude_group`** (`p1_group.py` 28/28)
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
**로그인 없이 → 401** · 이름 없이 → «익명» · 500자 이름 → 40자로 자름 ·
`stems` 5,000개 → 200개까지만 · `stems` 가 문자열이어도 500 아님 ·
다른 묶음·없는 stem 을 섞어도 **내 것만** 되돌아오고 남의 제외는 그대로.

---

## 4. 화면 항목 1~7 재현 (`b_layout.py` — 46 통과 / 6 «실패»)

다섯 창 **1100×700 · 1280×720 · 1366×768 · 1536×864 · 1920×1080**, 1차와 다른 방법
(요소의 실제 사각형 + `elementFromPoint` 격자 1,600점)으로 쟀습니다.

| 창 | 판정 단추 | 판정 줄 | 캔버스 높이 비율 | 가로 넘침 | 목록 딱지 |
|---|---|---|---|---|---|
| 1100×700 | 5/5 | 한 겹 65px | ①62.7% ②62.7% **③57.5%** | 0 | 개수 120·상태 59, 넘침 0 |
| 1280×720 | 5/5 | 한 겹 | 48.1%(창 넓이 기준) | 0 | 넘침 0 |
| 1366×768 | 5/5 | 한 겹 | 51.1% | 0 | 넘침 0 |
| 1536×864 | 5/5 | 한 겹 | 56.7% | 0 | 넘침 0 |
| 1920×1080 | 5/5 | 한 겹 | 64.9% | 0 | 넘침 0 |

→ **1차가 적은 1100×700 의 57.5 / 62.7% 를 내 코드로 똑같이 재현**했습니다(⑤ M2 합격).
의심 종류 단추 5개도 다섯 창 전부에서 창 밖으로 나가지 않습니다.

**묶음 썸네일 줄(구성원 20장짜리 사과 묶음 `…image976` 포함)**
- 캔버스·판정 줄을 **가리지 않습니다**(겹치는 넓이 0×0) ✅
- 캔버스를 **밀어내지도 않습니다**(20장 묶음 435px vs 4장 묶음 433px — 2px 차이) ✅
- ❌ **그런데 다섯 창 전부에서 «화면 밖» 입니다**: 오른쪽 패널의 맨 아래라서
  1100px 에서 **553px**, 1280 **533px**, 1366 **485px**, 1536 **279px**, 1920 에서도 **63px** 를 내려야 보입니다
  (1366×768 기준 패널을 **1,331px** 스크롤해야 함). → §9 **N3**

**범례 칩이 붓질을 막는 정도** — 캔버스 위 1,600점에서 «맨 위 요소가 캔버스가 아닌» 비율:

| 창 | 캔버스 전체 | 위쪽 1/4 |
|---|---|---|
| 1100×700 | **3.6%** | **14.5%** |
| 1280×720 | 2.8% | 11.0% |
| 1366×768 | 2.9% | 11.5% |
| 1536×864 | 1.2% | 4.8% |
| 1920×1080 | 0.9% | 3.8% |

→ 1차가 말한 «3.8%» 와 같은 자릿수입니다. **사진 왼쪽 위를 칠할 일이 있으면 1100px 에서는 걸립니다**
(위쪽 1/4 의 1/7). 칩 사이 빈틈으로는 통과합니다. **받아들일 만하다고 봅니다** — 다만 §9 N8 의 제안.

**범례를 눌러 레이어 껐다 켜기(⑦)** — «빨강 = 원본 라벨(GT)» 칩을 누르면 `#l-gt` 가 꺼지고, 다시 누르면 켜집니다 ✅

**묶음 썸네일로 이동한 뒤 `,`·`.`** — ❌ 3번째 썸네일로 `…image76` 을 연 뒤
`.`(다음)은 **아무 데도 안 갑니다**(목록 «끝» 에 붙었기 때문), `,`(이전)은 **목록의 앞 사진(대표)** 으로 갑니다.
1차 §10-2 가 «헷갈릴 수 있다» 고 적은 것이 실제로는 «다음이 안 먹는다» 로 나타납니다. → §9 N6

**포도·복숭아 «세기» 단추** — 둘 다 보입니다(«아직 안 센 2406장 세기» · «아직 안 센 125장 세기»).
누르면 2,531장을 0.2초씩(약 8분) 세고 결과는 전부 0입니다. → §9 N9

---

## 5. 초보자 눈 — 다섯 시나리오 (`b_use.py` · 스크린샷 `~/ff_shots/ui3/stage2/U*.png`)

처음 온 사람처럼 **화면만 보고** 1366×768 에서 해 봤습니다.

### 0) 처음 열었을 때 — 안내 5단계가 뜹니다 (`U0_tour1~5.png`)
단계별 글자수 **206 · 184 · 247 · 181 · 261자**(합 1,079자). 내용은 정확합니다.
- ⚠️ 이번 사이클이 넣은 **세 가지 새 기능(묶음 한 번에 · 열매 개수 · 노란 점선)이 안내에 없습니다.**
- ⚠️ «다시 안 보기» 를 **안 누르면 다음에 또 뜹니다**(기본이 안 눌린 상태). 매번 1,079자를 넘겨야 합니다.

### (a) 사진 한 장 검수 — `Ua_review.png` · **막힘 없음**
«이 사진에서 할 일» 줄과 판정 5단추가 한눈에 보입니다. 다만
- ⚠️ 오른쪽 패널 글자수 **1,122자**. 초보자가 처음 보면 어디를 봐야 할지 모릅니다.
- ❌ **할 일 줄에 영어가 그대로 나옵니다**: «자동 점검이 의심: **fg_too_low** — 눈으로 확인하세요.»
  바로 위 목록 화면에는 같은 것이 **«마스크가 거의 빔 10»** 이라는 한국어 단추로 있습니다.
  (`ui.js:171` 은 원시 이름, `ui.js:277` 의 `FLAG_KO` 사전을 안 씁니다.) → §9 N5

### (b) 중복 묶음 정리 — `Ub_list_dup.png` · `Ub_edit_top.png` · `Ub_edit_dupbox.png` · 🔴 **여기서 막힙니다**
«거의 같은 사진(중복)» 단추 → 504장, 카드가 흐리고 «중복#» 딱지가 붙어 좋습니다.
사진을 열면 할 일 줄이 «이 묶음의 대표(남길 장)입니다 — 나머지 19장은 이미 제외됐습니다» 라고 말합니다(P3 ✅).
**그런데 화면 어디에도 묶음 썸네일도, «이 묶음: 대표만 남기고 제외» 단추도 보이지 않습니다.**
오른쪽 패널을 **1,331px 내려야** 나옵니다(`Ub_edit_dupbox.png`). 초보자는 이 기능이 있는 줄 모릅니다.
- help.html 의 새 절은 «중복 묶음에 든 사진을 열면 **오른쪽 아래에** 그 묶음 사진들의 작은 그림이
  한 줄로 깔립니다» 라고 적혀 있습니다 — **«한참 내려야 한다» 는 말이 없습니다.** 문장이 화면과 어긋납니다.

### (c) 상자 그리기 — `Uc_boxmode.png` · **반쯤 막힘**
`X` 로 켜지고 «② 상자(검출 팀)» 칸이 열립니다. 다만 그 칸이 y=595(창 768)에서 시작해
**아래쪽 절반이 잘려** 보입니다 — «상자 저장»·«YOLO 로 내보내기» 는 스크롤해야 나옵니다.

### (d) 사과 번호 고치기 — `Ud_nummode.png` · **막힘 없음**
`K` 로 켜지고 «번호 2개 (최대 2) · 지움 0 합침 0 나눔 0 추가 0 · 되돌리기 0단계» 가 뜹니다.
지우기 D·합치기 M·나누기 X·새로 붙이기 N 이 한국어로 다 적혀 있어 따라 할 만합니다.
- ⚠️ 용어가 두 곳에서 다릅니다: 오른쪽 패널은 «③ 열매 번호 편집», 왼쪽 위 탭은 «③ 열매 번호 / 카운팅»,
  진행 현황은 «열매 개수(카운팅)». 세 이름이 같은 것을 가리킵니다.

### (e) «문제 있음» 68장에서 노란 점선 보고 고치기 — `Ue_list_flag.png` · `Ue_edit_flag.png` · **되지만 힘듦**
목록에서 상태 «문제 있음» 68장이 나오고, 열면 할 일 줄에
«**노란 점선 = 라벨 안 된 열매 후보 1곳** — 그 자리를 붓으로 칠하고 N 으로 번호를 붙이세요» 가 뜨고
사진 위에 노란 «후보 1» 상자가 실제로 보입니다(P4 ✅ — 이전에는 이게 아예 없었습니다).
- ⚠️ **상자가 화면에서 30×35화소**입니다(1080×1920 사진을 240px 폭으로 보여 주므로). 확대하지 않으면
  «점» 입니다. 확대(휠)를 모르는 사람은 여기서 멈춥니다. → §9 N7
- ⚠️ 메모 입력칸은 **폭 200px 에 83자** — «사람 확인 필요: 라벨 안 된 사과 후.» 까지만 보입니다.
  (할 일 줄에 앞부분이 나오게 된 것은 좋아졌지만, 좌표 숫자는 여전히 어디에도 안 보입니다.)
- ⚠️ 번호 레이어(흰 숫자 60여 개)가 사진을 덮어 노란 점선을 찾기 어렵습니다.

### 문서 대조 (help.html · README)

| 문장 | 판정 |
|---|---|
| help «중복 묶음에 든 사진을 열면 **오른쪽 아래에** … 작은 그림이 한 줄로 깔립니다» | ❌ 화면과 다름 — 다섯 창 전부에서 **스크롤해야** 보임 |
| help «…누르면 … **몇 장이 제외되는지 먼저 물어봅니다**» | ⚠️ 새로 제외할 것이 0장이면 **확인창 없이** 토스트만 뜸 |
| help «방금 그 단추로 제외한 것만 되돌아오고…» | ⚠️ 사실이나 «**이름이 다르면 안 돌아온다**» 는 말이 없음(실제로 그렇게 동작) |
| help «한 장에 0.2~0.6초라 사과 1,001장은 3~7분» | ✅ (1차 실측 188.6초 = 3.1분) |
| README §12 «목록 카드에도 `40개` 딱지가 붙습니다» | ✅ 카드 120장 전부에 붙음 |
| README §12 «사과 1,001장은 `inspection.csv` 의 `gt_instances` 와 전부 일치» | ✅ **내가 전수 재확인**(다른 것 0장, 합계 40,468) |
| README §12 «두 mtime 중 하나라도 바뀌면 그 장만 다시 셉니다» | ✅ 내 방식으로 재현 |
| README «다섯 창 × 세 작업 어디서나 5개 전부 · 한 겹» | ✅ 다섯 창(①) + 1100×700(①②③)에서 확인. **나머지 12벌은 안 봄** |
| README «1100×700 에서 캔버스 57.5~62.7%» | ✅ 같은 값 재현 |
| README §13 새 API 표 | ✅ 실제 응답과 일치(빠진 칸 0 — §7) |

---

## 6. 회귀 (전부 통과)

```
node --check app/static/app.js · ui.js                                ✅ (내가 고친 뒤에도)
node cycles/260917_신기능/cycle_1/stage1/sim.js                        ✅ 같은 숫자
node cycles/260917_신기능/cycle_2/stage2/modesim.js                    ✅ 31/31
node cycles/260917_신기능/cycle_2/stage1_client/boxsim.js               ✅ 50/50
python app/boxes.py                                                   ✅ 자체 점검 통과
python cycles/260917_신기능/cycle_5/stage1/live_c5.py    (실서버 GET)    ✅ 실패 0 (16항목, PID 3847740)
python cycles/260917_신기능/cycle_3/stage1/api_seed.py   (실서버 GET)    ✅ 표본 40장 · 기대와 다른 것 0
python cycles/260917_신기능/cycle_6_n6/stage1/n6_save.py               ✅ 실패 0  (N6)
python cycles/260917_신기능/cycle_6_n6/stage1/n1_cut.py                ✅ 실패 0  (N1)
```
**N6**(번호 저장이 번호 없는 전경을 지우지 않음)·**N1**(이진본이 번호본을 자름)은 P2 수정 **뒤에도**,
그리고 **내가 고친 뒤에도** 성립합니다(S7 의 «지운 알 0/1,034 되살아남 · 넓힌 500화소 손실 0» 그대로).
두 시험은 임시 폴더 + `test_client` 로 돌아 공용 `data/` 를 건드리지 않습니다.

---

## 7. 코드 리뷰 (`server.py` · `instances.py` · `dupes.py` diff 전문을 읽고)

| 보는 눈 | 결과 |
|---|---|
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
| **자물쇠(동시 저장)** | 🔴 **문제를 찾았습니다.** ① `write_status()` 의 임시 이름이 `PID` 뿐이라 **한 서버 안의 두 요청이 같은 임시 파일**을 씁니다 → 한쪽이 `os.replace` 하면 다른 쪽은 `FileNotFoundError`(500). ② `write_status_entry()`(번호 저장·되돌리기가 부름)만 **자물쇠 없이** 읽고 씁니다. 실측: 번호 저장 6 + 판정 저장 40을 동시에 던지니 **판정 40건이 통째로 사라지고 500 이 3건**. **고치기 전 코드(5142)에서도 똑같이 실패**하므로 이번 사이클이 만든 것은 아닙니다(0916 부터 있던 결함). → §8 고침3 · 고친 뒤 3회 연속 통과 |
| **원자적 쓰기** | `status.json`·캐시·마스크 전부 «임시 파일 → `os.replace`» ✅ (다만 임시 «이름» 이 위와 같이 겹쳤습니다) |
| **캐시 폴더** | `app/cache/instance_counts/` — **공용 `data/` 가 아님** ✅. 파일 위치 기준 절대경로라 모래상자는 자기 것을 씁니다 ✅ |
| **`src` 표식 위조** | `src` 는 **서버만** 씁니다(요청 본문에서 안 받음) ✅. `by` 는 화면이 보내므로 아무 이름이나 됩니다 — 되돌리기가 `by` 일치를 요구하므로 «남의 이름을 적으면 남의 제외를 되돌릴 수 있다» 는 성질이 남습니다(위험도 낮음: 되돌려도 `unreviewed` 가 될 뿐이고 백업이 남음) |
| **기존 API 칸** | 옛 코드(5142)와 새 코드(5141)의 응답을 12경로에서 맞대 봤습니다(`api_fields.py`) — **빠진 칸 0 · 코드 12/12 동일**, 늘어난 칸만 있습니다. ⚠️ 한 가지: `undo_duplicate_exclusions` 의 `kept` 는 이름은 그대로인데 **뜻이 바뀌었습니다**(전: 사람이 손으로 적은 «중복:» 제외 수 → 후: 표식 없는 모든 제외 수 = 지금 314). `kept_other` 가 새로 있으니 `kept` 는 다음 사이클에서 지워도 됩니다 |
| **ponytail** | 대체로 담백합니다. 과한 곳 셋: ㉠ `/api/list` 가 **호출마다** `flag_counts` 를 다시 셉니다(그 과일 전체 `inspection.csv` 순회) — 목록을 넘길 때마다 반복이고, 1차도 스스로 적었습니다. `_aux_cache` 처럼 mtime 캐시 한 줄이면 됩니다. ㉡ `cached_counts()` 가 호출마다 1,001개짜리 사전을 새로 만듭니다(목록에 쓰는 것은 120개). ㉢ `instances.py` 의 집계가 모듈 함수 12개 + 스레드 + 진행 상태로 200줄입니다 — 지금은 잘 돕니다만, 진행 표시를 빼면 절반입니다 |
| **그 밖** | `api_save_instances` 가 `before` 를 status 자물쇠 **밖**에서 읽습니다(읽은 뒤 쓰기까지 사이가 벌어짐). 고침3 으로 쓰기는 안전해졌지만, 아주 드물게 «읽은 값이 낡은» 채로 `prev` 가 만들어질 수 있습니다. 다음 사이클에서 `update_status` 안으로 합치는 편이 깔끔합니다 |
| **남은 같은 위험** | `.tmp%d % os.getpid()` 패턴이 `maskio.py:72` · `instances.py:50,209` · `boxes.py:130` · `server.py:359,773,1181` 에 **7곳 더** 있습니다. 같은 파일을 두 요청이 동시에 쓸 때만 문제이므로 지금 당장은 덜 위험하지만, **같은 한 줄 고침**을 권합니다(3차 판단) |

---

## 8. 내가 고친 것 — 자백 + diff 전문

**백업**: `app/_backup_260917_ui6_{server,instances}.py` · `app/static/_backup_260917_ui6_app.js`
**실서버(5111)는 재시작하지 않았습니다.** 서버 코드 두 개는 **아직 안 실린 채**이고, 3차가 재시작해야 합니다.
⚠️ 다만 `app.js` 는 정적 파일이라 **재시작 없이 이미 실서버 화면에 나갑니다**(고침2는 문구·단추 동작만 바꿉니다).

### 고침1 — 번호를 두 번 저장하면 flag 사유·노란 점선이 사라진다 (`instances.py`, P2 의 빈틈)
```diff
--- _backup_260917_ui6_instances.py
+++ instances.py
@@ -421,8 +421,15 @@
             note = ("번호 편집 " + " ".join(...)).strip()
-            if prev_note and not prev_note.startswith("번호 편집"):
-                note = (note + " · (이전) " + prev_note)[:300]
+            # 0918 UI사이클3 2차: **두 번째 저장**에서도 «(이전) …» 를 지키지 않으면, 화면에 남는
+            # 메모가 «번호 편집 …» 뿐이 되어 flag 사유와 P4 의 노란 점선(메모 좌표로 그린다)이
+            # 사라진다(실측: 두 번 저장하니 노란 화소 117 → 0). prev 에 넣어 둔 «맨 처음» 메모를 쓴다.
+            keep_note = prev_note
+            if prev_note.startswith("번호 편집"):
+                keep_note = str((before.get("prev") or {}).get("note") or "") \
+                    if isinstance(before.get("prev"), dict) else ""
+            if keep_note and not keep_note.startswith("번호 편집"):
+                note = (note + " · (이전) " + keep_note)[:300]
```
확인: `p2_twice.py` **3/3**(저장 0·1·2번 모두 노란 화소 117·108·117) · `p2_ab.py` 23/23 · N6 통과.

### 고침2 — «되돌렸습니다» 거짓 안내 + 되돌리기 단추가 사라짐 (`static/app.js`)
```diff
--- static/_backup_260917_ui6_app.js
+++ static/app.js
@@ -466,11 +466,17 @@
   (j.changed || []).forEach((s) => { S.dupMem[s] = "unreviewed"; });
   if (S.dupMem[S.stem] === "unreviewed" && S.item) { ... }
-  S.dupDone = null;
-  $("#dupundo").classList.add("hidden");
-  $("#dupmsg").textContent = `${(j.changed || []).length}장을 되돌렸습니다`
+  // 0918 UI사이클3 2차: 한 장도 안 되돌아왔는데 «되돌렸습니다» 라고 말하고 단추까지 숨겼다.
+  // (되돌리기는 «제외할 때 적힌 이름» 과 같을 때만 된다 — 이름을 바꿔 누른 사람은 지워진 줄 안다.)
+  const nBack = (j.changed || []).length;
+  if (nBack) {
+    S.dupDone = null;
+    $("#dupundo").classList.add("hidden");
+  }
+  $("#dupmsg").textContent = `${nBack}장을 되돌렸습니다`
     + ((j.kept || []).length ? ` · ${j.kept.length}장은 다른 사람이 넣은 제외라 그대로 둡니다` : "");
-  flash("방금 제외한 것을 되돌렸습니다");
+  flash(nBack ? "방금 제외한 것을 되돌렸습니다"
+    : `되돌리지 못했습니다 — 제외할 때 적혀 있던 이름으로만 되돌아옵니다 (지금 이름: ${who()})`, !nBack);
   drawDupStrip();
```
확인: `b_p1.py` **30/30** — 안홍렬이 누르면 빨간 글씨로 «되돌리지 못했습니다 — … (지금 이름: 안홍렬)»,
단추는 남아 있어 **이름을 곽동신으로 고치면 되돌릴 수 있습니다**.

### 고침3 — 동시에 저장하면 판정이 통째로 사라지고 500 이 난다 (`server.py`, 0916 부터 있던 결함)
```diff
--- _backup_260917_ui6_server.py
+++ server.py
@@ -339,7 +339,11 @@
 def write_status(fruit, data):
     p = status_file(fruit)
     os.makedirs(os.path.dirname(p), exist_ok=True)
-    tmp = p + ".tmp%d" % os.getpid()
+    # 0918 UI사이클3 2차: 임시 이름이 «PID» 뿐이라 **한 서버 안의 두 요청이 같은 임시 파일**을 썼다.
+    # 한쪽이 os.replace 로 옮기면 다른 쪽은 FileNotFoundError(500)로 죽고, 그 사이 다른 사람이
+    # 저장한 판정이 통째로 사라졌다(실측: 동시 46요청에서 판정 40건 손실 · 500 3건).
+    # 요청(스레드)마다 다른 임시 이름을 쓴다.
+    tmp = p + ".tmp%d.%d" % (os.getpid(), threading.get_ident())
@@ -386,10 +390,16 @@
 def write_status_entry(fruit, stem, entry):
-    """status.json 의 한 사진 항목을 통째로 바꿔 쓴다(번호 편집 기록용, 0917)."""
-    d = read_status(fruit)
-    d[stem] = entry
-    write_status(fruit, d)
+    """status.json 의 한 사진 항목을 통째로 바꿔 쓴다(번호 편집 기록용, 0917).
+
+    0918 UI사이클3 2차: 여기만 **자물쇠 없이** 읽고 썼다(다른 쓰기는 전부 status:<과일> 안에서 한다).
+    번호 저장이 판정 저장과 겹치면 방금 저장된 판정을 덮어쓸 수 있다 — 같은 자물쇠를 쓴다.
+    (부르는 쪽은 inst:<과일>:<사진> 자물쇠만 쥐고 있어 겹치지 않는다.)
+    """
+    with lock_for("status:" + fruit):
+        d = read_status(fruit)
+        d[stem] = entry
+        write_status(fruit, d)
     return entry
```
확인: `conc2.py`(번호 저장 6 + 판정 저장 40 동시) — **고치기 전 2/2 실패**(판정 40건 손실·500),
**고친 뒤 3/3 통과**(손실 0·500 0). `conc.py`(두 사람이 같은 묶음 동시 제외 + 판정 30건) **5/5**.
그리고 고친 뒤 `p1_ab`16/16 · `p1_group`28/28 · `p2_ab`23/23 · `api2`43/43 · `b_p1`30/30 · N6·N1 · 31/31 · 50/50 재확인.

**고치지 않고 글로만 남긴 것**: §9 의 N3·N5·N6·N7·N9 와 §7 의 ponytail 세 가지.

---

## 9. 새 문제 (심각도순)

| # | 심각도 | 문제 | 근거 |
|---|---|---|---|
| **N1** | 🔴 심각(**고침3 으로 고침**) | **두 사람이 동시에 저장하면 판정이 통째로 사라진다.** 번호 저장 6 + 판정 저장 40 을 동시에 던지니 판정 **40건 전부** 사라지고 500 이 3건. 원인은 ① `write_status` 의 임시 파일 이름이 PID 뿐 ② `write_status_entry` 만 자물쇠 없음. 0916 부터 있던 결함(옛 코드에서도 재현) | `conc2.py` · 서버 로그 `FileNotFoundError` |
| **N2** | 🔴 심각(**고침1 로 고침**) | **번호를 두 번 저장하면 flag 사유와 노란 점선이 사라진다**(P2 수정의 빈틈). 노란 화소 117 → 0 | `p2_twice.py` |
| **N3** | 🟠 보통 | **이번 사이클의 ①번 기능(묶음 썸네일 + «이 묶음: 대표만 남기고 제외»)이 다섯 창 전부에서 화면 밖이다.** 1366px 에서 오른쪽 패널을 **1,331px** 내려야 나오고, 화면에는 그런 안내가 없다. help.html 은 «오른쪽 아래에 … 깔립니다» 라고만 적혀 있다 | `b_layout.py` · `Ub_edit_top.png` |
| **N4** | 🟠 보통(**고침2 로 고침**) | 다른 사람 이름으로 «방금 제외한 것 되돌리기» 를 누르면 **0장이 되돌아왔는데 «되돌렸습니다»** 라고 말하고 단추까지 사라졌다 | `b_p1.py` |
| **N5** | 🟠 보통 | **할 일 줄에 영어 종류 이름이 그대로 나온다**(«자동 점검이 의심: fg_too_low»). 같은 사이클이 목록 단추에는 한국어 사전(`FLAG_KO`)을 만들어 두고 여기서는 안 쓴다(`ui.js:171` vs `ui.js:277`) | `b_use.py` |
| **N6** | 🟡 낮음 | 묶음 썸네일로 사진을 옮기면 그 사진이 «목록 끝» 에 붙어, 그 뒤 **`.`(다음)이 아무 데도 안 가고** `,`(이전)은 목록 앞 사진으로 간다 | `b_layout.py` |
| **N7** | 🟡 낮음 | 노란 점선이 화면에서 **30×35화소**라 확대를 모르는 사람은 못 찾는다. 번호 레이어의 흰 숫자가 위를 덮는다 | `Ue_edit_flag.png` |
| **N8** | 🟡 낮음 | 1100px 에서 범례 칩이 **캔버스 위쪽 1/4 의 14.5%** 를 덮어 그 자리에서는 붓질이 시작되지 않는다 | `b_layout.py` |
| **N9** | 🟡 낮음 | 포도·복숭아 «세기» 단추가 그대로 보인다 — 누르면 2,531장을 약 8분 세고 결과는 전부 0 | `L_dash_counts.png` |
| **N10** | 🟡 낮음 | 「일괄 제외 되돌리기」 API 가 **0장을 되돌려도** `_status_backup_<시각>.json` 을 먼저 만든다(공용 `data/` 에 파일이 쌓인다). 화면은 0장이면 API 를 안 부르므로 지금은 드물다 | `p1_ab.py` 실행 흔적 |
| **N11** | 🟡 낮음 | `undo_duplicate_exclusions` 응답의 `kept` 가 **이름은 그대로인데 뜻이 바뀌었다**(§7) | `api_fields.py` |
| **N12** | 🟡 낮음 | 메모 입력칸이 **폭 200px 에 83자** — 여전히 잘린다. 처음 안내(5단계)에 이번 새 기능 셋이 없고, «다시 안 보기» 를 안 누르면 매번 뜬다 | `b_use.py` |
| **N13** | ⚪ 참고 | `scripts/ff.py` 의 `open_photo` 가 **페이지가 다 뜨기 전에** 과일 select 를 만져, 엉뚱한 과일의 목록이 그대로 남은 채 «검색이 안 먹는» 일이 있었다(내가 실제로 겪음). 나는 `lib2.sandbox_browser().open_exact()` 로 우회했고 **연 사진의 이름을 매번 확인**했다. ab사이클2 P7 과 같은 계열 | `lib2.py` |
| **N14** | ⚪ 참고(앞일) | **데이터 v2 가 반영되면 P1 보호가 깨질 수 있다.** 지금 보호는 «3차 판정에는 `src` 표식이 없다» 는 데 기댄다. v2 반영 스크립트가 툴의 **일괄 제외 API 를 통해** 452장을 넣으면 전부 `src="dup_bulk"` 가 붙어, 되돌리기 한 번에 452장이 다시 날아간다. **반영은 반드시 파일에 직접 쓰고 `src` 를 넣지 말 것** | 코드 읽기 + `stage1_report.md` 머리말(v2) |

---

## 10. 시험한 조작 · 안 한 조작

| 조작 | 어디서 | 했나 | 결과 |
|---|---|---|---|
| 「중복 그룹 일괄 제외 적용」 진짜 클릭 | 모래상자 5141 | ✔ 사과 2회·블루베리 2회 | 확인창 글자 읽고 확인 · diff 0 |
| 「일괄 제외 되돌리기」 진짜 클릭 | 모래상자 5141 | ✔ 사과 2회·블루베리 1회 | «되돌릴 것이 없습니다» 로 막힘 · 실제 되돌리기도 1회(2장만) |
| «이 묶음: 대표만 남기고 제외»·«되돌리기» | 모래상자 5141 | ✔ 네 상황 × 브라우저 + API | 사람 판정 0장 변경 |
| 번호 저장·되돌리기(flag·exclude·판정없음) | 모래상자 5141·5142 | ✔ 각 5단계 | §2-3 |
| 좌표 깨진 메모 8가지 | 모래상자 5141 | ✔ | 오류 0 |
| 새 API 이상한 입력·동시 요청·캐시 손상 | 모래상자 5141 | ✔ | 500 0건 |
| 동시 쓰기(46요청·32요청) | 모래상자 5141·5142 | ✔ | **N1 발견** |
| 다섯 창 배치·목록 딱지·종류 단추 | 모래상자 5141 | ✔ | §4 |
| 초보자 다섯 시나리오 | 모래상자 5141 | ✔ | §5 |
| 회귀 8종 | 현재 코드·실서버 GET | ✔ | §6 |
| **실서버(5111) 쓰기 단추** | — | ✘ **일부러 안 함** | 지시. 실서버에는 **GET 과 `/login` POST 만** 보냈습니다(로그로 확인: 오늘 실서버 POST 는 `/login` 뿐) |
| 실서버 재시작 | — | ✘ | 서버 코드를 고쳤으므로 **3차에 맡깁니다** |
| 크롬·엣지·모바일 | — | ✘ | 서버에 파이어폭스뿐(1차와 같음) |
| 포도·복숭아 «세기» 실제 실행 | — | ✘ | 8분 × 결과 0. 단추가 보인다는 것만 확인 |
| 목록 15벌 전수(다섯 창 × 세 작업) | — | ✘ 일부 | 다섯 창은 ①마스크, 1100×700 은 ①②③. 나머지 12벌은 1차 수치를 그대로 두었습니다 |
| 사람이 실제로 붓질해서 flag 사진 고치기 | — | ✘ | 화면·안내만 봤습니다(마스크 저장은 이번 과제 밖) |
| 공용 `T/data`·원본·새 데이터셋·`T/inspect`·`T/cycles/260917_ab` 쓰기 | — | ✘ | 읽기만 |

---

## 11. 자취 (지울목록에 적을 것)

| 무엇 | 어디 | 크기 | 처리 |
|---|---|---|---|
| 내 시험 스크립트 **15개** | `T/cycles/260917_ui/cycle_3/stage2/{lib2,p1_ab,p1_group,p2_ab,p2_twice,api2,api_fields,conc,conc2,b_p1,b_p34,b_p4_match,b_layout,b_use,b_misc}.py` | 140KB | 남깁니다(재현용) |
| **고치기 «전» 모래상자** | `T/cycles/260917_ui/cycle_3/stage2/sandbox_before/` | 90MB(**대부분 하드링크** — 실제 추가 용량 약 1MB) | 남깁니다. 3차가 A/B 를 다시 보려면 필요합니다. 지울 때는 폴더째 지우면 됩니다 |
| 모래상자 서버 2대 | `127.0.0.1:5141`(고친 판) · `5142`(고치기 전) | — | **껐습니다** — 내가 띄운 PID 만 종료. 남의 5111·5100·5101·5105 는 안 건드렸습니다 |
| 모래상자 `data/` | 두 모래상자 모두 | — | 검수 끝에 **3차 판정 상태로 되돌렸습니다**(사과 exclude 314·flag 68 · `src` 0 · `prev` 0 / 블루베리 71·6). 새 모래상자에 시험이 만든 `masks_fixed` 6장·`instances_fixed` 4장은 **앞 사이클이 남긴 것**이라 그대로 두었습니다 |
| 스크린샷 39장 | `~/ff_shots/ui3/stage2/` | **23MB** | 남깁니다(증거) |
| 백업 3개 | `app/_backup_260917_ui6_{server,instances}.py` · `app/static/_backup_260917_ui6_app.js` | 185KB | 남깁니다 |
| 열매 개수 캐시 | `T/app/cache/instance_counts/` | 144KB | **안 건드렸습니다**(모래상자 것만 만졌습니다) |

**공용 `T/data` 지문(검수 끝)**: 파일 **7,395개 · 78,635,531바이트**(1차가 적은 값과 같음) ·
`data/apple/status.json` **22:22:02** · `data/blueberry/status.json` **22:24:21** — 3차 판정이 쓴 시각 그대로.
실서버 로그의 오늘 500 **0건**(로그에 있는 500 10건은 전부 09-16~17 00:22 의 옛 것).

---

## 12. 3차(Fable)가 판정해 줬으면 하는 것

1. **내가 고친 서버 코드 두 개(`server.py`·`instances.py`)를 실서버에 실을 것인가.**
   실으려면 저장 중인 사람이 없는지 로그로 보고 `bash app/run.sh restart` → 네 과일·게이트 확인.
   (고침3 은 «두 사람이 동시에 쓰면 판정이 사라진다» 라 사람에게 툴을 넘기기 «전» 에 들어가야 한다고 봅니다.)
2. **N3 — 묶음 칸을 어디에 둘 것인가.** 내 제안은 «오른쪽 패널의 맨 위로 올리기»(중복 사진일 때만 보이므로
   평소에는 자리를 안 먹습니다). 또는 할 일 줄의 «묶음» 글자를 누르면 그 칸으로 스크롤.
3. **N14 — 데이터 v2 반영 방법.** `src` 표식을 넣지 않는다는 것을 반영 스크립트에 못 박아 주세요.
4. **N5·N9 를 이번에 할지 다음으로 미룰지**(둘 다 한두 줄짜리).
5. 1차가 물은 것 셋에 대한 내 의견: ㉠ **범례 칩이 붓질을 막는 것은 받아들일 만합니다**(위쪽 1/4 의 14.5%,
   빈틈으로는 통과). ㉡ **일괄 제외가 flag 를 건너뛰는 규칙은 맞습니다**(사람이 «봐야 할 것» 을 기계가 빼면 안 됨).
   ㉢ `prev` 한 겹 보관은 괜찮습니다 — 다만 고침1 없이는 **화면에서 사유가 사라졌습니다**.

---

## 13. 그대로 재현하는 명령

```bash
T=/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴
PY=/home/kds0206/.conda/envs/kwak/bin/python
S2=$T/cycles/260917_ui/cycle_3/stage2
DR=/data/project/2026summer/kds0206/datasets_reviewed_260916

# 1) 모래상자 두 대 (포트가 비었는지 먼저: ss -ltnp | grep 514)
cd $T/cycles/260917_ui/cycle_1/stage2/sandbox/app && PORT=5141 LABELTOOL_DATA_ROOT=$DR \
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
cd $S2/sandbox_before/app && PORT=5142 LABELTOOL_DATA_ROOT=$DR \
보안 관련 값·설정 세부는 공개본에서 생략했습니다.

# 2) P1~P4  (앞 번호가 «고치기 전» — 실패해야 맞습니다)
cd $S2 && $PY p1_ab.py 5142    # 10 실패     ↔  $PY p1_ab.py 5141    # 16/16
          $PY p2_ab.py 5142    # 10 실패     ↔  $PY p2_ab.py 5141    # 23/23
          $PY b_p34.py 5142    # 노란 화소 0 ↔  $PY b_p34.py 5141
          $PY p1_group.py 5141 ; $PY p2_twice.py 5141 ; $PY b_p1.py ; $PY b_p4_match.py 5141

# 3) 새 API·동시 쓰기·배치·사용성
          $PY api2.py ; $PY api_fields.py ; $PY conc.py ; $PY conc2.py 5141
          $PY b_layout.py 5141 ; $PY b_use.py ; $PY b_misc.py

# 4) 회귀
export PATH=/home/kds0206/.local/node22/bin:$PATH
cd $T && node --check app/static/app.js && node --check app/static/ui.js
node cycles/260917_신기능/cycle_2/stage2/modesim.js            # 31/31
node cycles/260917_신기능/cycle_2/stage1_client/boxsim.js       # 50/50
$PY app/boxes.py
(cd cycles/260917_신기능/cycle_6_n6/stage1 && $PY n6_save.py && $PY n1_cut.py)
(cd cycles/260917_신기능/cycle_5/stage1 && $PY live_c5.py)      # 실서버 GET 전용

# 5) 끄기 — 반드시 PID 로 (남의 5111 을 죽이지 않게)
ss -ltnp | grep -E "514[12]"   # PID 확인 뒤 kill <PID>

# 6) 내 수정을 되돌리려면
cd $T/app && cp _backup_260917_ui6_server.py server.py && cp _backup_260917_ui6_instances.py instances.py
cd $T/app/static && cp _backup_260917_ui6_app.js app.js
```
