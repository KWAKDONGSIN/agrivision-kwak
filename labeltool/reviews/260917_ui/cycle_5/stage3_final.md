작성: 2026-09-18

# UI 개선 사이클 5(총괄) — 3차 최종 판정 (Fable 5.1) = UI 5회 사이클 종료

읽은 것: `stage1_work.md` · `stage2_review.md` 요지 · 2차의 서버 diff(`app/_backup_260918_ui10_server.py` ↔ 현재)를 내가 읽음 · 실서버 status.json 네 개에 `src`·`prev` 표식이 든 항목 수를 직접 셈(전부 0) · 재시작 뒤 진짜 브라우저로 실서버의 사과 목록과 flag 사과 `dataset2_back_900` 편집 화면을 1366×768 로 직접 봄(`~/ff_shots/final_{list_apple,edit_flag_apple}.png` — 판정 5단추 5/5 보임, 묶음 칸 맨 위, 할 일 줄에 flag 사유와 «이 묶음의 대표» 안내).

## ① 결론 — 합격. 최종 코드를 실서버에 실었습니다(04:49 재시작, PID 313878)
- `py_compile` 통과 → 로그에 최근 저장 0건 → `bash app/run.sh restart` **2026-09-18 04:49**. 확인: 네 과일 개수(사과 제외 572·flag 51·안 봄 378 / 블루베리 81·4·1,110 / 복숭아·포도 불변) · 쿠키 없는 API 401 · `/`·help·ui.js 200.
- 2차가 찾아 고친 것 **전부 인정**: ② 상자 작업에서 «수정본 저장» 이 살아 있어 붓질 없이 `masks_fixed` 가 생기던 것(회색 처리 — ③ 과 같은 방식이라 숨기기보다 일관) · 번호를 한 번 저장하면 «방금 제외한 것/일괄 제외 되돌리기» 가 자기 제외를 못 되돌리던 것(판별을 `src` 표식 하나로) · 메모 이중 기록 · 되돌린 사진에 `prev` 가 남아 제외가 되살아나던 것 · N-L 초벌 0개면 원본 라벨로 한 번 더 · N-M 로그인 제한이 맞은 로그인까지 세던 것. 잘린 PNG·빈 본문·12MB 등 8종 업로드는 전부 4xx 로 거절되고 기존 수정본·판정 무손상.
- **가장 위험하다고 지목된 «되돌리기 판별을 `src` 하나로»**: AI 3회 검수가 넣은 제외·flag 에는 `src` 가 없습니다(실서버 네 과일 status.json 에서 `src` 든 항목 0 — 내가 셈). `src` 는 서버의 묶음/일괄 제외 단추만 붙입니다. 따라서 되돌리기가 AI 판정을 건드릴 길이 없습니다 → 인정.

## ② 2차가 넘긴 판정
| # | 건 | 판정 |
|---|---|---|
| 1 | 실서버 재시작 | 했음(위) |
| 2 | `src` 단일 판별 | 인정(위) |
| 3 | ② 의 «수정본 저장» 회색 | 인정 |
| 4 | «제외 → 저장 → 사람이 `1` → 수정본 되돌리기» 가 제외로 돌아감 | **작은 결함으로 분류, 이번엔 안 고침.** 사람이 `1`·`3`·`4` 로 명시 판정하면 `prev` 를 지우는 것이 맞음(2줄). 다음 작업 목록 2순위. 그 전 사람 규칙: «제외를 풀었으면 그 사진에서 ‘수정본 되돌리기’ 를 누르지 말 것» |
| 5 | 범례 칩 49×24화소가 아직 사진 왼쪽 위 모서리를 덮음 | 그대로 둠(체크리스트·help 의 문장은 2차가 사실대로 고침) |
| 6 | 열린 문제 개수 | **표의 행을 센 값(76행)이 정본.** «66건(44/17/5)» 은 쓰지 않음 — 판정서에 2차가 넣은 정정 상자 유지 |
| 7 | ③ 번호 «보류» 문구 | 0917 새 기능 판정서의 «블루베리 번호 저장 보류» 는 **N6 보충의 2차 독립 재현(2026-09-17 19:20)으로 이미 해제**됐는데 그 문서가 갱신되지 않았던 것 → 그 문서 맨 위에 후속 한 줄을 넣음. UI 판정서는 «해제됨(포도 번호 켜기만 교수님 답 대기)» 로 |

## ③ 다섯 사이클 요약
1 작업 3칸·할 일 안내·범례·첫 방문 안내 / 2 판정 줄을 사진 아래로·경고 25% 상한 / 3 묶음 한 번에 정리·개수 화면·의심 종류 단추 + 툴 결함 P1~P4·동시 저장 소실 / 4 묶음 칸 맨 위·후보 자리로 확대·«수정본 되돌리기» 결함 / 5 제외 보존·저장 단추 가시성·총괄. 데이터가 사라질 수 있던 결함을 다섯 사이클에서 10건 가까이 찾아 고쳤고(실제 손실 0 — 여러 건은 0916~17 실서버에 잠복해 있었지만 밟은 사람이 없었음), 검수는 전부 서버의 진짜 Firefox 로 했습니다.

## ④ 남은 것
- **사람**: 크롬·엣지에서 `문서/260917_툴_사람확인_체크리스트.md`(«먼저 볼 것» 부터) · 과일·쪽을 나눠 맡기(같은 사진 동시 편집은 나중 저장이 이김, 경고 없음).
- **다음 작업 후보(우선순위순)**: ① 항목별 변경 이력 로그(`update_status` 한 곳에 jsonl 덧붙이기) ② 명시 판정 때 `prev` 지우기 ③ 같은 사진 동시 편집 경고 ④ `_save_verdict` 가 자물쇠 밖에서 읽는 구조 ⑤ «할 일» 문턱표 `TH` 갱신(데이터가 바뀜) ⑥ 범례 칩.
- 토요일 2026-09-19 18:01 Codex 점검에 넘길 질문 8개는 판정서 §10.

최종 판정서: `/data/project/2026summer/kds0206/문서/260918_툴UI개선_5회검수_최종판정.md`
