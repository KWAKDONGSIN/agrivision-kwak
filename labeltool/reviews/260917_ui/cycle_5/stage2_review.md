작성: 2026-09-18

# UI 개선 사이클 5(총괄) · **2차 검수** (Opus 5)

대상 `T = /data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴`
읽은 것: `cycle_4/stage3_final.md` ② · `cycle_5/stage1_work.md` 전문 · 사이클 1~4 의 stage1·stage2·stage3 ·
판정서 초안 · 체크리스트 최종판 · `app/static/help.html` · `app/README.md` · 바뀐 코드 전부(`_backup_260918_ui9_*` 대조) ·
`문서/260917_상자와번호기능_5회검수_최종판정.md` §8 · `문서/260918_딥시크확인_Fable판정.md`

내 시험 스크립트는 **전부 `cycles/260917_ui/cycle_5/stage2/`** 에, 스크린샷 **25장(16MB)** 은 `~/ff_shots/ui5/stage2/` 에 있습니다.
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
실서버 5111 에는 **GET 과 `/login` POST 만** 보냈습니다. 저장 단추는 한 번도 누르지 않았습니다.
시각은 전부 `date`·`ls -l --time-style=full-iso` 실측입니다(시작 **2026-09-18 04:08:26 KST**).

---

## ① 한 줄 결론

**1차의 작업 1·2·3 은 전부 실제로 들어갔고 재현됩니다. 그러나 1차가 «고쳤다» 고 적은 것 중 두 건은 덜 고쳐졌고,
1차의 이번 수정이 **새 결함 두 건**을 만들었습니다 — 넷 다 내가 재현해서 고쳤습니다(백업 `_backup_260918_ui10_*`).**

- 🟠 **내가 찾아 고친 것 4건** — ② 에서 «수정본 저장»·«상자 저장» 이 **둘 다 «(Ctrl+S)»** 로 나란히 살아 있던 것 ·
  **메모 이중 기록이 여전히 남아 있던 것**(1차가 «고쳤다» 고 자백한 그 버그의 절반만 고쳐져 있었음) ·
  제외 되돌리기가 **`prev` 를 남겨 «수정본 되돌리기» 가 제외를 되살리던 것** ·
  번호를 저장한 사진을 **«방금 제외한 것 되돌리기» 가 못 되돌리던 것**.
- ✅ **1차가 «작업 목록 밖» 이라고 남긴 N-L·N-M 도 고쳤습니다**(각각 2줄·8줄, 고치기 전/후 시험 있음).
- ⚠️ **1차 보고서의 숫자 두 개가 틀렸습니다** — «범례 히트테스트 0/5»(→ 실은 **1/5 가 아직 칩에 걸림**) ·
  «열린 문제 해결 44/미해결 17/사람 결정 5(합계 66)»(→ 표 행을 세면 **76행 = 48/2/22/3/1**).
- ✅ **판정서 초안의 «전부 실서버에 나가기 전에 잡았습니다» 는 사실과 달라** 고쳤습니다(여러 건이 0916~0917 실서버에 잠복).
- 🔴 **실서버는 지금 «새 화면 + 옛 서버» 섞인 판입니다.** 정적 파일(app.js·ui.js·help.html)은 재시작 없이 바로 나가므로
  내 프런트엔드 수정이 **이미 5111 에 실려 있고**(실측 확인), `server.py` 수정은 **아직 안 실렸습니다**.
  섞인 상태가 깨지지는 않는 것을 확인했지만(아래 ⑦), **실서버 재시작은 3차 판정에 맡깁니다.**

| 내 시험 | 결과 |
|---|---|
| N-C 섞어 누르기 (`r1_mix.py`) | 고치기 전 **50통과/3실패** → 고친 뒤 **53/0** |
| 제외 되돌리기 섞기 (`r2_undo.py`) | 고치기 전 **16통과/6실패** → 고친 뒤 **22/0** |
| N-L·N-M·망가진 PNG·경합 (`r3_api.py`) | **36/0** |
| 다섯 창 × 세 작업 + Ctrl+S·id·TOUR·범례 (`r4_layout.py`) | **234통과/1실패**(그 1건 = 아래 새 문제 3) |
| 회귀 11종 (1차의 `t4_reg.py` 그대로) | **11/0** |
| 문서 대조 29항목 (1차의 `t7_docs.py` 그대로) | **29/0** |
| 실서버 읽기 전용 눈 확인 (`r5_live.py`) | **21/0** |

---

## ② 합격 기준 — **시험을 돌리기 전에** 스크립트에 박아 둔 것

그대로 각 스크립트의 docstring 맨 위에 있습니다(고쳐 쓰지 않았습니다).

**N-C 섞어 누르기 (`r1_mix.py`)**
A 어떤 순서로 섞어도 «제외» 사진의 status 는 끝까지 exclude ·
B 되돌리기를 끝까지 하면 status·note·by 가 **처음과 글자 그대로** 같다 ·
C 원래 메모 조각이 최종 메모에 **한 번만** 나온다 ·
D «중복: » 으로 시작하는 메모는 어떤 저장을 해도 앞부분이 안 바뀐다 ·
E 묶음 제외 → 저장 → 되돌리기 를 섞어도 되돌릴 수 있다 ·
F 되돌리기로 unreviewed 가 된 사진에 `prev` 가 남지 않는다.

**되돌리기 (`r2_undo.py`)** G1 붓질 저장을 섞어도 되돌려진다 · G2 되돌린 뒤 `prev` 가 없다 ·
G3 **번호 저장**을 섞어도 되돌려진다 · G4 과일 통째 일괄 제외도 같다 ·
G5 **AI 3회 검수의 제외(src 없음)는 한 장도 안 건드려진다.**

보안 관련 값·설정 세부는 공개본에서 생략했습니다.
L1 AI 제안이 빈 사진에서 ai 0개 / gt ≥1개 · P1 망가진 본문 8종을 4xx 로 거절 + 기존 masks_fixed·판정 무손상 + 500·Traceback 0 ·
C1 마스크 저장과 «제외» 를 동시에 두드려도 exclude 가 fixed 로 뒤집히지 않는다.

**배치 (`r4_layout.py`)** B1 저장 단추가 뷰포트 안·한 겹·스크롤 0 · B2 판정 5단추 자리 5/5 · **B3 캔버스 ≥ 55%** ·
B4 ② 에서 «수정본 저장» 이 눌리지 않는다 · B5 ② 의 Ctrl+S 는 **상자**를 저장한다 ·
B6 ②③ 에서 메모 칸은 숨지만 값은 산다 · B7 요소 id 가 안 없어지고 겹치지 않고 단축키 X·K 가 그대로 ·
B8 캔버스 안내가 뜨고 클릭을 막지 않는다 · B9 범례가 접혀 있고 사진 왼쪽 위가 캔버스로 잡힌다 ·
B10 옛 TOUR 키가 있는 프로필에서 새 안내가 한 번 뜨고 «다시 보지 않기» 뒤에는 안 뜬다.

---

## ③ 항목별 결과

### 3-1. 과제 1 — N-C(제외 사진의 마스크 저장 규칙) ⚠️→✅

| 확인할 것 | 결과 | 증거 |
|---|---|---|
| «제외 → 붓질 저장 → 번호 저장 → 수정본 되돌리기 → 번호 되돌리기» 가 끝까지 일관한가 | ✅ | `r1_mix.py` 순서1 — status 끝까지 exclude · 마지막에 **note·by 가 글자 그대로 복구** · `prev` 안 남음. **«수정본 되돌리기» 가 번호 편집 사진을 400 으로 거절**하는 것도 확인(N1-c 가드가 이 순서를 안전하게 강제) |
| «AI 제안으로 교체» 를 섞을 때 | ✅ | 순서2 — exclude 유지 · `kept_exclude:true` · 메모가 «AI 제안으로 교체» 로 **덮이지 않고** «중복: …» 앞부분이 남음 |
| flag 사진 | ✅ | 순서3 — flag → **fixed 로 풀림**(의도) · flag 사유 메모가 살아 있음 · 되돌리면 flag·메모·이름 복구 |
| **메모가 이중으로 쌓이지 않는가** | ❌→✅ | **1차 판이 틀렸습니다.** 아래 «새 문제 1» |
| «일괄 제외 되돌리기» 의 `startswith("중복: ")`·`src` 판별이 새 메모 형식에서도 맞는가 | ❌→✅ | **틀립니다.** 아래 «새 문제 2» |
| 사람이 `1` 을 눌러 제외를 푸는 길이 화면에서 찾기 쉬운가 | ✅(다만 한 가지) | 저장하면 **alert 이 «…«1 원본 그대로 OK» 를 누르거나…» 라고 직접 알려 줍니다**(`app.js doAction`, 실제 글자 확인). 노란 «할 일» 줄에도 `1` 이 적힙니다. **그런데 `1` 을 누른 뒤 «수정본 되돌리기» 를 누르면 제외로 되돌아갑니다** — 아래 «새 문제 4» |
| `_save_verdict` 가 자물쇠 밖에서 읽는 구조의 경합 | ✅(재현 안 됨) · ⚠️(구조는 그대로) | `r3_api.py` C절 — 저장 60회와 «제외» 60회를 **동시에**, 6.4초, 120요청 전부 200, «제외 직후 fixed 로 뒤집힘» **0회**, Traceback 0. **왜 안 걸리는지도 확인했습니다**: 무거운 일(PNG 해독·파일 쓰기)이 **읽기보다 앞**에 있어 읽기→쓰기 틈이 마이크로초급입니다. 그래도 «읽고 나서 쓴다» 는 구조는 그대로라 **토요일 Codex 질문 4번**으로 남깁니다 |

### 3-2. 과제 2 — ②③ 저장 단추를 판정 줄로 ✅ (한 가지는 ❌→✅)

**가시성·한 겹·캔버스 비율 (내 실측, `r4_layout.json`)** — 다섯 창 × 두 사진 × 세 작업, 전부 스크롤 0 · 한 겹:

| 창 | 실측 뷰포트 | 캔버스(①/②/③) |
|---|---|---|
| 1100×700 | 1100×614 | 63.1% / 63.1% / 57.8% |
| 1280×720 | 1280×634 | 61.1% / 58.4% / 64.2% |
| 1366×768 | 1366×682 | 63.8% / 63.8% / 66.7% |
| 1536×864 | 1536×778 | 70.5% / 70.5% / 70.5% |
| 1920×1080 | 1920×994 | 76.9% / 76.9% / 76.9% |

**최저 57.8% · 최고 76.9%** — 1차가 적은 값과 같습니다(합격선 55% 통과). 1차 표를 **독립으로 재현했습니다.**

| 확인할 것 | 결과 | 증거 |
|---|---|---|
| ②상자 모드에서 «수정본 저장 (Ctrl+S)» 과 «상자 저장 (Ctrl+S)» 이 나란히 보이는 것이 헷갈리지 않는가 | ❌→✅ | **헷갈립니다. 그리고 위험합니다.** 아래 «새 문제 1(심각도 1위)» |
| Ctrl+S 가 실제로 무엇을 저장하는가 | ✅ | `r4_layout.py` B5 — ② 에서 상자 1개를 **진짜 드래그**로 그리고 **진짜 Ctrl+S** → 파일에 상자 1개 저장 · `has_fixed` **false 그대로** · 판정 **unreviewed 그대로**. 즉 Ctrl+S 는 «상자 저장» 입니다 |
| ②③ 에서 메모 칸을 숨긴 판단 | ✅(타당) + 한 가지 발견 | 숨겨도 **값은 살아 있습니다**(B6 — ② 로 옮긴 뒤 `#note`.value 유지). 그래서 그 상태에서 `4`(제외) 를 눌러도 메모가 지워지지 않습니다. 판단은 타당합니다(캔버스 55% 를 지키려면 필요) |
| 판정 1~4 단추가 ②③ 에서도 필요한가 | ✅ 필요 | ②③ 중에도 «이 사진은 못 쓰겠다»(4)·«문제 있음»(3) 판정은 자연스럽습니다. 자리도 다섯 창에서 5/5 유지(B2) |
| 기존 단축키·요소 id 가 그대로인가 | ✅ | `modesim 31/31` · `boxsim 50/50` + 내 DOM 점검: 요소 id **22개 전부 있음** · **겹치는 id 0** · `X` → ②, `K` → ③, `K` 한 번 더 → ① (B7) |

### 3-3. 과제 3 — 나머지

| 항목 | 결과 | 증거 |
|---|---|---|
| **TOUR_KEY** — 옛 키가 있는 프로필에서 새 안내가 한 번 뜨는가 | ✅ | B10 — `localStorage` 에 **옛 키 `tour_seen_260917_ui1` 만** 넣고 열었더니 안내가 뜨고 «**1 / 6**» · «다시 보지 않기» 뒤 새 키 `tour_seen_260918_ui5` 저장 · 다시 열면 **안 뜸** |
| **범례 기본 접힘** — 처음 온 사람이 색 뜻을 어디서 아나 | ✅ 세 곳에 적혀 있음 · ⚠️ 막히는 자리가 0 은 아님 | 처음 안내 4단계·6단계·사용법 §색 읽기에 «색 뜻 ▸ 를 누르면» 이 있습니다(글자 확인). 다만 아래 «새 문제 3» |
| `#taskbadge` | ✅ | 다섯 창 × ②③ 전부에서 뜨고 글자가 맞으며 `pointer-events:none`(B8). ① 에서는 안 뜸 |
| README §2 문장 | ✅ 정확 | «사과·블루베리는 팀 표준 원본에서 읽고 제외는 상태로 표시 · 포도·복숭아는 검수판 폴더 · 장수는 진행 현황 탭» — 사이클 4 3차가 시킨 그대로이고, 실서버 실측과 맞습니다(사과 1,001·블루베리 1,195·복숭아 125·포도 2,406) |

---

## ④ 새로 찾은 문제 (심각도순) — 넷 다 고쳤습니다

### 🟠 새 문제 1 — ② 상자 모드에서 «수정본 저장 (Ctrl+S)» 이 **살아 있어**, 잘못 누르면 붓질을 안 했는데도 «수정함» 이 된다

- **무엇**: 1차가 «상자 저장» 을 판정 줄로 옮긴 결과, ② 에서 **큰 초록 단추 «수정본 저장 (Ctrl+S)»** 과
  **큰 초록 단추 «상자 저장 (Ctrl+S)»** 이 **나란히** 놓였습니다. 둘 다 «(Ctrl+S)» 라고 적혀 있는데
  실제 `Ctrl+S` 는 **상자만** 저장합니다(`app.js` 단축키: `if (S.numMode) saveInstances(); else if (S.boxMode) saveBoxes(); else doAction("fixed")`).
  ③ 번호 모드에서는 app.js 가 `#btn-save` 를 **회색으로** 만드는데(`setNumMode`), **②에는 그 장치가 없었습니다.**
- **재현(고치기 전)**: 복숭아 `210629-t1-02` 를 ② 로 열고 «수정본 저장» 을 누름 →
  `masks_fixed/210629-t1-02.png` 가 **생기고** 판정이 `unreviewed` → **`fixed`(내 이름)** 로 바뀜.
  붓질은 한 번도 안 했으므로 내용은 원본과 같은 «가짜 수정본» 입니다. 내보내기는 `masks_fixed` 가 있으면 그것을 쓰므로
  기록상 «누가 고친 사진» 이 됩니다. (`r4_layout.py` B4 «고치기 전 재현» — 회색을 강제로 풀어 1차 판의 동작을 그대로 재현)
- **고침**(`ui.js` +14줄): ③ 이 하던 것과 **같은 방식**으로 ② 에서도 `#btn-save.disabled = true`.
  **자리는 그대로 둡니다**(판정 다섯 단추 위치가 작업마다 움직이면 더 헷갈립니다). 왜 회색인지 풍선말에 적었습니다.
- **고친 뒤**: 다섯 창 × ②③ 전부에서 회색 확인(B4 30항목) · ① 로 돌아오면 다시 눌림 · `modesim` 31/31 그대로.

### 🟠 새 문제 2 — **번호를 한 번 저장하면 «방금 제외한 것 되돌리기»·«일괄 제외 되돌리기» 가 그 사진을 못 되돌린다**

- **무엇**: 두 되돌리기가 `status == exclude` **그리고** `note.startswith("중복: ")` **그리고** `src` 표식을 봅니다.
  그런데 `instances.py api_save_instances` 는 메모를 **«번호 편집 erase=1 · (이전) 중복: …»** 으로 바꿉니다(앞부분이 «번호 편집»).
  그래서 **자기가 방금 넣은 제외**를 못 되돌리고, 화면은 «1장은 **다른 사람이 넣은 제외라** 그대로 둡니다» 라고
  **사실과 다른 말**을 합니다(`app.js` `#dupundo`).
  1차는 «앞부분이 안 바뀐다» 만 확인했는데, 그것은 **마스크 저장 경로만** 그렇습니다(1차도 §⑯-1 에서 이 한계를 인정).
- **재현(고치기 전)**: `r2_undo.py` G3 — 사과 묶음 하나를 일부러 «아직 안 봄» 으로 돌려놓고 «대표만 남기고 제외»(4장) →
  한 장에 번호 저장 → «방금 제외한 것 되돌리기» → `changed 3장 · kept ['…image6']`. 그 사진은 **exclude 로 남음.**
- **고침**(`server.py`): `api_undo_duplicate_exclusions`·`api_undo_exclude_group` 에서 **메모 조건을 뺐습니다.**
  판별은 **`src` 표식**(+ 묶음 쪽은 `by`)으로만 합니다 — `src` 는 이 단추들만 남기는 표식이라 P1 보호의 **본체**이고,
  메모 조건은 P1 이후 덤이었습니다(데이터 v2 도 «중복: » 메모를 쓰므로 메모로는 애초에 구분이 안 됩니다).
  **부수 효과(좋은 쪽)**: `api_duplicate_preview` 의 `n_undoable` 은 원래부터 `src` 만 보고 있었으므로,
  전에는 «N장 되돌릴 수 있습니다» 라고 미리 말한 뒤 실제로는 덜 되돌리는 어긋남이 있었습니다 → 이제 두 곳이 일치합니다.
- **고친 뒤**: G3 통과 · **G5 «AI 3회 검수 제외 568장이 한 장도 안 줄었다»** 통과(P1 보호 유지 실측).

### 🟠 새 문제 3 — 메모 이중 기록이 **아직 남아 있었다**(1차가 «고쳤다» 고 자백한 그 버그)

- **무엇**: 1차 판은 `if (note and not old.startswith(note.strip()))` 였습니다. 즉 **글자가 똑같을 때만** 안 붙입니다.
  그런데 메모 칸은 그 사진 메모로 **미리 채워져** 있고(`app.js:338`), 사람이 흔히 하는 행동은
  **그 뒤에 한 마디 덧붙이기**입니다. 그러면 `note` 가 `old` 보다 길어져 조건이 참이 되고 **옛 메모가 통째로 한 번 더** 붙습니다.
- **재현(고치기 전)**: `r1_mix.py` 순서4 — 메모 칸에 «중복: …image116 다시 봤음» 을 넣고 저장 →
  `중복: 20150919_174151_image116 · (수정본 저장) 중복: 20150919_174151_image116 다시 봤음` (사과·블루베리 둘 다).
- **고침**(`server.py` 한 줄): `new_note = note if note.startswith(old) else ((old + " · (수정본 저장) " + note)[:300] if note else old)`
  — 네 경우 전부 맞고 **어느 경우에도 앞부분(old)이 앞에 남습니다**:
  ① 손 안 댐(note == old) → 그대로 · ② 뒤에 덧붙임 → 보낸 것 그대로(중복 없음) ·
  ③ 지우고 딴 말 → 앞에 old + « · (수정본 저장) » · ④ 칸을 비움 → old 유지(제외 사유가 안 지워짐).
- **고친 뒤**: 순서4 통과 · 순서1~3 의 «메모 이중 아님» 전부 통과.

### 🟡 새 문제 4 — 제외를 **되돌린** 사진에 `prev` 가 남아, «수정본 되돌리기» 가 제외를 되살린다

- **무엇**: 사이클 4 가 넣은 `prev`(되돌아갈 자리)를 두 되돌리기 API 가 **치우지 않았습니다.**
  그래서 «묶음 제외 → 붓질 저장(prev=exclude 기록됨) → 방금 제외한 것 되돌리기(status=unreviewed)» 까지 한 사진에서
  **«수정본 되돌리기» 를 누르면 exclude 가 되살아납니다.** 그때는 `src` 표식이 이미 지워져 있어 **되돌리기 단추로 다시 풀 수도 없습니다.**
- **재현(고치기 전)**: `r2_undo.py` G2 — 되돌린 뒤 항목에 `prev: {status: exclude, note: '중복: …'}` 남음 →
  `/api/revert` → `status: exclude` 로 복귀.
- **고침**(`server.py` 2줄): 두 되돌리기에 `rec.pop("prev", None)` — 되돌렸으면 돌아갈 자리도 없앱니다.
- **고친 뒤**: G2 통과(«수정본 되돌리기» 뒤 `unreviewed · 수정본 되돌림`).
- **남는 것(고치지 않음)**: «제외 → 붓질 저장 → 사람이 `1` → 수정본 되돌리기» 는 **아직 제외로 돌아갑니다**
  (`r1_mix.py` 순서6 에 그대로 기록). 이것은 사이클 4 N-A 수정의 **설계된 동작**(«되돌리기는 저장 전으로 돌린다»)이라
  판단을 3차에 넘깁니다 — 사람의 `1` 을 조용히 뒤집는 것이 맞는지. 데이터가 사라지는 쪽은 아닙니다(더 안전한 쪽으로 틀림).

### 🟡 새 문제 5 — 범례를 접어도 캔버스 왼쪽 위 **49×24화소**는 칩이 먹는다 (1차 숫자 정정)

1차는 «(2%,2%) 히트테스트 **4/5 → 0/5**» 라고 적었습니다. **다시 재니 0/5 가 아닙니다.**

| 창 | 접힌 칩 위치(캔버스 기준) | (2%,2%) 를 누르면 | (5%,5%) 를 누르면 |
|---|---|---|---|
| 1100×614 | (8,8)~(57,32) | 캔버스 | **칩(SPAN.i.fold)** |
| 1366×682 | 같음 | 캔버스 | 캔버스 |
| 1920×994 | 같음 | **칩(SPAN.i.fold)** | 캔버스 |

칩은 **화소 크기가 고정**(49×24)이라 «몇 %» 로 찍으면 창 크기에 따라 맞기도 비껴가기도 합니다 —
**«0/5» 는 측정 방식이 만든 숫자**입니다. 바른 말은 «막히는 자리를 **660×28 → 49×24화소**로 줄였지만 0 은 아니다» 입니다.
칩이 «눌러서 접기» 단추라 `pointer-events` 를 끌 수도 없습니다. **코드는 고치지 않았고**(캔버스 밖으로 옮기면 55% 합격선과 부딪침),
사용법·체크판·판정서 **세 문서의 문장을 고쳤습니다**. 완전 해결안은 토요일 Codex 질문 3번.

---

## ⑤ 1차가 안 고치고 남긴 것 — 둘 다 고쳤습니다

### ✅ N-L «마스크에서 초벌 생성» 이 AI 제안이 빈 사진에서 0개

- **전제 확인**(`r3_api.py` L절): 포도 `740` — `source=ai` → **0개**, `source=gt` → **10개**.
  «원본 GT 로 되돌아가야 하지 않나» → **맞습니다.** 사람은 원본에 열매가 그려져 있는데도 하나하나 드래그해야 했습니다.
- **고침**(`app.js` +5줄, `seedBoxes` 안): `if (j && j.ok && !j.n && src === "ai") j = await seed("gt");`
  화면에 «**원본 GT**에서 10개 초벌» 이라고 어디서 만들었는지 그대로 적힙니다(`SEEDKO` 사전은 이미 있었습니다).
- **안 건드린 것**: 번호가 있는 과일(`src="inst"`)에서는 되돌리지 않습니다 — 번호가 있으면 번호가 정답이고,
  0개라면 그 사진에 번호가 없다는 뜻입니다. `boxsim.js` 가 글자 그대로 떼어 가는 `saveBoxes` 는 손대지 않았고
  `async function seedBoxes()` 줄도 그대로라 **boxsim 50/50 그대로 통과**합니다.

### ✅ N-M 로그인 제한이 성공한 로그인도 센다

- **재현·고침**(`server.py` +21줄): `login_rate_ok()` 를 **읽기만** 하게 바꾸고 `login_fail()` 을 새로 만들어
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
  → **무차별 대입 막기는 그대로이고, 한 사무실 IP 에서 여러 사람이 연달아 들어오는 것은 이제 안 막힙니다.**

---

## ⑥ 고친 것 — diff

`server.py` 4곳 · `app/static/app.js` 1곳 · `app/static/ui.js` 1곳. 백업 `_backup_260918_ui10_*`.
줄 수: `server.py` 1,341 → **1,370**(+29) · `app.js` 2,250 → **2,255**(+5) · `ui.js` 569 → **583**(+14). **합 +48줄.**

```diff
--- app/_backup_260918_ui10_server.py
+++ app/server.py
@@ login_rate_ok  (N-M)
-def login_rate_ok(ip):
-    """True 면 시도해도 된다. False 면 1분 안에 너무 많이 시도한 것."""
+def login_rate_ok(ip):
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
     …
-        q = [t for t in _login_tries.get(ip, []) if t > now - LOGIN_TRY_WINDOW]
-        if len(q) >= LOGIN_TRY_LIMIT:
-            _login_tries[ip] = q
-            return False
-        q.append(now)
-        _login_tries[ip] = q
-        return True
+        q = [t for t in _login_tries.get(ip, []) if t > now - LOGIN_TRY_WINDOW]
+        _login_tries[ip] = q
+        return len(q) < LOGIN_TRY_LIMIT
+
+
+def login_fail(ip):
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
+    …q.append(now); _login_tries[ip] = q

@@ login()  (N-M)
+    login_fail(request.remote_addr or "?")   # 0918 2차 N-M: 틀린 시도만 센다
     time.sleep(0.5)   # 무차별 대입 속도만 늦춥니다

@@ _save_verdict  (메모 이중 기록)
-    new_note = (old + " · (수정본 저장) " + note)[:300] \
-        if (note and not old.startswith(note.strip())) else old
+    new_note = note if note.startswith(old) \
+        else ((old + " · (수정본 저장) " + note)[:300] if note else old)

@@ api_undo_duplicate_exclusions  (새 문제 2·4)
-        n_undoable = sum(… and str(r.get("note", "")).startswith("중복: "))
+        n_undoable = sum(1 for r in data.values()
+                         if r.get("status") == "exclude" and r.get("src") == SRC_DUP_BULK)
     …
-            if rec.get("status") != "exclude" or not str(rec.get("note", "")).startswith("중복: "):
+            if rec.get("status") != "exclude":
                 continue
     …
             rec.pop("src", None)
+            rec.pop("prev", None)

@@ api_undo_exclude_group  (새 문제 2·4)
             if (not rec or rec.get("status") != "exclude"
-                    or not str(rec.get("note", "")).startswith("중복: ")
                     or rec.get("src") != SRC_DUP_GROUP
                     or (rec.get("by") or "") != by):
     …
             rec.pop("src", None)
+            rec.pop("prev", None)
```

```diff
--- app/static/_backup_260918_ui10_app.js       (N-L)
+++ app/static/app.js
-  const j = await api(`/api/boxes_seed?…&source=${src}`);
+  const seed = (s) => api(`/api/boxes_seed?…&source=${s}`);
+  let j = await seed(src);
+  // 0918 UI사이클5 2차(N-L): AI 제안이 **빈** 사진이 있다(포도 740: ai 0개 · 원본 GT 10개)
+  if (j && j.ok && !j.n && src === "ai") j = await seed("gt");
```

```diff
--- app/static/_backup_260918_ui10_ui.js       (새 문제 1)
+++ app/static/ui.js   (syncTaskUI 안)
+  const bs = $("#btn-save");
+  if (bs) {
+    bs.disabled = t !== "mask";
+    bs.title = t === "mask" ? "붓·다각형으로 고친 초록 그림을 파일로 저장합니다." : ( … 왜 회색인지 … );
+  }
```

---

## ⑦ 실서버(5111) — «새 화면 + 옛 서버» 섞인 판 (재시작은 3차 몫)

`r5_live.py`(GET·`/login` 만) **21/21**. `/api/health` → **PID 223681 · 2026-09-18 03:53 시작** 그대로.

- 정적 파일은 **재시작 없이 바로 나갑니다** → 실측: 실서버가 주는 `ui.js` 에 `bs.disabled = t !== "mask"` **있음** ·
  `app.js` 에 N-L 고침 **있음** · `help.html` 에 내가 고친 문장 **있음**.
  실서버 브라우저로 확인: ② 에서 «수정본 저장» **회색** · «상자 저장» 화면 안 · 캔버스 63.5% · 범례 접힘 · 판정 5단추 5/5.
- `server.py` 수정(4곳)은 **아직 안 실렸습니다.** 그래서 실서버에서는 지금도
  메모 이중 기록·`prev` 잔존·번호 저장 뒤 되돌리기 실패·로그인 성공 계수가 **남아 있습니다.**
- **섞인 상태가 깨지지 않는 것은 확인했습니다**: 내 프런트엔드 수정 둘 다 새 API 를 쓰지 않습니다
  (`ui.js` 는 순수 화면 · `app.js` 는 이미 있는 `/api/boxes_seed?source=gt` 를 한 번 더 부를 뿐).
- 네 과일 실측(지금 값 — 문서에는 적지 않습니다): 사과 1,001장(제외 572·flag 51) · 블루베리 1,195장(제외 81·flag 4) ·
  복숭아 125장(fixed 18·flag 30·ok 77) · 포도 2,406장(제외 3·fixed 12·flag 72). 「검수자별」에 AI 3회 검수 제외 653.
- 스크린샷 `~/ff_shots/ui5/stage2/LIVE2_{list,apple_mask,apple_box,help}.png`.

---

## ⑧ 판정서 초안·체크리스트·help·README 검수 — 고친 문장 목록

백업: `문서/_backup_260918_ui10_260918_툴UI개선_5회검수_최종판정.md` ·
`문서/_backup_260918_ui10_260917_툴_사람확인_체크리스트.md` ·
`app/static/_backup_260918_ui10_help.html` · `app/_backup_260918_ui10_README.md`

### 8-1. 먼저, **대조해서 맞은 것**(고치지 않음)

| 주장 | 대조 결과 |
|---|---|
| 줄 수 표(§9) app.js 2,008→2,250 · ui.js 569 · index 294 · style 332 · help 331 · README 521 · 서버 4파일 2,122→2,221 · 합 +1,533 | **전부 `wc -l` 과 일치**. 산수도 맞음(242+569+94+217+122+190+99 = 1,533) |
| «5사이클 × 3단계 = 15» · «크롬으로 본 사람 0» | 맞음 |
| «네 과일 모두 새로 제외할 사진 0장» | 맞음 — `n_will_change` 사과·블루베리·복숭아·포도 **전부 0**(내가 다시 부름) |
| 문턱표 기준 «2026-09-17 · 4,727장» | `ui.js` 의 `TH_ASOF` 와 글자 일치(낡았다는 경고도 이미 적혀 있음) |
| 체크리스트가 이름으로 부르는 사진 9장 | **9장 전부 열림**(내가 다시 부름) |
| 실서버 PID 223681 · 03:53:13 | 맞음 |
| 낡은 데이터 숫자(454·547·549) | **한 곳도 안 남아 있음**(두 문서 전문 grep). 판정서는 «사과 제외가 늘었습니다» 로 숫자 없이 적었음 — 방침대로 |
| 파일 경로 15개 | 전부 존재 |

### 8-2. 판정서 초안에서 고친 문장 (11곳)

| 어디 | 고치기 전 | 고친 뒤 / 왜 |
|---|---|---|
| 머리 | (최종 수정 줄 없음) | «최종 수정: 2026-09-18 (2차 검수가 숫자·주장을 근거와 대조해 고침)» 추가 |
| §1 표 ③ | «보류는 N6 수정으로 풀린 것으로 봅니다(3차 확인 필요)» | **새 기능 판정서에는 아직 «보류» 로 적혀 있음**을 명시하고, 어느 쪽을 확정할지는 3차가 정한다고 적음 (두 문서가 서로 다른 말을 하는 것을 숨기지 않게) |
| §1 표 «제외 사진» | — | «②③ 중에는 «수정본 저장» 이 회색» 추가 |
| §1 본문 | «실제로 잃은 데이터는 전부 0 입니다(**전부 실서버에 나가기 전에 잡았습니다**)» | 🔴 **사실과 다름** — 원본 보고서 ⑫ 자체가 «0916~0917 잠복» 이라고 적고 있습니다. → «잃은 데이터는 지금까지 0. 다만 여러 건은 이미 실서버에 올라가 있었고 그 길을 밟은 사람이 없어서 0» 으로 |
| §1 본문 | «알려진 미수정 결함은 가벼운 것 2건» | «가벼운 것 1건 + 구조적 한계 2건» (N-L·N-M 은 고쳤고, 딥시크가 짚은 둘을 올렸으므로) |
| §2 표 | (사이클 5 한 줄) | «5 (2차 검수)» 줄 추가 — 네 결함과 고침 |
| §2 숫자 표 | «범례 662×28 → 49×24(0.1~0.4%)» | «**0 은 아닙니다** — (8,8)~(57,32)화소는 아직 칩» 추가 + 2차가 다시 잰 숫자 줄 추가 |
| §3 전체 | «남은 결함 — 가벼운 것 둘»(N-L·N-M) | **(가) 2차가 고친 것 · (나) 남은 가벼운 것 1건 · (다) 구조적 한계 2건** 으로 다시 씀. (다)에 **«같은 사진 동시 편집은 나중 저장이 이기고 경고 없음 → 사람 규칙: 과일·쪽 나눠 맡기»** 와 **«항목별 변경 이력(덧붙이기 로그) 없음 — 마지막 상태만 남음. `update_status` 한 곳에 jsonl, 다음 할 일 1순위»** 를 분명히 적음 |
| §3 끝 | «해결 44 / 미해결 17 / 사람 결정 5» 를 사실처럼 인용 | ⚠️ **정정 상자**: 표 행을 세면 **76행 = 해결 48 · 부분 2 · 미해결 22 · 사람 결정 3 · 정보성 1**. 합계 66 과 안 맞고 **무엇을 중복으로 뺐는지 안 적혀 있어 재현 불가** → 3차가 정할 것. 그때까지 «66건» 을 쓰지 않음 |
| §4(다) | «지나가 보지 않은 길 … 아주 긴 메모» | ✅ **«마스크 업로드가 끊기면?» 을 이번에 시험해 지웠다**는 결과 추가(8종 4xx·무손상·500 0) + **못 본 것(진짜 네트워크 절단)** 을 남김 |
| §5 | «위 3 의 «초벌 0개» 를 이번 주에 고칠지» | 고쳤으므로 **범례 49×24를 없앨지**로 교체 · 3번에 ««수정본 저장» 이 회색» 추가 |
| §7 | 규칙 4 · 7까지 | 규칙 4 에 ««수정본 저장» 회색 · Ctrl+S 는 그 작업 저장» · **규칙 8 «같은 사진을 두 사람이 동시에 고치지 마세요»** 추가 |
| §8 | «되돌리기: ui9 백업» | «ui10(2차분만)» 과 «ui9(사이클 5 전체)» 를 나눠 적고, 🔴 **«2차의 서버 수정은 5111 에 아직 안 실렸다 — 재시작은 3차가 판단»** 줄 추가. 2차 보고서·스크립트 경로 추가 |
| §9 | 표 3열 | 4열(1차 판 / 지금)로 늘려 **2,255 · 583 · 2,250(합 약 +1,581, 2차분 +48)** 로 갱신 + «처음» 의 기준을 각주로 |
| §10 | «물을 것 5개» | **8개** — 1번을 «`src` 하나로 줄인 것이 P1 보호를 약하게 하지 않는가» 로 바꾸고, **6 마스크 PNG 업로드 중단 · 7 같은 사진 동시 편집 경고의 최소 구현 · 8 항목별 변경 이력 로그의 최소 구현** 추가(각각 고칠 파일·함수 이름을 적음) |

### 8-3. 체크리스트에서 고친 문장 (6곳) — **크롬을 처음 여는 초보자로 끝까지 따라가 보고**

파이어폭스로 A~H 를 한 줄씩 따라가며 **화면과 다른 문장**을 찾았습니다.

| 어디 | 무엇이 달랐나 | 고친 뒤 |
|---|---|---|
| 머리 | — | «2차 검수판» · «②③ 중에는 «수정본 저장» 이 회색» 추가 |
| 먼저 볼 것 4 | «메모 칸과 «수정본 되돌리기» 가 사라지는 것은 정상» — **«수정본 저장» 이 회색이 되는 것은 안 적혀 있었음**(내 수정으로 새로 생긴 화면) | 회색이 되는 것과 **왜**(붓질이 막혀 저장할 것이 없다) · **Ctrl+S 는 상자·번호 저장으로 간다**를 적음 |
| **B** | 🔴 «**사진 왼쪽 위 모서리**에 붓으로 그어 본다 → 초록이 칠해진다» — **실제로는 안 칠해집니다**(그 자리가 «색 뜻 ▸» 칸). 「«색 뜻» 칸 **바로 위**만 안 됩니다」 도 무슨 말인지 모르게 적혀 있었음 | ««색 뜻 ▸» 칸을 **살짝 비켜서**» 로 고치고, 막히는 자리가 **약 50×24화소**(접기 전 660×28)임을 숫자로 적고, 그 정도면 괜찮은지 한 줄 답을 부탁 |
| **F** | «AI 제안이 빈 사진이면 «덩어리가 없어…» 만 뜹니다. 드래그로 직접 그리세요» — **내 N-L 수정으로 달라짐** | «원본 라벨로 한 번 더 만들게 고쳤습니다(포도 740: 0개 → 10개)» · 오른쪽 칸에 «무엇에서 N개» 가 적힌다는 것 추가 |
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
| 마지막 질문 | 질문 3개 | ««수정본 저장» 이 회색이 되는 것» 을 3번에 넣고, **«색 뜻 ▸ 칸에서 붓질이 안 되는 것이 걸렸나»** 를 4번으로 추가 |

**막히는 항목은 없었습니다**(B 의 한 줄만 실제로 안 되는 동작이었고 고쳤습니다).
E1·E2·E3·D·G 는 문장과 화면이 맞았습니다 — G 의 «일괄 제외 되돌리기 → «이 단추로 넣은 제외가 없습니다»» 도
코드(`app.js` `n_undoable === 0` 분기)의 글자와 **한 글자씩 대조**해 맞는 것을 확인했습니다.

### 8-4. help.html 에서 고친 문장 (5곳) — 위에서 아래로 읽으며 화면과 대조

| 어디 | 무엇이 달랐나 | 고친 뒤 |
|---|---|---|
| §화면 어디에 뭐가 ③ | «칩이 사진 왼쪽 위를 덮어 그 자리에서 붓질이 시작되지 않던 것을 **0918 에 고쳤습니다**» — 과장 | «**660×28 → 50×24화소**로 줄였습니다 — «색 뜻 ▸» 칸 자리에서는 아직 안 됩니다. 그 칸을 살짝 비켜서 그으세요» |
| §화면 어디에 뭐가 ④ | 메모·되돌리기만 숨는다고 적혀 있었음 | «**«수정본 저장» 이 회색이 됩니다**» + 왜(«(Ctrl+S)» 단추가 둘 나란히 있으면 헷갈린다) + «그때 Ctrl+S 는 상자·번호 저장으로 갑니다» |
| §색 읽기 | «처음에는 접혀 있습니다 — **사진 왼쪽 위를 붓으로 칠할 수 있게**» — 과장 | «…접어도 «색 뜻 ▸» 칸 자리(약 50×24화소)에서는 붓질이 시작되지 않으니 그 칸만 비켜서» |
| §상자 | ««마스크에서 초벌 생성» 을 누르면 **AI 가** 열매마다 상자를 하나씩 놓습니다» — 무엇에서 만드는지가 실제와 다름(번호 있는 과일은 번호에서) | 번호 마스크 / AI 제안 / **AI 제안이 비면 원본 GT** 세 갈래를 적고, 오른쪽 칸에 무엇에서 만들었는지 적힌다는 것 |
| 꼬리말 | «최종 수정 2026-09-18(UI 개선 사이클 5)» | «… 사이클 5 · **2차 검수**)» |

**고치지 않은 것(맞았음)**: §판정 5가지의 «제외는 마스크 저장이 뒤집지 않는다» 경고 상자 · «수정본 저장은 자동으로 다음 장으로 안 넘어감»
(코드와 일치) · §안 될 때의 «두 사람이 같은 사진을 고침 → 나중에 저장한 쪽이 남습니다. 과일이나 쪽 범위를 나눠 맡으세요»
(딥시크 4-① 그대로. 이미 적혀 있었습니다) · 사용법 그림 **11장 전부 200** · 낡은 글귀 «5단계»·«늘 붙어 있습니다»·«접혀 있을 수 있습니다» **0**.

### 8-5. README

머리의 «최종 수정» 에 **2차 검수 줄**을 새로 얹고 1차 줄을 «그 앞» 으로 내렸습니다(네 수정과 각각의 «왜» 를 적음).
§2 의 «사과·블루베리는 팀 표준 원본에서 읽고 제외는 상태로 표시» 문장은 **대조해서 맞으므로 그대로 두었습니다.**

---

## ⑨ 최종 회귀 — 전부 통과

1차의 스크립트를 **같은 명령으로** 돌렸습니다(같은 숫자가 나오는 것이 «내 수정이 아무것도 안 깼다» 는 증거입니다).

```
node --check app/static/app.js                            ✅
node --check app/static/ui.js                             ✅
cycles/260917_신기능/cycle_1/stage1/sim.js                 ✅
cycles/260917_신기능/cycle_2/stage2/modesim.js             ✅ 31개 중 31개 통과, 0개 실패
cycles/260917_신기능/cycle_2/stage1_client/boxsim.js        ✅ 50개 중 50개 통과, 0개 실패
python app/boxes.py 자체 점검                              ✅
cycle_6_n6/stage1/n6_save.py   (N6)                       ✅ 실패 0
cycle_6_n6/stage1/n1_cut.py    (N1)                       ✅ 실패 0
cycle_5/stage1/live_c5.py      (읽기 전용 API 16항목)      ✅ 실패 0   ※ 모래상자 5172
cycle_3/stage1/api_seed.py     (상자 초벌 표본 40장)        ✅ 다른 것 0 ※ 모래상자 5172
py_compile server·instances·boxes·dupes·maskio             ✅
                                                    → t4_reg 11/11
cycle_5/stage1/t7_docs.py  (문서·풍선말·사진·그림 29항목)   ✅ 29/29 (내 help.html 수정 뒤 다시 돌림)
```

**P1·P2·N-A 계열은 내 시험이 직접 다시 밟았습니다**: P1 = `r2_undo.py` G5(AI 제외 568장 무손상) ·
P2 = `r1_mix.py` 순서1·3(번호 저장이 판정·메모를 안 덮음) · N-A = 순서1~3 의 «되돌리면 글자 그대로 복구».

---

## ⑩ 시험한 조작 · 안 한 조작

| 조작 | 어디서 | 했나 | 결과 |
|---|---|---|---|
| N-C 를 **여섯 가지 섞은 순서**로(제외·flag × 붓질/AI/번호/되돌리기/판정1) | 모래상자 5172 API | ✔ 고치기 전/후 | 3실패 → 53/0 |
| 묶음·일괄 제외 되돌리기에 붓질·번호 저장 섞기 | 〃 (묶음 하나를 일부러 «아직 안 봄» 으로 되돌려) | ✔ 고치기 전/후 | 6실패 → 22/0 |
| 다섯 창 × 세 작업 × 묶음/비묶음 (실측 뷰포트·`elementFromPoint`) | 모래상자 · 진짜 브라우저 | ✔ | 234/1 |
| ② 에서 **진짜 드래그 + 진짜 Ctrl+S** | 〃 | ✔ | 상자 저장 · 마스크 무변화 |
| ② 에서 «수정본 저장» 을 **살려서** 누르기(고치기 전 재현) | 〃 | ✔ | 가짜 수정본 + fixed 재현 |
| 옛 TOUR 키 프로필 · 범례 기하 실측 · id 22개 · 단축키 X·K | 〃 | ✔ | B7·B9·B10 통과 |
| 망가진 마스크 업로드 **8종** | 모래상자 API | ✔ | 8종 4xx · 파일·판정 무손상 · 500 0 |
| 로그인 제한(맞는 15번 / 틀린 12번) | 〃 | ✔ 고친 뒤 | M1·M2·M3 통과 |
| 저장 vs 제외 **동시** 120요청 | 〃 | ✔ | 소실 0 · 6.4초 |
| 회귀 11종 + 문서 29항목 | 현재 코드·모래상자 | ✔ | 11/11 · 29/29 |
| 실서버 GET 눈 확인(네 과일·편집·②·사용법·게이트) | 실서버 5111 | ✔ | 21/21 |
| **실서버 쓰기 단추** | — | ✘ 일부러 | GET·`/login` 만 |
| **실서버 재시작** | — | ✘ 일부러 | `server.py` 수정은 3차가 실을 것 |
| **크롬·엣지·모바일** | — | ✘ | 서버에 파이어폭스뿐 → 사람 체크리스트(다섯 사이클 연속 미확인) |
| 진짜 브라우저 **두 대** 동시 | — | ✘ | 사이클 4 2차가 했음 · 이번은 API 2갈래로 갈음 |
| 네트워크를 **진짜로 끊어** 업로드 중단 | — | ✘ | 본문 망가뜨리기로 갈음 → Codex 질문 6번 |
| 윈도우 배율 150% · 300자 넘는 메모 | — | ✘ | 판정서 §4(다)에 남김 |
| 공용 `T/data`·원본·새 데이터셋·`T/inspect`·`T/cycles/260917_ab` 쓰기 | — | ✘ | 읽기만 |
| `작업기록.md` 갱신 | — | ✘ | 금지 목록대로 |

---

## ⑪ 자취

| 무엇 | 어디 | 크기 | 처리 |
|---|---|---|---|
| 내 시험 스크립트 5개 | `cycles/260917_ui/cycle_5/stage2/r{1_mix,2_undo,3_api,4_layout,5_live}.py` | 약 45KB | 남깁니다(재현용) |
| 결과·로그 | 같은 폴더 `r1_mix.json`·`r2_undo.json`·`r3_api.json`·`r3_seed_grape740.json`·`r4_layout.json`·`r4_run.log`·`r5_live.json` | 약 90KB | 남깁니다 |
| 스크린샷 25장 | `~/ff_shots/ui5/stage2/` (`L2_<창>_<작업>_<과일>.png` 20장 · `B5_ctrls_box_peach.png` · `LIVE2_*.png` 4장) | **16MB** | 남깁니다(증거) |
| 백업 6개 | `app/_backup_260918_ui10_{server.py,README.md}` · `app/static/_backup_260918_ui10_{app.js,ui.js,help.html}` · `문서/_backup_260918_ui10_{판정서,체크리스트}` | 약 200KB | 남깁니다 |
| 모래상자 서버 | `127.0.0.1:5172` | — | **껐습니다** — 내가 띄운 PID 만(246304 → 256616 → 298704). 남의 **5111·5100·5101·5102·5104·5105 는 안 건드렸습니다** |
| 모래상자 `data/` | `cycles/260917_ui/cycle_1/stage2/sandbox/data/` | — | 내 시험은 끝에 status 를 되돌리고 만든 파일을 지웠습니다. 다만 `_status_backup_260918_04*.json` 2개가 «일괄 되돌리기» 시험 때 생겼습니다(모래상자 폴더 안) |
| 임시 파일 | 스크래치패드의 `d_*.diff`·`sbx.pid`·`live_c5.json` | 작음 | 자동으로 사라집니다 |

**공용 `T/data` 지문**: 파일 **7,397개** · **78,772,813바이트** · `apple`·`blueberry` `status.json` **09-18 03:07:53** ·
`grape` 09-17 00:55:26 · `peach` 09-17 14:59:05 — **1차가 적어 둔 것과 한 바이트도 다르지 않습니다.**
`T/inspect`·`T/cycles/260917_ab` 아래에 04:08 이후 바뀐 파일이 있지만 **제가 쓴 것이 아닙니다**(데이터 쪽 다른 세션 — 04:16·04:30).

---

## ⑫ 3차(Fable 5.1)가 판정할 것

1. 🔴 **실서버 재시작을 할지.** 지금 5111 은 «새 화면 + 옛 서버» 입니다(깨지지는 않음, ⑦). 내 `server.py` 수정 4곳을
   받아들이면 `py_compile` → 로그에 저장 중인 사람 없는지 확인 → `bash app/run.sh restart`.
   되돌리려면 `app/_backup_260918_ui10_server.py` 를 덮어쓰면 됩니다.
2. 🔴 **되돌리기 판별을 `src` 하나로 줄인 것**(새 문제 2 의 고침)이 **P1 보호를 약하게 만들지 않는지.**
   제 근거는 «`src` 는 그 두 단추만 남기는 표식이고, 메모 조건은 데이터 v2 도 «중복: » 을 쓰므로 애초에 구분력이 없다»
   + 실측 G5(AI 제외 568장 무손상)입니다. **여기가 이번 수정에서 가장 위험한 곳입니다.**
3. 🟠 **② 에서 «수정본 저장» 을 회색으로 만든 판단.** 대안은 ㉠ 아예 숨기기(판정 5단추가 4개로 보임) ㉡ 그대로 두고
   풍선말만 고치기(위험 유지) ㉢ 지금처럼 회색. ③ 과 같은 방식이라 ㉢ 을 골랐지만, ① 에서 붓질하다 ② 로 옮긴 사람이
   저장하려면 ① 로 돌아가야 합니다.
4. 🟡 **«제외 → 붓질 저장 → 사람이 `1` → 수정본 되돌리기» 가 제외로 돌아가는 것**(새 문제 4 의 남은 절반)을
   설계로 볼지 결함으로 볼지. 저는 손대지 않았습니다.
5. 🟡 **범례 49×24화소**를 이번에 없앨지(캔버스 밖으로 옮기면 55% 합격선과 부딪침), 문서에 적어 둔 대로 사람 결정에 맡길지.
6. ⚠️ **열린 문제 숫자를 하나로 정할 것** — 1차의 «44/17/5(합계 66)» vs 표 76행의 «48/2/22/3/1».
   판정서에는 지금 «정정 상자» 로 둘 다 적혀 있습니다.
7. ⚠️ **③ 열매 번호의 «보류» 문구** — 이 판정서는 «풀린 것으로 봅니다», `260917_상자와번호기능_5회검수_최종판정.md` 는
   «보류». 두 문서 중 하나를 고쳐야 합니다.
8. 판정서 §10 의 **질문 8개**가 충분히 구체적인지(아래 ⑬).

---

## ⑬ 토요일(2026-09-19) Codex·DeepSeek 질문 검토

1차의 5개를 읽고, **코드 위치를 짚지 않은 것을 보강**하고 코디네이터가 시킨 3개를 더해 **8개**로 만들었습니다.

| # | 1차 판 | 내가 한 것 |
|---|---|---|
| 1 | «제외 규칙이 맞나 + 메모를 앞에 두는 방식» | 메모 쪽은 **내가 고쳐서 시험까지 했으므로** 질문에서 내리고, 대신 **훨씬 아픈 것**으로 바꿨습니다 — «되돌리기 판별을 `src` 하나로 줄인 것이 P1 보호를 약하게 하지 않는가»(함수 이름 2개·시험 파일 명시) |
| 2 | «`display:contents` + `appendChild` 가 크롬·엣지에서도 같은 배치인가» | 그대로 둠 — 구체적이고 코드 위치(`ui.js` `#taskbtns`)가 적혀 있음 |
| 3 | «범례 기본 접힘» | 그대로 두되, 내 실측(**49×24화소가 아직 막힘**)을 §3-나 에 붙여 질문이 «정말 해결됐나» 로 읽히게 함 |
| 4 | «자물쇠 밖에서 읽는 구조» | 그대로 둠 + 내 경합 실측(120요청 0회)과 «왜 안 걸리는지»(무거운 일이 읽기 앞에 있다)를 보고서에 남겨 Codex 가 «그럼 안 고쳐도 되나» 를 판단할 재료를 줌 |
| 5 | ponytail 셋 | 그대로 둠 |
| **6 (새)** | — | **마스크 PNG 업로드 중단** — 내가 8종을 시험해 4xx·무손상을 확인했으므로, 남은 것만 물음: **TCP 절단 시 `request.get_json` 의 거동**과 **업로드 크기 상한이 없는 것**(마스크가 base64 200KB, 상한 없음) |
| **7 (새)** | — | **같은 사진 동시 편집 경고의 최소 구현** — «`/api/item` 의 `at` 를 브라우저가 들고 있다가 저장 때 보내고 다르면 409» 안을 제시하고, 고칠 곳(`api_save`·`api_status`·`update_status`·`app.js doAction`)을 적음. «사람 규칙으로 충분한가» 도 같이 물음 |
| **8 (새)** | — | **항목별 변경 이력 로그의 최소 구현** — «`update_status` 한 곳에서 `data/<과일>/status_log.jsonl` 에 한 줄» 안을 제시하고, 파일이 커지는 것·자물쇠 안 추가 쓰기 비용·«되돌리기 도구는 안 만들고 사람이 읽는 기록으로만» 이 게으른 답인지 물음 |
