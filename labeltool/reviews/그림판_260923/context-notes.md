작성: 2026-09-23

- 결정: 백엔드 API 는 그대로 쓴다. 새로 만드는 건 화면뿐 → 저장 파일 형식·통합 데이터셋 빌드가 안 깨진다.
- 결정: 한 장의 라벨 = 번호 배열 하나(0=배경). 이진 마스크는 번호>0 로 만든다.
- 결정: 원본에 «칠했는데 번호 없는» 자리는 회색(번호 없음)으로 보여 주고, 채우기통으로 번호를 붙이게 한다.
- 결정: 저장 순서 = 마스크(/api/save fixed) → 번호(/api/save_instances) → 상자(/api/boxes). 옛 툴 «지우개 하나로 마스크+번호» 와 같은 순서.
- 결정: AI 판정은 화면에서만 뺀다. status.json·proposals 삭제는 안 한다(되돌릴 수 없고, AI 제외 표시를 빌드가 쓸 수 있음).
- 2026-09-23 실측: 모래상자(sb_paint, 포트 5601) + 헤드리스 파이어폭스로 복숭아·사과·블루베리·포도 각 1장 — 채우기(새 번호)·되돌리기·붓·지우개·올가미·오른쪽 클릭 지우기·저장·다시 열기 동일 전부 통과. 저장 뒤 masks_fixed·instances_fixed·boxes 파일과 confirmed·confirmed_instances·confirmed_boxes(by=시험봇) 확인. 시험 스크립트: scratchpad test_paint.py (툴 tests 에는 안 넣음). 그림 ~/ff_shots/paint_260923/.
- 파이어폭스 WebDriver 는 페이지의 최상위 const 를 못 본다 → 시험은 window.eval 로 감쌌다.
- 옛 시험: u6 주소 계약에 /old 추가 · 옛 브라우저 시험 5개와 sandbox.browser·ff.login 이 로그인 뒤 /old 로 가게 고침(백업 _backup_260923_paint_*). syntax·unit·sim·api 822/0.
- 기존 확정(confirmed*) 은 전부 사람(임성후·익명) — 새 목록의 ✓ 에 AI 판정은 섞이지 않는다.
- 2026-09-23 15:05: ✨클릭 칠하기 = SAM2.1-b 를 별도 도우미(5112)로. 툴 서버에 torch 를 안 넣으려고(가볍게·죽어도 툴은 삶). 사진 전체로는 작은 열매가 뭉개져(IoU 0.48) 누른 점 주변만 잘라 넣음(0.66).
- 모델 초벌 = YOLO11m-seg 4과일 한 모델. 학습에 쓴 사진을 예측하므로 원 라벨을 닮는다 — «빠진 열매만 더하기» 는 원 라벨과 30% 넘게 겹치는 예측을 버린다.
