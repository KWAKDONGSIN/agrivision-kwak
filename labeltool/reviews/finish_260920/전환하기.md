대행: Codex
작성: 2026-09-20

# 사람이 마지막에 전환하기

현재 파일은 새 구조이고 실행 중인 PID1467097은 00:04:44에 뜬 옛 서버다. 이 문서를 만든 Codex는 실서버를 재시작하지 않았다.

## 먼저 확인

`/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/app/logs/server.log`에서 최근 15분 동안 외부 IP 접속이 없는지 확인한다. 접속이 있으면 사용이 끝날 때까지 기다린다. 자동 로그 판정만 믿지 말고 시각·접속 줄을 직접 본다. 이 작업의 localhost 점검과 외부 접속을 구분한다.

## 전환 명령 한 줄

```bash
cd /data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/app && bash run.sh restart
```

## 친 뒤 확인

출력된 PID가1467097과 다른지, 로그인되는지, 사진 한 장이 열리는지 확인한다. 다른 사람이 편집하기 전에 아래 점검을 실행한다. 네 과일의 첫 사진·상자·PNG를 읽고 첫 사진에 상자1개를 저장했다 되돌린다. 기존 상자·판정은 `_archive/smoke_시각/`에 먼저 보관하고 원래 바이트로 복원한다. 같은 사진을 다른 사람이 편집하는 동안 실행하지 않는다. 읽기만 하려면 `--read-only`를 붙인다.

```bash
/home/kds0206/.conda/envs/kwak/bin/python /data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/cycles/260920_structure/finish_260920/after_restart_smoke.py
```

## 잘못됐을 때 되돌리는 한 줄

```bash
cp -a /data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/_archive/pre_finish_260920/. /data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/ && bash /data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/app/run.sh restart
```

이것은 이번 마무리 직전의 디스크 파일로 돌아간다. 그때 디스크도 이미 분리 구조였으므로 옛2052줄 서버와는 다르다. 자료 `data/`와 실행 로그는 복원 대상에 포함하지 않는다. 새로 추가한 보고서 등은 남는다.

## 미리 해 본 복원

2026-09-20T12:55:51.310322+09:00 모래상자 포트5671에서 자기 서버를 종료한 뒤 `cp -a`로 백업을 복원하고 새 프로세스를 띄웠다. 원래84개 app 파일 지문 일치·로그인·4과일 조회를 확인했다. 증거는 `evidence/rollback_pre_finish.json`. 옛2052줄 서버도 별도 사본 포트5601에서4과일·4사진 조회 성공(`rollback_old.json`).

금지된 운영 `run.sh restart` 자체는 실행하지 않았다. 복원 파일과 서버 기동을 격리 사본에서 실측했고, 실제5111 전환은 위 한 줄을 사람이 실행하는 단계로 남겼다.
