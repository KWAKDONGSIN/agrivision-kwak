작성: 2026-09-18

# 사과·블루베리 5회 재검수 — 사이클 5(총괄) · 2단계 검수 보고서 (+ 최종 문서 동결)

누가: Claude Opus 5 (1M). **2026-09-18 03:37 시작 · 04:3x 작성** (`date` 실측).
읽은 것: 1단계 보고서 `cycle_5/stage1_report.md` + 판정서 초안 · 총괄이 추가한 `gap_eye_part{1,2}.csv` ·
`curve_eye_part{1,2}.csv` · 열쇠 2개 · `curve_and_stride.py` → `curve_stride_apple.md` · `stride_variants_apple.csv` ·
`build_exclusion_v2.py`(2단계 추가판) · manifest 2개 · `build_summary.json` · 툴 `status/duplicates.json` ·
실서버 `/api/fruits`(GET 1회) · README · 3차판정 · 사이클 1·3·4 3단계 판정서 · UI 사이클 4 3단계 판정서 ·
교수님확인 · 카톡 · PDF 생성기 2개 · 총괄이 보낸 추가 사실 2건(`session` 칸 · DeepSeek 확인 결과).

**한 일**: ① 1단계 주장을 **내 코드로 표본 재현**(가장 위험한 것 위주) · ② 최종 숫자를 **원천에서 처음부터
다시 계산**(앞 사이클·1단계 스크립트 import·복사 0) · ③ 1단계가 넘긴 판정 요청 5가지에 답 ·
④ **최종 숫자 사전** 작성 · ⑤ **두 번째(최종) 문서 동결 실행**(문서 16개 + PDF 2개 재생성) ·
⑥ 낡은 숫자 잔존 검색 · ⑦ 3단계가 판정할 것 목록.

**안 한 것(금지 사항 준수)**: 데이터셋 파일·manifest·원본·툴 `data/`·app 코드·`inspect/` 의 CSV **수정 0** ·
`--tool` 실행 0 · 실서버에 GET 외 요청 0(로그인 POST 대신 서명 쿠키를 만들어 `GET /api/fruits` 한 번) ·
`작업기록.md` 0줄 · GPU 0장 · 새 패키지 0개 · 삭제 0건 · kill 0건.
(총괄이 `build_reviewed_dataset_260917_v2.py` 에 `session` 칸 **코드**를 넣은 것은 내 수정이 아닙니다 —
§5 에서 코드부 diff 로 분리해 확인했습니다. 내가 그 파일에 손댄 것은 **docstring 뿐**입니다.)

쓴 스크립트: `stage2/scripts/{s2_recount,s2_probe,s2_mis_exact,s2_rebuild,s2_kappa,s2_session,s2_stale}.py`
(전부 새로 짬 · 출력 `stage2/logs/*.json`). 고친 내역 전문: `stage2/edits.diff`(1,752줄 · 파일 16개).

---

## 1. 동결 시각 — 실측

| 파일 | 시각 (`ls -l --time-style=full-iso`) | 뜻 |
|---|---|---|
| 툴 `data/{apple,blueberry}/status.json` · `duplicates.json` | **2026-09-18 03:07:53** | **판정값(제외·flag) 동결.** 이 뒤로 판정은 안 바뀜 |
| `datasets_reviewed_260917/{apple,blueberry}/manifest.csv` · `build_summary.json` | **2026-09-18 04:12:36** | 총괄이 **`session` 칸**을 덧붙인 시각. 장수·판정·파일은 **불변** |
| 실서버 `/api/fruits` | 03:45 GET | 사과 exclude **572** · flag 51 · reviewed 623 / 블루베리 81 · 4 · 85 |

**최종 장수: 사과 1,001 → 429 (제외 572 · flag 51) / 블루베리 1,195 → 1,114 (제외 81 · flag 4).**

---

## 2. 1단계 검수 — 위험한 주장부터 내 코드로 재현

### 2-1. 🟢 가장 위험한 주장을 **완전히 재현**했습니다 (제외 명단 독립 재생성)

`s2_rebuild.py` — `build_exclusion_v2.py` 를 import·복사하지 않고 **규칙 설명만 읽어 새로 구현**해
눈 판정 CSV 에서 제외 명단을 처음부터 다시 만들었습니다.

| | 1단계만 | 2단계까지 | manifest 와 비교 |
|---|---|---|---|
| 사과 | 간선 **1,204** · 정점 **820** → 제외 **454** · 남김 **547** | 2단계 새 쌍 207 → 추가 제외 **118** → 제외 **572** · 남김 **429** | **집합까지 동일**(나만 있는 이름 0 · manifest 에만 있는 이름 0) |
| 블루베리 | 간선 158 · 정점 123 → 제외 **81** · 남김 **1,114** | 2단계 해당 쌍 **0** → 변화 없음 | 동일 |

→ 1단계 보고서의 «454/547» 도, 총괄의 «572/429» 도 **둘 다 내 코드로 재현됩니다.**
(flag 후보 이름은 57개인데 그중 6개가 제외에 먹혀 51개가 남는 것까지 일치 — docstring 의 «메모 이전 6».)

### 2-2. 🟢 1단계의 최대 독립 집합 값도 정확 풀이로 확인

1단계는 «분기한정으로 MIS 372 · 최소 제외 448 · 초과 6장» 이라고 했습니다.
`s2_mis_exact.py`(성분 분해 + 차수 0/1 축약 + 최대차수 분기, scipy·networkx 안 씀)로 1단계 그래프를 풀면
**정점 820 · 간선 1,204 · MIS 372 · 최소 제외 448** — **한 자리도 안 다릅니다.**

### 2-3. 🟢 판정자 간 일치도(카파)도 소수 셋째 자리까지 일치

`s2_kappa.py` 로 카파를 다시 구현: 사과 80.0/78.3/88.3%(κ 0.560/0.513/0.717) · 블루베리 72.4/79.3/58.6%
(0.431/0.584/0.167) · 전체 77.5/78.7/78.7% · 셋 다 일치 60/89.
`interrater_agreement.md` 와 **전부 일치**, 다수결 규칙 어긋남 **0**, `ni_votes` 칸 전부 일치, 뒤집힌 쌍 9·1.

### 2-4. 🟢 1단계가 찾은 «간격 6~20 의 안 본 쌍» 은 **닫혔습니다**

`s2_probe.py` 가 남긴 사과 사진끼리 간격 60 이하의 쌍을 **전수**로 세어, 관계 CSV·눈 판정 기록
어디에도 없는 것을 셌습니다.

| 출처·간격 | 쌍 | 관계 CSV | 눈으로 봄 | **아무도 안 봄** |
|---|---:|---:|---:|---:|
| 현장 10 / 15 / 20 | 25 / 20 / 26 | 25 / 20 / 26 | 21 / 14 / 20 | **0 / 0 / 0** |
| dataset 30 | 60 | 60 | 34 | **0** |
| 현장 25 / 30 / 35 / 40 | 32 / 52 / 35 / 25 | 10 / 20 / 11 / 4 | 5 / 9 / 3 / 3 | 22 / 32 / 24 / 20 |
| 현장 45 / 50 / 55 / 60 | 39 / 36 / 41 / 41 | 8 / 4 / 2 / 4 | 2 / 0 / 1 / 1 | 31 / 32 / 39 / 36 |
| dataset 60 | 73 | 55 | 25 | 18 |

- 🟢 **«확인 범위» 안(현장 ≤20 · dataset ≤30)에 아무도 안 본 쌍 = 0개.** 1단계의 지적은 **완전히 해소**.
- 🟡 범위 **밖**에는 **254쌍**이 남아 있습니다(위 표의 합). 곡선 비율을 곱하면 그중 **약 70쌍**이
  «거의 같은 사진» 일 것으로 추정됩니다(±20쌍). → **열린 문제 #27 로 새로 올렸습니다.**

### 2-5. 🟢 곡선표·대안표를 원천 CSV 에서 다시 세어 **전부 일치**

`curve_stride_apple.md` 의 17행 전부, `stride_variants_apple.csv` 의 `keep_G*` 열 합계 전부
(394·294·248·187·137·105 · `keep_current` 429) 내 계산과 **같습니다**.
«20% 아래로 처음 떨어지는 지점» 을 내 코드가 표에서 직접 찾은 결과도 **현장 40 · dataset 90** 입니다.
합 552쌍 · 거의 같은 사진 229 · **다른 장면 0**.

### 2-6. 🔴 내가 새로 찾은 것 3건

| # | 무엇 | 근거 |
|---|---|---|
| **A** | **곡선의 간격별 비율에 판정자 효과 ±10~15%p.** 같은 표본을 반씩(**겹치는 쌍 0**) 나눠 본 두 판정자의 «거의 같은 사진» 비율이 `gap_eye` **78.0%(117/150) vs 60.0%(90/150)** · `curve_eye` **11.9% vs 5.6%**. 간격 구성이 비슷한데도(간격 10: 36 vs 54 · 30: 33 vs 27) 간격마다 한쪽이 계속 더 후함(간격 20: 76% vs 60% · 30: 88% vs 56%) → **비율 차이가 아니라 판정자 효과**. 두 part 는 겹치지 않으므로 **카파가 아닙니다** | `s2_recount.json` · 각 part CSV |
| **B** | **README 의 «완화 75그룹» 은 부정확.** 원천은 **76그룹**이고 그중 **진짜 75**입니다. 해시 쪽도 «47그룹» 은 1차 탐지 값이고 2차가 실제로 본 것은 **71그룹**(전부 진짜) | `round2_apple_dup{,new}_verdicts.csv` 71·76행 |
| **C** | **UI 사이클 4 3단계 판정서에 «사과 제외 454» 가 남음.** 03:05 실서버 재시작 확인 값인데 데이터 재동결이 **03:07:53** 이라 2분 차이로 낡았습니다. 내 사이클 문서가 아니라 **손대지 않았습니다** | `cycles/260917_ui/cycle_4/stage3_final.md:10` |

### 2-7. 그 밖에 내가 대조해 **전부 일치**한 것

폴더 실제 PNG = manifest 의 «남김» 집합(**이름까지**) · manifest 행 집합 = 원본 파일 집합 ·
`exclusion_v2` 의 뺀 사진 집합 = manifest 의 뺀 사진 집합 · `flags_v2` 의 needs_human 집합 = manifest ·
`rule_pending` 39/244(남긴 17/218) · `overlap_neighbors` 301/112 · 이웃 칸에 뺀 사진 이름 0 · 관계 대칭 ·
`split_group` 99/1,055(2장 이상 48/53 · 최대 39/4 · 빈칸 0) · 관계 1,411+773 / 158+63 · 중복 간선 0 ·
남긴 사진끼리 near_identical **0** · 두 장 다 남은 겹침 286/59 · 툴 `status.json` 625/86항목이 manifest 와
**집합까지** 동일 · `duplicates.json` 336묶음 833장 / 42묶음 123장 · `method` 문장 참 ·
md5 표본 24파일 원본과 동일 · `build_summary.json` 전 항목.
🟢 **`masks_fixed`·`instances_fixed` 는 여전히 빈 폴더**(내보내기에 수정본이 섞일 위험 없음).

---

## 3. 1단계가 넘긴 판정 요청 5가지 — 내 답

| # | 1단계 질문 | 내 판정 |
|---|---|---|
| ① | «간격 6~10 의 안 본 쌍 90개를 전수로 볼 것인가» | **해소됨.** 총괄이 300쌍을 전수·표본으로 판정해 118장을 더 뺐고, 내가 세어 보니 **확인 범위 안에 안 본 쌍 0개**입니다(§2-4). 남은 것은 범위 밖 254쌍 → «한계» 로 명시 + 간격 솎기 대안 |
| ② | «a025↔a026 을 합칠 것인가» | **합칠 필요 없습니다 — 이미 해결.** manifest 실측: `…image1086` 과 `…image1096` 이 **같은 `split_group` a022**, 그리고 `image1086` 은 **제외됨**(`action=excluded_duplicate`). 그 쌍은 2단계 눈 판정 `gap_eye` 에 **`near_identical` 로 들어 있습니다**(같은 촬영의 1066·1076 쌍과 함께 4쌍). 전수 확인: 확인된 관계가 `split_group` 을 넘는 것 **0건** · `session` 을 넘는 것 **0건**. → 분할표에 «한 줄 예외» 를 넣을 필요가 없습니다. 열린 문제 **#24 = 해결** |
| ③ | «16 vs 15 — 뺀 사진까지 세었다는 한 줄을 넣을지» | **넣을 필요가 없어졌습니다.** `split_group` 이 99개로 줄면서 **남긴 사진만 세도, 뺀 사진까지 세도 같은 99개**이고 옛 분할표를 넘는 것도 **양쪽 다 9개**입니다(전에는 15 vs 16 으로 갈렸음). 그래서 주의문을 지우고 값만 갱신했습니다. 참고로 제외 묶음이 옛 표를 넘는 수는 53 → **112** 로 늘어(옛 표를 쓰면 안 된다는 근거가 세짐) 그 값도 갱신했습니다 |
| ④ | «지울목록을 주석 처리한 방식이 맞는지» | **맞습니다. 그대로 둡니다.** 명령을 아예 지우면 «왜 못 지우는지» 라는 맥락이 사라져 나중에 누가 다시 쓸 수 있습니다. 지금은 ⛔ 상자 + 주석 + 4곳 정정 표시가 있어 «읽으면 실수할 수 없는» 상태입니다. 다만 `--tool` 을 다시 돌리지 말라는 경고와 한 묶음이므로, **스크립트 docstring 에도 그 파일 이름을 적어** 두 곳이 서로를 가리키게 했습니다 |
| ⑤ | «포도 3장 열린 문제를 다시 적을지» | **적습니다.** 실서버 `/api/fruits`(03:45 GET)가 여전히 포도 `exclude 3` 으로 보고합니다 — 판정서 열린 문제 **#13** 에 실측 시각과 함께 남겼습니다 |

---

## 4. 총괄이 보낸 추가 사실 2건 — 내가 직접 다시 확인

### 4-1. `session` 칸 (2026-09-18 04:12:36 신설) — 🟢 전부 확인

`s2_session.py` 로 다시 셌습니다.

| 항목 | 사과 | 블루베리 |
|---|---:|---:|
| `session` 수 (남긴 사진 / 전 행) | **17 / 17** | **288 / 288** |
| 빈칸 | 0 | 0 |
| 가장 큰 촬영 | 45장 (다음 34·30·30·30·29·28·27) | 55장 (다음 35·18·16·15) |
| `split_group` 이 두 `session` 에 걸친 경우 | **0** | **0** |
| 확인된 관계가 `session` 을 넘는 것 | **0** / 2,184 | **0** / 221 |
| 촬영 단위 5-폴드 크기 | 84·78·78·95·94 | 223·223·223·223·222 |
| ↳ 폴드를 넘는 관계 | **0** / 286 | **0** / 59 |

그리고 **다른 값이 하나도 안 바뀐 것**을 확인했습니다: 429/572/51 · 1,114/81/4 · 336/42 · 99/1,055 ·
301/112 · 17/218 · 칸은 8개 → **9개**, `build_summary` 에 `sessions_kept` 추가.
🟡 **덧붙일 실측 하나**: 사과는 촬영이 17개뿐이라 촬영 단위 5-폴드의 크기가 **84~95(±10%)** 로 흔들립니다.
블루베리는 288개라 고르게 갈립니다. 문서에 이 한 줄을 같이 적었습니다.

### 4-2. DeepSeek 확인 결과 — 수용 5건을 문서에 넣었습니다

| | 수용 항목 | 어디에 넣었나 |
|---|---|---|
| (가) | «폴드를 넘는 관계 0» → **«확인된 관계 기준 0»** 으로만 쓰고 **분할은 `session` 단위 GroupKFold 권고**(`split_group` 은 더 잘게 나눌 때의 최소 조건) | README(칸 설명·방침 2번·주의) · 판정서(§0-(나) · §1-1 · §5-④ · §6-7) · 3차판정 ⑤-4 · 교수님확인 14·16번 · 보고서 PDF §3-3 새 상자 · 총정리 PDF §7 |
| (나) | 429장을 «중복 제거된 데이터셋» 이라 부르지 않고 **«눈 검토 기반 부분 중복 제거(κ 0.51~0.72)»** | README 방침 4번 · 판정서 §0·§6 표 새 행 · 3차판정 ⑤-6 · 교수님확인 16번 · PDF 2개 |
| (다) | **권고안 한 줄 명시** — «촬영 단위 분할 + `keep_G40_90`(294장). 429장 판은 차선. 최종 선택은 교수님» | README 🔵 상자 · 판정서 §0 🔵 상자 · 3차판정 ⑤-8 · 교수님확인 16번 · 보고서 PDF §3-4 새 상자 · 총정리 PDF §7 |
| (라) | 성능은 **출처별(MinneApple / dataset1~4)로 나눠 보고** | README(주의) · 판정서 §0·§6 표 · 3차판정 ⑤-8 · 교수님확인 16번 · PDF 2개 |
| (마) | 교수님 질문에 **«‘동그랗게’ 사진 예시 확인»** + 실측 «구멍 있는 개체 38,781개 중 **180개(0.46%)**» | 교수님확인 **17번 신설** · 보고서 PDF §5 질문 9번 신설 |

**기각 3건은 문서에 넣지 않았습니다** — «간격 솎기를 놓쳤다»(대안표에 이미 있음) ·
«사과 중심 공동이 비어야 한다»(겉에서 보이는 구멍은 없음 — 해석 오류) ·
«클라이언트가 `status.json` 전체를 PUT»(우리 구조 아님). 판정서 열린 문제 **#46** 에 기각 사유를 남겼습니다.

---

## 5. 고친 곳 (백업 `_backup_260918_freeze2_*` 먼저 · 전문 `stage2/edits.diff`)

| 파일 | 무엇을 |
|---|---|
| `kds0206/datasets_reviewed_260917/README.md` | 표·본문 전부 현행화(429/572/336/17/301/99 + **`session` 17·288 행 신설**) · **«왜 «여기까지» 라는 멈춤점이 없는가» 절 신설**(687→549→547→429 표 · 방침 4개 · 🔵 권고 상자 · 간격별 곡선표 · 간격 솎기 대안표 · `stride_variants_apple.csv` 사용법 · 남은 한계 254쌍/약 70쌍) · `session` 칸 설명과 **분할 권고** · «완화 76그룹(진짜 75)» 정정 · 출처별 보고 권고 + 구멍 0.46% |
| `inspect/…/260917_사과블루베리_3차판정.md` | ④ 를 최종 동결판으로 갱신(+ `session` 줄 · «확인된 관계 기준» 표현) · **«⑤ 최종 판단» 신설**(멈춤점 없음 · 곡선 · 대안표 · `session` 분할 권고 · 한계 · 권고 한 줄 · 최적해 비교 불가) · 머리말의 «쓰지 말 것» 에 547·454 추가 |
| `final/build_reviewed_dataset_260917_v2.py` | **docstring 만** (코드 0줄 — AST 로 확인. 총괄이 넣은 `session` 코드와 분리해 코드부 diff 로 확인) |
| `inspect/…/round3_final/v2/build_exclusion_v2.py` | **docstring 만** (코드 0줄 — AST 동일 확인). «5. 2단계 추가» 항목과 최종 실행 결과 |
| `cycles/260917_ab/cycle_{1,3,4}/stage3_final.md` | 각각 맨 끝에 «2026-09-18 03:07:53 후속» 한 단락(cycle_4 는 «②-4 의 근거가 바뀌었다» = 최적해 비교 불가) |
| `cycles/260917_ab/cycle_3/stage3/{number_dict,freeze_report}.md` | 🆕 **머리말에 ⛔ «이 문서는 1차 동결판(547장) 기준 — 인용하지 마십시오» 상자**(다른 문서가 이 둘을 «정본» 으로 가리키고 있었음) |
| `cycles/260917_사과블루베리검수_UI개선_전체계획.md` | 상태 칸 — A3″ 를 «1차 동결판» 으로 내리고 **A3‴ 최종 동결판 행 신설** · A4 ⑤=2단계 ✅ · B4 ✅(03:05) · B5 진행 중 |
| `app/README.md` | **숫자 줄만 최소 수정**(454 → **572** · 묶음 299 → **336** · 01:33 → 03:07:53, 옛 값은 «1차 동결판» 으로 표시). 04:12 시점에 다시 읽어 UI 에이전트가 손댄 상태 위에 고쳤고, 그쪽이 바꿔 놓은 문장은 건드리지 않았습니다 |
| `kds0206/문서/260917_교수님확인_7건.md` | 15건 → **17건**(제목·소제목) · **16번 «사과를 얼마나 솎을까요» 신설**(곡선표 + 선택지 3개 표 + 🔵 권고 한 줄 + `session` 분할) · **17번 «‘동그랗게’ 사진 예시» 신설**(+구멍 0.46%) · 6·10·13·14번 숫자 현행화 |
| `kds0206/문서/260918_카톡_사과블루베리검수.txt` | 25줄 · 순수 텍스트 · 표/마크다운/이모지 0 · 실측값만(572/429/81/1,114 · 960쌍 · 687→429 · 곡선 · 선택지 · 권고 · `session` · 254쌍) |
| `kds0206/문서/260918_사과블루베리_5회검수_최종판정.md` | **최종 상태로 다시 씀** — 구조 유지, 숫자 전부 현행화, **열린 문제 40 → 46건 재분류**(#23 = «해결(확인 범위 안) + 사람 결정(어디까지 솎을지)» · #24 = 해결 · #27·#28·#29·#44 신설 · #45·#46 = DeepSeek 수용/기각), §0 결론에 총괄 판단 3개 + 🔵 권고 + «부분 중복 제거» 표현, §1 에 곡선·대안표·`session`·MIS 비교 불가, §6 한계 문장 4개 추가 |
| PDF 생성기 2개 | 아래 §6 |

---

## 6. PDF 2개 재생성 + 전 쪽 눈 확인

### 6-1. `tools/make_apple_blueberry_review_pdf_260917.py` → `문서/260917_사과블루베리_검수_최종보고서.pdf` (**47쪽**)

- 새로 센 것(본문에 손으로 적은 장수 0 — 전부 원천 CSV 에서 그 자리에 셈):
  `gap_eye`·`curve_eye` 300+252쌍, 간격 열쇠 2개, 곡선표 17행, 간격 솎기 6대안, 20% 지점, `session` 수.
- **§3-4 «얼마나 솎을 것인가 — 멈춤점이 없습니다» 신설**: «왜 못 정하는가» 상자 ·
  간격별 곡선표(20% 미만 칸 굵게) · 간격 솎기 대안표 · 선택지 3개 · 🔵 «우리 권고» 상자 ·
  «①번을 고르실 때 같이 적어야 하는 한계» 상자(254쌍·약 70쌍).
- §3-1 에 «줄어든 경위» **687(22:22) → 549(00:22) → 547(01:33) → 429(03:07:53)** 한 단락.
- §2 «어떻게 검수했나» 에 **2단계 눈 판정 행**과 **간격별 곡선 측정 행** 추가.
- §3-1 판정자 상자에 **78.0% vs 60.0% / 11.9% vs 5.6%** 실측 추가.
- §3-3 에 **«어느 칸으로 폴드를 나눌 것인가»** 상자 신설(`session` 권고 · «확인된 관계 기준» 명시).
- §0 표에 **촬영 단위(`session`) 행**과 «얼마나 더 솎을지 = 교수님 결정 대기» 행.
- §5 질문 **9개로**(1번 = 얼마나 솎을까요 · 9번 = «동그랗게» 예시 + 0.46%).
- «탐욕이 최적보다 N장 더 뺐다» 계산을 **삭제**(2단계 구조라 비교 불가) — 주석으로 이유를 남김.
- 🟢 **전 47쪽을 `pdftoppm -r 60 -png` 로 직접 봤습니다**: 겹침 0 · 빈 쪽 0 · 낡은 숫자 0 ·
  글꼴 누락 경고 0(🔴 이모지가 Noto CJK 에 없어 네모로 나오던 것을 **생성기에서 전부 제거**).
  고친 잘림 1건: **표지 세 번째 줄이 좌우로 잘렸던 것**을 두 줄로 나눔(내가 길게 쓴 탓 — 재생성해 확인).
- 남은 흠(안 고침 · 공용 `pdfdoc.py` 문제): 전면 그림 쪽의 부제가 오른쪽에서 잘리고 **쪽번호가 한 칸 밀립니다**
  (예: 24쪽·30쪽 바닥글이 «23»). 열린 문제 #37.

### 6-2. `tools/make_daily_summary_pdf_260917.py` → `문서/260917_오늘작업_총정리.pdf` (**18쪽**)

- §7 숫자 전부 현행화 + **촬영 단위(`session`) 행** 신설 + «줄어든 경위» 4단계 +
  **«왜 «최종 429장» 을 우리가 못 정하는가» 상자 신설**(곡선 요약 · 선택지 3개 · `session` 분할 · 🔵 권고 ·
  «부분 중복 제거» 표현 · 출처별 보고) + 판정자 78 vs 60 + 옛 분할표 112·9 + 최종 숫자 사전 경로.
- §8 을 «사이클 1~3» → **«사이클 1~4»** 로: 사이클 4 행 신설(묶음 칸 **오른쪽 맨 위** · 의심 종류 한국어 ·
  **«후보 자리로 확대»** · 범례 접기 · 복숭아·포도 «세기» 감춤 · 되돌릴 것 0장이면 백업 안 만듦 ·
  🔴 **마스크 «수정본 되돌리기» 가 검수 판정을 통째로 지우던 결함 수정** · 썸네일 500 · **실서버 재시작 03:05**),
  확인 값(195/195 · 59/59 · 161/161 · API 506회 소실 0 · 회귀 11/11), **사이클 5 는 «진행 중»**.
- §9 «교수님께 **17건**» + 16번이 가장 급함 · 분할표는 `session` 단위 권고. §12 파일 목록에 판정서·숫자 사전 추가.
- 🟢 **전 18쪽을 직접 봤습니다**: 겹침 0 · 빈 쪽 0 · 낡은 숫자 0 · 글꼴 경고 0.

---

## 7. 낡은 숫자 잔존 검색 (`s2_stale.py`)

대상 4뿌리(`datasets_reviewed_260917` · `문서` · `semantic-segmentation/tools` · `260916_라벨링툴`),
`.md/.txt/.py/.json`, 숫자 687·1,124·549·1,113·547·454·452·314 + 파생 293·299·134·1,204·680·325·399·1,074.
«옛 값» 표시(첫 판·중간 판·1차 동결·옛 값·쓰지 말 것·시각 표기 등)가 같은 줄이나 앞뒤 2줄에 있으면 통과.

**결과: 205건이 걸렸고, 전수 분류하니 «현재형으로 남은 낡은 숫자» 는 0건입니다.**

| 분류 | 건수 | 판정 |
|---|---:|---|
| 앞 사이클 보고서 본문(`cycle_{1,2,3,4}/stage{1,2}*`) | 약 90 | **그 시점의 기록** — 시각·사이클 이름이 붙어 있고, 각 3단계 판정서 끝에 «후속» 단락을 붙여 현행을 가리키게 했습니다 |
| `cycle_3/stage3/{number_dict,freeze_report}.md` | 20 | 🆕 **머리말에 ⛔ «1차 동결판 기준 — 인용 금지» 상자를 넣어 해결** |
| 1·2차 검수 보고서 | 6 | 3차판정 머리말이 «①~③ 은 첫 판 기록» 이라고 이미 밝힘 |
| 내 새 문서에서 **명시적으로 라벨된** 값(«1단계 454» · «687에서 549 547 429 순» · «1,074» 등) | 6 | 의도된 표기 |
| 다른 주제의 같은 숫자(포도 best epoch 547 · MinneApple val 134 · «병합 의심 680개체» · MCP 314개 도구 · JSON 좌표·화소값) | 약 80 | **무관** |
| UI `cycle_4/stage3_final.md:10` 의 «제외 454» | 1 | 03:05 재시작 값으로 시각이 붙어 있어 필터를 통과했지만, **2분 차이로 낡은 표기**입니다 → 내 사이클 문서가 아니라 손대지 않고 **열린 문제 #44 로 3단계에 올립니다** |

---

## 8. 내가 가정한 것

1. **«남긴 사진» = manifest 의 `action != excluded_duplicate`** (keep + needs_human).
2. **촬영·프레임 파싱**은 `curve_and_stride.py` 와 같은 규칙(숫자 꼬리 앞까지가 촬영)을 썼습니다.
   §2-4 의 «안 본 쌍» 수는 이 규칙에 의존합니다. manifest 의 `session` 칸과 대조하면 사과 17개로 일치합니다.
3. **§2-4 의 «약 70쌍» 은 추정**입니다 — 간격별로 «안 본 쌍 수 × 그 간격의 곡선 비율» 을 더한 값이고,
   곡선 비율 자체에 판정자 효과 ±10~15%p 가 있으므로 ±20쌍으로 적었습니다.
4. **실서버는 GET 만** 썼습니다(`app/server.py:64~73` 의 서명 규칙으로 쿠키를 만들어 `/api/fruits` 1회).
5. **PDF 는 두 개 다 전 쪽**을 그림으로 봤습니다(47쪽 · 18쪽). 부록의 썸네일 격자는 테두리 색과
   파일명 라벨이 보이는지 위주로 확인했고, 사진 한 장 한 장의 «정말 거의 같은가» 는 다시 판정하지 않았습니다.
6. **총괄이 넣은 `session` 코드는 검증만 하고 손대지 않았습니다.** 데이터는 이미 그 코드로 다시 만들어져
   있었고(04:12:36), 내가 다시 세어 판정값이 안 바뀐 것을 확인했습니다. `--tool` 은 돌리지 않았습니다.

---

## 9. 3단계(Fable)가 판정할 것

1. **최종 장수 표기를 확정해 주십시오.** 지금 모든 문서가 «사과 429장(눈 검토 기반 부분 중복 제거)» +
   «권고 = 촬영 단위 분할 + `keep_G40_90` 294장» 으로 통일돼 있습니다. 이 **두 숫자를 나란히 두는 방식**이
   맞는지, 아니면 하나만 앞세울지.
2. **열린 문제 #27(범위 밖 안 본 쌍 254개 · 약 70쌍 추정)** 을 «한계로만 적을지», 아니면 4단계 눈 판정을
   더 돌릴지. (돌리면 429장이 다시 줄고, **세 번째 동결**이 됩니다 — 제 판단으로는 «한계로 적고 멈춤» 입니다.)
3. **열린 문제 #44** — UI 사이클 4 3단계 판정서의 «제외 454»(03:05 값). 그 사이클 문서라 제가 손대지
   않았습니다. 고칠지, «03:05 시점 기록» 이라고 한 줄만 붙일지.
4. **`cycle_3/stage3/number_dict.md`·`freeze_report.md` 에 제가 붙인 ⛔ 상자**가 맞는 처리인지
   (제 판단: 다른 문서들이 이 둘을 «정본 숫자 사전» 으로 가리키고 있었으므로 필요했습니다).
5. **README 의 «완화 76그룹(진짜 75)» 정정**(§2-6-B)을 그대로 둘지. 1차 탐지 47그룹도 함께 적어 놨습니다.
6. **촬영 단위 5-폴드의 사과 폴드 크기 불균형(84~95 · ±10%)** 을 받아들일지, 아니면
   `split_group` 기준(110·110·109·109·109)으로 갈지. 제 권고는 **`session` 우선**(누수 안전 > 크기 균형)입니다.
7. **열린 문제 #13(포도 3장)** 을 이 판정서에 계속 실을지 — 실서버가 아직 `exclude 3` 으로 보고합니다.
8. **판정서의 열린 문제 46건 분류**가 맞는지(해결 12 · 미해결 18 · 사람 결정 15 · 해결+사람결정 1).
9. **`pdfdoc.py` 의 쪽번호 밀림·부제 잘림**(#37)을 이번에 고칠지. 공용 도구라 손대지 않았습니다.
10. **작업기록.md 갱신** — 저는 금지되어 있어 0줄입니다. 3단계나 총괄이 «사과 429 · 블루베리 1,114 ·
    `session` 칸 · 교수님확인 17건» 으로 「1. 현재 상태」·「3. 다음 할 일」을 갱신해야 합니다.

---

## 10. 산출물

| 무엇 | 어디 |
|---|---|
| 이 보고서 | `cycles/260917_ab/cycle_5/stage2_review.md` |
| **최종 숫자 사전** | `cycles/260917_ab/cycle_5/stage2/number_dict_final.md` |
| 내가 짠 스크립트 7개 | `cycles/260917_ab/cycle_5/stage2/scripts/s2_*.py` |
| 계산 결과 | `cycles/260917_ab/cycle_5/stage2/logs/s2_*.json` |
| 고친 내역 전문 | `cycles/260917_ab/cycle_5/stage2/edits.diff` (1,752줄 · 파일 16개) |
| 백업 | 각 파일 옆 `_backup_260918_freeze2_<파일명>` (16개) |
