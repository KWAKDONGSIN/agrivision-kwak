작성: 2026-09-18

# 사과·블루베리 5회 재검수 — 사이클 4(재현성) 3단계 최종 판정 (Fable 5.1)

> 🔴 **본문의 장수·제외 숫자는 판정하던 그 시각의 값입니다.** 최종(동결) 숫자는 이 문서 맨 아래
> «2026-09-18 01:33 후속» 단락과 `cycles/260917_ab/cycle_3/stage3/number_dict.md` 에 있습니다
> (사과 1,001→**547**·제외 **454** / 블루베리 1,195→**1,114**·제외 **81**).

읽은 것: `stage1_report.md`(v1 대상) · `stage2_review.md`(v2 대상, 8/8 합격) 요지.

## ① 재현성 — 합격
- v2 의 제외·flag 명단 스크립트는 2회 실행·현행 파일 **바이트 동일**, 2단계가 설명만 보고 다시 구현해도 **집합까지 일치**(사과 452·51, 블루베리 82·4). 빌드는 3회 실행 md5 동일이고, 2단계의 00:51 재현본과 내가 00:58 에 돌린 manifest 가 바이트 동일(다른 실행자·다른 시각). PDF 는 생성 시각 4바이트만 다름. `split_group` 은 입력 순서를 10번 섞어도 같은 묶음.
- 1단계의 흠 가운데 v2 가 해소한 것: 제외 명단 스크립트 부재 · «2차 대표 먼저» 규칙(최적 대비 16장 → **6장** 초과) · 이름 검사 · 멱등성.

## ② 2단계의 새 지적에 대한 판정
| # | 지적 | 판정 |
|---|---|---|
| 1 | 깨끗한 폴더에서 v2 를 못 만듦(v1 manifest 에서 rule_pending 을 읽음) | **고침**(01:20): `round3_final/v2/rule_pending_v1_{apple,blueberry}.csv`(25·244행)를 독립 입력으로 떼어 두고 스크립트가 그것을 읽음. v1 보존은 첫 판이 있을 때만 |
| 2 | `split_group` 빈칸 함정(관계 없는 사진 사과 87·블루베리 965장이 빈칸 → groupby 하면 한 폴드로 몰림) | **고침**: 관계 없는 사진은 혼자인 묶음 번호(`as0001`…). 남긴 사진 기준 묶음 수 사과 224(그중 2장 이상 70)·블루베리 1,055(52) |
| 3 | 판정자 간 일치도를 잰 데이터가 0 (세 조의 near_identical 비율이 모든 점수 구간에서 1조≥2조≥3조) | **측정합니다**: 세 조에서 20쌍씩 뽑은 사과 60쌍 + 블루베리 29쌍 전부 = 89쌍(`round3_final/v2/interrater_pairs.csv`)을 새 판정자 A·B 가 서로·옛 판정을 못 본 채 판정 중. 결과는 사이클 5 에서 일치율·카파로 보고하고, **블루베리 11장 제외는 세 판정(기존+A+B)의 다수결로 다시 확정** |
| 4 | 549 vs 최대 독립 집합 555(6장 차) | **549 유지.** 6장은 판정자 흔들림(±16장)보다 작고, 결정적이고 설명하기 쉬운 규칙(«flag 먼저 → 세션·프레임 순»)이 낫다. README 에 «최대보다 6장 더 뺌» 을 적음 |
| 5 | 문서 세 벌이 서로 다름(manifest ↔ 3차판정 ④ ↔ PDF 본문, README 안 298↔322) | **문서 동결 절차**: 판정자 일치도 결과까지 반영한 뒤 README·3차판정 ④·PDF 생성기 문장을 한 번에 고치고 PDF 를 **한 번만** 다시 만든다(사이클 3 의 3단계에서) |
| 6 | `duplicates.json` 의 method 가 «dHash…», docstring «아직 안 돌렸습니다» | **고침**(method = «사람 눈 판정 — 후보는 dHash·프레임 인접·AKAZE 겹침») |
| 7 | `--tool` 은 잠금 없이 공용 status.json 을 씀 | 인정. 사람이 쓰기 시작한 뒤에는 돌리지 말 것 — docstring 과 README 에 경고(사이클 3 의 3단계에서). 이번 두 번의 실행은 새벽·저장 0건 확인 뒤 |

## ③ 논문·보고에 적을 한계
사과 제외의 근거 1,197 관계는 100% 사람 눈 판정(=AI 판정자)이고, 판정자 기준 차이가 남긴 장수를 약 ±16장 흔든다. 자동 점수만으로는 자를 수 없었다(0.9 이상에서도 60~80%). 블루베리 1·2·3번 카메라의 제외는 재판정 뒤집힘이 56% 였던 판정에 기대므로 다수결로 다시 확인한다.

---

## 2026-09-18 01:33 후속 — ②-3·②-5 의 처리 결과 (동결)

- **②-3(판정자 간 일치도)**: 측정 끝. 89쌍(사과 60·블루베리 29)을 O·A·B 가 독립 판정 →
  사과 일치 **78~88%**(κ 0.51~0.72) · 블루베리 **59~79%**(κ 0.17~0.58) · 셋 다 일치 **60/89(67%)**.
  `round3_final/v2/interrater_agreement.md`. 다수결로 사과 9쌍·블루베리 1쌍이 뒤집혔고,
  **블루베리 3번 카메라 제외는 11쌍·10장으로 확정**됐습니다(전에는 12쌍·11장).
- **②-5(문서 동결)**: 이 다수결을 넣어 데이터셋을 마지막으로 다시 만든 뒤(01:33),
  README·3차판정 ④·스크립트 docstring·교수님 질문·카톡·PDF 생성기 2개를 **한 번에** 고치고 PDF 를 **한 번만** 다시 만들었습니다
  (`cycle_3/stage3/freeze_report.md`).
- **②-4(549 유지)의 값이 바뀌었습니다.** 다수결 반영으로 제외가 452 → **454**, 남는 장수는 549 → **547** 입니다.
  최대 독립 집합도 다시 풀어 **448**(두 방법 일치) — 초과 제외는 여전히 **6장**이고, 판단(«6장 때문에 규칙을 바꾸지 않는다»)은 그대로입니다.
- **②-7(`--tool`)**: 01:33 에 한 번 더 실행해 툴을 동결판에 맞췄습니다(사과 exclude 454·flag 51 / 블루베리 81·4).
  README 와 스크립트 docstring 에 «사람이 툴을 쓰기 시작한 뒤에는 돌리지 말 것» 경고를 넣었습니다.
- 최종 숫자 한 벌: `cycle_3/stage3/number_dict.md`.

---

## 2026-09-18 03:07:53 후속 — ②-4(«549 유지»)의 근거가 바뀌었습니다

- ②-4 는 «탐욕이 최대 독립 집합보다 6장 더 뺀다, 그래도 규칙을 바꾸지 않는다» 였습니다.
  1차 동결(01:33)에서 그 값이 454 vs 448(초과 6장)이었고, 사이클 5 의 2단계가
  내 정확 풀이로 **정점 820 · 간선 1,204 · 최대 독립 집합 372 · 최소 제외 448** 을 재확인했습니다.
- 그 뒤 **제외가 2단계 구조가 되어 이 비교 자체가 성립하지 않습니다.** 1단계(454장)를 고정한 채
  남긴 사진 안에서만 118장을 더 뺀 것이 최종 동결판(제외 **572** · 남김 **429**)입니다.
  전체 그래프의 최대 독립 집합은 376(정점 870·간선 1,411, 최소 제외 494)이지만
  1단계를 다시 풀면 남는 집합이 바뀌어 «안 본 쌍» 이 새로 생기므로 그 값에는 도달할 수 없습니다.
  **그래서 앞으로 «초과 6장»·«초과 78장» 같은 문장은 쓰지 않습니다**(상한만 말할 수 있습니다 — 최대 507장).
- ②-3(판정자 간 일치도)에 **덧붙일 실측**이 생겼습니다. 2단계·곡선 눈 판정에서 같은 표본을
  반씩 나눠 본 두 판정자의 «거의 같은 사진» 비율이 `gap_eye` **78.0% vs 60.0%** ·
  `curve_eye` **11.9% vs 5.6%** 였습니다(두 part 는 겹치는 쌍 0 · 간격 구성은 비슷).
  ③ 의 한계 문장(«±16장»)은 **그보다 크게 잡아야 합니다.**
- ②-7(`--tool`): 2026-09-18 **03:07:53** 에 한 번 더 실행했습니다(사과 exclude **572**·flag 51 /
  블루베리 81·4. 실서버 `/api/fruits` 03:45 GET 으로 확인). 경고 문구는 그대로 유효합니다.
- ②-6 의 재현성 잔흠(`overlap_orb.py` seed·`os.listdir` 순서·`pairsim.py` 절대경로·232쌍 스크립트 부재)은
  이번에도 고치지 않았습니다. 새 스크립트 `curve_and_stride.py` 는 seed 를 쓰지 않는 결정적 계산입니다.
