작성: 2026-09-17

> 🔴 **시점 문서 (2026-09-18 사이클 3 추가).** 이 보고서는 **2026-09-17 «첫 판(v1)» 데이터셋을 대상으로**
> 쓴 것입니다. 본문의 사과 **687**·블루베리 **1,124**, 제외 **314**·**71**, flag **68**·**6**,
> `rule_pending` **25**·**244**, 대표 **190**·**33** 그룹, 정점 520·간선 783 은 **그때의 값**이고
> **지금은 다릅니다.** 본문 숫자는 판정 근거로서 그대로 둡니다.
>
> **현행(v2, 2026-09-18 01:04 재빌드)**: 사과 1,001 → **549**(제외 **452** · 대표 302그룹 · flag **51** ·
> `rule_pending` **39**(폴더에 남은 사진만 세면 **22**) · `overlap_neighbors` **298**) / 블루베리 1,195 → **1,113**
> (제외 **82** = 4번 카메라 71 + 3번 카메라 11 · 대표 43그룹 · flag **4** · `rule_pending` **244**(남은 사진 **218**) ·
> `overlap_neighbors` **110**). 2026-09-18 00:50 에 `overlap_neighbors` 는 «폴더에 남아 있는 이웃» 만 적도록 고쳐졌고
> (뺀 사진 행은 빈칸), 분할용 **`split_group`** 칸이 새로 붙었습니다(01:04 재빌드 기준 사과 **224**개 · 블루베리 **1,055**개 —
> 그중 사진이 2장 이상인 묶음은 137·90개이고, 관계가 없는 사진은 `as####`·`bs####` 로 혼자 한 묶음입니다).
> 사과 간선 **1,197** · 정점 **821**. 원천은 `kds0206/datasets_reviewed_260917/{apple,blueberry}/manifest.csv`,
> 생성은 `inspect/260917_apple_blueberry/round3_final/v2/build_exclusion_v2.py` →
> `final/build_reviewed_dataset_260917_v2.py` 입니다.
> ✅ **툴 `data/` 도 2026-09-18 00:58 에 v2 로 반영됐습니다**(사과 exclude **452**·flag **51** / 블루베리 **82**·**4**,
> «거의 같은 사진» 묶음 302·43). 그 전까지는 314·68 / 71·6 이었습니다.

# 사과·블루베리 재검수 사이클 1 · 2단계 검수 보고서 (Opus 5)

1단계 보고서 `stage1_report.md` 의 주장을 **믿지 않고 처음부터 다시** 셌습니다.
1단계 스크립트는 하나도 import·복사하지 않았고, 열어 본 것은 1단계의 *결과 CSV* 뿐입니다
(`residual_dup_apple*.csv`, `flag_lost_by_exclusion.csv` — 대조용). 지표도 일부러 다른 것을 썼습니다.

산출물: `cycles/260917_ab/cycle_1/stage2/` (스크립트 8개 · 로그·CSV 25개 · 눈으로 본 시트 **95장**)
파이썬 `/home/kds0206/.conda/envs/kwak/bin/python` · GPU 미사용 · 새 패키지 설치 없음 · 삭제 없음.
⛔ 데이터셋·원본·툴 `data/`·`build_reviewed_dataset_260917.py`·PDF·README·`작업기록.md` **한 글자도 안 고쳤습니다**(제안만).

---

## 0. 합격 기준 (실행 전에 정한 것)

| # | 무엇 | 합격선 | 결과 |
|---|---|---|---|
| A | 1단계의 «파일 무결성 완벽» | md5 전수 3,622 다름 0 · 폴더↔manifest 차 0 · 깨진 PNG 0 | ✅ 41항목 전부 PASS |
| B | 1단계의 «독립 집합 성질» | 출처 CSV 에서 간선을 새로 만들어 정점 520·간선 783 재현, 남긴 것끼리 간선 0 | ✅ 15항목 전부 PASS |
| C | 잔존 중복 지표 | 이미 «눈으로 확인된» 사과 중복 783쌍의 **95% 이상**을 잡고, 확실히 다른 나무 1,000쌍의 **2% 미만**만 잡을 것 | ✅ 규칙 `내부점비율≥0.4 & 겹침≥0.5` → 재현 **97.6%**(764/783), 오경보 **0.4%**(4/1,000) |
| D | 눈 판정 | 후보 쌍을 시트로 직접 보고 true/partial 판정, 1단계가 본 40쌍과의 일치율 기록 | ✅ 265쌍 눈으로 봄, 1단계 40쌍과 **40/40 일치** |
| E | 제안 파일 | 3단계가 그대로 돌릴 수 있는 CSV 4개(+변형 1개) | ✅ §7 |

> 지표의 «문턱 숫자»는 **이미 라벨이 붙어 있는 쌍**(3차까지 확정된 중복 783쌍 / 서로 다른 세션 1,000쌍)으로
> 눈금을 맞춘 뒤 고정했고, **그 다음에** 시트를 봤습니다. 내 눈 판정으로 문턱을 되맞추지 않았습니다.

---

## 1. 1단계 주장별 재현 결과

### 1-1. 파일 무결성 — ✅ 전부 재현 (`logs/s2_integrity.log`, 41/41 PASS)

| 1단계 주장 | 2단계 실측 | 판정 |
|---|---|---|
| 이미지·마스크 3,622개가 원본과 md5 동일 | 3,622개 전수 md5 대조, 다름 **0** | ✅ |
| manifest 1,001/1,195행, action 분포 619·68·314 / 1,118·6·71 | 완전 일치 | ✅ |
| 폴더 == keep+needs_human (687·1,124), excluded 잔존 0 | 차집합 0 / 0 | ✅ |
| 깨진 PNG 0 · 0바이트 0 · 이미지↔마스크 크기 1,811쌍 일치 | 전부 일치 | ✅ |
| 사과 마스크 = 인스턴스 번호, 고유값 최대 124, vmax==255 인 장 0 | mode L 687/687 · 최대 고유값 124 · vmax255 **0장** · 인스턴스 합계 **28,534** | ✅ |
| 블루베리 마스크 값 ⊆ {0,255} | 1,124/1,124 · R=G=B 전수 성립 | ✅ |
| build_summary.json 숫자 == 실측 | 전부 일치 | ✅ |
| 제외본 note 의 «대표» 가 전부 남은 사진 | 385건 전부 성립 | ✅ (1단계는 이 항목을 표에 안 적었으나 성립) |
| 툴 `data/*/status.json` == manifest | 사과 exclude 314·flag 68, 블루베리 71·6, 차집합 0 / `duplicates.json` 190그룹·504장, 33그룹·104장 | ✅ |

### 1-2. 독립 집합 — ✅ 전부 재현 (`logs/s2_indep.log`, 15/15 PASS)

출처 CSV 3개에서 간선을 **내 코드로 다시 전개**(그룹=클릭):
`round2_apple_dup_verdicts`(true 71그룹) + `round2_apple_dupnew_verdicts`(true 75그룹, partial 1 제외)
+ `frameadj208_verdicts`(208쌍 전부 true) → **정점 520 · 간선 783** (1단계와 동일).

- ★ 남긴 687장끼리 «확인된 중복» 간선 **0** ✅
- ★ 뺀 314장마다 남긴 이웃 있음(고아 0), (제외본,대표) 쌍에 실제 간선 존재 ✅
- manifest 제외집합 == `apple_final_groups_independent.csv` (314==314, 190그룹), note 대표 불일치 0 ✅
- 사슬로 이었다면 94성분 → 426장 제거 (1단계 기록과 일치) ✅
- 블루베리 제외 71장 전부 Camera 4, 영상 33개, 대표 33장 전원 잔존 ✅

### 1-3. flag 12장 — ✅ 숫자까지 똑같이 재현 (`logs/s2_flag_lost.log`)

`round2_apple_missing_verdicts.csv` 의 `verdict==clean_fruit` 후보 **99개 / 사진 78장** →
그중 `excluded_duplicate` 가 된 사진 **12장**, 잃은 후보 **16개**, 그 12장의 대표 12장 중
`keep` **10장** · `needs_human` 2장. 1단계 명단과 12/12 동일.
추가로 찾은 것: `needs_human` 68장 중 **2장**(`dataset2_back_840`, `dataset2_back_900`)은
clean_fruit 후보가 아니라 «사진 전체 저라벨» 로 붙은 flag 라 후보 좌표가 없습니다(68 = 66 + 2).

### 1-4. rule_pending 구성 — ✅ (`logs/s2_integrity.log`)

사과 25행(폴더에 남은 것 15) · 블루베리 244행(남은 것 230).
블루베리 244 = «같은 포기가 겹쳐 보이는» 93 + «마른 꽃부리» 98 + candidate_unclear 등 53.

### 1-5. 문서 지적 2건 — ✅ 둘 다 1단계가 맞습니다 (`logs/s2_doc_check.log`)

- «같은 포기 50쌍»: `round2_dup_groups.csv` 의 Camera 1·2·3 = 그룹 **43** · 사진 **93** · 클릭 쌍 **57**,
  93−43 = **50**. → 1단계 말대로 «50쌍» 이 아니라 **«50장(43그룹)»** 이 맞는 표현입니다.
  manifest 의 «같은 포기가 겹쳐» 표시도 정확히 93장이고 그중 제외된 것은 0장입니다.
- 3차판정 15행의 경로: 적힌 `round3_apple_frameadj/apple_final_dup_groups.csv` 는 **94그룹·426장 제외**,
  실제 실행된 `round3_final/apple_final_groups_independent.csv` 는 **190그룹·314장 제외**.
  → 1단계 지적대로 «가리키는 파일이 다릅니다». **2단계 판단: 고쳐야 합니다**(§6).
- 경로 17개 전수 확인: 전부 실존 ✅ (README·3차판정의 백틱 경로를 다시 훑음, `logs/s2_paths.log`).

### 1-6. 1단계가 고친 1줄 — ⚠️ 사실은 맞지만 숫자가 오해를 부름

고친 문장 «**실행 결과 flag 는 66장**» 은 «78장 중 남은 것» 이라는 뜻에서는 정확합니다(실측 66).
그런데 manifest·README·PDF 의 `needs_human` 은 **68장**입니다(66 + 저라벨 2장).
같은 문서 안에서 66과 68이 같이 돌아다니면 다음 사람이 틀린 쪽을 옮겨 적습니다.
→ «flag 는 66장» 대신 «**이 항목으로 flag 된 것은 66장**(manifest 의 needs_human 68장 중)» 으로
고칠 것을 제안합니다. 나머지(12장·후보 16개·대표 10장) 는 전부 내 실측과 일치합니다.

---

## 2. 🔴 사과 잔존 중복 — 1단계의 «58쌍» 은 심하게 낮은 하한이었습니다

### 2-1. 무엇을 어떻게 셌나 (1단계와 다른 경로)

| | 1단계 | 2단계 |
|---|---|---|
| 후보 | 전체 쌍 썸네일 상관 → **상위 400쌍만** | **전수**: 같은 세션 프레임차 5~60 + dataset1~4 는 같은 영상 안 모든 쌍 → 사과 **8,029쌍** · 블루베리 **5,085쌍** |
| 지표 | ORB(2000) + RANSAC, 내부점비율×겹침 | **AKAZE** + RANSAC 호모그래피(내부점비율 × 호모그래피 겹침률) + **위상상관 이동량 겹침×ZNCC** 를 보조로 병기 |
| 문턱 | 상관 0.82 에서 자름, score≥0.5 | 이미 확정된 783쌍/확실한 음성 1,000쌍으로 눈금을 맞춰 **내부점비율≥0.4 & 겹침≥0.5** |

눈금 맞추기 결과(`logs/s2_calib.log`) — 위상상관만으로는 과수원 질감 때문에 갈리지 않았고(확정 중복의 score 중앙값 0.514 vs 다른 나무 0.285), AKAZE 호모그래피가 깨끗하게 갈립니다:

| | 확정 중복 783쌍 | 다른 세션 500쌍 | 같은 세션 300프레임 이상 500쌍 |
|---|---:|---:|---:|
| `내부점비율` 중앙값 | 0.746 | 0.222 | 0.227 |
| `호모그래피 겹침` 중앙값 | 0.946 | 0.003 | 0.005 |
| **규칙 통과** | **764 (97.6%)** | **1 (0.2%)** | **3 (0.6%)** |

### 2-2. 결과

| | 확정 쌍 | 2·3차가 **한 번도 안 본** 쌍 | 눈으로 본 쌍 |
|---|---:|---:|---|
| 사과 | **1,101** | **1,098** | 118쌍(시트 45장) |
| 블루베리 | **74** | **51** | 74쌍 전부(시트 22장, 경계 12쌍 포함) |

- 1단계의 58쌍 중 **57쌍이 내 확정 집합 안**에 있습니다(교차 확인). 빠진 1쌍
  (`20150921_132245_image976↔1101`)은 프레임차 125라 내 창(5~60) **밖**이었습니다 → 내 1,101도 하한입니다.
- 실제로 프레임차 61~400 까지 넓혀 다시 돌려 보니(`logs/s2_residual_akaze_far.log`)
  **146쌍이 더** 나왔고, 합치면 추가 제거가 371 → **384장**이 됩니다(13장 차이).
- 프레임차 분포: 30프레임 **306쌍** · 60프레임 **213쌍** · 10프레임 83 · 15프레임 70 · 20프레임 61 · 5프레임 3.
  → 3차 보충 검수가 본 208쌍은 **전부 MinneApple 프레임차 5** 였습니다(내가 세어 확인).
  **dataset1~4 의 이웃 프레임(30프레임 간격)은 한 쌍도 시트로 본 적이 없습니다.** 여기가 제일 큰 구멍입니다.

### 2-3. 눈 판정 (시트 45+22장, 한 시트 4쌍, 긴 변 1,538px)

시트에는 **점수도 출처도 안 적었고**, 띠별 층화 표본 + 1단계가 본 40쌍을 **섞어서 무작위 순서**로 봤습니다.

| 점수 띠 | 본 쌍 | 진짜 중복 | 겹침 중앙값 |
|---|---:|---:|---:|
| ≥0.6 | 26 | 25 | 0.927 |
| 0.5–0.6 | 34 | 33 | 0.926 |
| 0.4–0.5 | 27 | 26 | 0.843 |
| 0.3–0.4 | 28 | 27 | 0.799 |
| 0.25–0.3 | 16 | 16 | 0.698 |
| 0.2–0.25 | 16 | 15 | 0.727 |
| 0.15–0.2 | 12 | 11 | 0.779 |
| 0.1–0.15 | 12 | 12 | 0.694 |
| **0.05–0.1** | 8 | 8 | **0.392 ← 여기서 «화면 절반» 이 무너집니다** |

- 사과 179쌍 중 **173쌍이 진짜 중복**(96.6%), 6쌍은 partial
  (`174730_image266↔286/301`, `image176↔221`, `image276↔301` = 같은 나무를 다른 거리에서 /
  `dataset2_back_930↔990`, `dataset2_front_270↔1110` = 겹침이 딱 절반 근처).
- **겹침이 절반 아래로 떨어지는 지점 = 점수 0.1 부근**입니다. 0.05–0.1 띠는 눈에는 «같은 줄» 로 보이지만
  호모그래피 겹침 중앙값이 0.392 라 «화면 절반 이상» 기준에는 못 미칩니다 → 확정에서 뺐습니다.
- **1단계가 본 40쌍 블라인드 재판정: 40/40 true_duplicate → 일치율 100%.**

### 2-4. 추가로 빠지는 장수 (`logs/proposal_extra_drop*.csv`)

기존 제외 314/71장은 **그대로 두고**, 남은 사진끼리만 새 간선으로 탐욕적 독립집합
(남길 우선순위 = 2·3차가 고른 대표 → 사람확인 사진 → 선명도(라플라시안 분산)).

| | 지금 | 추가 제거 | 남는 장 |
|---|---:|---:|---:|
| 사과 (기본) | 687 | **371** | **316** |
| 사과 (사람확인 사진 보호판, 권장) | 687 | **378** | **309** |
| 블루베리 | 1,124 | **62** | **1,062** |

문턱을 달리 잡으면 (`logs/s2_make_proposals_dup.log`):

| 사과 문턱 | 쌍 | 추가 제거 | 남는 장 |
|---|---:|---:|---:|
| 겹침≥0.5 & 내부점≥0.4 (권장) | 1,101 | 371 | 316 |
| 겹침≥0.5 & 내부점≥0.5 | 699 | 275 | 412 |
| 겹침≥0.7 & 내부점≥0.6 | 315 | 157 | 530 |
| 겹침≥0.8 & 내부점≥0.6 (가장 보수적) | 236 | 136 | 551 |

> ⚠️ **기본판을 그대로 돌리면 flag 31장이 또 «중복 제외» 에 먹힙니다** — 1단계가 찾은 §4-2 와 똑같은 사고가
> 반복됩니다. 그래서 «사람확인 사진 보호판»(`proposal_extra_drop_protectflag.csv`)을 따로 만들었습니다.
> 보호판은 7장을 더 빼는 대신 사라지는 «flag 유지» 사진이 16장 → **8장**으로 줄고, 그 8장도
> 상대가 역시 사람확인 사진이라 구조적으로 둘 중 하나는 빠질 수밖에 없는 경우입니다(그 8장은 §4의 좌표 이전 대상).

### 2-5. 이게 왜 중요한가

- README 는 «남긴 사진끼리는 확인된 중복 관계가 없게 골랐습니다» 라고 적었습니다. 이 문장은
  **«확인된» 이라는 단어 때문에 기술적으로만 참**입니다 — 실제로는 남은 687장 안에
  화면 절반 이상 겹치는 쌍이 **1,101개** 있습니다. 읽는 사람은 «중복이 없다» 로 받아들입니다.
- 분할표를 다시 만들 때 이 1,101쌍이 폴드를 넘으면 그대로 누수입니다.
  **분할표 재생성 전에 반드시 반영해야 합니다.**

---

## 3. 블루베리 — Camera 4 는 깨끗, Camera 1·2·3 은 3차 판정을 다시 봐야 합니다

- **Camera 4**: 제외가 일어난 33개 영상뿐 아니라 **Camera 4 전체 57개 영상이 각각 정확히 1장씩만** 남았습니다
  (`logs/s2_indep.log`). «같은 영상 안에 대표가 2장 이상» 인 경우 **0건** ✅.
- **Camera 1·2·3**: 같은 영상 안 모든 쌍 5,085개를 전수 점수화 → 규칙 통과 **74쌍**
  (Camera 3 = 56 · Camera 2 = 13 · Camera 1 = 5). **74쌍 전부를 시트로 봤고 72쌍이 진짜 중복**
  (partial 3쌍 중 1쌍은 문턱 아래). 프레임차는 대부분 **60**(=파일 간격 1칸), 즉 **카메라가 거의 안 움직인 연속 프레임**입니다.
- **3차가 «빼지 않기로» 한 것과의 관계**: 3차는 `p000` 시트 한 장(마스크 IoU 0.02)을 보고
  «Camera 1·2·3 의 50쌍 = 같은 포기이지만 구도·배율이 크게 다름 → 거의 똑같은 이미지 아님» 이라고
  **50쌍 전체를 한꺼번에** 판정했습니다. 내가 쌍마다 재어 보니 그 클릭 쌍 57개 중 **23개**는
  화면이 절반 이상 겹칩니다(내 74쌍 중 23쌍이 여기에 해당). 나머지 **51쌍은 2·3차가 아예 본 적도 없는 쌍**입니다.
  → «같은 포기를 다른 구도로 찍은 것» 과 «거의 같은 프레임» 은 섞여 있고, 한 장의 시트로 50쌍을
  일괄 판정한 것은 근거가 약합니다. 내 74쌍은 **구도가 거의 같은 쪽**입니다(시트 `bb_00~21.jpg`).

---

## 4. 🟠 flag 가 중복 제외에 먹힌 12장 — 대표 사진 좌표까지 옮겨 놨습니다

`logs/proposal_flag_transfer.csv` · 시트 `sheets/flagmove_00~03.jpg` (위=뺀 사진 확대, 아래=대표 사진의 **옮긴 좌표** 확대)

- 후보 16개를 AKAZE 호모그래피로 대표 사진 좌표계에 다시 투영했습니다(내부점 249~1,534개로 전부 튼튼).
- 눈으로 확인: **15개는 대표 사진에도 같은 열매가 실제로 보입니다** (또렷 12 · 그늘이라 확대 필요 3).
  1개(`20150921_131833_image151` → 대표 `image146`)는 «원 사진 맨 위에서 잘린 사과» 라
  대표 화면 **밖**이어서 옮길 수 없습니다.
- flag 를 받아야 할 대표 사진 **11장**(그중 9장은 지금 `keep` → 새로 `needs_human` 이 됨,
  2장은 이미 `needs_human` 이라 후보만 합치면 됨). 적용하면 사과 flag 68 → **77장**.
- CSV 에 `rep_bbox` 를 넣어 두었으니 3단계는 그대로 manifest note 에 붙이면 됩니다
  (다만 카메라가 움직여 상자가 몇 픽셀 어긋날 수 있으니 note 에 «사람이 다시 잡을 것» 을 남기는 게 좋습니다).

---

## 5. flag 메모 품질 — 사과 68장 · 블루베리 6장 **전부** 봤습니다

시트 `sheets/afl_00~20.jpg`(사과 후보 83개) · `bfl_00.jpg`(블루베리 3개) · `nobox_00~01.jpg`(상자 없는 5장).
마스크 외곽선을 초록으로 덮어 «이미 라벨된 열매인지» 를 구분할 수 있게 만들었습니다.

### 5-1. 후보 상자 86개 분류

| 판정 | 사과(83) | 블루베리(3) | 뜻 |
|---|---:|---:|---|
| `real_missing` 진짜 라벨 안 된 열매 | **39 (47%)** | 1 | flag 유지 |
| `too_dark` 너무 어두워 판정 불가 | **19 (23%)** | 0 | 사람이 봐도 모름 |
| `rule_fallen_backrow` 낙과·뒷줄 | 12 (14%) | 0 | 규칙 문제 |
| `false` 오검출(마른 잎·풀·꽃) | 9 (11%) | 1 | 뺄 것 |
| `same_fruit_edge` 같은 열매의 안 칠해진 부분 | 4 (5%) | — | amodal 규칙 문제 |
| `rule_pending_dried_corolla` 마른 꽃부리 | — | 1 | 규칙 문제 |

1단계는 표본 29개로 «오검출 10% · 규칙 문제 21% · 판정 불가 28%» 라고 했는데,
**전수로는 오검출 11% · 규칙 문제 19% · 판정 불가 23%** 로 1단계 표본이 모집단을 잘 맞혔습니다 ✅.

### 5-2. 사진 단위 제안 (`logs/proposal_flag_reclass.csv`)

| | flag 유지 | rule_pending 으로 내림 | flag 해제(keep) |
|---|---:|---:|---:|
| 사과 68장 | **32** | **15** | **21** |
| 블루베리 6장 | **4** | **1** | **1** |

- «flag 해제» 21장은 후보가 전부 오검출이거나 너무 어두운 것뿐이라, 사람이 툴에서 열어 봐야
  아무것도 못 고칩니다. 지금 68장을 그대로 두면 **사람 시간의 약 3분의 1이 헛일**입니다.
- 블루베리 `Camera 3 Video (120)_241`: 상자 안이 **초록 어린 열매 + 마른 꽃부리**입니다
  → 1단계 지적대로 «규칙무관 진짜 누락 3» 이 아니라 **`rule_pending`(마른 꽃부리)** 이 맞습니다 ✅ 동의.
- 블루베리 `Camera 3 Video (13)_721`: 상자 안은 **아직 안 핀 흰 꽃** 입니다 → `false`, flag 해제.
- 블루베리 `Camera 3 Video (56)_121`: 라벨된 초록 열매들과 같은 종류의 **안 칠해진 초록 열매** → 타당 ✅.
- 상자 없는 5장(`dataset2_back_840/900`, `Camera 2 Video (7)_121/(7)_361/(9)_541`)은 전부 **타당한 flag** 입니다
  (앞 두 장 = 사진 전체 저라벨, 뒤 세 장 = GT 가 잎·줄기를 칠한 마스크 오류). 눈으로 확인했습니다.

> ⚠️ **§4 와 §5 는 순서가 있습니다**: 먼저 §4(좌표 이전)로 대표에 후보를 합치고, **그 다음** §5(재분류)를 적용해야
> 합니다. 반대로 하면 새로 옮겨 온 후보가 분류되지 않은 채 남습니다.
> (예: `20150919_174151_image196` 은 §5 에서 «rule_pending 으로 내림» 인데 §4 에서 새 후보를 받습니다.)

---

## 6. 마스크 형식 — README 에 꼭 한 줄 넣어야 합니다 (`logs/s2_maskmode.log`)

**두 폴더 4개 과일의 마스크가 전부 전수 검사되었고, 형식이 세 가지입니다:**

| 폴더 / 과일 | 장수 | PIL mode | `np.asarray` 모양 | 값 |
|---|---:|---|---|---|
| `260917/apple` | 687 | **L** | `(H,W)` | **열매 번호 1..123** (0/255 아님) |
| `260917/blueberry` | 1,124 | **RGB** | **`(H,W,3)`** | 0/255 (R=G=B 전수 성립) |
| `260916/grape` | 2,406 | L | `(H,W)` | 0/255 |
| `260916/peach` | 125 | L | `(H,W)` | 0/255 |

→ 1단계의 «블루베리 마스크는 RGB 3채널» 주장은 **전수로 참**입니다 ✅ (표본 60장이 아니라 1,124장 전부 확인).
그리고 **블루베리만 혼자 3채널**입니다. README 의 «(블루베리는 0/255)» 는 값만 말하고 모양을 안 말해서,
`(H,W)` 를 기대하는 학습 코드는 블루베리에서만 깨집니다.

**README 제안 문구(3단계가 그대로 붙이면 됩니다):**

```
- 마스크 읽는 법이 과일마다 다릅니다.
  · 사과   : mode L, (H,W). 화소값 = 열매 번호(1,2,3...). 이진 마스크가 필요하면 «0보다 크면 전경».
  · 블루베리: mode RGB, (H,W,3). 값은 0/255 이고 세 채널이 같습니다.
             → np.asarray(mask)[...,0] 로 한 채널만 쓰거나 Image.open(...).convert("L") 로 읽으세요.
  · 포도·복숭아(datasets_reviewed_260916): mode L, (H,W), 0/255.
```

---

## 7. 3단계(Fable)가 그대로 실행할 수 있는 제안 파일

전부 `cycles/260917_ab/cycle_1/stage2/logs/` 안에 있습니다.

| 파일 | 행수 | 칸 | 요약 숫자 |
|---|---:|---|---|
| `residual_true_pairs.csv` | **1,175** | `fruit,a,b,score,akaze_inl,akaze_ov,pc_score,frame_gap,sheet,eye_verdict,seen_by_round23` | 사과 1,101 + 블루베리 74. 2·3차가 본 적 없는 쌍 **1,149**. 눈으로 본 것 192쌍(진짜 185·부분 7) |
| `proposal_extra_drop.csv` | **433** | `fruit,stem,keep_stem,reason,was_flag,sharp_drop,sharp_keep` | 사과 371 + 블루베리 62 → 남는 **316 / 1,062**. ⚠️ flag 31장이 같이 빠짐 |
| `proposal_extra_drop_protectflag.csv` (**권장**) | **440** | 위 + `flag_proposal` | 사과 378 + 블루베리 62 → 남는 **309 / 1,062**. 사라지는 «flag 유지» 사진 **8장**(31→8) |
| `proposal_flag_transfer.csv` | **16** | `rep_stem,rep_action_now,from_stem,from_bbox,rep_bbox,visible_in_rep,sheet,note` | 옮길 수 있는 후보 **15/16**, 대표 사진 **11장**(새로 flag 되는 것 9장) → 사과 flag 68 → **77** |
| `proposal_flag_reclass.csv` | **74** | `fruit,stem,n_box,classes,proposal,reason,sheet,boxes` | 사과 32 유지 / 15 → rule_pending / 21 해제, 블루베리 4 / 1 / 1 |

**적용 순서**: ① `proposal_flag_transfer` → ② `proposal_flag_reclass` → ③ `proposal_extra_drop_protectflag`
→ ④ 분할표 재생성. ③ 을 먼저 하면 옮길 원본이 사라집니다.

---

## 8. 시험한 것 / 안 한 것

| 시험한 것 | 어떻게 |
|---|---|
| 파일 무결성 3,622개 | md5 전수 + PIL 디코딩 + 마스크 고유값 통계 (`s2_integrity.py`) |
| 독립 집합 성질 | 출처 CSV 3개에서 간선 재전개(정점 520·간선 783) (`s2_indep.py`) |
| 잔존 중복 | 후보 13,114쌍 전수 AKAZE 호모그래피 + 위상상관, 눈금 1,783쌍으로 맞춤 (`s2_calib.py`, `s2_residual_akaze.py`) |
| 프레임차 61~400 (추가) | MinneApple 10,769쌍 재스캔 → 146쌍 더 나옴 (`s2_residual_akaze_far.py`) |
| 눈 판정 | 쌍 265개(시트 67장) + flag 후보 86개(시트 22장) + 좌표 이전 16개(시트 4장) = **시트 95장** |
| 마스크 형식 | 4개 과일 4,342장 전수 mode·채널·값 |
| 문서 숫자·경로 | 백틱 경로 17개 존재 확인, 50쌍·426장 숫자 재계산 |
| 툴 상태 | `status.json`·`duplicates.json` == manifest |

| 안 한 것 | 왜 |
|---|---|
| **다른 세션·다른 영상 사이의 중복** | 후보 범위를 «같은 세션/같은 영상» 으로 한정했습니다. 같은 나무를 다른 촬영에서 또 찍었다면 못 잡습니다 |
| 블루베리 프레임차 400 초과 전수 | 블루베리는 같은 영상 안 **모든 쌍**을 봤으므로 이 구멍은 없습니다(사과 MinneApple 만 창이 있습니다) |
| 마스크 품질 자체(amodal/modal, 구멍, 병합) | 사이클 1 의 범위가 «데이터셋 무결성» 이라 손대지 않았습니다. 교수님 결정 대기 항목입니다 |
| 제외된 314/71장의 «잘못 뺀 것» 재검 | 1단계가 40쌍 육안 + 385쌍 ORB 전수로 확인했고, 나는 독립집합 성질만 재검했습니다 |
| PDF 본문 34쪽 | 글자 인코딩이 깨져 텍스트 추출이 안 되고, 어차피 데이터셋을 다시 만들면 재생성 대상입니다 |
| 분할표 재생성·누수 재계산 | 데이터셋 장수가 바뀌는 제안 단계라 의미가 없습니다. ③ 적용 뒤 4단계 몫 |
| flag 후보의 «정답 마스크» 작성 | 인스턴스 번호를 새로 붙여야 하므로 사람 몫(3차 판정 유지) |

---

## 9. 가정한 것

1. «같다» 의 기준은 `manifest.csv`. 툴 `status.json` 과 완전히 일치함을 따로 확인했습니다.
2. 그룹 판정은 그룹 안 모든 쌍을 중복으로 전개(클릭). 1단계와 같은 해석이고, 314장·190그룹이 맞아떨어져 검증됩니다.
3. «진짜 중복» = **같은 나무·같은 열매가 화면 절반 이상 겹침**(3차·보충 검수와 같은 기준).
   기계적으로는 `RANSAC 호모그래피 내부점비율 ≥ 0.4 & 호모그래피 겹침 ≥ 0.5` 로 읽었습니다.
4. 추가 제거의 «남길 쪽» 우선순위는 ① 2·3차가 고른 대표 ② 사람확인(flag) 사진 ③ 선명도.
   ②를 넣은 것은 지시에 없지만, 넣지 않으면 §4-2 와 똑같은 사고(flag 가 중복 제외에 먹힘)가 재발합니다.
   두 가지 판(기본 / 보호판)을 모두 냈으니 3단계가 고르면 됩니다.
5. 표본은 `random.Random(20260917)` 고정. 시트에는 점수·출처를 적지 않아 눈 판정이 숫자에 끌려가지 않게 했습니다.
6. 1단계 40쌍의 «블라인드» 는 완전하지 않습니다 — 1단계가 «40/40 진짜» 라고 적은 것을 이미 읽은 상태였고,
   다만 **어느 시트의 어느 쌍인지는 모르는 채** 다른 139쌍과 섞어서 봤습니다.
7. 좌표 이전은 원본 해상도 기준 호모그래피로 계산했습니다. 카메라가 움직였으므로 몇 픽셀 오차가 있습니다.
8. GPU·모델 추론 없음. 전부 CPU 파일 비교·화소 연산(최장 작업 6분 40초).

---

## 10. 3단계에 넘기는 판단거리

1. **문턱을 어디로 할 것인가** — 겹침 0.5/내부점 0.4 면 사과가 687 → 309장으로 줄어듭니다.
   «남는 장수» 와 «누수 없음» 중 무엇을 우선할지는 교수님 결정 사항일 수 있습니다(§2-4 표).
2. **README 문구** — «남긴 사진끼리는 확인된 중복 관계가 없게 골랐습니다» 는 사실이지만 오해를 부릅니다.
   «3차까지 **눈으로 본** 중복 관계는 없습니다. 아직 안 본 쌍에는 남아 있습니다» 로 바꿀 것을 제안합니다.
3. **블루베리 Camera 1·2·3 74쌍** — 3차가 «빼지 않기로» 한 판정의 근거(시트 한 장)가 약합니다.
   교수님께 드리는 질문 ④를 «50장(43그룹)» 이 아니라 «겹침 절반 이상인 74쌍» 으로 다시 여쭐 것을 제안합니다.
4. **3차판정 15행의 파일 경로** — 실제로 쓴 `round3_final/apple_final_groups_independent.csv` 로 고치는 게 맞습니다
   (지금 적힌 파일로 세면 426장이 나와 보고서의 314장과 안 맞습니다).
5. **«flag 66장» 표현** — manifest 의 68장과 헷갈립니다(§1-6).
