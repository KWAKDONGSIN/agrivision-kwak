작성: 2026-09-17

# 사과·블루베리 5회 재검수 — 사이클 1(데이터셋 무결성) 3단계 최종 판정 (Fable 5.1)

> 🔴 **본문의 장수·제외 숫자는 판정하던 그 시각의 값입니다.** 최종(동결) 숫자는 이 문서 맨 아래
> «2026-09-18 01:33 후속» 단락과 `cycles/260917_ab/cycle_3/stage3/number_dict.md` 에 있습니다
> (사과 1,001→**547**·제외 **454** / 블루베리 1,195→**1,114**·제외 **81**).

읽은 것: `stage1_report.md` · `stage2_review.md` 요지 · `stage2/logs/residual_true_pairs.csv`(1,175쌍)의 겹침 분포를 내가 다시 계산 · 내가 직접 연 시트 `stage2/sheets/ap_11.jpg`(겹침 0.5~0.8 대) · `ap_31.jpg`(0.8~0.9 대).

## ① 무결성 — 합격
두 단계가 서로 다른 코드로 3,622파일 md5 전수 동일 · manifest↔폴더 차 0 · 사과 번호 유지 · 독립 집합 성질(정점 520·간선 783) · 블루베리 15장 버그 잔재 0 을 재현. 뺀 사진 40쌍 눈 확인 오제외 0(2단계 블라인드 재판정 40/40).

## ② 🔴 잔존 중복 — 기준을 다시 세웁니다 (이번 사이클의 핵심 판정)
2단계는 «화면 절반 이상 겹침» 기준으로 남긴 사진 안에서 사과 1,101쌍·블루베리 74쌍을 더 찾았고, 전부 빼면 사과 1,001→**309장**이 됩니다. **그대로 채택하지 않습니다.**
- 내가 본 것: 겹침 0.5~0.8 대(`ap_11`)는 같은 나무이지만 기둥·큰 사과가 화면 반대쪽으로 옮겨 간 **«겹치는 이웃 구도»** 이고, 0.8 이상(`ap_31`)은 네 쌍 모두 **«거의 똑같은 사진»** 입니다. 교수님 말씀은 «영상 프레임별로 하면 **거의 똑같잖아요**… 걸러내고» 입니다. 절반 겹침은 그 말의 범위를 넘습니다. 3차 보충 검수가 본 5프레임 이웃 208쌍의 겹침도 최소 0.842·중앙 0.931 이었습니다(그때 문장으로 쓴 «절반 이상» 기준이 실제 본 것보다 느슨했던 것 — 기준 문장을 고칩니다).
- **확정 기준(2단):**
  - **1단 «거의 같은 사진»** = 같은 장면·거의 같은 구도(화면의 약 80% 이상이 같고 같은 열매가 거의 같은 자리). **눈으로 확인된 쌍만** 제외 간선.
  - **2단 «겹치는 이웃»** = 같은 나무/포기, 구도가 눈에 띄게 다름. **빼지 않음.** manifest 에 `overlap_neighbors` 로 기록하고, **분할표를 만들 때 같은 그룹으로 묶어** 누수를 막는 쪽으로 처리(지우는 것이 아니라 묶는 것). 더 솎아낼지는 교수님 결정.
- 자동 점수(AKAZE 겹침) ≥ 0.8 인 660쌍 중 아직 아무도 안 본 **549쌍을 보충 눈 판정 3명에게 전부** 맡겼습니다(`inspect/260917_apple_blueberry/round3_final/v2/tier1_unseen_part{1,2,3}.csv`, 점수는 시트에 안 보이게). 이미 눈 판정이 있는 111쌍 중 40쌍은 새 기준으로 재점검.
- 블루베리 Camera 1·2·3: 3차에서 «구도가 달라 안 뺀다» 를 시트 1장으로 일괄 판정한 것은 **근거가 부족했습니다**(2단계 지적 인정). 같은 2단 기준을 쌍마다 적용합니다 — 겹침 ≥0.8 이고 눈으로 near_identical 이면 제외.

## ③ 데이터셋 v2 에서 실행할 것 (보충 눈 판정이 끝나면)
1. 제외 간선 = 기존 783 + 새 1단 확정 쌍. 독립 집합은 **가장 많이 남기는 순서**(세션·프레임 순 — 사이클 4 가 «2차 대표 먼저» 규칙이 최적보다 16장 더 뺀다는 것을 보임)로, **사람 확인(flag) 사진을 먼저 남김**(메모 소실 방지). 기존 제외 314장은 간선이 그대로이므로 새로 풀어도 대부분 유지되지만, 결과가 달라지면 달라진 대로 v2 가 정본.
2. **만든 스크립트를 파일로 남김**(`round3_final/v2/build_exclusion_v2.py` — 사이클 4 의 흠 1번).
3. flag 이전: 중복으로 빠지는 사진의 «라벨 안 된 열매 후보» 가 대표에도 보이면 대표에 flag(2단계 `proposal_flag_transfer.csv` 16행, 대표 좌표 포함).
4. flag 재분류(2단계가 68+6장 전부 확인, `proposal_flag_reclass.csv`): 사과 32 유지 / 15 → rule_pending / 21 해제, 블루베리 4 / 1 / 1. **너무 어두워 판정 불가(too_dark) 는 해제가 아니라 유지**로 바꿉니다(사람이 확대해서 볼 가치가 있음 — 2단계 분류에서 too_dark 만 있는 사진은 flag 유지, 메모에 «어두움» 표시).
5. 새 폴더는 **`datasets_reviewed_260917/` 를 v2 로 갱신하지 않고** 같은 폴더 안에서 고칩니다 — 아직 아무도 쓰지 않은 오늘 만든 폴더이고, 두 벌을 두면 어느 것이 정본인지 헷갈립니다. v1 의 manifest·build_summary 는 `_v1_260917_2224/` 로 보존.
   (2026-09-18 사이클 3 확인: 실제로 만들어진 폴더 이름은 **`_v1_260917/`** 입니다 — `_2224` 가 안 붙었습니다. manifest 2개 + build_summary.json + README.md 4파일 보존됨.) 빠지는 사진 파일만 새 폴더에서 치움(원본은 그대로).
6. README: 블루베리 마스크가 RGB 3채널(값 0/255, 원본부터)임을 명시 · «남긴 사진끼리 확인된 중복 관계 없음» 문장을 «1단(거의 같은 사진) 관계 없음, 2단(겹치는 이웃)은 남아 있고 `overlap_neighbors` 에 있음» 으로 · «같은 포기 50쌍» → «43그룹 93장 57쌍».
7. 3차 판정서 15행의 파일 경로 정정, 1단계가 고친 17행은 v2 숫자로 다시 씀. PDF 재생성.
8. 툴 반영(status·duplicates)은 UI 사이클 3 의 서버 수정(대표 명시 존중·되돌리기 범위)과 충돌하지 않게 **그 작업이 끝난 뒤** 한 번에. `duplicates.json` 의 옛 1차 잔재 필드(near_groups·group_types)도 이때 정리.

## ④ 교수님께 여쭐 것에 추가
- «겹치는 이웃» (사과 약 470쌍·블루베리 약 45쌍)을 더 솎아낼지, 분할에서 묶기만 할지.

---

## 2026-09-18 01:33 후속 — 데이터셋 **동결**

이 판정서의 ③ «v2 에서 실행할 것» 은 전부 실행됐고, 그 뒤 **판정자 간 일치도를 실제로 재서** 다수결까지 반영했습니다.
아래가 **최종(동결) 숫자**입니다 — 이 문서 본문의 숫자는 판정 당시(2026-09-18 00:1x) 기록이니 인용하지 마십시오.

- 사과 1,001 → **547**(제외 **454** · 대표 299묶음 · flag 51 · 규칙 미정 남은 사진 22 · 겹치는 이웃 293 · `split_group` **134**)
- 블루베리 1,195 → **1,114**(제외 **81** = 4번 카메라 71 + 3번 카메라 **10** · 대표 42묶음 · flag 4 · 규칙 미정 218 · 겹치는 이웃 112 · `split_group` **1,055**)
- 확인된 관계: 사과 «거의 같은 사진» **1,204** + «겹치는 이웃» 680 / 블루베리 **158** + 63. `split_group` 은 이 관계 전부로 이었고 **빈칸 0**.
- 눈 판정 660쌍(사과 631·블루베리 29) → 다수결 뒤 거의 같은 사진 **432**(421·11) · 겹치는 이웃 228.
- **판정자 간 일치도(89쌍)**: 사과 78~88%(κ 0.51~0.72) · 블루베리 59~79%(κ 0.17~0.58) · 세 판정 모두 일치 60/89(67%).
  ④ 에서 «여쭐 것에 추가» 했던 물음은 그대로이고, ②의 «블루베리 3번 카메라» 는 다수결로 11쌍·10장으로 확정됐습니다.
- 탐욕이라 사과는 최적(448)보다 **6장** 더 뺐습니다(블루베리 0장). 근거 `cycle_3/stage3/number_dict.md`.

---

## 2026-09-18 03:07:53 후속 — **최종 동결**(사이클 5 총괄. 위 «01:33 후속» 은 1차 동결판 기록입니다)

이 판정서와 위 01:33 후속의 숫자(사과 547 · 제외 454 · `split_group` 134 · 관계 1,204·680)는
**1차 동결판의 값이고 이제 쓰지 마십시오.** 1단계 총괄 검수가 «남긴 사진끼리 같은 촬영·가까운
프레임인데 한 번도 판정받은 적 없는 쌍이 있다» 를 찾아냈고, 그 쌍 300개를 판정자 둘이 간격을
모르는 채 눈으로 판정(거의 같은 사진 207 · 겹치는 이웃 93 · 다른 장면 0)해
`build_exclusion_v2.py` 에 **2단계**를 더한 결과가 최종 동결판입니다.

- 사과 1,001 → **429** (제외 **572** = 1단계 454 + 2단계 118 · 대표 **336**묶음 · flag 51 ·
  규칙 미정 남은 사진 **17** · 겹치는 이웃 **301** · `split_group` **99**개(2장 이상 48 · 최대 39장))
- 블루베리 1,195 → **1,114** (변화 없음 — 2단계에 해당하는 쌍이 0)
- 확인된 관계: 사과 «거의 같은 사진» **1,411** + «겹치는 이웃» **773** / 블루베리 158 + 63.
  관계가 `split_group` 을 넘는 것 **0건**.
- ③ 의 6번 «남긴 사진끼리 확인된 중복 관계 없음» 문장은 **여전히 참**이지만(0건 확인),
  그것은 «확인된 관계가 0» 이라는 뜻입니다 — 범위를 넓히면 더 나왔습니다(687 → 549 → 547 → 429).
- **총괄 판단**: 쌍별 삭제에는 자연스러운 멈춤점이 없으므로 지금 폴더를 «확인된 관계를 전부
  반영한 판» 으로 두고, 누수 방지는 삭제가 아니라 `split_group`·촬영 단위 분할로 하고,
  얼마나 솎을지는 간격별 곡선·대안표(394/294/248 …)를 놓고 교수님이 정하실 일로 올립니다.
- 최종 숫자 한 벌: `cycle_5/stage2/number_dict_final.md`(`cycle_3/stage3/number_dict.md` 는 547장 기준).
