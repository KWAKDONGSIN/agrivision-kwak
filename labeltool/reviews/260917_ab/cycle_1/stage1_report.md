# 사과·블루베리 재검수 사이클 1 · 1단계 보고서 — 데이터셋 무결성

작성: 2026-09-17

> 🔴 **시점 문서 (2026-09-18 사이클 3 추가).** 이 보고서는 **2026-09-17 «첫 판(v1)» 데이터셋을 대상으로**
> 쓴 것입니다. 본문의 사과 **687**·블루베리 **1,124**, 제외 **314**·**71**, flag **68**·**6**,
> `rule_pending` **25**·**244**, 대표 **190**·**33** 그룹, 정점 520·간선 783 은 **그때의 값**이고
> **지금은 다릅니다.** 본문 숫자는 판정 근거로서 그대로 둡니다.
>
> **현행(v2, 2026-09-18 01:04 재빌드)**: 사과 1,001 → **549**(제외 **452** · 대표 302그룹 · flag **51** ·
> `rule_pending` **39**(폴더에 남은 사진만 세면 **22**) · `overlap_neighbors` **298**) / 블루베리 1,195 → **1,113**
> (제외 **82** = 4번 카메라 71 + 3번 카메라 11 · 대표 43그룹 · flag **4** · `rule_pending` **244**(남은 사진 **218**) ·
> `overlap_neighbors` **110**). 2026-09-18 00:50 에 `overlap_neighbors` 는 «폴더에 남아 있는 이웃» 만 적도록 고쳐졌고
> (뺀 사진 행은 빈칸), 분할용 **`split_group`** 칸이 새로 붙었습니다(01:04 재빌드 기준 사과 **224**개 · 블루베리 **1,055**개 —
> 그중 사진이 2장 이상인 묶음은 137·90개이고, 관계가 없는 사진은 `as####`·`bs####` 로 혼자 한 묶음입니다).
> 사과 간선 **1,197** · 정점 **821**. 원천은 `kds0206/datasets_reviewed_260917/{apple,blueberry}/manifest.csv`,
> 생성은 `inspect/260917_apple_blueberry/round3_final/v2/build_exclusion_v2.py` →
> `final/build_reviewed_dataset_260917_v2.py` 입니다.
> ✅ **툴 `data/` 도 2026-09-18 00:58 에 v2 로 반영됐습니다**(사과 exclude **452**·flag **51** / 블루베리 **82**·**4**,
> «거의 같은 사진» 묶음 302·43). 그 전까지는 314·68 / 71·6 이었습니다.

작성자: 1단계 작업·검수 (Claude Opus 5)
산출물 폴더: `platform/work/kwak_dongsin/260916_라벨링툴/cycles/260917_ab/cycle_1/stage1/`
(스크립트 `stage1/scripts/` 11개, 로그·CSV `stage1/logs/` 46개, 눈으로 본 시트 `stage1/sheets/` 41장)
대상: `kds0206/datasets_reviewed_260917/{apple,blueberry}` (2026-09-17 22:24 생성)

> 전부 **새로 쓴 코드**로 셌습니다. `final/build_reviewed_dataset_260917.py` 의 함수는 하나도
> import 하지 않았고, 그 스크립트를 다시 돌리지도 않았습니다.

---

## 결론 3줄

1. **파일 무결성은 완벽합니다.** 사과 687 · 블루베리 1,124장의 이미지·마스크 **3,622개가 원본과 md5 바이트 동일**,
   깨진 PNG 0, 크기 불일치 0, 제외본 잔존 0. **사과 마스크는 인스턴스 번호를 그대로 유지**(고유값 최대 124,
   0/255 로 뭉갠 장 0). **블루베리 첫 실행 버그의 잔재 없음** — 버그가 놓쳤을 56장이 전부 폴더에 없습니다.
   문서에 적힌 숫자 36개 중 34개가 실측과 정확히 일치.
2. **🔴 제일 큰 문제: 남긴 사과 687장 안에 «진짜 거의 같은 사진» 이 아직 최소 58쌍(90장) 남아 있습니다.**
   제가 다른 방법(ORB 호모그래피)으로 전수 탐색해 상위 40쌍을 눈으로 보니 **40쌍 전부 진짜 중복**이었고,
   그중 55쌍은 2·3차가 **한 번도 시트로 본 적이 없는 쌍**입니다(후보를 «해시 그룹 + 5프레임 이웃» 으로만 뽑아서,
   10·15·30프레임 떨어진 같은 나무가 통째로 빠졌습니다). 블루베리는 깨끗합니다(새로 나온 것 3쌍뿐).
3. **🟠 두 번째 문제: 사람 확인 flag 12장이 «중복 제외» 에 먹혀 사라졌습니다.** 실행 스크립트가
   제외를 먼저 처리하는 바람에, 라벨 안 된 사과 후보가 있던 78장 중 12장이 flag 없이 폴더에서 빠졌고
   (후보 16개 소실), **그 12장의 대표 중 10장은 flag 가 아닙니다**. 거의 같은 사진이니 같은 사과가
   그 10장에서도 안 칠해져 있을 가능성이 큽니다. 3차 판정서의 «78장을 flag» 를 실측(66장)으로 고쳤습니다.

---

## 1. 항목별 판정표

### 1-1. 파일 무결성 전수 (46항목 전부 PASS · `logs/verify_integrity.log`)

| 항목 | 결과 | 실측 |
|---|---|---|
| 사과 manifest 1,001행, stem 중복 0, 원본 전량과 집합 일치 | PASS | 1,001 / 차집합 0 |
| 블루베리 manifest 1,195행, 동일 | PASS | 1,195 / 차집합 0 |
| 사과 action 분포 | PASS | keep 619 · needs_human 68 · excluded_duplicate 314 (= 1,001) |
| 블루베리 action 분포 | PASS | keep 1,118 · needs_human 6 · excluded_duplicate 71 (= 1,195) |
| N 폴더 집합 == manifest 의 keep+needs_human | PASS | 사과 687 · 블루베리 1,124, 양쪽 차집합 0 |
| images 집합 == masks 집합 | PASS | 687·687 / 1,124·1,124 |
| **excluded 가 N 폴더에 하나도 없음** | PASS | 사과 314/314 부재 · 블루베리 71/71 부재 |
| **이미지 md5 == 원본 전수** | PASS | 사과 687 다름 0 · 블루베리 1,124 다름 0 |
| **마스크 md5 == 원본 전수** | PASS | 사과 687 다름 0 · 블루베리 1,124 다름 0 → «자동으로 고친 마스크 0» 확인 |
| 깨진 PNG 0 | PASS | 3,622장 전부 디코딩 성공 |
| 이미지↔마스크 크기 일치 | PASS | 1,811쌍 전부 일치, 불일치 0 |
| **사과 마스크 = 열매 번호(인스턴스) 유지** | PASS | mode L, 고유값 최대 **124**(= 123인스턴스) · 장당 평균 41.5 · **vmax==255 인 장 0** · 고유값 통계가 원본과 전수 일치. 687장 인스턴스 합계 **28,534** |
| 블루베리 마스크 값 ⊆ {0,255} | PASS | 고유값>2 인 장 0 (단 §4-2 주의) |
| 0바이트 파일·png 아닌 파일 | PASS | 0개 / 0개 |
| build_summary.json 8개 숫자 == 실측 | PASS | total·kept·excluded·needs_human·rule_pending 전부 일치 |
| **블루베리 첫 실행(공백 버그) 잔재** | PASS | 버그 재현 결과 그때 빠졌을 것은 **15장**(기록과 일치), 나머지 **56장도 전부 부재**. 이미지·마스크 양쪽 |
| 툴 `data/*/status.json` == manifest | PASS | 사과 exclude 314·flag 68 / 블루베리 exclude **71**·flag 6, 집합 완전 일치(첫 실행의 15장짜리 상태가 남지 않음) |
| 툴 `data/*/duplicates.json` == 실제 | PASS | 사과 190그룹·504장·drop 314 / 블루베리 33그룹·104장·drop 71, manifest 제외집합과 동일 |

### 1-2. 사과 «독립 집합» 재계산 (20항목 전부 PASS · `logs/verify_indep.log`)

간선을 **출처 CSV 세 개에서 직접 다시 만들었습니다**(그룹은 클릭으로 전개):
`round2_apple_dup_verdicts.csv`(해시 47그룹 + 2차가 본 프레임인접 24쌍) ·
`round2_apple_dupnew_verdicts.csv`(완화 75그룹, partial 1그룹 제외) ·
`frameadj208_verdicts.csv`(3차가 본 208쌍, 전부 true_duplicate)
→ **정점 520 · 간선 783**.

| 항목 | 결과 | 실측 |
|---|---|---|
| ★ **남긴 687장끼리 «확인된 중복» 간선 0** | PASS | 위반 0쌍 (독립 집합 성질 성립) |
| ★ **뺀 314장마다 남긴 이웃이 있음** | PASS | 이웃 없는 제외본 0장 |
| ★ 각 제외본과 그 «대표» 사이에 실제 간선 존재 | PASS | 간선 없는 (제외본,대표) 0쌍 |
| 과잉 제거(다시 넣어도 위반 없는 제외본) | PASS | 0장 → 최소 제거 |
| manifest 제외집합 == 독립집합 CSV | PASS | 314 == 314, 차 0 |
| manifest note 의 «대표 X» == CSV 대표 | PASS | 불일치 0건 |
| 참고: 사슬로 이었다면 | 기록 | 94성분·520장 → 426장 제거(실제는 314장). «과함» 판단은 타당 |
| 블루베리 Camera4 그룹 33·제외 71, 영상마다 1장 | PASS | 전부 일치, 대표 33장 전원 잔존 |
| 블루베리 Camera1·2·3 그룹(43개, 93장) 은 안 뺌 | PASS | 제외집합과 겹침 0 |

### 1-3. 숫자 사전 (36항목 중 34 일치 · `logs/number_dict.csv`)

전부 일치: 원본 1,001/1,195 · 남긴 687/1,124 · 뺀 314/71 · 대표 190 · Camera4 영상 33 ·
flag 68/6 · rule_pending 25/244 · 고친 마스크 0/0 · 해시 47 · 완화 75(본 것은 76) ·
프레임인접 208+24=232 · 사슬 94그룹·426장 · Camera4 그룹 안 104장 · 누락후보 300/300(세부 분류도 전부 일치) ·
블루베리 마스크 오류 3 · 규칙무관 추가 3 · PDF 34쪽.

다른 것 2건 → §3.

---

## 2. 눈으로 본 것

### 2-1. 뺀 사진 ↔ 대표 무작위 40쌍 (사과 30 · 블루베리 10, seed 3407)

시트 `sheets/drop_apple_{1..6}.jpg` · `sheets/drop_blueberry_{1,2}.jpg`, 표본 `logs/drop_sample40.json`.

**판정: 40쌍 전부 «진짜 거의 같은 사진». 잘못 뺀 것 0건.**

- 사과 30쌍: 전부 같은 나무·같은 지주·같은 그림자에 카메라만 조금 움직인 연속 프레임.
- 블루베리 10쌍: 전부 같은 송이(같은 잎 반점·같은 파란 표식까지 동일).
- 처음에 의심스러웠던 3쌍(`20150921_131346` 의 image976↔1031/1046/1051)은 원본 해상도로 다시 보니
  (`sheets/zoom_g000_all20.jpg`, `zoom_g000_976_1051.jpg`) **20프레임 전부 같은 나무**였습니다 — 오탐 아님.
  다만 그 클릭의 양 끝은 «같은 나무를 조금 다른 거리에서» 라 화면 겹침이 부분적입니다(§4-1).

**보조 수치(전수, `logs/drop_rep_orb.csv`)**: 제외본 385장을 전부 대표와 ORB 호모그래피로 대조 →
사과 314쌍 score 중앙값 **0.701**, 근거가 약한 쌍(<0.3)은 **1쌍뿐**. 블루베리는 접사·심한 확대라 ORB 가
자주 실패(중앙값 0.396, <0.1 이 17쌍)하지만 **가장 약한 15쌍을 눈으로 다시 보니 전부 같은 송이**
(`sheets/weak_apple_1.jpg`, `weak_blueberry_{1..3}.jpg`) — 수치가 낮은 것은 ORB 의 한계이지 오제외가 아닙니다.

### 2-2. 🔴 남긴 사진 안에 남은 중복 — 독립적 전수 탐색

방법(1·2차가 쓴 pHash 와 다른 경로): 64×64 회색 썸네일 정규화 상관으로 **전체 쌍 전수 계산**
(사과 235,641쌍 · 블루베리 631,126쌍) → 상위 400쌍에 **ORB(2000) + RANSAC 호모그래피** → 내부점비율 × 겹침.
스크립트 `scripts/scan_residual_dup.py`, 결과 `logs/residual_dup_{apple,blueberry}.csv`.

| 과일 | score≥0.5 | score≥0.3 | 상위 40쌍 육안 판정 |
|---|---:|---:|---|
| 사과 | **58쌍 (90장)** | 73쌍 (112장) | **40/40 진짜 중복** (오탐 0) |
| 블루베리 | 3쌍 | 7쌍 | 대부분 이미 `rule_pending` 에 적힌 «같은 포기 겹침» |

- 사과 58쌍은 **전부 같은 촬영 시퀀스** 안이고, **55쌍은 2·3차가 한 번도 시트로 본 적이 없습니다**
  (208/24쌍 목록에도, 2차 그룹 안에도 없음 — `logs/residual_dup_apple_top58.csv` 의 `seen_before` 칸).
- 프레임 간격 분포: 5프레임 3쌍 · **10프레임 17 · 15프레임 14 · 20프레임 5 · 25프레임 3 · 30프레임 12** · 그 외 4.
  → 3차 보충 검수가 **5프레임 이웃만** 후보로 뽑았기 때문에 10프레임 이상 떨어진 같은 나무가 통째로 남았습니다.
- 가장 뚜렷한 예: `20150921_131453_image991 ↔ image996` (상관 0.948 · 내부점 0.93 · 겹침 0.954, 둘 다 남음),
  `image701↔731`, `20150921_131625_image226↔236`, `dataset1_front_1321↔1351`, `dataset1_back_1↔31`.
- 블루베리에서 새로 나온 것은 `Camera 3 Video (14)` 의 프레임 1·121·241 조합 3쌍 정도 —
  이미 기록된 «같은 포기 겹침» 그룹의 연장선입니다(교수님 결정 대기 항목).

> README 의 «남긴 사진끼리는 확인된 중복 관계가 없게 골랐습니다» 는 **틀린 말은 아닙니다**
> («확인된» 관계는 실제로 0). 다만 읽는 사람은 «중복이 없다» 로 받아들입니다. 실제로는 남아 있습니다.

### 2-3. needs_human 무작위 25장 (사과·블루베리 74장 중, seed 3407)

시트 `sheets/flag_{1..5}.jpg`(전체+좌표) · `sheets/flagzoom_{1..5}.jpg`(후보 상자 600px 확대),
표본 `logs/flag_sample25.json`. 25장에 후보 상자 **29개**.

**메모의 좌표가 실제로 «라벨 안 된 열매» 를 가리키는가 — 29개 중:**

| 판정 | 개수 | 비율 | 예 |
|---|---:|---:|---|
| ✅ 진짜 라벨 안 된 열매 | 12 | 41% | dataset2_front_1200, dataset3_front_1080#1·#2, dataset1_front_361, dataset2_front_750, dataset3_front_780, dataset4_front_660#1·#2, dataset1_back_151, dataset2_back_780, dataset3_front_1350#1, Camera 3 Video (56)_121 |
| ⚠️ 낙과·뒷줄 (규칙 문제) | 4 | 14% | dataset3_front_270 · _240 · _210, dataset2_front_660 — 전부 **땅에 떨어진 사과** |
| ⚠️ 같은 과실의 안 칠해진 부분(경계·amodal) | 2 | 7% | dataset3_front_120(왼쪽 반만 칠해진 노란 사과), dataset1_front_1141 |
| ❌ 오검출(잎·마른 꽃부리) | 3 | 10% | dataset4_front_120(마른 잎) · dataset1_back_1111(노란 잎) · Camera 3 Video (120)_241(상자 안에 **마른 꽃부리**가 같이 들어감) |
| ❓ 너무 어두워 판정 불가 | 8 | 28% | 20150921_131453_image746 #1·#2, _131625_image746·image851·image666·image511, _131453_image401, dataset3_front_1350#2 |

- **오검출률은 10%(3/29)** 로 낮습니다. 다만 «라벨 안 된 열매» 로 flag 된 것의 **21%(6/29)는 사실 규칙 문제**
  (낙과 4 + 경계 2)라, 사람이 툴에서 열었을 때 «고칠 수 없는 것» 을 보게 됩니다.
- 블루베리 «규칙무관 진짜 누락» 3장 중 표본에 든 2장: `(56)_121` 은 타당, **`(120)_241` 은 상자 안에
  마른 꽃부리가 같이 들어가 있어 `rule_pending` 쪽이 맞아 보입니다**(3차 판정의 «규칙무관» 분류 재검토 필요).
- 어두워서 판정 불가 8개(28%)는 «사람이 봐도 모르는 것» 이라, 툴에서 사람이 볼 때 시간만 쓰게 됩니다.

---

## 3. 문서 숫자·경로 대조 — 다른 곳

| # | 무엇 | 어디 (파일·줄) | 적힌 값 | 실측 | 조치 |
|---|---|---|---|---|---|
| 1 | 라벨 안 된 사과 후보가 있는 사진을 flag | `inspect/260917_apple_blueberry/260917_사과블루베리_3차판정.md` **17행** (§① 6번 행) | «78장을 툴에서 사람 확인 flag» | 실제 flag **66장** (12장은 중복으로 먼저 빠짐) | **고침** (§5) |
| 2 | 블루베리 «같은 포기가 겹쳐 보이는» | README 36행 · 3차판정 13행 · PDF 2쪽·10쪽 | «50쌍» | 그룹 **43개** · 사진 **93장** · 실제 쌍 **57개**. 50 = 93−43 (대표가 아닌 사진 수) | **안 고침** — PDF 는 다시 만들어야 하고, 한 문서만 고치면 새 불일치가 생김. 표현을 «50장(43그룹)» 으로 바꾸는 게 맞음 |

그 밖에 **경로 오류는 없었습니다** — README·3차판정·PDF 부록E 가 가리키는 17개 경로가 전부 실존합니다
(`build_reviewed_dataset_260917.py`, `v3_seed3407/{apple,blueberry}.csv`, `0915랩미팅.txt`,
1·2·3차 보고서, `data/*/status.json`·`duplicates.json`·`duplicates_round1.json`·`_status_backup_260917_r3.json`,
`datasets_reviewed_260916`, PDF 34쪽).

다만 **가리키는 파일이 헷갈립니다**: 3차판정 15행은 최종 중복 그룹이
`round3_apple_frameadj/apple_final_dup_groups.csv` 라고 적었는데, 실제로 쓴 것은
`round3_final/apple_final_groups_independent.csv` 입니다. 앞 파일로 세면 **426장 제외**(94그룹)가 나와
보고서의 314장과 안 맞습니다. 경로 «오류» 는 아니라서 고치지 않았고, 2단계가 판단할 일로 넘깁니다.

---

## 4. 못 고친 것 / 넘기는 것 (심각도순)

### 4-1. 🔴 남긴 사과 687장 안의 잔존 중복 58쌍(90장)

- 데이터셋을 다시 만들어야 하는 일이라 1단계에서 손대지 않았습니다.
- 다시 걸러려면 후보를 **같은 시퀀스 안 ±40프레임 전부**(또는 제가 쓴 «전체 쌍 상관 → ORB» 경로)로 넓혀야 합니다.
  지금 방식(해시 그룹 + 5프레임 이웃)으로는 구조적으로 못 잡습니다.
- **분할표를 다시 만들 때가 특히 위험합니다.** 이 90장이 폴드를 넘어 갈라지면 학습·평가 누수가 됩니다
  (README 도 «분할표는 아직 없다» 고 적어 뒀으니, 재생성 전에 이 목록을 반드시 반영해야 합니다).
- 목록: `logs/residual_dup_apple_top58.csv` (상위 400 후보 전체는 `logs/residual_dup_apple.csv`).

### 4-2. 🟠 flag 12장이 «중복 제외» 에 먹힘 + 그 대표 10장은 flag 아님

- 원인: `build()` 가 `drop` 을 먼저 처리하고 `continue` 하므로, 제외본은 flag 분기까지 못 갑니다
  (`final/build_reviewed_dataset_260917.py` 41~43행).
- 결과: 라벨 안 된 사과 후보 **16개**가 데이터셋 어디에도 안 남았고, 그 12장의 대표 12장 중
  **10장은 `keep`**(사람이 볼 목록에 없음). 거의 같은 사진이라 같은 사과가 그 10장에도 안 칠해져 있을 것입니다.
- 명단: `logs/flag_lost_by_exclusion.csv`. 고치려면 «제외본의 flag 를 대표로 옮기기» 가 필요한데
  좌표를 그대로 옮길 수 없어(카메라가 움직임) 사람이 봐야 합니다 → 3단계 몫.

### 4-3. 🟠 블루베리 마스크는 **RGB 3채널**입니다

- README 34행은 «블루베리는 0/255» 라고만 적었는데, 실제 파일은 mode **RGB**(값은 0/255, R=G=B, 표본 60장 확인).
  `np.asarray(mask)` 하면 `(H,W,3)` 이 나옵니다. 원본이 그런 것이라 데이터 문제는 아니지만,
  학습 코드가 `(H,W)` 를 기대하면 그대로 깨집니다. README 주의문에 한 줄 보태는 게 좋겠습니다(1단계에선 안 고침).
- 사과는 mode L 이고 인스턴스 번호가 살아 있습니다(대비).

### 4-4. 🟡 flag 메모 품질

- 표본 29개 중 **오검출 3 · 규칙 문제 6 · 판정 불가 8** — 사람이 툴에서 열었을 때 절반 이상이
  «고칠 수 없거나 아무것도 없는» 상자입니다. 우선순위를 나눠 주는 게 좋겠습니다(3단계 몫).
- 블루베리 `Camera 3 Video (120)_241` 은 «규칙무관 진짜 누락 3» 중 하나인데 마른 꽃부리가 같이 들어갑니다.

### 4-5. 🟡 클릭(clique) 가정

- 2차 그룹 판정(«이 20프레임은 같은 나무»)을 **그룹 안 모든 쌍이 중복**으로 전개했습니다.
  그 결과 20프레임짜리 그룹의 양 끝(976↔1051)처럼 «같은 나무지만 화면 겹침은 부분적» 인 쌍도
  중복 관계로 셉니다. 제외 자체는 타당하지만(누수 방지), «거의 같은 사진» 이라는 설명과는 조금 다릅니다.

### 4-6. 🟡 문서 표현 2건

- «50쌍» (§3 #2) · README 의 «남긴 사진끼리 확인된 중복 관계 없음» (§2-2 끝).

---

## 5. 고친 것 (전·후)

**대상**: `platform/work/kwak_dongsin/260916_라벨링툴/inspect/260917_apple_blueberry/260917_사과블루베리_3차판정.md` **17행**
**백업**(먼저 만듦): 같은 폴더 `_backup_260917_abc1_260917_사과블루베리_3차판정.md`
**diff**: `stage1/logs/fix_3차판정.diff` (바뀐 줄 1줄, 그 외 0줄. 파일 31행 유지)

| 무엇 | 전 | 후 |
|---|---|---|
| §① 6번 행 끝 | «→ 78장을 툴에서 사람 확인 flag(후보 좌표를 메모에)» | 같은 문장 + «**실행 결과 flag 는 66장**(78장 중 12장이 근접 중복으로 먼저 빠짐. 그 12장의 후보 16개는 어디에도 안 남았고 대표 12장 중 10장은 flag 가 아님 — 사이클1·1단계 2026-09-17 확인, `cycles/260917_ab/cycle_1/stage1/logs/flag_lost_by_exclusion.csv`)» |

- ⛔ **`datasets_reviewed_260917/` 안의 파일은 하나도 안 건드렸습니다**(고칠 오류가 없었음).
- ⛔ 원본 `datasets_resized_2mp/`, 툴 `data/`, `build_reviewed_dataset_260917.py`, PDF, README 미접촉.
- GPU 미사용·새 패키지 설치 없음·삭제 없음·남의 프로세스 미접촉. `작업기록.md` 미수정(지시대로).

---

## 6. 가정한 것

1. **«같다» 의 기준은 manifest.csv** 로 두었습니다(툴 `status.json` 이 아니라). 실제로는 둘이 완전히 일치했습니다.
2. **간선 전개**: 2차의 «그룹» 판정은 그룹 안 모든 쌍을 중복으로 봤습니다(클릭). 이것이 3차가 실제로 쓴 해석과
   같다는 것은 «독립집합 CSV 의 314장 · 190그룹 · 각 (제외본,대표) 쌍에 간선 존재» 가 전부 맞아떨어져서 확인했습니다.
3. **md5 동일** 은 파일 바이트 기준입니다. 전수 통과했으므로 화소 비교는 자동으로 성립합니다.
4. **잔존 중복 탐색**은 «전체 쌍 썸네일 상관 → 상위 400쌍 ORB» 입니다. 400쌍은 표본이므로
   **58쌍은 하한**입니다(상관 0.82 에서 잘랐고, 그 아래에도 더 있을 수 있습니다).
5. «진짜 중복» 판정은 사진만 보고 한 육안 판정입니다. 애매한 것은 «부분 겹침» 으로 따로 적었습니다.
6. 무작위 표본은 `random.Random(3407)` 고정. 40쌍은 제외본 385장 중, 25장은 flag 74장 중.
7. 블루베리 첫 실행 버그는 **재현**해서 확인했습니다(`r['drop'].split('|')` 결과 중 앞뒤 공백이 없는 원소만
   매칭 → 15장). 기록의 «15장만 빠졌다» 와 일치합니다.
8. GPU·모델 추론은 쓰지 않았습니다. 전부 CPU 파일 비교·화소 연산입니다.

---

## 7. 2단계가 그대로 재현할 명령

파이썬은 전부 `/home/kds0206/.conda/envs/kwak/bin/python`, 작업 폴더는
`cycles/260917_ab/cycle_1/stage1/`.

| # | 명령 | 결과 | 로그·산출물 |
|---|---|---|---|
| 1 | `python scripts/verify_integrity.py` | 46항목 전부 PASS (md5 전수 3,622) | `logs/verify_integrity.log`, `verify_integrity_results.csv`, `md5_*.csv`, `probe_*.csv`, `blueberry_first_run_residue.json` |
| 2 | `python scripts/verify_indep.py` | 20항목 전부 PASS (정점 520·간선 783) | `logs/verify_indep.log`, `verify_indep_results.csv`, `apple_edges.json` |
| 3 | `python scripts/verify_flag_pending.py` | 14항목 중 1 FAIL(= §4-2 발견) | `logs/verify_flag_pending.log`, `expected_flag_pending.json` |
| 4 | `python scripts/make_drop_sheets.py` | 40쌍 시트 8장 → 육안 **오제외 0** | `sheets/drop_{apple,blueberry}_*.jpg`, `logs/drop_sample40.json` |
| 5 | `python scripts/scan_residual_dup.py` | 사과 58쌍·블루베리 3쌍(≥0.5), 시트 16장 → **40/40 진짜 중복** | `logs/residual_dup_*.csv`, `sheets/resid_*.jpg` (약 12분) |
| 6 | `python scripts/check_drop_rep_all.py` | 제외본 385쌍 전수 ORB | `logs/drop_rep_orb.csv` (약 7분) |
| 7 | `python scripts/make_flag_sheets.py` → `make_flag_zoom.py` | flag 25장·후보 29개 확대 | `sheets/flag_*.jpg`, `flagzoom_*.jpg`, `logs/flag_sample25.json` |
| 8 | `python scripts/make_zoom_g000.py` · `make_weak_sheets.py` | 의심 쌍·약한 근거 쌍 재확대 | `sheets/zoom_g000_*.jpg`, `weak_*.jpg` |
| 9 | `python scripts/number_dict.py` | 36항목 중 34 일치 | `logs/number_dict.csv`, `number_dict.log` |
| 10 | (인라인) 툴 상태 대조 | status.json·duplicates.json == manifest | `logs/tool_state_check.log` |
| 11 | `pdftoppm -png -r 110 -f 2 -l 3 … logs/pdfpage` | PDF 본문 숫자 확인(글자 인코딩이 어긋나 텍스트 추출은 안 됨 → 그림으로 봄) | `logs/pdfpage-0{1,2,3,4,5}.png`, `pdfpage-{11,34}.png` |

⛔ `final/build_reviewed_dataset_260917.py` 는 **절대 다시 돌리지 마세요**(툴 상태를 덮어씁니다).
