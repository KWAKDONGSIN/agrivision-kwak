작성: 2026-09-17
최종 수정: 2026-09-18 (사이클 5 총괄 — `status.json` 두 개를 지우라는 안내가 **위험해져서** ⛔ 경고를 달았습니다)

# 지울 목록 (사람이 실행) — 0917 기능 시험 잔재

기능을 확인하면서 생긴 파일입니다. 실험 결과에는 영향이 없고, 깨끗하게 지우려면 아래를 실행하시면 됩니다.

> ⛔⛔ **2026-09-18 02:39 정정 (사이클 5 총괄) — 아래 2) 와 덧붙임 2 의 3) 을 실행하지 마세요.**
> 이 문서를 쓸 때(2026-09-17) `data/apple/status.json` 은 시험 항목 1~2개짜리 «원래 없던 파일» 이었지만,
> **2026-09-18 01:33 동결 반영 뒤에는 사과 검수 결과 507항목(제외 454 · 문제 있음 51 · 안 봄 2),
> 블루베리 86항목(제외 81 · 문제 있음 4 · 안 봄 1)이 들어 있는 정본 파일**입니다.
> 지우면 **AI 3회 검수 결과가 툴에서 통째로 사라집니다.** (실측: `/api/fruits` 2026-09-18 02:28)
> 되살리려면 `--tool` 을 다시 돌려야 하는데 그것도 금지돼 있습니다(README §주의).
> 시험 잔재로 남은 것은 **빈 폴더뿐**이므로, 아래 2)·3) 에서는 `rm -f … status.json` 줄을 빼고
> `rmdir` 줄만 실행하십시오.

```bash
D=/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/data
# 1) 상자 시험 (복숭아 한 장, 이미 상자 0개로 비워 둠)
rm -f "$D/peach/boxes/210629-t4-17.json" "$D/peach/boxes_yolo/210629-t4-17.txt" "$D/peach/boxes_all.json"
rmdir "$D/peach/boxes" "$D/peach/boxes_yolo" 2>/dev/null
# 2) 번호 편집 시험
# ⛔ 아래 한 줄은 2026-09-18 부터 실행 금지 (사과 검수 결과 507항목이 들어 있습니다)
# rm -f "$D/apple/status.json"
rmdir "$D/apple/instances_fixed" "$D/apple/masks_fixed" 2>/dev/null
```

- 원본 데이터셋과 마스크는 처음부터 건드리지 않았습니다.
- `data/apple/status.json` 은 번호 편집·되돌리기 시험으로 항목이 생긴 것입니다(마지막 시험 사진 `20150919_174151_image361`). 원래 없던 파일이라 파일째 지우면 사과는 «아직 안 봄» 으로 돌아갑니다. **← 2026-09-18 01:33 이후로는 틀린 설명입니다. 위 ⛔ 상자를 보세요.**
- 빈 폴더 둘은 시험 파일을 API 로 되돌린 뒤 남은 껍데기입니다.

## 덧붙임 (2026-09-17 15시)
- 번호 편집 시험에서 만든 번호본·이진본 파일은 되돌리기 API 로 **이미 삭제됐습니다**(잔재 0 확인).
- 위 명령 중 `rmdir` 은 빈 폴더만 지우므로 안전합니다. 폴더에 파일이 남아 있으면 아무 일도 하지 않습니다.

## 덧붙임 2 (2026-09-17 15시 · 사이클2 2차 검수)

2차 검수가 **블루베리 초벌 번호 편집·저장 왕복**을 처음 시험해서 자취가 하나 더 늘었습니다.
번호본·이진본 파일은 되돌리기 API 로 이미 지워졌고(잔재 0 확인), 남은 것은 **원래 없던 파일 하나**와 **빈 폴더 하나**입니다.

```bash
D=/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/data
# 3) 블루베리 번호 편집 시험
# ⛔ 아래 한 줄은 2026-09-18 부터 실행 금지 (블루베리 검수 결과 86항목이 들어 있습니다)
# rm -f "$D/blueberry/status.json"
rmdir "$D/blueberry/instances_fixed" 2>/dev/null
```

- `data/blueberry/status.json` — 시험 «전» 에는 **없던 파일**입니다. 항목 1개:
  `Camera 1 Video (1)_1` = `{status: unreviewed, by: "AI 사이클2 2차 시험", note: "번호 편집 되돌림"}`
  (시험을 두 번 돌려 `at` 시각만 갱신됩니다 — 항목은 계속 1개입니다).
  블루베리는 아직 사람 검수 기록이 없으므로 파일째 지우면 시험 전으로 완전히 돌아갑니다.
  **← 2026-09-18 01:33 이후로는 틀린 설명입니다(지금은 검수 결과 86항목). 맨 위 ⛔ 상자를 보세요.**
- `data/blueberry/instances_fixed/` — 저장 때 만들어졌고 지금은 **빈 폴더**입니다. `masks_fixed/` 는 원래 있던 폴더입니다.
- 원본 데이터셋(`datasets_resized_2mp`)과 검출 팀 초벌(`work/park_seongmoon/.../instance_maps`)은 **읽기만** 했습니다.
- `data/peach/boxes/210629-t4-17.json` 의 `by`·`at` 는 2차가 상자 시험을 다시 돌려 `검수C2B 되돌림 / 15:00:19` 로 갱신됐습니다(상자는 여전히 0개).

## 덧붙임 3 (2026-09-17 15시 · 사이클3 1차)

사이클3 1차가 **초벌 상자 개수 대조**(읽기만)와 **고친 번호의 내보내기 왕복**(저장→내보내기→되돌리기)을
시험했습니다. 번호본·이진본 파일은 되돌리기 API 로 **이미 지워졌습니다**(잔재 0 확인).
**새 명령은 필요 없습니다** — 위 1)~3) 명령이 그대로 덮습니다. 달라진 것만 적습니다.

- `data/apple/status.json` — 항목이 **1개에서 2개로** 늘었습니다. 왕복 시험 대상이 `20150919_174151_image1`
  이어서 그 항목이 새로 생겼습니다(`{status: unreviewed, by: "AI 사이클3 1차 왕복시험", note: "번호 편집 되돌림"}`).
  기존 `20150919_174151_image361` 항목은 그대로입니다. 이 파일은 **원래 없던 파일**이라 위 2) 의
  `rm -f "$D/apple/status.json"` 한 줄로 사과가 완전히 시험 전으로 돌아갑니다.
  **← 2026-09-18 01:33 이후로는 실행 금지입니다(맨 위 ⛔ 상자).**
- `data/blueberry/status.json` — 항목은 **여전히 1개**(`Camera 1 Video (1)_1`)이고 `by`·`at` 만
  `AI 사이클3 1차 왕복시험 / 2026-09-17 15시대` 로 갱신됐습니다(왕복 시험을 다시 돌리면 `at` 만 또 갱신됩니다 —
  항목 수는 계속 1개입니다). 위 3) 의 `rm -f` 로 그대로 지워집니다.
- `data/{apple,blueberry}/instances_fixed/`, `data/apple/masks_fixed/` — 되돌리기 뒤 **빈 폴더**입니다
  (위 명령의 `rmdir` 이 지웁니다). `data/blueberry/masks_fixed/` 는 원래 있던 폴더입니다.
- `data/peach/boxes/210629-t4-17.json` — 사이클3 은 **건드리지 않았습니다**(상자 0개, 15:00:19 그대로).
- 내보내기 산출물은 **공용 `data/` 가 아니라 세션 임시 폴더**에 만들었고 공용 폴더에는 아무것도 남기지
  않았습니다. `cycle_3/stage1/roundtrip.py` 를 다시 돌리면 `/tmp/c3_roundtrip_<임의문자>/` 가 하나 생깁니다
  (임시 원본 폴더 심볼릭 링크 4개 + 내보낸 사진 2장). 재부팅하면 사라지고, 당장 지우려면 아래 한 줄입니다.
  원본 데이터셋·검출 팀 폴더(`work/park_seongmoon/`)는 **읽기만** 했습니다.

```bash
rm -rf /tmp/c3_roundtrip_*        # 왕복 시험 임시 폴더 (있을 때만)
```

### 사이클3 이 새로 만든 백업 사본 (지워도 되지만 2차 검수 전에는 두는 것이 좋습니다)

```bash
B=/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴
# 사이클3 1차가 고치기 전 사본 (규칙상 남긴 것)
rm -f "$B/app/_backup_260917_c3_boxes.py" "$B/app/_backup_260917_c3_instances.py" \
      "$B/app/_backup_260917_c3_README.md" "$B/app/static/_backup_260917_c3_app.js" \
      "$B/_backup_260917_c3_260917_지울목록_시험잔재.md" \
      "$B/cycles/260917_신기능/cycle_2/_backup_260917_c3_stage1_work.md" \
      "$B/cycles/260917_신기능/cycle_2/_backup_260917_c3_stage2_review.md" \
      "$B/cycles/260917_신기능/cycle_2/stage2/_backup_260917_c3_modesim.js" \
      "$B/cycles/260917_신기능/cycle_2/stage1_client/_backup_260917_c3_boxsim.js"
```

## 덧붙임 3-2 (2026-09-17 16시, 사이클 3 검수가 찾아낸 빠진 자취)
포도 상자 시험 파일 하나가 앞선 목록에서 빠져 있었습니다.

```bash
D=/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/data
rm -f "$D/grape/boxes/740.json" "$D/grape/boxes_yolo/740.txt" "$D/grape/boxes_all.json"
rmdir "$D/grape/boxes" "$D/grape/boxes_yolo" 2>/dev/null
```

## 덧붙임 4 (2026-09-17 17시, 포도 번호본 — «지울 필요는 없는» 산출물)
사이클 4 에서 CERTH 원본의 송이 번호를 툴이 읽을 수 있는 형태로 2,502장 변환해 두었습니다. 시험 잔재가 아니라 **의도한 산출물**입니다.

- 위치: `260916_라벨링툴/data/grape/instances_seed/` (2,502개, 약 34MB)
- 지금은 툴이 이 폴더를 **읽지 않습니다.** 교수님 확인 8번(전경 기준) 답을 받은 뒤 한 줄을 켜면 읽습니다.
- 포도 번호를 쓰지 않기로 하면 폴더째 지워도 툴 동작은 그대로입니다.

```bash
# 포도 번호를 안 쓰기로 결정했을 때만
rm -rf /data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/data/grape/instances_seed
```

## 덧붙임 5 (2026-09-17 18시, 사이클 4 임시 파일)
검수가 쓴 세션 임시 폴더입니다. 서버 밖이라 지워도 툴과 무관합니다.

```bash
rm -rf /tmp/c4_* /tmp/c4b_yolo_* /tmp/c4_expinst_*
```

- 사이클 4 의 3차 판정은 임시 파일을 스스로 지워 남은 것이 없습니다.
- `cycles/260917_신기능/cycle_4/stage3/` 은 검수 증거이자 사이클 5 의 수락 시험이므로 **지우지 마세요.**

### 덧붙임 5 보충 (사이클 5)
- `/tmp/c4_expinst_*` 3개가 더 생겼습니다. 위 명령에 포함해 두었습니다.
- 회귀 시험이 `data/apple/status.json` 과 `data/blueberry/status.json` 의 **기존 항목 두 개를 갱신**했습니다(항목 수는 늘지 않음). 두 파일은 덧붙임 1·2 의 명령으로 지우면 됩니다. **← 2026-09-18 01:33 이후로는 실행 금지(맨 위 ⛔ 상자).**

## 덧붙임 6 (2026-09-17 저녁, 최종 판정이 전수로 센 임시 파일)
`/tmp` 에 검수 임시 파일 16개가 남아 있습니다. 서버 밖이라 지워도 툴과 무관합니다.

```bash
rm -rf /tmp/c3_roundtrip_* /tmp/c4_* /tmp/c4b_yolo_*
```

- 내역: `c4_expinst_*` 11개(N6 보충에서 2개 늘어남) · `c4b_yolo_*` 2개 · `c4_oneline_*` · `c4_yolo_*` · `c4_grape_try*` · `c3_roundtrip_*`
- `data/apple/status.json`·`data/blueberry/status.json` 의 항목은 덧붙임 1·2 의 명령으로 지웁니다(항목 수는 늘지 않았고 값만 갱신됐습니다). **← 2026-09-18 01:33 이후로는 실행 금지(맨 위 ⛔ 상자).**
- ⛔ `cycles/` 아래 보고서와 시험 스크립트는 검수 증거이니 지우지 마세요.

## 2026-09-17 UI 사이클1 · 2차 검수(Opus 5) 가 남긴 것

| 무엇 | 경로 | 크기 | 지워도 되나 |
|---|---|---|---|
| 모래상자 서버 폴더 (app 복사 + data 복사본) | `cycles/260917_ui/cycle_1/stage2/sandbox/` | **95 MB** | 3차 판정이 끝나면 지워도 됨. **공용 data/ 가 아니라 복사본**이고, 시험이 만든 `masks_fixed/`·`boxes/`·`instances_fixed/`·`status.json` 이 들어 있음 |
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
| 모래상자 서버 프로세스 (0.0.0.0:5121) | — | — | **2026-09-17 21:42 에 껐습니다** (PID 3412853). 남은 프로세스 없음 |
| 2차 시험 스크립트 | `cycles/260917_ui/cycle_1/stage2/{probe.py, ops.py, ops2.py}` | 40 KB | 재현용으로 **남깁니다** |
| 2차 스크린샷·결과 | `~/ff_shots/ui1/stage2/` (PNG 33장 + JSON 7개) | 6 MB | 보고서가 경로로 가리키므로 **남깁니다** |
| 2차가 고친 파일의 백업 | `app/static/_backup_260917_ui2_ui.js`, `_backup_260917_ui2_index.html` | 35 KB | 되돌릴 때 필요 — **남깁니다** |


## 2026-09-20 EASY 작업 실측 갱신

대행: Codex · 측정 2026-09-20T21:04:55.809413

위의 과거 삭제 명령은 현재 상태를 반영하지 않는다. 이 갱신은 목록만 제공하며 어느 파일도 지우지 않았다. data 아래에는 실제 검수 정본이 있어 이름만 보고 시험 잔재라고 지우면 안 된다.

| 경로 | 현재 상태 | 판단 |
|---|---|---|
| `data/peach/boxes/210629-t4-17.json` | 166바이트 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |
| `data/peach/boxes_yolo/210629-t4-17.txt` | 0바이트 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |
| `data/peach/boxes_all.json` | 336바이트 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |
| `data/grape/boxes/740.json` | 168바이트 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |
| `data/grape/boxes_yolo/740.txt` | 0바이트 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |
| `data/grape/boxes_all.json` | 338바이트 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |
| `data/apple/instances_fixed` | 파일0개 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |
| `data/apple/masks_fixed` | 파일0개 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |
| `data/blueberry/instances_fixed` | 파일0개 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |
| `data/blueberry/masks_fixed` | 파일0개 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |
| `data/grape/instances_seed` | 파일2502개 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |
| `cycles/260917_ui/cycle_1/stage2/sandbox` | 파일7987개 | 삭제 안 함. 원본 여부와 사용자 기록 먼저 확인 |

- `data/apple/status.json` 현재625항목. 검수 정본이므로 삭제 대상 아님.

- `data/blueberry/status.json` 현재87항목. 검수 정본이므로 삭제 대상 아님.

- `data/grape/status.json` 현재183항목. 검수 정본이므로 삭제 대상 아님.

- `data/peach/status.json` 현재125항목. 검수 정본이므로 삭제 대상 아님.

새 시험 사본은 cycles/260921_쉽게/{candidate/tests/_sandbox,sandbox_after,sandbox_before,sandbox_scale}. 증거와 보고서가 이 경로를 참조한다. 운영 data와 분리돼 있으며 최종 검수 뒤에도 자동 삭제하지 않는다. 전후 스크린샷은 ~/ff_shots/easy_260921, 백업은 _archive/pre_easy_260920에 보존한다. 포도 instances_seed는 교수님 확인 대기 자료이며 시험 잔재가 아니다.
