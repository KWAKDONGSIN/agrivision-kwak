작성: 2026-08-02

──────────────────────────────────────────────────────────
아래 ─── 사이의 글을 전부 복사해서 로컬 클로드에 붙여넣으세요.
(Ctrl+A 로 이 파일을 전체선택하지 말고, ─── 안쪽만 복사)
──────────────────────────────────────────────────────────

───────────────── 여기부터 복사 ─────────────────

방금 네가 고친 논문 원고를 서버(연구실 GPU 서버)에 반영해야 해.
서버에는 LaTeX 컴파일러가 없어서, 컴파일 검증은 네가 한 Overleaf 결과가 정본이야.
아래 4가지를 해줘. 새로 고치는 건 없고, 정리해서 넘겨주기만 하면 돼.

[1] 최종본 확인
    - 최종 main.tex 의 전체 경로를 알려줘.
    - Overleaf 에서 에러 0 / 경고 몇 개 / 총 24쪽이 맞는지 다시 한번 확인해서 말해줘.
    - main.tex 말고 내가 건드린 파일이 또 있는지 알려줘.
      특히 references.bib 을 고쳤는지, 그림 파일을 새로 추가하거나
      파일 이름을 바꿨는지가 중요해. (서버에 그림 12개가 이미 있어서,
      안 바뀌었으면 그림은 다시 안 보내도 돼.)

[2] 변경 요약 문서 만들기
    파일 이름: 260802_원고정정_변경요약.md
    맨 윗줄에 "작성: 2026-08-02" 을 넣고, 아래 내용을 담아줘.
    - 코덱스 지적 9건 + 정정 12항목 중에서 "무엇을 어떻게 고쳤는지" 한 줄씩.
    - 각 항목마다 main.tex 의 몇 번째 줄 / 어느 절(section)을 고쳤는지.
    - 고치지 "못한" 것, 혹은 일부러 안 고친 것이 있으면 이유와 함께 반드시 적어줘.
      (여기서 빠뜨리면 서버 쪽에서 대조할 때 문제가 돼. 솔직하게 적어줘.)
    - 쪽수가 17쪽 → 24쪽으로 늘었는데, 어디가 늘어난 건지 한두 줄로.

[3] 넘길 파일 한 폴더에 모으기
    바탕화면에 "서버로_보낼것_260802" 라는 폴더를 새로 만들고 거기에 넣어줘.
    - main.tex                        (필수)
    - 260802_원고정정_변경요약.md      (필수)
    - references.bib                  (고쳤을 때만)
    - 새로 추가/이름변경한 그림 파일    (있을 때만)
    - 컴파일된 PDF                     (있으면 넣어줘. 눈으로 확인용)
    ※ zip 으로 묶지 마. 파일 그대로 두면 돼.
    ※ main_backup_260802.tex 는 넣지 마. 내 로컬에만 두면 돼.

[4] 마지막에 알려줄 것
    - 그 폴더의 전체 경로
    - 폴더 안 파일 목록과 각 파일 크기

주의: 원고 내용을 새로 고치거나 다듬지 마. 지금 상태 그대로 넘기는 게 목적이야.

───────────────── 여기까지 복사 ─────────────────


──────────────────────────────────────────────────────────
그 다음 (곽동신 님이 직접)
──────────────────────────────────────────────────────────

1) 로컬 클로드가 알려준 "서버로_보낼것_260802" 폴더를 윈도우 탐색기로 엽니다.

2) VS Code 왼쪽 파일 목록에서 아래 폴더를 찾습니다.
       incoming

3) 폴더 안의 "파일들을 전체선택(Ctrl+A)" 해서 incoming 위로 드래그합니다.
   ⚠️ 폴더째로 끌지 마세요. 폴더 드래그는 지난번 4번 다 실패했습니다.
      안에 있는 파일들만 골라서 끄는 겁니다. 다 합쳐도 몇 MB라 1분이면 끝납니다.

4) 다 올라가면 서버 클로드(저)에게 이렇게만 말씀하시면 됩니다.
       "incoming 에 올렸어"

   그러면 제가 이어서 합니다.
       ① 서버의 현재 포도 단독본을 main_grape_only_backup_260802.tex 로 복사 백업
       ② 새 main.tex 로 교체 (팀 공동 4작물본 main.tex 는 건드리지 않음)
       ③ diff 로 무엇이 바뀌었는지 목록 보고
       ④ 0802 에 확인해 드린 12항목이 실제로 반영됐는지 하나씩 대조
       ⑤ \ref 깨짐 · 그림 경로 존재 여부 점검
