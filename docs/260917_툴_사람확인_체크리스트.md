작성: 2026-09-17
최종 수정: 2026-09-19 («개수 세기» 사이클5 2차 — D-3 풍선말 문구 정정 + 사이클5 3항목 추가)

# 라벨링 툴 — 크롬에서 10분만 눌러 봐 주세요 (18항목)

AI 는 서버에 화면이 없어서 **파이어폭스**로만 눌러 봤습니다. 실제로 쓰는 **크롬**에서
글자·줄바꿈·마우스가 다를 수 있어, 2026-09-18·**09-19** 의 사이클들이 **실제로 고친 자리만** 골라
18줄로 줄였습니다. 한 줄에 «**무엇을 누른다 → 무엇이 보여야 한다**» 하나뿐입니다.

보안 관련 값·설정 세부는 공개본에서 생략했습니다.
- 🔴 **막히면 이렇게 알려 주세요**: 「**항목 번호 + 눌렀을 때 실제로 보인 것**」 한 줄 (예: «C-3 을 눌렀는데
  «확정이 풀렸습니다» 가 안 뜨고 아무 글자도 안 나왔습니다»). 화면 캡처 한 장이면 더 좋습니다. **고치는 것은 저희가 합니다.**
- 사진은 **검색 칸에 이름을 붙여넣고** 나온 카드를 누르면 됩니다.
- 장수·개수는 이 문서에 적지 않습니다 — «현황» 탭 숫자를 믿어 주세요.
- 다 끝나면 **F 묶음**으로 눌러 본 것을 되돌려 주세요(1분).

---

## A. 들어가기 (1분)

보안 관련 값·설정 세부는 공개본에서 생략했습니다.
      화살표로 끝까지 본 뒤 **닫는다** → 목록이 보인다. (안내 창이 탭 단추를 덮고 있어도 정상입니다 — 닫으면 됩니다.)
- [ ] **A-2** 오른쪽 위 **«내 이름»** 칸에 **본인 이름**을 적는다 → 적은 이름이 칸에 남아 있다.
      (확정 기록에 이 이름이 남습니다. 이름 없이 첫 판정을 누르면 툴이 **한 번만** 물어봅니다.)

## B. 목록 — 지금 무슨 작업인지 · 내 큐 (1분)

- [ ] **B-1** 과일 «사과» → 목록 **맨 위 필터 줄 왼쪽**에 지금 무슨 작업인지 **한 단어**
      (**🎨 마스크** / **▭ 상자** / **＃ 번호**)가 적혀 있다. 〔0918 사이클5 새로 넣은 것〕
- [ ] **B-2** 빠른 단추 **«☑ 내 큐»** 를 누른다 → 정렬이 «내 큐» 로 바뀌고 카드마다 **«미확정»** 딱지가 보인다.
      («내 큐»·«미확정» 은 **B-1 에 적힌 그 작업**의 확정을 기준으로 봅니다.)

## C. ① 마스크 — 확정하고, 고치고, 되돌리기 (4분)

- [ ] **C-1** 사과 `20150919_174151_image201` 을 연다 → 사진 **아래 첫 줄**에 «**AI 제안: … — 맞으면 Enter**»
      (또는 «⚠ … **미확정 · Enter=맞다**»)가 보인다 → **`Enter` 한 번** → «확정: …» 이 뜨고 **다음 장이 저절로** 뜬다.
      ⚠ 열 때 **노란 점선 자리로 저절로 확대**돼 있는 것이 정상입니다(누를 것 없음 · `0` 으로 원래 크기).
- [ ] **C-2** 복숭아 `210629-t1-01` 에서 **왼쪽 «🖌 붓»** 으로 한 번 긋고 **«저장»(Ctrl+S)** →
      «저장됨 — `.` 로 다음 · 맞으면 `Enter` 로 확정» 이 뜬다 → **`Enter`** → «확정: …».
      (**저장만으로는 확정이 아닙니다.** 저장은 다음 장으로 넘어가지 않는 것이 정상입니다.)
      ⚠ 왼쪽 도구 중 **처음 눌려 있는 것은 «✨ 자동채움»** 입니다(붓은 `B` 를 누르거나 직접 고릅니다).
- [ ] **C-3** 같은 사진에서 오른쪽 칸 `▸` → «더 보기» → **«수정본 되돌리기»** → 초록이 사라지고
      «**확정이 풀렸습니다 — 다시 확정하세요**» 가 **1초** 뜬다. 〔0918 사이클4 — 되돌리면 그 확정이 지워집니다〕
- [ ] **C-4** 왼쪽 **«🖼 원본만» → «🎭 마스크만» → «🔀 겹쳐»** 를 누르고 **`Q`** 키로도 차례로 돌려 본다 →
      «원본만» 은 색칠과 **점선까지 전부** 사라지고, «겹쳐» 로 돌아오면 **처음 그대로** 돌아온다.
- [ ] **C-5** 블루베리 `Camera 2 Video (7)_121` 에서 **붓 크기 슬라이더를 작게** 옮긴 뒤
      **브라우저 창을 완전히 닫고 다시 열어** 같은 과일 사진을 연다 → **붓 크기가 그대로** 남아 있다.
      🔴 이 항목은 **사람만 확인할 수 있습니다**(시험 도구로는 못 잽니다).

## D. ② 상자 · ③ 번호 (2분)

- [ ] **D-1** 사과 `20150919_174151_image201` 에서 왼쪽 맨 위 **«▭ 상자»** → 아래 줄에
      «**상자 0개 — «초벌» 을 누른 뒤 Enter**» 또는 «**AI 초벌 상자 N개 — 맞으면 Enter**» 가 보인다 →
      («0개» 면 왼쪽 **«✨ 초벌»** 을 먼저 누른다) → **`Enter`** → «상자 확정: 맞다» 가 뜨고 다음 장.
- [ ] **D-2** 사과 `20150919_174151_image1` 에서 **«＃ 번호»** → 알 하나를 **클릭**하고 **`D`** 로 지운 뒤
      아래 줄 **«번호 저장»** → «… 저장 완료» 가 뜨고, 그것만으로 **번호가 «수정함» 으로 확정**된다.
      (아래 줄이 «✔ 확정 — 수정함 …» 으로 바뀌는지 보세요.)
- [ ] **D-4** 사과 `20150919_174151_image1` 에서 **«＃ 번호»** → 사진 아래 첫 줄이
      «**원본 정답 번호 95개 — 맞으면 Enter**» 로 보인다(«AI 초벌 번호» 가 **아니어야** 합니다).
      〔0919 사이클5 — 원본 마스크의 정답 번호를 AI 산출물이라고 부르지 않게 고친 자리〕
      (Enter 는 누르지 않아도 됩니다. 문구만 봐 주세요.)
- [ ] **E-3** «데이터 정리» 탭 → 종류에서 «**개수(counts.csv)**» 를 찍고 조건은 «사람 확정만» 그대로 →
      «**개수 csv 는 마스크를 확정한 사진만 담깁니다 — 상자만 확정하면 빈 표**» 한 줄이 새로 보인다.
      «AI 제안 포함» 으로 바꾸면 그 줄이 사라진다. 〔0919 사이클5〕 («서버에 저장» 은 누르지 마세요)
- [ ] **E-4** 복숭아에서 «상자 YOLO» 를 내보내면 «**⚠ 상자가 0개여서 건너뛴 사진 N장(이름)**» 이 뜨고,
      그 사진의 txt 는 만들어지지 않는다. 〔0919 사이클5 — 전에는 0줄 txt 가 나가서 검출 학습이
      «이 사진에는 열매가 없다» 는 정답으로 읽었습니다. 2차에서 «그 폴더에 남아 있는 옛 0줄 txt» 도
      «낡은 txt» 로 세어 알려 주게 고쳤습니다 — 그 파일 삭제는 사람이 합니다〕
- [ ] **D-3** 포도 `1027` 을 연다 → 왼쪽 **«＃ 번호» 칸이 회색으로 눌리지 않고**, 마우스를 올리면
      «**③ 열매 번호는 «번호본이 있는 사진» 에서만 됩니다 (이 사진에는 번호본이 없습니다).**» 라는 이유가
      **풍선말**로 나온다. (`K` 를 눌러도 아무 일이 없는 것이 정상)
      〔0919 사이클5 2차 정정 — 예전 문구 «복숭아·포도는 번호가 없습니다» 는 이제 화면에 없습니다.
      복숭아는 번호본이 생겼고, 포도만 교수님 확인 전이라 꺼져 있습니다〕

## E. 목록 카드와 «데이터 정리» (1분)

- [ ] **E-1** 위쪽 **«목록»** 으로 돌아온다 → 방금 확정한 카드에 «**✔ 확정**» 딱지와 그 판정 색이 보이고,
      **스크롤 자리가 그대로**다. 카드에 마우스를 올리면 «사람이 … 확정했습니다 · AI 제안: …» 이 풍선말로 나온다.
      〔0918 사이클5 — 확정 직후 그 카드만 곧바로 바뀝니다〕
- [ ] **E-2** 맨 위 **«데이터 정리»** 탭 → 조건이 **«사람 확정만» 에 이미 찍혀 있고**, 그 아래에
      «**지금 확정된 사진 N장** … · **지금 조건으로 나갈 사진 N장**» 한 줄이 보인다. **«서버에 저장» 은 누르지 마세요.**
      (표에 «빈 폴더» 라고 적힌 줄이 있으면 그대로 두고 알려 주세요 — 지우는 것은 담당자가 합니다.)

## F. 끝나면 되돌리기 (1분)

- [ ] **F-1** **C-2** 의 복숭아 `210629-t1-01` → «더 보기» → «수정본 되돌리기»(C-3 에서 이미 했으면 건너뜁니다) ·
      **D-1** 의 사과는 왼쪽 «🧹 전부» → 아래 «상자 저장» · **D-2** 의 사과는 «더 보기» → «번호 되돌리기».
      남는 것은 `status.json` 의 판정·확정 기록뿐이고, **지우는 명령은 담당자가 실행합니다**
      (`platform/work/kwak_dongsin/260916_라벨링툴/260917_지울목록_시험잔재.md`).

---

## 헷갈리기 쉬운 것 — 고장이 아닙니다

| 이런 일이 | 왜 정상인가 |
|---|---|
| `1`(원본 OK) 을 눌렀는데 오른쪽 «상태:» 가 «제외» 그대로다 | `1`·`3`·`4` 는 **사람 확정 칸만** 씁니다. AI 제안은 일부러 남깁니다. 사람이 누른 것은 **아래 줄의 «✔ 확정 …»** 과 카드의 «✔ 확정» 딱지로 보입니다 |
| «저장» 을 눌렀는데 다음 장으로 안 넘어간다 | 눈으로 확인하라고 그렇게 두었습니다. `.` 로 넘기고, 확정은 `Enter` |
| 되돌렸더니 «확정» 이 없어졌다 | 맞습니다(0918 사이클4). 원본으로 돌아갔으니 확정도 풀립니다 — 다시 보고 `Enter` 한 번 |
| 상자를 확정했는데 마스크는 «미확정» 이다 | 확정 칸이 **셋**(마스크·상자·번호)이고 서로 독립입니다 |
| `Enter` 를 눌러도 아무 일이 없다 | 안내 창이 열려 있거나 · 메모·이름·검색 칸에 커서가 있거나 · Ctrl/Alt/Shift 와 함께 눌렀거나 · «테두리 채움/빼기»(다각형)가 켜져 있거나 · ③ 에서 **그리다 만 영역**이 있을 때(그때 Enter 는 «붙이기») 입니다 |
| 붓질이 안 된다 | 사진 **왼쪽 위**에 «② 상자 그리기 중»·«③ 번호 편집 중» 이 있는지 보세요 → 왼쪽 **«🎨 마스크»** |
보안 관련 값·설정 세부는 공개본에서 생략했습니다.

---

## 이번에 뺀 항목 (2026-09-18 · 사이클 5 총괄)

10분 안에 끝내려고 **288줄 → 15항목**으로 줄였습니다. 뺀 까닭을 한 줄씩 남깁니다.

- **«먼저 볼 것 5개»** — 지금 A·B·C 가 같은 자리를 밟습니다(중복).
- **«1 을 누르면 상태가 «원본 OK» 로 바뀐다»**(옛 144행) — **틀린 문장**이었습니다. `1` 은 확정 칸만 씁니다 →
  «헷갈리기 쉬운 것» 표 첫 줄로 옮겼습니다(오인 보고 1순위였습니다).
- **«1 을 눌러도 오른쪽 상태가 그대로»**(옛 96~97행) — 이제 **어느 경우에나** 그대로여서 막힘 여부를 구분하지 못합니다.
- **«＃ 번호 편집 중에는 Enter 가 안 먹어야 한다»**(옛 84~87행) — 지금 ③ 의 Enter 는 **번호 확정**입니다
  (그리다 만 영역이 있을 때만 «붙이기»). 남은 가드는 «헷갈리기 쉬운 것» 표로.
- **««🖌 붓» 이 처음부터 눌려 있다»**(옛 118행) — **기본 도구가 «✨ 자동채움»** 입니다 → C-2 에 한 줄로 적었습니다.
- **«점선이 손톱만 하다 → 🔍 를 누르면 커진다»**(옛 168~172행) — 열 때 **이미 그 자리로 확대**돼 있습니다(4.83배) → C-1 로.
- **«카드 테두리 색이 상태와 맞는다»**(옛 107행) — 기준이 «**지금 고른 작업의 사람 확정**» 으로 바뀌었습니다 → E-1 로.
- **진행률 줄 확인**(옛 82행) — «▭ 상자 N · ＃ 번호 N» 칸이 늘어 문장이 낡았습니다. 숫자는 «현황» 탭에서 봅니다.
- **③ 번호 저장 뒤 확인 줄 없음**(옛 191~192행) — 저장이 **확정까지** 한다는 것을 D-2 에 넣었습니다.
- **F 묶음(상자 세부 8줄)**(옛 212~225행) — ② 의 **Enter 확정·저장 확정·«상자 0개» 문구**를 하나도 보지 않았습니다 → D-1 한 줄로 바꿨습니다.
- **«남는 것은 판정 기록뿐»**(옛 254행) — 확정 세 칸이 더 남고 **화면에는 확정을 지우는 단추가 없습니다** → F-1 로.
- **머리말·묶음 목록**(옛 2~10·22행) — «데이터 정리»·«상자/번호 확정»·«마스킹 편의» 가 빠져 있었습니다 → 새 머리말과 B·D·E 로.
- **C 묶음(«제외» 사진이 고쳐도 제외로 남나)** — 사이클 1~3 이 시험으로 고정했고(회귀 통과), 사람이 잘못 누르면
  안 쓸 사진이 데이터셋에 들어가는 위험한 항목이라 뺐습니다.
- **D 묶음(중복 묶음 정리)·G 묶음(현황 탭)** — 네 과일 모두 «새로 제외할 사진 0장» 이라 눌러도 아무 일이 없습니다.
- **H 묶음(되돌리기 6줄)** — F-1 한 줄로 합쳤습니다.
- **소감 질문 9개** — 15항목을 다 본 뒤 «어디서 헤맸는지» 한 줄만 적어 주시면 됩니다.
