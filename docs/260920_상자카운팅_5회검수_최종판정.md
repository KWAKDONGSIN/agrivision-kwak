작성: 2026-09-20

# 「상자 + 개수 세기」 5사이클 — 5회 검수 최종 판정 (팀용)

판정: Fable 5.1 · 문서 조립: Opus 5(3차 보조) · 판정 시각(실측) **2026-09-20 00:04~00:3x**
근거 문서: 사이클별 판정서 `platform/work/kwak_dongsin/260916_라벨링툴/cycles/260919_count/cycle_{1,2,3,4}/stage3_final.md` ·
사이클 5 `cycle_5/stage1/stage1_work.md`(1차 · 재료 §5) · `cycle_5/stage2_review.md`(2차 검수 · **재료 정정 30곳**) ·
`cycle_5/stage3_final.md`(이 사이클 3차) · 논문 근거 `문서/260919_논문근거_대조표.md` · 형식 `문서/260919_툴_방향전환_5회검수_최종판정.md`

> 📌 지시서는 이 파일 이름을 `260919_…` 로 적었습니다. 실제로 만든 날이 **2026-09-20**(자정을 넘겼습니다)이라
> 「파일명 앞의 날짜 = 작성일」 규칙에 따라 **`260920_…`** 로 냈습니다. 내용은 지시서가 말한 그 판정서입니다.

> 🔴 **숫자마다 «언제 잰 값인가» 를 붙였습니다.** 2026-09-19 **22:55:23** 부터 사람이 툴을 쓰기
> 시작했기 때문입니다(복숭아 한 장 «문제 있음» 확정). 사람이 «제외» 를 더 누르면 복숭아 장수가
> 또 줄어듭니다 — **0920 재빌드 때 다시 세십시오.** 이 문서의 지표 표는 **2026-09-20 00:1x 에
> 실자료로 다시 돌린 값**입니다.

## 1. 결론

라벨링 툴은 2026-09-19 14:00 부터 09-20 00:04 까지 다섯 사이클(각각 만들기 → 검수 → 판정)을 돌아,
**상자 치기와 «과일 개수» 가 같은 화면·같은 확정 규칙 안으로 들어왔습니다.** 사진마다 개수 칸이 늘 보이고
(상자 수 · 번호 수 · 팀원 값), 상자나 번호를 `Enter` 로 확정하면 그것이 그 사진의 «개수 확정» 입니다 —
사람이 숫자를 손으로 칠 칸은 만들지 않았습니다. 내보내기에는 `counts.csv`(칸 12개)가 생기고, 통합
데이터셋 manifest 도 같은 규칙으로 개수 칸을 읽습니다. 초벌은 **«이미 있는 것» 을 먼저 쓰는 쪽으로
바뀌었습니다**(M1) — 복숭아 번호 초벌이 툴 자체 4-연결 CC 에서 **박성문 님 워터셰드 출력**으로 바뀌면서
복숭아 개수 오차가 **MAE 0.70 → 0.31**(R² 0.986)이 됐습니다. 논문 쪽은 `platform/01_references` **PDF
143편 전수**를 `pdftotext` 로 훑어 근거 대조표 **28칸**을 만들고(일치 11 · 부분 7 · 어긋남 6 · 근거 없음 2 ·
오지정 1 · PDF 없음 1), 인용 **51건을 글자까지 대조**했습니다(**지어낸 인용 0건**). 다섯 사이클에서 결함
**35건**(코드 34 + 시험 기대값 1)을 찾아 고쳤고, 회귀는 사이클 4 와 **숫자가 그대로**입니다(달라진 한 줄은
사람이 사진 한 장을 확정해서 «복숭아 125 → 124» 가 된 것입니다). 실서버는 이 5사이클에서 **세 번**
재시작했고 마지막이 **2026-09-20 00:04:44(PID 1467097)** 입니다.

남은 것은 네 가지입니다.

1. **마스크·상자·번호 확정은 아직 0장입니다.** 2026-09-19 22:55:23 에 복숭아 한 장이 처음 «확정» 됐지만
   그것은 «문제 있음(flag)» **판정**이고, 학습에 쓸 세 확정 칸(`confirmed`·`confirmed_boxes`·
   `confirmed_instances`)은 네 과일 전부 **0장**입니다(실측 `status.json` 00:2x). 재학습의 1순위는 여전히
   GPU 가 아니라 «Enter 를 누르는 일» 입니다.
2. **«사람 확정만» 경로는 실자료로 한 번도 돌지 않았습니다.** 0920 재빌드가 첫 실전입니다.
3. **모든 개수는 «정답 마스크 입력» 에서 나온 값 = 상한**입니다(M18). 예측 마스크로 센 값은 없습니다.
4. **포도는 자동으로 못 셉니다**(합계 +54.35% · R² −1.406). 교수님 확인 8번(«송이» 의 정의)이 오기 전까지
   포도 번호 초벌은 **끈 채**로 둡니다.

## 2. 5사이클 표

| 사이클 | 무엇을 | 고친 결함 | 실서버 재시작 | 판정서 |
|---|---|---|---|---|
| 1 (공격적) | 개수 칸 · 확정 유도 · `counts.csv` · 통합 manifest 개수 칸 · 상자 정의 문서화 | **11**(2차 즉시 7 + 3차 전 총괄 결정 4) + 문서 정정 4 | **2026-09-19 16:41:06~09** → PID 3915126 → **263865** | `cycle_1/stage3_final.md` |
| 2 (보통) | 논문 근거 대조표 1판(28칸) + 어긋난 문서 수정 | **코드 0** · 문서·대조표 정정 **24곳** | 없음(실서버 접속 0) | `cycle_2/stage3_final.md` |
| 3 (공격적) | 대조표 공격 검수(인용 51건 글자 대조 · 쪽 번호 전수 재실측 · 순환 튜닝 · 포도 실측) | **코드 0** · 판정 뒤집기 **4** · 문맥 이탈 **4** · 쪽 번호 **7** | 없음 | `cycle_3/stage3_final.md` |
| 4 (보통) | M1 팀 워터셰드 초벌 · M17 `count_metrics.py` · 그림 3종+근거 PDF · 브라우저 회귀 · 문서 | **20**(1차 13 + 2차 6 + 포도 끄기 1) | **2026-09-19 21:45:04~07** → PID 263865 → **964494** | `cycle_4/stage3_final.md` |
| 5 (종합) | 소수정 4 + 시험 위생 2 + 2차 즉시 고침 8 + 최종 판정서·Codex 질문·카톡 | **4**(1차) + 위생 2 · 2차가 코드·시험 7파일 + 문서 6개 | **2026-09-20 00:04:44** → PID 964494 → **1467097** | `cycle_5/stage3_final.md` |

검수 방식: **홀수 사이클은 공격적 리뷰, 짝수는 보통 리뷰, 5는 종합**(곽동신 지시 2026-09-18).
모든 «같다» 는 서로 다른 코드 경로의 증거만 인정합니다. 코드는 모래상자에서 만들고 **3차가 켭니다**.
사이클 2·3 이 «코드 0» 인 것은 대조표 전용 사이클이고 같은 파일을 사이클 1 이 쥐고 있었기 때문입니다
(«한 파일 한 작업자»).

**실서버 재시작 이력(실측)**: 16:41:06~09 → 21:45:04~07 → **00:04:44**(PID 는 `app/logs/server.pid` ·
`ps -o lstart` 로 확인).

## 3. 고친 결함 35건

캡션: **코드 34건 + 시험 기대값 1건(#7) = 35건.** 이 표를 세어 만든 값이며, 사이클별 판정서에는
«11»·«20» 같은 합계가 적혀 있지 않습니다(2차 검수 §4-1 정정).

| # | 사이클 | 무엇이 잘못됐나 | 어떻게 고쳤나 |
|---|---|---|---|
| 1 | 1 2차 | 🔴 개수 캐시가 지문을 안 봐, 번호를 95→10개로 고쳐 저장해도 화면·`counts.csv` 가 «95개»(거짓)를 말한다 — 지표 표 전체가 무의미해진다 | `instances.py count_fresh()` 신설 · `server.py`·`export_dataset.py` 가 그것을 쓴다 |
| 2 | 1 2차 | 상자를 저장(=확정)했는데 화면이 «사람 확정 개수: 아직 없습니다», 전부 지우면 거꾸로 «2개» — 두 값을 반대로 말한다 | `app.js cntReload()` — 그 한 장만 `/api/item` 을 다시 묻는다(규칙은 서버에만) |
| 3 | 1 2차 | 명령줄 내보내기가 «나갈 사진 0장» 이면 `FileNotFoundError` 로 죽는다(README 가 적어 둔 명령이 바로 그것) | `export_dataset.py` — 0장이어도 폴더·«0장» manifest·`counts.csv` |
| 4 | 1 2차 | `counts.csv` 와 manifest 가 판정을 따로 읽어 **다른 스냅샷**이 된다(내보내는 중 누가 확정하면 갈린다) | `counts_rows(..., st=)` — 같은 스냅샷 한 번만 읽는다 |
| 5 | 1 2차 | «개수만» 내보낸 폴더를 목록이 «빈 폴더» 라고 적어 사람이 실패로 읽는다 | `api_export_list` 의 빈 폴더 판정에 `n_counts` 를 넣었다 |
| 6 | 1 2차 | README §19 설명이 사실과 달랐다(칸 5개 ↔ 실제 6개 등) | `app/README.md` §19-1·19-3·19-4 정정 |
| 7 | 1 2차 | 앞 사이클 시험의 낡은 기대값 때문에 회귀가 «새로 깨진 것» 으로 보인다 **(이 한 건은 코드가 아니라 시험 파일입니다)** | `260918_paint/cycle_3/stage1/t2_ui.py` 기대값 (4,3,2)→(4,4,2) |
| 8 | 1 3차전 | 🔴 문구는 «툴은 고르지 않습니다» 인데 코드는 어긋날 때 **번호 쪽 수를 골라** 지표로 보냈다 | `boxes.py human_count()` 가 어긋나면 비우고 `conflict` · 통합 빌더도 같은 규칙 |
| 9 | 1 3차전 | 데이터셋에서 **빠진 사진**도 `counts.csv` 에 남아 MAE·R² 의 분모가 달라진다 | `exclude_reason()`·`included_stems()` 를 꺼내 «나간 사진» 만 담는다 |
| 10 | 1 3차전 | 번호 확정 사진을 열 때마다 다시 세어 한 장 218 ms 를 계속 낸다 | `count_now()` — 지문과 함께 캐시에 적는다(218 ms → **0.32 ms**) |
| 11 | 1 3차전 | 어긋남 노랑이 «확정» 초록을 덮고, 옛 서버 가드가 «값 없는 사진» 과 같은 글자다 | 확정이 있으면 초록 우선 · 옛 서버는 `개수 ?·?·?` |
| 12 | 4 1차 | 🔴 번호 없는 과일에서 툴이 이진 마스크를 **4-연결 CC** 로 세어 초벌이 어긋난다(복숭아 MAE 0.70 ↔ 팀 워터셰드 0.30) | `instances.py SEED_DIRS` 에 복숭아(+포도) · `seed_source` 를 API·화면에 실었다 |
| 13 | 4 1차 | 팀원 상자 파일이 그 사진에 없으면 초벌이 **404** 로 끝나 아무것도 안 나온다 | 마스크·번호로 물러서고 `fallback_from` 을 실었다(네 칸 404 → 200) |
| 14 | 4 1차 | M1 뒤 «복숭아·포도는 번호가 없습니다» 가 거짓이 됐는데 화면·문서에 그대로 남았다 | `ui.js`·`app.js`·`help.html` 4곳·`README` 2곳 |
| 15 | 4 1차 | 한 번의 내보내기가 판정을 **세 번** 읽어, manifest 는 «제외» 인 사진이 상자 YOLO 로 나갔다 | `export_worker` 가 맨 앞에서 한 번 읽어 셋에 같은 스냅샷을 넘긴다 |
| 16 | 4 1차 | `/api/item` 안에 «번호 수» 가 둘이라(옛 `n_inst`) 고쳐 저장하면 값이 갈린다 | `n_inst = cnt["instances"]` 로 맞췄다 |
| 17 | 4 1차 | `mk_reg_c4.py` 가 «있으면 그대로» 라 09-18 사본을 계속 돌려 **낡은 기대값 실패가 재생산**된다 | 있으면 멈추고 정본 md5 를 찍는 `mk_reg_cnt4.py`·`mk_c4_cnt4.py` |
| 18 | 4 1차 | 1픽셀·0폭 상자가 YOLO txt 에 `w=0.000781` 로 그대로 나간다(저장 때는 버리는데) | `export_boxes_to()` 에도 같은 `MIN_SIDE` · `dropped_small` 로 알린다 |
| 19 | 4 1차 | 그림 3종 **파일**이 08-11·08-12 판이라 논문·발표 그림이 옛 단정문을 담고 있었다 | `reports/figures/` 3종 재생성(18:50) |
| 20 | 4 1차 | 근거 PDF 스크립트만 08-12 그대로라 «낙과 미라벨(단정)»·«미라벨 유지» 를 찍는다 | `make_evidence_workflow_pdf.py` L126·L282~283 + PDF 재생성 |
| 21 | 4 1차 | 화면 «내 수정본» 색은 시안인데 사용법 그림만 초록이다 | `make_help_figs_260917.py` 색 통일 + `ex1~ex5.jpg` 재생성 |
| 22 | 4 1차 | 🔴 1차가 `app.js` 최상위에서 `window.` 를 가드 없이 만져 `boxsim.js` **50항목이 통째로 죽었다** | `typeof window !== "undefined"` 가드 — 50/50 복구 |
| 23 | 4 1차 | M1 으로 앞 사이클 시험 **5곳**의 기대값이 거짓이 됐다 | 정본·사본 양쪽 + 결정 번호 주석(옛 증거는 지우지 않고 칸을 옆에 추가) |
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
| 25 | 4 2차 | 🔴 `fig_samples_annotated` 캡션 **마지막 두 줄(교수님이 넣으라던 낙과 문장)이 그림 밖으로 잘렸다** | 그림 높이 +0.9in · 재생성(아래 여백 72px) |
| 26 | 4 2차 | 통합 개수 시험이 **공용** 캐시에 시험용 항목 5개를 쓴다 | `test_counts.py` 가 `INST.CACHE_DIR` 도 모래상자로 돌린다 |
| 27 | 4 2차 | 🔴 화면이 «나갈 상자 1장» 이라 적고 저장을 누르면 «0장» 이라며 거절한다 | 내보내기 가드가 «**사진(마스크·번호)이 필요할 때만** 막게» 됐다(`ui.js needPhoto`) · «상자 장수를 따로 보는» 것은 가드가 아니라 문구 함수 `expOutText`(`ui.js:1166`) |
| 28 | 4 2차 | «⚠순환» 이 **사람이 다 맞힌 줄**에도 붙어 거짓 설명이 달린다 | 알려진 순환 짝만 ⚠순환, 나머지 오차 0 은 **⚠오차0** |
| 29 | 4 2차 | help 그림 `ex4_box.jpg` 위 여백 0px 로 제목 윗선이 깎였다 | `save()` 에 바깥 여백 + 5장 재생성 |
| 30 | 4 2차 | 화면·문서의 거짓 문장 3종(복숭아 번호 출처를 «포도 CERTH» 라 함 등) | `app.js`·`help.html` 8곳·`README` +18 |
| 31 | 4 2차 | 🔴 1차가 **지난 사람 결정**(포도 번호 켜지 않기 · 교수님 확인 8번 미답)을 뒤집은 채 넘겼다 | 포도를 **껐다**(되살릴 한 줄은 주석) · 시험 기대값 되돌림 |
| 32 | 5 1차 | 사과 ③ 번호 하단 줄이 **원본 정답 번호를 «AI 초벌»** 이라고 불러, 사람이 «AI 가 만든 것이니 고쳐야겠다» 고 읽는다 | `ui.js` — `seed_source=gt_numbers` 일 때 «**원본 정답 번호**»(글자 예산 그대로) |
| 33 | 5 1차 | «사람 확정만 + 개수» 가 **빈 표**를 주는데 화면이 미리 말하지 않는다 | `ui.js expPlan()` 에 안내 한 줄(규칙은 그대로) |
| 34 | 5 1차 | 통합 빌더가 **복숭아 번호본을 몰라** 툴에서 번호를 확정해도 manifest 가 `count_source=none` 이다 | `build_merged_dataset.py SEED_DIRS` + 복숭아 · `instances_source=psm_watershed`(추정) |
| 35 | 5 1차 | 상자가 **0개인 옛 json** 이 0줄 YOLO txt 로 나가 검출 학습이 «열매 없음» 정답으로 읽는다 | `boxes.py export_boxes_to()` 가 건너뛴다(이름은 알려 준다) |

**사이클 5 2차가 그 위에 더 고친 8건**(결함 번호를 새로 붙이지 않고 §3 의 «닫힘 확인» 으로 둡니다 —
전부 «전 실패 → 후 통과» 증거가 있습니다):
① 🔴 **소수정 ④ 가 반만 닫혀 있었다** — 새 0줄 txt 는 안 만들지만 **옛 판이 만든 0줄 txt 가 고정 폴더
(`data/<과일>/boxes_yolo/`)에 그대로 남고** `stale_txt` 도 세지 않았다(실서버에 실제로 **2개**) →
`boxes.py` 가 그 옛 txt 도 «낡은 txt» 로 센다(지우지는 않는다) ·
② 풍선말에 마크다운 `**` 가 글자로 새어 나왔다(`ui.js taskConfLine()`) ·
③ 🔴 **정본 시험 4파일**이 다른 세션의 백업 이동(`_archive/backups_260920/`)으로 죽었다 → «없으면
아카이브에서 읽기» 폴백 ·
④ 🔴 처음 온 사람이 내보내기에서 막히는데 이유를 말하지 않았다(«세그 마스크» 가 기본으로 켜져 있다)
→ ««세그 마스크» 의 확정이 0장이어서 막았습니다 — 그 체크를 빼면 상자·개수만 나갑니다» ·
⑤ 통합 빌더 주석(L261)이 자기 코드와 **반대 말**을 했다 ·
⑥ 낡은 숫자·거짓 문장(125→124 · 1,668→1,667 · «아무도 확정 안 함») ·
⑦ Codex 두 파일의 «사람 확정 0장» 과 없는 경로 ·
⑧ 문서 세 개의 낡은 줄(`README` 145·197·619·620·1136~1143 · `help.html` 7·80·468 · 체크리스트 15→**18항목**).

**처음 온 사람 시나리오**(진짜 파이어폭스 1366×768 · 문서 없이 화면 문구만으로): **O 20 / △ 2 / X 0**
(고치기 전 12/5 → 고친 뒤 22/0). △ 2 = 내보내기에서 한 번 막히는 것 · 그때 받는 `counts.csv` 가 0줄인 것.
둘 다 «규칙» 이고 화면이 이제 그 이유를 말합니다.

## 4. 논문 근거 대조 결과

대조 자료: `platform/01_references/pdf/` **`*.pdf` 143편 전수**(폴더 파일 145개 중 2개는 `*_초록만.txt` —
실측 00:2x). 도구는 `pdftotext`. **근거를 못 찾은 것은 «근거 없음» 으로 적었고 지어내지 않았습니다.**

### 4-1. 집계 28칸 (표 §1 을 3차가 한 칸씩 다시 세었습니다 — 28칸 · 산술 일치)

| 판정 | 칸 수 | 어느 칸 |
|---|---:|---|
| **일치** | **11** | 1-b · 1-c · 2-a · 2-b · 2-c · 2-d · 4-a · 4-b · 4-c · 5-b · 5-c |
| **부분** | **7** | 1-a · 3-b · 5-a · 5-d · 6-b · 6-c · 7 |
| **어긋남** | **6** | 1-d(복숭아 CC) · 1-e(포도 CC) · 1-f(4↔8 연결) · 2-f(modal/amodal) · 3-a(라벨 범위 규칙 미정) · 3-c(그림 캡션 — **부분 해결**) |
| **근거 없음** | **2**(+1) | 2-e(`other` 클래스) · 6-a(2,073,600) · (+2-f 의 사과 쪽은 «어긋남» 과 겹쳐 셈) |
| **오지정 정정** | **1** | 5-e(`demsar2006` 은 누수 근거가 아니다 — Friedman 검정 근거로만) |
| **PDF 없음/미대조** | **1** | 6-d(SNIP · Amarnath · Ullah 3편) |
| **합계** | **28** | 11+7+6+2+1+1 (항목 1: 6칸 · 2: 6칸 · 3: 3칸 · 4: 3칸 · 5: 5칸 · 6: 4칸 · 7: 1칸) |

- 인용 **51건** 전부 `pdftotext` 로 **글자까지 일치** · **지어낸 인용 0건**(1차 51건 · 사이클 3 2차가 84건 재대조).
- 🔴 **표의 `p.N` 은 PDF 쪽입니다.** **6편 22건은 프리프린트라 논문에 옮길 수 없고**, `seo2024peach` 는
  **심사용 판**(4·5·7쪽 «FOR PEER REVIEW»)입니다 → **M19**(출판판 쪽 재확인)가 논문 인용 **전에**
  끝나야 합니다. MinneApple p.4 · `seo2024peach` p.5 · `maierhein2024metrics` p.204 세 쪽이 특히 그렇습니다.
- **문맥 이탈 4건**을 고쳤습니다: 4-b 의 «95.5~97.8%» 는 Table IV 가 아니라 **Table V(수확량)** ·
  1-e `zabawa` 는 **알 단위 + edge** 조건 · 7 의 `caicedo`·`santos` 는 **반자동 보조 도구** ·
  5-c `stride` 는 **촬영 단계**의 이야기였습니다.
- **쪽 번호 7건** 정정 · 판정 **뒤집기 4건**(1-e 일치→어긋남 · 5-a 일치→부분 · 7 «5편 일치»→3편 일치·2편 불일치 ·
  1-a 일치→부분).
- 🔴 **대조표 머리의 경고를 그대로 옮깁니다**: 이 근거들은 **사후 정당화**입니다. «논문을 보고 설계했다» 가
  아니라 «**이렇게 했는데, 알아보니 같은 방법이 문헌에 있더라**» 가 정확한 표현입니다.

### 4-2. 이 대조로 **고친 문서**

| 파일 | 무엇을 |
|---|---|
| `문서/260918_툴_방향_녹취기준.md` | «논문 근거» 절 — «AI 초벌 → 사람 확정» 이 문헌에 있는 방식임을 5편 인용문과 함께 |
| `platform/05_ai_dialogues/근거문서/260812_디텍션논문_근거조사.md` | §5 «아직 확인 못 한 것» 2건을 채움(포도 송이 단위 · 워터셰드 한계) |
| `platform/05_ai_dialogues/근거문서/260813_사진처리_전과정과_논문근거.md` | 마스크 NEAREST 줄에 `isensee2021nnunet` p.17 인용 · SNIP·Amarnath·Ullah 행에 «서버에 PDF 없음» |
| `문서/260813_내가_한_일과_이론_출처.md` | L88~89 «캡션에 낙과 미라벨 명시 ✅» → «관행은 ✅ 이지만 우리 캡션의 «사실» 은 ❌» · SNIP 재확인 못 함으로 정정 |
| `문서/260919_논문근거_대조표.md` | 규약 절 교체 · 판정 3칸 변경 · 문맥 이탈 4 · 실측 4 · 부록 C 2칸 닫음 · 부록 F·G·H |

🟡 **대조표에 남은 낱말 정리 6곳**(집계·본표는 최신이고, 칸 안의 **낱말**이 따라오지 않은 자리입니다 —
사이클 3 이 확정판으로 낸 문서라 이번에 손대지 않았습니다): 1-a 칸의 «일치» → «부분» · 1-a 의 «다른 점 4개» →
5개 · 3-c 의 «부분 해결» 옆에 «(집계에서는 어긋남)» · 7 을 «부분(3편 일치·2편 불일치)» 로 · 부록 C L251
«칸 10개 · 4-c 부분» → **12칸 · 일치** · 항목 요약 L152 «툴 쪽 2건» → **3건**(1-e 가 내려왔습니다) ·
부록 F L312·L325 에 각주. 더해 **«51건» 의 셈 규약 한 줄**(칸 × citekey × 쪽 · 5-d 의 `kapoor` 를 세면 52)이
필요합니다.

## 5. M1~M19 상태

| M | 무엇 | 상태 |
|---|---|---|
| **M1** | 툴 초벌을 팀 워터셰드 출력으로(포도는 CC/정답 송이) | ✅ **사이클 4 완료** · 사이클 5 가 **통합 빌더까지** 맞춤(복숭아) · 포도는 **끔**(교수님 8번) |
| **M2** | 4↔8 연결 통일 | 🟡 **부분** — 워터셰드가 CC 를 대체한 것은 **세 과일뿐**이고 **포도 2,406장은 지금도 `cc4`** 입니다(실측). `app/README.md` L1152~1155 도 «통일하지 않았다 · 4-연결이 더 정확 · 아직 정하지 않았다» 로 적혀 있습니다 — 그 문장을 그대로 두고 **상태를 내렸습니다**(2차 정정) |
| M3 | `counts.csv`·manifest 에 정답 개수 칸 + 지표 계산 | ✅ **완료**(`COUNTS_COLS` **12칸** 확인 · 사이클 4 `count_metrics.py`) |
| M4 | 상자 정의를 과일별 문장으로(«지금은 modal» 한 줄 금지) | ✅ **완료**(README §10 · help 카드) |
| M5 | `other`→`trunk` 또는 «선례 없음» 명시 | 🧑 **교수님 결정**(143편 전수 검색에서 선례를 **찾지 못했습니다**) |
| M6 | «포도 송이 상자는 알에 맞지 않는다» 한 줄 | 🟡 **부분** — README §16 에 **실측 문장은 있고**(붙은 송이 합쳐짐 · 가려진 송이 쪼개짐) `santos2020grapetracking` p.2 **인용 표기는 없음** |
| M7 | help «낙과·뒷줄 나무는 규칙 미정 — 손대지 말 것» | ✅ **완료**(README §10 + help 빨간 카드) |
| M8 | SNIP · Amarnath · Ullah **PDF 재확보** | 🧑 **사람(로컬 세션)** |
| M9 | 마스크 크기 변경 NEAREST ↔ one-hot+argmax | 🧑 **0920 재빌드 결정**(교수님) |
| M10 | `demsar2006` 을 누수 근거로 쓰지 않기 | ✅ **문서 반영**(대조표 5-e) · 논문 집필에서 지킬 것 |
| M11 | 교수님확인 2·9·10 에 **원논문 근거**를 붙여 재상정 | 🧑 **사람** — 근거는 준비됨(MinneApple p.4 · `seo2024peach` p.5) |
| M12 | 그림 3종 재생성 + 근거 PDF 정정 | ✅ **완료**(그림 mtime 18:50 / 20:27 · 근거 PDF 18:50 실물 확인) |
| M13 | 통합 README 분할 각주(포도는 사진 한 장 = 사실상 무작위) | ✅ **완료** |
| M14 | 포도 `n_gt` = CERTH 송이 · «자동으로는 못 센다» 를 결과로 싣기 | ✅ **코드 전수 확인 완료**(2,502장 전부 일치 · 합계 9,832). 🔴 사이클 3 3차 **완화 결정**: 포도를 개수 지표에서 **빼지 않고 정답 송이로 쓰되, «자동 계수 불가» 를 결과로 함께 싣는다 |
| M15 | 매개변수는 **사과 정답으로 골랐고** 숫자는 **정답 마스크 입력 = 상한** | ✅ **완료 3곳**(README §20-2 · 재학습 계획 §11 · 빌더 README) |
| M16 | `n_team_park` ↔ `n_team_im` 은 **바이트까지 같은 파일** | ✅ **README 반영** · 🧑 **임성후 님 확인이 먼저**(칸을 줄이는 것은 그 뒤) |
| M17 | MAE·RMSE·R² 계산 스크립트 | ✅ **완료**(`tools/count_metrics.py` · `--selftest` 5건 통과) |
| M18 | **예측 마스크로 센 값이 없다**(모든 개수가 `mask_source=gt`) | ⏭ **재학습 단계**(0920 이후) |
| M19 | `seo2024peach` 출판판 재확보 · 프리프린트 5편 쪽 확인 | 🧑 **사람 — 논문 인용 전 필수** |

집계: ✅ **완료 10** · 🟡 **부분 2**(M2 · M6) · 🧑 **사람·교수님 6**(M5 · M8 · M9 · M11 · M16 후속 · M19) ·
⏭ **뒤 단계 1**(M18) = 19. (M16 은 ✅ 와 🧑 를 함께 달고 있고 집계에서는 **사람 쪽으로만** 셉니다.)

## 6. 카운팅 지표 표

`tools/count_metrics.py` · **포도 번호를 끈 판**(= 지금 실서버와 같은 설정).
식은 `farjon2023countingreview` **PDF p.16 = 인쇄 p.1698** 의 식 (1)~(4) 그대로입니다
(`e=y−ŷ` · `MAE=Σ|e|/n` · `RMSE=√(Σe²/n)` · `R²=1−Σe²/Σ(y−ȳ)²`). 총량편향% = `Σŷ/Σy−1`
(farjon 식이 아니라 박성문 님 `eval/summary.md` §7.2 가 쓰는 값). 손계산 3장과 일치하고 `--selftest`
5건이 통과합니다.

🔴 **이 표는 2026-09-20 00:1x 에 실자료로 다시 돌린 값입니다**(`cycle_5/stage3/metrics_now.md`).
1차 보고서의 표와 **복숭아 세 줄과 꼬리줄이 다릅니다** — 2026-09-19 22:55:23 에 사람이 복숭아 한 장을
«문제 있음» 으로 확정해 그 사진이 데이터셋에서 빠졌기 때문입니다(**125 → 124장**).

| 과일 | 출처(무엇으로 센 값인가) | 사진 n | 정답 합계 | 센 합계 | 총량편향% | MAE | RMSE | R² |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 복숭아 | 사람이 저장한 상자 | 1 | 4 | 0 | −100.00 | 4.00 | 4.00 | — |
| 복숭아 | **열매 번호**(= 툴 초벌 = 박성문 워터셰드) | **124** | **975** | **937** | **−3.90** | **0.31** | **0.73** | **0.986** |
| 복숭아 | 박성문 자동 상자 | 124 | 975 | 937 | −3.90 | 0.31 | 0.73 | 0.986 |
| 복숭아 | 임성후 자동 상자 | 124 | 975 | 937 | −3.90 | 0.31 | 0.73 | 0.986 |
| 포도 | 사람이 저장한 상자 | 1 | 9 | 0 | −100.00 | 9.00 | 9.00 | — |
| 포도 | 박성문 자동 상자 | 2,403 | 9,424 | 14,546 | **+54.35** | 2.43 | 3.58 | **−1.406** |
| 포도 | 임성후 자동 상자 | 2,403 | 9,424 | 14,546 | +54.35 | 2.43 | 3.58 | −1.406 |
| 사과 | **열매 번호**(원본 마스크의 정답 번호) **⚠순환** | 429 | 17,567 | 17,567 | +0.00 | 0.00 | 0.00 | 1.000 |
| 사과 | 박성문 자동 상자 | 429 | 17,567 | 17,700 | +0.76 | **2.14** | 3.09 | **0.985** |
| 사과 | 임성후 자동 상자 | 429 | 17,567 | 17,700 | +0.76 | 2.14 | 3.09 | 0.985 |

꼬리줄(실측): «읽은 줄 **4,070**개 중 **정답이 없어 뺀 줄 1,114개** · 표 4개».
**블루베리는 표에 없습니다** — 정답 상자(`gt_boxes`)가 아예 없습니다(1,114줄 전부 제외). 논문에도 그렇게
적을 것. 포도 번호 초벌을 켜면 «포도 / 열매 번호 **⚠순환** 2,403 · 9,424 · 9,424 · +0.00 · 0.00 · 0.00 ·
1.000» 한 줄이 더 붙습니다(그것이 켠 판과 끈 판의 **유일한 차이**).

🟡 «복숭아 / 사람이 저장한 상자 n=1» 과 «포도 / 사람이 저장한 상자 n=1» 두 줄은 **시험 잔재 json 2개**가
만드는 줄입니다. 사람이 그 두 파일을 지우면 두 줄이 함께 사라집니다(§8 삭제 목록 2·3).

**«⚠순환» 과 «⚠오차0» 의 뜻**(스크립트가 표 아래에 직접 적습니다)

- **⚠순환** = 센 값과 정답이 **같은 파일**에서 나온 줄. 알려진 세 경우는 ① 사과 원본 번호 ↔ 그 마스크로
  만든 `gt_boxes` ② 포도 CERTH 송이 ↔ 같은 송이로 만든 `gt_boxes` ③ 통합본 `n_boxes` ↔
  `boxes_source=psm_gt` 입니다. MAE 0 · R² 1.000 은 정확도가 아니라 «**두 경로가 어긋나지 않는다**» 는
  확인입니다. 논문에 «우리 방법이 오차 0» 으로 쓰면 **사후 정당화**입니다.
- **⚠오차0** = 오차가 0 이지만 순환이라 **단정하지 않는** 줄. ① 순환일 수도 ② 표본이 적어 **사람이 정말
  다 맞힌** 것일 수도 있습니다 → `gt_source`·`human_source`(또는 `boxes_source`·`instances_source`)로
  확인한 뒤 쓰십시오. (사이클 4 2차가 반례를 만들어 잡은 자리입니다 — 사람이 두 장 다 맞힌 표에 «같은
  파일에서 나왔습니다» 라는 **거짓 설명**이 달렸습니다.)

🔴 **논문 표에 쓸 때 주의 2가지**

1. **복숭아·블루베리에서 «열매 번호»·«박성문»·«임성후» 세 줄은 같은 값입니다** — 124장 전부에서
   `n_instances == n_team_park`(M1 이 툴 초벌을 박성문 워터셰드로 바꿨으니 당연합니다). 표에서는 세 줄이
   같은 숫자로 나란히 보여 «세 방법이 일치한다» 로 오해하기 쉽습니다 — **한 번의 측정이 세 줄로 보이는
   것**입니다. (실측 00:1x — 복숭아 **124/124** · 블루베리 **1,114/1,114** 가 같고, **사과는 330장**에서
   번호와 팀원 상자 값이 다릅니다. 포도는 번호가 꺼져 있어 이 표에 «열매 번호» 줄이 없습니다 — 사이클 4 가
   켠 판에서 재었을 때는 1,857장이 달랐습니다.)
2. **사과 표는 1,001장이 아니라 429장**이고 정답 합계도 40,468 이 아니라 **17,567** 입니다(툴이 근접중복
   대표만 들고 있습니다). 캡션에 «사과 429장(대표 1장 기준)» 과 «**정답 마스크 입력 기준 = 상한**»(M15)을
   함께 적어야 합니다.

🔴 **명령 주의**: `--counts` 는 `nargs="+"` 라 **폴더 네 개를 한 번에** 주어야 합니다
(`--counts A --counts B …` 로 주면 **마지막 하나만** 읽습니다). 그리고 폴더에 `manifest.csv` 가 함께 있으면
그것까지 읽어 **표가 8개**가 됩니다 — `counts.csv` 만 담은 폴더를 주십시오.

## 7. 열린 문제

**2차가 3차에 넘긴 설계 8갈래 — 3차 결정: 전부 «다음 주 / 재학습 단계» 로 미룹니다. 지금 데이터가
사라지는 자리는 없습니다.** (하나하나 «지금 고칠까» 를 따졌고, 0920 재빌드를 앞두고 코드를 더 흔드는
비용이 이득보다 컸습니다.)

| # | 자리 | 선택지 | 3차 결정 |
|---|---|---|---|
| 1 | 화면 precount(상자 **파일 수**) ↔ 실제 txt 수 | ① 그대로 ② `box_set()`·`export_caps()` 가 상자 json 을 다 열어 줄 수를 센다 | **①** — 어긋나는 사진은 시험 잔재 2개뿐이고, 사람이 지우면 사라집니다. ②는 수천 장에서 `/api/export_plan` 이 느려집니다 |
| 2 | 복숭아 `instances/` 가 «추정» 인 것을 코드가 강제하지 않는다 | ① 그대로 + Codex 질문 ② 빌더가 `<과일>/instances/` 에 «추정입니다» 한 줄을 파일로 같이 쓴다(3줄) ③ 폴더를 `instances_est/` 로 | **①(②는 0920 전 언제든)** — ③은 반대(지난 판과 대조가 끊깁니다). 지금 `datasets_merged` 를 읽는 학습 코드가 **0건**이라 위험이 실현될 자리가 없습니다(실측) |
| 3 | `instances_source` 가 한 폴더에 두 이름(`detect_seed`·`psm_watershed`) | ① 그대로 + 값 사전 ② 하나로 합친다 | **①** — 합치면 지난 빌드와 대조가 끊깁니다. 필요해지면 코드에 «추정 집합» 한 줄로 충분합니다 |
| 4 | 복숭아·블루베리 번호를 화면이 «**AI** 초벌» 이라 부르는 것(출처는 박성문 님 워터셰드) | ① 그대로 ② «팀 초벌 번호» 로 ③ `seed_source` 가 `team:*` 일 때만 «팀 초벌» | **③ 을 다음 사이클에** — 사과에서 쓴 방식과 같고 글자 수도 같습니다(«AI 초벌 »↔«팀 초벌 »). 이번에는 고치지 않았습니다 |
| 5 | «사람 확정만» 이 종류마다 다른 것(마스크=`confirmed` · 상자=`confirmed_boxes` · 번호=`confirmed_instances`) | ① 그대로 ② 상자·번호 쪽에도 사이클5 ② 같은 안내 한 줄 | **② 를 다음 사이클에** — 규칙은 그대로 둡니다. 지금은 «개수» 쪽만 안내가 있어 균형이 맞지 않습니다 |
| 6 | `t6_view` 남은 실패 4건(문턱 «바뀐 화소 ≥ 0.5%») | ① 그대로 ② 전경 비율 대비 문턱 ③ 전경이 큰 사진으로 고정 | **③ 을 다음 사이클에** — 코드 결함이 아니고 흔들리지도 않습니다(139/4 재현) |
| 7 | 시험이 «125» 처럼 **살아 있는 자료 수**를 글자로 박아 두는 것 | ① 그대로 ② 툴이 말하는 수와 맞춘다 | **②** — 2차가 자기 사본에 이미 했습니다. 1차 사본(`cycle_5/stage1/{c5_fix,b2_export}.py`)에도 옮길 것 |
| 8 | 🔴 **시험 도구 기본 주소가 실서버**(`scripts/ff.py:16` → `http://127.0.0.1:5111`) · `s5_live_guard.py` 를 그냥 돌리면 `part_b` 가 실서버에 붙는다 | ① 규칙으로만 지킨다 ② `ff.py` 기본값을 «없으면 죽는 값» 으로 ③ `part_b` 를 `sys.argv` 로만 켠다 | **②+③ 을 «구조 정리» 사이클에** — 공용 도구라 이번에 손대지 않았습니다. **다음 사이클이 실수로 실서버에 쓰기를 보내는 유일한 통로입니다** |

**그 밖에 남은 틈**

- `runall_c5.sh` 로 `b5_oldserver` 가 죽습니다(그 시험이 쓰는 `sandbox_old4` 를 만드는 줄이 runall 에 없음).
  2차가 자기 사본에 `sync_old4()` 한 줄을 넣었습니다 — 1차 사본에도 옮길 것.
- 대조표에 남은 낱말 6곳(§4-2 끝) + «51건» 셈 규약 한 줄.
- 백업을 옮길 때 `grep '_backup_'` 을 **시험 코드에도** 돌려야 합니다. 폴백은 전부 «없으면
  `_archive/backups_260920/<원래 상대경로>` 에서 읽기» 한 가지 꼴이라 **아카이브 폴더 이름을 또 바꾸면
  그 네 줄이 다시 죽습니다.**
- 「번호를 고치면 마스크 확정도 풀어야 하나」 — 지난 5사이클에서 넘어온 설계 질문(교수님 ⑤), 아직 독립입니다.

## 8. 사람이 결정할 것

**교수님(안홍렬) — 6가지**

1. **modal / amodal 을 어느 쪽으로 통일할까.** 지금은 **사과 한 과일 안에서 섞여** 있습니다 — MinneApple
   사진은 가려진 데까지 원으로 통째(**«의심이 큰 상위 150개체» 표본 중 92개** · 치우친 표본이므로 전수로
   단정하지 않습니다), dataset1~3 은 보이는 것만. 복숭아는 원논문이 **amodal 이라고 명시**
   (`seo2024peach` p.11)하고, **MinneApple 원문에는 폴리곤 범위를 적은 문장이 아예 없습니다**(대조표 2-f).
2. **포도 «송이» 의 정의**(교수님 확인 8번 · 아직 답 없음). 이것이 오기 전까지 포도 번호 초벌은 **끈 채**로
   둡니다(`app/instances.py` 주석 한 줄만 되살리면 켜집니다). 실측: 닿은 송이 **31.4%** · 조각난 송이
   **43.5%** · 자동 계수 CC **+62.1%** / 워터셰드 **+123.5%** → **자동으로는 못 셉니다.**
3. **클래스 `2 other` 를 `trunk` 로 바꿀까.** **143편 전수 검색에서 `other` 를 쓴 선례를 찾지 못했습니다**
   (정규식 4종의 부재 증명입니다 — «없다» 를 증명한 것이 아니라 «찾지 못했다» 입니다). 남기면 README 에
   «논문 선례 없음 — 학습에 쓰지 않음» 을 적겠습니다.
4. **낙과(땅에 떨어진 과실)를 빼자는 권고** — 질문이 아니라 **근거 있는 권고**로 올립니다: MinneApple(p.4)과
   `seo2024peach`(p.5) **두 원저자가 모두 낙과를 뺐습니다**(*"except for dropped fruits"*). 우리는 규칙이
   없어 **낙과가 보이는 42장 중 5장만** 라벨돼 있습니다(**2차 검수의 표본 값이고 전수가 아닙니다**).
   빼기로 하면 남은 17장은 사람이 다시 손봐야 합니다.
5. **임성후 님 산출물 확인**(M16) — `im_seonghu/bbox_outputs` 가 박성문 님 것과 **4,823개 파일 내용까지
   동일**합니다(inode 는 다릅니다 = 복사본). 두 칸을 «서로 대조» 로 쓰면 **없는 독립성을 주장**하는 것이
   됩니다. 본인 산출물인지 확인한 뒤 칸을 하나로 줄일지 정해야 합니다.
6. **포도 번호 초벌을 켤까**(2번과 한 묶음) · **복숭아 `instances/` 를 «추정» 으로 통합본에 넣는 것**을
   그대로 두어도 되는지(0920 빌드부터 **124장**에 생깁니다 · 값 `psm_watershed`).

**박성문 — 3가지**

⑦ 블루베리 **정답 상자**(지금은 워터셰드 추정 1,114장 · 그래서 블루베리가 지표 표에 없습니다)
⑧ 상자 YOLO 폴더 구조 · 클래스 번호
⑨ `bbox_outputs/peach/all/instance_maps`(PNG 125장)를 통합 데이터셋의 `instances/` 로 쓰는 것에 동의하는지.
  덧붙여 `FALLBACK["grape"]="cc"` 는 그 폴더가 아니라
  **`park_seongmoon/semantic-segmentation/tools/instance_split.py:47`** 에 있고, 사이클 3 2차가 요구한
  «CC 는 쪼개지 않기 위한 차선이고 옳은 세기가 아니다(+62.1%)» **주석은 아직 없습니다**(박성문 님 파일이라
  우리가 고치지 않았습니다).

**곽동신**

- 🔴 **이름 칸에 이름을 적어 주십시오.** 2026-09-19 22:55:23 의 확정 1건이 `by: "익명"` 으로 남았습니다.
  누가 확정했는지가 `counts.csv`·manifest 의 `confirmed_by` 로 그대로 나갑니다.
- **삭제 목록**(툴·데이터를 건드리는 삭제라 3차가 하지 않았습니다):

| # | 무엇 | 왜 · 무엇이 함께 사라지나 |
|---|---|---|
| 1 | `260916_라벨링툴/app/cache/instance_counts/peach.json` | 시험이 남긴 **가짜 항목 5개**(`c01_both_same`·`c02_both_diff`·`c04_inst_only`·`c08_conf_nofile`·`c09_inst_nocache`)뿐이고 **실제 사진은 한 장도 없습니다**(실측 00:2x). 지우면 복숭아를 열 때 한 장씩 다시 셉니다(한 장 66 ms) |
| 2 | `260916_라벨링툴/data/peach/boxes/210629-t4-17.json` | 상자 **0개**인 시험 잔재. 지우면 지표 표의 «복숭아 / 사람이 저장한 상자 n=1 · MAE 4.00» 줄과 «건너뛴 사진 1장» 경고가 함께 사라집니다 |
| 3 | `260916_라벨링툴/data/grape/boxes/740.json` | 같은 것(상자 0개). 지우면 «포도 / 사람이 저장한 상자 n=1 · MAE 9.00» 줄이 사라집니다 |
| 4 | 🔴 `260916_라벨링툴/data/peach/boxes_yolo/210629-t4-17.txt` · `data/grape/boxes_yolo/740.txt` | **0바이트 = 검출 학습이 «열매 없음» 정답으로 읽는 파일**(둘 다 실서버에 그대로 있습니다). 위 json 만 지우면 이 txt 는 «낡은 txt» 로 경고에만 뜨고 **파일은 남습니다** — **둘 다** 지워야 합니다 |
| 5 | `platform/work/kwak_dongsin/260919_c5_fix_placeholder.txt` | 사이클 5 1차가 실수로 만든 12바이트 파일. 지워도 아무 영향이 없습니다 |
| 6 | 모래상자 폴더 — `cycles/260919_count/cycle_5/stage1/{sandbox*, sb_chars_*, c4_cnt5/sandbox*, reg_cnt5/sandbox, mergebuild*, t4_boxes_*}` · `cycle_5/stage2/` 와 `cycle_5/stage3/` 의 같은 것 · `cycle_4/stage2/{sandbox*, onoff_*, c4_cnt4b, reg_cnt4b, mergebuild2, counts_*}` | 시험용 사본입니다(각각 수백 MB). **로그·json·md 는 남기십시오** — 그것이 증거입니다 |

- **`scripts/ff.py` 를 쓰는 시험을 돌리기 전에 `LABELTOOL_URL` 을 반드시 지정하십시오.** 기본값이
  **실서버 5111** 입니다(§7 열린 문제 8).

## 9. 숫자 사전 (실측·출처)

| # | 숫자 | 뜻 | 출처 |
|---|---|---|---|
| 1 | **35건** | 5사이클이 고친 결함 = **코드 34 + 시험 기대값 1** (1: 11 · 2: 0 · 3: 0 · 4: 20 · 5: 4) | **§3 표를 세어 만든 값**(사이클별 판정서에는 이 합계가 없습니다) |
| 2 | **28칸** | 논문 근거 대조표(일치 11 · 부분 7 · 어긋남 6 · 근거 없음 2 · 오지정 1 · PDF 없음 1) | `문서/260919_논문근거_대조표.md` §1·§2 — 3차가 한 칸씩 다시 셈(00:2x) |
| 3 | **51건 / 0건** | 글자까지 대조한 인용 / 지어낸 인용 | 대조표 부록 A·H |
| 4 | **143편** | `01_references/pdf/` 의 `*.pdf` 전수(폴더 파일 145개 중 2개는 `*_초록만.txt` · 1차의 «145» 는 그래서 생긴 오기) | `ls` 실측 00:2x |
| 5 | **125 → 124** | 0920 빌드의 복숭아 열매 번호 장수 — **번호본이 있는 사진은 125장**이고, 한 장(`210629-t2-of13-03`)이 09-19 22:55:23 에 «문제 있음» 으로 확정돼 빠집니다 | `--dry-run` 00:1x · `status.json` |
| 6 | **1,543 → 1,667** | 통합본 «열매 번호» 합계(사과 429 + 블루베리 1,114 + **복숭아 124**) | `문서/260918_통합데이터셋_갱신법.md` §5① |
| 7 | **0.31 / 0.73 / 0.986** | 복숭아 번호 초벌 MAE·RMSE·R² — **124장 기준(2026-09-20 00:1x)**. 옛 툴 CC 와 비교한 **MAE 0.70 → 0.30** 은 `app/README.md` L1178 의 **125장 기준** 쌍입니다 | `cycle_5/stage3/metrics_now.md` · README §20 |
| 8 | **2.14 / 3.09 / 0.985** | 사과 팀원 자동 상자 MAE·RMSE·R²(429장 · 정답 17,567) | 같은 표 |
| 9 | **+54.35% · R² −1.406** | 포도 팀원 자동 상자 — **자동 계수 실패**(정답 송이 9,424) | 같은 표 |
| 10 | **1,114줄** | 정답이 없어 지표에서 뺀 줄 = 블루베리 전부(읽은 줄 **4,070** 중) | 같은 표 꼬리줄 |
| 11 | **확정 1장 / 0장** | 판정(flag) 확정 **1장**(09-19 22:55:23 · `by: 익명`) · 학습에 쓸 세 확정 칸(마스크·상자·번호)은 네 과일 **0장** | `data/*/status.json` 00:2x |
| 12 | **PID 1467097 · 2026-09-20 00:04:44** | 지금 실서버(이 사이클 3차가 켠 것) | `app/logs/server.pid` · `ps -o lstart` 00:0x |
| 13 | **16:41:06~09 · 21:45:04~07 · 00:04:44** | 이번 5사이클의 실서버 재시작 **세 번**(사이클 1 · 4 · 5) | 각 3차 판정서 · `server.log` |
| 14 | **전 11 실패 → 후 22/0** | 소수정 4 전/후 시험(2차도 같은 값으로 재현 — 단 «전» 판은 `lib_cnt5.sync_before()` 로 만들어야 합니다) | `c5_fix_{before,after}.log` |
| 15 | **162 / 267 / 225자 · 증감 0자** | 편집 화면 글자(마스크/상자/번호 · 1366×768). 마스크는 사진에 따라 155~162 | `b3_chars` |
| 16 | **205개 / 4곳** | 다른 세션이 아카이브로 옮긴 백업 / 그 때문에 죽은 시험 — «4곳» 은 **사이클 5 가 실제로 돌리는 범위**입니다. `_archive/backups_260920/INDEX.md`(23:04)는 전 사이클 기준으로 «죽는 시험 **45개**» 라고 적습니다 | `INDEX.md` · 2차 §7-3 |
| 17 | **1,337 / 2,664 / 491 / 2,591줄** | `app/static/ui.js` / `app/static/app.js` / `app/boxes.py` / `semantic-segmentation/tools/build_merged_dataset.py`(2차가 고친 뒤 · 실측 00:2x) | `wc -l` |
| 18 | **9,832송이 / 2,502장** | 포도 CERTH 정답 송이 — **원본 전체 기준**입니다. 지표 표의 «9,424 / 2,403장» 은 **툴이 보는(근접중복 대표만) 기준**입니다 | 사이클 3·4 · `count_metrics` |
| 19 | **O 20 / △ 2 / X 0** | 처음 온 사람 시나리오(진짜 파이어폭스 1366×768) | `cycle_5/stage2/scn_newcomer.json` |
| 20 | **18항목** | 크롬 체크리스트(사이클 5 가 3항목 추가: 사과 «원본 정답 번호» · 개수 csv 안내 줄 · 상자 0개 건너뜀) | `문서/260917_툴_사람확인_체크리스트.md` |
