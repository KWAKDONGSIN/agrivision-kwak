작성: 2026-09-18

최종 수정: 2026-09-18 (사이클 5 **2차 검수**가 숫자·주장을 근거와 대조해 고침 · 3차 판정 대기)

# 라벨링 툴 **화면(UI) 개선** — 5회 검수 최종 판정 (팀원용) 〔초안〕

2026-09-17 저녁부터 2026-09-18 새벽까지 «작업(Opus 5) → 검수(Opus 5) → 판정(Fable 5.1)» 을 한 사이클로 **다섯 번** 돌렸습니다.
주제는 0915 랩미팅의 «**굉장히 편하게** 이렇게 할 수 있는 … **일일이 우리가 할 수는 없었고**» 였습니다.
이 문서는 **1차 작업자가 쓴 초안**이고, 사이클 5 의 2차 검수와 3차 판정(Fable)이 손본 뒤 확정됩니다.
원본 보고서(증거·경로 전부): `platform/work/kwak_dongsin/260916_라벨링툴/cycles/260917_ui/cycle_5/stage1_work.md`
새 기능 두 개(상자·열매 번호)의 판정서는 따로 있습니다: `문서/260917_상자와번호기능_5회검수_최종판정.md`

## 1. 한 줄 결론 — 지금 써도 되나

**됩니다. 이제 «처음 온 사람이 설명 없이 한 장을 검수할 수 있는» 화면이 됐습니다.**
단 **크롬에서 눌러 본 사람이 아직 아무도 없습니다** — 그것만 먼저 해 주세요.

| 무엇 | 지금 | 조건 |
|---|---|---|
| ① 마스크 검수(네 과일) | **써도 됨** | 없음 |
| ② 상자 그리기(네 과일) | **써도 됨** | 내보낸 YOLO txt 는 **교수님의 클래스 번호 결정 전에는 학습에 넣지 말 것**(새 기능 판정서 5번) |
| ③ 열매 번호(사과·블루베리) | **써도 됨** | 새 기능 판정서의 «블루베리 번호 저장 보류» 는 **N6 보충 수정으로 풀린 것으로 봅니다** — 그 시험(`n6_save.py`)이 다섯 사이클 회귀에서 계속 통과합니다. ⚠️ 다만 **새 기능 판정서(`260917_상자와번호기능_…`)에는 아직 «보류» 로 적혀 있습니다** — 두 문서 중 어느 쪽을 확정할지는 **3차 판정이 정합니다**(2차 검수는 시험 통과만 확인했고 문구를 고치지 않았습니다) |
| 거의 같은 사진 묶음 정리 | **써도 됨** | 지금은 네 과일 모두 «새로 제외할 사진 0장» — 확인창이 뜨면 «취소» 하고 알려 주세요 |
| «제외» 사진을 고쳐 저장 | **써도 됨** | 그림만 저장되고 **판정은 «제외» 그대로**입니다. 되살리려면 `1`. ②③ 작업 중에는 «수정본 저장» 이 회색입니다(2차 검수가 고침) |
| 노트북 화면(1366×768 등) | **써도 됨** | **세로 614px 이상(1100×614 기준)**에서 쓰세요 |
| 크롬·엣지·휴대전화 | **미확인** | `문서/260917_툴_사람확인_체크리스트.md` 로 확인 부탁드립니다 |

다섯 사이클이 고친 결함은 **20묶음(세부 34건)** + 사이클 5 2차가 찾아 고친 **4건**, 그중
**사람이 한 일이나 AI 검수 판정이 사라질 수 있던 것이 7건**입니다.
**실제로 잃은 데이터는 지금까지 0** 입니다. 다만 «전부 실서버에 나가기 전에 잡았다» 는 **사실과 다릅니다** —
7건 중 여러 건(동시 저장 소실·일괄 되돌리기·번호 저장 덮어쓰기)은 **2026-09-16~17 동안 실서버에 이미 올라가 있었고**,
그 길을 밟은 사람이 아직 없어서 손실이 0인 것입니다(2차 검수가 원본 보고서와 대조해 정정).
지금 코드에 알려진 미수정 결함은 **가벼운 것 1건 + 구조적 한계 2건**(아래 3)입니다.

## 2. 다섯 사이클이 바꾼 것

| 사이클 | 그전에는 | 지금은 |
|---|---|---|
| 1 | 판정 단추 5개가 1366×768 에서 **0개** 보임 · 목록 카드가 38px 로 찌그러짐 · 안내 창 위에서 누른 `1` 이 뒤 사진에 **진짜 판정**을 냄 | 화면 첫 정비 · «지금 할 일» 3칸(①마스크 ②상자 ③번호) · 사진마다 «이 사진에서 할 일» 한 줄 · 안내 창이 키를 막음 |
| 2 | 판정 단추를 찾으려면 옆 패널을 굴려야 함 · «할 일» 경고가 사과 10장 중 8장에 뜸 | **판정 5단추를 사진 바로 아래 가로 줄로** · 경고 문턱을 과일마다 실측(그 과일의 20%만) · 레이어 칸을 4줄로 접음 |
| 3 | 중복 사진을 한 장씩 다섯 번 눌러 제외해야 함 · 개수(«몇 개인가»)가 화면에 없음 | 묶음 **«대표만 남기고 제외» 한 단추** + 되돌리기 · 의심 종류별 단추 · 진행 현황에 **열매 개수** · 목록에 상태 딱지 |
| 4 | 묶음 칸이 화면 밖(옆 패널을 1,331px 내려야 나옴) · 할 일 줄에 영어(`fg_too_low`) · 노란 점선이 30×35화소 | 묶음 칸을 **오른쪽 맨 위**로 · 의심 종류를 **한국어 6가지**로 · **«후보 자리로 확대»** 단추 · 처음 안내 6단계 |
| 5 (이번) | ②③ 의 **«저장» 단추가 다섯 창 전부에서 화면 밖** · 색 설명이 사진 왼쪽 위 붓질을 막음 · **«제외» 사진을 고쳐 저장하면 제외가 풀려 데이터셋에 되살아남** | 저장 단추를 **판정 줄로** · 색 설명을 **처음에는 접힌 채** · **제외는 마스크 저장이 뒤집지 않음** · 캔버스에 «② 상자 그리기 중» 안내 |
| 5 (2차 검수) | ② 에서 «수정본 저장»·«상자 저장» 이 **둘 다 «(Ctrl+S)» 라고 적힌 채 나란히** 살아 있었음 · 메모에 덧붙이면 옛 메모가 **한 번 더** 붙음 · 묶음 제외를 되돌린 사진이 «수정본 되돌리기» 로 **제외로 되살아남** · 번호를 저장한 사진은 «방금 제외한 것 되돌리기» 가 **못 되돌림** | ② 에서 «수정본 저장» 을 **회색으로**(③ 과 같은 방식) · 메모 이어 붙이기 규칙을 한 줄로 고침 · 되돌리기가 `prev` 도 치움 · 되돌리기 판별을 `src` 표식 하나로 · 초벌 0개면 **원본 GT 로 한 번 더**(N-L) · 로그인 제한이 **틀린 시도만** 셈(N-M) |

### 사람이 한 일이 사라질 수 있던 결함 7건 (전부 고쳤고, 실제 손실 0)

1. 안내 창 위에서 누른 키가 **뒤 사진에 진짜 판정**을 냄(검수자·시각까지 덮어씀) — 사이클 1
2. 두 사람이 **동시에 저장**하면 판정이 통째로 사라짐(46요청에서 40건 소실을 재현) — 사이클 3
3. «일괄 제외 되돌리기» 가 **AI 3회 검수의 제외 수백 장**을 지움 — 사이클 3
4. **번호 저장**이 판정·메모(문제 있음 사유·후보 좌표)를 덮어씀 — 사이클 3
5. 번호를 **두 번** 저장하면 그 사유가 사라짐 — 사이클 3
6. **«수정본 되돌리기»** 가 제외·문제있음 판정과 메모를 통째로 지움(되살릴 길 없음) — 사이클 4
7. **«수정본 저장»·«AI 제안으로 교체»** 가 제외를 풀어 데이터셋에 되살림 — **사이클 5(이번)**

### 이번 사이클이 잰 숫자 (측정값만)

| 항목 | 값 |
|---|---|
| 「고치기 전 → 고친 뒤」 같은 시험 | 제외 규칙 **16실패 → 0**(71통과) · 배치 **41실패 → 0**(160통과) |
| 다섯 창 × 세 작업 × 묶음/비묶음 | **25벌 전부** 저장 단추·판정 5단추가 스크롤 0 · 한 겹 |
| 캔버스가 화면에서 차지하는 높이 | **57.8 ~ 76.9%**(합격선 55%) |
| 색 설명(범례) 크기 | 펼침 662×28px(캔버스의 1.5~5.8%) → **접힘 49×24px**(0.1~0.4%). ⚠️ **0 은 아닙니다** — 접어도 캔버스 왼쪽 위 (8,8)~(57,32)화소는 «색 뜻 ▸» 칸이 먹습니다(2차 검수 실측, 다섯 창 전부 같은 자리) |
| 2차 검수가 다시 잰 것 | 다섯 창 × 세 작업 **234항목 중 233통과**(어긋난 1건 = 위 범례 잔여) · N-C 섞어 누르기 **53/53** · 되돌리기 섞기 **22/22** · 망가진 마스크 업로드 8종 **36/36** |
| 동시성 | 네 사람 몫 **260요청 전부 200** · 판정 소실 **0** · 서버 오류 **0** |
| 회귀 | **11/11**(modesim 31/31 · boxsim 50/50 포함) |
| 네 과일 × 세 작업 왕복 | **70/70** |
| 화면 풍선말 전수 | **89개** 중 낡은 글귀 0 |
| 검수 단계 | 5사이클 × 3단계 = **15** · **브라우저(크롬)로 본 사람 0** |

## 3. 남은 결함 · 구조적 한계

### (가) 사이클 5 1차가 남겼다가 **2차가 고친 것** — 이제 결함이 아닙니다

- ✅ **«마스크에서 초벌 생성» 이 0개** (N-L) — AI 제안이 빈 사진이면 **원본 GT 로 한 번 더** 만듭니다.
  실측: 포도 `740` 은 AI 제안 0개 / 원본 라벨 **10개** → 이제 10개가 나옵니다(`app.js` 2줄).
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
보안 관련 값·설정 세부는 공개본에서 생략했습니다.

### (나) 지금 남은 가벼운 결함 — 하나

- 🟡 **색 설명을 접어도 캔버스 왼쪽 위 49×24화소는 칩이 먹습니다.** 접기로 662×28 → 49×24 로 줄였지만
  **0 은 아닙니다**(2차 검수 실측: 다섯 창 전부 (8,8)~(57,32)화소). 그 자리에서 붓질을 시작하면 안 먹습니다.
  완전히 없애려면 색 설명을 **캔버스 밖**으로 옮겨야 하는데, 캔버스 높이 55% 합격선과 부딪칩니다
  → 토요일 Codex 질문 3번.

### (다) 고칠 수 있지만 **일부러 안 고친 구조적 한계** — 둘 (딥시크 확인에서도 같은 지적)

- 🟠 **같은 사진을 두 사람이 동시에 고치면 «나중에 저장한 쪽» 이 이기고, 경고가 전혀 없습니다.**
  버전 확인(«그 사이에 남이 고쳤습니다») 장치가 없습니다. 서로 **다른** 사진을 동시에 저장하는 소실은
  사이클 3·4 에서 고쳤고(브라우저 두 대 + API 506회 소실 0), 여기서 말하는 것은 **같은 한 장**입니다.
  → **사람 규칙으로 막습니다: 과일 또는 쪽(120장 단위) 범위를 나눠 맡으세요.** (`app/README.md`·사용법 «안 될 때» 에 적혀 있습니다)
- 🟠 **항목별 변경 이력(덧붙이기 로그)이 없습니다 — `status.json` 에는 «마지막 상태» 만 남습니다.**
  누가·언제(`by`·`at`)는 남고 일괄 작업 전 자동 백업(`_status_backup_*.json`)도 있지만,
  «이 사진의 판정이 어떻게 바뀌어 왔는지» 는 되짚을 수 없습니다. `update_status` **한 곳**에 jsonl 한 줄씩
  덧붙이면 되는 안전망이라 **다음 할 일 1순위**로 올려 둡니다(검수 없이 새벽에 끼워 넣지 않았습니다).

그 밖에 «고치지 않고 남긴 것»(코드 정리·낡은 문턱표·`kept` 칸 이름 등)은
원본 보고서 `cycle_5/stage1_work.md` ⑩ «열린 문제 전수표» 에 전부 적혀 있습니다.

> ⚠️ **2차 검수 정정**: 그 보고서는 «해결 44 / 미해결 17 / 사람 결정 5(합계 66)» 이라고 적었지만,
> 표의 행을 그대로 세면 **76행 = 해결 48 · 부분 2 · 미해결 22 · 사람 결정 3 · 정보성 1** 입니다.
> 중복 항목(«크롬 미확인» 이 사이클 1·5 에 두 번, 범례 문제가 C3·N8 과 C4·N-I 로 두 번 등)을
> 손으로 뺀 숫자로 보이지만 **무엇을 뺐는지가 적혀 있지 않아 재현할 수 없습니다.**
> 어느 숫자를 확정할지는 **3차 판정이 정합니다.** 그때까지 이 문서는 «66건» 을 쓰지 않습니다.

## 4. 아직 아무도 확인하지 못한 것

**(가) 🔴 크롬에서 눈으로 본 사람이 없습니다.** 서버에는 파이어폭스만 있습니다. 다섯 사이클 전부에서 반복된 유일한 구멍입니다.
→ `문서/260917_툴_사람확인_체크리스트.md`(2026-09-18 최종판). 맨 위 **«먼저 볼 것 5개»(5분)** 만이라도 먼저 해 주세요.
   **곽동신·최인훈.** 순서 권고: 먼저 볼 것 → C(«제외» 사진) → F(상자) → B(마스크) → E2·E3(번호) → D → G → A → H(되돌리기).

**(나) 휴대전화·태블릿.** 만들 때 **노트북·데스크톱만** 겨냥했습니다(세로 614px 이상(1100×614 기준)). 휴대전화는 안 봐도 됩니다.

**(다) 지나가 보지 않은 길**: 진짜 브라우저 **두 대**로 «제외 사진 저장» 을 동시에(이번은 서버 요청 4갈래·2갈래로 갈음) ·
   윈도우 배율 150% · 아주 긴 메모(300자 넘김) · 한 사진을 두 사람이 동시에 붓질(위 3-다).

   ✅ **이번에 시험해서 지운 것**: «마스크 업로드가 중간에 끊기면?» — 2차 검수가 모래상자에서
   잘린 PNG·빈 본문·칸 없음·글자 쓰레기·PNG 머리만 맞는 것·12MB 본문·크기가 다른 PNG **8종**을
   `/api/save` 에 보냈습니다. **8종 모두 4xx 로 거절**하고 **이미 있던 `masks_fixed` 파일과 판정이 한 글자도 안 바뀌었으며**
   서버 로그에 500·Traceback 이 **0** 이었습니다(`cycle_5/stage2/r3_api.py` P절 · 36/36).
   ⚠️ 시험하지 **못한** 것: 브라우저가 업로드 도중 진짜로 끊겼을 때(네트워크 절단) — 서버는 요청을 아예 못 받으므로
   같은 결과일 것으로 보지만 재보지는 않았습니다 → 토요일 Codex 질문 6번.

**(라) 문턱표가 낡았습니다.** «이 사진에서 할 일» 경고 기준은 **2026-09-17 의 데이터(4,727장)** 로 쟀습니다.
   오늘 새벽 데이터 재검수가 또 돌아 판정이 바뀌었으므로(사과 제외가 늘었습니다) 다시 재는 것이 좋습니다
   (`cycles/260917_ui/cycle_2/stage1/thresholds.py`).

## 5. 사람이 정할 것

**곽동신·최인훈 (화면을 써 보고 한 줄씩)**
1. 노란·주황 «이 사진에서 할 일» 경고가 **너무 자주** 뜨는가(세 장에 한 장쯤).
2. 사진 왼쪽 위 색 설명을 **처음부터 접어 두는 것**이 나은가, 펴 두는 것이 나은가.
3. ②③ 작업 중에 **메모 칸이 숨고 «수정본 저장» 이 회색이 되는 것**이 불편한가
   (판정 줄을 한 겹으로 지키려고, 그리고 잘못 눌러 «수정함» 이 되는 것을 막으려고 그렇게 했습니다).
4. 색 설명을 접어도 남는 **49×24화소**(위 3-나)를 없애려고 색 설명을 **캔버스 밖**으로 옮길지,
   그냥 둘지(옮기면 캔버스가 그만큼 줄어듭니다).

**교수님 (툴과 별개로 이미 올라간 것)**
5. 상자 클래스 번호 · 복숭아 amodal vs 윤곽 규칙 · 포도 송이 단위
   → 5번이 정해지기 전에는 **내보낸 YOLO 를 학습에 넣지 마세요.**
   복숭아·블루베리의 «무엇을 칠할지» 도 여기서 막혀 있습니다(툴 문제가 아닙니다).

## 6. 0915 랩미팅 요구 대비 — 충족표

| 녹취(07:28~10:27) | 화면의 어디 | 충족 |
|---|---|---|
| «**굉장히 편하게** 이렇게 할 수 있는» | 사진 아래 판정 줄(판정 5단추 + ②③ 의 저장 단추까지) · 한국어 «이 사진에서 할 일» · 처음 안내 6단계 | **충족**(다섯 창 × 세 작업 25벌에서 스크롤 0) |
| «**일일이 우리가 할 수는 없었고**» | 묶음 «대표만 남기고 제외» 한 단추 · 진행 현황의 일괄 제외 · 목록 «무엇부터 볼까» · «마스크에서 초벌 생성» · 열매 개수 자동 집계 | **충족** |
| AI 1차 → 사람 수정 | 빨강(원본)·파랑(AI)·초록(내 수정본) 겹쳐 보기 · `F`/`G` 로 덩어리째 가져오기 · 판정 `2` | **충족** |
| 카운팅과 세그멘테이션 둘 다 | «지금 할 일» 3칸 · 번호 없는 과일은 회색 + 이유 · 진행 현황의 열매 개수 | **충족** |
| 연속 스냅샷(거의 같은 사진) 걸러내기 | 목록 «거의 같은 사진(중복)» · 편집 화면 오른쪽 맨 위 묶음 칸(대표에 «남김») | **충족**(지금은 정리 끝) |
| «중간이 비어야 하는데 동그랗게» | 자동 점검이 잡아 «속이 찬 동그라미» 단추와 할 일 줄에 **한국어**로 | **충족** |
| «복숭아 안 된 것들 · 블루베리 없는 것들 추가» | 붓·다각형·스마트 채우기·`N` 새 번호 전부 됨 | **부분** — 무엇을 칠할지가 **교수님 결정 전**(툴 문제 아님) |
보안 관련 값·설정 세부는 공개본에서 생략했습니다.

**충족 7 · 부분 1 · 미충족 0.**

## 7. 쓸 때 지킬 것 (사람 규칙)

1. **오른쪽 위 «내 이름» 을 먼저 적으세요.** 안 적으면 «익명» 으로 기록됩니다(처음 판정 때 한 번 물어봅니다).
2. **고친 것을 남기는 것은 «수정본 저장»(Ctrl+S)·«AI 제안으로 교체»(2) 둘뿐입니다.** `1`·`3`·`4` 는 그림을 버립니다(물어봅니다).
3. **«제외» 사진은 그림을 고쳐도 «제외» 로 남습니다.** 되살릴 때만 `1`(원본 그대로 OK)을 누르세요.
   **남의 «제외» 를 함부로 풀지 마세요** — AI 3회 검수가 근거를 메모에 적어 두었습니다.
4. **②③ 이 켜져 있으면 붓질이 안 됩니다.** 사진 오른쪽 위에 «② 상자 그리기 중»·«③ 번호 편집 중» 이 보이면
   **① 마스크 검수** 를 눌러 돌아오세요. 그동안 **«수정본 저장» 은 회색**이고, `Ctrl+S` 는 그 작업의 저장
   («상자 저장»·«번호 저장»)으로 갑니다.
5. «중복 그룹 일괄 제외 적용»·지우는 명령은 **담당자만** 누릅니다.
6. **세로 614px 이상(1100×614 기준)**에서 쓰세요. 화면이 옛날 것 같으면 **Ctrl+F5**.
7. 장수·개수는 **«진행 현황» 탭의 값**이 정답입니다. 문서에 적힌 숫자는 금세 낡습니다.
8. 🔴 **같은 사진을 두 사람이 동시에 고치지 마세요.** 나중에 저장한 쪽이 이기고 **경고가 없습니다**
   → **과일 또는 쪽(한 쪽 120장)을 나눠 맡으세요.** (위 3-다)

## 8. 파일 위치

| 무엇 | 어디 |
|---|---|
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
| 🔴 실서버에 아직 안 실린 것 | 사이클 5 **2차 검수의 서버 수정**(`server.py` 4곳)은 파일에만 있고 **5111 은 아직 옛 코드로 돌고 있습니다.** 실서버 재시작은 **3차 판정이 판단**합니다 |
| 사람 확인 체크리스트 | `kds0206/문서/260917_툴_사람확인_체크리스트.md`(2026-09-18 최종판) |
| 이 판정서 | `kds0206/문서/260918_툴UI개선_5회검수_최종판정.md` |
| 새 기능 둘의 판정서 | `kds0206/문서/260917_상자와번호기능_5회검수_최종판정.md` |
| 사이클 1~5 보고서 전부 | `platform/work/kwak_dongsin/260916_라벨링툴/cycles/260917_ui/cycle_{1,2,3,4,5}/` |
| 열린 문제 전수표(표 76행 — 위 3 의 정정 참고) | 〃 `cycle_5/stage1_work.md` ⑩ |
| 2차 검수 보고서·시험 스크립트 | 〃 `cycle_5/stage2_review.md` · `cycle_5/stage2/r{1..4}*.py` · 스크린샷 `~/ff_shots/ui5/stage2/` |
| 담당자용 설명서 | `260916_라벨링툴/app/README.md`(최종 수정 2026-09-18) |
| 화면 속 사용법 | 툴 위쪽 «❓ 사용법·예시» = `app/static/help.html` |
| 시험 스크립트·증거 | 〃 `cycle_5/stage1/*.py` · 스크린샷 `~/ff_shots/ui5/`(31장) |
| 되돌리기(**2차 검수분만** 빼려면) | `app/_backup_260918_ui10_server.py` · `app/static/_backup_260918_ui10_{app.js,ui.js}` 를 되돌려 복사 |
| 되돌리기(**사이클 5 전체**를 빼려면) | `app/_backup_260918_ui9_*` · `app/static/_backup_260918_ui9_*` 를 되돌려 복사하고 `bash app/run.sh restart` |

## 9. 화면 파일이 얼마나 늘었나

2차 검수가 `wc -l` 로 다시 셌습니다(1차가 적은 숫자와 맞았고, 2차 수정분만 늘었습니다).

| 파일 | UI 사이클 1 시작 | 1차가 낸 판 | 지금(2차 포함) | 증감 |
|---|---|---|---|---|
| `app/static/app.js` | 2,008줄 | 2,250줄 | **2,255줄** | +247 |
| `app/static/ui.js` | **없었음** | 569줄 | **583줄** | +583 (화면 개선분 전용 새 파일) |
| `app/static/index.html` | 200줄 | 294줄 | 294줄 | +94 |
| `app/static/style.css` | 115줄 | 332줄 | 332줄 | +217 |
| `app/static/help.html` | 209줄 | 331줄 | (2차가 몇 줄 고침) | 약 +122 |
| `app/README.md` | 331줄 | 521줄 | (2차가 몇 줄 고침) | 약 +190 |
| 서버 파일 4개(`server`·`instances`·`boxes`·`maskio`) | 2,122줄 | 2,221줄 | **2,250줄** | +128 |
| **합계** | | +1,533줄 | **약 +1,581줄** | (2차 수정분 +48줄) |

※ «UI 사이클 1 시작» 은 프런트엔드는 `_backup_260917_ui1_*`, 서버 파일은 UI 사이클 3 2차 시작 판입니다
(UI 사이클 1·2 는 서버를 건드리지 않았습니다).

`ui.js` 를 따로 둔 이유: `app.js` 의 단축키·모드 전환 함수를 회귀 시험이 **글자 그대로 떼어 내** 돌립니다.
그래서 화면 개선분은 `app.js` 를 건드리지 않고 **위에 얹었습니다**(0.2초마다 상태를 읽어 화면만 맞춤).

## 10. 토요일(2026-09-19) Codex · DeepSeek 에게 물을 것 8개

1. **«제외는 마스크 저장이 뒤집지 않는다»** 가 맞는 규칙인가. 그리고 2차 검수가 **되돌리기의 판별을
   `src` 표식 하나로 줄인 것**(`server.py` 의 `api_undo_duplicate_exclusions`·`api_undo_exclude_group` 에서
   `note.startswith("중복: ")` 조건을 뺐다 — 번호를 저장하면 메모 앞부분이 «번호 편집 …» 으로 바뀌어
   자기가 넣은 제외를 못 되돌렸기 때문)이 **P1 보호(AI 3회 검수 제외를 지키는 것)를 약하게 만들지 않는가.**
   반례가 있는가. (2차 실측: 사과 568장의 AI 제외가 한 장도 안 건드려짐 — `cycle_5/stage2/r2_undo.py` G5)
2. **판정 줄로 «단추를 옮기는»** 방식(`ui.js` 가 `#boxsave` 등 다섯 단추를 `#taskbtns` 로 `appendChild`)이
   `display:contents` 와 함께 **크롬·엣지에서도** 같은 배치를 내는가. 더 게으른 대안이 있는가.
3. **색 설명(범례)을 기본 접힘**으로 둔 판단. 칩이 «눌러서 레이어 끄기» 라서 `pointer-events:auto` 이고, 그래서 펴 놓으면
   사진 왼쪽 위 붓질을 막습니다. 접기 말고 더 나은 방법(캔버스 밖으로 옮기기 등)이 있는가 — 캔버스 높이 55% 는 지켜야 합니다.
4. 저장 함수가 `status.json` 을 **자물쇠 밖에서 읽고 → 자물쇠 안에서 쓰는** 구조(`_save_verdict`·`api_save_instances` 둘 다)를
   `update_status` 안으로 합쳐야 하는가. 동시 260요청에서는 문제가 없었지만 구조로 보면 틈이 있습니다.
5. 남은 ponytail 후보 셋 — `/api/list` 가 호출마다 `flag_counts` 를 다시 세는 것 · `cached_counts()` 가 호출마다
   사전 1,001개를 새로 만드는 것 · `instances.py` 집계 200줄. 지금 고칠 값어치가 있는가, 아니면 그대로 둘 값어치가 있는가.
6. **마스크 PNG 업로드가 중간에 끊기면 어떻게 되는가.** 2차 검수는 «망가진 본문 8종» 을 `/api/save` 에 보내
   전부 4xx 거절 · 기존 `masks_fixed` 무손상 · 500 0 을 확인했습니다(`cycle_5/stage2/r3_api.py` P절).
   확인 못 한 것은 **전송 도중 TCP 가 끊긴 경우**(Flask 가 `request.get_json` 에서 어떻게 되는가)와
   업로드 크기 상한이 없는 것(1,080×1,920 마스크가 base64 로 약 200KB, 상한 없음 → 일부러 큰 것을 보내면?).
   **최소한의 상한·거절이 필요한가, 지금 구조로 충분한가.**
7. **같은 사진을 두 사람이 동시에 편집할 때 «경고» 의 최소 구현.** 지금은 나중 저장이 조용히 이깁니다.
   `/api/item` 이 주는 `at`(마지막 저장 시각)을 브라우저가 들고 있다가 저장할 때 같이 보내고, 서버가
   다르면 409 를 내는 «몇 줄» 방식이 맞는가. 아니면 사람 규칙(과일·쪽 나누기)으로 충분한가.
   (고칠 곳: `server.py` 의 `api_save`·`api_status`·`update_status` · 화면은 `app.js doAction`)
8. **항목별 변경 이력(덧붙이기 로그)의 최소 구현.** `update_status` **한 곳**에서
   `data/<과일>/status_log.jsonl` 에 «시각·사진·이전 판정·새 판정·이름» 한 줄씩 덧붙이는 6줄 안이 맞는가.
   파일이 무한정 커지는 것(사과 1,001장 × 여러 번)과 자물쇠 안에서 한 번 더 쓰는 비용을 어떻게 볼 것인가.
   **되돌리기 도구는 따로 만들지 않고 «사람이 읽는 기록» 으로만 두는 것**이 게으른 답인가.


---
## 3차(Fable 5.1) 확정 — 2026-09-18 04:51
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
- 되돌리기 판별을 `src` 표식 하나로 줄인 것은 인정 — AI 3회 검수가 넣은 제외·flag 에는 `src` 가 없고(실서버 네 과일에서 0건), `src` 는 서버의 묶음/일괄 제외 단추만 붙입니다.
- 열린 문제 개수는 **표의 행을 센 값이 정본**입니다(본문의 정정 상자 참조).
- «제외 → 저장 → 사람이 1 → 수정본 되돌리기» 가 제외로 돌아가는 것은 작은 결함으로 남깁니다(다음 작업 2순위, 그 전 사람 규칙: 제외를 풀었으면 그 사진에서 «수정본 되돌리기» 를 누르지 말 것).
- 다음 작업 1순위 = 항목별 변경 이력 로그(딥시크 확인에서도 같은 지적 — `문서/260918_딥시크확인_Fable판정.md`).
- 상세: `260916_라벨링툴/cycles/260917_ui/cycle_5/stage3_final.md`
