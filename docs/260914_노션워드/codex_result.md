실행 환경 오류로 작업을 진행하지 못했습니다. 파일 읽기와 지정 Python 실행 모두 다음 오류로 시작 단계에서 실패했습니다.

```text
bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted
```

`source.md`를 읽지 못했으므로 `make_docx.py`와 `.docx`는 생성하지 않았고, 문단·표·그림 수도 확인하지 못했습니다.

현재 세션에서는 실행 권한 설정을 변경할 수 없습니다. 샌드박스 오류를 해결한 뒤 다시 실행해 주세요.
