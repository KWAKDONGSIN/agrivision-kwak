# 과제: source.md → 노션 업로드용 Word(.docx) 생성 스크립트 작성·실행

이 폴더(작업 디렉터리)에 있는 `source.md`(한국어, 두 문서 합본: «실험 설계 이해 정리» + «HamNet 모델 공부»)를
가독성 좋은 Word 문서 `260914_노션업로드_실험설계정리_HamNet.docx` 로 만든다. 팀원 여러 명이 노션에서 함께 읽는 문서다.

## 반드시 지킬 것
- 파이썬: `/home/kds0206/.conda/envs/kwak/bin/python` (python-docx 설치돼 있음). 스크립트 파일명 `make_docx.py`. 실행까지 해서 .docx 를 만들어라.
- **내용을 줄이거나 바꾸지 마라.** 문장·숫자·«14일 수정» 표시 전부 보존. 요약 금지.
- 표로 바꿀 곳(불릿 → 표): §2 단계별 역할(단계/목적/핵심 내용/런 수), §3 데이터셋별 장수(6열), §6 데이터셋별 결과(4열), §8 헤더 12종·백본 12종(ID/모델/연도·게재처/계열), HamNet §6 Self-attention 과 차이(구분/attention/NMF), HamNet §8 논문 결과(항목/결과/의미), §16 작성한 자료(자료/경로). 표 첫 행은 굵게+연한 회색 배경.
- 강조 상자: «14일 수정»·«14일 추가» 표시가 있는 문단과 "결론:"·"주의:" 문장은 1칸짜리 표(연한 노랑 배경)로 상자 처리.
- 이미지: `(이 자리에 이미지 블록으로 03_flowchart_v2.png 업로드)` 자리에 `03_flowchart_v2.png`, `(이 자리에 이미지 블록으로 ChatGPT Image … 업로드)` 자리에 `gpt_flowchart.png` 를 페이지 폭(16cm)으로 삽입. 자리표시 문장은 지운다.
- 서식: 본문 글꼴 «맑은 고딕»(east-asian) / Calibri, 10.5pt, 줄간격 1.15. 제목 1 = 문서 제목(16pt), 제목 2 = `##` 절(13pt, 남색), 소제목 줄("단계별 역할", "규칙", "해석" 등 `#` 없이 홀로 있는 짧은 줄)은 굵게. 불릿은 Word 기본 목록 스타일. 백틱(`)은 지우고 그 안은 Consolas 로. `X ≈ D × C_code` 같은 수식도 Consolas.
- 두 문서 사이(`---` 줄)에서 페이지 나눔. 문서 맨 앞에 3줄 안내 없음 — 바로 제목부터.
- A4, 여백 2cm. 머리글/바닥글 없음.
- 마지막에 `python -c "import docx; d=docx.Document('260914_노션업로드_실험설계정리_HamNet.docx'); print(len(d.paragraphs), len(d.tables), len(d.inline_shapes))"` 로 문단 수·표 수·그림 수를 출력해 보여 줘라. 표는 7개 이상, 그림 2개여야 한다.
- 이 폴더 밖의 파일은 읽기만 하고 쓰지 마라.
