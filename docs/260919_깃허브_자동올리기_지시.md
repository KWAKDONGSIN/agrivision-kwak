작성: 2026-09-19 22:35
작성자: VS Code 세션(Claude) → tmux 세션 `claude2` 가 실행

# 사용자 요청 원문 (2026-09-19)
- 22:00 «내가 만든 것은 다 만든 후 깃에도 모든 내용을 업데이트해줘»
- 22:18 «(저장소는) 공개로 놔둘 거야 괜찮아»
- 22:25 «내가 그때 못 쓸 확률이 높은데 자동으로 되게 할 수 있는 건 없는가?»
보안 관련 값·설정 세부는 공개본에서 생략했습니다.

# 언제
**맨 마지막.** 카운팅 5사이클 → 구조 정리 5사이클 → Codex 2회 → Fable 종합 → 폰용 정리 문서가 끝난 **직후**, 다시 지시받지 않고 바로 실행한다. 사용자는 자리에 없다. 묻지 말고 아래 기본값대로 하고, 애매한 파일은 **빼고** 목록에 적는다.

# 이미 준비된 것 (22:31 실측)
- 저장소: `https://github.com/KWAKDONGSIN/agrivision-kwak` — **공개(Public)**, 사용자 결정(작업기록 §4).
- 올리는 열쇠: `~/.ssh/agrivision_deploy`(이 저장소 전용 배포 키, 쓰기 허용). 22번 포트 통함(443 도 통함). 시험 push 2회 성공(`a2aefc7` 빈 파일 추가 → `c691f0e` 삭제). 지금 `main` 은 빈 상태.
- 올릴 때는 **반드시 이 환경변수**를 쓴다(`~/.ssh/config` 는 고치지 않는다):
  `export GIT_SSH_COMMAND="ssh -i $HOME/.ssh/agrivision_deploy -o IdentitiesOnly=yes -o BatchMode=yes"`
- 주소: `git@github.com:KWAKDONGSIN/agrivision-kwak.git` (22번이 막히면 `ssh://git@ssh.github.com:443/KWAKDONGSIN/agrivision-kwak.git`)
- 커밋 이름: `KWAKDONGSIN` / 메일: `KWAKDONGSIN@users.noreply.github.com` (개인 메일을 공개 저장소에 남기지 않는다)

# 하는 법 — «모아 담는 폴더» 하나를 만들어 거기서만 깃을 쓴다
1. `kds0206/_git_agrivision-kwak/` 에 저장소를 `git clone` 한다(없으면). 이미 있으면 `git pull`.
2. 원본 폴더에서는 **깃 명령을 쓰지 않는다**(`git add`·`git init` 금지). 원본 → 모아 담는 폴더로 **복사만**(`rsync -a --copy-links` 또는 `cp`). 이동·삭제 금지.
3. 복사본에 아래 «가리기» 를 적용 → 비밀 검사 → 커밋 → push → 검증.
4. `kds0206/semantic-segmentation` 의 origin(인훈 계정 `ih021088-ui`)에는 **절대 push 하지 않는다.** 그 저장소의 설정도 바꾸지 않는다.

## 폴더 배치(모아 담는 폴더 안)
```
README.md                이 저장소가 무엇인지(쉬운 말) + 폴더 설명 + 데이터는 왜 없는지 + 출처·라이선스
labeltool/               라벨링 툴 (구조 정리 뒤의 app/ · tests/ · scripts/ · README)
dataset/                 통합 데이터셋의 «목록만»: manifest.csv · README.md · build_summary.json · 출처등록표 · 갱신법
scripts/                 semantic-segmentation/tools/ 중 곽동신이 만든 것
docs/                    kds0206/문서/ 의 md·txt (+ 50MB 이하 PDF·그림)
worklog/작업기록.md       작업기록(가리기 적용본)
NOT_UPLOADED.md          안 올린 것과 이유(자동 생성)
```

## 올리는 것
- 라벨링 툴: `platform/work/kwak_dongsin/260916_라벨링툴/` 의 `app/`·`tests/`·`scripts/`·README·판정서. 구조 정리가 끝난 판.
- `semantic-segmentation/tools/` 중 **곽동신이 만든 스크립트**: `git status` 의 미추적·수정 파일 + `git log --author=kds0206` 로 확인되는 것(`build_merged_dataset.py`·`count_metrics.py`·`instance_split.py`·`make_*.py` 등). 저장소 전체를 복사하지 않는다(남의 코드·이력).
- `kds0206/문서/` 의 md·txt, 그리고 50MB 이하 PDF·svg·png.
- `kds0206/작업기록.md`, `kds0206/CLAUDE.md`.
- 통합 데이터셋 최신 정본(그때 기준 `datasets_merged_*` 중 가장 새 것)의 `manifest.csv`·`README.md`·`build_summary.json` 과 과일별 `manifest.csv`.
- 사이클 기록 중 **md 보고서만**(`cycles/*/cycle_*/stage*.md`).

## 올리지 않는 것 (공개 저장소라서)
- **데이터셋 사진·마스크·번호·상자 파일 전부**(`images/`·`masks/`·`instances/`·`boxes/`, 툴의 `data/`, `exports/`). CERTH·MinneApple 재배포 조건 + 용량.
- 툴의 `cycles/**/sandbox/`·`mergebuild*/`·`_archive/`·`_backup_*`·`cache/`·`logs/`·`__pycache__/`·스크린샷.
- **남이 만든 것**: `platform/work/` 의 박성문(`park_seongmoon`)·임성후·인훈(`choi_inhun`)·교수님(`ahn_hongryul`) 폴더, `platform/planning/`, `platform/00_meetings/` 녹취, 논문 PDF(`01_papers` 등, 저작권), `~/.council/` 원문.
- 교수님께 보낸 질문·카톡 문구는 올려도 되지만(곽동신이 쓴 것), **녹취 원문을 길게 옮긴 문서**(`문서/*녹취*`)는 뺀다.
- 한 파일 50MB 초과, 체크포인트(`*.pth`·`*.pt`·`*.ckpt`), zip.

## 가리기(복사본에만 — 원본은 한 글자도 안 바꾼다)
- 서버 IP `<서버주소>` → `<서버주소>` (전 파일 치환).
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
- API 키 모양: `AIza[0-9A-Za-z_-]{20,}` · `sk-[0-9A-Za-z-]{20,}` · `ghp_`·`github_pat_` · `Bearer [0-9A-Za-z._-]{20,}` · `-----BEGIN .* PRIVATE KEY-----`.
- claude 세션 ID(`claude --resume …`)는 `<세션ID>` 로.
- 포트 번호·PID·서버 계정명(kds0206 등)은 그대로 둔다(IP 가 없으면 쓸모없고, 전부 가리면 기록을 못 읽게 된다).

## 비밀 검사 — push 전 필수, 실패하면 올리지 않는다
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
- `git diff --cached --stat` 로 올라갈 파일 수·총 용량 확인. 총 1GB 넘으면 멈추고 큰 것부터 뺀다.
- 검사를 통과 못 한 파일이 있으면 **그 파일만 빼고** 나머지를 올린다. 뺀 것은 NOT_UPLOADED.md 에.

## 커밋·push
- 커밋 한 번(또는 폴더별 몇 번). 메시지는 한국어로 짧게, 예: `260920 최종본: 라벨링 툴·통합 데이터셋 목록·문서·작업기록`.
- `git push mine main`(모아 담는 폴더의 remote 이름은 `origin` 이어도 됨). **`--force` 금지.**
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
보안 관련 값·설정 세부는 공개본에서 생략했습니다.

## 끝나면
- 작업기록 §2 에 항목(올린 파일 수·용량·커밋 해시·안 올린 것 요약·검사 결과), §3 체크, §1 표에 한 줄.
- 막히면: 되는 데까지(최소 = 모아 담는 폴더에 로컬 커밋) 해 두고 어디서 왜 막혔는지 작업기록에 적는다. 사용자가 돌아와 VS Code 세션에서 이어서 올릴 수 있게 명령 한 줄을 남긴다.
