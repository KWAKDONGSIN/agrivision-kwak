작성: 2026-08-10
⛔ 대체됨 (2026-08-10) — 복숭아·포도를 정답 어노테이션으로 다시 셌습니다. 260810_카톡_1번2번_최종.txt 를 쓰세요.
용도: 0810 랩미팅 역할분담 1·2번 결과 공유 — 카카오톡 복붙용 (개체 분리 반영판)
바뀐 점: 260810_카톡_역할분담.txt 를 대체합니다. 개수를 세는 방법을 «붙은 열매 분리»로
        고쳤기 때문에 숫자가 전부 바뀌었습니다. 옛 파일의 숫자는 쓰지 마세요.
최종 수정: 2026-08-10 (1번을 논문 Fig.2 형식 «라벨 칠한 샘플 사진»으로 다시 만듦)
첨부할 그림 5장 (순서대로):
  1) semantic-segmentation/reports/figures/fig_samples_annotated_4fruits.png  ← 1번 본체
  2) semantic-segmentation/reports/figures/fig_crop_overlay_4fruits.png       ← 1번 보조(크롭 확대)
  3) semantic-segmentation/reports/figures/fig_objects_per_image_4fruits.png  ← 2번
  4) semantic-segmentation/reports/figures/fig_instance_split_check.png       ← 개수 세는 법 검증
  5) semantic-segmentation/reports/figures/fig_centercrop_problem.png         ← 4번 담당자용
────────────────────────────────────────────────────────
【 메시지 1 — 본문 】
────────────────────────────────────────────────────────

[0810 미팅] 1번, 2번 끝냈습니다. 그림 5장 첨부합니다.

1번 — 첫 번째 그림입니다. MinneApple 논문 Fig.2 처럼 라벨을 색으로 칠한 샘플 사진에
512 크롭 선을 얹었습니다. 색이 다르면 다른 열매라는 뜻이고,
빨간 실선이 평가 때 쓰는 가운데 크롭, 주황 점선이 학습 때 쓰는 랜덤 크롭입니다.
과일마다 3장씩 넣었는데 개체 수가 25/50/75 백분위인 장을 고른 거라
적은 편·보통·많은 편이 한 줄에 다 보입니다. 줄 아래에 그 과일 전체 통계도 적었습니다.
두 번째 그림은 그 크롭을 확대해서 «AI 입력»과 «그 안의 정답»을 나란히 놓은 것입니다.

2번 — 세 번째 그림입니다. 논문 4쪽 Fig.3 형식으로 네 판을 만들었습니다.
(a) 장당 개체 수 분포(논문 Fig.3b 형식) / (b) 종류 수 대비 개체 수(논문 Fig.3a 형식)
(c) 장당 개체 수 비교 막대 / (d) 데이터셋 총 개체 수

그리고 교수님이 04:02에 말씀하신 부분(“세그멘테이션이라 오브젝트가 몇 개인지 셀 수가 없다”)을
해결해야 개수가 나와서, 붙어 있는 열매를 한 알씩 갈라내는 작업을 먼저 했습니다.
네 번째 그림이 그 검증입니다.

방법: 마스크에 거리변환을 걸어 열매 중심을 찾고 watershed로 나눕니다.
      디텍션 모델을 새로 학습하지는 않았습니다. 13:11 “정확하게 디텍션 알고리즘을
      테스트할 건 아니니 알고리즘 돌려서 나온 결과로 플롯만 만들면 된다”에 맞춘 겁니다.

◾ 정확도 (사과 1,001장 전수로 검증)
사과 마스크에는 열매마다 번호가 붙어 있어서 정답 개수를 알 수 있습니다.
- 정답 40.4개  vs  이 방법 40.7개 (+0.7%)
- 장별로 봐도 오차 중앙값 3.8%, 사진 95.5%가 정답 ±20% 안에 들어옵니다
- 참고로 그냥 덩어리째 세면 33.1개(-18.2%)로 크게 적게 셉니다

◾ 실측 결과 (리사이즈본 4,823장 전수)
- 블루베리 1,195장 : 장당 평균 36.9알 (중앙 26, 최대 204) / 총 44,064알
- 사과     1,001장 : 장당 평균 40.7알 (중앙 37, 최대 124) / 총 40,732알
- 복숭아     125장 : 장당 평균  7.4알 (중앙  5, 최대  24) / 총    931알
- 포도     2,502장 : 장당 평균  6.0송이 (중앙 5, 최대 32) / 총 15,102송이

◾ 포도만 다르게 셌습니다
포도는 라벨이 «송이» 단위로 그려져 있어서 알 단위로는 셀 수가 없습니다.
억지로 나누면 큰 송이 하나가 둘로 갈라져 송이 수가 부풀려집니다(첫 번째 그림 맨 아랫줄).
그래서 포도만 덩어리 그대로 = 송이 수로 셌습니다.

◾ 이 방법이 믿을 만하다는 근거
MinneApple 논문에 장당 평균 41.2개라고 적혀 있는데,
사과 마스크의 번호로 직접 센 값이 40.4개로 거의 일치합니다.

◾ 그림 (b)에 넣은 비교값
MinneApple 논문 p.4 본문에 글자로 적힌 값만 가져왔습니다.
COCO 7.7개 / ImageNet Detection·PASCAL VOC 3개 미만.
우리 데이터셋이 한 장에 훨씬 많다는 게 이걸로 보입니다.

────────────────────────────────────────────────────────
【 메시지 2 — 다른 분들께 도움될 내용 】
────────────────────────────────────────────────────────

◾ 4번(center crop 문제성) 하시는 분께
같은 계산에서 숫자가 같이 나와서 그림 한 장으로 만들어 뒀습니다 (다섯 번째 첨부).
512 가운데 크롭에 열매가 «온전히» 하나도 안 들어오는 사진 비율입니다.
- 포도 33.2% / 블루베리 13.1% / 사과 10.6% / 복숭아 2.4%
포도는 세 장에 한 장꼴입니다. 그리고 센터크롭은 사진의 12.6%만 채점하니까
나머지 87.4%는 평가에 아예 안 쓰입니다.
필요하시면 원본 수치 파일(json) 드리겠습니다. 그대로 쓰셔도 됩니다.

◾ 개수 세는 코드도 같이 쓰실 수 있습니다
semantic-segmentation/tools/instance_split.py  (붙은 열매 분리)
semantic-segmentation/tools/count_objects_4fruits.py  (전수 카운팅)
semantic-segmentation/tools/validate_instance_split.py  (사과 정답으로 정확도 검증)

◾ MinneApple 논문 참고 페이지 정정
팀 노트에 p.3으로 적혀 있는데, 녹음 다시 들어보니 p.4가 맞습니다.
10:34에 p.3을 가리켰다가 10:50에 교수님이 "이거 다 아니 근데 다음에 봐 보세요" 하시고
넘버 오브 카테고리 / 퍼센티지 오브 이미지를 설명하셨는데 그게 p.4 Fig.3입니다.
p.3은 샘플 사진이라 plot이 아닙니다.

◾ 땅에 떨어진 과실 건
교수님이 "그런 것도 얘기를 좀 써주는 게 좋을 것 같다"고 하신 부분인데,
MinneApple 논문 p.4에 근거 문장이 있습니다.
"the ones on the ground and trees in the background were not tagged"
그래서 우리 개수는 전부 «나무에 달린 과실»만 센 것이라고 그림 캡션에 적어 뒀습니다.

◾ 예전 숫자는 쓰지 말아 주세요
- 포도 "장당 4.14송이"는 100장짜리 토이셋 기준입니다. 지금은 2,502장 전수로 6.0송이입니다.
- 8월 10일에 공유한 "블루베리 27.3 / 사과 33.1 / 복숭아 7.1"은 붙은 열매를 하나로
  세던 값입니다. 위의 새 숫자로 바꿔 주세요.
