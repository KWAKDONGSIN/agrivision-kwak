작성: 2026-09-18
최종 수정: 2026-09-19 02:05 (§3 에 «종류별 확정»(상자 `confirmed_boxes` · 번호 `confirmed_instances`) 규칙 3-2 추가 — 4차 수정 보고서 `tools/tests_merged_260918/stage4_confirm_kinds.md`)

# 네 과일 통합 데이터셋 + Codex 2회 검수 — 계획서

지시(곽동신, 2026-09-18 17:10, 이전 세션 전달) 세 가지를 그대로 옮기고, 어떻게 할지 적는다.

## 1. 지시 원문 요지
1. 그림판식 툴 5회 사이클이 끝나면 **네 과일(사과·포도·복숭아·블루베리)을 하나로 통합**한 데이터셋을 만든다. 지금까지 거친 과정(검수판 폴더 + manifest + 사람 확정 반영 + 촬영 단위 분할 정보)대로. 팀원이 툴에서 계속 손보므로 **다시 돌릴 수 있는 스크립트**(멱등 · 날짜 폴더 · 덮어쓰기 금지 · 툴의 사람 확정을 읽어 반영)로. 0920(일)·0921(월)에 한 번씩 더 갱신. **마감 2026-09-19 18:00 전.**
2. 2026-09-19 18:00 Codex 한도 해제 → 검수는 **Codex 만**으로 «공격적 1회 + 보통 1회», Fable 이 둘을 종합해 최종 판정 하나. 질문 두 벌은 오늘 밤 미리 파일로. 18:01 예약 다시 걸기.
3. 갱신 절차 문서 `문서/2609xx_통합데이터셋_갱신법.md`.

## 2. 산출물(경로 고정)
| 것 | 경로 | 상태 |
|---|---|---|
| 빌드 스크립트 | `semantic-segmentation/tools/build_merged_dataset.py` (인자 `--date YYMMDD`, 기본 오늘) | 있음 (지문 `a01de4b800f1`) |
| 🔴 **정본 통합 데이터셋** | `/data/project/2026summer/kds0206/datasets_merged_260918_v3/` (11GB, 2026-09-18 19:23:56 완성) | **확정** |
| 이력 판(지우지 않음) | `datasets_merged_260918/`(17:39) · `datasets_merged_260918_v2/`(18:06) | 보존 |
| 데이터셋 구조 | `<정본>/{apple,grape,peach,blueberry}/{images,masks[,instances],boxes}` + `manifest.csv`(통합 4,823행) + `README.md` + `build_summary.json` | 있음 |
| 🔴 **최종 판정서** | `semantic-segmentation/tools/tests_merged_260918/stage3_final.md` (3차 Fable, 19:25) | **합격** |
| 1차 작업 보고서 | `semantic-segmentation/tools/tests_merged_260918/stage1_work.md` | 있음 |
| 2차(공격적) 검수 보고서 | `semantic-segmentation/tools/tests_merged_260918/stage2_review.md` | 있음 |
| 3차 전 소수정 보고서 | `semantic-segmentation/tools/tests_merged_260918/stage3_fix.md` | 있음 |
| 갱신법 문서 | `문서/260918_통합데이터셋_갱신법.md` | 있음 |
| 출처 등록표(+생성 스크립트) | `문서/260918_통합데이터셋_출처등록표.md` · `…/tests_merged_260918/stage3/make_registry_md.py` | v3 기준으로 다시 뜸 |
| Codex 질문 | `문서/260918_Codex질문_공격적.md` · `문서/260918_Codex질문_보통.md` | **아직 없음** (09-19 18:03 전까지 만들 것. 재료 = 2차 §5 의 «검증되지 않은 가정 10개» + 3차 판정서) |
| Codex 최종 판정 | `문서/260919_Codex검수_Fable최종판정.md` | 아직 없음 |

## 3. 통합 규칙(입력 우선순위)
1. 바탕: 검수판 `datasets_reviewed_260916/{peach,grape}` + `datasets_reviewed_260917/{apple,blueberry}`(각 manifest 의 action·note·session·split_group 그대로 승계. 복숭아·포도 manifest 에 session 이 없으면 파일명 규칙으로 만들고 근거를 README 에 적음).
2. 그 위에 **툴의 사람 확정**(`260916_라벨링툴/data/<과일>/status.json` 의 `confirmed` 칸, 없으면 `by` 가 «AI» 로 시작하지 않는 판정 = **`human_unconfirmed`** — 확정 칸 없이 사람이 누른 모든 판정. 사이클 2 이전 저장분뿐 아니라 오늘 사람이 마스크만 고쳐 저장한 것도 여기 들어온다. 2026-09-18 3차 결정 D2 로 `human_legacy` 에서 이름을 바꿨다)을 덮음: 확정 ok=keep · fixed=`masks_fixed/` 사본 사용 · exclude=제외 · flag=**제외(reason=confirmed_flag)**. AI 제안만 있는 사진은 검수판 판정 그대로(= AI 3회 검수 결과).
3. 열매 번호(`instances_fixed/`)·상자(`boxes/`)는 있으면 같이 복사(`instances_source` 값은 전부 영문 토큰 — `instances_fixed|detect_seed|reviewed_number_mask|none`, 2026-09-18 3차 결정 D9)(«이진본이 번호본을 자른다» 규칙은 툴 `instances.load_inst` 를 그대로 불러 씀).
3-2. 🔴 **툴의 «종류별 확정»(2026-09-19 추가 · 툴 사이클 4 반영).** 확정 칸은 셋이고 **서로 독립**이다 — 마스크 `confirmed` · 상자 `confirmed_boxes` · 번호 `confirmed_instances`(툴 `app/server.py CONFIRM_KINDS`).
   - **상자**: `confirmed_boxes` 가 `ok`·`fixed` → 툴 `boxes/<stem>.json` 을 **사람 상자**로 채택(`boxes_source=tool`, 새 칸 `boxes_confirmed_by`·`boxes_confirmed_at`). `exclude`·`flag` → **그 사진의 상자는 없음**(json·YOLO txt 를 만들지 않고 `note` 에 이유). 확정이 없으면 채택 순서는 **박성문 gt > 임성후** 이고, 확정 없는 툴 json 이 비어 있지 않으면 `boxes_source=tool_unconfirmed` 로 **표시만** 한다(채택하지 않는다 — 사람이 확정하지 않은 초벌 저장본이 정답 상자를 밀어내면 안 된다. 2차 검수 M2-7 에서 빈 json 이 실제로 정답 상자 9개·4개를 지웠다). 빈 툴 json 은 예전처럼 무시한다.
   - **번호**: `confirmed_instances` 가 `ok`·`fixed` → `instances_fixed/<stem>.png`(없으면 원본 번호본)을 채택하고 `instances_source=tool_confirmed`. `exclude`·`flag` → 번호 없음. 확정이 없으면 지금 규칙 그대로.
   - **마스크** `confirmed` 규칙과 `source` 칸은 **그대로**다. 상자·번호 확정은 **사진을 빼지 않는다.**
   - manifest 새 칸 4개: `boxes_confirmed_status`·`boxes_confirmed_by`·`boxes_confirmed_at`·`instances_confirmed_status`(값이 없으면 `-`). `build_summary.json` `counts` 에 `n_confirmed_mask`·`n_confirmed_boxes`·`n_confirmed_instances`. 자체 검사 3가지 추가(상자 확정 exclude 인데 txt 가 있으면 실패 · `tool_unconfirmed` 인데 채택됐으면 실패 · 번호 확정 exclude 인데 번호가 나갔으면 실패).
   - 시험 `tools/tests_merged_260918/stage4/test_confirm_kinds.py`(6×3 조합 전수, 200항목) · 보고서 `tools/tests_merged_260918/stage4_confirm_kinds.md`. 실측(2026-09-19 02:02): 지금은 네 과일의 확정이 전부 0건이고 «확정 없이 비지 않은 툴 json» 도 0장이라 **v3 대비 바뀌는 칸이 없다**(새 칸 4개만 `-`).
4. 통합 manifest 칸: `fruit, stem, image_source, mask_source, action, reason, note, source(reviewed|ai|human_confirmed|human_unconfirmed), confirmed_by, confirmed_at, session, split_group, has_instances, has_boxes` + (2026-09-19 추가) `boxes_confirmed_status, boxes_confirmed_by, boxes_confirmed_at, instances_confirmed_status`(값이 없으면 `-`).
5. 멱등·안전: 출력 폴더가 이미 있으면 **멈춤**(같은 날 두 번이면 `--suffix`), 입력 폴더는 읽기만, 원본 `datasets_resized_2mp`·`datasets_reviewed_*` 무변경, 심볼릭 링크가 아닌 실복사(0730 사고 재발 방지), 끝나면 장수·md5 요약을 `build_summary.json` 에.

## 4. 순서·시각 (2026-09-18 **실측**. 예상이 아니라 실제로 일어난 시각)

| 시각(실측) | 무엇 | 남은 것 |
|---|---|---|
| 17:29:03 → **17:39:06** (603.1초) | 1차(Opus5) 스크립트 + **첫 빌드** `datasets_merged_260918` | `logs/build_merged_260918.log` |
| 17:56:52 → **18:06:50** (597.6초) | 팀원 폴더(박성문·최인훈·임성후)를 입력에 더한 **v2 빌드** `datasets_merged_260918_v2` + 출처 등록표 기능 | `logs/build_merged_260918_v2.log` |
| **18:09** | 1차 작업 보고서 | `stage1_work.md` |
| **18:42** | **2차 검수(공격적)** — 1차 코드를 보지 않고 4,823행을 다시 계산해 대조. 즉시 수정 7+1건 | `stage2_review.md` |
| **19:10** | 3차 전 **소수정**(D1~D9) + 모래상자 실측 + v2→v3 차이 미리 계산(437칸) | `stage3_fix.md` |
| 19:13:59 → **19:23:56** (596.9초) | **v3 빌드** `datasets_merged_260918_v3` — 시험 20+13+64 전부 통과 뒤 실행, «검사 통과», 문제 0 | `logs/build_merged_260918_v3.log` |
| **19:25** | **3차 최종 판정 — 합격. 정본 = `datasets_merged_260918_v3/`** | `stage3_final.md` |
| **19:29~19:32** | 문서 3개를 v3 기준으로 갱신(출처 등록표 재생성 · 갱신법 · 이 계획서) + 팀 카톡 문구 | 이 문서 |

- 09-19 18:03 예약(CronCreate, 세션이 죽으면 새 세션에서 다시 걸 것): Codex 공격적 → Codex 보통 → Fable 종합. **마감 09-19 18:00 은 정본 v3 으로 이미 지켰습니다.**
- 09-20·09-21: 사람(또는 Claude)이 `build_merged_dataset.py` 한 줄로 재빌드 → `datasets_merged_260920/` · `datasets_merged_260921/`. 절차는 `문서/260918_통합데이터셋_갱신법.md`.

## 5. 지키는 것
확인 질문 금지 · 시각은 date 실측 · GPU 없음 · 남의 프로세스 kill 금지 · 툴 실서버 쓰기 금지(status.json 은 읽기만) · 검수는 실행 → 독립 검수 → 판정.

## 6. 정정(2026-09-18 17:42, 사용자) — 입력에 팀원 폴더 추가
팀원 수정본은 툴이 아니라 **각자 폴더**에 있다. 통합 스크립트가 반드시 읽는 것(전부 읽기 전용):
| 누구 | 경로(실측) | 무엇 | 반영 칸 |
|---|---|---|---|
| 박성문 | `platform/work/park_seongmoon/bbox_outputs/{apple,grape,peach}/gt_boxes/` (09-12, 정답 상자) · `blueberry/all/` (09-07 watershed 추정, gt_boxes 없음) | `gt_boxes/{json,csv,yolo_labels}` · blueberry `all/{json,instance_maps}` 미검증 | `boxes_source=psm_gt`, `instances_source` |
| 박성문 | `apple_check/review_list.csv`(09-17, `verdict` 열) · `confirmed_errors.csv` | 사과 번호 오류 눈 판정 | `apple_check_verdict` |
| 최인훈 | `choi_inhun/dup_audit_260917/peach/{REPORT.md,similar_pairs.csv,clusters*.txt}` | 복숭아 버스트 촬영 9그룹 45장 눈 확인 | `peach_dup_candidate`(제외 안 함, 교수님 결정 항목) |
| 최인훈 | `choi_inhun/mask_audit_260908/suspects.csv`(240행, `reason` 칸) | 의심 마스크 reason | `mask_audit_reason` |
| 임성후 | `im_seonghu/bbox_outputs/{apple,blueberry}/all` 09-07 판(박성문 `all/` 과 md5 동일 파일) | 박성문 gt_boxes > all 순. 블루베리만 `lsh`(추정) | `boxes_source=lsh` |
| 툴 | `260916_라벨링툴/data/<과일>/{status.json confirmed,masks_fixed,instances_fixed,boxes}` | 사람 확정 | `source=human_confirmed`, `boxes_source=tool` |

- 과일마다 이미지·세그 마스크(0/255)·인스턴스(있으면)·상자(YOLO)·manifest(출처 칸: 누가 어디서).
- **출처 등록표** `문서/260918_통합데이터셋_출처등록표.md`(경로·소유자·mtime·개수·해시·반영 칸) + `build_summary.json` 에 같은 내용.
- 재실행(0920·0921) 때 팀원 폴더의 새 파일이 자동으로 잡히게: 직전 빌드 `build_summary.json` 과 파일 개수·최신 mtime 비교 → «바뀐 입력» 로그·README 표.

(정정 18:11: 17:42 판 표의 «peach·grape 없음»·«suspects.csv 없음» 은 내가 목록을 잘라 본 오류. 1차 에이전트 실측이 맞음 — 출처 등록표 `문서/260918_통합데이터셋_출처등록표.md` 기준.)

---

## 7. 완료 상태 (2026-09-18 19:32 기준)

### 끝난 것
- **통합 데이터셋 정본 확정** — `/data/project/2026summer/kds0206/datasets_merged_260918_v3/`
  (2026-09-18 19:23:56 완성 · 596.9초 · 11GB · 문제 0건 · 자체 검사 통과).
  남긴 사진 **사과 429 · 포도 2,403 · 복숭아 125 · 블루베리 1,114 = 4,071장**,
  통합 `manifest.csv` **4,823행**(제외한 사진 행 포함), 열매 번호 1,543, 상자 4,071.
- **검수 3단계 완료** — 1차 작성(Opus5) → 2차 공격적 검수(코드를 보지 않고 전수 재계산) →
  소수정(D1~D9) → 3차 최종 판정(Fable) **합격**. 시험 **20 + 13 + 64 항목 전부 통과**.
- **재실행 가능성 확보** — 멱등(폴더가 있으면 멈춤) · 입력 읽기 전용 · 실복사(심볼릭 링크 아님) ·
  입력 76개의 지문 등록 · 직전 빌드와 견준 «바뀐 입력» 자동 표 · 사라진 입력이면 exit 3.
- **문서 3개 v3 기준 갱신** — 출처 등록표(스크립트로 재생성) · 갱신법 · 이 계획서.

### 남은 것
- 🔴 **Codex 2회 검수 (09-19 18:03)** — 질문 두 벌을 아직 만들지 않았습니다.
  재료: 2차 §5 «검증되지 않은 가정 10개» + 3차 판정서 `stage3_final.md`.
- 🔴 **사람이 결정할 것 셋** ① 블루베리 정답 상자를 박성문에게 요청할지(지금은 watershed 추정)
  ② 복숭아 중복 후보 55장 13그룹을 얼마나 솎을지(교수님) ③ 포도 촬영 정보가 없어 사실상 임의 분할인 것.
- **0920·0921 재빌드** — 그날 `source` 집계에 `human_confirmed`·`human_unconfirmed` 가 처음 나타납니다.
- **모래상자 삭제(사람 손으로)** — `tools/tests_merged_260918/{sandbox, stage2/sandbox, stage3/sandbox}`.
  스크립트는 지우지 않습니다.
