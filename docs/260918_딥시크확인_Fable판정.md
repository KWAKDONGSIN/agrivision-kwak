작성: 2026-09-18

# 딥시크(DeepSeek) 확인 → Fable 판정
사과·블루베리 데이터 검수 + 라벨링 툴 UI 개선에 대해, 토요일 Codex 점검 전에 딥시크에게 한 번 확인받고 Fable 5.1 이 항목별로 «받을 것 / 아닌 것» 을 가렸습니다.

- 참여한 AI: **DeepSeek(deepseek-v4-pro, 학교 게이트웨이, 1회 시도 성공 04:11)** · **Claude Fable 5.1**(판정). Codex·Gemini 는 부르지 않았습니다(지목 없음).
- 질문(4.5KB 로 줄인 요약): `~/.council/260918_사과블루베리_UI/question.md` · 딥시크 원문: 같은 폴더 `deepseek.md` · 내가 답을 읽기 **전에** 적어 둔 내 의견: `claude_먼저쓴답.md`
- 주의: 딥시크는 파일을 못 읽습니다. 요약문만 보고 답했으므로, 우리 구조를 잘못 짐작한 부분은 코드·파일로 확인해 기각했습니다.

## 한눈에

| # | 딥시크의 말 | 판정 | 근거·조치 |
|---|---|---|---|
| 1-가 | «교수에게 선택을 넘기는 것은 책임 회피. 하나의 권고안을 내라» | **부분 수용** | 공개 데이터의 60~70% 를 버리는 결정은 교수님 몫이라 선택지는 그대로 드립니다. 다만 **권고안을 한 줄로 명시**하기로 했습니다: «촬영 단위 분할 + 간격 솎기 `keep_G40_90`(294장)». 판정서·README·교수님 질문·PDF 에 반영 |
| 1-나 | «표준 방법인 프레임 간격 솎기를 놓쳤다» | **기각** | 놓치지 않았습니다. 질문문에도 있고 `round3_final/v2/stride_variants_apple.csv`(394/294/248/187/137/105장)로 이미 만들어 뒀습니다. 딥시크가 제안한 «현장 50·다른 출처 90프레임» 은 우리 곡선(현장 40프레임 14%·60프레임 18% / 다른 출처 90프레임 18%)과 같은 자리입니다 → 우리 권고(40·90)와 사실상 일치 |
| 2 | «429장 판을 ‘중복 제거된 데이터셋’ 이라 부르면 안 된다. ‘수동 검토 기반 부분 중복 제거’ 로 적어라» | **수용** | 내 사전 의견과 같습니다. 표현을 **«눈 검토 기반 부분 중복 제거(판정자 일치 κ 0.51~0.72)»** 로 통일. 단 «단일 연구자» 는 사실과 달라 고쳐 씁니다(서로 독립인 AI 판정자 여럿) |
| 3-가 | «‘가운데가 비어야’ 는 사과 중심 공동(꽃받침)이 배경이어야 한다는 뜻. 그 오류 확인을 빠뜨렸다» | **기각(해석)** · **일부 수용(질문 추가)** | 겉에서 찍은 사과에는 뚫린 구멍이 없습니다(꽃받침도 과실 표면). 그리고 구멍은 이미 셌습니다: 사과 라벨 개체 38,781개 중 구멍이 있는 것 180개(0.46%). 다만 교수님 문장의 뜻을 우리가 확인받은 적은 없으므로 **«교수님이 보신 ‘동그랗게’ 사진이 어떤 것인지»** 를 질문에 추가 |
| 3-나 | «amodal·modal 이 섞이면 같은 모양에 다른 정답을 배워 지표 해석이 안 된다. 출처별로 나누거나 규칙을 통일하라» | **수용** | 내 사전 의견과 같습니다. 규칙 통일은 교수님 결정(질문 1번), 그 전까지 **성능을 출처별(MinneApple / dataset1~4)로 나눠 보고**할 것을 권고로 추가 |
| 4-① | «같은 사진을 두 사람이 고치면 나중 저장이 덮어쓴다(버전 확인 없음)» | **부분 수용** | 사실이고 README 에 이미 적혀 있습니다(«나중에 저장한 쪽이 남습니다»). 경고창은 없습니다 → 열린 문제로 유지, 사람 규칙 «과일·쪽을 나눠 맡기». (서로 **다른** 사진의 동시 저장 소실은 이번에 고쳤고 브라우저 두 대 + API 506회로 소실 0 확인) |
| 4-② | «status.json 이 커지면 브라우저가 느려진다» | **기각** | 브라우저는 status.json 을 받지 않습니다. 목록 API 가 120장씩 쪽으로 줍니다. 1,001·2,406장으로 이미 쓰고 있음 |
| 4-③ | «크롬 등 다른 브라우저 미검증» | **수용(이미 1순위)** | 서버에 Firefox 뿐입니다. `문서/260917_툴_사람확인_체크리스트.md` 의 «먼저 볼 것» 이 바로 이것 |
| 4-④ | «저장 도중 끊기면 잘린 JSON 이 남는다. 원자적 교체를 확인하라» | **기각(이미 됨)** · 일부 수용 | 클라이언트가 status.json 을 통째로 올리는 구조가 아닙니다. 서버가 항목 단위로 자물쇠 안에서 임시 파일 → `os.replace`. 다만 **마스크 PNG 업로드가 중간에 끊긴 경우**는 따로 시험한 적이 없어 토요일 Codex 질문에 추가 |
| 4-⑤ | «사용자 식별·작업 로그·되돌릴 수단» | **부분 수용** | 이름(by)·시각은 남고 일괄 작업 전 자동 백업이 있습니다. 그러나 **항목별 변경 이력(덧붙이기 로그)은 없습니다** — 마지막 상태만 남음. `update_status` 한 곳에 6줄이면 되는 안전망이라 **다음 할 일 1순위**로 올림(검수 없이 새벽에 끼워 넣지는 않음) |
| 5 | «가장 위험한 주장 = ‘폴드를 넘는 관계 0’. split_group 은 확인된 쌍만 이으므로 확인 못 한 중복이 폴드를 넘을 수 있다» | **수용** | 옳은 지적입니다(내가 꼽은 1순위와는 달랐지만 더 아픈 곳). 조치: ① 문구를 **«확인된 관계 기준 0»** 으로 ② manifest 에 눈 판정과 무관한 **`session`(촬영 단위) 칸 추가** — 사과 17개·블루베리 288개, split_group 이 session 을 넘는 경우 0(내가 확인) ③ **분할은 session 단위 GroupKFold 를 권고**, split_group 은 더 잘게 나눌 때의 최소 조건 |

## 결론
- 받은 것 5(권고안 명시·표현 수정·출처별 보고·session 칸과 분할 권고·교수님께 ‘동그랗게’ 예시 확인), 부분 수용 3, 기각 4(우리 구조·사실을 잘못 짐작한 것).
- 딥시크가 가장 잘 짚은 것은 5번입니다. «확인된 것만으로 만든 묶음» 을 «누수 없음» 의 증거처럼 쓰면 안 됩니다. 촬영 단위 분할이 정답이고, 교수님의 기존 분할표가 MinneApple 을 촬영 시퀀스로 묶은 것도 같은 이유입니다(dataset1~4 는 아직 장 단위 — 분할표 재생성 때 고칠 것).
- 토요일(2026-09-19 18:01) Codex 점검에 넘길 질문에 추가: 마스크 PNG 업로드 중단 시 동작 · 같은 사진 동시 편집 경고의 최소 구현 · 항목별 변경 이력 로그의 최소 구현.
