작성: 2026-09-25

# 데이터셋 비교 — 후보 `datasets_merged_260924` vs 정본 `datasets_merged_260918_v3`

정본 전환은 사람이 정할 일이라 이 문서는 **비교와 권고만** 합니다. 두 폴더는 읽기만 했습니다(파일 내용 해시 대조).

## 한눈에

| | 정본 v3 (09-18) | 후보 0924 | 차이 |
|---|---:|---:|---:|
| 사과 | 429 | 429 | 0 |
| 포도 | 2403 | 2300 | **−103** |
| 복숭아 | 125 | 123 | **−2** |
| 블루베리 | 1114 | 1112 | **−2** |
| 합계 | 4071 | 3964 | **−107** |

- manifest 는 두 판 모두 4,823행이고 (fruit, stem) 이 100% 같습니다. 새로 생긴 사진은 없고 **빠진 것 107장 + 내용이 바뀐 것 5장**뿐입니다.
- 두 판에서 남아 있는 사진은 이미지 파일이 한 장도 바뀌지 않았습니다(해시 동일). 분할 묶음(`split_group`)·촬영 단위(`session`)도 전부 같습니다.

## 1. 빠진 사진 107장

### 포도 103장 — 최인훈 포도 중복 주석(확정 목록)
사유 `cih_grape_duplicate`. 근거 문서는 `260920_grape_삭제대상.md` 라고 0924 README 에 적혀 있습니다(이번에 열어 보지는 않음). 번호(stem)는 다음과 같습니다(v3 에서 102장은 keep, 1장은 needs_human 이었음).

7, 12, 21, 40, 44, 65, 100, 145, 214, 244, 247, 264, 301, 336, 363, 369, 372, 402, 403, 461, 465, 475, 507, 537, 539, 558, 565, 579, 589, 590, 591, 659, 714, 737, 738, 804, 860, 881, 903, 904, 909, 927, 1019, 1029, 1035, 1047, 1068, 1073, 1076, 1089, 1091, 1101, 1103, 1106, 1109, 1111, 1127, 1139, 1171, 1175, 1180, 1192, 1196, 1197, 1205, 1209, 1211, 1212, 1225, 1240, 1241, 1261, 1279, 1310, 1328, 1507, 1684, 1758, 1776, 1778, 1782, 1838, 1842, 1861, 1873, 1948, 1953, 1962, 1976, 2013, 2080, 2085, 2099, 2105, 2117, 2151, 2179, 2286, 2287, 2323, 2339, 2354, 2455

### 복숭아 2장 — 사람이 «문제 있음» 으로 판정
`210629-t2-of13-03`, `210629-t2-of16-04` (사유 `confirmed_flag`, 고치기 전이라 뺌).

### 블루베리 2장 — 같은 이유로 뺌
`Camera 3 Video (78)_61`, `Camera 3 Video (94)_121` (`confirmed_flag`).

(참고) 블루베리 `Camera 3 Video (28)_121`(confirmed_flag)·`Camera 3 Video (17)_241`·`Camera 4 Video (15)_301`(human_exclude, 사람이 제외)는 v3 에서도 이미 «거의 같은 사진» 으로 빠져 있어 장수는 그대로이고 사유만 바뀌었습니다.

## 2. 내용이 바뀐 사진 5장 (남아 있으나 파일이 다름)

| 과일 | 사진 | 무엇이 바뀌었나 |
|---|---|---|
| 사과 | `20150919_174151_image1` | 그림판에서 사람이 저장(2026-09-24 20:06). 마스크 277화소 변화, 열매 번호 95→96개(배경 0 제외), 상자 95→96개. 이 사진이 사과에서 사람이 손댄 유일한 장 |
| 복숭아 | `210629-t1-01` | 그림판 저장(2026-09-23 13:56). 마스크 화소는 같고 상자 파일만 갱신(11개) |
| 복숭아 | `210629-t2-18` | 마스크 −15,755 화소(125,213→109,458). 원천이 `reviewed_260916` → `masks_fixed`(2026-09-21 사람 수정) |
| 복숭아 | `210629-t2-of11-01` | 마스크 −2,639 화소(151,098→148,459). 같은 이유 |
| 그 밖 | 포도 `2112` | 파일은 그대로, manifest 의 action 만 `image_replaced`→`keep` |

## 3. 폴더 구조 차이
- 복숭아에 `instances/` 폴더 123장이 새로 생겼습니다(v3 에는 없음). 122장은 박성문 님 watershed **자동 초벌**(`psm_watershed`)이고 사람 검수 값이 아닙니다. 1장만 툴 확정(`210629-t1-01`).
- 사과·복숭아 `boxes/` 에 `boxes_all_tool.json` 이 추가됐습니다(상자 모음, 내용은 6개 키).
- manifest 에 열이 늘었습니다(22→33열: `boxes_confirmed_*`, `instances_confirmed_status`, `spec_fixed`, `n_boxes`, `n_instances`, `n_count_human`, `count_source`, `count_conflict`, `n_gt`). `count_conflict` 는 남긴 3,964장 전부 0.
- 남긴 3,964장의 action(0924): keep 3,779 · needs_human 134 · mask_fixed 40(사과 1·복숭아 39) · image_replaced 11.

## 4. 권고 (정본 전환은 사람이 정하세요)

1. **0924 를 정본 후보로 삼는 것을 권합니다.** 잃는 사진은 사람·팀이 근거를 남기고 뺀 것뿐이고, 이미지·분할 묶음이 그대로라 이전 결과와 비교가 쉽습니다.
2. **다만 두 가지를 먼저 확인해야 합니다.**
   - 포도 103장 제거로 분할 묶음이 2403→2300 이 됩니다. 5-fold 배정(교수님 프로토콜, P=2,304 run)과 장수가 달라지므로 **전환 시점을 학습 sweep 시작 전에 맞춰야** 합니다. 이미 도는 sweep 이 있으면 섞지 마세요.
   - 복숭아 `instances/` 122장은 자동 초벌이라 «열매 번호가 사람 확인됨» 으로 쓰면 안 됩니다.
3. 문제 있음 판정 4장(복숭아 2·블루베리 2)은 고치면 다시 넣을 수 있습니다. 툴에서 고치는 대로 0925 이후 판이 필요합니다.
4. 0924 는 v3 이후 사람이 새로 손댄 사진이 사과 1장·복숭아 1장 정도라, «사람이 확인한 라벨» 은 v3 와 크게 다르지 않습니다. 정본 전환의 이득은 **중복 제거 103장**이 대부분입니다.

## 확인하지 못한 것
- `260920_grape_삭제대상.md` 원문과 103장이 실제로 일치하는지는 열어 보지 않았습니다.
- 사과 `image1` 의 번호 95→96/97 표기는 manifest(`n_gt=95`, `n_instances=96`)와 파일 값이 달라 보입니다. 다음 빌드에서 `n_gt` 갱신 여부를 보면 됩니다.
