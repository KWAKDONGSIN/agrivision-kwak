작성: 2026-09-19 22:58
작성자: VS Code 세션(Claude). 읽는 이: **Codex** (Claude 한도가 찼을 때 감시 스크립트가 자동으로 띄운다)

# 너는 왜 불려 왔나
- 사용자 곽동신(데이터사이언스 초보, 한국어로 쉽게)이 2026-09-19 21:40·23:00 에 지시: «Claude 한도가 가득 차면 바로 Codex 로 다 넘겨라. 모든 권한을 쓰게 하고 자동으로 이어서 하게 하라.»
- Claude(tmux 세션 `claude2`)가 하던 긴 작업이 한도로 멈췄다. 사용자는 자리에 없다. **묻지 말고** 이어서 끝까지 한다. 갈림길에서는 합리적인 기본값을 고르고 무엇을 가정했는지 작업기록에 적는다.

# 제일 먼저 읽을 것 (이 순서로)
1. `/data/project/2026summer/kds0206/CLAUDE.md` — 공통 규칙(작업기록 갱신·문서 이름·확인 질문 금지)
2. `/data/project/2026summer/kds0206/작업기록.md` 의 「3. 다음 할 일」 맨 위 **«🧭 지금 어디»** — Claude 가 멈춘 자리. 그리고 「2. 작업 이력」 위쪽 10개 항목.
   (파일이 5,700줄이 넘는다. 통째로 읽지 말고 `grep -n "지금 어디\|^## \|^### 2026-09-19\|^### 2026-09-20"` 로 자리를 찾아 그 부분만 읽는다.)
3. 남은 일의 지시서 — 전부 `/data/project/2026summer/kds0206/문서/` 안:
   - `260919_상자카운팅_논문근거_5사이클_지시.md` (카운팅 5사이클 — 거의 끝났을 것)
   - `260919_툴_구조정리_인수인계_5사이클_지시.md` (구조 정리 5사이클)
   - `260919_속도단축_병렬화_지시.md` (겹쳐 돌리는 규칙)
   - `260919_깃허브_자동올리기_지시.md` (맨 마지막: 깃허브에 올리기)
   - 폰용 전체 정리 문서(md + PDF + 툴 정적 페이지) 지시는 작업기록 2026-09-19 15:47 항목
4. 진행 중이던 단계의 산출물: `/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/cycles/260919_count/` 와 `cycles/260920_structure/` 의 `cycle_n/stage*.md`. **마지막으로 쓰인 stage 파일**을 읽어 어디까지 됐는지 판단한다. 반쯤 쓰인 단계는 처음부터 다시 하지 말고 이어서 한다.

# 남은 순서 (작업기록의 «지금 어디» 가 우선 — 거기 적힌 자리부터)
카운팅 사이클 5 → 구조 정리 사이클 1~5 → (원래는 Codex 공격적+보통 검수 → Fable 종합) → 폰용 정리 문서 → 깃허브 올리기.
- 네가 Codex 이므로 «Codex 검수 2회» 는 네가 그대로 한다(공격적 1회, 보통 1회, 각각 새 시각으로). «Fable 종합» 은 Claude 가 없으니 **네가 종합 판정서를 쓰되 맨 위에 «Claude 한도로 Codex 가 대행» 이라고 적는다.**
- 각 사이클은 원래 «작업(Opus) → 검수(Opus) → 판정(Fable)» 3단계다. 너 혼자이므로: 작업을 끝낸 뒤 **검수를 별도 단계로**(자기 시험을 재사용하지 말고 다른 경로로 재현) 하고 `stage2_review.md`·`stage3_final.md` 를 같은 자리에 같은 형식으로 쓴다. 파일마다 첫 줄에 `대행: Codex (Claude 한도 초과)` 를 적는다.

# 반드시 지킬 것 (권한은 전부 열려 있다 — 그래서 더 조심한다)
- ⛔ **남의 프로세스 kill 금지.** 서버는 공용이다. 5100·5105(교수님 주피터)·5001(root)·다른 계정의 프로세스는 건드리지 않는다. 내(kds0206) 것이라도 실서버(포트 5111)와 주피터(5101)는 아래 경우 말고는 끄지 않는다.
- ⛔ **삭제 금지.** `rm -rf` 를 쓰지 않는다. 지워야 하면 `_archive/` 로 옮기고 목록을 남긴다. 기존 체크포인트·결과·데이터셋을 덮어쓰지 않는다(새 날짜 폴더에 만든다).
- ⛔ 원본 데이터(`datasets_resized_2mp`·`datasets_reviewed_*`·`datasets_merged_260918_v3`·툴 `data/`)는 읽기만. 팀원·교수님 폴더(`platform/work/` 의 남의 폴더, `platform/planning/`)는 읽기만.
- 모든 코드 작업은 **모래상자 복사본**에서. 실서버 5111 은 구조 정리 사이클 5 의 전체 회귀 통과 뒤 `run.sh restart` 한 번만.
- «동작 무변경»(구조 정리)의 증거는 기준선 sha256·API JSON 동일로만 인정한다. 통과 못 하면 전환하지 말고 실서버는 지금 코드로 둔다.
- 깃: `kds0206/semantic-segmentation` 의 origin(인훈 계정)에는 **절대 push 금지**. 내 저장소는 `260919_깃허브_자동올리기_지시.md` 대로만. `--force` 금지.
- GPU 학습은 이 작업 범위에 없다. 돌리지 않는다.
- 시각은 추측하지 말고 `date` 로 재서 적는다. 날짜는 YYYY-MM-DD.
- 긴 명령은 `nohup`·`timeout` 을 쓴다. 브라우저 시험은 `260916_라벨링툴/scripts/ff.py`(headless Firefox, 스크린샷은 `~/ff_shots/` 에만). 끝나면 네가 띄운 모래상자 서버·geckodriver 는 네 PID 만 골라 끈다.

# 기록 (세션이 또 끊겨도 이어지게)
- 단계를 시작할 때마다 작업기록 「3. 다음 할 일」 의 **«🧭 지금 어디»** 줄을 고친다(날짜·시각·무엇을 하는 중·다음은 무엇).
- 단계를 끝낼 때마다 「2. 작업 이력」 맨 위에 항목 추가(요청 요약·만든/고친 파일 경로·못 끝낸 것). 제목에 `(Codex 대행)` 을 붙인다.
- 진행 요약을 `/data/project/2026summer/kds0206/logs/handover/STATUS.md` 에 한 줄씩 덧붙인다(`date '+%F %H:%M'` + 한 일).
- **전부 끝나면** `/data/project/2026summer/kds0206/logs/handover/ALL_DONE` 파일을 만든다(내용: 끝난 시각과 요약 3줄). 감시 스크립트가 이 파일을 보고 너를 다시 부르지 않는다.
- **더 못 가면**(같은 자리에서 세 번 실패, 권한·한도 문제 등) `logs/handover/STUCK` 파일에 어디서 왜 막혔는지, 사용자가 돌아와 무엇을 하면 되는지 적고 멈춘다. 억지로 돌파하지 않는다.
- 한 번의 실행에서 다 못 끝내도 된다. 감시 스크립트가 같은 대화를 이어서 다시 부른다. 그러니 **단계 하나를 제대로 끝내고 기록하는 것**이 여러 단계를 대충 하는 것보다 낫다.
