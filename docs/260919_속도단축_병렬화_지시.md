작성: 2026-09-19 21:36
작성자: VS Code 세션(Claude) → tmux 세션 `claude2` 가 실행

# 사용자 요청 원문 (2026-09-19 21:33)
«최대한 빠르게 만들려면 어떻게 해야 되는가?»

# 원칙 — 검수 횟수·품질 기준은 그대로, **기다리는 시간만 없앤다**
- 5사이클 × (Opus5 작업 → Opus5 검수 → Fable 판정), 홀수 공격적·짝수 보통, 동작 무변경, 모래상자, 실서버 전환은 구조 사이클 5 에서만 — **전부 그대로**.
- 바꾸는 것은 «앞 단계가 끝나길 그냥 기다리던 자리» 를 겹쳐 돌리는 것뿐.
- 지금 돌고 있는 카운팅 사이클 4 2차 에이전트는 **건드리지 않는다**(중단·재시작 금지).

# 겹쳐 돌릴 자리 5곳
1. **카운팅 사이클 5(종합) ∥ 구조 사이클 1(기준선).**
   카운팅 사이클 4 3차 판정·실서버 켜기가 끝나 코드가 굳는 즉시 두 개를 동시에 시작.
   - 구조 1 이 먼저 할 것: `_archive/pre_refactor_260920/` 스냅샷 · `_backup_*` 195개 옮기기 · `tests/` 모으기 · `run_all.sh`.
   - 기준선 sha256·API JSON 은 `run_all.sh --rebaseline` 같은 **한 줄 명령으로 다시 뜰 수 있게** 만든다.
   - 카운팅 사이클 5 가 코드를 한 줄이라도 고치면(소수정) → 그 뒤에 기준선을 다시 뜨고, 구조 1 검수는 **다시 뜬 기준선**을 본다. 카운팅 5 가 문서만 고치면 그대로.
2. **구조 사이클 2(서버) ∥ 구조 사이클 3(화면).** 구조 1 합격 직후 동시에 시작.
   - 파일 소유를 나눈다: 사이클 2 = `app/*.py`·`core/`·`api/`·`domain/` 만 / 사이클 3 = `app/static/**` 만. 서로의 파일은 읽기만.
   - 모래상자·포트를 따로 쓴다. 각자 자기 검수·판정까지 끝낸 뒤, **둘을 합친 판에서 `run_all.sh` + 브라우저 10장 시나리오를 한 번 더** 돌려 합격해야 사이클 4 로 간다(합친 판 시험 = 생략 금지).
3. **구조 사이클 4 의 문서 초안을 미리.** 사이클 2·3 이 도는 동안 README 의 1(5분 시작)·3(데이터 파일 형식)·5(규칙표)·8(한계) 절 초안을 별도 에이전트가 써 둔다(구조에 안 걸리는 절). 2·4·6·7 절(구조도·API 표·시험·레시피)은 2·3 합격 뒤에 채운다.
4. **Codex 공격적 ∥ Codex 보통.** 질문 파일 두 개를 구조 사이클 5 2차 검수가 도는 동안 미리 만들어 두고, 사이클 5 합격 직후 **두 개를 동시에** 던진다. 한도에 막히면 1회만 재시도 후 나머지 순서 진행(기존 규칙).
5. **폰용 정리 문서 뼈대를 미리.** 구조 사이클 4 가 도는 동안 md 뼈대(이미 끝난 부분: 그림판 5사이클·통합 데이터셋·카운팅 5사이클·논문 대조표)를 써 둔다. Codex·Fable 종합 결과는 나중에 그 단락만 채우고 PDF·정적 페이지를 만든다.

# 단계 안에서 줄일 것 (품질과 무관한 낭비만)
- 2차 검수가 1차 재현용으로 **통합 데이터셋 전체 재빌드(11GB)** 를 도는 일은 검수 1회당 1번까지. 같은 검수 안에서 두 번째부터는 복숭아 125장 + 과일별 20장 표본으로.
- 에이전트를 띄울 때 지시문에 «읽을 파일 목록» 을 경로로 못 박아 준다(탐색 시간 절약).
- 끝난 에이전트의 반복 알림(«finished» 재통지)에는 답하지 않는다.
- 각 단계 시작 때 작업기록 §3 «지금 어디» 한 줄은 그대로 유지. 병렬일 때는 **두 줄**(무엇과 무엇이 동시에 도는지).

# 하지 않는 것
- 검수 단계 생략·합치기, 사이클 수 줄이기, 공격적 검수를 보통으로 낮추기 — 안 한다.
- 구조 사이클 2·3 을 기준선(사이클 1) 합격 전에 시작 — 안 한다(기준선 없이는 «동작 무변경» 을 증명할 수 없다).
- 구조 사이클 4 의 «맥락 없는 새 에이전트 시험» 을 미리 — 안 한다(구조가 굳어야 의미가 있다).

# 예상 효과 (지난 사이클 실측 소요 기준)
- 순차 그대로: 전체 끝 ≈ 2026-09-20 23:00
- 위 5곳 병렬: 1번 −약 3시간 · 2번 −약 4시간 · 3~5번 −약 2시간 → **전체 끝 ≈ 2026-09-20 14:00 전후**(빠르면 12시, 늦으면 17시)
