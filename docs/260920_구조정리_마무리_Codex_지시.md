작성: 2026-09-20 08:45
작성자: 곽동신 개인 Claude 세션(Opus 5). 읽는 이: **Codex** (gpt-6-astra, 전체 권한)
앞선 지시서: `260919_한도초과시_Codex_인계_지시.md` — **그 «반드시 지킬 것» 과 «기록» 절은 이 지시서에도 그대로 적용된다.**

# 🔴 이번 작업의 경계 — 먼저 읽어라

**너는 실서버를 재시작하지 않는다.** `run.sh restart` · `run.sh stop` · `kill` 을 **실행하지 마라.**
포트 5111 의 PID 1467097 은 이 작업이 끝날 때까지 **그대로 돌고 있어야 한다.**

네가 할 일은 **«언제든 사람이 한 줄만 치면 전환되는 상태»를 만들어 놓는 것**까지다.
전환 자체는 사용자가 직접 한다. 네가 준비를 끝내면 사용자가 칠 명령 한 줄을 알려 주고 멈춘다.

# 너는 왜 또 불려 왔나

2026-09-20 03:41:25 에 네가 `ALL_DONE` 을 쓰고 끝냈다. 사용자가 아침에 결과를 확인했고,
**«운영 전환 보류» 로 닫힌 것이 사실은 «반쯤 전환된 상태»** 라는 것을 발견했다.

- 디스크의 `app/` 은 **이미 새 구조**다(`server.py` 93줄, `api/`·`core/`·`domain/`, `static/js/` 12파일, 옛 `app.js` 는 18줄 껍데기).
- 그런데 돌고 있는 프로세스 **PID 1467097 은 00:04:44 에 뜬 옛 코드**를 메모리에 물고 있다.
- 즉 **화면(정적 파일)은 이미 새 것, 서버 파이썬만 옛 것**이다. 네가 남긴 «엄격 기준선 정적 6칸 차이» 가 정확히 이것이다.
- 이 상태가 위험한 이유: **서버가 어떤 이유로든 죽었다 뜨면 그 순간 검증 없이 새 코드로 전환된다.**

사용자 지시(2026-09-20 08:2x): **«codex 로 나머지 부족한 것을 완성시켜라.»**
그래서 이번에는 남은 것을 **전부 끝내고**, 전환 직전 상태까지 만들어 둔다. 묻지 말고 진행한다.

# 지금 상태 (2026-09-20 08:40 실측 — 믿지 말고 네가 다시 확인해라)

| 항목 | 값 |
|---|---|
| 실서버 | PID **1467097** · `python -u server.py` · 00:04:44 기동 · 포트 5111 · **건드리지 마라** |
| 서버 로그 마지막 줄 | **03:29:33** (네가 한 점검). 그 뒤 사람 접속 0 |
| 운영 `app/` vs 후보 `cycles/260920_structure/codex_resume/candidate/app/` | `.py` 실제 차이 **8파일**(`api/{boxes,counts,dashboard,dupes,export,instances,masks,photos}.py`) + `README.md`. 나머지는 `__pycache__`·`cache` 뿐 |
| 후보 전체 회귀 | 1,124 단정 통과 / 0 실패 / 527초 (구조5, 03:18) |
| 엄격 기준선 | rc=1, 차이 **6칸** = `/` · `/box` · `/static/app.js` 각각의 bytes·sha256 |
| 깃허브 | `KWAKDONGSIN/agrivision-kwak` main `dcc5291` (600파일). 원격·로컬 일치, 작업트리 clean |

# 할 일 — 이 순서로

## A. 되돌릴 길부터 — **1·2 는 이미 끝났다. 3 부터 시작해라.**
1. ✅ 끝남. 시작 시각 **2026-09-20 09:04:39** (`finish_260920/evidence/start_time.txt`).
2. ✅ 끝남. 운영 `app/` **84파일**의 sha256 을 `finish_260920/evidence/pre_change_app_sha256.json` 에 남겼다
   (`__pycache__`·`cache`·`logs` 제외). **다시 뜨지 말고 이것을 «바꾸기 전» 기준으로 써라.**
   - 참고로 이미 확인된 것: 스냅샷 `_archive/pre_refactor_260920/` 은 실재하고 `MANIFEST.txt` 1,608줄,
     `app/` 1,563파일, 그 안의 `server.py` 는 **2,052줄(옛 코드)** 이다. 되돌릴 길은 살아 있다.
   - 실서버 PID 1467097 의 cwd 는 `260916_라벨링툴/app` 이다.
3. **되돌리기 리허설을 모래상자에서 한 번 실제로 해 보고** 결과를 적는다. 여기서 실패하면 더 가지 말고 `STUCK` 에 적는다.

## B. 후보 → 운영 «파일» 반영 (프로세스는 그대로 둔다)
4. 후보 `candidate/app/api/*.py` 8파일과 `candidate/app/README.md` 를 운영 `app/` 에 반영한다.
   - 덮어쓰기 전 원본을 `_archive/pre_finish_260920/` 에 **복사**해 둔다(옮기지 말고 복사. 삭제 금지).
   - `__pycache__` 는 반영 대상이 아니다.
   - ⚠️ 파일을 바꿔도 **돌고 있는 프로세스는 옛 코드 그대로**다. 그것이 의도다.
5. **상자 전부무효 보호**가 실제로 작동하는지 **모래상자에서** 재현한다.
   `boxes:[null]` · 숫자 항목 `[3]` · 그 밖의 무효 1종 → **HTTP 400 이고 기존 상자 파일 바이트가 보존**되는지 확인.
   유효 저장·혼합 입력·«명시적 빈 목록 = 되돌리기» 계약은 **그대로 살아 있어야 한다**(회귀 금지).
6. `scripts/ff.py` 의 «`LABELTOOL_URL` 없으면 서버에 붙기 전에 중단» 보호를 운영본에 반영한다.
7. **비유한 좌표 버그**를 고친다: 좌표에 `Infinity`/`NaN` 이 오면 지금은 `round()` 에서 예외가 나 **500** 이다.
   **400 + 오류 메시지**로 바꾸고 기존 파일은 손대지 않는다.
   - 이것만 «동작 변경» 이다. 사용자가 이번에 명시적으로 허가했다. 판정서에 **별도 항목**으로 적는다.

## C. 기준선 정리 — 6칸을 «지우지» 말고 «설명»해라
8. 정적 6칸(`/`·`/box`·`/static/app.js` 의 bytes·sha256)은 **사이클 3 의 화면 분리로 생긴 의도된 변경**이다.
   근거는 이미 있다 — 12장 스크린샷 바이트 동일, 36개 UI 상태 동일, 콘솔 오류 0, 탭 전환 8/8.
9. 옛 기준선을 조용히 덮어쓰지 말고 다음을 **모두** 한다:
   - 옛 기준선을 `tests/fixtures/baseline_260920_pre_split/` 로 **보존**한다(이름을 바꿔 남긴다).
   - 새 기준선을 `--rebaseline` 로 새로 뜬다.
   - `tests/fixtures/BASELINE_CHANGELOG.md` 에 **«무엇이 · 왜 · 누가 승인해서» 바뀌었는지** 적는다.
     승인자는 **사용자 곽동신 (2026-09-20 08:2x «진행해줘»)**. 6칸 각각의 옛/새 sha256 을 표로 적는다.
   - 판정서에 «원래 기준선이 통과해서가 아니라 **6칸 차이를 사람이 승인해서** 전환 가능 상태로 본다» 고 분명히 쓴다.
     통과했다고 쓰지 마라.

## D. 전환 관문 (모래상자에서만)
10. 반영된 코드로 **전체 회귀를 한 번 더** 돌린다: `bash tests/run_all.sh --browser --check-baseline`.
    - 단정 실패가 **하나라도** 있으면 A-3 의 길로 파일을 되돌리고 `STUCK` 에 적는다.
    - 새 기준선 기준으로 rc=0 이어야 한다.
11. 브라우저 12장(4과일×3장)을 **다시** 찍어 비교한다. 의미 있는 차이가 있으면 멈추고 적는다.
12. **모래상자에서 새 코드로 서버를 띄워** 스모크를 돌린다(포트는 5401~5799 중 네 것).
    로그인 전 `/api/fruits` 401 → 로그인 → 4과일 목록 → 각 과일 첫 사진 item·boxes·실제 이미지 200/PNG 서명 →
    상자 1개 저장·되돌리기 → 내보내기 안내. 끝나면 **네가 띄운 것만** 끈다.

## E. 전환 준비물 만들기 (실행은 사람이 한다)
13. `finish_260920/전환하기.md` 를 만든다. 사용자가 읽고 그대로 따라 할 수 있게:
    - 사용자가 칠 명령 **한 줄**(작업 폴더 포함 전체 경로로)
    - 치기 전 확인할 것: `app/logs/server.log` 마지막 15분 안에 외부 IP 접속이 없을 것
    - 친 뒤 확인할 것: 새 PID, 로그인, 사진 한 장 열기
    - **잘못됐을 때 되돌리는 명령 한 줄** (`_archive/pre_finish_260920/` 복원 + 다시 띄우기)
    - 되돌리기 명령이 정말 맞는지 **모래상자에서 미리 한 번 해 보고** 확인했다고 적는다
14. 전환 뒤에 사용자가 돌릴 수 있는 스모크 스크립트 `finish_260920/after_restart_smoke.py` 를 만들어 둔다
    (D-12 와 같은 항목을 5111 에 대고 확인, 읽기 위주, 상자 저장은 되돌리기까지 한 쌍으로).

## F. 마무리 기록

> 🔴 **이번에는 Claude 가 뒤를 봐 주지 않는다.** 사용자의 개인 Claude 도 주간 한도를 90% 썼다.
> 그러니 **A~E 를 끝낸 뒤, 판정서를 쓰기 전에 «별도 검수 패스»를 네가 직접 한다.**
> 1차(작업) → 2차(검수) → 3차(판정) 세 파일을 `finish_260920/` 에 같은 형식으로 남겨라:
> `stage1_work.md` · `stage2_review.md` · `stage3_final.md`.
> 2차는 **1차의 로그를 인용하지 말고** 적어도 한 경로를 새 사본·새 포트로 다시 재현해서 쓴다.
> 2차에서는 최소한 이 셋을 의심해라 — ① 후보 반영이 정말 다 들어갔나(빠진 파일 없나)
> ② 새 기준선이 옛 것을 조용히 덮어 버리지 않았나 ③ B-7 좌표 수정이 정상 입력을 막지 않나.

15. **최종 판정서를 다시 쓴다.** 지금 `문서/260920_툴_구조정리_5회검수_최종판정.md` 는 5.7KB 로,
    같은 날 카운팅 판정서(41.9KB)에 비해 너무 얇다. 다음을 반드시 넣어라:
    - 전후 줄 수·파일 수·시험 수 (지시서 §4 사이클 5 가 요구한 것)
    - 새 사람 인수인계 시험 실측(7초 / 8분33초 / 8분11초)
    - **못 한 것을 솔직히**: 긴 함수 3개, window 전역 5개, Any 48개, 동시편집·변경이력·재학습 고리 없음
    - 이번에 바꾼 것(B-5·6·7)과 **기준선 승인**(C-9)을 각각 별도 항목으로
    - **«파일은 새 것, 돌고 있는 프로세스는 옛 것» 이라는 현재 상태**와, 전환이 사람 손에 남아 있다는 것
16. 작업기록 「2. 작업 이력」·「3. «🧭 지금 어디»」 갱신, `STATUS.md` 에 줄 추가.
17. 깃허브 `KWAKDONGSIN/agrivision-kwak` 에 **바뀐 코드·문서만** 다시 올린다.
    - 비밀 검사를 **다시** 돌린다(새로 들어간 파일이 있으므로). `NOT_UPLOADED.md` 갱신.
    - 사진·마스크·번호·상자·데이터·키·체크포인트·녹취는 **여전히 제외**.
    - `--force` 금지. 인훈 님 origin 에 push 금지.
18. **사용자에게 보고할 글을 네가 직접 쓴다** (Claude 가 대신 써 주지 않는다).
    `finish_260920/260920_마무리_보고.md` — 곽동신(데이터사이언스 초보)이 읽을 글이다.
    - 쉬운 한국어, 전문용어는 풀어서. **한 장을 넘기지 마라.**
    - 무엇을 고쳤나 / 무엇을 확인했나(숫자) / **못 한 것** / **사용자가 지금 할 일 한 가지**(전환) 순서.
    - 추정과 실측을 섞지 마라. 실측이 아닌 것은 «추정» 이라고 쓴다.
    - 맨 끝에 **카톡에 붙일 5줄 요약**도 함께.
    - 🔴 **사용자는 폰으로 읽는다.** 같은 내용을 **폰용 HTML** 로도 만들어
      `app/static/finish_260920.html` 에 올려라(툴 주소 `/static/finish_260920.html` 로 열린다).
      03:27 에 만든 `summary_260920.html` 과 같은 조건 — **폭 390px·글자 17px·가로 넘침 없음**을
      실제로 확인하고, 비로그인 302 / 로그인 200 도 확인해서 적는다.
      ⚠️ 새 정적 문서를 **추가만** 하는 것이다. 앱 코드 교체·재시작이 아니다.
19. **한 번에 다 못 끝내면** — 마지막에 `STATUS.md` 맨 아래에 «다음에 이어받는 법» 을 적어라:
    남은 단계 번호와, 사용자가 칠 이어받기 명령
    (`codex exec resume --last --dangerously-bypass-approvals-and-sandbox --skip-git-repo-check -m gpt-6-astra` 에
    «이어서 해» 를 넣는 형태). 중간에 멈추는 것은 괜찮다. **단계 하나를 제대로 끝내고 기록하는 것**이
    여러 단계를 대충 하는 것보다 낫다.
20. 끝나면 `logs/handover/FINISH_DONE` 을 만든다(끝난 시각 + 요약 3줄 + «전환은 사람이 할 일로 남겨 둠»).
    **`ALL_DONE` 은 지우거나 고치지 마라** — 어젯밤 기록이다.

# 이번에도 지킬 것
- ⛔ **실서버 5111 재시작·정지 금지.** `run.sh restart|stop`·`kill`·`pkill` 을 실행하지 마라.
- ⛔ 삭제 금지. 덮어쓰기 전에 `_archive/` 로 복사. `rm -rf` 금지.
- ⛔ 남의 프로세스 kill 금지. 5100·5105·5001·다른 계정 것은 손대지 않는다.
- ⛔ 원본 데이터(`datasets_*`, 툴 `data/`)는 읽기만. 팀원 폴더·`platform/planning/` 은 읽기만.
- 시각은 `date` 실측. 추정값을 실측처럼 쓰지 마라.
- 자기 시험 로그만 인용하지 말고 **최소 한 경로는 새로 재현**해라.
- 같은 자리에서 세 번 막히면 `STUCK` 에 적고 멈춘다.
- 모든 새 문서 첫 줄에 `대행: Codex` 를 적는다.

# 기록 위치
- 이번 작업 폴더: `platform/work/kwak_dongsin/260916_라벨링툴/cycles/260920_structure/finish_260920/`
  (`evidence/` 하위에 증거 JSON·로그)
- 진행 요약: `kds0206/logs/handover/STATUS.md` (단계마다 한 줄)
- 완료 표식: `kds0206/logs/handover/FINISH_DONE`
- 막힘: `kds0206/logs/handover/STUCK`
