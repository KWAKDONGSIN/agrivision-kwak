대행: Codex (Claude 한도 초과)
작성: 2026-09-20 02:52

# 라벨링 툴을 처음 이어받는 사람의 첫 1시간

툴 위치는 `/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/`입니다.
지금 구조 후보는 그 아래 `cycles/260920_structure/codex_resume/candidate/`에 있습니다.
후보 검증이 끝나기 전에는 운영 서버를 다시 켜거나 정적 파일을 덮지 않습니다.

1. 0~5분: `app/README.md` 1절을 읽습니다. 원본 데이터는 읽기만 합니다. 내 작업용 사본인지 먼저 확인합니다.
2. 5~15분: README의 모래상자 서버 예제로 로그인 화면 응답을 확인합니다. 실서버 5111은 건드리지 않습니다.
3. 15~30분: `bash tests/run_all.sh --browser`를 실행하고 `tests/_out/bundles.tsv`와 로그를 봅니다. 자료가 없는 공개 저장소만으로는 전체 시험을 재현할 수 없습니다.
4. 30~40분: README 5절 규칙표에서 A1(이진본이 번호본을 자른다)과 A3(N6 쓰기 규칙)을 찾습니다.
5. 40~60분: README 7절의 응답 칸 추가 연습을 **연습 사본에만** 합니다. 본문 저장 규칙은 바꾸지 않습니다.

A1의 실제 함수는 `app/domain/rules.py:cut_by_binary()`이며 `app/api/instances.py:load_inst()`에서 부릅니다.
상자 저장은 `POST /api/boxes`, 함수 `app/api/boxes.py:api_boxes_save()`입니다. `/api/save_boxes`라는 주소는 없습니다.
번호 저장은 `POST /api/save_instances`이며 PNG 필드는 `png`입니다.

처음 지울 때: E 지우개 / G 자동지움 / 오른쪽 더 보기의 전부 지우기. 번호는 ③ 번호 모드에서 선택 후 D.
원본은 바뀌지 않으며, 저장 전 Ctrl+Z를 쓸 수 있습니다.

대화 이력 없이 README만 받은 새 에이전트 시험을 끝냈습니다.
A1 찾기 7초, 서버 실행과 첫 전체시험 8분33초, 응답 필드 추가와 두 번째 전체시험 8분11초였습니다.
두 번 모두 1,102 통과/0 실패입니다. 최종 시각은 2026-09-20 03:07:53입니다.
근거: `cycles/260920_structure/codex_resume/handoff_trial/260920_handoff_trial.md`.
시험 사본의 연습 필드는 제품 후보에 합치지 않았습니다.

현행 한계: 오래된 화면 저장을 revision으로 거절하지 않습니다. 같은 사진을 동시에 맡지 마세요.
JS 전역 호환 속성 5개와 역방향 UI 호출, 단축키 표/분기의 중복, 긴 함수 3개는 남아 있습니다.
타입 설명은 공개 함수 113개에 보완했지만 복합값 Any가 남아 완전한 타입 검사는 아닙니다.

상자 요청이 전부 무효일 때 기존 상자를 지우는 위험의 보호 수정은 별도 `codex_resume/safety_fix/`에 보존했습니다.
사용자의 데이터 소실 예외 수정 지시에 따라 후보에 명시적으로 포함했습니다. 이 잘못된 요청의 응답만 200→400으로 바뀌며, 기존 고정 표본 API·내보내기 지문이 그대로인지 따로 검사합니다. 실서버 적용은 전체 검수 뒤 구조 5가 판정합니다.
