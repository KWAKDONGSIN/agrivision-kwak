작성: 2026-09-19 20:10
작성자: VS Code 세션(Claude) → tmux 세션 `claude2` 가 실행
순서: **「상자+개수 세기」 5사이클(진행 중) 이 끝난 직후 → 이 작업 → 그 뒤에 Codex 검수 → 폰용 정리 문서.** 시간 제한 없음(사용자 14:03 지시).

# 사용자 요청 원문 (2026-09-19 20:05)
«"지금은 동작하지만 다른 사람이 이어받기는 어렵습니다" 라는 단점을 지금 돌아갈 때 고쳐 가면서 리뷰해 줘. 최대한 완벽하게 구조를 짜고 싶어.»

# 0. 지금 무엇이 문제인가 (20:05 실측)
- 실제 코드 10파일 **8,677줄**: `server.py` 2,052 · `app.js` 2,661 · `ui.js` 1,297 · `instances.py` 681 · `boxes.py` 474 · `help.html` 596 · `index.html` 357 · `style.css` 360 · `dupes.py` 113 · `maskio.py` 86.
- `app/`·`app/static/` 안에 `_backup_*` 파일 **195개**가 실제 파일과 섞여 있다. 처음 온 사람은 어느 것이 진짜인지 모른다.
- 회귀 시험(`regress_all.py`, `sim.js`, `boxsim.js`, `modesim.js`, ff 시나리오)이 `cycles/*/cycle_n/stage*/` 여기저기에 흩어져 있고 «한 번에 돌리는 명령» 이 없다.
- `app.js` 한 파일에 상태·API·캔버스·마스크 도구·상자·번호·개수·목록·단축키가 다 들어 있다. `server.py` 도 인증·상태 저장·목록·마스크·내보내기·중복이 한 파일이다.
- 규칙(«이진본이 번호본을 자른다», N6 쓰기 규칙, 되돌리기는 `src` 표식 있는 것만, 0개 저장 = 삭제 …)이 코드 주석과 사이클 문서에만 있고 **한 장의 목록**이 없다.

# 1. 목표 — «처음 온 사람이 1시간 안에 돌리고, 반나절 안에 기능 하나를 고칠 수 있다»
측정 가능한 합격 기준(사이클 4 에서 실제로 잰다):
- (가) 맥락 없는 새 에이전트가 README 만 보고 **서버 실행 + 전체 시험 통과** 까지 30분 이내.
- (나) 같은 에이전트가 «상자 저장 응답에 칸 하나 추가» 를 README 의 레시피대로 해서 시험이 통과하기까지 2시간 이내.
- (다) «이진본이 번호본을 자른다 규칙은 어디에 있나» 를 README 규칙표에서 30초 안에 찾는다(파일·함수명).
- (라) **동작 무변경**: 리팩터 전후로 ① 고정 표본에 대한 API 응답 JSON 동일 ② 내보내기(마스크·번호·상자 YOLO·counts.csv·manifest) **바이트 동일** ③ 기존 회귀 시험 전부 통과 ④ 실제 브라우저 10장 시나리오 동일.

# 2. 목표 구조 (번들러·프레임워크 추가 없음 — 지금처럼 Flask + 순수 JS `<script>` 순서 로딩)
```
260916_라벨링툴/
  app/
    server.py            ≤ 150줄: 앱 만들기 + 모듈 register 호출만
    core/                설정·경로(paths.py) · 인증(auth.py) · 상태 저장소(status_store.py: status.json 읽기/쓰기/잠금/백업 — 쓰는 길 하나) · 공통(util.py: now_str, err_json, lock_for)
    api/                 기능별 register(app, ctx) — 이미 boxes.py·instances.py 가 쓰는 방식 그대로
      photos.py(목록·item·썸네일) · masks.py(마스크 읽기/저장/되돌리기/제안) · boxes.py · instances.py · counts.py · dupes.py · export.py(데이터 정리 탭) · dashboard.py
    domain/              파일 형식 하나에 함수 하나: maskio.py · statusfmt.py(항목 스키마·confirmed_* 규칙) · rules.py(«자르기»·N6 등 데이터 규칙 함수 — 주석이 아니라 이름 있는 함수)
    static/
      index.html · help.html · style.css
      js/  state.js(S 하나) · api.js · view.js(캔버스·확대·보기 전환) · mask.js · boxes.js · instances.js · counts.js · list.js · export.js · keys.js(단축키 표 한 곳) · tour.js · main.js(순서대로 붙이기)
  tests/
    run_all.sh           한 번에: python 단위(pytest 없으면 assert 스크립트) + node 시뮬(sim/boxsim/modesim) + API 회귀(regress_all) + (선택) ff 브라우저 시나리오
    fixtures/            고정 표본(복숭아 3장 등 작은 것)과 기대값(JSON·sha256)
    unit/ sim/ api/ browser/
  scripts/               ff.py 등 운영 보조
  data/                  (그대로)
  _archive/backups_260920/   `_backup_*` 195개를 **옮긴다**(삭제 아님) + 목록 파일
  docs/  또는 app/README.md 재편(아래 3절)
```
- 이름 규칙: 파일 = 기능 명사(영문 소문자), 함수 = 동사+명사, 상수는 `core/paths.py`·`domain/rules.py` 에 모은다(매직 넘버 금지: 3000, 0.15%, 120장/쪽 등은 이름을 붙인다).
- 파이썬 공개 함수에 한 줄 docstring + 타입 힌트. 80줄 넘는 함수는 쪼갠다(단, 동작 무변경이 먼저 — 못 쪼개면 그대로 두고 목록에 남긴다).
- 상태 쓰기는 `status_store` 한 길만. 지금 여러 곳에서 status.json 을 직접 여는 것을 전부 그리로 모은다.
- JS: 전역은 `S`·`API`·`UI` 셋만. 파일 사이 의존은 «위에서 아래로» 만(main.js 가 마지막). 회귀 시뮬(boxsim.js 등)이 함수를 떼어 쓰므로 함수 이름은 유지한다.

# 3. 문서 (인수인계의 본체)
`app/README.md` 를 다음 차례로 **다시 쓴다**(지금 것은 `_archive` 로):
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
2. 구조도: 파일 → 역할 → 주요 함수 (표 한 장)
3. 데이터 파일 형식: `status.json` 항목 스키마(status/by/at/note/prev/src/confirmed/confirmed_boxes/confirmed_instances) · `boxes/<stem>.json` · `duplicates.json` · `masks_fixed` · `instances_fixed` · exports 폴더 구조 — 예시 JSON 포함
4. API 표: 주소·메서드·입력·출력·어느 파일
5. **규칙표**: 규칙 한 줄 · 왜(결정 날짜·문서 링크) · 코드 위치(파일:함수) · 지키는 시험 이름
6. 시험: `bash tests/run_all.sh` 와 각 묶음 뜻, 모래상자 띄우는 법, ff.py 사용법
7. 레시피: 과일 추가 / API 응답 칸 추가 / 확정 종류 추가 / 색 바꾸기 / 정적 파일만 바꿨을 때 vs 서버 재시작이 필요한 때
8. 알려진 한계·미결(동시 편집 경고 없음, 변경 이력 없음, 재학습 고리 없음) 과 결정 기록 링크(사이클 판정서들)
추가로 `문서/2609xx_툴_인수인계.md`: 처음 온 사람의 첫 1시간 순서(무엇을 먼저 읽고 무엇을 켜 보는가) — 사이클 4 의 «새 에이전트 시험» 결과로 다듬는다.

# 4. 5사이클 (홀수 공격적 · 짝수 보통 · 5 종합 / Opus5 작업 → Opus5 검수 → Fable 판정)
| 사이클 | 내용 |
|---|---|
| 1 (공격적) | **기준선 고정**: 고정 표본 API 응답·내보내기 sha256·회귀 결과를 `tests/fixtures/` 에 저장(리팩터 전 코드로). `tests/` 만들고 흩어진 시험을 모아 `run_all.sh` 한 번에 통과. `_backup_*` 195개 → `_archive/backups_260920/`(목록 파일). `_archive/pre_refactor_260920/` 에 app 전체 스냅샷(되돌릴 길). 검수는 «기준선이 정말 전부를 덮는가» 를 공격적으로(덮이지 않는 경로 목록) |
| 2 (보통) | **서버 분리**: `core/`·`api/`·`domain/`, `server.py` ≤150줄, status 쓰기 한 길, 규칙 함수화. 기준선 (라) 전부 동일 |
| 3 (공격적) | **화면 분리**: `static/js/*` 10파일, 전역 셋, 단축키 표 한 곳. 검수는 «동작 무변경» 을 공격적으로(브라우저 10장 시나리오 + 시뮬 + 예전 함수 이름 유지 여부) |
| 4 (보통) | **문서 + 새 사람 시험**: README 재작성·인수인계 문서 → 맥락 없는 새 에이전트(대화 이력 없이, 폴더만)에게 1절 (가)(나)(다) 를 시키고 시간을 잰다. 막힌 곳마다 README 를 고친다(두 번까지) |
| 5 (종합) | 최종 판정서 `문서/2609xx_툴_구조정리_5회검수_최종판정.md`: 전후 줄 수·파일 수·시험 수·새 사람 시험 시간, 못 한 것(솔직히), 실서버 전환(전체 회귀 통과 뒤 `run.sh restart`), 작업기록·§4 결정 갱신 |

# 5. 지킬 것
- **동작을 바꾸지 않는다.** 이 작업에서 기능을 추가·삭제하지 않는다. 고치고 싶은 버그가 보이면 목록에만 적고 판정서로 넘긴다(단, 데이터 소실 결함은 예외 — 즉시 고치고 별도 표기).
- 실서버 5111 은 사이클 5 전환 전까지 지금 코드로 계속 돈다(사람이 쓰고 있을 수 있음). 모든 작업은 모래상자 복사본에서. 전환 직전에 `data/` 는 건드리지 않았음을 확인.
- 스냅샷·아카이브는 옮기기만(삭제 금지). 사이클 기록 `cycles/260920_structure/cycle_n/`.
- 시각은 `date` 실측. 끝나면 이어서 Codex(공격적+보통) → Fable 종합 → 폰용 정리 문서(순서는 14:06·15:47 지시대로, 정리 문서에 이 구조 정리 결과도 한 단락).
