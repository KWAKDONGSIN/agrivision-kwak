작성: 2026-09-07 (3차 전면 재작성. 교수님 0905 프로토콜 + 09-07 분할 산출물 반영)

[메시지 1]
성문님, 질문 다시 정리해서 답합니다.
사진은 팀표준_4과일_2MP 쓰는 게 맞습니다. png인 것도 맞습니다.
폴드는 cv1~cv5 폴더가 아니라 교수님이 새로 만든 분할표를 씁니다.

[메시지 2]
png 관련
팀표준은 4과일 화소 수를 맞추려고 리사이즈해서 새로 저장한 사본이라 이미지, 마스크 전부 png로 통일했습니다.
png는 무손실이고, 원본 bmp와 팀표준 png를 픽셀 단위로 비교해봤는데 완전히 같습니다. 마스크도 같습니다.
짝은 확장자가 아니라 images, masks 폴더와 파일 이름으로 찾아서 둘 다 png여도 안 섞입니다.
팀 코드로 실제로 읽어봤고 전부 정상이었습니다.
팀 코드는 bmp를 안 읽어서 블루베리_6fold(bmp, 옛 분할)는 못 씁니다.

[메시지 3]
폴드 관련
0820 미팅 때 정한 게 5폴드 7:1:2였고, 그게 팀표준_4과일_2MP/splits/blueberry 의 cv1~cv6 폴더입니다.
그런데 교수님이 0905 프로토콜에서 이걸 바꾸셨습니다.
새 방식은 전체 20%를 파일럿(안에서 3폴드)으로 떼고, 나머지 80%를 M1~M5 다섯 덩어리로 나눠서 본실험에 씁니다.
그래서 cv1~cv5 폴더는 이제 본실험용이 아닙니다.

[메시지 4]
새 분할표 위치
platform/work/ahn_hongryul/pilot/protocol_splits/v2_seed3407/blueberry.csv
교수님이 09-07 오전에 만드셨고, 사진 경로가 팀표준 png로 적혀 있습니다.
블루베리 1195장 = 파일럿 239 + 본실험 956. 영상(Camera N Video (M)) 단위로 나눠서 누수 없습니다.
아직 동결 전 draft라 바뀔 수 있으니, 실험 돌리기 전에 교수님께 확정 여부만 확인하면 됩니다.

[메시지 5]
한 줄 정리
사진: 팀표준_4과일_2MP (png)
폴드: 교수님 분할표 blueberry.csv (pilot 3폴드 + main M1~M5)
안 쓰는 것: 블루베리_6fold, cv1~cv6 폴더, 타일링_512

[짝 문제만 짧게 답할 때]
확인했습니다. 문제 없습니다.
이미지는 images 폴더, 마스크는 masks 폴더에 따로 있고 짝은 파일 이름으로 찾습니다.
확장자는 안 봅니다. 그래서 둘 다 png여도 1111.png끼리 그대로 짝이 맞고 섞일 일이 없습니다.
팀 코드로 팀표준 블루베리를 실제로 읽어봤는데 train, val, test 전부 정상이었습니다.
그냥 팀표준_4과일_2MP 그대로 쓰시면 됩니다.
