작성: 2026-09-25

# 팀원 작업환경 전수 조사 — 라벨링 웹툴·초벌 모델·데이터셋에 가져올 것

## 조사 범위와 방법

- `find <dir> -newermt '2026-09-20' -type f` 로 각 디렉터리를 최근 파일부터 훑었다. 큰 이미지·가중치·conda 패키지는 이름·크기만 보고 열지 않았다.
- 09-24 22:00 이전에 이미 본 것으로 지시받은 choi_inhun의 `label_audit_260923`·`auto_label_260923`(run_grounded_sam.py)·`dup_audit_260917`은 내용을 다시 읽지 않고 목록만 확인했다.
- 읽기 권한 문제는 없었다. 대상 9곳 모두 접근 가능했다.

## 디렉터리별 결과

### choi_inhun (읽음)

- `260920_grape_삭제대상.md` — 곽동신 육안검수 99장 + 최인훈 추가확정 103장 = 포도 삭제 확정 202장, 보류 33장(후보쌍 4 + 후보_중간 27 + 애매 2). 원본은 `kds0206/datasets_resized_2mp/grape/images/`, 현행 분할표는 `260916_라벨링툴/final/round_d/protocol_splits_v4_reviewed/grape.csv`.
  - 쓸모: 데이터 품질 — 포도 202장을 다음 분할표 갱신에 반영할 수 있음.
  - 가져오는 법: 확정 202장 번호 리스트를 `protocol_splits_v4_reviewed/grape.csv`에서 제외 처리. 보류분은 아직 손대지 않음.
  - 우선순위: 중 (실행은 검출팀/곽동신 판단 필요, 삭제 자체는 라벨링툴 변경 아님)
- `label_audit_260923/`, `auto_label_260923/run_grounded_sam.py`, `dup_audit_260917/` — 이미 본 것으로 지시받음. 새 변경 없음(목록만 재확인).

### im_seonghu, park_seongmoon (읽음, 각각 code/ 안 .git pack만 09-20 이후 갱신)

- 두 사람의 `code/`는 semantic-segmentation 학습 코드(mmseg류, configs 다수)이며 im_seonghu와 동일한 git pack 해시 — 같은 저장소를 각자 clone해 쓰는 구조. 라벨링 툴과 직접 관련 없음, 세그멘테이션 학습 파이프라인.
- **park_seongmoon/260917_라벨링툴_열매번호편집_작업지시서.md** — 이것이 핵심 발견. 박성문이 09-17에 작성한, "우리 라벨링 툴(`260916_라벨링툴/app/`)에 **열매 번호(인스턴스 ID) 편집 기능**을 추가하라"는 AI용 지시서. 지우기/합치기/나누기/새로붙이기 4동작, `/instances`·`/api/save_instances` API, uint16 PNG 저장 규격까지 구체적으로 설계되어 있음. 완료 보고서(`2609XX_열매번호편집_보고서.md`)가 `260916_라벨링툴/`에 없고, `instances_fixed`/`gtIsInstance` 문자열도 현재 `app.js`/`server.py`에 없이 09-19 백업본에만 흔적이 있음 — **아직 우리 툴에 구현되지 않은 것으로 보임.**
  - 쓸모: 툴 기능 직결 — 사과 오류 38개, 블루베리 초벌 번호 45,124개를 고치는 데 필요한 기능이 이미 설계돼 있음.
  - 가져오는 법: 이 문서를 그대로 작업지시서로 채택해 구현 여부 재확인 후 진행.
  - 우선순위: **상**
- `park_seongmoon/apple_check/confirmed_errors.csv` 등 — 위 기능의 1차 사용 대상(사과 38개 오류) 데이터. 함께 가져와야 함.

### jjw0602 (읽음, 09-20 이후 새 파일 없어 전체 구조 확인)

- `INSTANCE_SEGMENT/` — 산업 결함(Cable, PCB, Capacitor 등) 인스턴스 분할 데이터셋 정리 + 모델조사(`랩미팅_모델조사`, `instance_segmentation_census_2026.md`). 우리 과일 세그멘테이션과 도메인이 달라 데이터 자체는 못 씀.
  - 쓸모: 낮음(다른 도메인). 모델 비교 방법론만 참고 가치.
  - 우선순위: 하

### lsh0616 (읽음, 09-20 이후 새 파일 없어 기존 문서 확인)

- **`260915_블루베리정답지_생성방안.md`** — 블루베리 인스턴스 정답이 없는 문제를 다룬 방법론 정리. 결론: SAM 등 pseudo-GT를 "자기 오차 모르고" 쓰면 안 되며, 사람이 직접 개수를 세는 A1(40장)을 앵커로 먼저 확보하고, 그다음 도구 제안 검수(D1) → SAM pseudo-GT(C2) 순으로 가라는 권장 경로. `blueberry_count_gt/` 산출물(계수 기준 이미지, 40장 표본, 양식) 준비 완료.
  - 쓸모: 우리 SAM 도우미로 블루베리 인스턴스 초벌을 만들 때 "이 초벌이 얼마나 틀리는지"를 검증할 앵커가 필요하다는 점, 그리고 `counting_standard.jpg`(꽃봉오리/씨방은 안 세는 기준)를 그대로 라벨링 기준으로 채택 가능.
  - 가져오는 법: `blueberry_count_gt/` 40장 표본과 계수기준 이미지를 라벨링 가이드/QA 기준으로 복사, SAM 초벌 정확도 검증에 A1 결과를 앵커로 사용.
  - 우선순위: 상
- `260917_라벨링툴_열매번호편집_작업지시서.md` — park_seongmoon과 동일 문서 사본(작성자 박성문, lsh0616 폴더에도 존재). 위 항목과 동일 건.
- `semantic-segmentation/`, `dataset_6fold`, `weights/` 등 — 세그멘테이션 학습 실험 전용. 라벨링 툴과 무관.

### nsm0404 (읽음)

- `experiments/2026-08-27_experiment/Capacitor_0/.../notes.md` — image_gen으로 합성 결함(빨강 마스크 기반) 생성 시도, "원본 크기 보존 실패로 채택 불가"라는 실패 기록. 산업 결함(Capacitor) 도메인, 과일과 무관.
  - 쓸모: 낮음.
  - 우선순위: 하

### plant (읽음)

- `work/lim/notes/260922_복합스트레스_연구주제_정리.md` — 식물 복합 스트레스 연구주제 정리. 라벨링툴/세그멘테이션과 무관한 별도 연구.
  - 우선순위: 하 (가져올 것 없음)

### dup_review (읽음, 빈 디렉터리)

- 디렉터리는 존재(nsm0404 소유)하나 안이 비어 있음. 가져올 것 없음.

### defect-seg-benchmark (읽음)

산업 결함(비-과일) 인스턴스 분할 20개 모델 파일럿 벤치마크가 진행 중(양진우 담당, Codex 작성 문서 다수).

- **`work/yang_jinwoo/260922_SAM제외_선정기준_20개.md`** — SAM 계열 완전 배제(C0 기준)를 확정하고 Mask R-CNN, HTC, SOLOv2, YOLO11-seg, Mask2Former, RF-DETR Seg 등 20개 비-SAM 인스턴스 분할 모델을 선정한 근거표. 5장 학습 같은 극소량 라벨 상황에서 어떤 모델이 후보가 되는지 체계적으로 정리됨.
- **`work/yang_jinwoo/pilot20_20260922/결과요약.md`** — 실제 5장 학습·5장 평가 파일럿 결과(mask AP). HTC 16.82, QueryInst 14.32, Mask2Former 9.29, Mask R-CNN 10.02가 상위, PointRend·RTMDet-Ins·EoMT는 AP 0(설정 문제로 추정), RF-DETR Seg는 엔진 오류로 실패.
  - 쓸모: 우리 SAM+YOLO 초벌 파이프라인 외에 "적은 라벨로 학습되는 인스턴스 분할 모델"이 필요해지면(예: 블루베리 인스턴스 초벌을 SAM 대신 학습형 모델로 바꾸는 경우) 이 비교표가 후보 압축에 바로 쓸모 있음. 단 도메인이 산업 결함이라 가중치 자체는 못 쓰고, 모델 선정·코드베이스 정보만 재사용 가능.
  - 가져오는 법: 지금 당장 채택할 필요는 없음. 초벌 모델 교체를 검토할 때 이 두 문서를 참고자료로.
  - 우선순위: 중
- `GPU점유.md` — 팀 공유 V100×8 사용표 + 환경 주의사항(Volta sm_70이라 최신 PyTorch/mmcv 빌드 위험, Detectron2는 비교적 안전, MMDetection은 별도 conda 환경 격리 권장). 우리도 같은 V100 클러스터를 쓰므로 SAM/YOLO 학습 환경 구성 시 참고할 실무 팁.
  - 우선순위: 중 (환경 구성 시 참고)

## 가져올 것 상위 6개

1. **[상] 열매번호(인스턴스) 편집 기능 지시서** — `park_seongmoon(=lsh0616)/260917_라벨링툴_열매번호편집_작업지시서.md`. 사과 38개 오류 수정 + 블루베리 초벌 45,124개 교정용 기능이 설계만 되고 미구현 상태. 그대로 작업지시서로 채택.
2. **[상] 사과 오류 대상 CSV** — `park_seongmoon/apple_check/confirmed_errors.csv` 등. 위 기능의 1차 실사용 데이터.
3. **[상] 블루베리 정답지 생성 방법론 + 40장 표본** — `lsh0616/260915_블루베리정답지_생성방안.md`, `lsh0616/blueberry_count_gt/`. SAM 초벌 검증용 앵커와 계수 기준으로 그대로 채택 가능.
4. **[상] 블루베리 초벌 인스턴스 맵** — `park_seongmoon/bbox_outputs/blueberry/all/instance_maps/`(uint16, 1,195장 45,124개, watershed 결과). 열매번호 편집 기능의 두 번째 사용처로 이미 준비돼 있음.
5. **[중] 포도 삭제 확정 202장** — `choi_inhun/260920_grape_삭제대상.md`. 분할표(`grape.csv`) 갱신 시 반영.
6. **[중] V100 환경 주의사항 + 비-SAM 20개 모델 벤치마크** — `defect-seg-benchmark/GPU점유.md`, `work/yang_jinwoo/260922_SAM제외_선정기준_20개.md` / `pilot20_20260922/결과요약.md`. 환경 구성 팁은 바로 참고, 모델 후보표는 초벌 모델 교체 검토 시 참고.
