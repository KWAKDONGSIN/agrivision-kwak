작성: 2026-09-17
최종 수정: 2026-09-18 (데이터 **최종 동결판** 숫자로 현행화 + **16번 «사과를 얼마나 솎을까요» 추가 — 가장 중요한 질문** + 17번 «‘동그랗게’ 사진 예시 확인»)

# 교수님께 확인받을 것 17건 (0915 미팅 후속 + 0917~18 새로 나온 것)

> 🔴 **가장 급한 것은 16번입니다.** 사과를 얼마나 솎아낼지 — 이것이 정해지지 않으면 분할표도,
> 논문에 쓸 «사과 N장» 도 정할 수 없습니다. 나머지는 라벨 규칙 쪽이라 병렬로 가도 됩니다.

검수를 하다 보니 **규칙이 정해지지 않아 못 고친 것**들입니다. 이게 정해지면 재라벨과 본실험 순서가 풀립니다.

## 0915 검수에서 나온 4건

1. **가려진 부분을 정답에 넣을 것인가.**
   복숭아·포도·블루베리는 잎·가지에 가려진 부분까지 통째로 칠해져 있습니다(amodal). «보이는 것만» 으로 바꾸면 마스크를 전부 다시 그려야 합니다.
   - 🔴 2026-09-18 정정: **사과는 «전부 amodal» 이 아니라 출처마다 다릅니다.** MinneApple 사진은 가려진 부분까지 통째로,
     dataset1~3 사진은 보이는 것만 칠했습니다. 한 데이터셋 안에 두 규칙이 섞여 있습니다. 자세한 것은 아래 9번입니다.
     (사과에서 잎에 가린 자리가 파여 C 자 모양인 개체가 2,873개(7.1%)라는 실측은 그대로입니다.)
2. **라벨 범위** — 땅에 떨어진 과실, 뒷줄 나무의 과실, 연두색 미숙과는 라벨 대상인가요.
3. **포도 라벨 단위** — 지금은 «송이» 입니다. «알» 단위로 바꿀까요. 지주대는 어떻게 할까요.
4. **블루베리 «없는 것»** — 🔴 2026-09-18 정정: **찾았습니다.**
   «라벨이 없어 보이는» 후보 300개를 하나씩 확대해 보니 마른 꽃부리 155 · 판단 불가 51 · 잎 42 ·
   초점 밖 뒷줄 27 · 꽃 16 · 오검출 6 이었고, **정말로 빠뜨린 또렷한 열매는 3개**였습니다.
   그러니까 개수를 빠뜨린 것이 아니라 **«어디까지 칠하나» 라는 규칙**이 문제입니다. 아래 11번으로 여쭙니다.

## 0917 새로 나온 3건

5. **상자 종류 번호를 검출 팀과 맞출 것인가.**
   새로 만든 상자 기능은 과실 0번, 송이 1번, 기타 2번입니다. 검출 팀의 포도 학습 설정은 클래스가 하나뿐이고 송이가 0번입니다.
   좌표 형식은 같고 번호만 다릅니다. 하나로 맞추지 않으면 내보낸 파일을 그대로 학습에 쓸 수 없습니다.
6. **사과·블루베리도 검수판 데이터셋을 만들 것인가.**
   🔴 2026-09-18 정정: **만들었습니다.** `kds0206/datasets_reviewed_260917/` 에 사과 **429장**·블루베리 **1,114장**입니다
   (원본은 1,001·1,195. 거의 같은 사진 572·81장을 뺐고, 대표는 336·42묶음입니다).
   사과 근접 중복은 처음 센 47그룹 131장보다 훨씬 많았습니다. **사과는 얼마나 솎을지가 아직 열려 있습니다 — 16번이 그 질문입니다.**
   개수 평가 전에 폴드를 다시 짜야 하는 것은 그대로입니다(아래 14번).
7. **복숭아 재학습을 지금 한 바퀴 돌릴 것인가.**
   복숭아는 125장 중 116장 판정이 끝났습니다(수정 39·OK 77). «AI 초벌 → 사람 체크 → 재학습» 의 재학습 기준(30%)을 넘었습니다.
   GPU 8장이 지금 비어 있고, 4회전을 4장에 나누면 약 4시간입니다. 돌릴지 말지 지시가 필요합니다.

8. **포도 송이 번호를 원본 정답에서 끌어올까요.**
   포도는 우리 마스크가 «송이 영역» 이라 잎이 가로지르면 한 송이가 여러 조각으로 잡힙니다. 실측 10장에서 정답 51송이가 109개로 쪼개졌습니다.
   그런데 확인해 보니 **CERTH 원본에 송이마다 번호가 매겨진 정답이 이미 있습니다**(2,502장 9,832송이). 이것을 툴의 포도 번호로 끌어오면 블루베리와 같은 방식으로 셀 수 있습니다.
   원본과 우리 마스크가 어긋나 보이던 것은 **두 번의 크기 변환에서 생긴 경계 1~2픽셀 차이**였습니다. 우리 마스크로 자르고 빈 곳을 가까운 번호로 채우면 전경이 정확히 일치합니다(네 장 실측).
   그래서 여쭐 것은 한 문장입니다. **«전경의 기준을 우리 마스크로 두어도 되겠습니까»** 예라고 하시면 포도도 블루베리처럼 번호 단위로 셀 수 있습니다.
   툴 쪽 준비는 끝났습니다(2,502장 변환 완료). 실제로 켜는 것은 번호 저장 쪽 보충 수정이 재현 확인된 뒤입니다.

## 0917~18 사과·블루베리 검수에서 나온 9건

9. **사과 라벨을 가려진 부분까지 칠할까요, 보이는 것만 칠할까요.**
   같은 «사과» 안에서 출처마다 다릅니다. MinneApple 사진은 잎에 가려진 부분까지 원으로 통째 칠했고
   (의심이 큰 상위 150개체 중 92개), dataset1~3 사진은 잎과 옆 사과를 피해 보이는 부분만 칠했습니다.
   교수님이 «중간이 비어야 되는데 동그랗게» 라고 하신 것이 MinneApple 쪽입니다.
   어느 쪽으로 통일할지 정해 주시면 그쪽을 다시 그리겠습니다. (1번 질문의 사과판 구체 증거입니다.)

10. **사과 낙과와 뒷줄 나무의 사과를 셀까요.**
    낙과가 보이는 42장 중 5장만 라벨돼 있습니다(2차 검수의 표본 값입니다). 뒷줄 나무도 출처마다 다릅니다.
    지금은 고치지 않고 «규칙 미정» 으로 표시만 해 뒀습니다(폴더에 남은 사진 **17장**).

11. **블루베리 마른 꽃부리를 열매에 포함할까요.**
    4번에서 적은 대로 후보 300개 중 155개(52%)가 어린 열매 끝에 붙은 시든 꽃잎이었습니다.
    초록 어린 열매와 초점 밖 뒷줄 열매도 같은 질문입니다. 폴더에 남은 사진 218장에 표시해 뒀습니다.
    포함한다고 하시면 그만큼 다시 그려야 하고, 뺀다고 하시면 지금 라벨 그대로 갑니다.

12. **블루베리 «같은 포기를 다른 구도로 찍은» 사진을 뺄까요.**
    50쌍(사진 93장, 이어 보면 묶음 43개)입니다. «거의 같은 사진» 은 아니어서 빼지 않고 표시만 해 뒀습니다.
    묶음마다 1장씩만 남기면 1,114장이 **1,074장**이 됩니다. 영상 단위로 폴드를 나누면 누수는 없고 표본 수만 부풀립니다.

13. **사과 «겹치는 이웃» 301장을 더 솎아낼까요, 분할에서 같은 폴드로 묶기만 할까요.**
    «거의 같은 사진» 은 이미 572장을 뺐습니다. 남은 것은 같은 나무를 걸어가며 다른 구도로 찍은 사진들입니다.
    묶음마다 1장씩만 남기면 429장이 **99장**, 서로 겹치지만 않게 솎으면 **277장**이 됩니다.
    다만 이 «겹치는 이웃» 관계 773쌍 중 사람이 눈으로 본 것은 303쌍뿐이고 나머지 470쌍은 자동 점수로만 기록했습니다.
    저희 판단으로는 빼지 말고 같은 폴드로 묶기만 하는 쪽이 맞아 보입니다.
    (이 질문은 **16번과 같은 질문의 일부**입니다 — 16번에서 한 번에 정해 주시면 됩니다.)

14. **중복을 뺐으니 분할표를 다시 만들어야 합니다.**
    사과·블루베리 모두 폴드마다 빠진 장수가 다릅니다. 옛 분할표(`v3_seed3407`)로 실험하면 뺀 사진이 그대로 들어갑니다.
    사과 dataset1~4 는 지금 장마다 따로 묶여 있어 영상 단위로 다시 묶어야 합니다.
    manifest 에 **`session`(촬영) 칸**과 `split_group` 칸을 넣어 뒀습니다. **`session` 을 같은 폴드로 보내는 쪽을 권고합니다**
    (`split_group` 은 더 잘게 나눌 때의 최소 조건. 둘 다 빈칸 없음).
    사과는 제외 그룹 336개 중 **112개**, 묶음 99개 중 **9개**가 옛 표의 `main_fold` 를 넘습니다. 블루베리는 0개입니다.
    실제로 `GroupKFold(5)` 로 만들어 확인했습니다 — **확인된 관계 기준** 폴드를 넘는 관계 **0**, 쪼개진 묶음 0
    (폴드 크기: 촬영 기준 사과 84·78·78·95·94 — 촬영이 17개뿐이라 ±10% · 블루베리 223·223·223·223·222).

15. **«거의 같은 사진» 판정을 사람이 표본으로 다시 볼까요.**
    이 판정은 자로 잰 값이 아니라 보는 사람의 판단입니다. 그래서 같은 89쌍을 AI 판정자 셋이 서로 모르게 판정해 봤더니
    **사과는 78~88%**(κ 0.51~0.72), **블루베리는 그보다 낮은 59~79%**(κ 0.17~0.58)만 일치했고,
    셋이 모두 같은 답을 낸 쌍은 89쌍 중 60쌍(67%)이었습니다. 지금 숫자는 셋의 다수결로 정한 것입니다.
    판정자 기준이 달라지면 사과에서 남는 장수가 **약 ±16장** 움직입니다.
    교수님이나 인훈 님이 **표본 몇십 쌍만 직접 봐 주시면** 이 기준이 사람 눈과 맞는지 한 번에 확인됩니다.
    (원하시면 시트 형태로 바로 만들어 드리겠습니다.)

16. 🔴 **사과를 얼마나 솎을까요.** — 가장 중요한 질문입니다. 셋 중에서 골라 주시면 됩니다.

    먼저 상황을 한 줄로 말씀드리면: **사과는 천천히 걸으며 찍은 영상이라 «거의 같은 사진» 이
    있다/없다로 갈리지 않고 연속체입니다.** 그래서 확인하는 범위를 넓힐 때마다 더 나왔습니다 —
    **687장 → 549장 → 547장 → 429장.** 쌍을 하나씩 지우는 방식에는 «여기가 끝» 이라는 자연스러운
    멈춤점이 없습니다. 그래서 저희가 임의로 정하기보다 여쭙는 것이 맞다고 판단했습니다.

    **프레임 간격별로 «거의 같은 사진» 비율을 실제로 재 봤습니다**
    (판정자는 간격을 모르는 채 화면만 보고 판정 · 합 552쌍 · «전혀 다른 장면» 판정은 **0건**):

    | 프레임 간격 | 5 | 10 | 15 | 20 | 25 | 30 | 40 | 60 | 90 | 120 | 180 |
    |---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
    | 현장 촬영(MinneApple) | 100% | 69% | 72% | 68% | 38% | 55% | **14%** | 18% | 7% | 7% | 0% |
    | dataset1~4 | — | — | — | — | — | 84% | — | 67% | **18%** | 11% | 0% |

    → **비율이 처음 20% 아래로 떨어지는 지점은 현장 40프레임 · dataset 90프레임**입니다.

    **선택지:**

    | | 방법 | 사과 장수 | 장점 | 단점 |
    |---|---|---:|---|---|
    | ① | **지금 판 그대로** (눈으로 확인된 관계를 전부 반영) | **429** | 실제로 본 것만 뺐음 | 판정자에 따라 흔들림(아래) · 아직 안 본 쌍 254개가 범위 밖에 남음 |
    | ② | **간격 솎기** — 같은 촬영에서 G프레임 이상 떨어진 사진만 남김 | **394**(30/60) · **294**(40/90) · **248**(60/90) · 187 · 137 · 105 | 눈 판정이 안 들어가서 **누가 다시 돌려도 같은 결과**(논문에 규칙 한 줄로 쓸 수 있음) | 실제로 다른 사진도 규칙에 걸려 빠질 수 있음 |
    | ③ | **솎지 않고 촬영 단위로 묶기만** | **429**(또는 원본 1,001) | 표본을 안 버림 · 누수는 분할로 막음 | 논문에서 «표본 수가 부풀려졌다» 는 지적을 받을 수 있음 |

    - ②를 고르실 때 **곡선상 20% 지점은 `keep_G40_90` = 294장**입니다. 목록은
      `stride_variants_apple.csv` 에 이미 만들어 뒀고, 사진을 지우지 않고 **학습 목록만 걸러 쓰는** 방식입니다.
    - **폴드 누수는 세 방법 모두 «분할» 로 막습니다.** manifest 의 **`session`(촬영) 칸**을 같은 폴드에 넣으면
      확인된 관계 2,405개 중 폴드를 넘는 것이 **0개**입니다(실제로 만들어 확인). 즉 **누수 방지를 위해
      사진을 더 지울 필요는 없습니다.** 지우는 것은 «표본 수를 정직하게 세기» 위한 것입니다.
      (다만 이 «0» 은 **눈으로 확인한 관계 기준**입니다 — 아직 아무도 안 본 쌍 254개는 이 확인 밖입니다.)
    - **왜 ①의 숫자를 못 믿나:** 이 판정은 사람(AI) 눈 판정이고, 같은 표본을 반씩 나눠 본 두 판정자의
      «거의 같은 사진» 비율이 **78% vs 60%** 로 갈렸습니다. ②는 이 흔들림이 아예 없습니다.

    🔵 **저희 권고를 한 줄로 적으면 이렇습니다.**
    «**촬영 단위로 폴드를 나누고, 장수는 간격 솎기 `keep_G40_90`(294장)을 씁니다.**
    294장은 위 곡선이 20% 아래로 꺾이는 지점이고, 눈 판정이 안 들어가서 **누가 다시 돌려도 같은 결과**입니다.
    지금 폴더의 429장 판은 눈 판정에 기대므로 **차선**입니다. **최종 선택은 교수님께 맡깁니다.»**

    - 어느 쪽을 고르셔도 **폴드 누수는 manifest 의 `session`(촬영) 칸으로 막습니다.**
      확인된 관계 중 촬영을 넘는 것이 0개이고, 촬영 단위 5-폴드를 실제로 만들어 봐도 폴드를 넘는 관계가 0 입니다.
      (`split_group` 칸은 그보다 잘게 나눌 때 지켜야 하는 최소 조건입니다.)
    - 논문·보고에서는 이 폴더를 «중복 제거된 데이터셋» 이라 쓰지 않고
      «**눈 검토 기반 부분 중복 제거**(판정자 일치 κ 0.51~0.72)» 라고 쓰겠습니다.
    - 사과는 amodal·modal 이 섞여 있으니 **성능은 출처별(MinneApple / dataset1~4)로 나눠 보고**하겠습니다.

17. **교수님이 0915 에 보신 «동그랗게» 사진이 어떤 것이었는지 짚어 주시겠습니까.**
    저희가 찾은 것은 MinneApple 쪽입니다 — 잎에 가려진 부분까지 원으로 통째 칠한 사진들
    (보고서 그림 2). dataset1~3 쪽은 반대로 보이는 부분만 정밀하게 칠했습니다(그림 3).
    같은 «사과» 안에 두 규칙이 섞여 있어서, 교수님이 보신 쪽이 어느 것인지 확인하면 9번 답이 바로 정해집니다.
    - 덧붙여 실측을 하나 드립니다: **사과 라벨 개체 중 «구멍» 이 있는 것은 38,781개 중 180개(0.46%)** 입니다.
      즉 «중간이 비어 있어야 하는데 안 비었다» 가 아니라, **가려진 부분을 원으로 메워 버린 것**이 쟁점입니다.

## 지금 상태 한 줄씩

- 라벨링 툴: 돌고 있습니다. 마스크 브러시 + **상자(네모) 모드** + **열매 번호 편집**(0917 추가 중)
- 검수 진행(사람이 직접 본 것): 복숭아 116/125 · 포도 87/2,406 · 사과 0/429 · 블루베리 0/1,114
  (사과·블루베리는 AI 3회 검수가 끝나 검수판 폴더까지 만들었고, 사람이 볼 차례인 사진이 사과 51장·블루베리 4장입니다)
- 실험: 파일럿0 64런 완주. 본실험은 데이터 확정 뒤로 대기
