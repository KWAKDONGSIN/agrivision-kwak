# 자동 검수 5회 — 최종 판정서

작성: 2026-09-17
누가: 자동 검수 사이클 5/5 · 1단계 (Claude Opus 5). 앞 사이클 1~4 와 웹사이트 사이클의 보고서를 전부 읽고,
숫자는 원천 파일에서 **다시 계산**해 대조한 결과입니다.
대상: `platform/work/kwak_dongsin/260916_라벨링툴/` · `kds0206/datasets_reviewed_260916/`

---

## 0. 결론 먼저

### 지금 상태로 교수님께 보고할 수 있나 → **네, 됩니다.**

0915 랩미팅에서 교수님이 시키신 세 가지(① 라벨링 툴을 웹사이트처럼 만들어라 ② 세그멘테이션 데이터를
검수해라 ③ 연속 스냅샷 같은 사진을 골라내라)는 **셋 다 물건이 나와 있습니다.**
보여드릴 것은 이 세 가지입니다.

보안 관련 값·설정 세부는 공개본에서 생략했습니다.
2. 검수 보고서 PDF — `문서/260916_데이터검수_최종보고서.pdf` (23쪽)
3. 하루 작업 총정리 PDF — `문서/260916_오늘작업_총정리_전체사진.pdf` (44쪽, 뺀 사진·고친 사진 전부 그림으로)

### 실험은 지금 시작하면 안 됩니다 → **교수님 결정 4건이 먼저입니다.**

교수님이 직접 "데이터를 좀 확실하게 만들어 놓고 그다음에 진행해야 될 것 같다,
지금 실험해 봤자 결과가 의미 없다"고 하셨습니다(0915 녹취 08:33~09:10).
저희가 검수를 하다 보니 **규칙 자체가 안 정해져서 못 고친 것**이 남았습니다. 그게 결정 4건입니다.

**본실험(P)을 시작해도 되는 조건 — 이 순서대로 다 끝났을 때:**

| 순서 | 조건 | 지금 |
|---|---|---|
| 1 | 사람이 툴에 들어가 실제로 눌러 보고 «쓸 만하다» 확인 | ❌ 아직 (AI 는 curl 로만 확인) |
| 2 | 교수님 결정 4건 확정 | ❌ 아직 |
| 3 | 그 규칙으로 재라벨 (복숭아 flag 30장, 포도는 전수) | ❌ 아직 |
| 4 | 재라벨 반영해 분할표 v5 확정 | ❌ 아직 (지금은 v4) |
| 5 | 본실험 시작 | ⛔ 위 4개 뒤 |

> **파일럿0 v2 64런 결과는 지금 검수 범위에서는 유지됩니다.** 민감도 분석에서 4쌍 순위가 그대로였지만, 그 근거는 복숭아 시험 fold 9장의 라벨 변화뿐이고 포도는 제외만 있었습니다. **포도 1–2위 격차가 0.0015 라 포도 전수 재라벨 뒤에는 순위가 바뀔 수 있으므로, 재라벨 후 민감도 분석을 1회 다시 돌려야 합니다.**

---

## 1. 숫자 사전 (2026-09-17 원천 파일에서 다시 계산)

앞으로 어떤 문서·발표·카톡에도 **이 값만** 쓰면 됩니다.

| 항목 | 값 | 어디서 나온 값인가 |
|---|---|---|
| 복숭아 — 마스크 고친 사진 | **39장** | `inspect/round3/peach_fix_log.csv` (39행) |
| 복숭아 — 추가한 복숭아 개수 | **83개** | 같은 파일 `n_added` 합 |
| 복숭아 — 전체 개체 수 | 공개 COCO **977** + 83 = **1,060** | `peach-data/{train,val,test}.json` (805+88+84) |
| 복숭아 — 사람이 볼 사진(flag) | **30장** = 누락 의심 9 + 윤곽 조정 21 | `data/peach/status.json` (fixed 18·flag 30·ok 77) |
| 포도 — 원본 / 파일 / 쓰는 사진 | 2,502 / 2,406 / **2,403** | `grape/manifest.csv`, 분할표 v4 `grape.csv` |
| 포도 — 뺀 사진 | **99** = 근접 중복 98 + 포도 아닌 사진 1 | 같은 manifest (`excluded_*`) |
| 포도 — 송이(bunch) 수 | 9,832 − 408 = **9,424** | CERTH 공개 주석 zip (mimc 3파일) |
| 포도 — 사람이 볼 사진(flag) | **72장** | `data/grape/status.json` |
| 근접 중복 그룹 | **94그룹 192장** (해밍≤10 의 91그룹 + 해밍 11 의 3그룹) | `data/grape/duplicates.json` |
| 교차 확인 — 다른 계열 모델도 같은 자리를 절반 이상 잡음 | 83개 중 **54개 (65.1%)** | `final/crosscheck/peach_accepted_vs_vwformer_r50.csv` |
| 교차 확인 — 20% 이상 겹침 | 83개 중 **69개 (83.1%)** | 같은 파일 |
| 라벨 보강이 후보 모델에 준 «초과 이득» | **+0.0077** Dice (순위 4쌍 전부 불변) | `final/sensitivity/sensitivity_cycle1_recheck.json` |
| 분할표 v4 — 포도 fold | main 390/377/388/384/383 · pilot 119/119/121/122 | `protocol_splits_v4_reviewed/grape.csv` |

> **옛 값과 헷갈리지 마세요.** 85개 · 41장 · 96장 · 2,404 · 2,406(사용) · 9,427 · 9,434 · 1,062 ·
> flag 29 · flag 8 은 **전부 중간에 거쳐 간 값**입니다. 지금 문서에 이 값이 «현재» 로 적혀 있으면 틀린 것입니다.

---

## 2. 사이클별 한 줄 요약

| 사이클 | 무엇을 봤나 | 결과 한 줄 |
|---|---|---|
| 1 · 데이터셋 무결성 | 장수·md5·마스크·중복·분할표 | 추가 성분 85개를 **하나씩 전부** 다시 보고 오검출 1·경계 번짐 1 제거 → **83개·39장**. 근접 중복 2쌍을 더 찾아 제외 |
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
| 3 · 문서 일관성 | 문서·PDF·사이트의 숫자 | 옛 숫자가 «현재» 로 남은 곳 12군데를 고침. 원천에서 따로 센 «숫자 사전» 20항목이 전부 일치 |
| 4 · 재현성 | 스크립트를 처음부터 다시 실행 | 핵심 7종이 **바이트/화소까지 동일**하게 재현. 중복 판정이 **사진 줄이는 방법(보간법)에 흔들린다**는 것을 발견 → 1쌍 더 제외 |
| 5 · 총괄 (이 문서) | 앞 4회 + 웹사이트의 남은 문제 | 숫자를 원천에서 다시 계산해 전 문서 대조(8곳 더 고침), 남은 문제를 «해결/미해결/사람 결정» 으로 분류 |
보안 관련 값·설정 세부는 공개본에서 생략했습니다.

---

## 3. 교수님 0915 지시 대비 — 했다 / 부분 / 안 했다

| 교수님 지시 (0915 녹취) | 상태 | 무엇이 나왔나 / 무엇이 빈나 |
|---|---|---|
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
| ② "세그멘테이션… 동그랗게 라벨링 된 것, 복숭아 안 된 것, 블루베리 없는 것 검수해야 될 것 같은데" (09:25~09:38) | **부분** | 4과일 4,823장 전수 측정 완료. 복숭아 누락 = **실재 확인**, 83개 보강. 사과 «동그랗게» = 두 원인 규명. **블루베리 «없는 것» 은 근거를 못 찾음 → 교수님께 어느 사진인지 여쭤야 함.** 그리고 «동그랗게(amodal)» 를 고칠지 말지가 결정 1 |
| ③ "연속 스냅샷… 거의 똑같은 이미지 골라내고 가공해야" (07:38~10:10) | **했다** | 4과일 전수 근접 중복 탐지. 결과: **블루베리는 0장**, 실제로는 사과 47그룹 131장·포도 94그룹 192장. 포도는 그룹당 1장만 남겨 99장 제외 |
| (전제) "데이터를 확실하게 만들어 놓고 그다음에 진행" (08:33) | **아직** | 결정 4건이 안 나와 재라벨을 못 했습니다. 본실험은 대기 |

## 3-1. 팀 카톡 결정(2026-09-15) 대비

| 팀 결정 | 상태 | 비고 |
|---|---|---|
| 세그 팀 담당 과일 = **복숭아·포도** | **했다** | 검수·툴·분할표 전부 이 두 과일 |
| **라벨링 툴은 팀 통합 1개** | **부분** | 툴 한 개로 4과일(복숭아·포도·사과·블루베리) 전부 볼 수 있게 만들었습니다. 다만 검출 팀(블루베리 bbox)이 쓸 **네모 상자 그리기 기능은 없습니다** — 지금은 마스크 브러시 전용 |
| 방식 = **AI 가 1차로 그리고 → 사람이 수정** | **했다** | AI 1차 제안 4,823장 생성(`ai_pass/`), 툴에서 «AI 제안» 겹쳐 보기 + 브러시 수정 + 판정. 복숭아 83개를 찾은 것도 이 고리 |

---

## 4. 열린 문제 표 (사이클 1~5 + 웹사이트에서 남은 것 전부)

상태 표기 — **해결** / **미해결**(AI 가 더 할 수 있음) / **사람 결정**(사람이 정하거나 눌러야 함)

| # | 항목 | 어디서 나왔나 | 상태 | 근거 파일 | 누가 |
|---|---|---|---|---|---|
| 1 | 가려진 부분을 정답에 넣을지(amodal vs visible) | 사이클1 · 미팅 §6 | **사람 결정** | 검수보고서 PDF §7 | 교수님 |
| 2 | 라벨 범위 — 낙과·뒷줄 나무·연두색 미숙과 | 미팅 §6 | **사람 결정** | 같은 곳 | 교수님 |
| 3 | 포도 라벨 단위 — 송이 vs 알 (+ 지주대) | 미팅 §6 | **사람 결정** | 같은 곳 | 교수님 |
| 4 | 블루베리 «없는 것» 이 어느 사진인지 | 미팅 §6 · 사이클3 | **사람 결정** | `inspect/260916_데이터검수_보고서.md` §5-2 | 교수님 |
| 5 | 툴을 사람이 실제로 눌러 확인 (복숭아 30장·포도 72장) | 사이클1·2 | **사람 결정** | `app/README.md` | 곽동신·최인훈 |
| 6 | 브라우저 확인창 6종이 실제로 뜨는지 눈으로 확인 | 사이클2 | **사람 결정** | `cycles/cycle_2/stage3_final.md` | 곽동신 |
| 7 | 포도 «송이 누락» 전수 재라벨 (72장으로는 부족, 표본 84%가 누락) | 딥시크 E · 사이클1 | **미해결** | `문서/260916_딥시크판정_Fable검증.md` | 결정 3 뒤 사람+툴 |
| 8 | 추가한 83개 중 29개의 윤곽을 과실 경계로 줄이기 | 사이클1 | **미해결** | `cycles/cycle_1/stage2/peach85_verdicts.csv` | 결정 1 뒤 사람 |
| 9 | `_to_delete_260916.txt` 3장(163·60·1191) 파일을 정말 지울지 | 사이클1·4 | **사람 결정** | `datasets_reviewed_260916/grape/_to_delete_260916.txt` | 곽동신 |
| 10 | 깃허브 공개 — 사이트·저장소 범위를 교수님께 승인받기 | 웹사이트 P0-5 | **사람 결정** | `cycles/site/stage3_final.md` | 곽동신→교수님 |
| 11 | 깃허브 push (조립 → grep → git) 는 사람이 직접 | 웹사이트 P1-6 | **사람 결정** | `문서/260916_자동화_안내_컴꺼도되게.md` §4 | 곽동신 |
| 12 | 조립된 저장소(`~/repos/fruit-seg-labeltool`, 09-16 23:32)가 **낡음** — push 전에 반드시 재조립 | 사이클5 | **미해결** | 위 안내문 §4 | 곽동신 (명령 그대로) |
| 13 | 근접 중복 임계값이 축소 보간법에 흔들림 (BILINEAR 해밍 11 = LANCZOS 9) | 사이클4 | **해결(문서화)** | `final/round_d/…_3차수정판.md` | — (논문에 한계로 적음) |
| 14 | 분할표 `grape.csv` 를 지금 다시 생성하면 `group_source` 3칸이 달라짐 (2030·2047·763) | **사이클5 새 발견** | **미해결(영향 없음)** | `cycles/cycle_5/stage1/repro/` | 분할표 확정 때 사람이 선택 |
| 15 | `main.tex` 에 데이터 절 3차수정판을 아직 안 넣음 (1,077→977/1,060, 2,502→2,403, 9,832→9,424) | 사이클3 | **미해결** | `final/round_d/260916_논문_데이터절_초안_3차수정판.md` | 사람 확인 뒤 |
| 16 | `pdfdoc.py` 의 «빈 쪽» 판정 버그 (그림만 있는 쪽 겹침) | 사이클3 | **미해결(급하지 않음)** | `작업기록.md` §3 | AI 다음 작업 |
| 17 | `cycles/cycle_4/repro/build/datasets_reviewed_REPRO/` 6.0GB 삭제 | 사이클4 | **사람 결정** | 같은 경로 | 곽동신 |
| 18 | 재현성 흠 3개 — `rebuild_with_rejects.py` 가 `mask_source` 를 안 고치고 `act='keep'` 하드코딩, `build_peach_fixed_masks.py` 가 로그를 두 번 엶 | 사이클4 P2-10 | **미해결(데이터는 정상)** | `inspect/round3/` | AI 다음 작업 |
| 19 | `build_reviewed_dataset.py` 를 다시 돌리면 안 된다는 경고 | 사이클4 → 사이클5 | **해결** | 파일 첫 줄에 ⛔ 경고 추가함 | — |
| 20 | 내부용 사이트(`site/dist_internal/`)에 «뺀 97장» 이 남음 — 원본 md 는 고쳤으니 **다시 빌드하면 사라짐** | 사이클5 | **미해결** | `site/dist_internal/results.html` | 재조립 때 자동 해결 (#12) |
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
| 22 | 딥시크가 제기한 «후보 생성 모델 편향» | 딥시크 A | **해결(정량화)** | `final/sensitivity/` | 초과 이득 +0.0077, 순위 불변 |

**요약: 해결 5 · 미해결 9 · 사람 결정 10 (사이클5 3단계 재분류: #13·#22 미해결, #14 해결).**
**미해결 7개 중 실험을 막는 것은 7·8번(재라벨)뿐이고, 그 둘도 교수님 결정 1·3 이 먼저입니다.**

---

## 5. 사람이 할 일 — 이 순서대로

### ① 툴 확인 (30분, 곽동신·최인훈)
1. 브라우저에서 `http://<서버주소>:5111` — «안전하지 않음» 경고가 나오면 그냥 이동
보안 관련 값·설정 세부는 공개본에서 생략했습니다.
3. 이름 적고 → 복숭아 → **flag 30장**부터. 사진 눌러서 원본/AI 제안/내 수정본 겹쳐 보기
4. 포도 **flag 72장** 도 같은 방식
5. 눌러 보면서 «이 버튼이 이상하다» 싶은 게 있으면 메모 → 다음 사이클에서 고칩니다

### ② 교수님께 결정 4건 여쭙기 (다음 랩미팅)
1. **가려진 부분** — 지금 4과일 전부 잎·가지에 가려진 데까지 정답에 들어 있습니다. «보이는 것만» 으로 바꿀까요? (바꾸면 마스크 전부 다시 그려야 합니다)
2. **라벨 범위** — 땅에 떨어진 과실 / 뒷줄 나무 과실 / 연두색 미숙과는 라벨 대상인가요?
3. **포도 단위** — 지금은 «송이» 인데 «알» 로 바꿀까요?
4. **블루베리** — «없는 것» 이라고 보신 사진이 어느 것인가요? 실측으로는 못 찾았습니다

> 같이 보여드릴 것: 검수 보고서 PDF §7 · 총정리 PDF 부록 F

### ③ 재라벨 (결정 뒤)
- 복숭아: flag 30장 + 윤곽 줄일 29개 성분
- 포도: 결정 3 에 따라 2,403장 전수 (툴에서 나눠서)
- 끝나면 `export/export_dataset.py` 로 내보내기

### ④ 분할표 확정
- 지금 v4(`protocol_splits_v4_reviewed/`, 포도 2,403행)를 그대로 쓸지, 재라벨분을 반영한 v5 를 만들지 결정
- ⛔ **v4 CSV 로 fold 를 다시 생성하지 마세요** (`manifest.json` 의 `WARNING_do_not_regenerate_folds` 참조)

### ⑤ 본실험(P) 시작
- 위 4개가 끝난 뒤. 그 전에는 «결과가 의미 없다»는 교수님 말씀이 그대로 유효합니다

### (병행) 깃허브 공개 — 교수님 승인 뒤
`문서/260916_자동화_안내_컴꺼도되게.md` §4 의 명령을 **순서대로** 치세요.
조립 → grep → git. grep 에 한 줄이라도 나오면 push 하지 말고 물어보세요.

---

## 6. 이번 사이클(5)에서 고친 것

원천에서 숫자를 다시 계산해 전 문서를 훑은 결과, **옛 값이 «현재» 로 남아 있던 8곳**을 고쳤습니다.
(전부 백업 `_backup_260917_c5_*` 를 먼저 만들었습니다.)

| 파일 | 무엇이 틀렸나 → 무엇으로 |
|---|---|
| 분할표 `manifest.json` | `peach.note` 의 «needs_human 8 · keep 78» → **9 · 77** (사이클4 에서 t3-13 이 바뀐 걸 반영 안 했음) |
| 같은 파일 | `cycle3_fix_260916.totals_current` 라는 **이름이 «현재» 인데 값은 옛 값** → `…_at_cycle3_snapshot` 으로 이름 변경 + 주의 문구 |
| 논문 데이터절 3차수정판 | «제외 99장이 가진 **405**» → **408** (9,832−405=9,427 로 산수가 안 맞았음) |
| 같은 파일 | «83/39/1,060/**97**» → **98** · «잎 우세 28·불명 2» → **27·2 (합 29, 83개 기준)** |
| `datasets_reviewed_260916/README.md` | «제외 99장이 갖고 있던 **405**개» → **408** (같은 산수 오류) |
| 검수보고서 PDF 생성기 | «91그룹 186장 → 95장(+2 = 97장)» → **94그룹 192장 → 98장** · «ok 78» → **77** · «exclude 98» → **99** · «누락 의심 8» → **9** · «근접 중복 97장» → **98장 + 비포도 1장** |
| 총정리 PDF 생성기 | «flag 30장(누락 의심 **8**+21)» → **9**+21 · «복숭아 **8장**» 하드코딩 → 파일에서 세도록 (실제 9장) |
| 카톡 문구 | «99장을 빼고 **2404**장» → **2403**장 (2502−99=2403) · «2장을 더» → **3장** |
| 딥시크 검증 문서 | «뺀 **97**장» → **98**장 (2곳) |
| `작업기록.md` §1·§3 | 사용 2,404→**2,403** · 중복 97→**98** · 합 98→**99** · flag 29(8+21)→**30**(9+21) |
| `문서/260916_자동화_안내…md` §3 | 사이클 진행 상태표 (1만 «끝남» → 1·2·3·4 «끝남», 5 «진행 중») |
| `final/build_reviewed_dataset.py` | 첫 줄에 ⛔ «다시 돌리지 마세요» 경고 추가 (돌리면 사람 판정이 사라집니다) |

PDF 두 개는 **다시 만들었고**(검수보고서 21→23쪽, 총정리 41→44쪽),
«자동 검수 5회 결과» 절을 새로 넣었습니다(사이클별 한 줄 + 최종 숫자표 + 안 끝난 것 5가지).

---

## 7. 가정한 것 (틀릴 수 있는 부분)

1. **«현행 문서» 의 범위** — 사이트 dist, PDF 생성기 2종, 검수판 README, 논문 초안 3차수정판,
   자동화 안내, 카톡 문구, 딥시크 검증 문서, 분할표 manifest, `작업기록.md` §1·§3 으로 잡았습니다.
   «그 시점 기록» 이라고 명시된 문서(1차·2차 검수 보고서, `build_summary.json`, `작업기록.md` §2 이력)는
   **일부러 안 고쳤습니다.**
2. **`grape.csv` 는 손대지 않았습니다.** 지금 생성기를 다시 돌리면 3칸(`group_source`)이 달라지지만,
   fold 배정(pool·pilot_fold·main_fold)은 2,403행 전부 같고 `assignment_sha256` 는 현행 파일을 가리킵니다.
   바꾸면 sha 가 바뀌어 더 헷갈리므로 **기록만 남기고 그대로 뒀습니다.**
3. **툴은 curl 로만 확인했습니다.** 서버에 화면이 없어 브라우저 실조작은 못 했습니다.
4. **송이 수 9,424** 는 CERTH zip 의 `mimc_{train,valid,test}.json` 3개만 센 값입니다.
   같은 zip 의 `single-instance-one-class.json`(100개)은 다른 부분집합이라 뺐습니다.
5. **«블루베리에 중복 없음»** 은 60프레임 간격 기준 실측입니다. 교수님이 다른 데이터를 보신 것일 수 있습니다.


## 사이클5 3단계(Fable) 정정 — 2026-09-17 01:30
- 열린 문제 재분류: **#13(임계값 보간법 의존을 논문에 명시)** → 미해결(3차수정판 본문에는 «bilinear downscaling (9 under Lanczos)» 한 구절만 있고 한계 절은 없음) · **#22(초과 이득 정량화)** → 미해결(복숭아 9장 근거, 포도 재라벨 뒤 재실행 필요) · **#14(grape.csv group_source 3칸)** → 해결(생성기 재실행본으로 교체, sha256 재계산). #4 근거 절은 §4-2·§9-4.
- 추가 열린 문제 3건: **F1 AI 탐지 재현율 미측정**(누락 의심 9장에 후보가 없어 FN 확실) · **딥시크 B 보수안**(교정 12장 폐기 여부는 교수님 선택) · **툴 `masks_fixed` 파일이 남으면 상태가 «원본 OK» 여도 내보내기에 수정본이 들어감**(사이클2 경고만).
- **사람이 할 일 순서 정정**: ① 툴 브라우저 실조작 10분(쓸 만한지) → ② 교수님 결정 4건(amodal·라벨 범위·송이/알·블루베리) → ③ 그 규칙으로 재라벨: 복숭아 **125장 전수**(flag 30 우선), 포도 전수 → ④ 민감도 분석 1회 재실행(`final/sensitivity/run_all.sh`, 약 7~12분) → ⑤ 분할표 v5 확정 → ⑥ 본실험.
- 총정리 PDF 부록 A 문구(«2장» → 3장 163·60·1191)·부록 D(복숭아 flag 30장 전부) 정정, 카톡 «40장쯤» → 39장.
