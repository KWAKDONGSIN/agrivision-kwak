대행: Codex (Claude 한도 초과)
작성: 2026-09-20 03:35

# 공개 복사본 비밀 검사

대상 저장소는 KWAKDONGSIN/agrivision-kwak 하나이며, 원본 semantic-segmentation origin은 변경하거나 push하지 않았다.
후보 소스·검수 문서·데이터 목록만 복사했다. 운영 전환 보류를 README에 적었다.

- 텍스트의 서버 IP, 알려진 운영 로그인 값, API 토큰 형태, private-key 블록, Claude resume 식별자를 검사했다. 실제 비밀값은 이 기록에도 남기지 않았다.
- 서술 문서의 보안 관련 설정 문장은 공개본에서 통째 생략했다. 원본은 수정하지 않았다.
- PDF11개는 pdftotext로 본문을 검사했다. 최종 폰용 PDF의 비밀값을 올리지 않는다는 안내 문구는 실제 값이 없어 허용했다. 서버 주소/실제 값/키 패턴을 포함한 다른 PDF는 공개에서 제외했다.
- password/passwd/secret/token/api_key 등 단어가 있는40줄을 모두 확인했다. 환경변수 참조·로그인 폼 필드·모래상자 시험·단축키/PDF 문자열 토큰이며 실제 비밀값은 없었다.
- 별도 암호/PW 문맥3줄은 로컬 모래상자 예제와 옛 시험 설명이었다. 운영 로그인 값이 아니다.
- 공개 Python150개 AST, JavaScript19개 node --check 통과. 외부 데이터가 필요한 전체 회귀를 공개 저장소만으로 재현했다고 주장하지 않는다.
- 사진/마스크/번호/상자, 툴 data/exports, 키파일, 체크포인트, 논문 원문, 녹취, 모래상자·아카이브·로그·스크린샷은 제외했다.
- 처음 수집에 섞인 tools 시험 사본과 _baseline_prev427개 보관 파일은 **커밋 전에** 공개 폴더에서 로컬 아카이브로 옮겼다. 비밀 검사 실패를 숨긴 것이 아니라 수집 범위를 재점검해 제외한 기록이다.

최종 크기와 파일 수는 staging 직전에 다시 측정한다. 1GB 및 개별50MB 상한을 적용한다.
검사 도구와 값 없는 상세 JSON은 로컬 codex_resume/evidence에 보존한다. 원본은 삭제하지 않았다.
