# -*- coding: utf-8 -*-
"""실험 설계만 — 중학생 눈높이 공부 노트 (핸드폰 PDF).

작성: 2026-09-15
사용:  ~/.conda/envs/kwak/bin/python tools/make_design_study_pdf_260915.py
출력:  문서/260915_실험설계_중학생설명_공부용.pdf
범위:  교수님(안홍렬)이 platform/planning/ 에 짜 둔 실험 설계만. 모델(헤더·백본·HamNet) 설명은 인훈 담당이라 뺀다.
숫자:  파일럿0 v2 64런 완주분(2026-09-15 12:30 집계, runs_v2/*/budget_checkpoints.json · pilot0_summary.json).
"""
import sys
from pathlib import Path
REPO = Path('/data/project/2026summer/kds0206/semantic-segmentation')
BASE = Path('/data/project/2026summer/kds0206')
sys.path.insert(0, str(REPO / 'tools'))
import pdfdoc  # noqa: E402
pdfdoc.PW, pdfdoc.PH = 4.4, 8.4
from pdfdoc import Doc  # noqa: E402

OUT = BASE / '문서' / '260915_실험설계_중학생설명_공부용.pdf'
FLOW = BASE / '문서' / '260912_실험설계_순서도_2판.png'

d = Doc(OUT, '실험 설계, 아주 쉽게', subtitle='교수님이 짜신 실험 순서를 중학생 눈높이로 · 발표 공부용',
        footer='곽동신 · 2026-09-15 · 실험 설계만', date='2026-09-15')
d.cover(lines=['2026-09-15(화) 랩미팅 · 실험 설계 5분',
               '모델 설명은 인훈 담당 → 이 책엔 없음',
               '파일럿0 64런 완주 숫자(09-15 12:30) 반영'], badge='공부용')


def chapter(n, name, summary=''):
    d.pagebreak(); d.chapter(n, name, summary)


d.toc([
    (0, '3시간 공부 계획', '읽기 → 외우기 → 말하기'),
    (1, '한 문장과 그림', '설계 전체를 한눈에'),
    (2, '왜 이렇게 복잡하게 하나', '시험지를 미리 본 학생 이야기'),
    (3, '재료', '과일 4개, 조합 144개'),
    (4, '1단계 딱 한 번 자르기', '20% 연습장 / 80% 봉인 시험지'),
    (5, '2단계 연습장 안 4조각 회전', '교과서·모의고사·수능'),
    (6, '3단계 규칙 정하기 실험', 'C1 · 파일럿0 (오늘 완주 숫자)'),
    (7, '4단계 예선 P', '2,304런, 결선팀 C = A∪B'),
    (8, '5단계 본실험 M1~M5', '두 명의 우승자'),
    (9, '점수 재는 법', '슬라이딩 윈도우 · Dice'),
    (10, '숫자 카드', '외울 숫자만'),
    (11, '미정인 것 · 하면 안 되는 말', '발표에서 조심할 것'),
    (12, '5분 대본 · 예상 질문', '실험 설계만'),
    (13, '용어 사전', '모르는 말 찾아보기'),
    (14, '세 AI 에게 물어본 결과', '쓸 것과 버릴 것'),
])

# ───────────────────────── 0 ─────────────────────────
chapter(0, '3시간 공부 계획', '읽기 1시간 · 외우기 30분 · 말하기 30분')
d.p('다 외우려 하지 말고 «순서»와 «이유»만 잡으면 됩니다.')
d.steps([
    '1시간 — 1장~9장을 처음부터 끝까지 한 번 읽는다. 모르는 말은 13장 용어 사전.',
    '30분 — 10장 숫자 카드와 12장 대본을 소리 내어 3번 읽는다.',
    '30분 — 순서도(1장 그림)만 보고 아무것도 안 보고 5분 말해 본다. 막히는 곳만 다시 읽는다.',
    '남는 시간 — 12장 예상 질문을 혼자 묻고 답한다.',
])
d.tip('내 발표는 «교수님이 실험을 어떤 순서로, 왜 그렇게 짜셨나»만 말하면 됩니다. 모델이 뭔지, HamNet 이 뭔지는 인훈 씨 몫입니다. 모델 질문이 오면 "그 부분은 인훈 씨가 이어서 설명합니다" 라고 넘기면 됩니다.', head='범위를 좁히세요')

# ───────────────────────── 1 ─────────────────────────
chapter(1, '한 문장과 그림', '이 한 문장이면 절반은 끝')
d.quote('헤더 12개 × 백본 12개 = 144개 조합을, 데이터의 20%로 먼저 거르고, 나머지 80%에서 전부 한 번 + 유망한 것만 다섯 번 평가한다.', who='설계를 한 문장으로')
d.p('말로 풀면 이렇습니다. 과일 사진에서 과일 부분만 색칠하는 인공지능을 144가지 방식으로 만들어 봅니다. 그런데 144개를 전부 정성껏 시험하면 너무 오래 걸립니다. 그래서 데이터 일부(20%)로 «예선»을 치러 유망한 팀만 뽑고, 나머지 데이터(80%)로 «본선»을 치릅니다.')
d.p('단계는 다섯 개입니다. ① 딱 한 번 자르기 → ② 연습장(20%) 안을 4조각으로 → ③ 규칙 정하기 실험(C1·파일럿0) → ④ 예선 P → ⑤ 본실험 M1~M5. 아래 그림이 그 순서입니다.')
d.fullfig(FLOW, caption='순서도 2판 (2026-09-12, 09-13 결정 31 반영)', sub='위 = 파일럿(20%) 영역, 아래 = 본실험(80%) 영역')

# ───────────────────────── 2 ─────────────────────────
chapter(2, '왜 이렇게 복잡하게 하나', '시험지를 미리 본 학생 금지')
d.h2('시험지를 미리 본 학생')
d.p('어떤 학생이 수능 문제를 미리 보고 공부했다면, 그 학생의 수능 점수는 실력이 아닙니다. 인공지능도 똑같습니다. 점수를 재는 데 쓸 사진을 «고르는 과정»이나 «학습»에 한 번이라도 쓰면, 그 점수는 부풀려집니다. 이걸 «누수(leak)»라고 부릅니다.')
d.p('교수님 설계의 규칙은 전부 이 하나를 지키려고 있습니다. «어떤 팀을 뽑을지 정하는 데 쓴 사진»과 «최종 점수를 재는 사진»을 절대 섞지 않는다.')
d.warn('우리 랩에서 실제로 한 번 걸렸습니다. 옛날에 CCASeg 라는 헤더를 후보에 넣을 때 «본실험 성적이 좋아서» 넣었는데, 그건 시험지를 보고 학생을 뽑은 셈이라 논문에 쓸 수 없게 됐습니다. 그래서 이번엔 «후보 고르기는 20% 안에서만» 이 철칙이 됐습니다.', head='실제로 있었던 일')
d.h2('이 설계가 답하려는 질문 4개')
d.steps([
    '어떤 조합(헤더+백본)을 추천할 것인가?',
    '좋은 백본(몸통)은 헤더(머리)가 바뀌어도 여전히 좋은가? — «주효과»',
    '성능은 부품의 합인가, 궁합인가? — «상호작용»',
    '최신 기법의 이득이 작고·많고·겹친 과실(블루베리)에서도 나타나는가?',
])
d.note('주효과 = 백본만 바꿔도 점수가 오르내리는 힘. 상호작용 = 특정 헤더와 특정 백본이 «같이 있을 때만» 잘 되는 현상. 144조합을 전부 같은 시험지로 재야 이 둘을 갈라 볼 수 있습니다. 그래서 본실험 M1 에서 144개를 «전부» 한 번씩 돌립니다.', head='말 뜻')

# ───────────────────────── 3 ─────────────────────────
chapter(3, '재료', '과일 4개 · 조합 144개')
d.table(['과일', '장수', '특징'], [
    ['블루베리', '1,195', '작고·많고·겹침. 영상 프레임(같은 영상 = 형제 사진)'],
    ['사과', '1,001', '촬영 시퀀스 단위 그룹'],
    ['복숭아', '125', '아주 적음'],
    ['포도', '2,502', '가장 많음(송이 단위 라벨)'],
], [0.22, 0.16, 0.62], size=8.5)
d.p('모델은 «백본(몸통, 사진을 보고 특징을 뽑음)» 12종과 «헤더(머리, 특징으로 색칠 결과를 만듦)» 12종을 갈아 끼워 144가지를 만듭니다. 이 144개는 2026-09-08 에 목록을 «동결»(더 이상 안 바꿈)했고, 144개 전부 잠깐씩 돌려 에러 없이 도는지 확인(스모크 테스트)을 마쳤습니다.')
d.tip('«동결(freeze)» = 자물쇠를 채운다. 한 번 정하면 결과를 보고 되돌리지 않는다는 뜻. 이 설계엔 자물쇠가 여러 번 나옵니다(후보 144개 → 분할 → 학습 규칙 → 결선팀 C).', head='자주 나오는 말')

# ───────────────────────── 4 ─────────────────────────
chapter(4, '1단계 — 딱 한 번 자르기', '20% 연습장 · 80% 봉인 시험지')
d.h2('비유')
d.p('문제집 한 권을 두 뭉치로 가릅니다. 20%는 «연습장»입니다. 여기서는 마음껏 풀고, 규칙도 정하고, 어느 팀이 유망한지도 고릅니다. 80%는 «봉인한 진짜 시험지»입니다. 연습장에서 모든 결정을 끝낸 뒤에야 봉인을 뜯고 최종 점수를 잽니다.')
d.h2('형제 사진은 같은 뭉치로')
d.p('블루베리는 영상에서 잘라낸 프레임이라, 1초 뒤 사진은 거의 똑같이 생겼습니다. 한 장은 연습장에, 그 옆 장은 시험지에 들어가면 «시험지를 미리 본 것»과 같습니다. 그래서 같은 영상(Camera N Video M)은 통째로 한쪽에 넣습니다. 사과는 촬영 시퀀스 단위로 같은 처리를 합니다. 포도·복숭아는 그룹 정보가 없어 장 단위로 나눕니다.')
d.table(['과일', '전체', '연습장 20%', '시험지 80%', '80%의 한 조각(M1~M5)'], [
    ['블루베리', '1,195', '239', '956', '191'],
    ['사과', '1,001', '200', '801', '160'],
    ['복숭아', '125', '25', '100', '20'],
    ['포도', '2,502', '500', '2,002', '400'],
], [0.2, 0.16, 0.2, 0.2, 0.24], size=8)
d.bullets([
    '자르는 데 쓴 난수 씨앗(seed) = 3407. 같은 씨앗이면 누가 돌려도 똑같이 잘립니다.',
    '그룹(영상·시퀀스)을 안 쪼개고 나누므로 정확히 20.0% 가 아니라 «20% 에 가장 가깝게» 입니다. 실제 장수는 위 표대로 기록해 둡니다.',
    '자른 결과는 파일로 저장하고 «지문(hash)»을 기록해 둡니다. 나중에 누가 바꿨는지 바로 드러납니다.',
    '옛 실험에서 검증에 이미 썼던 복숭아 23장·포도 250장은 «이미 본 사진»이라 표시해서 연습장 쪽에만 둡니다. 시험지에는 절대 안 들어갑니다.',
])
d.note('"연습장 20%는 왜 버리나요?" → 버리는 게 아니라 «고르는 용도»로 다 씁니다. 다만 최종 점수에는 안 씁니다. 고르는 사진과 점수 재는 사진을 완전히 분리하기 위해서입니다.', head='예상 질문')

# ───────────────────────── 5 ─────────────────────────
chapter(5, '2단계 — 연습장 안 4조각', '교과서 · 모의고사 · 수능')
d.h2('학습 · 검증 · 시험은 뭐가 다른가')
d.table(['역할', '비유', '하는 일'], [
    ['학습(train)', '교과서', '모델이 보고 배우는 사진'],
    ['검증(val)', '모의고사', '«언제 제일 잘하나»를 보고 그 시점 모델을 고름(체크포인트)'],
    ['시험(test)', '수능', '점수만 잰다. 이걸 보고 뭘 고르면 부정행위'],
], [0.22, 0.18, 0.6], size=8.5)
d.p('모의고사 점수를 보고 «이 시점 실력이 제일 좋네, 이걸 제출하자»라고 고르는 건 정당합니다. 하지만 수능 점수를 보고 «이 시점으로 하자»라고 고르면, 수능 점수가 더 이상 공정한 점수가 아닙니다. 그래서 검증과 시험을 따로 둡니다.')
d.h2('4조각을 돌아가며 (4회전)')
d.p('연습장을 1·2·3·4 네 조각으로 나눕니다. 회전마다 «시험 1조각, 검증 1조각(다음 번호), 학습 나머지 2조각»입니다. 그래서 과일 하나당 4번 학습합니다.')
d.table(['회전', '시험', '검증', '학습'], [
    ['t1v2', '1', '2', '3, 4'],
    ['t2v3', '2', '3', '4, 1'],
    ['t3v4', '3', '4', '1, 2'],
    ['t4v1', '4', '1', '2, 3'],
], [0.25, 0.2, 0.2, 0.35], size=9)
d.p('왜 4번이나 하나? 한 조각만 시험지로 쓰면 «그 조각이 유난히 쉬웠는지 어려웠는지» 운에 결과가 흔들립니다. 네 조각이 돌아가며 시험지를 맡으면 운이 평균으로 씻겨 나갑니다. 모든 조각이 시험 1번·검증 1번씩 쓰입니다.')
d.h2('왜 3조각 → 4조각으로 바꿨나')
d.p('교수님 2026-09-11 결정 29.')
d.p('처음엔 3조각(학습 2·검증 1)이었습니다. 그땐 검증 조각 하나로 «시점 고르기»와 «점수 재기»를 둘 다 했습니다. 모의고사와 수능이 같은 시험지였던 셈입니다. 이걸 갈라내려고 시험 조각을 하나 더 뒀습니다. 대신 학습 사진은 연습장의 2/3 에서 2/4 로 줄었습니다.')
d.table(['과일', '학습', '검증', '시험'], [
    ['블루베리', '119~120', '59~60', '59~60'],
    ['사과', '82~118', '41~66', '41~66'],
    ['복숭아', '12~13', '6~7', '6~7'],
    ['포도', '250', '125', '125'],
], [0.25, 0.25, 0.25, 0.25], size=8.5)
d.warn('사과는 «촬영 시퀀스 통째로» 나누다 보니 조각 크기가 41~66장으로 고르지 않습니다. 복숭아는 검증·시험이 6~7장뿐이라 점수가 크게 흔들립니다. 발표에서 «복숭아는 표본이 작아 한계로 보고한다»고 말하면 됩니다.', head='알아 둘 약점')

# ───────────────────────── 6 ─────────────────────────
chapter(6, '3단계 — 규칙 정하기 실험', 'C1 · 파일럿0 (오늘 완주)')
d.h2('왜 규칙을 먼저 정하나')
d.p('144팀을 예선에서 공정하게 비교하려면 전원이 «같은 규칙»으로 공부해야 합니다. 어떤 팀은 100일, 어떤 팀은 800일 공부시키면 불공정합니다. 그래서 대표 선수 몇 명으로 «며칠이면 충분한가»를 먼저 알아봅니다. 이 대표 선수 실험이 C1 과 파일럿0 입니다.')
d.note('epoch(에폭) = 교과서를 처음부터 끝까지 한 번 다 보는 것. 800 epoch = 교과서 800번 정독. 조기종료(early stopping) = 모의고사 점수가 한동안 안 오르면 공부를 멈추는 규칙.', head='말 뜻')
d.h2('C1 — 빨리 멈추면 손해 보나?')
d.p('48런, 완료, 지금은 이력.')
d.p('대표 조합 2×2(헤더 2개 × 백본 2개) × 과일 4개 × 3조각 = 48번, 각각 300 epoch 를 끝까지 돌리며 매 epoch 모의고사 점수를 기록했습니다. 그 기록 위에서 «느긋한 규칙(80일 지나고 40일 안 오르면 멈춤)»과 «성급한 규칙(40일 지나고 10일 안 오르면 멈춤)»을 적용해 보면, 성급한 규칙이 얼마나 손해 보는지가 나옵니다.')
d.table(['본 것', '값'], [
    ['성급한 규칙이 놓친 IoU (중앙값)', '0.0085'],
    ['최악의 경우', '0.604 (복숭아 한 런: 42일에 멈췄는데 175일에 훨씬 좋아짐)'],
    ['과일별 중앙값', '포도 0.003 · 사과 0.007 · 블루베리 0.017 · 복숭아 0.030'],
    ['평균 멈춘 시점', '느긋 164 epoch · 성급 63 epoch'],
    ['든 시간', '런당 1.72시간, 합계 82 GPU시간'],
], [0.42, 0.58], size=8)
d.p('교훈: 대체로는 빨리 멈춰도 괜찮지만, 작은 데이터셋(복숭아)은 «늦게 트이는 학생»이 있어 성급하게 멈추면 크게 손해 볼 수 있습니다. 그래서 교수님은 규칙을 바로 정하지 않고 더 긴 실험(파일럿0)을 추가하셨습니다. C1 은 옛 3조각 기준이라 지금은 «이력»으로만 남깁니다.')
d.h2('파일럿0 — 800 epoch 끝까지')
d.p('64런, 2026-09-15 완주.')
d.p('같은 대표 2×2 × 과일 4 × «4회전» = 64번을 800 epoch 끝까지 돌립니다. 멈추지 않습니다. 그리고 100·200·…·800 epoch 경계마다 «그때까지 모의고사 1등이었던 모델»을 따로 저장합니다(결정 31, 2026-09-13). 그러면 «100일 예산이면 점수가 얼마, 400일이면 얼마»를 뒤에서 비교할 수 있습니다.')
d.bullets([
    '첫 판(09-11 시작)은 이 구간별 저장이 없어 판정을 못 해서, 09-13 00:11 에 64런 전부 다시 돌렸습니다(v2). 첫 판 숫자는 발표에서 쓰지 않습니다.',
    '매 epoch 시험 점수도 «기록»은 하지만, 그걸 보고 뭘 고르지는 않습니다(고르면 수능 문제를 본 셈).',
    '이 단계의 공식 판정은 저장된 구간별 모델을 «전체 영상 슬라이딩»으로 다시 채점해서 합니다(9장). 아래 숫자는 그 전 단계인 «센터크롭» 값입니다.',
])
d.h2('오늘 완주한 64런 숫자')
d.p('센터크롭 시험 Dice, 2026-09-15 12:30 집계. 표의 «이득» = 큰 예산의 최고 모델 점수 − 작은 예산의 최고 모델 점수. 괄호 안은 16런 중 최댓값.')
d.p('«예산 100 epoch 의 최고 모델» 대비 «예산 800 의 최고 모델» 점수가 얼마나 오르나(이득)를 과일별로 봤습니다.')
d.table(['과일', '100→800 이득', '400→800 이득', 'best epoch'], [
    ['포도', '+0.003 (+0.018)', '0.000 (+0.008)', '418'],
    ['블루베리', '+0.012 (+0.060)', '0.000 (+0.011)', '470'],
    ['사과', '+0.004 (+0.241)', '0.000 (+0.003)', '196'],
    ['복숭아', '+0.012 (+0.458)', '0.000 (+0.013)', '402'],
], [0.22, 0.3, 0.28, 0.2], size=8)
d.h2('이 표를 말로 하면')
d.bullets([
    '"네 과일 모두 400 epoch 이후의 이득은 중앙값 0 입니다. 100 에서 800 으로 늘려도 중앙값은 +0.003~+0.012 로 작습니다."',
    '"다만 최댓값은 사과 +0.241, 복숭아 +0.458 처럼 큰 런이 하나씩 있습니다. 작은 데이터셋에서 늦게 트이는 런이 실제로 있다는 뜻이라, 중앙값만 보고 «100 이면 충분»이라고 단정하지 않습니다."',
    '"이건 센터크롭 값이고, 교수님 프로토콜의 최종 판정은 구간별 체크포인트를 전체영상 슬라이딩으로 평가해서 합니다. 그 단계는 아직입니다."',
])
d.warn('사과 «시험 조각 1» 회전은 네 조합 모두 시험 Dice 가 0.10~0.63 으로 낮은데, 같은 런의 검증 Dice 는 0.83~0.85 로 멀쩡합니다. 학습이 무너진 게 아니라 «그 시험 조각이 다른 조각과 분포가 달라 보인다»가 맞는 말입니다. 블루베리도 조각 1 이 가장 낮습니다(0.83~0.87). 복숭아 VWFormer×ResNet-50 두 런은 100 epoch 시점 점수가 0.40~0.42 로 매우 낮다가 800 에서 0.74~0.88 로 올라왔습니다(늦게 트인 예).', head='발표에서 조심할 표현')
d.note('runs_v2/<런>/budget_checkpoints.json 의 center_crop_test_fg_dice 를 100·400·800 구간에서 뽑아 계산. 64/64 모두 800 epoch 완주(completed_epochs=800). 첫 판 runs/ 는 쓰지 않음.', head='숫자 출처')

# ───────────────────────── 7 ─────────────────────────
chapter(7, '4단계 — 예선 P', '2,304런 · 결선팀 C = A∪B')
d.h2('규모')
d.p('144조합 × 과일 4 × 4회전 = 2,304런. 학습 규칙은 3단계에서 정한 것을 그대로 쓰고 예선 중엔 절대 안 바꿉니다.')
d.h2('점수는 이렇게 낸다')
d.steps([
    '회전마다: 검증 조각(모의고사)으로 «언제 모델»을 고르고, 시험 조각(수능)에서 Dice 를 잰다.',
    '과일마다 4회전 점수를 평균낸다.',
    '과일 4개 점수를 «같은 무게»로 평균낸다. 포도가 2,502장이라고 목소리가 커지면 안 되니까, 4과일 한 표씩.',
    '속도·메모리 같은 효율은 점수에 안 넣는다(결정 20). 효율은 따로 재서 보고만 한다.',
])
d.h2('결선 진출팀 C = A ∪ B')
d.p('A = 헤더마다 1등 백본(12개). B = 백본마다 1등 헤더(12개). C 는 둘의 합집합. 겹치는 게 있으니 12개 이상 23개 이하입니다. 동점이면 이름(ID) 사전순.')
d.p('비유: 12개 반(헤더)에서 각 반 1등을 뽑고(A), 12개 과목(백본)에서 각 과목 1등을 뽑아(B) 합치면 결선 진출자. 반 1등이면서 과목 1등이면 한 번만 세니 12~23명.')
d.h3('3×3 장난감 예시')
d.table(['', '백본 x', '백본 y', '백본 z'], [
    ['헤더 1', '0.90', '0.85', '0.80'],
    ['헤더 2', '0.70', '0.92', '0.60'],
    ['헤더 3', '0.88', '0.75', '0.89'],
], [0.22, 0.26, 0.26, 0.26], size=9)
d.bullets([
    'A(행마다 1등): 헤더1-x, 헤더2-y, 헤더3-z',
    'B(열마다 1등): x열 → 헤더1-x, y열 → 헤더2-y, z열 → 헤더3-z',
    'C = A∪B = {헤더1-x, 헤더2-y, 헤더3-z} = 3개(여기선 전부 겹쳐서 최소 크기). 안 겹치면 최대 6개.',
])
d.h2('왜 C 를 줄이나 (이 말이 중요)')
d.p('«통계 검출력 때문에» 라고 하면 안 됩니다(교수님이 금지한 표현). 바른 이유 셋: ① 계산량(144개를 5번씩 하면 너무 오래 걸림) ② 축 대표성(헤더 축·백본 축 각각의 1등이 다 들어감) ③ 설명 단순성.')
d.h2('예선 끝나면 자물쇠')
d.p('C 가 정해지면 잠급니다. 본실험 결과를 보고 «저 팀도 넣자, 이 팀은 빼자» 하지 않습니다(결정 25). 그리고 코드·가중치·설정·분할의 지문(hash)을 frozen/ 폴더에 기록합니다.')
d.note('복숭아(125장)는 순위가 다른 세 과일과 얼마나 다른지(Kendall τ 라는 «순위 일치도»)를 따로 보고합니다(결정 19). 가중치를 바꾸진 않습니다.', head='복숭아 처리')

# ───────────────────────── 8 ─────────────────────────
chapter(8, '5단계 — 본실험 M1~M5', '봉인을 뜯는다 · 우승자 둘')
d.h2('M1 — 144팀 전부 시험지 한 장')
d.p('학습 = M2∪M3∪M4∪M5, 시험 = M1. 144조합 × 과일 4 = 576런. 여기 1등이 «Full-grid winner(전체 격자 우승자)». 144개를 전부 같은 시험지로 재니까 2장의 질문 2·3(주효과·상호작용)에 답할 수 있습니다.')
d.h2('M2~M5 — C 만 시험지 4장 더')
d.p('C 에 든 팀만 M2, M3, M4, M5 를 차례로 시험지로 삼아 4번 더 학습합니다. 과일 4 × 4조각 × |C| = 16×|C| = 192~368런. 이러면 C 는 다섯 조각 전부에서 점수가 있어 «5-fold OOF(다섯 번 시험 평균)»가 완성됩니다. 여기 1등이 «Cross-validated winner(교차검증 우승자)» = 논문의 최종 추천.')
d.h2('우승자가 둘인 이유')
d.p('M1 1등은 «시험지 한 장» 기준이라 운이 섞일 수 있습니다. 5장 평균 1등이 더 믿을 만합니다. 둘이 같으면 좋고, 다르면 «왜 다른가»를 보고합니다: 분할에 따라 순위가 바뀌는지, 예선→본선에서 순위가 바뀌었는지, 과일마다 다른지, 상호작용이 있는지, C 가 M1 상위권을 얼마나 잡았는지.')
d.warn('«144조합을 5-fold 로 평가했다»는 틀린 말입니다. 144개는 M1 한 번뿐이고, 5-fold 는 C 만입니다. 둘을 꼭 구분해서 말하세요. «M1 결과 보고 C 를 조정했다»도 금지입니다.', head='틀리기 쉬운 말')

# ───────────────────────── 9 ─────────────────────────
chapter(9, '점수 재는 법', 'Dice · 슬라이딩 윈도우')
d.h2('Dice — 색칠이 정답과 얼마나 겹치나')
d.p('정답 마스크(사람이 칠한 과일 영역)와 모델이 칠한 영역이 얼마나 겹치는지. 완전히 같으면 1, 하나도 안 겹치면 0. 식은 «2 × 겹친 넓이 ÷ (정답 넓이 + 모델 넓이)». IoU 는 비슷한 사촌(«겹친 넓이 ÷ 합친 넓이»).')
d.p('사진마다 Dice 를 구하고 사진 단위로 평균합니다(image-macro). 큰 사진 하나가 점수를 독차지하지 않게 하려는 겁니다. 배경까지 섞어 평균내는 mIoU 는 배경이 99% 라 차이가 묻혀서 주지표로 안 씁니다.')
d.h2('슬라이딩 윈도우 vs 센터크롭')
d.p('모델은 한 번에 512×512 조각만 봅니다. 사진은 그보다 훨씬 큽니다. 두 가지 채점법이 있습니다.')
d.table(['방법', '비유', '쓰는 곳'], [
    ['센터크롭', '사진 가운데만 512 잘라서 채점', '학습 중 검증(매 epoch), 효율 측정'],
    ['슬라이딩 윈도우', '돋보기로 사진 전체를 겹치며 훑어 채점 (창 512, 384씩 이동 = 25% 겹침)', '최종 시험 점수(프로토콜 지표)'],
], [0.24, 0.42, 0.34], size=8)
d.p('센터크롭은 빠르지만 사진 가운데만 보니 과실의 75~77% 를 아예 못 봅니다(실측). 그래서 «최종 점수»는 반드시 전체 영상 슬라이딩으로 잽니다. 임계값(threshold)은 0.5 고정, 뒤처리·TTA(시험 시 증강) 없음. 조건을 전 팀 똑같이 두는 게 공정의 핵심입니다.')
d.h2('통계')
d.p('팀끼리 «짝지어(paired)» 비교하고 여러 번 비교할 때 생기는 우연을 보정합니다. Friedman 검정은 보조로, 과일 4개를 블록으로 씁니다. (자세한 건 결과가 나온 뒤 이야기.)')

# ───────────────────────── 10 ─────────────────────────
chapter(10, '숫자 카드', '이것만 외우면 된다')
d.table(['무엇', '숫자'], [
    ['조합 수', '12 × 12 = 144'],
    ['연습장 / 시험지', '20% / 80%, 딱 한 번, seed 3407'],
    ['연습장 안', '4조각 · 4회전 (시험1·검증1·학습2)'],
    ['C1', '2×2 × 4과일 × 3조각 = 48런, 300 epoch (이력)'],
    ['파일럿0', '2×2 × 4과일 × 4회전 = 64런, 800 epoch, 100마다 저장 (09-15 완주)'],
    ['예선 P', '144 × 4 × 4 = 2,304런'],
    ['결선팀 C', 'A∪B, 12~23개'],
    ['본실험 M1', '144 × 4 = 576런'],
    ['본실험 M2~M5', '16 × |C| = 192~368런'],
    ['벤치마크 합계', '3,072~3,248런 (C1·파일럿0 포함 3,184~3,360)'],
    ['최종 채점', '전체영상 슬라이딩 512 / stride 384 / 0.5'],
    ['설계 바뀐 날', '09-11 결정 29·30 (4분할·파일럿0) · 09-13 결정 31 (구간별 저장, v2 재실행)'],
], [0.34, 0.66], size=8.2)
d.tip('3,072 는 어디서? 2,304(예선) + 576(M1) + 192(M2~M5 최소) = 3,072. 최대는 2,304 + 576 + 368 = 3,248.', head='더하기 확인')

# ───────────────────────── 11 ─────────────────────────
chapter(11, '미정 · 하면 안 되는 말', '모르면 «미정»이 정답')
d.h2('아직 안 정해진 것')
d.bullets([
    '파일럿0 결과로 epoch 예산·조기종료·체크포인트 규칙을 확정하는 일 — 완주는 됐고, 전체영상 슬라이딩 평가와 교수님 판정이 남음',
    '공통 학습 설정의 세부: weight decay · warm-up 길이 · 최대 update 수(복숭아 125장 vs 포도 2,502장이 20배라 epoch 대신 update 로 맞출지) · seed 개수 · gradient clipping · 실패(NaN·OOM) 판정',
    '슬라이딩 겹침 부분을 합치는 법(확률 평균 vs logit 평균)',
    'GPU 시간 재산정(C1·파일럿0 실측으로)',
    '문서끼리 어긋난 곳 3 — 파일럿 문서 §1 표엔 파일럿0 «미정»인데 결정 31 은 «epoch 예산 판정»으로 명시. 교수님께 확인할 것',
])
d.h2('하면 안 되는 말 5')
d.table(['틀린 말', '바른 말'], [
    ['후보 선정에 본실험 성능을 썼다', '후보는 20% 연습장 안에서만 골랐다'],
    ['C 를 줄인 이유는 통계 검출력', '계산량 · 축 대표성 · 설명 단순성'],
    ['144조합 M1 순위 = C 의 5-fold 순위', '둘은 다른 시험. 구분해서 보고'],
    ['전체 데이터 5-fold CV', '20% 전용 파일럿 + 144 공통 M1 홀드아웃 + 파일럿 선별 C 의 5-fold OOF'],
    ['전수조사(exhaustive survey)', '후보 감사(candidate audit) / scoping review'],
], [0.44, 0.56], size=8)

# ───────────────────────── 12 ─────────────────────────
chapter(12, '5분 대본 · 예상 질문', '실험 설계만')
d.h2('대본 (약 5분)')
d.steps([
    '(30초) "저는 교수님이 짜 주신 실험 설계의 순서와 이유만 설명하겠습니다. 모델 설명은 인훈 씨가 이어서 합니다. 한 문장으로 하면, 144조합을 데이터의 20%로 먼저 거르고, 80%에서 전부 한 번 + 유망한 것만 다섯 번 평가하는 설계입니다."',
    '(40초) "설계의 목적은 하나, 시험지를 미리 본 학생을 만들지 않는 것입니다. 후보를 고르는 데이터와 최종 점수를 재는 데이터를 완전히 분리합니다. 그래서 데이터를 딱 한 번 20/80 으로 자르고, 같은 영상·같은 시퀀스는 한쪽에 몰아 넣습니다."',
    '(50초) "20% 파일럿 안은 4조각입니다. 회전마다 시험 1, 검증 1, 학습 2. 체크포인트는 검증으로 고르고 점수는 시험 조각으로 잽니다. 9월 11일에 3조각에서 4조각으로 바뀌었는데, 고르는 조각과 재는 조각을 파일럿 안에서도 갈라내기 위해서입니다."',
    '(60초) "예선 전에 학습 규칙부터 정합니다. C1 48런에서 성급한 조기종료가 놓치는 IoU 는 중앙값 0.0085 였지만 복숭아에서 최악 0.6 이 나와서, 교수님이 800 epoch 를 끝까지 보는 파일럿0 64런을 추가하셨습니다. 100 epoch 마다 그때까지 최고 검증 모델을 저장해서 몇 epoch 예산이면 충분한지 봅니다. 64런은 오늘 완주했습니다. 센터크롭 중간값으로는 네 과일 모두 400 이후 이득이 0 이고, 다만 사과·복숭아엔 늦게 트이는 런이 하나씩 있습니다. 최종 판정은 전체영상 슬라이딩으로 하며 아직 남았습니다."',
    '(50초) "규칙이 얼면 예선 P 입니다. 144조합 × 4과일 × 4회전 = 2,304런. 과일별 4회전 평균을 4과일 동일 가중으로 합칩니다. 헤더별 1등 백본 A 와 백본별 1등 헤더 B 의 합집합이 결선팀 C, 12~23개. 효율은 점수에 안 넣습니다. 여기서 C 를 잠급니다."',
    '(50초) "본실험은 80%를 M1~M5 로 나눕니다. M1 에서 144조합 전부 576런, 여기 1등이 Full-grid winner. C 만 M2~M5 를 추가해 192~368런, 5-fold 1등이 Cross-validated winner 이고 논문의 최종 추천입니다. 합치면 3,072~3,248런입니다."',
    '(20초) "최종 점수는 센터크롭이 아니라 전체영상 슬라이딩 윈도우로 잽니다. 센터크롭은 과실의 75% 이상을 못 보기 때문입니다. 미정인 것은 epoch 예산 확정과 세부 학습 설정입니다. 이상입니다."',
])
d.h2('예상 질문')
d.qa('왜 20% 를 따로 떼나?', '후보를 고르는 데이터와 최종 점수를 재는 데이터를 분리하기 위해서입니다. 옛날 CCASeg 때 본실험 성능으로 후보를 고른 게 문제가 됐습니다.')
d.qa('왜 3조각에서 4조각으로 바꿨나?', '3조각일 땐 검증 조각 하나가 «시점 고르기»와 «점수 재기»를 둘 다 해서, 파일럿 안에서도 둘을 갈라내려고 시험 조각을 하나 더 뒀습니다(결정 29).')
d.qa('파일럿0 은 왜 800 epoch 나 돌리나?', 'C1 에서 성급한 조기종료가 복숭아에서 0.6 이나 놓친 경우가 있어서, 긴 궤적에서 «몇 epoch 예산이면 충분한가»를 구간별 체크포인트로 직접 보려는 것입니다.')
d.qa('그래서 몇 epoch 로 정해도 되나?', '아직 아닙니다. 센터크롭 중앙값으로는 400 이후 이득이 0 이지만, 최종 판정은 구간별 체크포인트를 전체영상 슬라이딩으로 평가해서 교수님이 하십니다.')
d.qa('C 는 왜 12~23개인가?', 'A(헤더별 1등 백본 12개)와 B(백본별 1등 헤더 12개)의 합집합입니다. 전체 1등 조합은 자기 행에서도 열에서도 1등이라 A 와 B 에 모두 들어가므로 최소 하나는 겹칩니다. 그래서 최대 23, 전부 겹치면 12 입니다.')
d.qa('C 를 줄이는 이유가 통계 검출력 아닌가?', '아닙니다. 계산량, 헤더·백본 축의 대표성, 설명의 단순성 때문입니다. 검출력이라고 쓰면 안 된다고 교수님이 명시하셨습니다.')
d.qa('두 우승자가 다르면?', '분할 의존성, 예선→본선 순위 변화, 과일별 이질성, 상호작용, C 가 M1 상위권을 얼마나 잡았는지를 보고합니다. 최종 추천은 5-fold 쪽입니다.')
d.qa('사과는 왜 흔들리나?', '시험 조각 1 회전에서만 네 조합 모두 시험 Dice 가 낮고 검증은 멀쩡합니다. 그 조각의 분포가 달라 보이며 그룹 단위 분할의 영향으로 추정합니다. 원인은 확인 중입니다.')
d.qa('복숭아는 125장뿐인데?', '가중치는 안 바꾸고, 복숭아 순위와 나머지 세 과일 순위의 Kendall τ 를 따로 보고하며 한계로 씁니다.')
d.qa('센터크롭은 왜 최종 점수로 안 쓰나?', '사진 가운데 512 만 보니 과실의 75~77% 를 못 봅니다. 학습 중 검증에만 쓰고, 최종은 전체영상 슬라이딩입니다.')
d.qa('(모델 질문이 오면)', '"그 부분은 인훈 씨가 이어서 설명합니다." 라고 넘깁니다.')

# ───────────────────────── 13 ─────────────────────────
chapter(13, '용어 사전', '모르는 말은 여기')
for w, e, m in [
    ('세그멘테이션', 'segmentation', '사진의 픽셀마다 «과일이다/아니다»를 칠하는 일'),
    ('헤더 / 백본', 'head / backbone', '머리 / 몸통. 몸통이 특징을 뽑고 머리가 색칠 결과를 만든다'),
    ('조합', 'pair, combination', '헤더 하나 + 백본 하나. 12×12 = 144개'),
    ('런', 'run', '학습 한 번. «조합 × 과일 × 회전» 하나가 런 하나'),
    ('epoch', '에폭', '학습 데이터를 처음부터 끝까지 한 번 다 보는 것'),
    ('체크포인트', 'checkpoint', '학습 도중 특정 시점의 모델을 파일로 저장한 것'),
    ('조기종료', 'early stopping', '검증 점수가 한동안 안 오르면 학습을 멈추는 규칙'),
    ('학습/검증/시험', 'train/val/test', '교과서 / 모의고사 / 수능'),
    ('fold, 조각', 'fold', '데이터를 나눈 한 덩어리'),
    ('회전', 'rotation', '어느 조각이 시험을 맡을지 바꿔 가며 반복하는 것'),
    ('k-fold CV', 'cross-validation', 'k조각이 돌아가며 시험을 맡아 k번 평가'),
    ('OOF', 'out-of-fold', '각 조각이 시험이었을 때의 점수를 모아 붙인 것'),
    ('홀드아웃', 'holdout', '따로 떼어 두고 점수 재는 데만 쓰는 데이터. M1 이 공통 홀드아웃'),
    ('누수', 'leakage', '시험 데이터가 고르기·학습에 새어 들어가는 것'),
    ('그룹 분할', 'group split', '같은 영상·시퀀스는 통째로 한쪽에 넣는 분할'),
    ('seed', '난수 씨앗', '같은 씨앗이면 같은 «무작위» 결과. 3407'),
    ('hash', '지문', '파일 내용에서 뽑은 짧은 코드. 바뀌면 지문도 바뀜'),
    ('동결', 'freeze', '정한 뒤 다시 안 바꾸는 것'),
    ('스모크 테스트', 'smoke test', '잠깐 돌려 에러 없이 도는지만 확인'),
    ('Dice / IoU', '', '칠한 영역과 정답이 겹치는 정도. 1 이 만점'),
    ('image-macro', '', '사진마다 점수 내고 사진 단위로 평균'),
    ('센터크롭', 'center crop', '사진 가운데 512×512 만 잘라 보는 것'),
    ('슬라이딩 윈도우', 'sliding window', '창을 조금씩 옮기며 사진 전체를 훑는 것'),
    ('stride', '', '창을 한 번에 옮기는 거리. 384 = 창 512 의 75%'),
    ('threshold', '임계값', '확률이 이 값 이상이면 «과일». 0.5'),
    ('TTA', 'test-time augmentation', '시험 때 사진을 뒤집어 여러 번 예측해 합치는 꼼수. 안 씀'),
    ('주효과 / 상호작용', 'main effect / interaction', '부품 하나의 힘 / 특정 짝일 때만 나는 힘'),
    ('A∪B', '합집합', '헤더별 1등 백본 ∪ 백본별 1등 헤더 = 결선팀 C'),
    ('Kendall τ', '켄달 타우', '두 순위표가 얼마나 비슷한지(1 이면 같음)'),
    ('regret', '후회', '성급한 규칙이 느긋한 규칙보다 놓친 점수'),
    ('GPU-h', 'GPU 시간', 'GPU 한 장을 1시간 쓴 양'),
]:
    d.term(w, e, m)
d.h2('이 책의 근거 파일')
d.bullets([
    'platform/planning/260906_파일럿_단계_및_결정규칙.md (2026-09-13 수정) — 단계·결정 권한 정본',
    'platform/planning/260905_00_프로토콜_관리원칙.md — 결정 12~31',
    'platform/planning/260905_01_데이터_및_분할.md · 05_학습_및_검증.md · 06_시험_평가_및_보고.md',
    'platform/04_experiments/260911_파일럿0/README.md · runs_v2/ (64런 완주)',
    '문서/260912_교수님_실험설계_쉬운말정리.md · 260912_실험설계_순서도_2판.png',
])

# ───────────────────────── 14 ─────────────────────────
chapter(14, '세 AI 에게 물어본 결과', 'Codex · Gemini · DeepSeek')
d.p('2026-09-15 12:36, Codex(gpt-6-astra)·Gemini·DeepSeek 에게 «중학생 비유·틀리기 쉬운 말·예상 질문»을 물었습니다. DeepSeek 은 학교 게이트웨이 시간 초과(524)로 답 없음(규칙대로 1회만 시도).')
d.h2('일치한 것 — 그대로 써도 됨')
d.bullets([
    '20/80 = 문제집(또는 요리 재료)을 딱 한 번 가르고 연습분은 본평가에 안 넣는다.',
    '검증 = 코치·모의고사 점수(언제 모델을 쓸지 고름) / 시험 = 성적표(고르는 데 안 씀).',
    '«전체 데이터 5-fold CV» 는 틀린 말. 5-fold 는 C 만.',
    '파일럿0 완주 ≠ 학습 규칙 확정 (Codex). 분석·판정이 남았다.',
    '포도가 점수를 좌우하지 않게 4과일 동일 가중 (Codex 예상 질문 4).',
])
d.h2('쓸 만한 비유')
d.bullets([
    'Gemini: 센터크롭은 «열쇠구멍으로 방 안 보기», 슬라이딩은 «돋보기로 방 전체 훑기».',
    'Codex: 4회전은 «네 봉투 — 둘로 공부, 하나로 점검, 하나로 시험, 역할을 돌린다».',
    'Codex: A∪B 는 «신발(헤더)마다 가장 잘 뛰는 선수 + 선수(백본)마다 가장 잘 맞는 신발» 합치기. 전체 점수 상위 몇 개만 뽑는 것과 다르다는 점이 핵심.',
])
d.h2('틀렸거나 조심할 것')
d.bullets([
    'Gemini «조기종료 규칙은 놓치는 차이가 작아(0.0085) 과감히 도입했다» — 틀림. 규칙은 아직 미확정이고, 최악 0.6 때문에 파일럿0 를 추가한 것.',
    'Gemini «M1 = 예선, M2~M5 = 결승» — 틀림. 예선은 P(파일럿), M1 은 본실험의 공통 시험, M2~M5 는 C 의 추가 시험.',
    'Gemini «C1 을 300 epoch 로 끝낸 게 논리적 허점» — 그래서 파일럿0(800) 를 추가한 것이므로 허점이 아니라 순서. 다만 «C1 은 이력» 이라고 말하면 됨.',
    'Codex «3,072~3,248 은 벤치마크만» — 맞음. 이 책 10장처럼 C1·파일럿0 포함 3,184~3,360 을 구분해 말할 것.',
    'Codex «C 상한 23 은 동점 규칙이 있어야 성립» — 맞음. 동점은 ID 사전순으로 통일돼 있어 성립(12장 Q&A 반영).',
])
d.note('원문: ~/.council/260915_중학생설명/{codex.md, gemini.md}. 서버 클로드 사전 의견: ~/.council/내의견_260915_중학생설명.md', head='원문 위치')
d.save()
print(OUT)
