# -*- coding: utf-8 -*-
"""「우리 방법을 논문 어디서 따왔다고 말할 것인가」 + 「지금부터 할 일」 안내서 PDF.

목적
  1. 팀원에게 그대로 말할 수 있는 «주장 문장»과 그 근거 위치를 한 표로 준다.
  2. 서버 Claude 와 로컬 클로드(브라우저 가능)를 어떻게 나눠 쓸지 규칙으로 정한다.
  3. 곽동신이 지금부터 손으로 해야 할 일을 클릭 단위로 적는다.

근거는 문서/260812_카운팅방법_근거_확정본.md (Crossref 8/8 검증 + 원문 대조) 기준.
숫자는 output/*.json 에서 읽는다.

사용:  $PY tools/make_evidence_workflow_pdf.py
출력:  reports/근거_결론과_다음단계_안내서.pdf
"""
import json
import sys
from pathlib import Path

REPO = Path('/data/project/2026summer/kds0206/semantic-segmentation')
BASE = Path('/data/project/2026summer/kds0206')
sys.path.insert(0, str(REPO / 'tools'))
from pdfdoc import Doc                                       # noqa: E402

STATS = REPO / 'output/object_stats_4fruits_split.json'
VALID = REPO / 'output/instance_split_validation.json'
OUT = REPO / 'reports/근거_결론과_다음단계_안내서.pdf'
DATE = '2026-08-12'
ORDER = ['blueberry', 'apple', 'peach', 'grape']


def build():
    s = json.loads(STATS.read_text())
    v = json.loads(VALID.read_text())
    F = s['fruits']
    ws = v['watershed']['1.1']
    tot_img = sum(F[f]['images'] for f in ORDER)

    d = Doc(OUT,
            title='어디서 따왔다고\n말할 것인가',
            subtitle='개체 계수 방법의 문헌 근거 — 결론 · 역할분담 · 지금부터 할 일',
            volume='안내서',
            footer='근거 결론과 다음 단계 안내서',
            date=DATE)

    d.cover(lines=[
        '근거 논문 : Crossref 로 8건 전수 검증 (8/8 일치)',
        '인용문    : 원문 PDF 로 대조 확인',
        f'대상 데이터 : 4과일 {tot_img:,}장 전수',
    ], badge='팀에 바로 말해도 되는 상태')

    d.toc([
        (1, '30초 결론', '팀원이 물으면 이 세 문장만 말하면 된다.'),
        (2, '무엇을 어디서 따왔나', '우리 행위 하나하나에 논문의 «어느 절»이 대응하는지 표로.'),
        (3, '따온 것 vs 우리가 더한 것', '베낀 게 아니다. 무엇을 새로 했는지 분명히 한다.'),
        (4, '반박이 들어오면', '예상 질문 6개와 대답. 근거 문장을 그대로 붙여 뒀다.'),
        (5, '서버 클로드와 로컬 클로드 나눠 쓰기', '둘이 무엇을 잘하고 못하는지, 왕복 절차.'),
        (6, '지금부터 할 일', '오늘 · 이번 주 · 미팅 전. 클릭 단위로.'),
        (7, '하면 안 되는 것', '인용 금지 4건과 그 이유.'),
    ])

    # ═════════════════════════════════════════════ 1장
    d.chapter(1, '30초 결론',
              '팀원이 "그 방법 어디서 나온 거예요?"라고 물으면 이 세 문장이면 끝납니다.')

    d.h1('세 문장')
    d.steps([
        '**사과 과수원 논문에 똑같은 방법이 있습니다.** Bargoti & Underwood 2017 (Journal of '
        'Field Robotics)은 픽셀 단위 세그멘테이션 결과를 **워터셰드로 후처리해서 사과를 한 알씩 '
        '검출하고 셉니다.** 그 논문에서도 워터셰드가 가장 성적이 좋았고 검출 F1 0.858입니다.',
        '**디텍션 모델을 꼭 돌려야 하는 게 아닙니다.** 포도알 논문 Zabawa et al. 2020 (ISPRS '
        'Journal)은 **"무거운 인스턴스 세그멘테이션을 피하려고"** 세그멘테이션 + 후처리로 셌다고 '
        '논문에 직접 써 놨습니다.',
        f'**한계까지 문헌에 있고, 우리는 그걸 숫자로 쟀습니다.** 가려서 조각나면 과다계수, 붙어 '
        f'있으면 과소계수 — 둘 다 논문에 적혀 있습니다. 우리는 사과 정답으로 실측해서 '
        f'**분리 {ws["total_bias_pct"]:+.1f}% / 덩어리 {v["cc"]["total_bias_pct"]:+.1f}%** 를 확인했습니다.',
    ])

    d.tip('한 마디로: **"우리가 지어낸 방법이 아니라, 사과·포도 논문에 있는 방법을 그대로 쓰고 '
          '정확도까지 우리 데이터로 재봤다"** 입니다.')

    d.h1('근거 논문 3편만 외우면 됩니다')
    d.table(
        ['논문', '무엇의 근거인가'],
        [['**Bargoti & Underwood 2017**\nJ. Field Robotics 34(6):1039–1060',
          '거리변환 + 워터셰드로 열매를 한 알씩 세는 것. **우리 방법 그 자체**'],
         ['**Zabawa et al. 2020**\nISPRS J. 164:73–83',
          '디텍션 모델 없이 세그멘테이션 + 후처리로 세는 것. 붙은 개체가 합쳐지는 문제'],
         ['**Häni et al. 2020 (MinneApple)**\nIEEE RA-L 5(2):852–858',
          '장당 개체 수 통계로 데이터셋을 정당화하는 것. 우리 그림 2·3의 형식']],
        [0.36, 0.64], size=9.2)

    # ═════════════════════════════════════════════ 2장
    d.chapter(2, '무엇을 어디서 따왔나',
              '우리가 한 행위 하나하나에 논문의 어느 부분이 대응하는지 정리했습니다. '
              '이 표가 이 문서의 핵심입니다.')

    d.h1('행위 → 근거 대응표')
    d.p('«논문의 어느 부분»까지 적어 두었으니, 교수님이 "그거 어디 나와 있어요?"라고 하시면 '
        '이 칸을 그대로 읽으면 됩니다.')

    d.table(
        ['우리가 한 일', '따온 논문', '논문의 어느 부분'],
        [['마스크에 **거리변환**을 걸어\n열매 중심을 찾음',
          'Bargoti &\nUnderwood 2017',
          '본문 과실 검출 절 — "computing **distances of individual fruit pixels to the nearest '
          'background pixel**, resulting in a local maximum … around the **centre of each fruit**"'],
         ['**워터셰드**로 붙은 열매를\n한 알씩 분리',
          'Bargoti &\nUnderwood 2017',
          '초록 — "The pixel-wise fruit segmentation output is processed using the **Watershed '
          'Segmentation (WS)** … to **detect and count individual fruits**"'],
         ['**디텍션 모델을 안 씀**',
          'Zabawa et al.\n2020',
          '서론 기여 부분 — "**To avoid a computationally intensive instance segmentation** … '
          'There is no need to explicitly perform an instance segmentation."'],
         ['**marker-controlled** 방식을\n고름 (봉우리를 마커로)',
          'Kornilov &\nSafonov 2018',
          '2.3 Oversegmentation Problem 절 — 표준 워터셰드의 과분할을 지적하고 '
          'marker-controlled 를 해법으로 제시'],
         ['**장당 개체 수 그래프**\n(그림 3)',
          'MinneApple 2020\n+ COCO 2014',
          'MinneApple p.4 V. Dataset Statistics 와 Fig.3 / COCO 원논문의 "the **number of labeled '
          'instances per image**" 비교'],
         ['**라벨 칠한 샘플 사진**\n+ 크롭 선 (그림 2)',
          'MinneApple 2020',
          'p.3 Fig.2 — "Samples of **annotated images** … annotated with object instance masks"'],
         # 🔴 2026-09-19 «개수 세기» 사이클4(M12 · 사이클3 3차 §2-5): 전에는 이 줄이 «낙과는
          # 라벨 없음» 이라고 **단정**했다. 사실은 다르다 — 원논문이 낙과를 빼라고 적었을 뿐이고,
          # 우리 사과 라벨에는 **낙과 라벨이 남은 장이 있다**(낙과가 보이는 42장 중 5장, 사이클2
          # 실측). 그림 3종의 캡션은 이미 이 사실로 고쳐졌으므로(09-19) 이 표도 같은 말을 한다.
          ['캡션에 «**낙과는 원칙적으로\n라벨 대상이 아님**» + 우리\n자료의 예외를 함께',
          'MinneApple 2020',
          'p.4 IV. Image Annotation — "the ones **on the ground** and trees in the background '
          '**were not tagged**". ⚠ 우리 사과에는 낙과 라벨이 남은 장이 있다(낙과가 보이는 42장 중 '
          '**5장**) → 캡션은 «원칙적으로 라벨 대상이 아니지만 규칙 미정(교수님 확인 대기)» 으로 적는다'],
         ['**정답과 대조해 오차를\n실측**해서 보고',
          'Bargoti 2017 /\nZabawa 2020',
          '두 논문 모두 자기 계수 방법을 정답·다른 방법과 비교해 수치로 방어함 '
          '(F1 0.858 / Mask R-CNN 과 비교)']],
        [0.20, 0.16, 0.64], size=8.4)

    d.note('표의 인용문은 전부 **원문에서 글자 그대로 확인**한 것입니다. '
           '서지정보(권·호·페이지)는 Crossref 로 8건 전수 조회해 일치를 확인했습니다.')

    # ═════════════════════════════════════════════ 3장
    d.chapter(3, '따온 것 vs 우리가 더한 것',
              '"그럼 남의 방법 그대로 쓴 거네요?"라는 말이 나올 수 있습니다. 아닙니다. '
              '무엇이 기존 것이고 무엇이 우리 것인지 분명히 해 둡니다.')

    d.h1('기존 것 (따온 것)')
    d.bullets([
        '거리변환 + 워터셰드로 붙은 객체를 가르는 아이디어 — Bargoti 2017',
        '세그멘테이션 후처리로 개수를 세는 방식 — Zabawa 2020',
        '장당 개체 수 통계로 데이터셋을 정당화하는 형식 — MinneApple, COCO',
    ])

    d.h1('우리가 더한 것')
    apple = F['apple']
    d.table(
        ['우리가 새로 한 것', '왜 의미가 있나'],
        [[f'**정확도를 실제로 쟀다** — 사과 {v["images"]:,}장 전수, 정답 대비 '
          f'{ws["total_bias_pct"]:+.1f}%',
          'Bargoti 는 자기 데이터에서 F1 을 냈을 뿐, **우리 데이터에서 이 방법이 맞는지**는 '
          '아무도 안 재봤다. 우리가 쟀다'],
         ['**정답 라벨을 3종 발굴** — 사과 인스턴스 ID · 복숭아 COCO 폴리곤 · 포도 CERTH RLE',
          '알고리즘 값이 아니라 **정답으로 표를 채웠다.** 포도는 논문 Table 1 의 9,832송이와 정확히 일치'],
         ['**과일마다 방법을 다르게** 적용 — 포도는 쪼개지 않음',
          '포도 라벨이 «송이» 단위라 쪼개면 부풀려진다. **라벨 성격을 보고 판단**한 것이라 방어 가능'],
         [f'**4과일 {tot_img:,}장 전수** — 표본 추출 안 함',
          '"몇 장만 골라 잰 것 아니냐"는 반박이 원천 차단됨'],
         ['**한계를 먼저 밝힘** — 블루베리는 정답이 없어 오차 측정 불가',
          '심사자가 지적하기 전에 우리가 먼저 적어 두는 것이 훨씬 강하다']],
        [0.36, 0.64], size=8.8)

    d.tip('발표에서 쓸 문장: **"방법은 선행 연구를 따랐고, 그 방법이 우리 데이터에서 맞는지를 '
          '정답으로 검증한 것이 우리 기여입니다."**')

    # ═════════════════════════════════════════════ 4장
    d.chapter(4, '반박이 들어오면',
              '나올 만한 질문 6개와 대답입니다. 근거 문장을 그대로 넣어 두었습니다.')

    d.qa('"교수님이 디텍션 돌리라고 하셨는데 왜 안 돌렸나요?"',
         '녹취 13:11 에 "정확히 테스트할 건 아니니까 알고리즘 돌린 결과로 플롯만 만들면 된다"고 '
         '하셨습니다. 그리고 Zabawa et al. 2020 은 무거운 인스턴스 세그멘테이션을 피하려고 '
         '일부러 세그멘테이션 + 후처리를 택했다고 논문에 써 놨습니다. 무엇보다 우리는 '
         f'사과 정답으로 오차를 실측해서 {ws["total_bias_pct"]:+.1f}% 임을 확인했습니다. '
         '디텍션 모델을 돌렸어도 이보다 나은 근거는 안 나옵니다.')

    d.qa('"워터셰드 그거 옛날 방법 아닌가요?"',
         '2024년 논문(상추 LettuceNet, Agriculture 14(8):1412)과 2021년 논문(샬롯, Frontiers in '
         'Robotics and AI 8:627067)에도 같은 계열의 후처리가 쓰입니다. 2024년 과실 세그멘테이션 '
         '논문이 Bargoti 의 워터셰드를 선행연구로 인용하기도 합니다. 지금도 쓰이는 방법입니다.')

    d.qa('"그 방법 부정확하지 않나요?"',
         '부정확할 수 있고, 그 한계가 문헌에 적혀 있습니다. Bargoti 는 "가려서 조각난 열매는 다시 '
         '합칠 수 없다"고 했고, Kornilov 2018 은 표준 워터셰드가 과분할을 일으킨다고 했습니다. '
         '그래서 우리는 marker-controlled 방식을 썼고, 두 오차 방향을 실제로 쟀습니다 — '
         f'사과 덩어리 세기 {v["cc"]["total_bias_pct"]:+.1f}%, 포도 조각남 +53.6%. '
         '측정해서 보고한 것이 핵심입니다.')

    d.qa('"블루베리는 검증 안 했잖아요?"',
         '맞습니다. 블루베리는 서버 어디에도 인스턴스 정답 라벨이 없어서 오차를 잴 수 없었습니다. '
         '그래서 그림 캡션에 "정답 라벨이 없어 오차를 측정하지 못했다"를 명시했습니다. '
         '숨기지 않고 적어 두는 것이 방어입니다.')

    d.qa('"포도만 왜 다르게 셌나요?"',
         'CERTH 라벨이 «알»이 아니라 «송이» 단위로 그려져 있어서입니다. 쪼개면 큰 송이 하나가 '
         '둘로 갈려 부풀려집니다. 정답 9,832송이를 그대로 썼고, 이 값은 CERTH 논문 Table 1 과 '
         '정확히 일치합니다.')

    d.qa('"우리 데이터셋으로 논문 쓸 이유가 있나요?"',
         '바로 그 질문에 답하려고 그림 3을 만들었습니다. MinneApple 은 장당 41.2개, COCO 는 7.7개, '
         'PASCAL VOC 는 2.3개입니다. 우리 4과일은 블루베리 36.9 / 사과 40.4 로 일반 데이터셋의 '
         '5배가 넘습니다. "종류는 1가지인데 개체는 수십 개"라는 성격 자체가 다릅니다. '
         '이 논법은 COCO 원논문이 처음 쓴 방식입니다.')

    # ═════════════════════════════════════════════ 5장
    d.chapter(5, '서버 클로드와 로컬 클로드 나눠 쓰기',
              '이번에 둘을 같이 써서 좋은 결과가 나왔습니다. 왜 좋았는지, 앞으로 어떻게 나눌지 '
              '규칙으로 정해 둡니다.')

    d.h1('둘의 차이')
    d.table(
        ['', '서버 클로드 (지금 이 창)', '로컬 클로드 (노트북)'],
        [['잘하는 것',
          '· 데이터·코드·GPU\n· 숫자 계산과 전수 검증\n· 그림·PDF 생성\n· 작업기록 관리',
          '· **크롬으로 실제 웹페이지 열람**\n· 막힌 사이트 접근\n· 대학 도서관 로그인\n· Crossref 등 조회'],
         ['못하는 것',
          '· MDPI·Elsevier 등 **차단됨**\n· 유료 논문 본문 접근 불가',
          '· 우리 데이터·코드에 접근 못 함\n· 숫자를 직접 못 잼'],
         ['이번에 한 일',
          '방법 찾기 · arXiv 원문 대조 · 문서화 · **최종 검증**',
          '**서지 확정** · 추가 논문 3편 · **한계 근거 발굴**']],
        [0.13, 0.44, 0.43], size=8.6)

    d.h1('규칙 3가지')
    d.steps([
        '**숫자와 코드는 서버가 단일 출처.** 로컬 클로드에게 우리 숫자를 물어보지 마세요. '
        '서버가 준 숫자를 «전달»만 하십시오. 두 곳에서 각각 계산하면 반드시 어긋납니다.',
        '**막힌 논문은 로컬이 연다.** 서버가 "403" 또는 "유료라 못 봄"이라고 하면 그때 로컬에 넘깁니다.',
        '**최종 병합은 서버가 한다.** 로컬 결과를 그대로 쓰지 말고 서버에 붙여넣어 검증받으십시오. '
        '이번에도 8건 중 1건은 문구가 달랐고 1건은 재확인이 안 됐습니다.',
    ])

    d.warn('이번에 실제로 잡아낸 것 — 로컬 보고서의 샬롯 논문 인용문 1개가 원문과 **문구가 달랐습니다.** '
           '서버가 원문을 다시 열어 확인한 판본으로 교체했습니다. '
           '**한쪽 결과만 믿고 논문에 넣었으면 잘못된 인용이 됐을 것입니다.**')

    d.h1('왕복 절차 (앞으로 계속 이렇게)')
    d.steps([
        '서버 클로드에게 일을 시킨다 → 서버가 할 수 있는 데까지 하고 **«막힌 것»을 목록으로** 만든다.',
        '서버가 `문서/…_붙여넣기.txt` 를 만들어 준다 → 그걸 로컬 클로드에 복사해 붙인다.',
        '로컬 클로드가 md 파일로 답한다 → 그 내용을 **서버 창에 그대로 붙여넣는다.**',
        '서버가 검증하고 «확정본» 문서를 만든다 → 그걸 팀에 준다.',
    ])

    # ═════════════════════════════════════════════ 6장
    d.chapter(6, '지금부터 할 일',
              '순서대로만 하시면 됩니다. 오늘 할 것은 1번 하나입니다.')

    d.h1('오늘 — 팀 카톡 보내기 (10분)')
    d.steps([
        'VS Code 왼쪽에서 `문서/260812_카톡_근거조사답변.txt` 를 엽니다.',
        'Ctrl+A → Ctrl+C 로 전부 복사합니다.',
        '팀 카톡방에 붙여넣고 보냅니다. **표나 이모지를 덧붙이지 마세요** (그대로가 규칙에 맞습니다).',
        '이어서 파일 2개를 첨부합니다 — `문서/260812_카운팅방법_근거_확정본.md` 와 '
        '`문서/260812_참고문헌.bib`. VS Code 에서 파일을 오른쪽 클릭 → Download 로 내려받아 첨부합니다.',
    ])
    d.note('1·2번 결과 그림 5장은 **아직 안 보냈다면** `문서/260810_카톡_1번2번_최종.txt` 와 함께 '
           '같이 보내면 됩니다. 그림 3(장당 개체 수)은 2026-08-12 에 각주를 고쳐 다시 뽑았으니 '
           '**새로 받은 파일**을 보내십시오.')

    d.h1('오늘 또는 내일 — 로컬 클로드에 2차 요청 (5분)')
    d.p('남은 확인 3건이 있습니다. 이미 붙여넣기 파일을 만들어 두었습니다.')
    d.code('문서/260812_로컬클로드_2차요청_붙여넣기.txt', caption='이 파일 내용을 통째로 복사해 로컬 클로드에 붙여넣기')
    d.bullets([
        '**Kornilov 2018** 의 과분할 문장 — 서버는 MDPI 차단(403)으로 못 봤습니다.',
        '**학술지 최종판** 문구 대조 — 지금 인용문은 arXiv 판 기준입니다.',
        '**상추 논문(LettuceNet)** 의 정확도 수치 — 본문 표에서 확인 필요.',
    ])
    d.p('답이 오면 그 내용을 **이 서버 창에 그대로 붙여넣어 주세요.** 제가 검증해서 확정본을 갱신합니다.')

    d.h1('이번 주 — 논문에 반영')
    d.steps([
        '`문서/260812_참고문헌.bib` 의 9편을 논문 `references.bib` 에 합칩니다. '
        '(중복 키가 있으면 서버 창에서 말씀해 주시면 제가 정리합니다)',
        '`문서/260812_카운팅방법_근거_확정본.md` 6절의 **영문 Method 문단**을 논문 Method 절에 붙입니다.',
        # 🔴 2026-09-19 사이클4(M12): «미라벨을 **유지**» 는 이제 틀린 지시다 — 그림 3종의 캡션은
        # 09-19 에 «원칙적으로 라벨 대상이 아니지만 우리 사과 일부에 남아 규칙 미정» 으로 **바뀌었다**.
        # 논문에 옮길 때 옛 단정으로 되돌리지 않도록 지시문 자체를 사실에 맞춘다.
        '데이터셋 설명 절에 그림 2·3 을 넣고, 캡션에 «낙과는 원칙적으로 라벨 대상이 아니지만 우리 '
        '사과 일부(낙과 보이는 42장 중 5장)에 라벨이 남아 **규칙 미정**(교수님 확인 대기)»과 '
        '«블루베리는 정답 라벨 없음»을 적습니다. 그림 3종의 캡션이 이미 그 문장입니다 — '
        '옛 «낙과 미라벨» 단정으로 되돌리지 마십시오.',
    ])

    d.h1('내 몫이 아닌 것 (헷갈리지 마세요)')
    d.p('0810 미팅 지시 12개 중 곽동신이 가져간 것은 **1·2번뿐**입니다. 아래는 다른 사람 몫이거나 '
        '다음 관문입니다. "왜 안 했냐"는 말이 나오면 이 표를 보여주십시오.')
    d.table(['번호', '할 일', '누구 몫'],
            [['①', '헤더·백본 조합 확정 + 근거 보고서', '팀 공동 — 다음 관문'],
             ['②', '센터크롭 평가 타당성 (슬라이딩 윈도우 여부)',
              '다른 담당자 — 다만 **내가 만든 센터크롭 그림이 근거로 들어감**'],
             ['③', '고정 설정 근거 문서', '팀 공동 — 다음 관문'],
             ['⑦~⑫', '전체 실험 스윕과 결과 제시', '①②③ 끝난 뒤']],
            [0.09, 0.50, 0.41], size=9.0)

    # ═════════════════════════════════════════════ 7장
    d.chapter(7, '하면 안 되는 것',
              '근거가 확인되지 않은 것을 쓰면, 근거를 찾은 노력이 통째로 무너집니다.')

    d.h1('인용 금지 4건')
    d.table(['항목', '왜 금지인가'],
            [['Objects365 의 "장당 박스 9.04 vs COCO 7.34"',
              '서버·로컬 **둘 다 원문을 못 열었습니다.** 검색 요약에만 나온 숫자입니다'],
             ['Maheswari 2021 리뷰의 Bargoti 워터셰드 문장',
              '검색 요약에는 있다고 나왔는데 **원문에서 찾지 못했습니다.** 둘 다 못 찾았습니다'],
             ['Palacios 2020 본문의 connected component 문장',
              '본문이 **유료 구간**이라 확인 불가. 초록의 수치만 인용 가능'],
             ['Dorj et al. 2017 직접 인용',
              '원문을 아무도 안 열었습니다. Rong 2026 리뷰를 **경유해서만** 언급 가능']],
            [0.34, 0.66], size=8.8)

    d.warn('공통 원칙: **검색 결과 요약을 근거로 쓰지 않습니다.** 원문을 열어 문장을 확인한 것만 씁니다. '
           '확인 못 한 것은 "확인 못 했다"고 적습니다. 이번 조사 내내 이 원칙을 지켰고, '
           '그래서 이 문서를 그대로 교수님께 보여드려도 됩니다.')

    d.gap(1)
    d.h1('마지막 정리')
    d.qa('"결론이 뭔가?"',
         '우리 계수 방법은 사과 과수원 논문(Bargoti & Underwood 2017, Journal of Field Robotics)의 '
         '워터셰드 후처리를 따른 것이고, 디텍션 모델을 쓰지 않은 선택은 포도알 논문(Zabawa et al. '
         '2020, ISPRS Journal)이 같은 이유로 한 선택입니다. 통계 그림 형식은 MinneApple 과 COCO 를 '
         '따랐습니다. 그 방법의 한계까지 문헌에 있고, 우리는 그 오차를 정답으로 실측했습니다.')
    d.qa('"내가 지금 당장 할 일은?"',
         '카톡 문구(문서/260812_카톡_근거조사답변.txt)를 복사해서 팀에 보내고, 확정본 md 와 bib '
         '두 파일을 첨부하는 것. 그다음 로컬 클로드에 2차 요청 붙여넣기. 그게 전부입니다.')

    d.save()


if __name__ == '__main__':
    build()
