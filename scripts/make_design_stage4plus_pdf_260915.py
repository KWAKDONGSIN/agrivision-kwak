# -*- coding: utf-8 -*-
"""교수님 실험 설계 4~7단계만 — 아주 쉽게 (핸드폰 PDF).

작성: 2026-09-15
사용:  ~/.conda/envs/kwak/bin/python tools/make_design_stage4plus_pdf_260915.py
출력:  문서/260915_실험설계_4장부터_아주쉽게.pdf
원칙:  한 쪽에 한 가지. «문제 → 해결법 → 작은 예 → 그래서» 순서. 실제 숫자는 파일럿0 v2 64런(09-15 집계).
       흐름판(260915_실험설계_흐름대로_공부용.pdf) 1~3장은 이해됐고 4~7장이 안 된다는 요청으로 만듦.
"""
import sys
from pathlib import Path
REPO = Path('/data/project/2026summer/kds0206/semantic-segmentation')
BASE = Path('/data/project/2026summer/kds0206')
sys.path.insert(0, str(REPO / 'tools'))
import pdfdoc  # noqa: E402
pdfdoc.PW, pdfdoc.PH = 4.4, 8.4
from pdfdoc import Doc  # noqa: E402

OUT = BASE / '문서' / '260915_실험설계_4장부터_아주쉽게.pdf'

d = Doc(OUT, '4단계부터, 아주 쉽게', subtitle='학습 규칙 → 예선 → 본실험 → 채점',
        footer='곽동신 · 2026-09-15 · 4~7단계', date='2026-09-15')
d.cover(lines=['흐름판의 4·5·6·7장만 다시 씀', '한 쪽에 한 가지 · 문제 → 해결법 → 예 → 그래서'], badge='쉬운판')


def pg(title, sub=''):
    d.pagebreak(); d.h1(title)
    if sub: d.p(sub)


# ─────────────────────────── 복습 ───────────────────────────
pg('먼저 3줄 복습', '1~3단계에서 한 일.')
d.steps([
    '겨룰 팀을 정했다. 헤더 12 × 백본 12 = 144팀. (1단계)',
    '사진을 20 대 80 으로 한 번만 잘랐다. 20% = 연습장, 80% = 봉인한 진짜 시험지. (2단계)',
    '연습장 20% 를 4조각으로 나눴다. 학습 2 · 모의고사(검증) 1 · 수능(시험) 1, 돌아가며 4번. (3단계)',
])
d.p('이제 연습장에서 할 일이 둘 남았습니다. «공부 기간 정하기»(4단계)와 «예선»(5단계). 그다음 봉인을 뜯어 «본실험»(6단계)을 하고, 처음부터 끝까지 «채점법»(7단계)은 하나로 고정합니다.')
d.table(['단계', '한 줄', '어디서'], [
    ['4', '며칠 공부시킬지 정한다', '연습장 20%'],
    ['5', '예선을 치러 결선팀을 뽑는다', '연습장 20%'],
    ['6', '진짜 시험을 본다', '시험지 80%'],
    ['7', '점수 매기는 법', '전부'],
], [0.12, 0.56, 0.32], size=8.5)

# ═══════════════════════════ 4단계 ═══════════════════════════
d.pagebreak(); d.chapter(4, '며칠 공부시킬까', '학습 규칙 정하기 (C1 → 파일럿0)')

pg('4-1. 문제', '144팀 전원에게 «같은 공부 기간»을 줘야 공정합니다. 그럼 며칠이 맞을까요?')
d.table(['공부 기간이', '무슨 일이 생기나'], [
    ['너무 짧으면', '늦게 실력이 트이는 팀이 억울하게 진다'],
    ['너무 길면', '144팀 × 16번 학습이라 시간이 어마어마하게 든다'],
], [0.3, 0.7], size=8.5)
d.p('«공부 하루» = epoch(에폭). 교과서(학습 사진)를 처음부터 끝까지 한 번 보는 것. 800 epoch = 교과서 800번 정독.')
d.tip('144팀 전원을 돌리기 «전에» 답을 알아야 합니다. 그래서 대표 4팀으로 먼저 실험합니다. 4팀 = 헤더 2종 × 백본 2종.', head='해결법')

pg('4-2. 실험 1 = C1', '질문: «일찍 그만두게 하면 손해 보나?»')
d.p('대표 4팀에게 300일 공부를 시키며 «매일» 모의고사 점수를 적었습니다. 4팀 × 과일 4 × 3조각 = 48번.')
d.p('그 점수표 위에 «그만두기 규칙» 두 개를 대 봅니다.')
d.table(['규칙', '내용'], [
    ['성급한 규칙', '40일 지나고, 10일 동안 점수가 안 오르면 그만'],
    ['느긋한 규칙', '80일 지나고, 40일 동안 점수가 안 오르면 그만'],
], [0.28, 0.72], size=8.5)
d.h2('작은 예 — 복숭아 한 팀의 점수표')
d.table(['날', '모의고사 점수', '성급한 규칙'], [
    ['30일', '0.30', ''],
    ['42일', '0.32', '← 여기서 그만'],
    ['100일', '0.33', ''],
    ['175일', '0.92', '실제로는 여기서 확 오름'],
], [0.2, 0.36, 0.44], size=8.5)
d.p('이 팀은 성급한 규칙이면 42일에 끝나서 0.6 을 놓칩니다. (실제 값: 최악 손해 0.604)')

pg('4-3. C1 의 결론', '48번 중 대부분은 일찍 그만둬도 손해가 거의 없었습니다.')
d.table(['본 것', '값'], [
    ['손해(놓친 점수)의 중앙값', '0.0085 (거의 0)'],
    ['최악', '0.604 (복숭아)'],
    ['과일별 중앙값', '포도 0.003 · 사과 0.007 · 블루베리 0.017 · 복숭아 0.030'],
], [0.4, 0.6], size=8.3)
d.p('읽는 법: 데이터가 적은 과일(복숭아)일수록 «늦게 트이는» 팀이 있다. 일찍 끊으면 그 팀이 억울해진다.')
d.warn('교수님은 «300일로는 다 못 봤다»고 판단해 규칙을 확정하지 않고 더 긴 실험을 추가하셨습니다. C1 은 옛 3조각 분할이라 지금은 참고 기록입니다. (2026-09-07 완료)', head='그래서')

pg('4-4. 실험 2 = 파일럿0', '질문: «며칠이면 충분한가?»')
d.p('대표 4팀에게 이번엔 800일 공부. 도중에 안 멈춥니다. 4팀 × 과일 4 × 4회전 = 64번.')
d.h2('핵심 장치 — 답안지 보관')
d.p('100일, 200일, … 800일마다 «지금까지 모의고사 최고였던 날의 답안지(모델)»를 따로 보관합니다.')
d.table(['보관함', '들어 있는 것'], [
    ['100일 상자', '1~100일 중 모의고사 최고였던 날의 모델'],
    ['200일 상자', '1~200일 중 최고'],
    ['400일 상자', '1~400일 중 최고'],
    ['800일 상자', '1~800일 중 최고'],
], [0.3, 0.7], size=8.5)
d.p('이 상자들을 나중에 수능(시험 조각)으로 채점하면 «100일 공부시킨 답안지 vs 800일 공부시킨 답안지»를 비교할 수 있습니다.')

pg('4-5. 작은 예 — 상자 채점', '한 팀·한 과일에서 이렇게 나온다고 합시다.')
d.table(['상자', '수능 점수', '앞 상자보다'], [
    ['100일', '0.930', ''],
    ['200일', '0.940', '+0.010'],
    ['400일', '0.945', '+0.005'],
    ['800일', '0.945', '0'],
], [0.3, 0.35, 0.35], size=8.5)
d.p('이 팀은 400일 뒤로는 하나도 안 올랐습니다. 그러면 «400일이면 충분»입니다. 800일 상자 점수 − 100일 상자 점수 = +0.015 를 «100→800 이득»이라고 부릅니다.')
d.p('64번 전부에 이 계산을 하고, 과일별로 16번의 중앙값을 보면 실제 답이 나옵니다.')

pg('4-6. 실제 결과', '2026-09-15 64/64 완주. 센터크롭 채점 기준. 괄호는 16번 중 최댓값.')
d.table(['과일', '100→800 이득', '400→800 이득'], [
    ['포도', '+0.003 (+0.018)', '0.000 (+0.008)'],
    ['블루베리', '+0.012 (+0.060)', '0.000 (+0.011)'],
    ['사과', '+0.004 (+0.241)', '0.000 (+0.003)'],
    ['복숭아', '+0.012 (+0.458)', '0.000 (+0.013)'],
], [0.28, 0.36, 0.36], size=8.5)
d.h2('읽는 법 (세 문장)')
d.bullets([
    '400일 넘게 시켜도 중앙값으로는 하나도 안 오른다 (0.000).',
    '100일에서 800일로 늘려도 중앙값은 +0.003~+0.012 로 작다.',
    '하지만 사과·복숭아엔 늦게 트인 팀이 하나씩 있다 (최댓값 +0.24, +0.46). 그래서 «100일이면 충분»이라고 단정하지 않는다.',
])
d.note('09-11 첫 판은 «상자 보관»이 없어서 09-13 에 64번 전부 다시 돌렸습니다. 첫 판 숫자는 쓰지 않습니다.', head='알아 둘 것')

pg('4-7. 남은 일과 말할 문장', '4단계는 여기까지 왔습니다.')
d.steps([
    '보관한 상자들을 진짜 채점법(7단계, 사진 전체 훑기)으로 다시 채점한다. ← 아직',
    '교수님이 «N일 + 그만두기 규칙»을 확정한다. ← 아직',
    '규칙을 잠근다. 그다음 5단계.',
])
d.tip('① "예선 전에 공부 기간을 정해야 해서, 대표 4팀으로 먼저 실험했습니다."\n② "C1 에서는 일찍 그만두면 복숭아가 0.6 이나 손해 보는 경우가 나왔습니다."\n③ "그래서 파일럿0 에서 800일 끝까지 시키며 100일마다 최고 답안지를 보관했고, 400일 이후엔 중앙값으로 안 오른다는 결과가 오늘 나왔습니다. 확정은 슬라이딩 채점 뒤 교수님이 하십니다."', head='내가 말할 문장 3개')

# ═══════════════════════════ 5단계 ═══════════════════════════
d.pagebreak(); d.chapter(5, '결선팀 뽑기', '예선 P')

pg('5-1. 문제', '144팀 전원을 진짜 시험지에서 다섯 번씩 보게 하면?')
d.table(['계산', '값'], [
    ['144팀 × 시험 5장 × 과일 4', '2,880번 학습'],
], [0.6, 0.4], size=8.5)
d.p('너무 많습니다. 그래서 연습장(20%)에서 «예선»을 치러 결선팀만 추립니다. 결선팀은 나중에 5장 다 보고, 나머지는 1장만 봅니다(6단계).')
d.tip('예선 = 144팀 전원이 연습장에서 4회전씩. 144 × 과일 4 × 4회전 = 2,304번. 공부 기간은 4단계에서 잠근 것 그대로.', head='해결법')

pg('5-2. 한 팀의 점수는 이렇게', '예: «UPerNet + ResNet-50» 팀.')
d.table(['과일', '4회전 수능 점수', '평균'], [
    ['포도', '0.94 0.95 0.95 0.96', '0.950'],
    ['블루베리', '0.92 0.93 0.91 0.94', '0.925'],
    ['사과', '0.82 0.84 0.83 0.85', '0.835'],
    ['복숭아', '0.90 0.92 0.88 0.94', '0.910'],
], [0.22, 0.5, 0.28], size=8.3)
d.p('최종 = (0.950 + 0.925 + 0.835 + 0.910) ÷ 4 = 0.905')
d.h2('왜 «÷ 4» 인가')
d.p('포도는 2,502장, 복숭아는 125장. 사진 수대로 평균하면 포도가 점수를 거의 다 정합니다. 그래서 과일 4개를 «같은 무게»로 평균합니다. 한 과일 = 한 표.')
d.p('속도·메모리 같은 «효율»은 점수에 안 넣습니다. 따로 재서 보고만 합니다.')

pg('5-3. 결선팀 고르기 = A ∪ B', '144팀 점수를 헤더 12줄 × 백본 12칸 표에 적습니다.')
d.h2('3×3 으로 줄인 예')
d.table(['', '백본 x', '백본 y', '백본 z'], [
    ['헤더 1', '0.90', '0.85', '0.80'],
    ['헤더 2', '0.70', '0.92', '0.60'],
    ['헤더 3', '0.88', '0.75', '0.89'],
], [0.22, 0.26, 0.26, 0.26], size=9)
d.steps([
    'A = 각 «줄»(헤더)에서 1등 칸. 헤더1→x(0.90), 헤더2→y(0.92), 헤더3→z(0.89)',
    'B = 각 «칸»(백본)에서 1등 줄. x칸→헤더1(0.90), y칸→헤더2(0.92), z칸→헤더3(0.89)',
    'C = A 와 B 를 합친다. 겹치면 한 번만. 여기선 {헤더1+x, 헤더2+y, 헤더3+z} = 3팀',
])
d.p('실제 12×12 에서는 A 12개 + B 12개, 겹치는 걸 빼면 12~23팀. 동점이면 이름 순.')

pg('5-4. 왜 «상위 몇 팀»이 아니라 A∪B 인가')
d.h2('상위 몇 팀으로 뽑으면')
d.p('잘하는 백본 하나가 12개 헤더와 짝지어 상위권을 독차지할 수 있습니다. 그러면 결선에 백본이 하나뿐이 됩니다.')
d.h2('A∪B 로 뽑으면')
d.p('헤더 12개 각각의 «최고 짝»이 들어가고(A), 백본 12개 각각의 «최고 짝»도 들어갑니다(B). 헤더 축·백본 축 두 쪽 대표가 빠짐없이 결선에 갑니다.')
d.warn('C 를 줄이는 이유를 «통계 검출력 때문»이라고 말하면 안 됩니다. 바른 이유: «계산량, 헤더·백본 두 축의 대표성, 설명의 단순성».', head='금지어')

pg('5-5. 잠금과 말할 문장')
d.p('예선이 끝나면 결선팀 C 를 잠급니다. 본실험(6단계) 결과를 보고 «저 팀도 넣자, 이 팀은 빼자» 하는 건 금지. 코드·가중치·설정·분할의 지문(hash)도 이때 기록합니다.')
d.p('상태: 아직 안 함. 4단계 규칙이 잠기면 시작.')
d.tip('① "예선은 144팀 전부를 20% 연습장에서 4회전씩, 2,304런입니다."\n② "점수는 과일별 4회전 평균을 다시 4과일 같은 무게로 평균한 것이고, 효율은 안 넣습니다."\n③ "헤더별 1등 12개와 백본별 1등 12개를 합친 12~23팀이 결선팀 C 이고, 여기서 잠급니다."', head='내가 말할 문장 3개')

# ═══════════════════════════ 6단계 ═══════════════════════════
d.pagebreak(); d.chapter(6, '진짜 시험', '본실험 M1~M5')

pg('6-1. 봉인을 뜯는다', '80% 시험지를 M1·M2·M3·M4·M5 다섯 조각으로 나눕니다.')
d.table(['과일', '80% 전체', '한 조각'], [
    ['블루베리', '956', '191'],
    ['사과', '801', '160'],
    ['복숭아', '100', '20'],
    ['포도', '2,002', '400'],
], [0.34, 0.33, 0.33], size=8.5)
d.p('시험은 두 번 봅니다. 시험 1 은 전원, 시험 2 는 결선팀만.')

pg('6-2. 시험 1 = M1', '144팀 전원, 같은 시험지 한 장.')
d.p('M2+M3+M4+M5 로 공부하고 M1 으로 시험. 144 × 과일 4 = 576번. 여기 1등 = «전체 우승자(Full-grid winner)».')
d.h2('왜 전원을 한 번은 재나')
d.p('144팀을 «같은 시험지»로 재야 이 두 질문에 답할 수 있어서입니다.')
d.table(['질문', '쉽게'], [
    ['주효과', '백본만 바꾸면 헤더가 뭐든 점수가 같이 오르나? (좋은 몸통은 어느 머리에 붙여도 좋은가)'],
    ['상호작용', '특정 머리 + 특정 몸통 «짝»일 때만 유독 잘하나?'],
], [0.24, 0.76], size=8.3)
d.h2('작은 예')
d.table(['', '백본 x', '백본 y'], [
    ['헤더 1', '0.80', '0.90'],
    ['헤더 2', '0.82', '0.92'],
], [0.3, 0.35, 0.35], size=9)
d.p('y 가 x 보다 어느 헤더에서나 +0.10 → 주효과(백본 y 가 그냥 좋다). 만약 헤더2+y 만 0.99 였다면 → 상호작용(그 짝이 특별하다).')

pg('6-3. 시험 2 = M2~M5', '결선팀 C 만 시험지 4장 더.')
d.p('C 에 든 팀만 M2, M3, M4, M5 를 차례로 시험지로 삼아 4번 더 공부·시험. 과일 4 × 조각 4 × |C| = 16 × |C| = 192~368번.')
d.p('그러면 C 팀은 다섯 조각 전부의 점수가 있습니다. 5장 평균 1등 = «최종 우승자(Cross-validated winner)» = 논문의 최종 추천.')
d.h2('작은 예 — 두 팀')
d.table(['팀', 'M1', 'M2', 'M3', 'M4', 'M5', '평균'], [
    ['갑', '0.93', '0.90', '0.91', '0.90', '0.91', '0.910'],
    ['을', '0.92', '0.92', '0.93', '0.92', '0.93', '0.924'],
], [0.12, 0.13, 0.13, 0.13, 0.13, 0.13, 0.23], size=8.3)
d.p('M1 한 장만 보면 갑이 1등. 다섯 장 평균은 을이 1등. 한 장엔 운이 섞이니 최종 추천은 을. 둘이 다르면 «왜 다른가»를 논문에 씁니다.')

pg('6-4. 왜 전원을 5장 다 안 보나 · 합계')
d.p('전원 5장 = 2,880번. 대신 «전원 1장 + 결선팀만 5장»으로 하면 576 + 192~368 = 768~944번. 이게 이 설계가 시간을 줄이는 방법입니다.')
d.table(['단계', '학습 횟수(런)'], [
    ['5 예선 P', '2,304'],
    ['6 시험 1 (M1)', '576'],
    ['6 시험 2 (M2~M5)', '192~368'],
    ['합계', '3,072~3,248'],
], [0.5, 0.5], size=8.5)
d.p('상태: 아직 안 함.')
d.tip('① "본실험은 80% 를 M1~M5 다섯 조각으로 나눕니다."\n② "M1 에서 144팀 전원이 같은 시험지를 한 번 보고, 여기 1등이 Full-grid winner 입니다. 전원을 같은 시험지로 재야 주효과와 상호작용을 볼 수 있습니다."\n③ "결선팀 C 만 나머지 4장을 더 봐서 5장 평균 1등이 Cross-validated winner 이고, 이게 논문의 최종 추천입니다."', head='내가 말할 문장 3개')

# ═══════════════════════════ 7단계 ═══════════════════════════
d.pagebreak(); d.chapter(7, '점수 매기는 법', '채점법')

pg('7-1. Dice = 겹친 만큼', '사람이 칠한 정답과 모델이 칠한 영역이 얼마나 겹치나.')
d.table(['예', '픽셀'], [
    ['정답이 칠한 곳', '100'],
    ['모델이 칠한 곳', '90'],
    ['둘이 겹친 곳', '80'],
], [0.5, 0.5], size=8.5)
d.p('Dice = 2 × 80 ÷ (100 + 90) = 0.84')
d.p('완전히 같으면 1, 하나도 안 겹치면 0. 사진마다 구하고 사진 단위로 평균합니다. 큰 사진 하나가 점수를 독차지하지 않게.')

pg('7-2. 큰 사진, 작은 창', '모델은 한 번에 512×512 조각만 봅니다. 사진은 1920×1080.')
d.table(['방법', '어떻게', '어디에'], [
    ['센터크롭', '사진 가운데 512×512 하나만 잘라 채점', '학습 중 모의고사. 빠름'],
    ['슬라이딩 윈도우', '512 창을 384씩 옮기며 사진 전체를 훑어 채점. 겹친 곳은 합침', '최종 시험 점수'],
], [0.24, 0.46, 0.3], size=8)
d.h2('왜 최종은 슬라이딩인가')
d.p('센터크롭은 가운데 한 조각만 보니 과실의 75~77% 를 아예 못 봅니다(실측). 가운데만 보고 준 점수는 진짜 점수가 아닙니다.')
d.p('4단계 «실제 결과» 표가 센터크롭 값이라, «최종 판정은 슬라이딩으로 다시 채점한 뒤»라고 말하는 이유가 이것입니다.')

pg('7-3. 전 팀 같은 조건 · 말할 문장')
d.bullets([
    '«과일이다» 판정 기준 확률 0.5 고정.',
    '뒤처리 없음.',
    '시험 때 사진을 뒤집어 여러 번 예측하는 꼼수(TTA) 없음.',
    '144팀 전부 똑같이.',
])
d.p('상태: 규칙은 정해졌고 슬라이딩 채점 코드도 있습니다. 파일럿0 상자들에 적용하는 게 다음 일.')
d.tip('① "점수는 Dice, 정답과 겹친 만큼입니다. 사진마다 구해 평균합니다."\n② "최종 채점은 512 창을 384씩 옮기며 사진 전체를 훑는 슬라이딩 윈도우입니다."\n③ "센터크롭은 과실의 75% 이상을 못 봐서 학습 중 검증에만 씁니다."', head='내가 말할 문장 3개')

# ─────────────────────────── 한 장 요약 ───────────────────────────
pg('4~7단계 한 장 요약')
d.table(['단계', '문제', '해결법', '지금'], [
    ['4', '며칠 공부시킬까', '대표 4팀. C1(300일, 일찍 끊으면 손해?) → 파일럿0(800일, 100일마다 상자)', '64런 완주, 판정 대기'],
    ['5', '144팀 다 5장은 너무 많다', '연습장 예선 2,304런 → A∪B 12~23팀', '앞'],
    ['6', '진짜 1등은?', 'M1 전원 1장(576) + C 만 4장 더(192~368)', '앞'],
    ['7', '어떻게 채점?', 'Dice · 슬라이딩 윈도우 · 0.5 · 전 팀 동일', '규칙 확정'],
], [0.1, 0.24, 0.42, 0.24], size=7.8)
d.h2('4~7단계 대본 (2분 30초)')
d.p('"예선 전에 공부 기간부터 정합니다. 대표 4팀으로 C1 에서 일찍 끊으면 손해 보는지 봤더니 복숭아에서 0.6 손해가 나왔고, 그래서 파일럿0 에서 800 epoch 끝까지 시키며 100 마다 최고 답안지를 보관했습니다. 오늘 64런이 끝났고 중앙값으로는 400 이후 안 오릅니다. 확정은 슬라이딩 채점 뒤 교수님이 하십니다."')
d.p('"규칙이 잠기면 예선입니다. 144팀 전부 20% 에서 4회전씩 2,304런. 과일별 평균을 4과일 같은 무게로 합치고, 헤더별 1등과 백본별 1등을 합친 12~23팀이 결선팀 C 입니다. 여기서 잠급니다."')
d.p('"본실험은 80% 를 M1~M5 로 나눕니다. M1 에서 전원이 한 번, 576런, 1등이 Full-grid winner. C 만 4장 더 봐서 5장 평균 1등이 Cross-validated winner, 논문의 최종 추천입니다. 합계 3,072~3,248런."')
d.p('"채점은 Dice, 최종은 512 창을 384씩 옮기는 슬라이딩 윈도우입니다. 센터크롭은 과실의 75% 이상을 못 봐서 학습 중 검증에만 씁니다."')
d.save()
print(OUT)
