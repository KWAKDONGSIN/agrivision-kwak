# -*- coding: utf-8 -*-
"""0915 랩미팅 후속 — 복숭아·포도 데이터 검수 최종 보고서 PDF. 작성: 2026-09-16
실행: /home/kds0206/.conda/envs/kwak/bin/python tools/make_data_review_report_260916.py
출력: 문서/260916_데이터검수_최종보고서.pdf
"""
import csv, json, sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent))
from pdfdoc import Doc

K = Path('/data/project/2026summer/kds0206')
T = Path('/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴')
R2, R3, F = T / 'inspect/round2', T / 'inspect/round3', T / 'final'
REV = K / 'datasets_reviewed_260916'
OUT = K / '문서/260916_데이터검수_최종보고서.pdf'

pm = list(csv.DictReader(open(REV / 'peach/manifest.csv', encoding='utf-8')))
gm = list(csv.DictReader(open(REV / 'grape/manifest.csv', encoding='utf-8')))
fixlog = list(csv.DictReader(open(R3 / 'peach_fix_log.csv')))
gfix = json.load(open(F / 'grape_image_fix/fix_records.json'))
gfix_scores = list(csv.DictReader(open(F / 'grape_image_fix/proposal_scores.csv')))
dups = json.load(open(T / 'data/grape/duplicates.json'))['groups']
leak = json.load(open(R2 / 'round2_leakage_summary.json')) if (R2 / 'round2_leakage_summary.json').exists() else {}
acc = json.load(open(R3 / 'round3_peach_accept.json'))

d = Doc(OUT, title='복숭아·포도 데이터 검수 최종 보고서', subtitle='0915 랩미팅 지시 처리 결과 — 뺀 사진·고친 마스크·새 폴더 전부', date='2026-09-16')
# 최종 수정: 2026-09-16 밤 (§10 사이클1 + 사이클3 문서 대조로 본문 숫자를 현행값으로 통일)
d.cover(lines=['담당: 곽동신 (세그멘테이션 팀 = 곽동신·최인훈)', '검수 3회: Opus 5 → Opus 5 → Fable 5.1 (최종 판정·실행)',
               '원본 datasets_resized_2mp 는 무변경 · 결과는 datasets_reviewed_260916 새 폴더'], badge='최종')

d.h1('0. 한눈에 보기')
d.p('교수님이 0915 랩미팅에서 시키신 것은 세 가지였습니다. ① 라벨링 툴을 웹사이트처럼 만들어라 ② 세그멘테이션 데이터를 검수한 뒤에 실험해라 ③ 영상에서 뽑은 거의 같은 사진을 골라내라. 우리 팀 담당은 **복숭아·포도** 이므로 이 둘을 끝까지 했고, 사과·블루베리는 교수님 지적이 실측에서 보이는지만 확인했습니다.')
d.table(['항목', '복숭아', '포도'], [
    ['원본 장수', '125', '2,502'],
    ['새 폴더 장수', '125', '2,406 (파일) / **2,403** (실제 사용)'],
    ['뺀 사진', '0', '**99** (거의 같은 사진 95 + 사이클1 추가 2 + 포도 아닌 사진 1)'],
    ['마스크 고친 사진', '**39장** (라벨 안 된 복숭아 83개 추가)', '0 (자동으로 못 고침, 아래 §5-4)'],
    ['사진을 바꾼 것', '0', '**12장** (사진↔마스크가 서로 다른 장면이던 변환 오류)'],
    ['사람이 툴에서 더 볼 것(flag)', '**29장** (누락 의심 8 + 윤곽 조정 21)', '72장'],
    ['새 폴더', '`datasets_reviewed_260916/peach/`', '`datasets_reviewed_260916/grape/`'],
], [.3, .35, .35])
d.note('위 표는 **사이클1 재검수까지 반영한 현행 숫자**입니다(2026-09-16 밤). 이전 판에 있던 «85개·41장·96장·flag 8» 은 사이클1 이전 값이며, 무엇이 왜 바뀌었는지는 §10 에 있습니다.', head='현행 숫자')
d.note('새 폴더 경로: **/data/project/2026summer/kds0206/datasets_reviewed_260916/** — 원본을 «복사»해서 만든 실제 파일(링크 아님)입니다. 원본 `datasets_resized_2mp/` 는 한 글자도 안 바꿨습니다. 사진마다 무엇을 했는지는 각 과일 폴더의 `manifest.csv` 에 한 줄씩 있습니다.')
d.p('라벨링 툴은 **http://<서버주소>:5111** 에 켜 두었습니다(지금은 새 폴더를 보고 있음). 사용법은 `platform/work/kwak_dongsin/260916_라벨링툴/app/README.md`.')

d.h1('1. 어떻게 검수했나 (3회)')
d.p('같은 데이터를 세 번, 서로 다른 AI 가 독립적으로 봤습니다. 앞 사람의 결론을 믿지 않고 사진을 다시 열어 판정을 대조하는 방식입니다.')
d.table(['회차', '누가', '무엇을', '결과물'], [
    ['1차', 'Opus 5', '4과일 4,823장 전수 형태 측정 · 복숭아 125장 육안 · 포도 의심 100장 · 근접 중복 탐지', '`inspect/260916_데이터검수_보고서.md`'],
    ['2차', 'Opus 5 (다른 세션)', '복숭아 125장 전부 다시 육안(4칸 시트) · 포도 중복 60쌍 전부 + 의심 148장 + 무작위 69장 · 완화 임계값 재실행 · 폴드 누수 점검', '`inspect/round2/260916_2차검수_보고서.md` + 판정표 CSV 4개'],
    ['3차', 'Fable 5.1 (이 문서)', '1·2차가 갈린 사진 직접 판정 · 복숭아 후보 139개를 하나씩 보고 채택/기각 · 포도 12장 불일치 원인 규명(원본 zip 대조) · 새 폴더 생성 · 툴 반영', '`inspect/round3/`, `final/`, 이 PDF'],
], [.08, .17, .5, .25])
d.p('1차와 2차가 갈린 복숭아는 24장이었습니다(1차만 지적 10장, 2차만 지적 14장). 3차에서 24장을 포함해 후보가 있는 49장을 전부 다시 봤습니다. 1차가 «5개 이상 누락»이라 한 `t4-20` 은 실제로 2개였고, 1차가 «2~3개 누락»이라 한 `t3-17` 은 1개였습니다. 2차가 «누락»으로 본 것 중 AI 후보가 없어 자동으로 못 고친 8장은 flag 로 남겼습니다.')
d.warn('AI 가 판정한 것이므로 **곽동신·최인훈이 툴에서 한 번 훑어보는 것**을 전제로 합니다. 특히 «잎에 가려진 부분까지 칠할 것인가»(§4-3)와 «포도 송이 vs 알»(§5-4)은 교수님 결정이 필요합니다.')

d.h1('2. 라벨링 툴 (교수님 지시 ①)')
d.p('Flask 로 만든 웹 툴입니다. 팀원 4명이 같은 주소로 들어가 과일을 고르고, 사진마다 **원본 마스크(빨강) / AI 제안(파랑) / 내 수정본(초록)** 세 겹을 켜고 끄며 브러시·지우개·다각형·«스마트 채우기»(클릭한 AI 덩어리 통째로 넣기/빼기)로 고칩니다. 되돌리기 20단계, 단축키로 이전/다음 사진. 저장하면 `data/<과일>/masks_fixed/` 에만 쓰고 원본은 절대 안 건드립니다.')
d.bullets(['**판정 버튼 5개**: 원본 그대로 OK / AI 제안으로 교체 / 수정본 저장 / 문제 있음(flag) / 제외(exclude)',
           '**우선순위 정렬**: AI 제안과 원본이 많이 다른 사진부터(라벨 누락 후보). 중복 그룹·의심 플래그 필터',
           '**중복 일괄 제외** 버튼 + `excluded_list.txt` 내보내기 + 삭제 스크립트 생성기(실행은 사람이)',
           '**사과는 알마다 다른 색**으로 표시(인스턴스 라벨). 교수님이 보신 «동그랗게 채워진 한 덩어리»가 이진화 표시 문제였기 때문(§6)',
           '**AI 초벌**: 파일럿0 체크포인트(BackboneUNet×MiT-B2)로 4과일 4,823장 전체영상 슬라이딩 추론. 누수 없음(파일럿 사진은 그 사진이 시험 fold 였던 회전만 사용, main 사진은 파일럿0 가 한 장도 안 봄)',
           '내보내기 `export/export_dataset.py` — 툴에서 고친 것을 모아 새 폴더 생성(중복 자동 제외). 재학습 절차 `ai_pass/retrain_loop.md`'])
d.p('경로: `platform/work/kwak_dongsin/260916_라벨링툴/app/` (server.py, static/, run.sh, README.md). 켜기 `bash app/run.sh`, 끄기 `bash app/run.sh stop`.')

d.pagebreak(); d.h1('3. 복숭아 — 무엇이 문제였고 무엇을 했나')
d.h2('3-1. 마스크는 원본 COCO 정답과 같다 → 누락은 원본 주석 자체의 누락')
d.p('125장 마스크를 원논문 인스턴스 정답(폴리곤 977개)과 겹쳐 보니 IoU 중앙 0.986, 빠진 정답 개체 0개였습니다. 즉 우리 변환은 정확하고, 인훈 님이 0908에 발견한 «보이는데 라벨 없는 복숭아»는 **원본 주석의 누락**입니다. 정답 파일과 대조해서는 못 찾고 사진을 눈으로 봐야 합니다.')
d.h2('3-2. 라벨 안 된 복숭아 83개를 39장에 추가했습니다')
d.p('AI 제안에서 «원본에 없고 AI 만 잡은» 덩어리(면적 800px 이상, 채움도 0.55 이상)를 후보로 뽑아 번호를 붙이고, 3차에서 139개를 하나씩 보고 **복숭아가 분명한 85개를 채택**했고, 뒤이은 전수 재판정(사이클1, §10)에서 오검출 1개와 경계 번짐 1개를 빼 **최종 83개**가 되었습니다. 기각한 것: 땅에 떨어진 복숭아, 뒷줄 먼 나무의 것, 사람 얼굴·신발(`t4-20`), 주황 바구니(`t3-09`, `t3-10`), 잎 반사, 가장자리 불명확. 채택한 덩어리는 구멍을 메운 뒤 원본 마스크에 합쳤습니다(원본 화소는 하나도 안 지움).')
d.fig(F / '../inspect/round3/peach_fix_figs/210629-t4-17.png', height=0.30, caption='t4-17 — 왼쪽: 원본 라벨(하늘색 4개) / 오른쪽: 추가한 복숭아(빨강 10개). 이 데이터셋에서 누락이 가장 심한 사진')
d.fig(F / '../inspect/round3/peach_fix_figs/210629-t4-18.png', height=0.30, caption='t4-18 — 8개 추가')
rows = [[r['stem'].replace('210629-', ''), r['n_added'], r['comp_idx'], f"{int(r['gt_px']):,} → {int(r['new_px']):,}"] for r in fixlog]
d.table(['사진', '추가 개수', '채택한 후보 번호', '마스크 화소(전 → 후)'], rows, [.25, .15, .3, .3], size=8.2)
d.p('전후 그림: `inspect/round3/peach_fix_figs/<사진>.png` (41장이 남아 있는데 그중 `t2-of15-04`·`t3-13` 두 장은 되돌린 것이라 현행이 아닙니다 — 현행은 위 표의 39장). 후보 번호가 찍힌 판정 시트: `inspect/round3/peach_cand/`. 판정 원본: `inspect/round3/round3_peach_accept.json`, 최종판 `round3_peach_accept_v2.json`.')
d.h2('3-3. 사람이 더 볼 것 — 누락 의심 8장 (manifest needs_human)')
flags = [r for r in pm if r['action'] == 'needs_human']
d.table(['사진', '왜'], [[r['stem'].replace('210629-', ''), r['note'].replace('2차가 누락 지적했으나 AI 후보가 없거나 불명확 → 사람이 툴에서 확인: ', '')] for r in flags], [.25, .75], size=8.5)
d.h2('3-4. 잎에 가려진 부분까지 동그랗게 칠한 라벨 — 고치지 않았습니다 (교수님 결정 필요)')
d.p('복숭아 라벨은 «보이는 부분»이 아니라 **가려진 부분까지 포함한 통짜 원**(amodal)으로 그려져 있습니다(약 40장에서 뚜렷). 이건 틀린 게 아니라 «어떤 규칙으로 그렸는가»의 문제이고, 바꾸면 4과일 마스크를 전부 다시 그려야 하므로 손대지 않았습니다. 세그멘테이션은 보통 보이는 화소를 맞히는 과제라 잎을 복숭아로 답해야 점수를 받는 상황이 됩니다.')
d.fig(F / 'figs/peach_amodal_t2-of15-01.png', height=0.22, caption='t2-of15-01 — 원본 | 라벨(하늘): 잎 위까지 완전한 원 | AI 제안(빨강): 보이는 부분만')
d.h2('3-5. 복숭아 근접 중복: 없음')
d.p('연속 촬영처럼 보이는 파일명끼리도 상관계수 최대 0.68(기준 0.90)이라 서로 다른 사진입니다. 뺀 복숭아 사진은 0장입니다.')

d.pagebreak(); d.h1('4. 포도 — 무엇이 문제였고 무엇을 했나')
d.h2('4-1. 사진과 마스크가 서로 다른 장면이던 12장 — 사진을 올바른 것으로 바꿨습니다')
d.p('AI 제안과 원본 마스크가 거의 안 겹치는 사진(Dice 0.06~0.38)을 열어 보니 마스크가 송이와 전혀 다른 자리에 있었습니다. 원본 CERTH zip 을 직접 대조한 결과 **우리 변환 스크립트의 버그**였습니다. zip 안에 같은 파일명(예 `104.png`)이 두 폴더(`single-instance-one-class/` 와 `multiple-instance-multiple-class/test_set/`)에 있는데, `tools/prepare_certh_grape.py` 가 파일명으로만 찾아 **먼저 나온 다른 사진**을 가져오고 마스크는 test_set 주석으로 만들었습니다. 15개 파일명이 겹쳤고 그중 3개(`2330`·`893`·`969`)는 두 사진이 같아 문제없고, **12장이 어긋났습니다.** 원본 CERTH 주석 자체는 정상입니다.')
d.fig(F / 'grape_image_fix/figs/1080_before_after.png', height=0.28, caption='1080 — 왼쪽: 잘못 짝지어진 사진 위에 마스크(하늘) / 오른쪽: 올바른 사진(test_set) 위에 같은 마스크')
d.p('교정: 올바른 사진을 원래 파이프라인과 같은 방식(긴 변 1920, BILINEAR)으로 리사이즈해 교체했습니다. 마스크는 원래 맞았으므로 그대로. 교정 후 AI 제안과의 Dice 가 전부 0.82 이상으로 올라 «이제 사진과 마스크가 같은 장면»임을 확인했습니다.')
d.table(['사진', '교정 전 Dice', '교정 후 Dice', '분할표 소속'], [[r['stem'], f"{float(r['dice_before_fix']):.2f}", f"{float(r['dice_after_fix']):.2f}", 'main(80%) fold ' + {'104':'1','1080':'5','1235':'3','1366':'4','1374':'2','1385':'1','198':'4','2109':'5','2112':'1','2201':'3','2241':'5','233':'3'}[r['stem']]] for r in gfix_scores], [.2, .25, .25, .3], size=8.5)
d.warn('12장 전부 본실험(main 80%) 풀이라 파일럿0(20%) 결과에는 영향이 없습니다. 그러나 **옛 포도 학습(0731 54조합, C1 등)은 이 12장을 잘못된 채로 썼습니다.** 2,502장 중 12장(0.5%)이라 순위가 뒤집힐 크기는 아니지만 논문 데이터 절에 «변환 오류 12장 교정» 한 줄을 넣을 재료입니다. 스크립트 수정 안: `ZipSource.find()` 를 폴더 경로까지 포함해 찾도록.')
d.h2('4-2. 거의 같은 사진 94그룹 192장 → 그룹마다 1장만 남기고 98장 뺐습니다 (해밍≤10 의 91그룹 95장 + 사이클1·4 의 해밍 11 3그룹 3장)')
d.p('사진 해시(dHash 64비트)와 축소 그레이 상관계수로 «거의 같은 프레임»을 찾았습니다. 1차 임계값(해밍≤6·상관≥0.95)은 60쌍, 2차가 완화한 임계값(해밍≤10·상관≥0.90)은 91그룹 186장이었고, 2차가 60쌍 전부 + 완화로 늘어난 34그룹 중 20그룹을 눈으로 확인해 오탐 0, 3차가 나머지 14그룹을 확인했습니다. **완전히 같은 파일은 0장**, 전부 «같은 촬영본을 살짝 다르게 확대·이동한 것»입니다. 파일 번호가 이어지지 않아(`1`–`8`, `1035`–`568`) 번호로는 못 거릅니다. 대표는 «더 넓게 찍힌 쪽»(2차 기준)을 남겼습니다.')
d.fig(T / 'inspect/gallery/grape/dup_examples.png', height=0.42, caption='근접 중복 예 4쌍 — 카메라가 아주 조금 움직인 같은 장면')
d.p('**폴드 누수**: 1차 60쌍 중 **50쌍(83%)이 학습/검증/시험 fold 를 가로질러** 들어가 있었습니다(`protocol_splits/v3_seed3407/grape.csv` 가 사진 1장 = 그룹 1개라 중복을 묶는 장치가 없음). 그룹당 1장만 남기면 누수는 사라집니다. 단, **분할표를 새 폴더 기준으로 다시 만들어야 합니다**(§8).')
rows = [[g[0], ', '.join(g[1:])] for g in dups]
d.table(['남긴 대표', '뺀 사진'], rows, [.25, .75], size=8.0)
d.h2('4-3. 포도 사진이 아닌 것 1장')
d.p('`1278` — 손에 든 표지판이 화면 대부분. 학습에 쓸 수 없어 뺐습니다.')
d.h2('4-4. 보이는 송이인데 라벨이 없는 사진 — 자동으로 못 고쳤습니다 (72장 flag)')
d.p('2차가 무작위 100장 중 69장을 열어 보니 58장(84%)에서 화면에 보이는 송이가 라벨에 없었습니다(대개 왼쪽·가장자리 송이). 3차가 무작위 시트를 다시 봐도 6장 중 4장이 그랬습니다. **AI 제안도 같은 라벨로 학습해서 같이 놓치므로** «AI 와 원본의 차이»로는 이 문제를 찾을 수 없고, 자동 추가도 불가능합니다. 라벨 단위가 «알»이 아니라 «송이»이고, 앞을 가로지르는 지주대·잎까지 송이에 포함돼 있는 것도 확인했습니다. 이 둘은 교수님 결정(§8) 뒤에 툴에서 사람이 고쳐야 합니다. 2차가 사진을 보고 «송이 누락/잎 위 채움/기타»로 판정한 72장은 툴에 flag 로 표시해 두었습니다.')
d.fig(R2 / 'grape_random_sheets/s000.png', height=0.55, caption='무작위 표본 예 — 가운데 칸이 원본 라벨(빨강). 1178 은 오른쪽 아래 송이, 306 은 왼쪽 위 송이가 라벨에 없음')
flags = [r for r in gm if r['action'] == 'needs_human']
d.p('flag 72장: ' + ', '.join(r['stem'] for r in flags))

d.pagebreak(); d.h1('5. 사과·블루베리 — 교수님 지적이 실측에서 보이는가 (요약)')
d.h2('5-1. 사과 «가운데가 비어야 하는데 동그랗게 채워진 라벨» = 표시 문제 + 라벨 규칙')
d.p('사과 마스크는 0/255 가 아니라 **알마다 번호가 들어간 인스턴스 라벨**(값 0~95)입니다. 세그멘테이션용으로 이진화하면 붙은 알들이 한 덩어리가 됩니다 — 정답 개체 40,468개가 덩어리 33,099개로(18.2% 병합, 1,001장 중 833장). 교수님이 보신 «동그랗게 채워진 한 덩어리»는 이것일 가능성이 큽니다. 라벨을 고칠 게 아니라 **표시** 문제라 툴에서 알마다 다른 색으로 그리게 했습니다. 다른 하나는 복숭아와 같은 «가려진 부분까지 채움» 규칙(개체 볼록도 중앙 0.925, 구멍 없는 개체 99.5%).')
d.fig(T / 'inspect/gallery/apple/zoom/dataset2_back_1080.png', height=0.26, caption='사과 4알이 각각 라벨돼 있지만 이진 마스크에서는 하트 모양 한 덩어리로 보임')
d.h2('5-2. 블루베리 «없는 것 추가» → 근거를 못 찾았습니다')
d.p('빈 마스크 0, 크기 불일치 0, 보이는 열매는 거의 다 촘촘히 라벨돼 있습니다. 색으로 «라벨 안 된 알»을 찾아보면 배경의 파란 그물망만 잡힙니다. 교수님이 보신 사진이 어느 것인지 확인이 필요합니다.')
d.h2('5-3. 연속 스냅샷은 블루베리가 아니라 사과·포도에 있습니다 (교수님 지시 ③)')
d.table(['', '블루베리', '사과', '포도', '복숭아'], [
    ['인접 프레임 쌍 상관 중앙', '**0.092**', '0.829', '0.110', '0.100'],
    ['거의 같은 프레임 그룹', '**0**', '47그룹 131장', '94그룹 192장 (해밍≤10 91그룹 + 해밍 11 쌍 3개, §10)', '0'],
    ['이유', '60프레임(약 2초) 간격이라 화면이 확 바뀜', '5프레임 간격', '같은 촬영본이 두 번 들어옴', '—'],
], [.28, .18, .18, .18, .18])
d.p('블루베리는 검출 팀(박성문·임성후) 담당이고 실측으로는 중복이 없어서 손대지 않았습니다. 사과 131장은 검출 팀이 쓸 때 참고하도록 `data/apple/duplicates.json` 에 남겨 두었습니다.')

d.h1('6. 새 폴더와 툴 반영')
d.bullets(['`datasets_reviewed_260916/peach/` — 125장(이미지 복사 125, 마스크: 고친 39 + 원본 86), `manifest.csv`',
           '`datasets_reviewed_260916/grape/` — 파일 2,406장·사용 2,403장(사진 교체 12, 뺀 99 중 96은 파일도 없고 `163`·`60`·`1191` 은 파일만 남음 → `grape/_to_delete_260916.txt`), `manifest.csv`',
           '툴 `data/peach/status.json`: fixed 18 · flag 30 · ok 77 (고친 39장 중 윤곽 조정이 필요한 21장은 fixed 가 아니라 flag) / `data/grape/status.json`: fixed 12 · flag 72 · exclude 99 · 나머지 unreviewed',
           '툴 `data/peach/masks_fixed/` 에 고친 39장, `data/grape/duplicates.json` 을 완화판으로 교체(당시 91그룹 → 사이클1·4 로 현행 94그룹 192장)(1차판은 `duplicates_round1.json`)',
           '툴 서버는 새 폴더를 보도록 재기동(`LABELTOOL_DATA_ROOT=…/datasets_reviewed_260916`). 원본으로 되돌리려면 `bash app/run.sh restart`'])
d.p('manifest.csv 의 action 값: keep / mask_fixed / image_replaced / needs_human / excluded_duplicate / excluded_not_grape. 뺀 사진은 새 폴더에 파일이 없고 manifest 에만 남습니다.')

d.h1('7. 남은 일 · 교수님께 여쭐 것')
d.bullets(['**결정 1** 가려진 부분까지 칠할 것인가(amodal) vs 보이는 것만 — 지금 4과일 전부 «포함». 바꾸면 전부 재라벨',
           '**결정 2** 라벨 범위 — 땅에 떨어진 것·뒷줄 나무·연두색 미숙과를 라벨할 것인가(복숭아 원본은 안 함, 일관성 없음)',
           '**결정 3** 포도 라벨 단위 — 송이 그대로 vs 알 단위. 송이라면 앞을 가리는 지주대·잎은 뺄 것인가',
           '**결정 4** 블루베리 «없는 것» — 교수님이 보신 사진 특정',
           '(사람) 툴에서 복숭아 flag 30장(누락 의심 8 + 윤곽 조정 21) · 포도 flag 72장 확인, 포도 «송이 누락» 은 결정 3 뒤 재라벨',
           '(끝남) 분할표는 v4 `final/round_d/protocol_splits_v4_reviewed/grape.csv` 가 현행(2,403행, 뺀 99장 제거). 옛 `protocol_splits/v3_seed3407/grape.csv`(2,502행)로 포도 실험을 돌리면 안 됨',
           '(코드) `tools/prepare_certh_grape.py` 의 파일명 충돌 버그 수정',
           '(논문) 데이터 절에 «복숭아 라벨 누락 83개 보정(개체 977 → 1,060) · 포도 변환 오류 12장 교정 · 근접 중복 98장 + 포도 아닌 사진 1장 제거(사용 2,403장·송이 9,424)» 기재'])

d.h1('8. 가정과 한계 (솔직하게)')
d.bullets(['판정은 전부 AI 가 축소 시트를 보고 한 것입니다. 복숭아 개수는 ±1 오차가 있을 수 있고, 가장자리·그늘의 작은 후보는 **기각(사람 확인)** 쪽으로 보수적으로 판정했습니다.',
           '복숭아 추가 마스크의 윤곽은 AI 제안 그대로입니다. 전수 재판정(§10) 결과 83개 중 54개만 «보이는 과실» 모양이고 29개는 윤곽이 잎까지 덮습니다. 원본의 amodal 규칙과 섞인 상태이므로 결정 1 뒤에 통일이 필요합니다.',
           '근접 중복 임계값(해밍≤10·상관≥0.90)은 2차가 분포를 보고 권고한 값입니다. `inspect/round2/find_near_duplicates_loose.py --ham --corr` 로 바꿔 재실행할 수 있습니다.',
           '포도 «송이 누락» 비율(84%)은 무작위 69장 표본 기준입니다. 전수는 아닙니다.',
           '툴은 curl 로 API 전부 검증했지만 브라우저 실제 조작은 서버에 GUI 가 없어 못 했습니다. 곽동신이 크롬으로 한 번 눌러 봐야 합니다.'])


d.pagebreak(); d.h1('9. 후속 (같은 날 오후) — 딥시크 판정 · 남은 일 3건 · 순위 민감도 분석')
d.h2('9-1. 딥시크(deepseek-v4-pro) 판정과 Fable 검증')
d.p('사용자 지시로 딥시크에게 이 과정 전체를 판정시키고(게이트웨이 120초 제한은 `gateway-ask` 스트리밍으로 해결) 그 답을 파일·숫자로 검증했습니다. 결과는 `문서/260916_딥시크판정_Fable검증.md`.')
d.table(['딥시크 지적', '판정', '근거'], [
    ['A 후보 생성 모델(BackboneUNet×MiT-B2) 한 개라 그 조합에 유리', '**맞음(단, «무효 수준»은 과장)**', '§9-3 민감도 분석으로 정량화. 다른 계열(VWFormer×R50) 교차 확인 시 83개 중 54개(65.1%) 재현'],
    ['B 시험 풀 12장 «라벨 수정 = 오염»', '**오독**', '마스크는 무변경. 잘못 짝지어진 이미지 파일을 원래대로 되돌린 것(원본 주석과 Dice 1.000)'],
    ['C 중복은 지우지 말고 같은 fold 로', '갈림, 제거 유지', '같은 fold 에 두면 가중치 2배·이중 채점. 오탐 0 확인. 되돌리기는 `--keep-duplicates`'],
    ['D amodal/visible 혼재는 안 됨', '**맞음**', '결정 전엔 학습에 쓰지 않는 것이 맞음'],
    ['E 포도 송이 누락 84% 면 벤치마크 불가', '**방향 맞음**', '사람 전수 재라벨 또는 보조 데이터셋으로 격하'],
    ['F2 IoU 0.986 이면 왜 누락?', '**오독**', '0.986 은 «우리 마스크 vs 원본 COCO» = 변환 정확성'],
], [.36, .24, .40], size=8.5)
d.h2('9-2. 남은 일 3건 (Opus5 작업 → Opus5 재검증 → Fable 확정)')
d.bullets(['**분할표 v4** `260916_라벨링툴/final/round_d/protocol_splits_v4_reviewed/{grape,peach}.csv` — 포도 2,403행(v3 의 pool·fold 전부 불변, 경로만 새 폴더, 뺀 99장 제거), 복숭아 125행. 2차가 학습 코드(`ProtocolManifestDataset`)로 실제 로드까지 확인. **주의: 이 CSV 로 fold 를 다시 생성하면 안 됨**(group 열 때문에 GroupKFold 를 타 시드를 무시, 기존 배정과 21% 만 일치 — manifest 에 경고문)',
           '**`tools/prepare_certh_grape.py` 버그 수정**(백업 `_backup_260916_…`) — 주석 split 에 대응하는 폴더를 우선 탐색, 충돌 경고, manifest 에 zip 내부 경로 기록. 2차 검증: 2,502장 경로 비교에서 충돌 15 외 동일, train 30장·test 251장 재변환 화소 동일. `969` 는 재인코딩만 다른 사본이라 미교체(재생성 시 969 만 미세하게 달라짐을 README 에 명시)',
           '**논문 데이터 절 초안 3차 수정판** `round_d/260916_논문_데이터절_초안_3차수정판.md` — 작업자 초안의 사실 오류 5건 수정(사과 중복 131장 잔존인데 «전 데이터 중복 없음», 기준 불일치, 후보 체크포인트 배정 오기, «사람이 판정» 표현, **«추가분도 amodal»은 거짓 — 추가분은 모델 윤곽**. 사이클1 전수 재판정: 83개 중 54개만 «보이는 부분», 29개는 잎까지 덮음). `main.tex` 는 사람 확인 뒤 반영. 발견: main.tex 복숭아 개체 1,077 은 공개 json 실측 **977** (검수판 = 977+83 = **1,060**)'])
d.h2('9-3. 라벨 보강 전후 순위 민감도 분석 (딥시크 A 에 대한 답)')
d.p('파일럿0 체크포인트 16개(4쌍×4회전)로 **파일럿 시험 fold 만**(복숭아 25장·포도 500장, 봉인된 80% 풀은 한 장도 안 봄) 슬라이딩 추론 후, 원본 정답(a)과 검수판 정답(b)으로 각각 채점했습니다. 2차 검수자가 저장된 예측 2,100장에서 지표를 독립 재계산(최대 차 5e-7)하고 부트스트랩 1,000회를 돌렸으며, 3차가 1런(6장)을 GPU 로 재추론해 저장 예측과 화소 단위 동일함을 확인했습니다. 산출물 `260916_라벨링툴/final/sensitivity/`.')
d.table(['', '복숭아 (a) 원본', '복숭아 (b) 검수판', '포도 (a)', '포도 (b)'], [
    ['1위', 'BB-UNet×MiT-B2 0.870', 'BB-UNet×MiT-B2 0.887', 'BB-UNet×MiT-B2 0.924', 'BB-UNet×MiT-B2 0.923'],
    ['2위', 'VWFormer×MiT-B2 0.851', 'VWFormer×MiT-B2 0.865', 'VWFormer×MiT-B2 0.922', 'VWFormer×MiT-B2 0.922'],
    ['3위', 'BB-UNet×R50 0.844', 'BB-UNet×R50 0.852', 'VWFormer×R50 0.915', 'VWFormer×R50 0.914'],
    ['4위', 'VWFormer×R50 0.814', 'VWFormer×R50 0.820', 'BB-UNet×R50 0.912', 'BB-UNet×R50 0.911'],
    ['부트스트랩: 1위 바뀌는 비율', '0.3%', '0.0%', '3.9%', '7.0%'],
], [.28, .18, .18, .18, .18], size=8.5)
d.bullets(['**순위는 두 정답에서 동일**(복숭아·포도 모두). 포도 1–2위(0.0015)·3–4위(0.0029)는 사실상 동점이라 «MiT-B2 계열 > R50 계열» 까지만 말할 수 있음',
           '**편향은 실재하나 작음**: 후보 생성 모델의 초과 이득 = Dice +0.0077(25장, 95% CI [+0.0027, +0.0143]) / 바뀐 9장 한정 +0.0217. 1–2위 격차(0.0196→0.0227)의 약 40%',
           '**2차 발견: 검수판에 추가된 화소 40,734개는 9장 전부 후보 모델 예측에 100% 포함**(다른 모델의 추가영역 재현율 79% / 60% / 47%). 추가 화소를 «2개 이상 모델 합의»로 제한하면 초과 이득이 +0.0217 → +0.0133, «3개 이상»이면 +0.0014 로 사라짐. **편향의 94%가 단일 모델 단독 화소에서 발생**',
           '이득은 거의 전부 Precision 상승(0.833→0.915): «모델이 이미 잡고 있었는데 정답에 없어 오탐으로 깎이던 복숭아»가 정답이 된 것. Recall 은 거의 불변',
           '포도는 파일럿 시험 fold 에서 마스크 변경 0·제외 19장(중복)만 → 전 쌍 −0.0005~−0.0008 로 동일하게 내려감. 송이 누락 재라벨 뒤 재실행 필요(스크립트 약 7분)'])
d.warn('권고(3차): 추가한 83개의 **위치**는 사진으로 확인했지만 **윤곽**은 후보 모델 예측 그대로여서 그 모델에 유리한 성분이 남아 있습니다. 사람 확인 때 툴에서 윤곽을 다시 그리거나, 자동 보강을 계속 쓴다면 «2개 이상 모델이 동의한 화소만 채택» 규칙으로 바꾸는 것이 맞습니다. 현재 마스크는 그대로 두고 논문에 이 수치를 명시합니다.', head='3차 판단')

d.pagebreak(); d.h1('10. 자동 검수 사이클 1/5 결과 (2026-09-16 밤, 데이터셋 무결성)')
d.p('Opus5 작업 → 다른 Opus5 가 85개 추가 성분을 **전수** 재판정 → Fable 이 판정·실행. 파일 무결성(장수·md5·superset·교체 12장)은 전부 재현됐고, 아래가 바뀐 점입니다.')
d.table(['발견', '판정·조치'], [
    ['추가 성분 85개 전수 재판정: 깨끗한 과실 54 · 잎 우세 28 · 오검출 1(누런 잎 `t2-of15-04`) · 불명 2', '오검출 1 + 경계 번짐 1(`t3-13`) 제거 → **39장 83개**. 두 장은 원본 마스크로 복귀. 잎 우세·불명 성분이 있는 21장은 툴에 flag(윤곽 축소는 사람)'],
    ['포도 근접 중복 2쌍 추가(`163↔2047`, `60↔2030`, 해밍 11·상관 0.91, main↔pilot 누출)', 'main 쪽 `163`·`60` 을 manifest·분할표 v4 에서 제외(파일은 남김, `_to_delete_260916.txt`). 사용 장수 **2,403**'],
    ['분할표 v4 `peach.csv` 의 foreground_fraction 이 고치기 전 값', '검수판 마스크로 재계산(39행), review_action 동기화'],
    ['민감도 분석 재채점', '복귀 2장은 파일럿 시험 fold 에 없어 **수치 불변**(초과 이득 +0.0077, 순위 동일)'],
], [.45, .55], size=8.5)
d.warn('앞 장들의 숫자는 **사이클3 문서 대조(2026-09-16)에서 현행값으로 고쳤습니다** — 83개·39장·99장·사용 2,403장·flag 30(누락 의심 9 + 윤곽 조정 21). 사이클1 이전 값은 85개·41장·96장·2,406장·flag 8 이었습니다. 상세 판정표: `260916_라벨링툴/cycles/cycle_1/stage2/peach85_verdicts.csv`, 최종 보고 `cycles/cycle_1/stage3_final.md`, 문서 대조 `cycles/cycle_3/stage1_report.md`.', head='숫자 갱신')

d.pagebreak(); d.h1('11. 자동 검수 5회 결과 (2026-09-16 밤 ~ 09-17)')
d.p('«Opus 5 가 하고 → 다른 Opus 5 가 다시 보고 → Fable 5.1 이 판정» 을 한 사이클로 해서 다섯 번, 웹사이트는 따로 한 번 돌렸습니다. 사이클마다 1·2·3단계 보고서가 `260916_라벨링툴/cycles/cycle_N/` 에 남아 있습니다.')
d.table(['사이클', '무엇을 봤나', '결과 한 줄'], [
    ['1 · 데이터셋 무결성', '장수·md5·마스크·중복·분할표', '추가 성분 85개 전수 재판정 → **83개·39장**(오검출 1·경계 번짐 1 제거), 근접 중복 2쌍 더 찾아 제외'],
    ['2 · 라벨링 툴', '코드·동작·보안', '비밀번호 우회 62종 전부 차단, 22건 수정(되돌리기 자동 백업·수정 유실·README 데이터 루트 등)'],
    ['3 · 문서 일관성', '문서·PDF·사이트의 숫자', '옛 숫자 12곳을 현행으로, 원천에서 다시 센 «숫자 사전» 20항목 일치 확인'],
    ['4 · 재현성', '스크립트를 처음부터 다시 실행', '핵심 7종 바이트/화소 동일. 중복 판정이 **축소 보간법에 흔들림**을 발견 → 1쌍 더 제외'],
    ['5 · 총괄', '앞 4회 + 웹사이트의 남은 문제', '숫자를 원천에서 다시 계산해 전 문서 대조, 남은 문제를 «해결/미해결/사람 결정» 으로 분류'],
    ['웹사이트(별도)', '공개 사이트·깃허브 리포', '공개용/내부용 빌드 분리, 툴에 비밀번호 게이트, 공개는 교수님 승인 뒤 사람이 push'],
], [.2, .25, .55], size=8.5)
d.h2('11-1. 최종 숫자 (원천 파일에서 다시 계산, 2026-09-17)')
d.table(['항목', '값', '원천'], [
    ['복숭아 — 마스크 고친 사진 / 추가한 복숭아', '**39장 / 83개**', '`inspect/round3/peach_fix_log.csv`'],
    ['복숭아 — 개체 수', '공개 COCO 977 + 83 = **1,060**', '`peach-data/{train,val,test}.json`'],
    ['복숭아 — 사람이 볼 사진(flag)', '**30** (누락 의심 9 + 윤곽 조정 21)', '`data/peach/status.json`'],
    ['포도 — 쓰는 사진 / 뺀 사진', '**2,403 / 99** (중복 98 + 포도 아님 1)', '`grape/manifest.csv`, v4 `grape.csv`'],
    ['포도 — 송이(bunch) 수', '9,832 − 408 = **9,424**', 'CERTH 공개 주석 zip'],
    ['포도 — 사람이 볼 사진(flag)', '**72**', '`data/grape/status.json`'],
    ['근접 중복 그룹', '**94그룹 192장** (해밍≤10 의 91 + 해밍 11 의 3)', '`data/grape/duplicates.json`'],
    ['교차 확인(다른 계열 모델도 같은 자리)', '83개 중 54개 **65.1%** / 20% 이상 겹침 69개 **83.1%**', '`final/crosscheck/`'],
    ['라벨 보강의 후보 모델 편향(초과 이득)', '**+0.0077** Dice (순위 4쌍 전부 불변)', '`final/sensitivity/`'],
], [.36, .34, .3], size=8.5)
d.h2('11-2. 아직 안 끝난 것 5가지')
d.bullets([
    '**교수님 결정 4건** — 가려진 부분까지 칠할지(amodal) · 라벨 범위(낙과·뒷줄·미숙과) · 포도를 송이로 셀지 알로 셀지 · 블루베리 «없는 것» 이 어느 사진인지. 이게 정해져야 재라벨 규칙이 정해집니다.',
    '**사람이 툴에서 확인** — 복숭아 flag 30장·포도 flag 72장. 브라우저로 직접 눌러 본 적이 아직 없습니다(서버에 화면이 없어 AI 는 curl 로만 확인).',
    '**포도 송이 누락은 72장으로 안 끝납니다** — 표본에서 84% 가 누락이라, 규칙이 정해지면 2,403장 전수 재라벨이 필요합니다.',
    '**중복 임계값이 구현에 흔들립니다** — 사진을 줄이는 방법(BILINEAR/LANCZOS)에 따라 같은 쌍이 해밍 11 도 되고 9 도 됩니다. 논문에 축소 방법까지 적고 한계로 밝혀야 합니다.',
    '**공개(깃허브)는 교수님 승인 뒤** — 사이트·저장소에서 회의록·논문 약점·서버 주소·실명을 뺐지만, 공개 자체는 사람이 확인하고 직접 올립니다.',
])
d.note('사이클별 상세: `260916_라벨링툴/cycles/cycle_{1..5}/stage{1_report,2_review,3_final}.md` · 총괄 판정서 `kds0206/문서/260917_5회검수_최종판정.md`', head='어디를 보면 되나')

d.h1('부록. 파일 경로')
d.table(['무엇', '경로'], [
    ['새 데이터셋', '`/data/project/2026summer/kds0206/datasets_reviewed_260916/`'],
    ['작업 폴더(툴·검수·AI 제안 전부)', '`/data/project/2026summer/platform/work/kwak_dongsin/260916_라벨링툴/`'],
    ['지시서', '`260916_작업지시서.md`'],
    ['1차 검수 보고서', '`inspect/260916_데이터검수_보고서.md` + 갤러리 `inspect/gallery/<과일>/index.html`'],
    ['2차 검수 보고서·판정표', '`inspect/round2/260916_2차검수_보고서.md`, `round2_*.csv`'],
    ['3차 판정·수정 마스크', '`inspect/round3/round3_peach_accept.json`, `peach_masks_fixed/`, `peach_fix_figs/`'],
    ['포도 12장 교정', '`final/fix_grape_mismatch.py`, `final/grape_image_fix/`(fix_records.json, figs/, proposal_scores.csv)'],
    ['새 폴더 생성 스크립트', '`final/build_reviewed_dataset.py`'],
    ['AI 1차 제안', '`ai_pass/260916_AI1차제안_보고서.md`, `data/<과일>/proposals/`, `proposal_scores.csv`'],
    ['툴', '`app/` (README.md 에 사용법), 주소 http://<서버주소>:5111'],
    ['미팅 정리', '`platform/00_meetings/260915_랩미팅_정리.md`'],
    ['카톡 문구', '`kds0206/문서/260916_카톡_데이터검수_결과.txt`'],
    ['딥시크 판정·검증', '`kds0206/문서/260916_딥시크판정_Fable검증.md`, `~/.council/260916_데이터검수판정/`'],
    ['분할표 v4·스크립트 수정·논문 초안', '`final/round_d/` (`protocol_splits_v4_reviewed/`, `260916_논문_데이터절_초안_3차수정판.md`, `review2/`)'],
    ['민감도 분석', '`final/sensitivity/` (`260916_민감도분석_보고서.md`, `review2/260916_2차검수E_보고서.md`, `preds/`)'],
    ['이 PDF 생성기', '`kds0206/semantic-segmentation/tools/make_data_review_report_260916.py`'],
], [.3, .7], size=8.5)
d.save()
print('saved', OUT)
