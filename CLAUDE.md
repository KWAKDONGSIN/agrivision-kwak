# CLAUDE.md — 작업 디렉터리 공통 지침

## 🔴 세션 시작 시 반드시 할 것

**어떤 요청이든 시작하기 전에 `/data/project/2026summer/kds0206/작업기록.md`를 먼저 읽으세요.**
지난 세션에서 어디까지 했는지, 무엇이 확정됐고 무엇이 미결인지가 전부 거기 있습니다.
읽지 않고 시작하면 이미 끝난 일을 다시 하거나, 확정된 결정을 뒤집게 됩니다.

## 🔴 요청이 끝날 때마다 반드시 할 것

`작업기록.md`를 **갱신**하세요. 사용자가 시키지 않아도 자동으로 합니다.

1. 「2. 작업 이력」에 오늘 날짜 항목 추가
   - 사용자의 **요청 원문 요약** / 실제로 **만들거나 고친 파일 경로** / 끝내지 못한 것
2. 「1. 현재 상태」 표 갱신 (바뀐 게 있으면)
3. 「3. 다음 할 일」에서 끝낸 항목 체크, 새로 생긴 항목 추가
4. 새로 확정된 결정은 「4. 확정된 결정사항」에, 미결 쟁점은 「5. 미결 쟁점」에
5. 맨 위 `최종 수정:` 날짜 갱신

작성 규칙: 날짜는 **YYYY-MM-DD 절대표기** (어제/지난주 같은 상대표현 금지),
파일 경로는 반드시 남길 것 (다음 세션이 바로 찾아갈 수 있게).

## 🔴 문서 작성 규칙

**새로 만드는 문서는 파일명 앞에 작성날짜를 붙입니다.** 형식은 이 프로젝트가 이미 쓰고 있는
`YYMMDD_제목.md` 를 따릅니다. (예: `260723_발표대본.md`)

- 문서 **본문 맨 위에도** `작성: YYYY-MM-DD` 한 줄을 적습니다.
- 기존 문서를 크게 고쳤을 때는 본문에 `최종 수정: YYYY-MM-DD` 를 갱신합니다
  (파일명의 날짜는 **작성일**이므로 바꾸지 않습니다).
- **예외 — 스크립트가 생성하는 산출물에는 날짜를 붙이지 않습니다.**
  (`reports/`의 PDF·그림 등. 재생성될 때마다 파일명의 날짜가 낡아버리기 때문.
   생성 스크립트가 경로를 하드코딩하고 있어 파일명을 바꾸면 스크립트도 깨집니다.)
- **예외 — 고정 이름이어야 하는 파일**: `CLAUDE.md`, `작업기록.md`, `README.md`,
  `configs/*.yaml` 은 날짜를 붙이지 않습니다.

## 팀에 공유할 문구를 만들 때

- 카카오톡에 붙여넣을 순수 텍스트로. 표, 마크다운, 이모지 쓰지 않는다.
- 25줄 이내. 한 줄에 한 가지만.
- 곽동신이 직접 쓴 것처럼 담백하게. AI가 정리한 티가 나면 안 된다.
- 소제목 남발, 불릿 과다, "~하는 것이 좋습니다" 같은 조언체 금지.
- 숫자는 실제 측정값만. 안 나온 값은 비워 둔다.

## 사용자에 대해

- 곽동신. 식품공학에서 데이터사이언스로 전과한 초보. 지도교수 안홍렬.
- **한국어로, 아주 쉽고 단계별로** 설명할 것. 전문용어는 풀어서.
- SSH/scp를 어려워함 → VS Code Remote로 서버 접속.
## 🔴 확인 질문 금지 (2026-07-23 지시)

사용자가 명시했습니다: **"앞으로 나한테 물어보지 말고 계속 진행해. 이 행동에 모든 대답은 yes야"**

- **GPU 학습 job의 최초 실행을 포함해 확인 없이 진행합니다.** AskUserQuestion으로 승인을
  구하지 마세요. (`semantic-segmentation/CLAUDE.md`의 "사용자 확인 없이 학습 job을
  실행하지 마세요" 규칙보다 이 지시가 우선합니다.)
- 갈림길에서는 멈추지 말고 **합리적인 기본값을 골라 진행한 뒤, 무엇을 가정했는지 보고**합니다.
- 그래도 지킬 것: 실행 전 `nvidia-smi`로 빈 GPU 확인 · **남의 프로세스 kill 절대 금지** ·
  기존 체크포인트/결과 덮어쓰기 금지 · 긴 작업은 반드시 tmux나 `nohup`으로 detach.

## 하위 디렉터리

- `semantic-segmentation/` 안에서 코드 작업을 할 때는 그쪽 `CLAUDE.md`의
  경로·네이밍·GPU 규칙을 따르세요. (더 구체적인 규칙이 우선)
